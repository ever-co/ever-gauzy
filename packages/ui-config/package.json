{
	"name": "@gauzy/ui-config",
	"version": "0.1.0",
	"description": "A configuration library for the Gauzy framework, providing environment settings and configurations to be shared across various Angular applications within the Gauzy ecosystem.",
	"type": "module",
	"author": {
		"name": "Ever Co. LTD",
		"email": "ever@ever.co",
		"url": "https://ever.co"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/ever-co/ever-gauzy",
		"directory": "packages/ui-config"
	},
	"bugs": {
		"url": "https://github.com/ever-co/ever-gauzy/issues"
	},
	"homepage": "https://ever.co",
	"license": "AGPL-3.0",
	"private": true,
	"scripts": {
		"lib:build": "yarn nx build ui-config --configuration=development",
		"lib:build:prod": "yarn nx build ui-config --configuration=production",
		"lib:watch": "yarn nx build ui-config --watch --configuration=development",
		"generate-env": "cross-env ts-node ../../.scripts/configure.ts",
		"generate-env:prod": "cross-env NODE_ENV=production ts-node ../../.scripts/configure.ts --environment=prod",
		"generate-env:dev": "cross-env NODE_ENV=development ts-node ../../.scripts/configure.ts --environment=dev",
		"replace-env": "cross-env node scripts/replace-env.js",
		"replace-env:prod": "cross-env NODE_ENV=production node scripts/replace-env.js",
		"replace-env:dev": "cross-env NODE_ENV=development node scripts/replace-env.js",
		"setup-env": "yarn generate-env && yarn replace-env",
		"setup-env:prod": "yarn generate-env:prod && yarn replace-env:prod",
		"setup-env:dev": "yarn generate-env:dev && yarn replace-env:dev"
	},
	"peerDependencies": {
		"@angular/common": "21.0.7",
		"@angular/core": "21.0.7"
	},
	"dependencies": {
		"tslib": "^2.6.2"
	},
	"devDependencies": {
		"@types/jest": "30.0.0",
		"@types/node": "^24.10.0",
		"cross-env": "^10.1.0",
		"ts-node": "^10.9.2",
		"jest-preset-angular": "16.0.0"
	},
	"keywords": [
		"gauzy",
		"angular",
		"configuration",
		"environment",
		"settings",
		"library"
	],
	"engines": {
		"node": ">=22",
		"yarn": ">=1.22"
	},
	"sideEffects": false
}
