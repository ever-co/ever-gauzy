type Query {
  """
  The accounting templates of the caller's tenant, with the copies the platform ships that belong to no
  tenant and no organization.
  """
  accountingTemplates(filter: AccountingTemplateFilter, sort: [AccountingTemplateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): AccountingTemplateConnection!

  """
  One accounting template, or null when there is none.
  
  A template that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none. The delivered node route answers the same miss with a `400`,
  because its handler catches every failure broadly and states no message with it — a refusal raised
  *about* a miss rather than a fact a client can act on — and this surface answers the miss itself,
  which is the same fact in this protocol's vocabulary. The read is the delivered one, tenant and
  global copies included, so a template the platform ships is reachable here.
  """
  accountingTemplate(id: ID!): AccountingTemplate

  """How many accounting templates the caller's tenant holds."""
  accountingTemplateCount: Int

  """
  The template one type of record is rendered through, for one organization, in one language,
  resolved through the delivered fallback: the requested language for this tenant, the requested
  language globally, English for this tenant, then English globally. The first of the four that
  answers is the answer.
  
  All three members are required, because the delivered query DTO requires all three: it validates the
  type against the accounting template vocabulary and the language against the platform's own, and it
  accepts an organization identifier in place of the organization object the REST body may carry
  instead. `languageCode` is passed to the resolved read beside the language of the request, which is
  the pair the delivered route passes from its query member and its `language` header.
  
  The answer is nullable: a template the fallback chain could not reach any row for is answered as no
  template at all rather than as a fabricated empty one — the difference between "this record has no
  template" and "this record has an empty template" is one a caller about to render a document has to
  be able to see.
  """
  accountingTemplateFor(
    """
    Which record to resolve the template for: `invoice`, `estimate` or `receipt`.
    """
    templateType: String!

    """The language to resolve the template in."""
    languageCode: String!

    """The organization to resolve the template for."""
    organizationId: ID!
  ): AccountingTemplate

  """
  Converts a document — MJML or plain text — into the HTML a preview shows, with its placeholders
  rendered from sample values and from the organization the caller states.
  
  Nothing is written and nothing is stored: the delivered route converts and answers.
  """
  accountingTemplatePreview(
    """The document to convert."""
    data: String!

    """
    The organization the sample values are read from, as the delivered body states it. Omit and the preview renders without one.
    """
    organization: JSON
  ): AccountingTemplatePreview!

  """The activity log of the caller's tenant, newest first."""
  activityLogs(filter: ActivityLogFilter, sort: [ActivityLogSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ActivityLogConnection!
  addressRoles(addressId: ID!): [AddressRole!]!
  addressRoleDefault(role: AddressRoleEnum!, ownerAddressIds: [ID!]!): ID

  """The addresses of the caller's organization, newest first."""
  addresses(filter: AddressFilter, sort: [AddressSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): AddressConnection!

  """One address of the caller's organization, or null when there is none."""
  address(id: ID!): Address

  """The API call logs of the caller's tenant, most recent request first."""
  apiCallLogs(filter: ApiCallLogFilter, sort: [ApiCallLogSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ApiCallLogConnection!

  """
  The invitations of the caller's tenant, newest first. An appointment's invitees are this connection
  narrowed by `appointmentId`; an employee's invitations are the same connection narrowed by `employeeId`.
  """
  appointmentEmployees(filter: AppointmentEmployeeFilter, sort: [AppointmentEmployeeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): AppointmentEmployeeConnection!

  """One invitation of the caller's tenant, or null when there is none."""
  appointmentEmployee(id: ID!): AppointmentEmployee

  """How many invitations the caller's tenant holds."""
  appointmentEmployeeCount: Int

  """
  The approval policies of the caller's tenant, newest first.
  
  Narrowed by `approvalType: { nin: ["TIME_OFF", "EQUIPMENT_SHARING"] }` this is the read the
  delivered `GET /request-approval` route serves: the policies an approval request may be filed
  under.
  """
  approvalPolicies(filter: ApprovalPolicyFilter, sort: [ApprovalPolicySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ApprovalPolicyConnection!

  """
  One approval policy of the caller's tenant, or null when there is none.
  
  This field carries the edit permission, not the view permission its read siblings carry, because
  the delivered `GET /:id` route carries it: the route is inherited from the base controller without
  a permission of its own, so the controller's class-level edit permission is the one it runs under.
  Stating anything else here would widen GraphQL past REST on a route the controller decided.
  """
  approvalPolicy(id: ID!): ApprovalPolicy

  """
  How many approval policies the caller's tenant records. Carries the edit permission, for the reason
  `approvalPolicy` states: the inherited count route declares none of its own.
  """
  approvalPolicyCount: Int

  """
  The OAuth clients the caller's credential may see: its tenant's own rows, and — for a super
  administrator — the installation-wide rows beside them.
  """
  oauthClients(filter: OAuthClientFilter, sort: [OAuthClientSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OAuthClientConnection!

  """
  One OAuth client, or null when there is none the caller may see.
  
  A client that is not there — or that belongs to another tenant and is not installation-wide — answers
  `null` rather than a refusal: GraphQL has one answer for "no such row" on a field that may have none,
  and the REST route's `404` is that same fact stated in the other protocol's vocabulary. Which rows a
  caller may see is the delivered lookup's decision, taken from the credential, not an argument here.
  """
  oauthClient(id: ID!): OAuthClient

  """
  The slots of the caller's tenant, newest first.
  
  When the caller states no sort, the order is the platform's own: newest first, then the identifier,
  because the delivered read states no order of its own and a cursor walk needs a total one.
  """
  availabilitySlots(filter: AvailabilitySlotFilter, sort: [AvailabilitySlotSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): AvailabilitySlotConnection!

  """
  One slot of the caller's tenant, or null when there is none.
  
  A slot that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such row"
  on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  availabilitySlot(id: ID!): AvailabilitySlot

  """How many slots the caller's tenant holds."""
  availabilitySlotCount: Int

  """The messages the caller may read, newest publication first."""
  broadcasts(filter: BroadcastFilter, sort: [BroadcastSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): BroadcastConnection!

  """
  One message the caller may read, or null when there is none — or none they may read.
  """
  broadcast(id: ID!): Broadcast

  """
  How many messages the caller's tenant holds, before the visibility rule the reads apply.
  
  **The delivered count route is declared by the CRUD base and shadowed by this controller's own
  by-identifier read.** Nest registers a controller's own methods before the base class's, so the
  `GET /:id` this controller declares is matched before the inherited `GET /count` path — and a request
  to the count path is answered as a by-identifier read of a record whose identifier is the word
  `count`. This field mirrors the route the controller declares rather than reproducing that collision:
  it makes the same `countBy` call the handler makes, so a client asking how many messages exist is
  answered instead of refused, and the ordering is the controller's to resolve rather than something
  this surface can fix by staying silent about a capability the resource has.
  """
  broadcastCount: Int

  """
  The candidacies of the caller's tenant, in no order of their own: the delivered read applies none,
  so the connection applies the platform's — newest first, the identifier last so a cursor walk over
  them is stable.
  """
  candidates(filter: CandidateFilter, sort: [CandidateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateConnection!

  """One candidacy of the caller's tenant, or null when there is none."""
  candidate(id: ID!): Candidate

  """How many candidacies the caller's tenant holds."""
  candidateCount: Int

  """
  The documents filed under the candidacies of the caller's tenant. `candidateId` narrows them to one person's file.
  """
  candidateDocuments(filter: CandidateDocumentFilter, sort: [CandidateDocumentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateDocumentConnection!

  """One document, or null when there is none."""
  candidateDocument(id: ID!): CandidateDocument

  """How many documents the caller's tenant holds."""
  candidateDocumentCount: Int

  """
  The courses of study filed under the candidacies of the caller's tenant. `candidateId` narrows them to one person's file.
  """
  candidateEducations(filter: CandidateEducationFilter, sort: [CandidateEducationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateEducationConnection!

  """One course of study, or null when there is none."""
  candidateEducation(id: ID!): CandidateEducation

  """How many courses of study the caller's tenant holds."""
  candidateEducationCount: Int

  """
  The prior engagements filed under the candidacies of the caller's tenant. `candidateId` narrows them to one person's file.
  """
  candidateExperiences(filter: CandidateExperienceFilter, sort: [CandidateExperienceSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateExperienceConnection!

  """One prior engagement, or null when there is none."""
  candidateExperience(id: ID!): CandidateExperience

  """How many prior engagements the caller's tenant holds."""
  candidateExperienceCount: Int

  """
  The skills filed under the candidacies of the caller's tenant. `candidateId` narrows them to one person's file.
  """
  candidateSkills(filter: CandidateSkillFilter, sort: [CandidateSkillSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateSkillConnection!

  """One skill, or null when there is none."""
  candidateSkill(id: ID!): CandidateSkill

  """How many skills the caller's tenant holds."""
  candidateSkillCount: Int

  """
  The origins the caller's tenant records, which is a vocabulary rather than one person's file: the
  row a candidacy points at through its own `sourceId` is read from `candidateSource(id)`.
  """
  candidateSources(filter: CandidateSourceFilter, sort: [CandidateSourceSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateSourceConnection!

  """One origin, or null when there is none."""
  candidateSource(id: ID!): CandidateSource

  """How many origins the caller's tenant records."""
  candidateSourceCount: Int

  """
  The interviews of the caller's tenant. `candidateId` narrows them to the sittings of one candidacy.
  """
  candidateInterviews(filter: CandidateInterviewFilter, sort: [CandidateInterviewSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateInterviewConnection!

  """One interview, or null when there is none."""
  candidateInterview(id: ID!): CandidateInterview

  """How many interviews the caller's tenant holds."""
  candidateInterviewCount: Int

  """
  The panel seats of the caller's tenant. `interviewId` narrows them to the panel of one sitting.
  """
  candidateInterviewers(filter: CandidateInterviewerFilter, sort: [CandidateInterviewerSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateInterviewerConnection!

  """One panel seat, or null when there is none."""
  candidateInterviewer(id: ID!): CandidateInterviewer

  """How many panel seats the caller's tenant holds."""
  candidateInterviewerCount: Int

  """
  The technologies the panels assess. `interviewId` narrows them to the vocabulary of one sitting.
  """
  candidateTechnologies(filter: CandidateTechnologyFilter, sort: [CandidateTechnologySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateTechnologyConnection!

  """One technology, or null when there is none."""
  candidateTechnology(id: ID!): CandidateTechnology

  """How many technologies the caller's tenant records."""
  candidateTechnologyCount: Int

  """
  The personal qualities the panels assess. `interviewId` narrows them to the vocabulary of one sitting.
  """
  candidatePersonalQualities(filter: CandidatePersonalQualityFilter, sort: [CandidatePersonalQualitySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidatePersonalQualityConnection!

  """One personal quality, or null when there is none."""
  candidatePersonalQuality(id: ID!): CandidatePersonalQuality

  """How many personal qualities the caller's tenant records."""
  candidatePersonalQualityCount: Int

  """
  The verdicts of the caller's tenant. `interviewId` narrows them to one sitting and `candidateId` to one candidacy.
  """
  candidateFeedbacks(filter: CandidateFeedbackFilter, sort: [CandidateFeedbackSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateFeedbackConnection!

  """One verdict, or null when there is none."""
  candidateFeedback(id: ID!): CandidateFeedback

  """How many verdicts the caller's tenant holds."""
  candidateFeedbackCount: Int

  """
  The criterion ratings of the caller's tenant. `feedbackId` narrows them to the criteria of one verdict.
  """
  candidateCriterionsRatings(filter: CandidateCriterionsRatingFilter, sort: [CandidateCriterionsRatingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CandidateCriterionsRatingConnection!

  """One criterion rating, or null when there is none."""
  candidateCriterionsRating(id: ID!): CandidateCriterionsRating

  """How many criterion ratings the caller's tenant holds."""
  candidateCriterionsRatingCount: Int

  """
  The hostnames of the caller's organization, optionally of one channel, the canonical host first.
  """
  channelDomains(filter: ChannelDomainFilter, sort: [ChannelDomainSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ChannelDomainConnection!

  """One hostname of the caller's organization, or null when there is none."""
  channelDomain(id: ID!): ChannelDomain

  """The channels of the caller's organization, the default one first."""
  channels(filter: ChannelFilter, sort: [ChannelSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ChannelConnection!

  """One channel of the caller's organization, or null when there is none."""
  channel(id: ID!): Channel

  """The comments of the caller's tenant, newest first."""
  comments(filter: CommentFilter, sort: [CommentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CommentConnection!

  """One comment of the caller's tenant, or null when there is none."""
  comment(id: ID!): Comment

  """
  How many comments the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  commentCount: Int

  """
  The company-account memberships of the caller's organization, newest first.
  """
  contactBuyers(filter: ContactBuyerFilter, sort: [ContactBuyerSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ContactBuyerConnection!

  """
  The credentials of the caller's organization, newest first. No row of this connection carries a
  secret: the projection is applied before the connection is built, so the four members the table
  stores are not filterable, not sortable and not selectable here.
  """
  contactCredentials(filter: ContactCredentialFilter, sort: [ContactCredentialSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ContactCredentialConnection!

  """The groups of the caller's organization, newest first."""
  contactGroups(filter: ContactGroupFilter, sort: [ContactGroupSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ContactGroupConnection!

  """One group of the caller's organization, or null when there is none."""
  contactGroup(id: ID!): ContactGroup

  """The contacts of the caller's tenant, newest first."""
  contacts(filter: ContactFilter, sort: [ContactSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ContactConnection!

  """One contact of the caller's tenant, or null when there is none."""
  contact(id: ID!): Contact

  """
  How many contacts the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  on a party list has to keep apart. The delivered count route binds its query string to the store's
  own `where`, a shape no schema can state, so the field offers no narrowing of its own rather than
  an argument the resolver could not pass on.
  """
  contactCount: Int

  """The platform's country master, by ISO 3166-1 code."""
  countries(filter: CountryFilter, sort: [CountrySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CountryConnection!

  """The platform's currency master, by ISO 4217 code."""
  currencies(filter: CurrencyFilter, sort: [CurrencySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): CurrencyConnection!

  """
  The SMTP configurations of the caller's tenant, the installation-wide one included.
  """
  customSmtpSettings(filter: SmtpSettingFilter, sort: [SmtpSettingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): SmtpSettingConnection!

  """One SMTP configuration, or null when there is none."""
  customSmtpSetting(id: ID!): SmtpSetting

  """How many SMTP configurations the caller's tenant holds."""
  customSmtpSettingCount: Int

  """
  The configuration one organization's mail is sent through: its own when it has one, the
  installation-wide one otherwise. The answer's `id` says which of the two it is.
  """
  smtpSetting(
    """
    The organization to resolve the configuration for. Omit for the installation-wide one.
    """
    organizationId: ID
  ): SmtpSetting!

  """
  The widgets of the caller's tenant, in the order the dashboards place them.
  """
  dashboardWidgets(filter: DashboardWidgetFilter, sort: [DashboardWidgetSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): DashboardWidgetConnection!

  """
  One widget of the caller's tenant, or null when there is none.
  
  A placement that is not there answers `null` rather than a refusal: GraphQL has one answer for "no
  such row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  dashboardWidget(id: ID!): DashboardWidget

  """How many widgets the caller's tenant holds."""
  dashboardWidgetCount: Int

  """The dashboards of the caller's tenant, the default one first."""
  dashboards(filter: DashboardFilter, sort: [DashboardSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): DashboardConnection!

  """
  One dashboard of the caller's tenant, or null when there is none.
  
  A dashboard that is not there answers `null` rather than a refusal: GraphQL has one answer for "no
  such row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  dashboard(id: ID!): Dashboard

  """How many dashboards the caller's tenant holds."""
  dashboardCount: Int

  """The deals of the caller's tenant, newest first."""
  deals(filter: DealFilter, sort: [DealSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): DealConnection!

  """One deal of the caller's tenant, or null when there is none."""
  deal(id: ID!): Deal

  """
  How many deals the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero.
  """
  dealCount: Int

  """
  Whether an account with the given address exists on this installation.
  
  The answer is one boolean: which account it is, and anything about it, is not answered and not asked
  for — the delivered read counts matching rows rather than reading one.
  """
  emailCheck(
    """The address to check."""
    email: String!
  ): EmailCheckResult!

  """
  The messages the caller's organization has sent, newest first, as the ledger of what went out.
  
  The delivered read narrows by the organization and the tenant the request states and by nothing else,
  so the filters below are how a caller asks after one recipient, one subject or one outcome — which is
  also how one message is read back, since this resource serves no node route.
  """
  emailHistories(filter: EmailHistoryFilter, sort: [EmailHistorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmailHistoryConnection!

  """
  The message parts of the caller's tenant, with the organization-less copies the whole tenant falls
  back to, in the order of the address they are read by.
  """
  emailTemplates(filter: EmailTemplateFilter, sort: [EmailTemplateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmailTemplateConnection!

  """
  One message part of the caller's tenant, or null when there is none.
  
  The criterion is the delivered node route's own, and it is narrower than the list's: the row is read
  under the caller's tenant, so a copy that belongs to no tenant — one the connection above answers —
  has no node read at all. That asymmetry is the controller's, and resolving it on one surface only is
  exactly what the two-protocol rule forbids; a caller that means such a copy states it as a filter.
  """
  emailTemplate(id: ID!): EmailTemplate

  """
  How many message parts the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports on
  a message catalogue has to keep apart. The delivered count route binds its query string to the
  store's own `where`, a shape no schema can state, so the field offers no narrowing of its own rather
  than an argument the resolver could not pass on; what it counts is the rows stamped with the caller's
  tenant, which is a smaller set than the connection's total — see the note above.
  """
  emailTemplateCount: Int

  """
  The subject text and the body source that will be used for one template, in one language, for one
  organization — resolved through the delivered fallback: the organization's own copy of each part
  first, then the tenant-wide copy, then the English copy of either.
  
  The organization is required rather than optional. The delivered reader's first lookup names the
  organization and the tenant together, and a lookup that named no organization would be answered with
  the first matching copy of any organization — so the fallback to the tenant-wide copy is what this
  field answers when the organization has no copy of its own, not what an omitted argument would
  select.
  
  `languageCode` defaults to the language the request names, exactly as the delivered route's `language`
  header does, which is itself English when the request names none.
  """
  emailTemplateContent(
    """The template to resolve, without the `/subject` or `/html` surname."""
    name: String!

    """
    The language to resolve it in. Defaults to the language of the request.
    """
    languageCode: String

    """The organization to resolve it for."""
    organizationId: ID!
  ): EmailTemplateContent!

  """
  Converts a document — MJML, Handlebars, or plain text — into the HTML a preview shows, with the
  placeholders rendered from sample values.
  
  Nothing is written and nothing is stored: the delivered route dispatches a query, and the conversion
  is answered rather than kept.
  """
  emailTemplatePreview(
    """The document to convert."""
    data: String!
  ): EmailTemplatePreview!

  """The appointments of the caller's tenant, newest first."""
  employeeAppointments(filter: EmployeeAppointmentFilter, sort: [EmployeeAppointmentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeAppointmentConnection!

  """One appointment of the caller's tenant, or null when there is none."""
  employeeAppointment(id: ID!): EmployeeAppointment

  """How many appointments the caller's tenant has."""
  employeeAppointmentCount: Int

  """
  Signs an appointment's identifier: the token an invitation is answered with, exactly as
  `GET /sign/:id` produces it. The answer is text rather than a row — it is the credential the
  invitation carries, not a projection of the appointment — so it is a field of its own.
  """
  signEmployeeAppointment(id: ID!): String!

  """
  The appointment an invitation token names, or null when the token names none — which is the answer
  for a token that does not decode and for one whose payload carries no appointment identifier. The
  decode route is the same capability stated for a client that holds the token and not the row.
  """
  decodeEmployeeAppointment(token: String!): ID

  """
  The availability rows of the caller's tenant, newest first.
  
  When the caller states no sort, the order is the platform's own: newest first, then the identifier,
  because the delivered read states no order of its own and a cursor walk needs a total one.
  """
  employeeAvailabilities(filter: EmployeeAvailabilityFilter, sort: [EmployeeAvailabilitySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeAvailabilityConnection!

  """
  One availability row of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  employeeAvailability(id: ID!): EmployeeAvailability

  """How many availability rows the caller's tenant holds."""
  employeeAvailabilityCount: Int

  """The awards of the caller's tenant, newest first."""
  employeeAwards(filter: EmployeeAwardFilter, sort: [EmployeeAwardSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeAwardConnection!

  """One award of the caller's tenant, or null when there is none."""
  employeeAward(id: ID!): EmployeeAward

  """
  How many awards the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  employeeAwardCount: Int

  """The levels of the caller's tenant, newest first."""
  employeeLevels(filter: EmployeeLevelFilter, sort: [EmployeeLevelSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeLevelConnection!

  """One level of the caller's tenant, or null when there is none."""
  employeeLevel(id: ID!): EmployeeLevel

  """
  How many levels the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  employeeLevelCount: Int

  """The notification settings of the caller's tenant, newest first."""
  employeeNotificationSettings(filter: EmployeeNotificationSettingFilter, sort: [EmployeeNotificationSettingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeNotificationSettingConnection!

  """
  One notification setting of the caller's tenant, or null when there is none.
  """
  employeeNotificationSetting(id: ID!): EmployeeNotificationSetting

  """How many notification settings the caller's tenant holds."""
  employeeNotificationSettingCount: Int

  """The notifications of the caller's tenant, newest first."""
  employeeNotifications(filter: EmployeeNotificationFilter, sort: [EmployeeNotificationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeNotificationConnection!

  """One notification of the caller's tenant, or null when there is none."""
  employeeNotification(id: ID!): EmployeeNotification

  """How many notifications the caller's tenant has."""
  employeeNotificationCount: Int

  """
  The recent visits of the caller's own employee record, most recent first, as the delivered read
  answers them.
  
  `totalCount` is the count of the rows that read answered rather than the count of every visit the
  store holds: the read answers a bounded set, and the connection's filter, order and page are applied
  over exactly that set.
  """
  employeeRecentVisits(filter: EmployeeRecentVisitFilter, sort: [EmployeeRecentVisitSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): EmployeeRecentVisitConnection!

  """
  The standing costs of the caller's organization, newest arrangement first.
  """
  employeeRecurringExpenses(filter: EmployeeRecurringExpenseFilter, sort: [EmployeeRecurringExpenseSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeRecurringExpenseConnection!

  """
  One standing cost of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such row"
  on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  employeeRecurringExpense(id: ID!): EmployeeRecurringExpense

  """How many standing costs the caller's tenant holds."""
  employeeRecurringExpenseCount: Int

  """
  What moving one arrangement's beginning to the stated instant would mean, and the arrangements in the
  way.
  
  The delivered read compares the instant with the arrangement's own beginning and, when the change
  would move it forward or backward, reads the sibling arrangements of the same parent that fall inside
  the gap. A change the delivered edit refuses is answered here as the verdict that names it rather than
  raised, because this field is the question the edit asks itself before it decides what to write.
  """
  employeeRecurringExpenseStartDateUpdateType(
    """The arrangement whose beginning would move."""
    recurringExpenseId: ID!

    """The instant the beginning would move to."""
    newStartDate: DateTime!
  ): EmployeeRecurringExpenseStartDateUpdateType!

  """The settings of the caller's tenant, newest first."""
  employeeSettings(filter: EmployeeSettingFilter, sort: [EmployeeSettingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeSettingConnection!

  """One setting of the caller's tenant, or null when there is none."""
  employeeSetting(id: ID!): EmployeeSetting

  """How many settings the caller's tenant holds."""
  employeeSettingCount: Int

  """
  What one organization's employees earned, spent and are owed over a range, as a total, per employee
  and per day of the range.
  """
  employeeStatisticsAggregate(
    """
    The organization whose roster is aggregated. The delivered read resolves the roster through it and has no default for it.
    """
    organizationId: ID!

    """
    The start of the range. The aggregate computes its periods from the two dates.
    """
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime
  ): EmployeeStatisticsAggregate!

  """
  One employee's income, expense, profit and bonus, one entry per month of the delivered twelve-month
  walk.
  """
  employeeStatisticsSeries(
    """The employee whose series is read."""
    employeeId: ID!

    """
    The date the walk is anchored to. Omit it and the delivered read answers the walk from the current month.
    """
    valueDate: DateTime
  ): EmployeeStatisticsSeries!

  """One employee's statistics, one row per month of a range, newest first."""
  employeeMonthlyStatistics(
    """The employee whose months are read."""
    employeeId: ID!

    """
    The start of the range. Omit it and the delivered handler defaults it to the start of the current month.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered handler defaults it to the end of the current month.
    """
    endDate: DateTime

    """
    The organization the incomes and expenses are narrowed to, where the caller states one.
    """
    organizationId: ID
  ): [EmployeeMonthlyStatistic!]!

  """
  The individual income and expense lines behind an employee's totals, selected by source.
  
  `type` is one of the six the delivered handler switches on — `INCOME`, `BONUS_INCOME`,
  `NON_BONUS_INCOME`, `EXPENSES`, `EXPENSES_WITHOUT_SALARY` or `PROFIT` — and a value the handler does
  not recognise is answered with an empty list, which is the delivered behaviour rather than a refusal
  invented here.
  """
  employeeStatisticsHistory(
    """The employee whose lines are read."""
    employeeId: ID!

    """Which of the sources the lines are drawn from."""
    type: String!

    """
    The start of the range. Omit it and the delivered handler defaults it to the start of the current month.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered handler defaults it to the end of the current month.
    """
    endDate: DateTime

    """
    The organization the lines are narrowed to, where the caller states one.
    """
    organizationId: ID
  ): [EmployeeStatisticsHistoryEntry!]!

  """
  The engagements of the caller's tenant, in no order of their own: the delivered read applies none,
  so the connection applies the platform's — newest first, the identifier last so a cursor walk over
  them is stable.
  """
  employees(filter: EmployeeFilter, sort: [EmployeeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EmployeeConnection!

  """
  One engagement of the caller's tenant, or null when there is none — and the caller's own engagement
  whatever identifier it names, unless it may change the selected employee, which is the delivered
  route's own scope.
  """
  employee(id: ID!): Employee

  """How many engagements the caller's tenant holds."""
  employeeCount: Int

  """
  The engagements of one organization that count as working over a range: started by the range's end
  and not ended before its range began. Narrowed to the caller's own engagement unless it may select
  an employee, which is the delivered read's own scope rather than a permission stated here.
  """
  workingEmployees(
    """
    The organization whose roster is read. The delivered read has no default for it.
    """
    organizationId: ID!

    """
    The end of the range. Omit it and the read answers every live engagement of the organization.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the read answers every live engagement of the organization.
    """
    endDate: DateTime
    filter: WorkingEmployeeFilter
    sort: [WorkingEmployeeSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): EmployeeConnection!

  """
  How many engagements of one organization count as working over a range. The delivered count route
  requires the organization and reads the range from the same input the list does, so the field
  states both; see the note above on why this count is not argument-less.
  """
  workingEmployeeCount(organizationId: ID!, startDate: DateTime, endDate: DateTime): Int

  """
  One organization's live roster, projected to the columns a picker needs. Narrowed by a team or a
  project when the caller names one, which is a join the delivered read performs and the connection
  cannot: the rows it returned do not carry either membership.
  """
  employeeMembers(
    """The organization whose roster is read."""
    organizationId: ID!

    """The team to narrow the roster to. Omit it for the whole organization."""
    organizationTeamId: ID

    """
    The project to narrow the roster to. Omit it for the whole organization.
    """
    organizationProjectId: ID
    filter: EmployeeMemberFilter
    sort: [EmployeeMemberSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): EmployeeConnection!

  """The subscriptions of the caller's tenant, newest first."""
  entitySubscriptions(filter: EntitySubscriptionFilter, sort: [EntitySubscriptionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EntitySubscriptionConnection!

  """One subscription of the caller's tenant, or null when there is none."""
  entitySubscription(id: ID!): EntitySubscription

  """
  How many subscriptions the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  entitySubscriptionCount: Int

  """The sharing policies of the caller's organization, in name order."""
  equipmentSharingPolicies(filter: EquipmentSharingPolicyFilter, sort: [EquipmentSharingPolicySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EquipmentSharingPolicyConnection!

  """One policy of the caller's tenant, or null when there is none."""
  equipmentSharingPolicy(id: ID!): EquipmentSharingPolicy

  """How many policies the caller's tenant holds."""
  equipmentSharingPolicyCount: Int

  """The sharing periods of the caller's tenant."""
  equipmentSharings(filter: EquipmentSharingFilter, sort: [EquipmentSharingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EquipmentSharingConnection!

  """One sharing period of the caller's tenant, or null when there is none."""
  equipmentSharing(id: ID!): EquipmentSharing

  """How many sharing periods the caller's tenant holds."""
  equipmentSharingCount: Int

  """
  The sharing periods of one organization, with the employees and the teams each one is handed to.
  
  **A root field of its own rather than a filter on `equipmentSharings`, and the reason is the read
  rather than a preference.** The delivered route's reader joins the employee and team pivots and
  inner-joins the asset, none of which the list read joins, so its rows carry memberships the connection
  cannot answer and leave out the periods whose asset row is gone. Folding it in would mean answering
  rows without the memberships the route exists to answer.
  
  The organization is the path segment of the delivered route, and the same connection protocol is
  applied to the rows it returns, so a caller pages and narrows it exactly as it pages the list above.
  """
  equipmentSharingsByOrganization(organizationId: ID!, filter: EquipmentSharingFilter, sort: [EquipmentSharingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): EquipmentSharingConnection!

  """
  The sharing periods one employee filed, with the employees and the teams each one is handed to.
  
  **A root field of its own for the two reasons above, and one more.** The delivered read joins both
  pivots, and it narrows on `createdByUserId` — the user who filed the request — rather than on the
  employee pivot the path segment's name suggests. That is a fact about the period's author, not about
  who holds the asset, and no member of `EquipmentSharingFilter` states it: a filter on the row's own
  employee would answer a different set of periods under a name that reads like this one.
  """
  equipmentSharingsByEmployee(employeeId: ID!, filter: EquipmentSharingFilter, sort: [EquipmentSharingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): EquipmentSharingConnection!

  """The equipment of the caller's tenant, in name order."""
  equipments(filter: EquipmentFilter, sort: [EquipmentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EquipmentConnection!

  """One asset of the caller's tenant, or null when there is none."""
  equipment(id: ID!): Equipment

  """How many assets the caller's tenant tracks."""
  equipmentCount: Int

  """
  The invitation one token names, verified against the installation's own signing secret and answered
  only while it is still valid.
  
  The address is required beside the token because the delivered read verifies the token's claims
  against the row it finds, address included: a token presented with a different address matches no
  row. `relations` is the delivered query's own member and states which of the two relations the answer
  should carry, because the read joins only what it is asked for.
  """
  estimateEmailValidation(
    """
    The address the invitation was sent to. Verified against the token's own claim.
    """
    email: String!

    """The signed token the invitation carried."""
    token: String!

    """
    The relations to join onto the answer. Omit for none, which is the delivered default.
    """
    relations: [EstimateEmailRelation!]
  ): EstimateEmail!

  """
  The outbox rows of the caller's tenant, the row whose turn comes first at the head.
  
  Read this to answer what is waiting and what has been waiting longest — the two questions the
  backlog runbook asks — and narrow it by `status`, `eventName` or `aggregateId` rather than by
  reading the whole queue.
  """
  eventOutbox(filter: EventOutboxRecordFilter, sort: [EventOutboxRecordSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): EventOutboxRecordConnection!

  """
  One outbox row of the caller's tenant, or null when there is none.
  
  The row carries the event's body and the last dispatch error. The delivery records of that row are
  the `eventDeliveries` connection narrowed by this row's `eventId`, which is what the REST detail
  read's `expand=deliveries` attaches and what a delivery record names: a record outlives the event
  row, so the event id — not the row's primary key — is the key that survives.
  """
  eventOutboxRecord(id: ID!): EventOutboxRecord

  """
  The delivery records of the caller's tenant, newest first.
  
  One record per `(event, consumer)` pair, which is the ledger that makes at-least-once dispatch
  safe: a record is written before the consumer is invoked and completed after it returns, so a
  consumer that already acknowledged an event has a `PUBLISHED` row and a consumer that crashed has
  a `PENDING` one. Narrow it by `status` and `consumerKey` to answer which consumer stopped applying
  events, or by `eventId` to read one event's whole fan-out.
  """
  eventDeliveries(filter: EventDeliveryFilter, sort: [EventDeliverySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): EventDeliveryConnection!

  """
  One delivery record of the caller's tenant, or null when there is none.
  """
  eventDelivery(id: ID!): EventDelivery

  """The meeting lengths of the caller's tenant, in title order."""
  eventTypes(filter: EventTypeFilter, sort: [EventTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): EventTypeConnection!

  """One meeting length of the caller's tenant, or null when there is none."""
  eventType(id: ID!): EventType

  """How many meeting lengths the caller's tenant offers."""
  eventTypeCount: Int

  """
  The categories of the caller's tenant, in the vocabulary's own name order.
  """
  expenseCategories(filter: ExpenseCategoryFilter, sort: [ExpenseCategorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ExpenseCategoryConnection!

  """
  One category of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  expenseCategory(id: ID!): ExpenseCategory

  """How many categories the caller's tenant holds."""
  expenseCategoryCount: Int

  """The expenses of the caller's tenant, newest cost first."""
  expenses(filter: ExpenseFilter, sort: [ExpenseSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ExpenseConnection!

  """
  One expense of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  expense(id: ID!): Expense

  """How many expenses the caller's tenant holds."""
  expenseCount: Int

  """
  The expenses of one employee, including the organization's split expenses.
  
  The employee is the path segment of the delivered route. `filterDate` is the date whose month
  narrows the read, which is the value that route reads out of its `data` parameter; omit it and the
  read answers every month, as the route does when the parameter carries none.
  """
  splitExpensesByEmployee(employeeId: ID!, filterDate: DateTime): [ExpenseSplitRow!]!

  """
  The caller's own expenses, including the organization's split expenses.
  
  The employee is resolved from the credential, as the delivered route resolves it, so a caller
  cannot ask this question about anyone else. `filterDate` narrows the read to one month, as it does
  above.
  """
  mySplitExpenses(filterDate: DateTime): [ExpenseSplitRow!]!

  """
  The import ledger of the caller's tenant, newest import first: one entry per archive the platform
  took in, with the outcome recorded on it.
  """
  importHistories(filter: ImportHistoryFilter, sort: [ImportHistorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ImportHistoryConnection!

  """The markers of the caller's tenant, newest first."""
  favorites(filter: FavoriteFilter, sort: [FavoriteSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): FavoriteConnection!

  """One marker of the caller's tenant, or null when there is none."""
  favorite(id: ID!): Favorite

  """
  How many markers the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  favoriteCount: Int

  """
  The markers of the caller's OWN employee record, newest first.
  
  This is a root field of its own rather than a filter on `favorites` because the delivered read
  resolves the employee from the credential and overwrites the criterion's employee with it: the
  narrowing is by who is asking, which no condition on the rows can state, and the field therefore
  takes no employee argument — naming one would be an argument the read ignores and a reader would
  reasonably read as honoured.
  
  The arguments are the connection protocol's own, because the delivered route binds the same query
  DTO the list route binds and this surface has no query string to hand it: `filter` narrows the rows
  the read returned, `sort`/`page` walk them, and the total is the size of the set the filters
  selected. A caller whose credential carries no employee identity is answered by the read itself —
  that read falls back to the employee the request stated, and this field states none — so the
  refusal such a caller meets is the delivered read's own and not a set this surface invents.
  """
  favoritesByEmployee(filter: FavoriteFilter, sort: [FavoriteSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): FavoriteConnection!

  """
  The catalogue of capabilities of this installation, in the order the delivered parent route fixes
  for the same rows.
  """
  features(filter: FeatureFilter, sort: [FeatureSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): FeatureConnection!

  """
  The caller's tenant's toggle rows: which capability is on, for which scope. A tenant-wide row and
  an organization's own row are both answered, and `organizationId` is what tells them apart.
  """
  featureToggles(filter: FeatureToggleFilter, sort: [FeatureToggleSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): FeatureToggleConnection!

  """
  The toggle definitions this installation's provider declares for the platform's own codes, as the
  public definition route answers them.
  """
  featureToggleDefinitions: [FeatureToggleDefinition!]!

  """
  The policies of the caller's tenant, newest first. An organization's own policy is this connection
  narrowed by `organizationId`.
  """
  goalGeneralSettings(filter: GoalGeneralSettingFilter, sort: [GoalGeneralSettingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): GoalGeneralSettingConnection!

  """One policy of the caller's tenant, or null when there is none."""
  goalGeneralSetting(id: ID!): GoalGeneralSetting

  """How many policies the caller's tenant holds."""
  goalGeneralSettingCount: Int

  """The catalogue entries of the caller's tenant, newest first."""
  goalKpiTemplates(filter: GoalKPITemplateFilter, sort: [GoalKPITemplateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): GoalKPITemplateConnection!

  """
  One catalogue entry of the caller's tenant, or null when there is none.
  """
  goalKpiTemplate(id: ID!): GoalKPITemplate

  """How many catalogue entries the caller's tenant holds."""
  goalKpiTemplateCount: Int

  """The measures of the caller's tenant, newest first."""
  goalKpis(filter: GoalKPIFilter, sort: [GoalKPISort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): GoalKPIConnection!

  """One measure of the caller's tenant, or null when there is none."""
  goalKpi(id: ID!): GoalKPI

  """How many measures the caller's tenant holds."""
  goalKpiCount: Int

  """The catalogue entries of the caller's tenant, newest first."""
  goalTemplates(filter: GoalTemplateFilter, sort: [GoalTemplateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): GoalTemplateConnection!

  """
  One catalogue entry of the caller's tenant, or null when there is none.
  """
  goalTemplate(id: ID!): GoalTemplate

  """How many catalogue entries the caller's tenant holds."""
  goalTemplateCount: Int

  """
  The periods of the caller's tenant, in the taxonomy's own order. One period by its name is this
  connection narrowed by `name`, which is the read the delivered by-name route performs.
  """
  goalTimeFrames(filter: GoalTimeFrameFilter, sort: [GoalTimeFrameSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): GoalTimeFrameConnection!

  """
  One period of the caller's tenant, or null when there is none.
  
  This is the capability the CRUD base's `GET /:id` route declares. On the delivered router that path
  is answered by the controller's own by-name read instead, for the ordering reason stated above; the
  capability is unchanged, so it is stated here under its own name.
  """
  goalTimeFrame(id: ID!): GoalTimeFrame

  """How many periods the caller's tenant holds."""
  goalTimeFrameCount: Int

  """
  The objectives of the caller's tenant, oldest first, which is the order the delivered list route
  reads them in.
  """
  goals(filter: GoalFilter, sort: [GoalSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): GoalConnection!

  """One objective of the caller's tenant, or null when there is none."""
  goal(id: ID!): Goal

  """How many objectives the caller's tenant holds."""
  goalCount: Int

  """
  The stored keys of the caller's tenant and organization, newest first.
  
  This is the read an operator arrives at when an integration is stuck: it answers what a client is
  holding, how far the first attempt got, and what that attempt produced. Narrow it by `scope` and
  `key` rather than by reading the table — a key row is found by the two values the stuck client can
  state, which is exactly what the table's unique index is built on.
  """
  idempotencyKeys(filter: IdempotencyKeyFilter, sort: [IdempotencyKeySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): IdempotencyKeyConnection!

  """
  One stored key of the caller's tenant and organization, or null when there is none.
  
  A key belonging to another tenant or another organization answers `null` rather than a refusal,
  because the read is scoped from the credential: a caller is never told that a key it may not read
  exists. That is the REST route's `404` stated in this protocol's vocabulary.
  
  The stored response is not a member of what this field answers — see `IdempotencyKey` for why.
  """
  idempotencyKey(id: ID!): IdempotencyKey

  """The images of the caller's tenant, newest first."""
  imageAssets(filter: ImageAssetFilter, sort: [ImageAssetSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ImageAssetConnection!

  """One image of the caller's tenant, or null when there is none."""
  imageAsset(id: ID!): ImageAsset

  """How many images the caller's tenant holds."""
  imageAssetCount: Int

  """The incomes of the caller's tenant, newest first."""
  incomes(filter: IncomeFilter, sort: [IncomeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): IncomeConnection!

  """
  One income of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such row"
  on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  income(id: ID!): Income

  """How many incomes the caller's tenant holds."""
  incomeCount: Int

  """
  The caller's own incomes.
  
  The employee is resolved from the credential, as the delivered route resolves it, so a caller cannot
  ask this question about anyone else — and one with no employee row is refused rather than answered an
  empty list, which is the delivered read's own behaviour. `filterDate` is the date whose month narrows
  the read, which is the value the route reads out of its `data` parameter; omit it and the read answers
  every month.
  """
  myIncomes(filterDate: DateTime): [Income!]!

  """
  The synchronisation decisions of one configured integration, with the tied rows the delivered read
  loads beside them.
  """
  integrationEntitySettings(
    """
    The configured integration whose decisions are read. The delivered route takes it in the path.
    """
    integrationId: ID!
    filter: IntegrationEntitySettingFilter
    sort: [IntegrationEntitySettingSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): IntegrationEntitySettingConnection!

  """
  The integrations the caller's tenant has connected, in the resource's own selection: a row with no
  catalogue entry is not a connection.
  """
  integrationTenants(filter: IntegrationTenantFilter, sort: [IntegrationTenantSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): IntegrationTenantConnection!

  """One connected integration, or null when there is none."""
  integrationTenant(
    id: ID!

    """
    The relations to load with the row, as the delivered node route takes them from its query
    string. `integration` and `settings` are the two the type carries.
    """
    relations: [String!]
  ): IntegrationTenant

  """How many connections the caller's tenant holds."""
  integrationTenantCount: Int

  """
  The connection for one provider and organization, most recently updated first, or null when the
  tenant has none. This is the read a caller makes before it synchronises.
  """
  integrationTenantByOptions(
    """The provider to resolve, in the contract's own provider vocabulary."""
    name: String

    """
    The organization the connection belongs to. Omit for the tenant-wide one.
    """
    organizationId: ID

    """
    The relations to load with the row, as the delivered route takes them from its query string.
    """
    relations: [String!]
  ): IntegrationTenant

  """
  The integration catalogue: what this installation can be connected to, in the order the catalogue
  places its entries.
  
  `integrationTypeId`, `searchQuery` and `isPaid` are the delivered read's own narrowing, in the
  vocabulary its own filter object uses; `filter`, `sort` and the page narrow and order the rows that
  read returned.
  """
  integrations(
    """
    The facet to read the catalogue through. The delivered read requires one.
    """
    integrationTypeId: ID

    """
    The name prefix the delivered read matches against, case-insensitively. This is the term the
    read hands the store rather than a filter applied afterwards, which is why it is an argument.
    """
    searchQuery: String

    """
    Read only the entries that require a paid provider plan (`true`) or only those that do not
    (`false`). The delivered read narrows only when this is stated, which is what its own
    `filter` member means.
    """
    isPaid: Boolean
    filter: IntegrationFilter
    sort: [IntegrationSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): IntegrationConnection!

  """The facets of the catalogue, in the order the catalogue places them."""
  integrationTypes(filter: IntegrationTypeFilter, sort: [IntegrationTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): IntegrationTypeConnection!

  """
  The invitations of the caller's tenant, newest first.
  
  The same service method the delivered list route calls, with the route's own defaults for an
  unstated request: the route binds its query DTO to the query string — a `where`, the relations to
  join, the page and the soft-delete flag — and this surface has no query string to bind, so the read
  runs with none of them and the connection protocol's `filter` is applied to the rows the service
  returns. The tenant is added to the criterion by the service, from the credential rather than from
  the caller, so a filter naming another tenant narrows to nothing rather than reading across tenants.
  """
  invites(filter: InviteFilter, sort: [InviteSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): InviteConnection!

  """
  The invitations waiting for the caller, newest first.
  
  **A field of its own rather than a filter on the register**, because the delivered read is narrowed
  by who is asking rather than by anything about the rows: it resolves the caller's own address out of
  the credential and selects that address's pending, unexpired rows — the ones whose state is still
  `INVITED`, whose instant has not passed or is absent, and which are neither inactive nor archived.
  No filter over the register can state "addressed to me", and the register is scoped to the tenant
  rather than to a person, so a caller that needs its own queue cannot reach it by narrowing.
  
  Its rows carry fewer members than the connection's, and that is the delivered read's own projection:
  it selects the identifier, the expiry and the teams, so every other member of `Invite` is absent on
  these nodes. A client that needs a member this read does not select reads the row through
  `invites`, which answers the register's columns.
  """
  myInvites(filter: InviteFilter, sort: [InviteSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): InviteConnection!

  """
  Presents the token a message's link carried.
  
  The same query the delivered `GET /validate` route dispatches, with the route's own two members: the
  address and the token. The read verifies the token's own signature against the platform's secret and
  then matches the invitation the verified payload names, so a token that was not issued for that
  address is refused rather than matched against a row.
  
  A miss is a refusal rather than an empty answer: the delivered reader answers an invitation that is
  not there, has lapsed, or has already been answered with a bare `400`, and this field lets that
  refusal through rather than turning it into a null. "No such invitation" and "the token was wrong"
  are one answer on purpose: telling them apart would confirm which tokens are live. The field is
  nullable only because a query may have no row, never because this read answers one.
  """
  inviteByToken(email: String!, token: String!): Invite

  """
  Presents the code that was mailed to an address.
  
  **A query, although the delivered route is a `POST`.** The verb belongs to the transport: the code
  travels in a body because a body is where a `POST` carries it, not because anything is written. The
  delivered read matches the pending, unexpired invitation of that address by its code and answers it,
  and this field says so in the vocabulary of a read.
  
  A miss is a refusal for the same reason it is on the token above, and with the same deliberate
  indistinguishability.
  """
  inviteByCode(email: String!, code: String!): Invite

  """The log entries of the caller's tenant, newest first."""
  invoiceEstimateHistories(filter: InvoiceEstimateHistoryFilter, sort: [InvoiceEstimateHistorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): InvoiceEstimateHistoryConnection!

  """
  One log entry, or null when there is none.
  
  An entry that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  invoiceEstimateHistory(id: ID!): InvoiceEstimateHistory

  """How many log entries the caller's tenant holds."""
  invoiceEstimateHistoryCount: Int

  """The billed lines of the caller's tenant, newest first."""
  invoiceItems(filter: InvoiceItemFilter, sort: [InvoiceItemSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): InvoiceItemConnection!

  """
  One line of the caller's tenant, or null when there is none.
  
  A line that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  invoiceItem(id: ID!): InvoiceItem

  """How many lines the caller's tenant holds."""
  invoiceItemCount: Int

  """The invoices and estimates of the caller's tenant, newest first."""
  invoices(filter: InvoiceFilter, sort: [InvoiceSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): InvoiceConnection!

  """
  One invoice or estimate of the caller's tenant, or null when there is none.
  
  A document that is not there answers `null` rather than a refusal: GraphQL has one answer for "no
  such row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  invoice(id: ID!): Invoice

  """How many invoices and estimates the caller's tenant holds."""
  invoiceCount: Int

  """
  The highest number the caller's tenant's series has reached, or zero when it has issued none.
  """
  highestInvoiceNumber: InvoiceNumberSeries!

  """
  The document as a PDF, for the caller's own language.
  
  The language is the request's: the delivered route reads it from the `language` header and this
  field reads the same header off the same request, so one document is rendered in one language over
  either protocol. Null when the delivered renderer produced nothing — it logs a failure rather than
  raising it, and the route answers an empty body in that case.
  """
  downloadInvoicePdf(id: ID!): InvoiceDocument

  """
  The document's payment record as a PDF, for the caller's own language, on the same terms as the
  document render above.
  """
  downloadInvoicePaymentPdf(id: ID!): InvoiceDocument

  """
  The catalogue entries of the caller's tenant, newest first. One goal template's key results are
  this connection narrowed by `goalId`.
  """
  keyResultTemplates(filter: KeyResultTemplateFilter, sort: [KeyResultTemplateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): KeyResultTemplateConnection!

  """
  One catalogue entry of the caller's tenant, or null when there is none.
  """
  keyResultTemplate(id: ID!): KeyResultTemplate

  """How many catalogue entries the caller's tenant holds."""
  keyResultTemplateCount: Int

  """
  The updates of the caller's tenant, newest first. One key result's updates are this connection
  narrowed by `keyResultId`, which is the read the delivered by-key-result route performs.
  """
  keyResultUpdates(filter: KeyResultUpdateFilter, sort: [KeyResultUpdateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): KeyResultUpdateConnection!

  """
  One update of the caller's tenant, or null when there is none.
  
  This is the capability the CRUD base's `GET /:id` route declares. On the delivered router that path
  is answered by the controller's own by-key-result read instead, for the ordering reason stated above;
  the capability is unchanged, so it is stated here under its own name.
  """
  keyResultUpdate(id: ID!): KeyResultUpdate

  """How many updates the caller's tenant holds."""
  keyResultUpdateCount: Int

  """
  The key results of the caller's tenant, newest first. An objective's key results are this
  connection narrowed by `goalId`; one employee's are the same connection narrowed by `ownerId`.
  """
  keyResults(filter: KeyResultFilter, sort: [KeyResultSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): KeyResultConnection!

  """
  One key result of the caller's tenant, or null when there is none.
  
  This read is the controller's own rather than the CRUD base's, and it is a field of its own rather
  than the connection narrowed: its reader loads the updates that hang off the row and the goal, lead
  and owner it points at, none of which the list read joins. The members those relations would answer
  are the identifiers the row always holds, and a key result's updates are read through
  `keyResultUpdates`.
  """
  keyResult(id: ID!): KeyResult

  """How many key results the caller's tenant holds."""
  keyResultCount: Int

  """The languages the platform knows."""
  languages(filter: LanguageFilter, sort: [LanguageSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): LanguageConnection!
  unitCategories: [UnitCategory!]!
  unitCategory(id: ID!): UnitCategory
  units(categoryId: ID): [Unit!]!
  unit(id: ID!): Unit

  """The mentions of the caller's tenant, newest first."""
  mentions(filter: MentionFilter, sort: [MentionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): MentionConnection!

  """One mention of the caller's tenant, or null when there is none."""
  mention(id: ID!): Mention

  """
  How many mentions the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  mentionCount: Int

  """The stores of the caller's tenant, newest first."""
  merchants(filter: MerchantFilter, sort: [MerchantSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): MerchantConnection!

  """One store of the caller's tenant, or null when there is none."""
  merchant(id: ID!): Merchant

  """How many stores the caller's tenant holds."""
  merchantCount: Int

  """The official holidays of the caller's tenant, earliest first."""
  officialHolidays(filter: OfficialHolidayFilter, sort: [OfficialHolidaySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OfficialHolidayConnection!

  """
  One official holiday of the caller's tenant, or null when there is none.
  """
  officialHoliday(id: ID!): OfficialHoliday

  """The operations of the caller's tenant, newest first."""
  operations(filter: OperationFilter, sort: [OperationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OperationConnection!

  """
  One operation of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  operation(id: ID!): Operation

  """
  The operations of one aggregate, newest first.
  
  At most one of them is live at a time — the runtime's exclusivity rule — so what this usually
  answers is the run in flight followed by the attempts that came before it, which is what an
  operator investigating an aggregate reads.
  
  **A connection rather than a bare array**, and for the same reason `operations` is one: an
  aggregate's history grows with every attempt, every retry and every compensation the runtime runs,
  so the client that has to render it needs the count and a page rather than an unbounded list. The
  two members that name the aggregate stay the identity of the read; everything else is the same
  narrowing `operations` takes, over the same rows.
  """
  operationsByAggregate(aggregateType: String!, aggregateId: ID!, filter: OperationFilter, sort: [OperationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OperationConnection!

  """The awards of the caller's tenant, newest first."""
  organizationAwards(filter: OrganizationAwardFilter, sort: [OrganizationAwardSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationAwardConnection!

  """One award of the caller's tenant, or null when there is none."""
  organizationAward(id: ID!): OrganizationAward

  """How many awards the caller's tenant records."""
  organizationAwardCount: Int

  """The parties of the caller's organization, newest first."""
  organizationContacts(filter: OrganizationContactFilter, sort: [OrganizationContactSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OrganizationContactConnection!

  """One party of the caller's organization, or null when there is none."""
  organizationContact(id: ID!): OrganizationContact

  """
  The parties one employee works, as the delivered route answers them.
  
  The answer is a projection rather than the whole row — the identifier, the name and the avatar URL,
  which is what the route selects — so every other member of the type is null here, and the caller
  that needs one reads the party by its identifier. `contactType` narrows the set the way the route's
  own query parameter does, and `organizationId` is required because the route's query DTO requires
  it and the read is scoped by it.
  """
  organizationContactsByEmployee(employeeId: ID!, organizationId: ID!, contactType: ContactType): [OrganizationContact!]!

  """
  How many parties the caller's organization holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  on a customer base has to keep apart. The delivered count route binds its query string to the
  store's own `where`, a shape no schema can state, so the field offers no narrowing of its own
  rather than an argument the resolver could not pass on.
  """
  organizationContactCount: Int

  """The departments of the caller's tenant, in name order."""
  organizationDepartments(filter: OrganizationDepartmentFilter, sort: [OrganizationDepartmentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationDepartmentConnection!

  """One department of the caller's tenant, or null when there is none."""
  organizationDepartment(id: ID!): OrganizationDepartment

  """
  How many departments the caller's tenant has.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  on a headcount has to keep apart. The delivered count route binds its query string to the store's
  own `where`, a shape no schema can state, so the field offers no narrowing of its own rather than
  an argument the resolver could not pass on.
  """
  organizationDepartmentCount: Int

  """
  The departments one employee is a member of, as the delivered route answers them.
  
  The route states the employee and nothing else — no filter, no order and no page of its own — so
  the field states no argument beyond the employee. The answer is the connection: it is built over
  the rows the read returns, ordered by the resource's own default, and presented under the query
  protocol's own page, with the total the read selected. The read joins the department-member pivot
  the list read does not, which is why this capability is a root field of its own rather than a
  `members` filter on `organizationDepartments`.
  """
  organizationDepartmentsByEmployee(employeeId: ID!): OrganizationDepartmentConnection!

  """The documents of the caller's tenant, newest first."""
  organizationDocuments(filter: OrganizationDocumentFilter, sort: [OrganizationDocumentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationDocumentConnection!

  """One document of the caller's tenant, or null when there is none."""
  organizationDocument(id: ID!): OrganizationDocument

  """How many documents the caller's tenant keeps."""
  organizationDocumentCount: Int

  """The employment types of the caller's tenant, newest first."""
  organizationEmploymentTypes(filter: OrganizationEmploymentTypeFilter, sort: [OrganizationEmploymentTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationEmploymentTypeConnection!

  """
  One employment type of the caller's tenant, or null when there is none.
  """
  organizationEmploymentType(id: ID!): OrganizationEmploymentType

  """How many employment types the caller's tenant keeps."""
  organizationEmploymentTypeCount: Int

  """The languages of the caller's tenant, newest first."""
  organizationLanguages(filter: OrganizationLanguageFilter, sort: [OrganizationLanguageSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationLanguageConnection!

  """One language row of the caller's tenant, or null when there is none."""
  organizationLanguage(id: ID!): OrganizationLanguage

  """How many language rows the caller's tenant keeps."""
  organizationLanguageCount: Int

  """The positions of the caller's tenant, newest first."""
  organizationPositions(filter: OrganizationPositionFilter, sort: [OrganizationPositionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationPositionConnection!

  """One position of the caller's tenant, or null when there is none."""
  organizationPosition(id: ID!): OrganizationPosition

  """How many positions the caller's tenant keeps."""
  organizationPositionCount: Int

  """The project modules of the caller's tenant, newest first."""
  organizationProjectModules(filter: OrganizationProjectModuleFilter, sort: [OrganizationProjectModuleSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationProjectModuleConnection!

  """
  One project module of the caller's tenant, or null when there is none.
  
  The parent module of a module is this field with the child's own `parentId`.
  """
  organizationProjectModule(id: ID!): OrganizationProjectModule

  """
  How many modules the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports on
  a plan has to keep apart. The delivered count route binds its query string to the store's own `where`,
  a shape no schema can state, so the field offers no narrowing of its own rather than an argument the
  resolver could not pass on.
  """
  organizationProjectModuleCount: Int

  """
  The modules the caller is a member of, read through the delivered reader that resolves the employee
  from the credential — a narrowing no filter can state, because a filter narrows rows and this narrows
  by who is asking.
  
  `totalCount` is the count of the rows this read answered rather than of every module in the store.
  """
  employeeProjectModules(filter: OrganizationProjectModuleFilter, sort: [OrganizationProjectModuleSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationProjectModuleConnection!

  """
  The modules the caller's own teams share, read through the delivered reader that joins the team
  membership pivot the list read does not.
  
  `totalCount` is the count of the rows this read answered rather than of every module in the store.
  """
  teamProjectModules(filter: OrganizationProjectModuleFilter, sort: [OrganizationProjectModuleSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationProjectModuleConnection!

  """
  The modules one employee works, read through the delivered reader that joins the module-member pivot
  by the employee this field names.
  
  The organization is stated because the delivered read is scoped by it: the route's own query DTO
  requires it, and the read narrows the rows on that column rather than falling back to a scope of its
  own — a field that omitted it would answer a criterion of `undefined`, which is not a scope any
  caller asked for.
  
  `totalCount` is the count of the rows this read answered rather than of every module in the store.
  """
  organizationProjectModulesByEmployee(employeeId: ID!, organizationId: ID!, filter: OrganizationProjectModuleFilter, sort: [OrganizationProjectModuleSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OrganizationProjectModuleConnection!

  """
  The projects of the caller's organization, in the order the list reads them.
  
  `GET /organization-projects/pagination` has no field of its own: it is the same rows under the
  same filters, and the page it slices by `take`/`skip` is what this connection's `limit` and
  `offset` state.
  """
  organizationProjects(filter: OrganizationProjectFilter, sort: [OrganizationProjectSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationProjectConnection!

  """
  One project of the caller's organization, or null when there is none.
  
  The read behind this field also records the caller's recent visit to the project, which is what
  the delivered route does: reading one project is the same operation on both surfaces, side effect
  included, rather than a second read that would leave the caller's history disagreeing with the
  protocol it was built over.
  """
  organizationProject(id: ID!): OrganizationProject

  """
  How many projects the caller's organization holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds
  its query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  organizationProjectCount: Int

  """
  The projects one employee works, as the delivered route answers them.
  
  Two things make this a root field of its own rather than a filter on the connection. The read
  behind it **inner-joins the project-member pivot**, which the list read does not join, so the
  employees a project is worked by are not a member of the rows the connection carries and a filter
  on them could not be evaluated. And it **answers a projection of the row** rather than the row: it
  selects the identifier, the name, the avatar URL, the currency, the billing basis, the public flag,
  the owner basis and the task list type, and joins the project's teams beside them. Every other
  member of the type is therefore absent on these answers, and a caller that needs one reads the
  project by its identifier.
  
  `organizationId` is required because the route's query DTO requires it and the read is scoped by
  it; `organizationContactId` and `organizationTeamId` narrow the set the way the same DTO's members
  do, and a caller that states neither gets every project the employee works in the organization.
  """
  organizationProjectsByEmployee(employeeId: ID!, organizationId: ID!, organizationContactId: ID, organizationTeamId: ID): [OrganizationProject!]!

  """
  The projects linked to a repository, as the delivered read answers them.
  
  **This is a root field of its own rather than a filter on the connection.** The read narrows on a
  repository custom field and joins the custom-field relation, and it reads only the rows whose
  repository link is set: neither is a member of a row this connection carries, so no filter could
  state the question — and narrowing the connection by `organizationId` would answer a different set
  of rows rather than these.
  
  **The read answers a bounded page of its own.** It takes ten rows unless the caller states
  otherwise, and its `skip` is a one-based page number rather than an offset, so an unstated request
  is the first ten linked projects in the store's own order. The connection then applies `filter`,
  `sort` and its page **over the rows that read returned**, which is why `totalCount` here is the
  count of those rows rather than of every linked project in the store: the total is the size of the
  set the filters selected, and a number that pretended to count beyond the set the delivered read
  answers would be a second, disagreeing count of the same question.
  """
  syncedOrganizationProjects(filter: OrganizationProjectFilter, sort: [OrganizationProjectSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OrganizationProjectConnection!

  """
  Whether one employee manages one project.
  
  A question rather than a row, so it is a field of its own: the answer is read from the
  project-member pivot, and it is what the delivered route refuses or admits the caller by.
  """
  isOrganizationProjectManager(projectId: ID!, employeeId: ID!): Boolean!

  """
  The standing costs of the caller's organization, newest arrangement first.
  """
  organizationRecurringExpenses(filter: OrganizationRecurringExpenseFilter, sort: [OrganizationRecurringExpenseSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationRecurringExpenseConnection!

  """
  One standing cost of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such row"
  on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  organizationRecurringExpense(id: ID!): OrganizationRecurringExpense

  """How many standing costs the caller's tenant holds."""
  organizationRecurringExpenseCount: Int

  """
  What moving one arrangement's beginning to the stated instant would mean, and the arrangements in the
  way.
  
  The delivered read compares the instant with the arrangement's own beginning and, when the change
  would move it forward or backward, reads the sibling arrangements of the same parent that fall inside
  the gap. A change the delivered edit refuses is answered here as the verdict that names it rather than
  raised, because this field is the question the edit asks itself before it decides what to write.
  """
  organizationRecurringExpenseStartDateUpdateType(
    """The arrangement whose beginning would move."""
    recurringExpenseId: ID!

    """The instant the beginning would move to."""
    newStartDate: DateTime!
  ): OrganizationRecurringExpenseStartDateUpdateType!

  """
  The organization's shared arrangements as one employee's share of each, for one month.
  
  The delivered read selects the shared arrangements in force in the month, counts the people the
  organization employs, and divides each arrangement's amount by that count — so the quotient is a
  computation over a range rather than a stored amount, and the row it is answered on carries the whole
  it came from beside it. The month is the year and the calendar month's zero-based position, which is
  what the delivered read passes to its date constructor; both are required because the read builds one
  instant from the two and measures every arrangement against it.
  """
  organizationRecurringExpenseShares(
    """
    The organization whose shared arrangements are read. It must be the caller's own.
    """
    organizationId: ID!

    """The four-digit year the month belongs to."""
    year: Int!

    """The month, zero-based: January is `0`."""
    month: Int!
  ): [OrganizationRecurringExpenseShare!]!

  """The sprints of the caller's tenant, newest first."""
  organizationSprints(filter: OrganizationSprintFilter, sort: [OrganizationSprintSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationSprintConnection!

  """One sprint of the caller's tenant, or null when there is none."""
  organizationSprint(id: ID!): OrganizationSprint

  """How many sprints the caller's tenant holds."""
  organizationSprintCount: Int

  """The strategic initiatives the caller may see, newest first."""
  organizationStrategicInitiatives(filter: OrganizationStrategicInitiativeFilter, sort: [OrganizationStrategicInitiativeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationStrategicInitiativeConnection!

  """
  One strategic initiative, or null when there is none — or when the caller may not see this one. The
  delivered read refuses an initiative outside the caller's visibility scope with the same miss it
  answers for a row that is not there, which is what this field reproduces.
  """
  organizationStrategicInitiative(id: ID!): OrganizationStrategicInitiative

  """
  The strategic initiatives aligned to one project, as the delivered route answers them: a bare list
  with no page, narrowed by the same visibility rule as every other read here.
  """
  organizationStrategicInitiativesByProject(projectId: ID!): [OrganizationStrategicInitiative!]!

  """How many strategic initiatives the caller's tenant holds."""
  organizationStrategicInitiativeCount: Int

  """
  The task settings the delivered read resolves for one organization: the row scoped to the
  organization itself, or null when none was ever filed.
  """
  organizationTaskSetting(organizationId: ID!): OrganizationTaskSetting

  """
  The join requests of the caller's tenant, newest first.
  
  The same service method the delivered list route calls, with the route's own defaults for an
  unstated request: the route binds its query DTO to the query string — a `where`, the relations to
  join, the page and the soft-delete flag — and this surface has no query string to bind, so the read
  runs with none of them and the connection protocol's `filter` is applied to the rows the service
  returns. The tenant is applied to the criterion by the service, from the credential rather than from
  the caller, so a filter naming another tenant narrows to nothing rather than reading across tenants.
  """
  organizationTeamJoinRequests(filter: OrganizationTeamJoinRequestFilter, sort: [OrganizationTeamJoinRequestSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationTeamJoinRequestConnection!

  """
  The teams of the caller's tenant, in the order the delivered list read answers them.
  """
  organizationTeams(filter: OrganizationTeamFilter, sort: [OrganizationTeamSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationTeamConnection!

  """
  The teams of the caller, read through the delivered reader that resolves them from the credential.
  
  A root field of its own because its narrowing is a fact about the caller and not about the row: the
  delivered read answers a caller that may not choose whose teams are read the teams its own employee
  is an active member of. No argument names an employee here, so a caller cannot read anyone else's
  teams through this field.
  """
  myOrganizationTeams(filter: OrganizationTeamFilter, sort: [OrganizationTeamSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): OrganizationTeamConnection!

  """
  One team of the caller's tenant, or null when there is none.
  
  The read behind it is the delivered statistics read, dispatched as the same query the `GET /:id`
  route dispatches, and the field states no arguments of its own — so the route's own defaults apply
  and nothing is joined beyond the relation the entity always carries.
  """
  organizationTeam(id: ID!): OrganizationTeam

  """
  How many teams the caller's tenant has.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds
  its query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  organizationTeamCount: Int

  """The suppliers of the caller's tenant, in the master's own name order."""
  organizationVendors(filter: OrganizationVendorFilter, sort: [OrganizationVendorSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationVendorConnection!

  """
  One supplier of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such row"
  on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  organizationVendor(id: ID!): OrganizationVendor

  """How many suppliers the caller's tenant holds."""
  organizationVendorCount: Int

  """
  The organizations of the caller's tenant, in the order the organization list reads them in.
  """
  organizations(filter: OrganizationFilter, sort: [OrganizationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): OrganizationConnection!

  """One organization of the caller's tenant, or null when there is none."""
  organization(id: ID!): Organization

  """How many organizations the caller's tenant has."""
  organizationCount: Int
  paymentTerms: [PaymentTerm!]!
  paymentTerm(id: ID!): PaymentTerm
  paymentTermSchedule(id: ID!, total: String!, basisDate: String!, currencyDecimals: Int, currency: String): PaymentTermSchedule!

  """The payments of the caller's tenant, newest movement first."""
  payments(filter: PaymentFilter, sort: [PaymentSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): PaymentConnection!

  """
  One payment of the caller's tenant, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the other
  protocol's vocabulary.
  """
  payment(id: ID!): Payment

  """How many payments the caller's tenant holds."""
  paymentCount: Int

  """The payroll runs of one organization, newest pay period first."""
  payrollRuns(
    """
    The organization whose runs are read. Required, and checked against the caller's memberships.
    """
    organizationId: ID!
    filter: PayrollRunFilter
    sort: [PayrollRunSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): PayrollRunConnection!

  """
  One payroll run with its lines, or null when there is none.
  
  A run that is not there — or that belongs to another organization — answers `null` rather than a
  refusal: GraphQL has one answer for "no such row" on a field that may have none, and the REST route's
  `404` is that same fact stated in the other protocol's vocabulary. The lookup is scoped by the
  organization the caller names and by the caller's own tenant, which is the delivered read's own rule.
  """
  payrollRun(
    """The run to read."""
    id: ID!

    """The organization the run belongs to."""
    organizationId: ID!
  ): PayrollRun

  """
  Total pay across every paid run of one organization, grouped by currency.
  
  Only paid runs are counted: a draft or an approved run is a plan rather than money that has left, and
  the delivered read selects on the paid status alone. The answer is one entry per currency, and the
  currency is on each entry rather than stated once, because one organization can pay in more than one.
  """
  payrollRunStatistics(
    """The organization to report on."""
    organizationId: ID!
  ): [PayrollStatistics!]!

  """
  What each employee earned, was deducted and takes home in one payroll run.
  
  The breakdown is computed from the run's own lines rather than read from a table: there is no summary
  row to page, which is why the answer is a list tied to one run rather than a connection.
  """
  payrollRunSummary(
    """The run to break down."""
    id: ID!

    """The organization the run belongs to."""
    organizationId: ID!
  ): [PayrollSummary!]!

  """The pipelines of the caller's tenant, newest first."""
  pipelines(filter: PipelineFilter, sort: [PipelineSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): PipelineConnection!

  """One pipeline of the caller's tenant, or null when there is none."""
  pipeline(id: ID!): Pipeline

  """
  How many pipelines the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero.
  """
  pipelineCount: Int

  """
  The deals whose stage belongs to one pipeline, in the board's own stage order.
  
  A pipeline that is not there, or one of another tenant, answers an empty page rather than a
  refusal: that is what the delivered read does, because it catches the failure and answers an empty
  result set — and turning that into an error here would tell a caller something the REST route does
  not.
  """
  pipelineDeals(pipelineId: ID!, filter: DealFilter, sort: [DealSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): DealConnection!

  """
  The product categories of the caller's tenant, in the taxonomy's own sibling order.
  """
  productCategories(filter: ProductCategoryFilter, sort: [ProductCategorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ProductCategoryConnection!

  """One category of the caller's tenant, or null when there is none."""
  productCategory(id: ID!): ProductCategory

  """How many categories the caller's tenant has."""
  productCategoryCount: Int

  """The options of the caller's tenant."""
  productOptions(filter: ProductOptionFilter, sort: [ProductOptionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ProductOptionConnection!

  """One option of the caller's tenant, or null when there is none."""
  productOption(id: ID!): ProductOption

  """
  How many options the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  inventory has to keep apart. The delivered count route binds its query string to the store's own
  `where`, a shape no schema can state, so the field offers no narrowing of its own rather than an
  argument the resolver could not pass on.
  """
  productOptionCount: Int

  """The settings of the caller's tenant."""
  productVariantSettings(filter: ProductVariantSettingFilter, sort: [ProductVariantSettingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ProductVariantSettingConnection!

  """One setting, or null when there is none."""
  productVariantSetting(id: ID!): ProductVariantSetting

  """
  How many settings the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  inventory has to keep apart. The delivered count route binds its query string to the store's own
  `where`, a shape no schema can state, so the field offers no narrowing of its own rather than an
  argument the resolver could not pass on.
  """
  productVariantSettingCount: Int

  """The product types of the caller's tenant, newest first."""
  productTypes(filter: ProductTypeFilter, sort: [ProductTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ProductTypeConnection!

  """One product type of the caller's tenant, or null when there is none."""
  productType(id: ID!): ProductType

  """How many product types the caller's tenant has."""
  productTypeCount: Int

  """The prices of the caller's tenant."""
  productVariantPrices(filter: ProductVariantPriceFilter, sort: [ProductVariantPriceSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ProductVariantPriceConnection!

  """One price of the caller's tenant, or null when there is none."""
  productVariantPrice(id: ID!): ProductVariantPrice

  """
  How many prices the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  inventory has to keep apart. The delivered count route binds its query string to the store's own
  `where`, a shape no schema can state, so the field offers no narrowing of its own rather than an
  argument the resolver could not pass on.
  """
  productVariantPriceCount: Int

  """
  The variants of the caller's tenant, newest first.
  
  The variants of one product are these rows narrowed to that product, which is what the REST
  sub-route answers and what `filter: { productId: { eq: ... } }` states here.
  """
  productVariants(filter: ProductVariantFilter, sort: [ProductVariantSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): ProductVariantConnection!

  """One variant, or null when there is none."""
  productVariant(id: ID!): ProductVariant

  """
  How many variants the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  inventory has to keep apart. The delivered count route binds its query string to the store's own
  `where`, a shape no schema can state, so the field offers no narrowing of its own rather than an
  argument the resolver could not pass on. It is not the connection's `totalCount` either: that is
  the count of the rows the connection narrowed to, and this is the count of the caller's rows.
  """
  productVariantCount: Int

  """
  The products of the caller's tenant, newest first.
  
  `language` is the GraphQL spelling of the `language` request header the REST list route reads and
  of the `local/:langCode` path segment its per-language sibling carries. The rows are the same rows
  with the translation for that language merged onto them, which is what makes `name` and
  `description` readable on them, and a caller that states no language is answered in English —
  the same fallback the delivered routes apply.
  """
  products(filter: ProductFilter, sort: [ProductSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, language: String): ProductConnection!

  """
  One product, or null when there is none.
  
  `idOrSlug` names the row the way the delivered route names it: a value that is an identifier is
  read by identifier, and any other value is read by the product's slug. One argument carries both
  because they name one row of one resource, and which column a value addresses is a fact about the
  resource rather than about the request that carried it.
  
  There is deliberately no `productBySlug` beside this field. Parity is capability parity, and this
  capability is "read one product by the value that names it"; a second root field resolving the same
  row of the same route under a second name would be a second surface for one door, which is what the
  platform's own naming doctrine forbids. A client that holds a slug states it here.
  
  `language` selects which of the two delivered reads by id the request means: without it the field
  answers the row `GET /products/:idOrSlug` answers, and with it the row
  `GET /products/local/:langCode/:id` answers, which is that same row with its translation merged.
  """
  product(id: ID!, language: String): Product

  """
  How many products the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero — two facts a client that reports
  inventory has to keep apart. The delivered count route narrows by a `where` fragment carried in the
  query string, which is a shape no schema can state, so the field states no narrowing of its own
  rather than an argument it could not honour.
  """
  productCount: Int

  """The reactions of the caller's tenant, newest first."""
  reactions(filter: ReactionFilter, sort: [ReactionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ReactionConnection!

  """One reaction of the caller's tenant, or null when there is none."""
  reaction(id: ID!): Reaction

  """
  How many reactions the caller's tenant records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  reactionCount: Int

  """The regions of the caller's organization, newest first."""
  regions(filter: RegionFilter, sort: [RegionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): RegionConnection!

  """One region of the caller's organization, or null when there is none."""
  region(id: ID!): Region

  """
  The reports of the platform's catalogue, and whether the named organization's menu shows each one.
  
  Omit `organizationId` and every row answers `showInMenu: false`, which is the answer the delivered
  read gives the same query string. The reports the menu shows are this field narrowed to
  `filter: { showInMenu: { eq: true } }`, which is the delivered `GET /api/report/menu-items` route
  stated as a filter of the one list rather than as a second root field.
  """
  reports(
    """
    The organization whose menu is being read. Omitting it answers `showInMenu: false` throughout.
    """
    organizationId: ID
    filter: ReportFilter
    sort: [ReportSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): ReportConnection!

  """
  The headings of the report catalogue.
  
  The categories a report may be filed under, which is the other half of the menu a client assembles.
  """
  reportCategories(filter: ReportCategoryFilter, sort: [ReportCategorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ReportCategoryConnection!

  """
  The approval requests of the caller's organization, newest first.
  
  The same service method the delivered list route calls, with the route's own defaults for an
  unstated request: the route binds its `data` envelope to `relations` and `findInput`, and this
  surface has no envelope to bind, so the read runs with neither — no joined collection and no
  criterion — and the connection protocol's `filter` is applied to the rows the service returns. The
  tenant and the organization are applied to the criterion by the service, from the credential rather
  than from the caller, so a filter naming another organization narrows to nothing rather than
  reading across organizations.
  """
  requestApprovals(filter: RequestApprovalFilter, sort: [RequestApprovalSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): RequestApprovalConnection!

  """
  The requests one employee has to answer, newest first.
  
  **A field of its own rather than a filter on the register**, because the delivered read asks a
  different question: it resolves the employee, walks that employee's own approval collection through
  the pivot the list read does not join, and then loads each request it finds with the approval
  policy, the employee approvals, the team approvals and the tags — none of which the list read loads.
  The rows it answers are the requests the caller has raised, so the set is not the register narrowed
  by a column.
  
  The employee is named rather than assumed because the delivered route carries it in the path and
  reads the collection of that employee; a read of the caller's own queue is not what this route
  serves, and the service narrows the answer to the rows the caller created in the organization they
  are acting in.
  """
  requestApprovalsByEmployee(employeeId: ID!, filter: RequestApprovalFilter, sort: [RequestApprovalSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): RequestApprovalConnection!

  """
  One request, or null when there is none.
  
  **No permission is stated, and that is the parity rather than an omission.** The delivered
  `GET /:id` is inherited from the CRUD base without a permission of its own, so it runs under the
  controller's class chain and no permission; a permission here would be a scope the REST route does
  not have, and the asymmetry between reading one request and listing them is the controller's to
  resolve rather than this surface's to resolve on one protocol only.
  
  A request that is not there answers `null` rather than a refusal: GraphQL has one answer for "no
  such row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  requestApproval(id: ID!): RequestApproval

  """
  How many requests the caller's organization records.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  requestApprovalCount: Int

  """The links of the caller's tenant, newest first."""
  resourceLinks(filter: ResourceLinkFilter, sort: [ResourceLinkSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ResourceLinkConnection!

  """One link of the caller's tenant, or null when there is none."""
  resourceLink(id: ID!): ResourceLink

  """How many links the caller's tenant holds."""
  resourceLinkCount: Int

  """
  The role permissions the caller may read, in the catalogue's own order.
  
  The rows are the ones the delivered list reader answers for this caller — every row of the tenant
  for a super administrator, every row but the super administrator's own for a holder of
  `CHANGE_ROLES_PERMISSIONS`, and the acting role's own rows for anybody else — narrowed by `filter`
  and paged by the connection protocol. The permissions of one role are this same field narrowed by
  `roleId`, which is the narrowing the delivered route expresses through its own query string.
  """
  rolePermissions(filter: RolePermissionFilter, sort: [RolePermissionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): RolePermissionConnection!

  """
  One role permission of the caller's tenant, or null when there is none.
  """
  rolePermission(id: ID!): RolePermission

  """
  The permissions of the role the caller is acting in.
  
  This is the delivered `GET /role-permissions/me` read, and it is the one read of this resource that
  asks for no permission: the route that serves it states an empty permission set, and the field
  states the same one, so a caller that may not administer roles at all is still answered its own
  rows. The rows are narrowed by the delivered reader to the enabled ones — a permission this role
  does not carry is not answered here, which is what makes the answer usable as the list of what the
  credential may do.
  """
  myRolePermissions: [RolePermission!]!

  """
  How many role permissions the caller's tenant has.
  
  The delivered count route's own answer: every row of the tenant, whatever role it belongs to and
  whatever the caller may read through the list above — which is why the two numbers differ, and why
  neither is a substitute for the other.
  """
  rolePermissionCount: Int
  roles: Role!
  role(id: ID!): Role

  """
  The roles of the caller's tenant.
  
  The order is the resource's own: by name, which is what a role is and what an administrator selects
  between, with the identifier as the last key so the order is total and a cursor walk over it is
  stable.
  """
  tenantRoles(filter: RoleFilter, sort: [RoleSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): RoleConnection!

  """
  The role the delivered options look-up answers, or null when this tenant has none.
  
  `name` is the only member the route's own query DTO narrows by, and it defaults to the employee
  role exactly as that DTO defaults it, because that default is what the route answers a caller that
  states nothing. The answer is one role rather than a page of them, which is why this is a field of
  its own and not a filter on the connection above — see the note at the top of this document.
  """
  roleByOptions(name: String): Role

  """How many roles the caller's tenant has."""
  roleCount: Int

  """
  The numbering series of the caller's organization, by key.
  
  A series is configuration an administrator reads whole — an installation holds a handful of keys,
  not a ledger — but the connection is offered all the same, because the same filters, the same sort
  and the same cursor walk are what make this resource's list reachable the way every other
  resource's list is.
  """
  sequences(filter: SequenceFilter, sort: [SequenceSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): SequenceConnection!

  """
  One numbering series of the caller's organization, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal — and so does a row of another
  organization, because a caller is never told that a series it may not read exists. That is the REST
  route's `404` stated in this protocol's vocabulary.
  """
  sequence(id: ID!): Sequence

  """The shares of the caller's tenant, newest first."""
  sharedEntities(filter: SharedEntityFilter, sort: [SharedEntitySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): SharedEntityConnection!

  """One share of the caller's tenant, or null when there is none."""
  sharedEntity(id: ID!): SharedEntity

  """How many shares the caller's tenant holds."""
  sharedEntityCount: Int

  """
  The record a share's token points at, with the share's own rules applied to it.
  
  The answer is the **target**, not the share: the delivered read resolves the token, resolves the
  record the share names, applies the rules to select the members a reader may see and to bound how
  far the reader may walk its relations, and answers that document. Its shape therefore depends on
  what the share was created with, which is why the field is typed as `JSON` rather than as a type
  this schema could declare.
  
  A token that is not there, one whose share has been withdrawn and one whose target has since gone
  are one answer on purpose — the delivered method catches every failure and raises the same miss —
  so this field is a refusal rather than a null, because telling the three apart would confirm which
  tokens are live.
  """
  sharedEntityByToken(token: String!): JSON

  """The skills of the caller's tenant, newest first."""
  skills(filter: SkillFilter, sort: [SkillSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): SkillConnection!

  """One skill of the caller's tenant, or null when there is none."""
  skill(id: ID!): Skill

  """How many skills the caller's tenant holds."""
  skillCount: Int

  """
  The platform's own aggregates: how much of everything this installation holds.
  
  The answer is computed on the request from the records the other domains own, so it is a document
  of totals rather than a row. It carries no argument: there is nothing to narrow, order or page.
  
  The delivered read is scoped by the caller's own credential where there is one — the services it
  aggregates apply the tenant of the request context — and covers the installation's whole tables
  where there is none, which is the state a public caller is in. This field inherits that behaviour
  rather than restating it, because it makes the same call the route makes.
  """
  globalStats: GlobalStats!

  """The tag types of the caller's tenant."""
  tagTypes(filter: TagTypeFilter, sort: [TagTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TagTypeConnection!

  """One tag type of the caller's tenant, or null when there is none."""
  tagType(id: ID!): TagType

  """How many tag types the caller's tenant holds."""
  tagTypeCount: Int

  """The tags of the caller's tenant."""
  tags(filter: TagFilter, sort: [TagSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TagConnection!

  """
  The tags the caller may pick from where they act: the tenant-level rows together with the rows of
  the caller's own organization, the platform's own tags left out, as `GET /tags/level` answers them.
  """
  tagsByLevel(filter: TagFilter, sort: [TagSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TagConnection!

  """One tag of the caller's tenant, or null when there is none."""
  tag(id: ID!): Tag

  """How many tags the caller's tenant holds."""
  tagCount: Int

  """The tasks of the caller's tenant and organization."""
  tasks(filter: TaskFilter, sort: [TaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaskConnection!

  """
  The tasks assigned to the caller, read through the delivered reader that resolves the employee
  from the credential — a narrowing no filter can state, because a filter narrows rows and this
  narrows by who is asking.
  """
  myTasks(filter: TaskFilter, sort: [TaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskConnection!

  """
  The tasks one employee holds, read through the delivered reader that joins the assignment pivot
  the list reader does not.
  
  The controller spells this capability twice — `GET /tasks/employee/:id`, with the employee in the
  path, and `GET /tasks/employee`, with the employee in the query string — and the two answer the
  same rows. That is one capability, so it is one field, mirroring the route that names the employee
  in the path.
  """
  employeeTasks(employeeId: ID!, filter: TaskFilter, sort: [TaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskConnection!

  """
  The tasks a team works, read through the delivered reader that joins the team pivot the list
  reader does not.
  """
  teamTasks(filter: TaskFilter, sort: [TaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskConnection!

  """
  The tasks filed under a project module, read through the delivered reader that joins the module
  pivot the list reader does not.
  """
  moduleTasks(filter: TaskFilter, sort: [TaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskConnection!

  """
  The tasks whose start date or due date falls in the windows the caller states, as the board's
  "what starts and what is due today" read answers them.
  
  This is the connection narrowed — the four bounds are the `between` operator on the two date
  columns, which `TaskFilter` states — and it is a field of its own only because the delivered reader
  carries a default the caller cannot state: it reads tasks that are not screening tasks unless told
  otherwise, and it is the one reader that reads them that way.
  """
  tasksByDate(
    startDateFrom: DateTime
    startDateTo: DateTime
    dueDateFrom: DateTime
    dueDateTo: DateTime

    """
    Whether to read screening tasks. Omitted means what the delivered reader means: neither.
    """
    isScreeningTask: Boolean
    createdByUserId: ID
    projectId: ID
    organizationSprintId: ID
    filter: TaskFilter
    sort: [TaskSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TaskConnection!

  """
  The tasks a saved view selects: the delivered reader loads the view's stored filter set and
  replays it.
  
  A projection the connection cannot state — the narrowing lives in a row rather than in the
  request — so it is a field of its own rather than a filter on `tasks`.
  """
  tasksByView(viewId: ID!, filter: TaskFilter, sort: [TaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskConnection!

  """One task of the caller's tenant, or null when there is none."""
  task(
    id: ID!

    """
    Whether to also answer the epic the task descends from, which is a recursive read.
    """
    includeRootEpic: Boolean
  ): Task

  """How many tasks the caller's tenant holds."""
  taskCount: Int

  """
  The highest task number in a project — the projection a client reads to know where the next task
  will land. Null when the project holds none.
  
  The project is the only member the delivered route needs from the caller; the organization and the
  tenant are read from the credential, which is the value every client of the route sends.
  """
  taskMaxNumber(projectId: ID): Int

  """
  The estimates the caller's tenant holds. One task's estimates are this connection with `taskId` in
  `filter`.
  """
  taskEstimations(filter: TaskEstimationFilter, sort: [TaskEstimationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaskEstimationConnection!

  """One estimate, or null when there is none."""
  taskEstimation(id: ID!): TaskEstimation

  """How many estimates the caller's tenant holds."""
  taskEstimationCount: Int

  """
  The links between tasks the caller's tenant holds. One task's links are this connection with
  `taskFromId` or `taskToId` in `filter`.
  """
  taskLinkedIssues(filter: TaskLinkedIssueFilter, sort: [TaskLinkedIssueSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaskLinkedIssueConnection!

  """One link, or null when there is none."""
  taskLinkedIssue(id: ID!): TaskLinkedIssue

  """How many links the caller's tenant holds."""
  taskLinkedIssueCount: Int

  """The saved views the caller's tenant holds."""
  taskViews(filter: TaskViewFilter, sort: [TaskViewSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaskViewConnection!

  """One saved view, or null when there is none."""
  taskView(id: ID!): TaskView

  """How many saved views the caller's tenant holds."""
  taskViewCount: Int

  """The days planned in the caller's tenant and organization."""
  dailyPlans(filter: DailyPlanFilter, sort: [DailyPlanSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): DailyPlanConnection!

  """
  The days planned for the caller, resolved from the credential rather than from an argument.
  """
  myDailyPlans(filter: DailyPlanFilter, sort: [DailyPlanSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): DailyPlanConnection!

  """
  The days planned for the caller's team, as the delivered reader resolves the team from the
  credential.
  """
  teamDailyPlans(filter: DailyPlanFilter, sort: [DailyPlanSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): DailyPlanConnection!

  """The days planned for one employee."""
  employeeDailyPlans(employeeId: ID!, filter: DailyPlanFilter, sort: [DailyPlanSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): DailyPlanConnection!

  """
  The plans one task is filed into — the inverse of the plan's own task list, read through the
  delivered reader that joins the pivot.
  """
  dailyPlansForTask(taskId: ID!, filter: DailyPlanFilter, sort: [DailyPlanSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): DailyPlanConnection!

  """One planned day, or null when there is none."""
  dailyPlan(id: ID!): DailyPlan

  """How many planned days the caller's tenant holds."""
  dailyPlanCount: Int

  """The tasks that are being screened before they become work."""
  screeningTasks(filter: ScreeningTaskFilter, sort: [ScreeningTaskSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): ScreeningTaskConnection!

  """One screening decision, or null when there is none."""
  screeningTask(id: ID!): ScreeningTask

  """How many screening decisions the caller's tenant holds."""
  screeningTaskCount: Int

  """The lifecycle states a task can be in."""
  taskStatuses(filter: TaskStatusFilter, sort: [TaskStatusSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskStatusConnection!

  """One status, or null when there is none."""
  taskStatus(id: ID!): TaskStatus

  """How many statuses the caller's tenant holds."""
  taskStatusCount: Int

  """The sizes a task can be sized as."""
  taskSizes(filter: TaskSizeFilter, sort: [TaskSizeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskSizeConnection!

  """One size, or null when there is none."""
  taskSize(id: ID!): TaskSize

  """How many sizes the caller's tenant holds."""
  taskSizeCount: Int

  """The urgencies a task can be worked at."""
  taskPriorities(filter: TaskPriorityFilter, sort: [TaskPrioritySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskPriorityConnection!

  """One priority, or null when there is none."""
  taskPriority(id: ID!): TaskPriority

  """How many priorities the caller's tenant holds."""
  taskPriorityCount: Int

  """The releases a task can be filed against."""
  taskVersions(filter: TaskVersionFilter, sort: [TaskVersionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskVersionConnection!

  """One version, or null when there is none."""
  taskVersion(id: ID!): TaskVersion

  """How many versions the caller's tenant holds."""
  taskVersionCount: Int

  """The kinds of work a task can be."""
  issueTypes(filter: IssueTypeFilter, sort: [IssueTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): IssueTypeConnection!

  """One issue type, or null when there is none."""
  issueType(id: ID!): IssueType

  """How many issue types the caller's tenant holds."""
  issueTypeCount: Int

  """The kinds of relation one task can have to another."""
  taskRelatedIssueTypes(filter: TaskRelatedIssueTypeFilter, sort: [TaskRelatedIssueTypeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): TaskRelatedIssueTypeConnection!

  """One related-issue type, or null when there is none."""
  taskRelatedIssueType(id: ID!): TaskRelatedIssueType

  """How many related-issue types the caller's tenant holds."""
  taskRelatedIssueTypeCount: Int

  """
  The whole vocabulary one board needs, in one request.
  
  This is the projection the six connections above cannot state: the delivered route answers every
  section at once, because a board that renders a task form needs all of them before it can render
  anything. Each section of the answer is the connection its own root field answers, built by the
  same codec, so a page walked here resumes there.
  """
  taskMetadata(
    organizationId: ID!
    organizationTeamId: ID
    projectId: ID

    """
    Which sections to answer. Omitted means all of them, as the delivered route means.
    """
    include: [TaskMetadataSection!]
  ): TaskMetadata!

  """
  The tenant of the credential that asks. The subject comes from the caller and never from an
  argument, which is the delivered read route's own shape.
  """
  tenant: Tenant

  """
  The setting rows of the caller's tenant and organization, tenant-wide rows included.
  """
  tenantSettings(filter: TenantSettingFilter, sort: [TenantSettingSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TenantSettingConnection!

  """One setting row of the caller's tenant, or null when there is none."""
  tenantSetting(id: ID!): TenantSetting

  """How many setting rows the caller's tenant holds."""
  tenantSettingCount: Int

  """
  The caller's tenant's settings as the delivered document route answers them: a value per key, with
  the secret-bearing keys masked.
  """
  tenantSettingValues: JSON!

  """
  The installation-wide defaults (`tenantId IS NULL`) as the delivered global route answers them,
  masked the same way. A tenant's own row overrides the default for that tenant only.
  """
  globalTenantSettingValues: JSON!

  """The UI flavour this tenant's users are served."""
  tenantUiPreferences: TenantUiPreferences!

  """The legal documents a new account must accept, as currently published."""
  termsAcceptanceDocuments(
    """
    The locale to publish the documents in. Omit for the corpus's own default rendering.
    """
    locale: String
  ): [TermsAcceptanceDocument!]!

  """
  The leave balances the caller may see, newest year first.
  
  The organization and the three narrowing members are the delivered query DTO's own, and they narrow
  the read rather than the answer: the delivered method applies them to the store and returns one page
  of its own. When the caller states no sort, the order is the delivered read's — `year` descending —
  with the identifier appended so the order is total.
  """
  timeOffBalances(
    """
    The organization the balances belong to. The delivered query DTO requires it.
    """
    organizationId: ID!

    """
    One employee's balances. A caller without the permission to change the selected employee is answered its own whatever it states here.
    """
    employeeId: ID

    """One policy's balances."""
    policyId: ID

    """The year the balances are tracked for."""
    year: Int
    filter: TimeOffBalanceFilter
    sort: [TimeOffBalanceSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TimeOffBalanceConnection!

  """
  The caller's own leave balances, newest year first.
  
  The delivered `GET /me` route answers these by resolving the employee from the credential, so this
  field takes no employee and a caller that has no engagement of its own is refused by the delivered
  read — its refusal, in its own words, rather than a second one restated here.
  """
  myTimeOffBalances(
    """
    The organization the balances belong to. The delivered query DTO requires it.
    """
    organizationId: ID!

    """One policy's balances."""
    policyId: ID

    """The year the balances are tracked for."""
    year: Int
    filter: TimeOffBalanceFilter
    sort: [TimeOffBalanceSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TimeOffBalanceConnection!

  """
  The policies of the caller's tenant, newest first.
  
  When the caller states no sort, the order is the platform's own — newest first, then the identifier,
  because the delivered read states no order of its own and a cursor walk needs a total one.
  """
  timeOffPolicies(filter: TimeOffPolicyFilter, sort: [TimeOffPolicySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TimeOffPolicyConnection!

  """
  One policy of the caller's tenant, or null when there is none.
  
  This field carries the edit permission, not the view permission its read sibling carries, because
  the delivered `GET /:id` route it mirrors is inherited without a permission of its own and therefore
  runs under the controller's class-level edit permission. Reading one policy and listing them
  requiring different grants is the controller's own asymmetry; widening it here, on one surface only,
  is what the two-protocol rule forbids.
  """
  timeOffPolicy(id: ID!): TimeOffPolicy

  """How many policies the caller's tenant holds."""
  timeOffPolicyCount: Int

  """
  The requests filed in one organization over a range, newest filed first.
  
  The range is inclusive at both ends, which is the comparison the delivered read performs. When the
  caller states no sort, the order is the platform's own — newest filed first, then the identifier,
  because the delivered read states no order of its own and a cursor walk needs a total one.
  """
  timeOffRequests(
    """
    The organization whose requests are read. The delivered read scopes its criterion by it and has no default for it.
    """
    organizationId: ID!

    """The start of the range the requests are read over."""
    startDate: DateTime!

    """The end of the range."""
    endDate: DateTime!

    """
    Narrows the read to one employee's requests. The delivered read applies this to the store through an inner join, so it decides which rows are read rather than which are answered.
    """
    employeeId: ID
    filter: TimeOffRequestFilter
    sort: [TimeOffRequestSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TimeOffRequestConnection!

  """
  One request of the caller's tenant, or null when there is none.
  
  This field carries the edit permission, not the view permission its read sibling carries, because the
  delivered `GET /:id` route it mirrors is inherited without a permission of its own and therefore runs
  under the controller's class-level edit permission.
  """
  timeOffRequest(id: ID!): TimeOffRequest

  """How many requests the caller's tenant holds."""
  timeOffRequestCount: Int

  """
  The activities of one organization, in the order the delivered list read means: longest first.
  
  Ordered by duration descending because that is the order the delivered reader hands its rows over in,
  and the connection restates it rather than inventing one — a connection's order is what its cursors
  walk, so it has to be the order the rows arrived in.
  """
  activities(
    """
    The organization the activities are read from. The delivered query requires it.
    """
    organizationId: ID!

    """
    Narrows to the activities recorded in this range, both bounds included.
    """
    startDate: DateTime

    """
    Narrows to the activities recorded up to this instant, both bounds included.
    """
    endDate: DateTime

    """
    Narrows to these engagements. A caller that may not change the selected employee is answered its
    own activities whatever it states here — that branch is the delivered read's, not this surface's.
    """
    employeeIds: [ID!]

    """Narrows to these projects."""
    projectIds: [ID!]

    """Narrows to these activity titles, exactly as stated."""
    titles: [String!]

    """Narrows to these activity kinds: `APP` and `URL`."""
    types: [String!]

    """
    Narrows to the activities whose slot belongs to a log tracked from these sources —
    `BROWSER`, `DESKTOP`, `MOBILE`, `BROWSER_EXTENSION` and the integrations the platform ships.
    This is the *log's* source, not the activity's own column, which `filter` narrows by.
    """
    source: [String!]

    """
    Narrows to the activities whose slot belongs to a log of these kinds — `TRACKED`, `MANUAL`,
    `IDLE` or `RESUMED`.
    """
    logType: [String!]

    """Narrows to the activities whose slot falls inside this activity band."""
    activityLevel: ActivityLevelInput

    """
    The fields the returned rows are narrowed by, applied to the rows the read answered.
    """
    filter: ActivityFilter

    """
    The order the returned rows are put in. At most three keys are accepted.
    """
    sort: [ActivitySort!]

    """The cursor page."""
    page: PageInput

    """The page size, walking forwards from `after`."""
    first: Int

    """Resume after this cursor. Exclusive."""
    after: String

    """The page size, walking backwards from `before`."""
    last: Int

    """Resume before this cursor. Exclusive."""
    before: String

    """The offset page size. State this or the cursor style, never both."""
    limit: Int

    """The offset to start at. State this or the cursor style, never both."""
    offset: Int
  ): ActivityConnection!

  """
  One day's tracked time per engagement and title — a computation over the activities, not a list of
  them.
  
  The delivered read groups by day, title and engagement and folds the rows: it counts how many
  activities each group holds and sums how long they lasted. Nothing it answers is a column of an
  activity, which is why this is a root field of its own rather than a connection over activities.
  """
  dailyActivities(
    """
    The organization the days are computed for. The delivered query requires it.
    """
    organizationId: ID!

    """The first day of the range, both bounds included."""
    startDate: DateTime

    """The last day of the range, both bounds included."""
    endDate: DateTime

    """
    Computes the days of these engagements. A caller that may not change the selected employee is
    answered its own days whatever it states here.
    """
    employeeIds: [ID!]

    """Computes the days of these projects."""
    projectIds: [ID!]

    """Computes the days of these activity titles, exactly as stated."""
    titles: [String!]

    """Computes the days of these activity kinds."""
    types: [String!]

    """
    Computes the days of the activities whose slot belongs to a log tracked from these sources.
    """
    source: [String!]

    """
    Computes the days of the activities whose slot belongs to a log of these kinds.
    """
    logType: [String!]

    """
    Computes the days of the activities whose slot falls inside this activity band.
    """
    activityLevel: ActivityLevelInput
  ): [DailyActivity!]!

  """
  The report of tracked time, grouped the way the caller asks — a computation over the activities.
  
  The delivered route reads the report rows once, and then nests them one of three ways when the caller
  states a grouping. This is one field rather than three because it is one computation: `groupBy` states
  how the same rows are nested, and exactly one member of `DailyActivityReport` is filled by it. A caller
  that states no grouping is answered the rows unnested, which is what the delivered route does with a
  request that states none.
  
  The share each row carries — `durationPercentage` — is computed against the group the row is nested
  under, so the same activity carries a different share under two groupings. That is the delivered
  mapper's arithmetic and is stated on the row rather than re-derived here.
  """
  dailyActivitiesReport(
    """
    The organization the report is computed for. The delivered query requires it.
    """
    organizationId: ID!

    """
    How to nest the report rows. Omit it for the rows themselves; a value the delivered route does not
    group by is not offered, because the route would answer the rows unnested for it and an argument
    whose effect is "no grouping" is stated by omitting the argument.
    """
    groupBy: DailyActivityReportGrouping

    """The first day of the range, both bounds included."""
    startDate: DateTime

    """The last day of the range, both bounds included."""
    endDate: DateTime

    """
    Reports on these engagements. A caller that may not change the selected employee is answered its
    own activity whatever it states here.
    """
    employeeIds: [ID!]

    """Reports on these projects."""
    projectIds: [ID!]

    """Reports on these activity titles, exactly as stated."""
    titles: [String!]

    """Reports on these activity kinds."""
    types: [String!]

    """
    Reports on the activities whose slot belongs to a log tracked from these sources.
    """
    source: [String!]

    """Reports on the activities whose slot belongs to a log of these kinds."""
    logType: [String!]

    """Reports on the activities whose slot falls inside this activity band."""
    activityLevel: ActivityLevelInput
  ): DailyActivityReport!

  """
  The sessions recovered from the tracking payloads of one organization, over a range.
  
  A session is not a row: it is the grouping of the payloads that carry the same session identifier, and
  the delivered read can either fold a session's rows together across the slots it spans or answer one row
  per slot. Both are this one field, because both are the same decoded rows stated two ways.
  
  The range defaults to the last forty-eight hours when the caller states neither bound, which is the
  delivered read's own default: it exists so that a request which states no range cannot ask the platform
  to decode every payload it has ever stored.
  """
  trackingSessions(
    """
    The organization the tracking is read from. The delivered query requires it.
    """
    organizationId: ID!

    """The first day of the range, both bounds included."""
    startDate: DateTime

    """The last day of the range, both bounds included."""
    endDate: DateTime

    """
    Reads the tracking of these engagements. A caller that states none is answered its own — the
    delivered read resolves the caller's engagement from the credential, and that branch is its own.
    """
    employeeIds: [ID!]

    """Reads the tracking of the slots attributed to these projects."""
    projectIds: [ID!]

    """Reads one session identifier rather than every session of the scope."""
    sessionId: String

    """
    Whether to fold the rows of one session together across the slots it spans. Absent means the
    delivered query's own default, which is to fold: a session that crossed a slot boundary is one
    session, and answering it twice would be answering a fact about slot boundaries rather than about
    tracking.
    """
    groupBySession: Boolean

    """
    Whether to carry the decoded form of each payload as well as its encoded form. Absent means the
    delivered query's own default, which is to carry the encoded form only: the encoded payload is what
    the client that produced it can re-read, and the decoded document is the larger of the two.
    """
    includeDecodedData: Boolean
  ): CustomTrackingSessions!

  """
  One slot's tracking data, when the slot holds any.
  
  A slot that holds no tracking is answered as a slot with none — `hasTrackingData: false` and the message
  that says so — because that is what the delivered read answers. A slot that is not there at all is a
  miss, and a miss is `null` on a field that may have none: the delivered route's `404` is that same fact
  stated in the other protocol's vocabulary.
  """
  timeSlotTrackingData(
    """The slot to read."""
    timeSlotId: ID!
  ): CustomTrackingTimeSlotData

  """
  The sessions one session identifier belongs to, over an optional range.
  
  This is the read a client that has just submitted a payload uses to see what it submitted. The
  identifier is required and the range is not, and the range defaults to the last twenty-four hours when
  the caller states neither bound — the delivered read's own default, which keeps a read of one identifier
  from decoding every payload that ever carried it.
  
  The read never carries decoded payloads: it answers the encoded form only, which is the delivered
  method's own choice and is not offered as a choice here.
  """
  trackingSessionsBySessionId(
    """The session identifier to read."""
    sessionId: String!

    """The first day of the range, both bounds included."""
    startDate: DateTime

    """The last day of the range, both bounds included."""
    endDate: DateTime
  ): [CustomTrackingSessionResponse!]!

  """
  The sessions a tracker is reporting right now.
  
  A session is active while it reported activity within the threshold, which is why the answer is a
  window rather than a filter: it is computed from the clock at the moment of the call, and a session
  leaves it by being idle rather than by being written.
  
  The threshold is floored at one minute, stated on the argument below and performed by the field, so a
  caller that states a value the delivered route would clamp is answered exactly what the route answers.
  """
  activeTrackingSessions(
    """
    Reads the active sessions of this engagement. Omit it for the caller's own.
    """
    employeeId: ID

    """
    How many minutes of inactivity still count as active. Absent means the delivered route's own
    default of thirty minutes, and a value below one is read as **one** — the floor the delivered route
    applies, applied here too rather than accepted and clamped silently.
    """
    activityThresholdMinutes: Int
  ): [CustomTrackingSessionResponse!]!

  """
  One authorized employee's activity over a period of local calendar days: how many days carried
  activity, how long it lasted, which days were the first and the last, and — when asked — the
  per-day breakdown.
  
  A caller that may not view this employee's profile is refused by the delivered read, which is why
  this field answers a non-null summary rather than an empty one.
  """
  timeTrackingProfileActivity(
    """
    The organization the read is scoped to, and the organization the named engagement must belong to.
    """
    organizationId: ID!

    """The engagement whose activity is summarized."""
    employeeId: ID!

    """
    The first local calendar date of the period, inclusive, in `YYYY-MM-DD`. Resolved against `timeZone`.
    """
    startDate: String!

    """
    The local calendar date the period ends on, exclusive, in `YYYY-MM-DD`. It must be after `startDate`, and the span must not exceed 366 local days.
    """
    endDate: String!

    """
    The IANA time zone the two local dates are resolved against, for example `Europe/Madrid`. An instant that falls on a different local day in another zone is counted on the day this zone puts it on.
    """
    timeZone: String!

    """
    The team the caller's access is evaluated with, where the caller states one. It can refuse the request where omitting it would serve it, because the delivered policy reads it.
    """
    organizationTeamId: ID

    """
    Whether to answer the per-day breakdown. The delivered read answers it only for `true`, and the route's own default is `false`.
    """
    includeDaily: Boolean
  ): ProfileActivity!

  """
  How much of one organization worked over a range, and how actively: two counts for the range, the
  same two for today, and the average activity and tracked duration behind each period.
  """
  timeTrackingCounts(
    """
    The organization whose tracked time is counted. Every delivered read resolves its rows through it and none has a default.
    """
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read answers the current week.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current week.
    """
    endDate: DateTime

    """
    The start of the period the two `today` figures cover, which is not the range's start. Omit it and the delivered read answers the current day.
    """
    todayStart: DateTime

    """
    The end of the period the two `today` figures cover. Omit it and the delivered read answers the current day.
    """
    todayEnd: DateTime

    """
    Narrows the read to these engagements. Omitted, every engagement the caller may see is read.
    """
    employeeIds: [ID!]

    """Narrows the read to these projects."""
    projectIds: [ID!]

    """Narrows the read to these teams."""
    teamIds: [ID!]

    """
    Narrows the read to the time slots whose activity falls in this band. Omitted, every slot counts.
    """
    activityLevel: TimeTrackingActivityLevelInput

    """
    Narrows the read to these time-log types, by value: `TRACKED`, `MANUAL`, `IDLE` or `RESUMED`. Carried as a value rather than declared as an enum because the vocabulary belongs to the contracts package.
    """
    logType: [String!]

    """
    Narrows the read to these time-log sources, by value: `DESKTOP`, `BROWSER`, `BROWSER_EXTENSION`, `MOBILE`, `HUBSTAFF`, `UPWORK`, `TEAMS` or `CLOC`.
    """
    source: [String!]

    """
    Reads only the caller's own engagement. The delivered read applies it in addition to the caller's permissions, never instead of them.
    """
    onlyMe: Boolean
  ): TimeTrackingCounts!

  """
  The members of one organization who tracked time over a range, each with the hours behind them.
  """
  timeTrackingMembers(
    """The organization whose members are read."""
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read answers the current week.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current week.
    """
    endDate: DateTime

    """
    The start of the period the two today figures cover, which is not the range's start. Omit it and the delivered read answers the current day.
    """
    todayStart: DateTime

    """
    The end of the period the two today figures cover. Omit it and the delivered read answers the current day.
    """
    todayEnd: DateTime

    """Narrows the read to these engagements."""
    employeeIds: [ID!]

    """Narrows the read to these projects."""
    projectIds: [ID!]

    """Narrows the read to these teams."""
    teamIds: [ID!]
  ): [TimeTrackingMemberStatistic!]!

  """
  The projects of one organization that were worked on over a range, the five with the most tracked
  time first.
  """
  timeTrackingProjects(
    """The organization whose projects are read."""
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read answers the current week.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current week.
    """
    endDate: DateTime

    """Narrows the read to these engagements."""
    employeeIds: [ID!]

    """
    Narrows the read to these projects. A project outside the list is not read, whatever time was tracked against it.
    """
    projectIds: [ID!]

    """Narrows the read to these teams."""
    teamIds: [ID!]

    """
    Reads only the caller's own engagement, in addition to the caller's permissions.
    """
    onlyMe: Boolean
  ): [TimeTrackingProjectStatistic!]!

  """
  The tasks of one organization that were worked on, each with the time it took today and its share of
  the range.
  
  Mirrors `POST /timesheet/statistics/tasks`, and is a query because that route is a read: the verb
  there carries a filter that does not fit a query string, not a change.
  """
  timeTrackingTasks(
    """The organization whose tasks are read."""
    organizationId: ID!

    """
    The start of the range. With no range and no `defaultRange`, the read answers every task the organization has ever tracked time against.
    """
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """
    The start of the period the today figure covers, which is not the range's start.
    """
    todayStart: DateTime

    """The end of the period the today figure covers."""
    todayEnd: DateTime

    """Narrows the read to these engagements."""
    employeeIds: [ID!]

    """Narrows the read to these projects."""
    projectIds: [ID!]

    """
    Selects which tasks are read. Omitted, every task the range touched is read.
    """
    taskIds: [ID!]

    """
    Narrows the read to these teams. The delivered criterion is an either-or over this list and `organizationTeamId`, so stating both widens the read rather than intersecting it.
    """
    teamIds: [ID!]

    """
    Narrows the read to one team, beside `teamIds` rather than instead of it, for the reason stated there.
    """
    organizationTeamId: ID

    """
    Keeps the first N answers after the per-task totals have been aggregated, so it truncates a ranked answer rather than narrowing the read. Omitted, every task the range touched is answered.
    """
    take: Int

    """
    With no range stated, computes one: the current week, or the unit `unitOfTime` names. The delivered read's own default is `false`, which answers the whole history instead.
    """
    defaultRange: Boolean

    """
    The unit `defaultRange` computes its range in — the read's own default is `week` for the range and `day` for the today figures — stated as a value rather than declared as an enum because the vocabulary belongs to the moment library.
    """
    unitOfTime: String

    """
    Reads only the caller's own engagement, in addition to the caller's permissions.
    """
    onlyMe: Boolean
  ): [TimeTrackingTaskStatistic!]!

  """
  The manual time logs of one organization over a range, the five most recently started first.
  """
  timeTrackingManualTimes(
    """The organization whose manual logs are read."""
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read answers the current week.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current week.
    """
    endDate: DateTime

    """Narrows the read to these engagements."""
    employeeIds: [ID!]

    """Narrows the read to these projects."""
    projectIds: [ID!]

    """Narrows the read to these teams."""
    teamIds: [ID!]

    """
    Reads only the caller's own engagement, in addition to the caller's permissions.
    """
    onlyMe: Boolean
  ): [TimeTrackingManualTimeStatistic!]!

  """
  The three members of one organization who tracked time most recently over a range, each with up to
  nine of their most recent time slots and the screenshots filed against them.
  """
  timeTrackingTimeSlots(
    """The organization whose slots are read."""
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read answers the current week.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current week.
    """
    endDate: DateTime

    """Narrows the read to these engagements."""
    employeeIds: [ID!]

    """
    Narrows the slots to those whose logs were tracked against these projects.
    """
    projectIds: [ID!]

    """Narrows the slots to those whose logs were tracked in these teams."""
    teamIds: [ID!]

    """
    Reads only the caller's own engagement, in addition to the caller's permissions.
    """
    onlyMe: Boolean
  ): [TimeTrackingTimeSlotStatistic!]!

  """
  What one organization spent its time on over a range, the five activity titles with the most tracked
  time first.
  """
  timeTrackingActivities(
    """The organization whose activity is read."""
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read answers the current week.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current week.
    """
    endDate: DateTime

    """Narrows the read to these engagements."""
    employeeIds: [ID!]

    """Narrows the read to these projects."""
    projectIds: [ID!]

    """
    Narrows the read to these teams, through the logs of the slots the activity was recorded in.
    """
    teamIds: [ID!]

    """
    Reads only the caller's own engagement, in addition to the caller's permissions.
    """
    onlyMe: Boolean
  ): [TimeTrackingActivityStatistic!]!

  """
  The time logs of one organization, oldest first.
  
  The order is the delivered read's own — both of its branches fix `startedAt` ascending — so the
  same rows read over either protocol come back in the same sequence. The employee narrowing the
  delivered read performs from the credential is not a filter here: a caller that may not change the
  selected employee is answered its own logs, or those of the employees it manages, which is the
  scope the route itself applies.
  """
  timeLogs(
    """
    The organization whose logs are read. The delivered read applies it to every query it builds and has no default for it.
    """
    organizationId: ID!

    """
    The start of the range. The delivered read applies the two ends together, through the platform's own date-range formatter.
    """
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """
    The employees to answer for, where the caller names any. The read hands the pair of employee and
    team selectors to its own manager check, so a caller that may not change the selected employee is
    answered the employees it manages among those it named rather than being refused.
    """
    employeeIds: [ID!]

    """
    The teams the named employees are managed within, which the read's own manager check consults.
    """
    teamIds: [ID!]

    """
    The activity window to answer for, expressed as a percentage of tracked activity.
    """
    activityLevel: TimeLogActivityLevelInput
    filter: TimeLogFilter
    sort: [TimeLogSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TimeLogConnection!

  """One time log of the caller's tenant, or null when there is none."""
  timeLog(id: ID!): TimeLog

  """
  The logs of one employee that overlap a window, or an empty list when none does.
  
  The overlap is the delivered read's own: a log is answered when either of the two instants the
  caller names falls inside the log's own interval, which is the condition a hand-recorded interval
  is checked against before it is written.
  """
  timeLogConflicts(
    """The start of the window."""
    startDate: DateTime!

    """The end of the window."""
    endDate: DateTime!

    """The employee whose logs are compared against the window."""
    employeeId: ID!

    """The organization the logs must belong to."""
    organizationId: ID!

    """
    Logs to leave out of the comparison — the row being edited, when the caller is editing one.
    """
    ignoreId: [ID!]
  ): [TimeLog!]!

  """
  One report of one organization over a range, grouped the way the caller asks.
  
  The answer's shape is chosen by `groupBy`: by date (the delivered default), by employee, by project
  or by client. A value the delivered calculation does not recognise is answered the date grouping,
  which is the delivered behaviour rather than a refusal invented here. Days are read in the time
  zone the caller names.
  """
  timeLogDailyReport(
    """The organization whose logs are reported."""
    organizationId: ID!

    """
    How the report is grouped: `date`, `employee`, `project` or `client`. An absent or unrecognised value is the date grouping.
    """
    groupBy: String

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees to report on."""
    employeeIds: [ID!]

    """The projects to report on."""
    projectIds: [ID!]

    """The tasks to report on."""
    taskIds: [ID!]

    """The teams to report on."""
    teamIds: [ID!]

    """The sources to report on."""
    source: [String!]

    """The log types to report on."""
    logType: [String!]

    """The payroll period to report on."""
    timesheetId: ID

    """
    `true` reports only the edited logs, `false` only those that have not been edited.
    """
    isEdited: Boolean

    """The activity window to report on."""
    activityLevel: TimeLogActivityLevelInput

    """The time zone the days are read in."""
    timeZone: String
  ): [TimeLogDailyReportEntry!]!

  """
  How much of every day of a range was tracked, recorded by hand, idle and resumed.
  
  One entry per day of the range, whether or not anything was recorded on it, and every figure is in
  hours.
  """
  timeLogDailyReportChart(
    """The organization whose logs are charted."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees to chart."""
    employeeIds: [ID!]

    """The projects to chart."""
    projectIds: [ID!]

    """The tasks to chart."""
    taskIds: [ID!]

    """The teams to chart."""
    teamIds: [ID!]

    """The sources to chart."""
    source: [String!]

    """The log types to chart."""
    logType: [String!]

    """The payroll period to chart."""
    timesheetId: ID

    """
    `true` charts only the edited logs, `false` only those that have not been edited.
    """
    isEdited: Boolean

    """The activity window to chart."""
    activityLevel: TimeLogActivityLevelInput

    """The time zone the days are read in."""
    timeZone: String
  ): [TimeLogDailyReportChartPoint!]!

  """
  What one organization owes for a range, priced per employee and per day.
  
  The amounts are the delivered calculation's own: each employee's summed duration over the day,
  priced at the rate the read selected for them. The answer is money and the lines it is made of,
  which is what an operator reconciles against payroll.
  """
  timeLogOwedAmountReport(
    """The organization whose owed amount is reported."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees to price."""
    employeeIds: [ID!]

    """The projects to price."""
    projectIds: [ID!]

    """The tasks to price."""
    taskIds: [ID!]

    """The teams to price."""
    teamIds: [ID!]

    """The sources to price."""
    source: [String!]

    """The log types to price."""
    logType: [String!]

    """The payroll period to price."""
    timesheetId: ID

    """
    `true` prices only the edited logs, `false` only those that have not been edited.
    """
    isEdited: Boolean

    """The activity window to price."""
    activityLevel: TimeLogActivityLevelInput

    """The time zone the days are read in."""
    timeZone: String
  ): [TimeLogOwedAmountReportEntry!]!

  """
  The same sums as one figure per day, for a chart.
  
  This is the day's total and `timeLogOwedAmountReport` is the lines that total is made of; the two
  are deliberately two fields, because a chart and the table behind it are two different reads of one
  calculation.
  """
  timeLogOwedAmountReportChart(
    """The organization whose owed amount is charted."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees to price."""
    employeeIds: [ID!]

    """The projects to price."""
    projectIds: [ID!]

    """The tasks to price."""
    taskIds: [ID!]

    """The teams to price."""
    teamIds: [ID!]

    """The sources to price."""
    source: [String!]

    """The log types to price."""
    logType: [String!]

    """The payroll period to price."""
    timesheetId: ID

    """
    `true` prices only the edited logs, `false` only those that have not been edited.
    """
    isEdited: Boolean

    """The activity window to price."""
    activityLevel: TimeLogActivityLevelInput

    """The time zone the days are read in."""
    timeZone: String
  ): [TimeLogOwedAmountReportChartPoint!]!

  """
  One entry per employee who recorded time in the range, with the days behind the entry.
  
  `sum` is the employee's summed duration over the range, **in seconds**, and it is null where the
  range holds no log of theirs at all — the delivered answer rather than a fabricated zero.
  `activity` is the tracked activity of the range as a percentage of its tracked time.
  """
  timeLogWeeklyReport(
    """The organization whose week is reported."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees to report on."""
    employeeIds: [ID!]

    """The projects to report on."""
    projectIds: [ID!]

    """The tasks to report on."""
    taskIds: [ID!]

    """The teams to report on."""
    teamIds: [ID!]

    """The sources to report on."""
    source: [String!]

    """The log types to report on."""
    logType: [String!]

    """The payroll period to report on."""
    timesheetId: ID

    """
    `true` reports only the edited logs, `false` only those that have not been edited.
    """
    isEdited: Boolean

    """The activity window to report on."""
    activityLevel: TimeLogActivityLevelInput

    """The time zone the days are read in."""
    timeZone: String
  ): [TimeLogWeeklyReport!]!

  """
  How much of every employee's limit the range used, one period per entry.
  
  `duration` is one of `day`, `week` or `month` — the three the delivered calculation converts an
  employee's weekly limit to — and an absent value is a day, which is the delivered default. Both the
  durations and the limits are **in seconds**.
  """
  timeLogTimeLimitReport(
    """The organization whose limits are reported."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees to report on."""
    employeeIds: [ID!]

    """The projects to report on."""
    projectIds: [ID!]

    """The tasks to report on."""
    taskIds: [ID!]

    """The teams to report on."""
    teamIds: [ID!]

    """The sources to report on."""
    source: [String!]

    """The log types to report on."""
    logType: [String!]

    """The payroll period to report on."""
    timesheetId: ID

    """
    `true` reports only the edited logs, `false` only those that have not been edited.
    """
    isEdited: Boolean

    """The activity window to report on."""
    activityLevel: TimeLogActivityLevelInput

    """The time zone the periods are read in."""
    timeZone: String

    """
    The period each entry covers: `day`, `week` or `month`. An absent value is a day.
    """
    duration: String
  ): [TimeLogTimeLimitReportEntry!]!

  """
  How much of every project's budget the range consumed.
  
  The delivered read walks the projects and their logs directly rather than through the time-log
  query builder, so it takes the organization, the range and the two selectors it actually reads and
  nothing else.
  """
  projectBudgetLimit(
    """The organization whose projects are reported."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees whose time counts against the budget."""
    employeeIds: [ID!]

    """
    The projects to report on. Omit it and every project that was worked on is answered.
    """
    projectIds: [ID!]
  ): [ProjectBudgetLimit!]!

  """How much of every client's budget the range consumed."""
  clientBudgetLimit(
    """The organization whose clients are reported."""
    organizationId: ID!

    """The start of the range."""
    startDate: DateTime

    """The end of the range."""
    endDate: DateTime

    """The employees whose time counts against the budget."""
    employeeIds: [ID!]

    """The projects whose time counts against the budget."""
    projectIds: [ID!]
  ): [ClientBudgetLimit!]!

  """The slots one organization recorded over a range, oldest first."""
  timeSlots(
    """
    The organization whose slots are read. The delivered read resolves the range and the logs through it.
    """
    organizationId: ID!

    """
    The start of the range. Omit it and the delivered read takes the start of the current day.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read takes the end of the current day.
    """
    endDate: DateTime

    """
    The employees whose slots are read. Omit it and every employee of the organization counts,
    unless the caller may not select employees — in which case the delivered read narrows the
    answer to the caller's own.
    """
    employeeIds: [ID!]

    """
    The projects whose slots are read, by the project behind each slot's logs.
    """
    projectIds: [ID!]

    """
    The sources the logs behind the slots are narrowed to, by value (`DESKTOP`, `BROWSER`, …).
    """
    source: [String!]

    """
    The kinds of log the slots are narrowed to, by value (`TRACKED`, `MANUAL`, `IDLE`, `RESUMED`).
    """
    logType: [String!]

    """
    The band of activity the slots are narrowed to, as percentages of the slot. The delivered read
    converts the band into the seconds its activity counter is stored in — a ten-minute slot is six
    hundred seconds, so one percent of it is six — and narrows by those; the conversion is stated
    here because a caller stating a raw counter instead would be stating a different question.
    """
    activityLevel: TimeSlotActivityLevelInput

    """
    The fields the returned slots are narrowed by, applied to the rows the read returned.
    """
    filter: TimeSlotFilter

    """
    The order the returned slots are answered in. At most three keys are accepted.
    """
    sort: [TimeSlotSort!]

    """Cursor pagination, exclusive with `limit` and `offset`."""
    page: PageInput

    """The page size, walking forwards from `after`."""
    first: Int

    """Resume after this cursor. Exclusive."""
    after: String

    """The page size, walking backwards from `before`."""
    last: Int

    """Resume before this cursor. Exclusive."""
    before: String

    """
    The page size, walking from `offset`. Exclusive with `page`, `first`, `after`, `last` and `before`.
    """
    limit: Int

    """
    How many rows to skip, in the connection's default order. Exclusive with the cursor arguments.
    """
    offset: Int
  ): TimeSlotConnection!

  """One slot of the caller's organization, or null when there is none."""
  timeSlot(
    """The slot to read."""
    id: ID!
  ): TimeSlot

  """
  The caller's timer as it stands today: the seconds recorded in the period, whether a log is open,
  and which log the state was derived from.
  """
  timerStatus(
    """The organization whose logs the state is computed over."""
    organizationId: ID!

    """
    The source the logs are narrowed to, by value (`DESKTOP`, `BROWSER`, …). Omit it and every source counts.
    """
    source: String

    """
    The start of the period the computation is anchored to. Omit it and the delivered read takes the start of the current day.
    """
    todayStart: DateTime

    """
    The end of the period. Omit it and the delivered read takes the end of the current day.
    """
    todayEnd: DateTime

    """
    The employee whose timer is read. The delivered read honours it only for a caller that may
    change the selected employee, and answers every other caller its own timer whatever is named
    here — which is the route's rule, not one this surface states twice.
    """
    employeeId: ID
  ): TimerStatus!

  """
  The last worked state of each employee the read considered, one entry per employee.
  """
  timerWorkedStatus(
    """The organization whose logs the states are computed over."""
    organizationId: ID!

    """
    The source the logs are narrowed to, by value. Omit it and every source counts.
    """
    source: String

    """
    The employee whose state is read. Used when no set of employees is stated.
    """
    employeeId: ID

    """
    The employees whose states are read. The delivered read consults the set only for a caller
    that may change the selected employee or read another member's last log, and answers every
    other caller its own state — which is the route's rule, not one this surface states twice.
    """
    employeeIds: [ID!]
  ): [TimerWorkedStatus!]!

  """
  The project change requests raised against one period, newest first — which is the order the
  delivered read states and the connection reproduces.
  
  A caller that may not approve timesheets is answered only the requests raised against its own
  periods, or refused, which is the delivered read's own scope.
  """
  timesheetProjectChangeRequests(
    """
    The period whose requests are read. The delivered route takes it from its path and refuses an absent value.
    """
    timesheetId: ID!

    """
    The organization the period belongs to. The delivered route takes it from its query string and refuses an absent value.
    """
    organizationId: ID!
    filter: TimesheetProjectChangeRequestFilter
    sort: [TimesheetProjectChangeRequestSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TimesheetProjectChangeRequestConnection!

  """
  The timesheets of the caller's tenant within a range, newest first.
  
  The read is the delivered list route's own, so it narrows to the caller's own timesheets when the
  caller may not change the selected employee — a scope the read applies from the credential and this
  field does not restate.
  """
  timesheets(
    """
    The organization the rows belong to. The delivered read narrows by it in the store, so a caller that states none is answered the rows of no organization.
    """
    organizationId: ID

    """
    The start of the range. Omit it and the delivered read answers the current month.
    """
    startDate: DateTime

    """
    The end of the range. Omit it and the delivered read answers the current month.
    """
    endDate: DateTime

    """
    The statuses to answer. Omit it and every status is answered; a value the vocabulary does not carry is ignored by the read rather than refused.
    """
    status: [String!]

    """
    The employees to answer for. The delivered read replaces this with the caller's own employee when the caller may not change the selected employee.
    """
    employeeIds: [ID!]
    filter: TimesheetFilter
    sort: [TimesheetSort!]
    page: PageInput
    first: Int
    after: String
    last: Int
    before: String
    limit: Int
    offset: Int
  ): TimesheetConnection!

  """One timesheet of the caller's tenant, or null when there is none."""
  timesheet(id: ID!): Timesheet

  """
  How many timesheets the caller's tenant holds within a range. Argument-less, and narrowed by the
  read's own defaults and by the credential, exactly as the count route is when it is given no
  options.
  """
  timesheetCount: Int

  """The memberships of the caller's tenant, newest first."""
  userOrganizations(filter: UserOrganizationFilter, sort: [UserOrganizationSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): UserOrganizationConnection!

  """One membership of the caller's tenant, or null when there is none."""
  userOrganization(id: ID!): UserOrganization

  """
  How many memberships the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  userOrganizationCount: Int

  """
  How many organizations the person of one membership belongs to.
  
  The delivered route takes the membership's identifier, reads the row, and counts the live
  memberships the account it names holds — so the answer is a fact about the person, asked from the
  membership, which is why it is a field of its own and not a filter on the connection above. A
  membership that is not there is refused rather than answered as a zero: the delivered route raises
  instead of returning, and this field carries the same refusal through rather than flattening a
  missing row into a count of nothing.
  """
  userOrganizationOrganizationsCount(userOrganizationId: ID!): Int

  """The accounts of the caller's tenant, newest first."""
  users(filter: UserFilter, sort: [UserSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): UserConnection!

  """One account of the caller's tenant, or null when there is none."""
  user(id: ID!): User

  """
  One account of the caller's tenant, by the address it signs in with, or null when there is none.
  
  The address is how an operator reaches an account whose identifier they do not hold, and the read
  behind this field is the platform's own address look-up — the method the route calls, under the
  guards and the permission the route carries, so the scope it runs in is the route's rather than a
  second one stated here.
  """
  userByEmail(email: String!): User

  """
  The account the caller's own credential names.
  
  Deliberately unguarded beyond authentication: reading the account one is already signed in as is not
  a capability a permission grants, and the route this mirrors states none. The answer is never null —
  the credential that reached this field is the proof the row exists — so a non-null field is the
  honest shape rather than a convenience.
  """
  me: User!

  """
  How many accounts the caller's tenant holds.
  
  Nullable by design: a count is an aggregate a resource may legitimately have no answer for, and a
  non-null field would turn "not answered" into a fabricated zero. The delivered count route binds its
  query string to the store's own `where`, a shape no schema can state, so the field offers no
  narrowing of its own rather than an argument the resolver could not pass on.
  """
  userCount: Int

  """The stock locations of the caller's tenant, newest first."""
  warehouses(filter: WarehouseFilter, sort: [WarehouseSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): WarehouseConnection!

  """One stock location of the caller's tenant, or null when there is none."""
  warehouse(id: ID!): Warehouse

  """How many stock locations the caller's tenant holds."""
  warehouseCount: Int

  """
  The stock levels held at one location, each joined to the product it counts and to the variant
  levels under it.
  
  The delivered read declares no order of its own — it hands the store a criterion and takes the
  rows as they come back — so the connection states one, as it does for the location list.
  """
  warehouseInventory(warehouseId: ID!, filter: WarehouseProductFilter, sort: [WarehouseProductSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): WarehouseProductConnection!

  """
  The subscriptions of the caller's organization, newest first.
  
  A subscription's secret is answered as a fingerprint and never as material, on this field as on
  every other read of the resource.
  """
  webhookSubscriptions(filter: WebhookSubscriptionFilter, sort: [WebhookSubscriptionSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): WebhookSubscriptionConnection!

  """
  One subscription of the caller's organization, or null when there is none.
  
  A row that is not there answers `null` rather than a refusal: GraphQL has one answer for "no such
  row" on a field that may have none, and the REST route's `404` is that same fact stated in the
  other protocol's vocabulary.
  """
  webhookSubscription(id: ID!): WebhookSubscription

  """
  The delivery log of the caller's organization, newest first.
  
  The stored body of a delivery is not a member of the type this field answers; what the endpoint
  answered, how long it took and why it failed are. A subscription's deliveries are this same
  connection narrowed by `filter: { subscriptionId: … }`.
  """
  webhookDeliveries(filter: WebhookDeliveryFilter, sort: [WebhookDeliverySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int): WebhookDeliveryConnection!

  """One delivery of the caller's organization, or null when there is none."""
  webhookDelivery(id: ID!): WebhookDelivery
  collections(filter: CollectionFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CollectionConnection!
  collection(id: ID!): Collection
  collectionBySlug(slug: String!): Collection
  collectionProducts(filter: CollectionProductFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CollectionProductConnection!
  collectionVariants(filter: CollectionVariantFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CollectionVariantConnection!
  collectionChannels(filter: CollectionChannelFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CollectionChannelConnection!
  productVariantFacets(filter: TagProductVariantFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): TagProductVariantConnection!
  productPublications(filter: ProductPublicationFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): ProductPublicationConnection!
  productPublication(id: ID!): ProductPublication
  productVariantPublications(filter: ProductVariantPublicationFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): ProductVariantPublicationConnection!
  productRelations(filter: ProductRelationFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): ProductRelationConnection!
  productRelation(id: ID!): ProductRelation
  productVariantMedia(filter: ProductVariantMediaFilter, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): ProductVariantMediaConnection!

  """Price lists of the caller's organization, most useful first."""
  priceLists(filter: PriceListFilter, sort: PriceListSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PriceListConnection!

  """One price list by its identifier."""
  priceList(id: ID!): PriceList

  """Price rows of the caller's organization."""
  productPrices(filter: ProductPriceFilter, sort: ProductPriceSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): ProductPriceConnection!

  """One price row by its identifier."""
  productPrice(id: ID!): ProductPrice

  """Tax-inclusivity preferences of the caller's organization."""
  pricePreferences(filter: PricePreferenceFilter, sort: PricePreferenceSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PricePreferenceConnection!

  """One tax-inclusivity preference by its identifier."""
  pricePreference(id: ID!): PricePreference

  """Foreign-exchange rates of the caller's organization."""
  exchangeRates(filter: ExchangeRateFilter, sort: ExchangeRateSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): ExchangeRateConnection!

  """One exchange rate by its identifier."""
  exchangeRate(id: ID!): ExchangeRate

  """
  The effective price of each requested variant for one context.
  
  A variant with no price configured is omitted from the result rather than resolved to zero: a
  missing price is a configuration gap to report, never a free product. A variant priced by the
  legacy variant retail price is returned with `source: VARIANT_RETAIL_PRICE`, so a caller can
  tell a real price from a fallback.
  """
  resolvePrice(input: ResolvePriceInput!): [ResolvedPrice!]!
  taxCategories(filter: TaxCategoryFilter, sort: [TaxCategorySort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaxCategoryConnection!
  taxCategory(id: ID!): TaxCategory
  taxRates(filter: TaxRateFilter, sort: [TaxRateSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaxRateConnection!
  taxRate(id: ID!): TaxRate

  """
  The ordered parts one rate is made of; an empty list is the rate's one implied part.
  """
  taxRateParts(id: ID!): [TaxRatePart!]!

  """
  Resolves the rates that apply to a destination, most specific zone first.
  """
  resolveTaxRate(input: ResolveTaxRateInput!): [ResolvedTaxRate!]!

  """
  Computes the tax of a set of amounts.
  
  The amounts come from the caller's own totals chain; what comes back is the breakdown, in the
  shape of the platform's tax ledger rows, which the caller persists through that ledger. This
  field stores nothing — the handler it mirrors says so in as many words — which is why it is a
  query and not a mutation: no row is written and the route carries the view grant rather than an
  edit one, exactly as the resolution beside it does. A mutation here would tell a generated client
  that calling it changes something, which is the one thing this operation never does.
  """
  calculateTax(input: TaxCalculationInput!): TaxCalculation!
  taxRegimes(filter: TaxRegimeFilter, sort: [TaxRegimeSort!], page: PageInput, first: Int, after: String, last: Int, before: String, limit: Int, offset: Int, withDeleted: Boolean): TaxRegimeConnection!
  taxRegime(id: ID!): TaxRegime

  """The rates one regime selects."""
  taxRegimeRates(id: ID!): [TaxRegimeRate!]!

  """
  Resolves the regime a document is taxed under, from the party's assignment and then the destination.
  """
  resolveTaxRegime(input: ResolveTaxRegimeInput!): ResolvedTaxRegime

  """
  The levels of a location, of a variant, or of both.
  
  The page is read from the service's own paged read, which answers the window and counts the set the
  filters select on one predicate — the two things a connection owes its caller and the reason this
  field was a bare list until that read existed.
  """
  stockLevels(warehouseId: ID, variantId: ID, page: PageInput, withDeleted: Boolean): StockLevelConnection!
  stockLevel(warehouseId: ID!, variantId: ID!): StockLevel
  availableQuantity(warehouseId: ID!, variantId: ID!): Float!
  stockMovements(warehouseId: ID!, variantId: ID!, page: PageInput, withDeleted: Boolean): StockMovementConnection!
  stockMovement(id: ID!): StockMovement
  stockReservations(referenceType: String, referenceId: ID, status: String, page: PageInput, withDeleted: Boolean): StockReservationConnection!
  stockReservation(id: ID!): StockReservation
  stockTransfers(status: String, page: PageInput, withDeleted: Boolean): StockTransferConnection!
  stockTransfer(id: ID!): StockTransfer
  stockTransferLines(transferId: ID!, page: PageInput, withDeleted: Boolean): StockTransferLineConnection!
  stockTransferLine(id: ID!): StockTransferLine
  stockAlerts(variantId: ID, isActive: Boolean, page: PageInput, withDeleted: Boolean): StockAlertConnection!
  stockAlert(id: ID!): StockAlert
  stockAdjustments(warehouseId: ID, variantId: ID, status: String, page: PageInput, withDeleted: Boolean): StockAdjustmentConnection!
  stockAdjustment(id: ID!): StockAdjustment
  stockCounts(warehouseId: ID, status: String, mode: String, page: PageInput, withDeleted: Boolean): StockCountConnection!
  stockCount(id: ID!): StockCount
  stockCountLines(stockCountId: ID!, page: PageInput, withDeleted: Boolean): StockCountLineConnection!
  stockCountLine(id: ID!): StockCountLine
  stockCountVariance(stockCountId: ID!): StockCountVariance!
  channelWarehouses(channelId: ID, warehouseId: ID, page: PageInput, withDeleted: Boolean): ChannelWarehouseConnection!
  channelWarehouse(id: ID!): ChannelWarehouse

  """Zones of the caller's organization."""
  warehouseZones(filter: WarehouseZoneFilter, page: PageInput, withDeleted: Boolean): WarehouseZoneConnection!

  """One zone, with the positions inside it."""
  warehouseZone(id: ID!): WarehouseZone

  """Bins of the caller's organization."""
  warehouseBins(filter: WarehouseBinFilter, page: PageInput, withDeleted: Boolean): WarehouseBinConnection!

  """One bin, with its place in the hierarchy."""
  warehouseBin(id: ID!): WarehouseBin

  """
  Everything under a bin, itself included, read through the closure table.
  """
  warehouseBinSubtree(id: ID!, page: PageInput, withDeleted: Boolean): WarehouseBinSubtreeConnection!

  """The derived contents of a bin."""
  warehouseBinContents(id: ID!, page: PageInput): WarehouseBinContentsConnection!

  """
  Measures a requested quantity against a bin's declared capacity, in the capacity's own unit.
  """
  warehouseBinCapacity(input: WarehouseBinCapacityInput!): WarehouseBinCapacityCheck!

  """
  Bins whose capacity is declared without the unit it is counted in; pallet positions first.
  """
  warehouseBinCapacityWarnings(warehouseId: ID): [WarehouseBinCapacityWarning!]!

  """Pick waves of the caller's organization."""
  pickWaves(filter: PickWaveFilter, page: PageInput, withDeleted: Boolean): PickWaveConnection!

  """One wave, with its pick lists."""
  pickWave(id: ID!): PickWave

  """Pick lists of the caller's organization."""
  pickLists(filter: PickListFilter, page: PageInput, withDeleted: Boolean): PickListConnection!

  """One pick list, with its lines and their bins."""
  pickList(id: ID!): PickList

  """The lines of a pick list, in the order the pick path visits them."""
  pickListLines(pickListId: ID!, page: PageInput, withDeleted: Boolean): PickListLinesConnection!

  """One pick line."""
  pickListLine(id: ID!): PickListLine

  """Pack slips of the caller's organization."""
  packSlips(filter: PackSlipFilter, page: PageInput, withDeleted: Boolean): PackSlipConnection!

  """One pack slip, with the lines it covers."""
  packSlip(id: ID!): PackSlip

  """Carrier manifests of the caller's organization."""
  carrierManifests(filter: CarrierManifestFilter, page: PageInput, withDeleted: Boolean): CarrierManifestConnection!

  """One manifest, with the shipments it covers."""
  carrierManifest(id: ID!): CarrierManifest

  """List carts of the caller's organization."""
  carts(status: String, customerId: ID, email: String, page: PageInput, withDeleted: Boolean): CartConnection!

  """Read one cart with its lines, delivery choices and promotions."""
  cart(id: ID!): Cart

  """Read the cart a buyer token names."""
  cartByToken(token: String!): Cart

  """List checkout sessions."""
  checkoutSessions(cartId: ID, status: String, page: PageInput, withDeleted: Boolean): CheckoutSessionConnection!

  """Read one checkout session."""
  checkoutSession(id: ID!): CheckoutSession

  """List orders of the caller's organization."""
  orders(status: String, paymentStatus: String, fulfillmentStatus: String, customerId: ID, channelId: ID, page: PageInput, withDeleted: Boolean): OrderConnection!

  """Read one order."""
  order(id: ID!): Order

  """Read one order by its human number."""
  orderByNumber(number: String!): Order

  """The computed totals of one order."""
  orderTotals(id: ID!): OrderTotals

  """The totals history of one order, one row per version, newest first."""
  orderSummaries(orderId: ID!, page: PageInput, withDeleted: Boolean): OrderSummaryConnection!

  """The money ledger of one order."""
  orderTransactions(orderId: ID!, type: String, page: PageInput, withDeleted: Boolean): OrderTransactionConnection!

  """The timeline of one order."""
  orderHistory(orderId: ID!, page: PageInput, withDeleted: Boolean): OrderHistoryConnection!

  """The changes of one order."""
  orderChanges(orderId: ID!, status: String, page: PageInput, withDeleted: Boolean): OrderChangeConnection!

  """Read one change with its actions."""
  orderChange(id: ID!): OrderChange

  """
  Every item and credit-note item one order line was billed through, oldest first.
  """
  orderLineInvoices(orderLineId: ID!, page: PageInput, withDeleted: Boolean): OrderLineInvoiceConnection!

  """How much of one order line is left to bill."""
  orderLineInvoicingPosition(orderLineId: ID!, basisQuantity: Decimal): OrderLineInvoicingPosition!
  paymentProviders(filter: PaymentProviderFilter, sort: PaymentProviderSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentProviderConnection!
  paymentProvider(id: ID!): PaymentProvider
  paymentCollections(filter: PaymentCollectionFilter, sort: PaymentCollectionSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentCollectionConnection!
  paymentCollection(id: ID!): PaymentCollection
  paymentSessions(filter: PaymentSessionFilter, sort: PaymentSessionSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentSessionConnection!
  paymentSession(id: ID!): PaymentSession
  paymentCaptures(filter: PaymentCaptureFilter, sort: PaymentCaptureSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentCaptureConnection!
  paymentCapture(id: ID!): PaymentCapture
  refunds(filter: RefundFilter, sort: RefundSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): RefundConnection!
  refund(id: ID!): Refund
  refundReasons(filter: RefundReasonFilter, sort: RefundReasonSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): RefundReasonConnection!
  refundReason(id: ID!): RefundReason
  refundLines(filter: RefundLineFilter, sort: RefundLineSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): RefundLineConnection!
  refundLine(id: ID!): RefundLine
  paymentWebhookEvents(filter: PaymentWebhookEventFilter, sort: PaymentWebhookEventSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentWebhookEventConnection!
  paymentWebhookEvent(id: ID!): PaymentWebhookEvent
  paymentAccountHolders(filter: PaymentAccountHolderFilter, sort: PaymentAccountHolderSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentAccountHolderConnection!
  paymentAccountHolder(id: ID!): PaymentAccountHolder
  paymentMethodTokens(filter: PaymentMethodTokenFilter, sort: PaymentMethodTokenSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PaymentMethodTokenConnection!
  paymentMethodToken(id: ID!): PaymentMethodToken

  """
  One page of the promotions of the caller's organization, most specific first.
  """
  promotions(filter: PromotionFilter, sort: PromotionSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PromotionConnection!

  """One promotion by id."""
  promotion(id: ID!): Promotion

  """
  Whether a code may be used, and why not when it may not. Nothing is consumed.
  
  `cartId` is the basket the code would be applied to; it is carried so a GraphQL caller sends
  the same request the REST route takes, and the answer is computed from the coupon's own window
  and limits.
  """
  validateCoupon(code: String!, cartId: ID, customerId: ID): CouponValidationPayload!

  """One page of campaigns."""
  campaigns(filter: CampaignFilter, sort: CampaignSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CampaignConnection!

  """One campaign by id."""
  campaign(id: ID!): Campaign

  """
  One page of campaign ceilings, with the per-value consumption of a split one.
  """
  campaignBudgets(filter: CampaignBudgetFilter, sort: CampaignBudgetSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CampaignBudgetConnection!

  """One page of coupons."""
  coupons(filter: CouponFilter, sort: CouponSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): CouponConnection!

  """One coupon by id."""
  coupon(id: ID!): Coupon

  """One page of the redemption ledger, most recent first."""
  promotionUsages(filter: PromotionUsageFilter, sort: PromotionUsageSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): PromotionUsageConnection!

  """One page of gift cards."""
  giftCards(filter: GiftCardFilter, sort: GiftCardSort, page: PageInput, limit: Int, offset: Int, withDeleted: Boolean): GiftCardConnection!

  """One gift card by id."""
  giftCard(id: ID!): GiftCard

  """ The balance of a card, read from its code and answered from its ledger.
  """
  giftCardBalance(code: String!, pin: String): GiftCardBalancePayload!

  """List shipping profiles."""
  shippingProfiles(page: PageInput, withDeleted: Boolean): ShippingProfileConnection!

  """Read one shipping profile with its variants."""
  shippingProfile(id: ID!): ShippingProfile

  """
  The profile a variant ships under, which is its own attachment or the organization default.
  """
  shippingProfileForVariant(variantId: ID!): ShippingProfile

  """List shipping options."""
  shippingOptions(page: PageInput, withDeleted: Boolean): ShippingOptionConnection!

  """Read one shipping option."""
  shippingOption(id: ID!): ShippingOption

  """
  The options a cart may choose between, each with the reason it is or is not available.
  """
  shippingOptionsForContext(input: ShippingEligibilityInput): [ShippingOptionEligibility!]!

  """Price one option for a cart context."""
  shippingRate(shippingOptionId: ID!, input: ShippingEligibilityInput): ShippingRate

  """List fulfilments."""
  fulfillments(orderId: ID, status: String, warehouseId: ID, direction: String, page: PageInput, withDeleted: Boolean): FulfillmentConnection!

  """Read one fulfilment with its lines."""
  fulfillment(id: ID!): Fulfillment

  """What an order line still has to ship."""
  fulfillmentOutstanding(orderLineId: ID!): Decimal!

  """Returns of the caller's organization."""
  orderReturns(filter: OrderReturnFilter, page: PageInput, withDeleted: Boolean): OrderReturnConnection!

  """One return, with its lines and its governed reason."""
  orderReturn(id: ID!): OrderReturn

  """Governed return reasons, as a two-level tree."""
  orderReturnReasons(filter: OrderReturnReasonFilter, page: PageInput, withDeleted: Boolean): OrderReturnReasonConnection!

  """One governed return reason."""
  orderReturnReason(id: ID!): OrderReturnReason

  """Claims of the caller's organization."""
  orderClaims(filter: OrderClaimFilter, page: PageInput, withDeleted: Boolean): OrderClaimConnection!

  """One claim, with its lines and its linked return."""
  orderClaim(id: ID!): OrderClaim

  """Exchanges of the caller's organization."""
  orderExchanges(filter: OrderExchangeFilter, page: PageInput, withDeleted: Boolean): OrderExchangeConnection!

  """One exchange, with its outbound lines and its inbound return."""
  orderExchange(id: ID!): OrderExchange

  """The lines of a return."""
  orderReturnLines(returnId: ID!, page: PageInput, withDeleted: Boolean): OrderReturnLineConnection!

  """The lines of a claim."""
  orderClaimLines(claimId: ID!, page: PageInput, withDeleted: Boolean): OrderClaimLineConnection!

  """The outbound lines of an exchange."""
  orderExchangeLines(exchangeId: ID!, page: PageInput, withDeleted: Boolean): OrderExchangeLineConnection!

  """Subscription plans of the caller's organization."""
  subscriptionPlans(filter: SubscriptionPlanFilter, page: PageInput, withDeleted: Boolean): SubscriptionPlanConnection!

  """One plan."""
  subscriptionPlan(id: ID!): SubscriptionPlan

  """A plan by the code the organization knows it by."""
  subscriptionPlanByCode(code: String!): SubscriptionPlan

  """Subscriptions of the caller's organization."""
  subscriptions(filter: SubscriptionFilter, page: PageInput, withDeleted: Boolean): CustomerSubscriptionConnection!

  """One subscription, with its lines and its billing history."""
  subscription(id: ID!): CustomerSubscription

  """The recurring lines of a subscription."""
  subscriptionItems(filter: SubscriptionItemFilter, page: PageInput, withDeleted: Boolean): SubscriptionItemConnection!

  """One recurring line."""
  subscriptionItem(id: ID!): SubscriptionItem

  """Billing cycles of the caller's organization."""
  subscriptionBillings(filter: SubscriptionBillingFilter, page: PageInput, withDeleted: Boolean): SubscriptionBillingConnection!

  """One billing cycle."""
  subscriptionBilling(id: ID!): SubscriptionBilling

  """Purchase orders of the caller's organization."""
  purchaseOrders(filter: PurchaseOrderFilter, page: PageInput, withDeleted: Boolean): PurchaseOrderConnection!

  """One purchase order, with its lines, its receipts and its supplier."""
  purchaseOrder(id: ID!): PurchaseOrder

  """Goods receipts of the caller's organization."""
  goodsReceipts(filter: GoodsReceiptFilter, page: PageInput, withDeleted: Boolean): GoodsReceiptConnection!

  """One goods receipt, with its lines and the movements they produced."""
  goodsReceipt(id: ID!): GoodsReceipt

  """Standing negotiated terms of the caller's organization."""
  vendorProductTerms(filter: VendorProductTermFilter, page: PageInput, withDeleted: Boolean): VendorProductTermConnection!

  """One negotiated term."""
  vendorProductTerm(id: ID!): VendorProductTerm

  """
  What the standing agreement prices a quantity at, and what it says about delivery.
  """
  resolveVendorProductTerm(input: ResolveVendorProductTermInput!): VendorProductTermResolution!

  """Rights of the caller's organization."""
  entitlements(filter: EntitlementFilter, page: PageInput, withDeleted: Boolean): EntitlementConnection!

  """One right, with its activations and its keys."""
  entitlement(id: ID!): Entitlement

  """Activations, by right, device or state."""
  entitlementActivations(filter: EntitlementActivationFilter, page: PageInput, withDeleted: Boolean): EntitlementActivationConnection!

  """
  Issued credentials. The digest and the ciphertext are never part of the answer.
  """
  entitlementKeys(filter: EntitlementKeyFilter, page: PageInput, withDeleted: Boolean): EntitlementKeyConnection!

  """
  Whether a right may be exercised, with the code that explains the answer.
  """
  checkEntitlement(input: CheckEntitlementInput!): EntitlementCheckResult!

  """Lists the seller accounts of the caller's organization."""
  sellers(page: PageInput, withDeleted: Boolean): SellerConnection!

  """Reads one seller by id or by its human-usable code."""
  seller(idOrCode: String!): Seller

  """Reads a seller's statement over a period, in one currency."""
  sellerStatement(sellerId: ID!, currency: String): SellerStatement

  """
  Reads what a seller is owed in one currency. A negative figure is a reported fact rather than an error.
  """
  sellerBalance(sellerId: ID!, currency: String): SellerBalance

  """Lists what sellers offer."""
  sellerOfferings(page: PageInput, withDeleted: Boolean): SellerOfferingConnection!

  """
  Lists the per-seller split of orders: the ledger that says what each seller earned.
  """
  sellerTransactions(page: PageInput, withDeleted: Boolean): SellerTransactionConnection!

  """
  Reconciles the split of the orders in a window against the money they captured.
  """
  sellerSplitReconciliation(orderId: ID, sellerId: ID): [SellerSplitReconciliation!]!

  """Lists payout instructions."""
  sellerPayouts(page: PageInput, withDeleted: Boolean): SellerPayoutConnection!

  """Reads one payout with its lines."""
  sellerPayout(id: ID!): SellerPayout

  """Lists the ledger rows one payout covers."""
  sellerPayoutLines(sellerPayoutId: ID!, page: PageInput, withDeleted: Boolean): SellerPayoutLineConnection!

  """Lists what providers reported they settled."""
  sellerSettlements(page: PageInput, withDeleted: Boolean): SellerSettlementConnection!

  """Search every indexed entity the caller may see."""
  search(input: SearchInput!, page: PageInput): SearchResult!

  """Type-ahead over the indexed entities."""
  searchSuggest(q: String!, entities: [String!], limit: Int, channelId: ID): [SearchSuggestion!]!

  """
  Facet value counts for a filter set, on the same predicate a page would use.
  """
  searchFacets(input: SearchInput!): [SearchFacet!]!

  """Which entity fields are indexed, with their weights."""
  searchIndexDefinitions(entity: String, page: PageInput): SearchIndexDefinitionConnection!

  """One index definition."""
  searchIndexDefinition(id: ID!): SearchIndexDefinition

  """Index freshness and document counts."""
  searchIndexStatus(entities: [String!], page: PageInput): SearchIndexStatusConnection!
}

type Mutation {
  """
  Files a template. A caller holding MJML source wants `saveAccountingTemplate` below, which compiles
  the source and stores both copies; this field stores the columns it is given.
  """
  createAccountingTemplate(input: CreateAccountingTemplateInput!): AccountingTemplate!

  """
  Changes a template that exists. The delivered edit hands the store the stated columns beside the path
  identifier, so a member the caller leaves out is written as the column's default rather than left as
  it is — see `UpdateAccountingTemplateInput`.
  """
  updateAccountingTemplate(input: UpdateAccountingTemplateInput!): AccountingTemplate!

  """
  Removes a template outright. The row is gone and nothing reads it back, and the record that was
  rendered through it is rendered through whatever the lookup falls back to next — which is why a
  caller that means "stop using this copy" reaches for the soft removal below.
  """
  deleteAccountingTemplate(id: ID!): Boolean!

  """
  Withdraws a template without removing it: the row keeps its identifier and the delivered reads stop
  answering it.
  """
  softDeleteAccountingTemplate(id: ID!): AccountingTemplate!

  """Puts a withdrawn template back."""
  recoverAccountingTemplate(id: ID!): AccountingTemplate!

  """
  Saves the document of one template: the MJML source is stored and the compiled body is derived from
  it.
  
  The copy is resolved by its type, its language and its organization, and the row is created when the
  there is none — which is what makes this the write an editor reaches rather than either of the two
  above.
  """
  saveAccountingTemplate(input: SaveAccountingTemplateInput!): AccountingTemplate!
  assignAddressRole(input: CreateAddressRoleInput!): AddressRole!
  setAddressRoleDefault(addressId: ID!, role: AddressRoleEnum!, ownerAddressIds: [ID!]!): AddressRole!
  revokeAddressRole(addressId: ID!, role: AddressRoleEnum!): Boolean!

  """
  Records an address. A stated default is moved through the set-default operation rather than
  written, so the party's authoritative column, the address's mirror and the role row agree.
  """
  createAddress(input: CreateAddressInput!): Address!

  """
  Changes the descriptive facts of an address. A body that states a validation verdict is refused.
  """
  updateAddress(input: UpdateAddressInput!): Address!

  """
  Removes an address, softly, which is the only removal the book offers. An address the party names
  as its current default is refused: the default is moved first, which is one call.
  """
  deleteAddress(id: ID!): Address!

  """
  Removes an address under the spelling the soft-delete route uses. Removal here is soft under either
  spelling — both routes reach one operation, and both refuse an address the party names as its
  current default — so this field and `deleteAddress` are the same call. It is stated as its own field
  because a client that reads the route reaches the capability the route promises, rather than having
  to know that the other spelling of the removal is the same one.
  """
  softDeleteAddress(id: ID!): Address!

  """
  Puts a withdrawn address back, clearing the marker the withdrawal set. Restoring undoes a removal, so
  it requires the edit grant the two removals require, as the `PUT /:id/recover` route it mirrors does:
  a role that may only read the address book cannot bring back an address an editor withdrew.
  """
  recoverAddress(id: ID!): Address!

  """
  Makes an address the party's default for one role, or clears that default when `isDefault` is
  false. Setting it releases the flag from the sibling the party named before; clearing it is the
  one way to reach "this party has no default for this role" without contradicting the party's own
  column.
  """
  setDefaultAddress(input: SetDefaultAddressInput!): Address!

  """
  Removes one recorded call, or withdraws it.
  
  With no `forceDelete` — and with it stated as `false` — the row is marked and stays readable through
  the `deletedAt` filter. With it stated as `true` the row is removed from the store outright.
  """
  deleteApiCallLog(
    """The row to remove."""
    id: ID!

    """
    Remove the row outright rather than withdrawing it. Omit for a withdrawal, which is the route's own default.
    """
    forceDelete: Boolean
  ): Boolean!

  """
  Invites an employee to an appointment, and answers the row it wrote, as the inherited `POST /` does.
  """
  createAppointmentEmployee(input: CreateAppointmentEmployeeInput!): AppointmentEmployee!

  """
  Changes an invitation, and answers the row the write produced. The delivered route answers the store's
  update result — a statement about the write rather than a row — so the row is read back through the same
  service the node query uses; see `UpdateAppointmentEmployeeInput` for why an absent member is left as it
  is.
  """
  updateAppointmentEmployee(input: UpdateAppointmentEmployeeInput!): AppointmentEmployee!

  """
  Removes an invitation outright. The delivered route answers the store's delete result, which is not a
  row, so the field answers whether the removal was performed; a caller naming an invitation that is not
  there is answered with the service's own `404`-shaped refusal rather than with a success.
  """
  deleteAppointmentEmployee(id: ID!): Boolean!

  """
  Withdraws an invitation without removing it: the row keeps its identifier and the delivered list stops
  answering it. The answer is the withdrawn row, whose `deletedAt` says when it happened.
  """
  softDeleteAppointmentEmployee(id: ID!): AppointmentEmployee!

  """Puts a withdrawn invitation back."""
  recoverAppointmentEmployee(id: ID!): AppointmentEmployee!

  """
  Files a policy for one organization.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller,
  and the code the platform matches on is derived from the label rather than stated — see
  `CreateApprovalPolicyInput`.
  """
  createApprovalPolicy(input: CreateApprovalPolicyInput!): ApprovalPolicy!

  """
  Renames a policy, or rewrites the note beside its label.
  
  The command the delivered route dispatches carries the path identifier beside the body, and this
  field dispatches the same pair. The code is derived from the label again, so renaming a policy
  renames the code the platform matches on; a member the caller leaves out is written as the column's
  default rather than left as it is, because the delivered update assigns every member it holds.
  """
  updateApprovalPolicy(input: UpdateApprovalPolicyInput!): ApprovalPolicy!

  """
  Removes a policy outright. The answer says whether the removal happened rather than returning the
  removed row, because the delivered route answers the store's delete result.
  """
  deleteApprovalPolicy(id: ID!): Boolean!

  """
  Withdraws a policy without removing it: the row keeps its identifier and the delivered list stops
  answering it, while the requests already filed under it keep their history.
  """
  softDeleteApprovalPolicy(id: ID!): ApprovalPolicy!

  """Puts a withdrawn policy back, clearing the marker the withdrawal set."""
  recoverApprovalPolicy(id: ID!): ApprovalPolicy!

  """
  Registers a client. The identifier, the client secret and the code-signing secret are generated
  server-side, and the plaintext secret is answered here exactly once.
  """
  createOAuthClient(input: CreateOAuthClientInput!): OAuthClientSecret!

  """
  Changes a client that exists. `clientId` and both secrets are outside this operation — the rotation
  below is the only route that replaces a secret — and a member the caller leaves out is left as it is.
  """
  updateOAuthClient(input: UpdateOAuthClientInput!): OAuthClient!

  """
  Generates a fresh client secret, stores its hash and answers the plaintext once. Access tokens
  already issued under the previous secret stay valid until they expire; only new token exchanges are
  affected. The code-signing secret is not rotated, so an authorization code already in flight stays
  verifiable — which is the delivered operation's own boundary rather than a simplification here.
  
  The answer is the same shape the creation answers, because it is the same fact: one plaintext secret,
  once.
  """
  rotateOAuthClientSecret(id: ID!): OAuthClientSecret!

  """
  Revokes a client. The delivered route performs a withdrawal rather than a removal — the row keeps its
  identifier and its audit trail, and the lookup the authorization and token endpoints use stops
  resolving it — so a revoked third party can obtain no new authorization code and exchange no new
  token. The delivered route answers no content; this field answers the fact of the revocation, which is
  the one member of that answer a caller reads.
  """
  deleteOAuthClient(id: ID!): Boolean!

  """
  Files one slot.
  
  **A slot that conflicts with one the tenant already has is merged into it rather than filed beside
  it.** The delivered write looks for the slots whose window overlaps the stated one and widens the
  existing row to cover both, removing the rows it folded in; the stored vocabulary's merge mode is what
  decides that, and the route dispatches it with the mode that merges. The answer is the row the write
  produced.
  
  It is nullable because the write can produce nothing at all: the delivered handler returns before it
  writes when the body states no start or no end, and a non-null field would turn that empty answer into
  an execution error rather than the answer the route itself gives.
  """
  createAvailabilitySlot(input: CreateAvailabilitySlotInput!): AvailabilitySlot

  """
  Files several slots at once.
  
  The write is the command the `POST /bulk` route dispatches, and the answer is the list of rows the
  write produced — what the delivered command answers, rather than a statement about the write. Each
  entry is folded through the single-slot write above, so the merge applies entry by entry and **the
  answer is a list of rows rather than a positional echo of the input**: an entry the write answered
  nothing for is not a row, and the list may therefore be shorter than the input. A caller reads the rows,
  never the positions.
  """
  createAvailabilitySlots(input: [CreateAvailabilitySlotInput!]!): [AvailabilitySlot!]!

  """
  Writes a slot under an identifier the caller states.
  
  The call is the route's own, and it is **an upsert**: the delivered service writes a new row, or
  updates the existing one the stated identifier names, with the columns the body states. The identifier
  travels inside the input because this surface has no path to take it from — the delivered route merges
  the same value from its path into the same body.
  """
  updateAvailabilitySlot(input: UpdateAvailabilitySlotInput!): AvailabilitySlot!

  """
  Removes a slot outright.
  
  The delivered route answers the store's own delete result rather than a row, so this field answers
  whether the removal ran — the fact that call establishes.
  """
  deleteAvailabilitySlot(id: ID!): Boolean!

  """
  Withdraws a slot without removing it: the row keeps its identifier and the delivered list stops
  answering it.
  """
  softDeleteAvailabilitySlot(id: ID!): AvailabilitySlot!

  """Puts a withdrawn slot back."""
  recoverAvailabilitySlot(id: ID!): AvailabilitySlot!

  """
  Publishes a message.
  
  The write is dispatched as the same command the REST create route dispatches, which is what makes it
  the same publication rather than a second one: the handler stamps the caller's own employee and tenant
  onto the row, defaults the publication instant to now when the body states none, records the activity
  entry, and tells the audience the visibility rule selects. A message published over this protocol is
  therefore one the audience is notified about exactly as a REST caller's is.
  """
  createBroadcast(input: CreateBroadcastInput!): Broadcast!

  """
  Changes a message that exists.
  
  The same command the REST edit route dispatches, with the same body: a member the caller leaves out is
  left as it is, the polymorphic pair is not a member at all, and the write refuses a caller who is
  neither the message's publisher nor a holder of the change-employee permission — see
  `UpdateBroadcastInput`. The delivered write relabels its own failures as bad requests; that wrapper is
  the handler's translation of the failure and not part of the write, so it is not restated here.
  """
  updateBroadcast(input: UpdateBroadcastInput!): Broadcast!

  """
  Removes a message outright.
  
  This is the hard removal, and it is the one to reach for only for a row published in error: the
  employee-notification rows a delivery produced are the store's own to cascade or to leave pointing at
  an identifier that no longer resolves, and a message the audience has already read is withdrawn
  instead.
  """
  deleteBroadcast(id: ID!): Boolean!

  """
  Withdraws a message without removing the row.
  
  The delivered list stops answering it and the notifications that reference it keep resolving the
  identifier they hold. The answer is the withdrawn row, whose `deletedAt` says when it happened.
  """
  softDeleteBroadcast(id: ID!): Broadcast!

  """Puts a withdrawn message back."""
  recoverBroadcast(id: ID!): Broadcast!

  """
  Files a candidacy, and the account it belongs to.
  
  The delivered create always files an account — it resolves the candidate role, hashes the stated
  password and adds the new account to the organization — so the account's members are required here
  for the same reason they are required there.
  """
  createCandidate(input: CreateCandidateInput!): Candidate!

  """
  Files several candidacies in one call, each with the payload `createCandidate` takes.
  
  The delivered route answers the rows it created rather than a page of them, so the field does the
  same: a client that files five candidacies and reads back four has learned something the page
  envelope would have hidden.
  """
  createCandidatesBulk(input: [CreateCandidateInput!]!): [Candidate!]!

  """Edits a candidacy. A member the caller leaves out is left as it is."""
  updateCandidate(input: UpdateCandidateInput!): Candidate!

  """
  Moves a candidacy along the pipeline — the delivered route at `PUT /:id/:status`.
  
  The field answers the row the move produced, which is what both delivered handlers answer: the
  hire files the engagement and the account's new role and answers the updated candidacy, and the
  rejection writes the state, sends the message and answers the row.
  """
  updateCandidateStatus(id: ID!, status: CandidatePipelineAction!): Candidate!

  """
  Removes a candidacy outright. The rows that point at it are the store's own constraint to answer for.
  """
  deleteCandidate(id: ID!): Boolean!

  """
  Withdraws a candidacy without removing it: the account, the contact and the interviews keep the identifier they point at.
  """
  softDeleteCandidate(id: ID!): Candidate!

  """Puts a withdrawn candidacy back."""
  recoverCandidate(id: ID!): Candidate!

  """Files a document under a candidacy."""
  createCandidateDocument(input: CreateCandidateDocumentInput!): CandidateDocument!

  """Edits a document. A member the caller leaves out is left as it is."""
  updateCandidateDocument(input: UpdateCandidateDocumentInput!): CandidateDocument!

  """Removes a document outright."""
  deleteCandidateDocument(id: ID!): Boolean!

  """Withdraws a document without removing it."""
  softDeleteCandidateDocument(id: ID!): CandidateDocument!

  """Puts a withdrawn document back."""
  recoverCandidateDocument(id: ID!): CandidateDocument!

  """Files a course of study under a candidacy."""
  createCandidateEducation(input: CreateCandidateEducationInput!): CandidateEducation!

  """
  Edits a course of study. A member the caller leaves out is left as it is.
  """
  updateCandidateEducation(input: UpdateCandidateEducationInput!): CandidateEducation!

  """Removes a course of study outright."""
  deleteCandidateEducation(id: ID!): Boolean!

  """Withdraws a course of study without removing it."""
  softDeleteCandidateEducation(id: ID!): CandidateEducation!

  """Puts a withdrawn course of study back."""
  recoverCandidateEducation(id: ID!): CandidateEducation!

  """Files a prior engagement under a candidacy."""
  createCandidateExperience(input: CreateCandidateExperienceInput!): CandidateExperience!

  """
  Edits a prior engagement. A member the caller leaves out is left as it is.
  """
  updateCandidateExperience(input: UpdateCandidateExperienceInput!): CandidateExperience!

  """Removes a prior engagement outright."""
  deleteCandidateExperience(id: ID!): Boolean!

  """Withdraws a prior engagement without removing it."""
  softDeleteCandidateExperience(id: ID!): CandidateExperience!

  """Puts a withdrawn prior engagement back."""
  recoverCandidateExperience(id: ID!): CandidateExperience!

  """Files a skill under a candidacy."""
  createCandidateSkill(input: CreateCandidateSkillInput!): CandidateSkill!

  """Edits a skill. A member the caller leaves out is left as it is."""
  updateCandidateSkill(input: UpdateCandidateSkillInput!): CandidateSkill!

  """Removes a skill outright."""
  deleteCandidateSkill(id: ID!): Boolean!

  """Withdraws a skill without removing it."""
  softDeleteCandidateSkill(id: ID!): CandidateSkill!

  """Puts a withdrawn skill back."""
  recoverCandidateSkill(id: ID!): CandidateSkill!

  """Files an origin into the vocabulary the candidacies point at."""
  createCandidateSource(input: CreateCandidateSourceInput!): CandidateSource!

  """Edits an origin. A member the caller leaves out is left as it is."""
  updateCandidateSource(input: UpdateCandidateSourceInput!): CandidateSource!

  """
  Removes an origin outright. A candidacy that still points at it is the store's own constraint to refuse.
  """
  deleteCandidateSource(id: ID!): Boolean!

  """
  Withdraws an origin without removing it: the candidacies that point at it keep the identifier.
  """
  softDeleteCandidateSource(id: ID!): CandidateSource!

  """Puts a withdrawn origin back."""
  recoverCandidateSource(id: ID!): CandidateSource!

  """Schedules a sitting."""
  createCandidateInterview(input: CreateCandidateInterviewInput!): CandidateInterview!

  """Edits a sitting. A member the caller leaves out is left as it is."""
  updateCandidateInterview(input: UpdateCandidateInterviewInput!): CandidateInterview!

  """
  Removes a sitting outright. The feedbacks, the panel and the two vocabularies that point at it are the store's own constraint to answer for.
  """
  deleteCandidateInterview(id: ID!): Boolean!

  """Withdraws a sitting without removing it."""
  softDeleteCandidateInterview(id: ID!): CandidateInterview!

  """Puts a withdrawn sitting back."""
  recoverCandidateInterview(id: ID!): CandidateInterview!

  """Puts one engagement on one panel."""
  createCandidateInterviewer(input: CreateCandidateInterviewerInput!): CandidateInterviewer!

  """Edits one panel seat. A member the caller leaves out is left as it is."""
  updateCandidateInterviewer(input: UpdateCandidateInterviewerInput!): CandidateInterviewer!

  """Removes one panel seat outright."""
  deleteCandidateInterviewer(id: ID!): Boolean!

  """Withdraws one panel seat without removing it."""
  softDeleteCandidateInterviewer(id: ID!): CandidateInterviewer!

  """Puts a withdrawn panel seat back."""
  recoverCandidateInterviewer(id: ID!): CandidateInterviewer!

  """
  Puts several engagements on one panel in one call, and answers the rows it created.
  
  The delivered route answers the rows its handler wrote, in the order it wrote them, so the field
  does the same.
  """
  createCandidateInterviewersBulk(input: CreateCandidateInterviewersBulkInput!): [CandidateInterviewer!]!

  """
  Clears one sitting's panel.
  
  The delivered route answers nothing at all, so the field answers the fact that the write was
  accepted — which is what the route's own `200` states, and all it states.
  """
  deleteCandidateInterviewersByInterview(interviewId: ID!): Boolean!

  """
  Removes every panel seat one set of engagements holds.
  
  The delivered route reads the engagements out of its query string and answers nothing; the field
  states them as the identifiers the delivered handler reads, and answers the fact that the write was
  accepted.
  """
  deleteCandidateInterviewersByEmployee(employeeIds: [ID!]!): Boolean!

  """Files one technology under a sitting."""
  createCandidateTechnology(input: CreateCandidateTechnologyInput!): CandidateTechnology!

  """Edits a technology. A member the caller leaves out is left as it is."""
  updateCandidateTechnology(input: UpdateCandidateTechnologyInput!): CandidateTechnology!

  """
  Removes a technology outright. The criteria rated against it are the store's own constraint to answer for.
  """
  deleteCandidateTechnology(id: ID!): Boolean!

  """Withdraws a technology without removing it."""
  softDeleteCandidateTechnology(id: ID!): CandidateTechnology!

  """Puts a withdrawn technology back."""
  recoverCandidateTechnology(id: ID!): CandidateTechnology!

  """
  Files several technologies under one sitting, and answers the rows it created.
  """
  createCandidateTechnologiesBulk(interviewId: ID!, technologies: [String!]!): [CandidateTechnology!]!

  """
  Writes a new number on each of a verdict's technology criteria.
  
  The delivered handler dispatches one update per row without awaiting them and answers nothing, so
  the field states that the write was accepted rather than a page it would have to read before the
  writes it has just started have landed.
  """
  updateCandidateTechnologiesBulk(technologies: [CandidateTechnologyRatingInput!]!): Boolean!

  """
  Removes technologies from one sitting, or clears the sitting's whole technology vocabulary when the
  caller states no row.
  
  The delivered handler reads exactly that: the named rows when they are stated, and otherwise every
  row of the sitting.
  """
  deleteCandidateTechnologiesBulk(interviewId: ID!, technologies: [ID!]): Boolean!

  """Files one personal quality under a sitting."""
  createCandidatePersonalQuality(input: CreateCandidatePersonalQualityInput!): CandidatePersonalQuality!

  """
  Edits a personal quality. A member the caller leaves out is left as it is.
  """
  updateCandidatePersonalQuality(input: UpdateCandidatePersonalQualityInput!): CandidatePersonalQuality!

  """
  Removes a personal quality outright. The criteria rated against it are the store's own constraint to answer for.
  """
  deleteCandidatePersonalQuality(id: ID!): Boolean!

  """Withdraws a personal quality without removing it."""
  softDeleteCandidatePersonalQuality(id: ID!): CandidatePersonalQuality!

  """Puts a withdrawn personal quality back."""
  recoverCandidatePersonalQuality(id: ID!): CandidatePersonalQuality!

  """
  Files several personal qualities under one sitting, and answers the rows it created.
  """
  createCandidatePersonalQualitiesBulk(interviewId: ID!, personalQualities: [String!]!): [CandidatePersonalQuality!]!

  """
  Removes personal qualities from one sitting, or clears the sitting's whole quality vocabulary when
  the caller states no row.
  """
  deleteCandidatePersonalQualitiesBulk(interviewId: ID!, personalQualities: [ID!]): Boolean!

  """Files a verdict."""
  createCandidateFeedback(input: CreateCandidateFeedbackInput!): CandidateFeedback!

  """
  Edits a verdict, and recomputes the sitting's average from the verdicts that remain — which is what
  the delivered handler does when the body names the sitting; see `UpdateCandidateFeedbackInput`.
  """
  updateCandidateFeedback(input: UpdateCandidateFeedbackInput!): CandidateFeedback!

  """
  Removes a verdict outright, through the plain removal the controller inherits.
  
  This is *not* the same operation as the verdict-scoped removal below: this one leaves the sitting's
  average as it stands, and that one recomputes it.
  """
  deleteCandidateFeedback(id: ID!): Boolean!

  """
  Removes one verdict from one sitting and recomputes that sitting's average from the verdicts that
  remain, which is the whole of what the delivered route does.
  """
  deleteCandidateFeedbackByInterview(interviewId: ID!, feedbackId: ID!): Boolean!

  """Withdraws a verdict without removing it."""
  softDeleteCandidateFeedback(id: ID!): CandidateFeedback!

  """Puts a withdrawn verdict back."""
  recoverCandidateFeedback(id: ID!): CandidateFeedback!

  """Files one criterion rating under a verdict."""
  createCandidateCriterionsRating(input: CreateCandidateCriterionsRatingInput!): CandidateCriterionsRating!

  """
  Edits one criterion rating. A member the caller leaves out is left as it is.
  """
  updateCandidateCriterionsRating(input: UpdateCandidateCriterionsRatingInput!): CandidateCriterionsRating!

  """Removes one criterion rating outright."""
  deleteCandidateCriterionsRating(id: ID!): Boolean!

  """Withdraws one criterion rating without removing it."""
  softDeleteCandidateCriterionsRating(id: ID!): CandidateCriterionsRating!

  """Puts a withdrawn criterion rating back."""
  recoverCandidateCriterionsRating(id: ID!): CandidateCriterionsRating!

  """
  Files a whole verdict's criteria in one call, and answers the rows it created.
  """
  createCandidateCriterionsRatingsBulk(input: CreateCandidateCriterionsRatingsBulkInput!): [CandidateCriterionsRating!]!

  """
  Writes a new number on each of a verdict's criteria in one call.
  
  The delivered handler pairs the numbers with the rows by position and saves the rows, so the field
  answers the rows the write produced — which is what the delivered handler answers.
  """
  updateCandidateCriterionsRatingsBulk(input: UpdateCandidateCriterionsRatingsBulkInput!): [CandidateCriterionsRating!]!

  """
  Removes every criterion rating of one verdict.
  
  The delivered handler answers nothing, so the field answers the fact that the write was accepted.
  """
  deleteCandidateCriterionsRatingsByFeedback(feedbackId: ID!): Boolean!

  """
  Attaches a hostname to a channel. The hostname is normalised before anything is read, and the
  channel's first host is made primary whatever the input says.
  """
  createChannelDomain(input: CreateChannelDomainInput!): ChannelDomain!

  """
  Changes the flags of a hostname that exists. Setting `isPrimary` moves the flag from the current
  primary — an update is the deliberate operation, so it does not have to be told twice.
  """
  updateChannelDomain(input: UpdateChannelDomainInput!): ChannelDomain!

  """
  Detaches a hostname. Refused for the channel's only host unless `force` is stated, because a
  channel no request can resolve to cannot serve (invariant I-25).
  """
  deleteChannelDomain(id: ID!, force: Boolean): ChannelDomain!

  """
  Answers whether a hostname reaches the channel it is bound to: the same resolution the request
  guard makes, which is the half of DNS/TLS readiness this platform can decide.
  """
  verifyChannelDomain(id: ID!): ChannelDomainVerification!

  """Opens a sales context. The channel is stored `DRAFT`."""
  createChannel(input: CreateChannelInput!): Channel!

  """
  Changes the descriptive facts of a channel. A body that states a `code` is refused.
  """
  updateChannel(input: UpdateChannelInput!): Channel!

  """
  Retires a channel: it keeps its rows and stops serving. The organization's default channel is
  refused, and a hard delete is not offered — the order side's reference is `RESTRICT`.
  """
  deleteChannel(id: ID!): Channel!

  """
  Claims the organization's default channel, releasing the flag from the previous holder.
  """
  setDefaultChannel(id: ID!): Channel!

  """
  Moves the channel along its lifecycle: `DRAFT → ACTIVE | ARCHIVED`, `ACTIVE ↔ INACTIVE`,
  `ARCHIVED` terminal. Reaching `ACTIVE` requires a hostname, because a channel no request can
  resolve to would be active in the database and unreachable in fact.
  """
  setChannelStatus(input: SetChannelStatusInput!): Channel!

  """
  Replaces the channel's region links. The operation is a set, and a member the channel still names
  as its default region cannot be left out.
  """
  replaceChannelRegions(input: ReplaceChannelRegionsInput!): [ChannelRegion!]!

  """
  Posts a comment against a row of another resource.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller. A mention is published for every employee the body names, which is why that member is here
  even though nothing about it is stored on the comment row.
  """
  createComment(input: CreateCommentInput!): Comment!

  """
  Edits a comment that exists.
  
  The command the delivered route dispatches carries the path identifier beside the body, and this
  field dispatches the same pair. The row the comment is attached to cannot be changed here: the
  delivered update DTO omits both members rather than moving a comment to another resource.
  
  The answer is the edited row, read back through the same service the reads call — the handler itself
  answers the store's update result, which is a statement about the write rather than a row.
  """
  updateComment(input: UpdateCommentInput!): Comment!

  """
  Removes a comment outright. The answer says whether the removal happened rather than returning the
  removed row, because the delivered route answers the store's delete result.
  """
  deleteComment(id: ID!): Boolean!

  """
  Withdraws a comment without removing it: the row keeps its identifier and the delivered list stops
  answering it, while the replies that point at it keep their parent.
  """
  softDeleteComment(id: ID!): Comment!

  """Puts a withdrawn comment back, clearing the marker the withdrawal set."""
  recoverComment(id: ID!): Comment!

  """
  Attaches a buyer to a company account. The account must be a company and must be live, the two sides
  must be different parties, and the buyer must not already belong to a live account.
  """
  createContactBuyer(input: CreateContactBuyerInput!): ContactBuyer!

  """
  Removes a membership, softly, which is the only removal path there is: an order placed by this buyer
  must remain attributable to the membership that authorised it.
  """
  deleteContactBuyer(id: ID!): ContactBuyer!

  """
  Records a party's login. The password is hashed by the platform's password hasher before the
  service is reached; a document that states a hash instead is refused by the schema, because the
  input type has no member for one.
  """
  createContactCredential(input: CreateContactCredentialInput!): ContactCredential!

  """
  Revokes a credential, which is the only removal path there is: the row is kept so that the sessions,
  the audit entries and the export archive that referenced it stay readable, and a party that
  registers again is attached to the same contact rather than duplicated.
  """
  revokeContactCredential(id: ID!): ContactCredential!

  """
  Makes several parties members of a group by hand. The whole list is validated before any of it is
  written, and a hand-written membership of a rule-based group is refused: that group's membership is
  computed from its rules and is never materialised.
  """
  addContactGroupMembers(input: AddContactGroupMembersInput!): [ContactGroupMember!]!

  """
  Removes the membership of several parties, each of the provenance the caller states. An operator
  removes the hand-written membership and never the derived row an evaluation or an import owns.
  """
  removeContactGroupMembers(input: RemoveContactGroupMembersInput!): [ContactGroupMember!]!

  """
  Creates a static or a rule-based group. A body that states `isSystem` is refused.
  """
  createContactGroup(input: CreateContactGroupInput!): ContactGroup!

  """
  Changes a group. A group the platform maintains is refused a code change, and a static group with
  hand-written members is refused a change of kind.
  """
  updateContactGroup(input: UpdateContactGroupInput!): ContactGroup!

  """
  Soft-deletes a group, which is the only removal path there is: a price list, a promotion and a rule
  may all name it, and the rows that do keep working while it is recoverable. A group the platform
  maintains is not removable at all.
  """
  deleteContactGroup(id: ID!): ContactGroup!

  """
  Soft-deletes a group under the spelling the soft-delete route uses. Removal here is soft under either
  spelling — both routes reach one operation — so this field and `deleteContactGroup` are the same
  call, and a group the platform maintains is refused by both. It is stated as its own field because a
  client that reads the route reaches the capability the route promises, rather than having to know
  that the other spelling of the removal is the same one.
  """
  softDeleteContactGroup(id: ID!): ContactGroup!

  """
  Puts a withdrawn group back, clearing the marker the withdrawal set, under the route the CRUD base
  maps. Restoring undoes a removal, so it requires the delete grant the two removals require, as the
  `PUT /:id/recover` route it mirrors does: a role that may only read the groups cannot bring back one
  that was removed. A recovery is not announced on `contactGroupChanged`, because the route it mirrors
  announces nothing and this field adds no second behaviour of its own.
  """
  recoverContactGroup(id: ID!): ContactGroup!

  """
  Records a contact.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller, so a contact cannot be written into a tenant the caller is not acting in. The organization
  is stated, because it is a column of this row and the delivered route writes the value it is
  given.
  """
  createContact(input: CreateContactInput!): Contact!

  """
  Changes the facts of a contact that exists. A member the caller leaves out is left as it is, which
  is what the delivered partial update does.
  """
  updateContact(input: UpdateContactInput!): Contact!

  """
  Removes a contact outright: the row is gone and nothing reads it back.
  
  The two removals this resource serves are not the same operation, so both are stated rather than
  one of them being left to a client to discover on the other protocol. The party that pointed at
  the removed row keeps its own attributes — its `contactId` is nulled by the database — which is
  why a caller that means "withdraw this record" reaches for the soft removal below instead.
  """
  deleteContact(id: ID!): Boolean!

  """
  Withdraws a contact without removing it: the row stays in the table, marked as removed, and the
  recovery below reads it back. The answer is the row as it now stands, which is what the delivered
  route answers with.
  """
  softDeleteContact(id: ID!): Contact!

  """Puts a withdrawn contact back, clearing the marker the withdrawal set."""
  recoverContact(id: ID!): Contact!

  """
  Files an SMTP configuration for an organization, or for the whole installation when no organization
  is stated. The credentials travel one way: they are stored, and no read answers them back.
  """
  createSmtpSetting(input: CreateSmtpSettingInput!): SmtpSetting!

  """
  Replaces the writable columns of an SMTP configuration. The delivered edit binds the creation body
  and the identifier from the path, so a member the caller leaves out is written as the column's
  default rather than left as it is.
  """
  updateSmtpSetting(input: UpdateSmtpSettingInput!): SmtpSetting!

  """
  Removes an SMTP configuration outright. The delivered route answers with the store's deletion
  result; this field answers the fact of the removal, which is the one member of it a caller reads.
  """
  deleteSmtpSetting(id: ID!): Boolean!

  """Withdraws an SMTP configuration without removing it."""
  softDeleteSmtpSetting(id: ID!): SmtpSetting!

  """Puts a withdrawn SMTP configuration back."""
  recoverSmtpSetting(id: ID!): SmtpSetting!

  """
  Opens a connection with the configuration the caller states and reports whether the transport
  accepted it. The connection is closed immediately: nothing is sent, and nothing is stored.
  """
  validateSmtpSetting(input: ValidateSmtpSettingInput!): Boolean!

  """
  Places a widget on a dashboard.
  
  The delivered write resolves the tenant from the credential and the employee from the caller when the
  body names none, and it writes an activity-log entry for the row it created, which the field inherits
  by dispatching the same command rather than by repeating the write.
  """
  createDashboardWidget(input: CreateDashboardWidgetInput!): DashboardWidget!

  """
  Changes a placement that exists. A member the caller leaves out is left as it is.
  """
  updateDashboardWidget(input: UpdateDashboardWidgetInput!): DashboardWidget!

  """
  Removes a placement outright. The answer is whether the removal ran, which is the whole of what the
  delivered route establishes.
  """
  deleteDashboardWidget(id: ID!): Boolean!

  """
  Withdraws a placement without removing it: the row keeps its identifier and stops being answered by
  the list, and the restoration below brings it back.
  """
  softDeleteDashboardWidget(id: ID!): DashboardWidget!

  """Puts a withdrawn placement back."""
  recoverDashboardWidget(id: ID!): DashboardWidget!

  """
  Creates a dashboard.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  The delivered create also writes an activity-log entry for the row it created, which the field
  inherits by dispatching the same command rather than by repeating the write.
  """
  createDashboard(input: CreateDashboardInput!): Dashboard!

  """
  Changes a dashboard that exists.
  
  Only the user who created the dashboard may change it: the delivered service refuses everyone else
  with a `403`, and the field inherits that refusal because it calls the same service. A body that
  promotes this dashboard to be the default demotes the creator's other defaults in the same step.
  """
  updateDashboard(input: UpdateDashboardInput!): Dashboard!

  """
  Removes a dashboard outright.
  
  Only the user who created the dashboard may remove it, on the same terms as the edit above. The
  answer is whether the removal ran, which is the whole of what the delivered route establishes.
  """
  deleteDashboard(id: ID!): Boolean!

  """
  Withdraws a dashboard without removing it: the row keeps its identifier and stops being answered by
  the list, and the restoration below brings it back.
  """
  softDeleteDashboard(id: ID!): Dashboard!

  """Puts a withdrawn dashboard back."""
  recoverDashboard(id: ID!): Dashboard!

  """
  Files a deal. This is the one write of the resource that runs under the edit permission, because
  it is the one write whose route states one.
  """
  createDeal(input: CreateDealInput!): Deal!

  """
  Changes the facts of a deal. A member the caller leaves out is left as it is, because the
  delivered edit is a partial column update rather than a replacement of the row.
  """
  updateDeal(input: UpdateDealInput!): Deal!

  """
  Removes a deal outright. The two removals this resource serves are not the same operation, so both
  are stated rather than one of them being left to a client to discover on the other protocol.
  """
  deleteDeal(id: ID!): Boolean!

  """
  Withdraws a deal without removing it: the row keeps its identifier and the answer is the row as it
  now stands, which is what the delivered route answers with.
  """
  softDeleteDeal(id: ID!): Deal!

  """Puts a withdrawn deal back, clearing the marker the withdrawal set."""
  recoverDeal(id: ID!): Deal!

  """
  Changes a sent message.
  
  The delivered body declares one member, so this input declares one: a message is a record of what was
  sent, and the only fact about it the API lets a caller restate is whether it is still active. The
  answer is the row the write produced, read back through the same reader the list uses.
  """
  updateEmailHistory(input: UpdateEmailHistoryInput!): EmailHistory!

  """
  Sends a message again, to the address the named row carries, and answers the row with the outcome of
  the attempt written onto it.
  
  The delivered handler reads the organization out of its body and the message out of its path, and the
  answer is the row the attempt produced: the same message with its delivery status restated. The
  transport is resolved for the organization the caller names, which is why that one member is stated
  rather than derived — an organization that has configured its own transport sends through it.
  """
  resendEmailHistory(id: ID!, input: ResendEmailHistoryInput): EmailHistory!

  """
  Asks for the address on the caller's own account to be changed, and mails a code to the new address.
  
  The same service method the delivered route calls, with the same language: the route reads the
  `language` request header, `RequestContext.getLanguageCode()` reads the same header off the same
  request, and the mailed message is written in it.
  
  The answer says the request was received and says nothing else — not whether the address was already
  taken, not whether the mail went out. A caller that learns either of those from this answer has
  learned it about an address that may not be its own, which is what the delivered handler's own
  `finally` prevents.
  """
  requestEmailReset(input: RequestEmailResetInput!): EmailResetOutcome!

  """
  Presents the code mailed to the new address.
  
  The same service method the delivered route calls. The account and the address it is being changed
  from are read from the credential rather than stated, so the code is matched against the caller's own
  attempt.
  
  The answer is the same acknowledgement the request answers with, and for the same reason: the
  delivered handler answers it in a `finally`, so a caller cannot use this field to find out whether a
  code was live, whether it matched, or whether the address had meanwhile been taken by somebody else.
  """
  verifyEmailReset(input: VerifyEmailResetInput!): EmailResetOutcome!

  """
  Files one part of one message, with the body as the caller states it.
  
  The part is addressed by `name` and `languageCode`, and an absent `organizationId` files the
  tenant-wide copy every organization of the tenant is answered with. The delivered create compiles
  nothing: a caller holding only MJML source wants `saveEmailTemplate` below, which stores the source
  and derives the compiled body from it.
  """
  createEmailTemplate(input: CreateEmailTemplateInput!): EmailTemplate!

  """
  Changes the facts of a message part that exists. A member the caller leaves out is left as it is, and
  `organizationId: null` moves the copy to the tenant-wide one — see `UpdateEmailTemplateInput`.
  """
  updateEmailTemplate(input: UpdateEmailTemplateInput!): EmailTemplate!

  """
  Removes a message part outright. The copy is gone and nothing reads it back; the organization that
  was falling back to it is answered by the copy above it in the chain, which is why a caller that
  means "withdraw this copy" reaches for the soft removal below instead.
  """
  deleteEmailTemplate(id: ID!): Boolean!

  """
  Withdraws a copy without removing it: the row keeps its identifier and the delivered reads stop
  answering it.
  """
  softDeleteEmailTemplate(id: ID!): EmailTemplate!

  """Puts a withdrawn copy back."""
  recoverEmailTemplate(id: ID!): EmailTemplate!

  """
  Saves both parts of one message for one language: the subject text and the MJML source of the body,
  with the compiled body derived from the source.
  
  This is the write the platform's own template editor reaches, and it is the only one that compiles —
  `createEmailTemplate` and `updateEmailTemplate` store the columns they are given.
  """
  saveEmailTemplate(input: SaveEmailTemplateInput!): EmailTemplate!

  """Files an appointment and answers the row it wrote, as `POST /` does."""
  createEmployeeAppointment(input: CreateEmployeeAppointmentInput!): EmployeeAppointment!

  """
  Changes the facts of an appointment, and answers the row the write produced. The delivered route
  answers the store's update result — a statement about the write rather than a row — so the row is
  read back through the same service the node query uses; see `UpdateEmployeeAppointmentInput` for why
  an absent member is left as it is.
  """
  updateEmployeeAppointment(input: UpdateEmployeeAppointmentInput!): EmployeeAppointment!

  """
  Removes an appointment outright. The delivered route answers the store's delete result, which is not
  a row, so the field answers whether the removal was performed; a caller naming an appointment that is
  not there is answered with the service's own `404`-shaped refusal rather than with a success.
  """
  deleteEmployeeAppointment(id: ID!): Boolean!

  """
  Withdraws an appointment without removing it: the row keeps its identifier and the delivered list
  stops answering it. The answer is the withdrawn row, whose `deletedAt` says when it happened.
  """
  softDeleteEmployeeAppointment(id: ID!): EmployeeAppointment!

  """Puts a withdrawn appointment back."""
  recoverEmployeeAppointment(id: ID!): EmployeeAppointment!

  """
  Files one availability row.
  
  The write is the command the REST route dispatches, so the tenant the new row belongs to is stamped
  from the credential rather than stated by the caller, and the status is stored through the column's
  own transformer exactly as it is over REST.
  """
  createEmployeeAvailability(input: CreateEmployeeAvailabilityInput!): EmployeeAvailability!

  """
  Files several availability rows at once.
  
  The write is the command the `POST /bulk` route dispatches, and the answer is the list of rows the
  write created — what the delivered command answers, rather than a statement about the write. The
  tenant is stamped from the credential for every row, so no member of the input can place a row in a
  tenant the caller is not acting in.
  """
  createEmployeeAvailabilities(input: [CreateEmployeeAvailabilityInput!]!): [EmployeeAvailability!]!

  """
  Changes an availability row that exists.
  
  **A member that is absent is left as it is, and this is the opposite of what the create above does
  with the same vocabulary.** The delivered service reads the row under the caller's own tenant and
  then writes the columns the caller stated, so a member the body omits keeps the value the row already
  has — see `UpdateEmployeeAvailabilityInput`. The identifier travels inside the input because the
  delivered route takes it from the path and the write reads it from the body.
  
  The answer is the row as it stands after the write. The delivered call answers the store's own result
  rather than a row, so the field reads the row back through the same read the one-row query performs.
  """
  updateEmployeeAvailability(input: UpdateEmployeeAvailabilityInput!): EmployeeAvailability!

  """
  Removes an availability row outright.
  
  The delivered route answers the store's own delete result rather than a row, so this field answers
  whether the removal ran — the fact that call establishes.
  """
  deleteEmployeeAvailability(id: ID!): Boolean!

  """
  Withdraws an availability row without removing it: the row keeps its identifier and the delivered
  list stops answering it.
  """
  softDeleteEmployeeAvailability(id: ID!): EmployeeAvailability!

  """Puts a withdrawn availability row back."""
  recoverEmployeeAvailability(id: ID!): EmployeeAvailability!

  """
  Files an award for one employee of one organization.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  """
  createEmployeeAward(input: CreateEmployeeAwardInput!): EmployeeAward!

  """
  Changes the stated facts of an award that exists.
  
  The delivered edit is a partial column update rather than a replacement of the row, so a member the
  caller leaves out is left as it is — see `UpdateEmployeeAwardInput` for the members the delivered
  body accepts.
  """
  updateEmployeeAward(input: UpdateEmployeeAwardInput!): EmployeeAward!

  """
  Removes an award outright. The answer says whether the removal happened rather than returning the
  removed row, because the delivered route answers the store's delete result.
  """
  deleteEmployeeAward(id: ID!): Boolean!

  """
  Withdraws an award without removing it: the row keeps its identifier and the recovery below reads it
  back.
  """
  softDeleteEmployeeAward(id: ID!): EmployeeAward!

  """Puts a withdrawn award back, clearing the marker the withdrawal set."""
  recoverEmployeeAward(id: ID!): EmployeeAward!

  """
  Files a level for one organization.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  """
  createEmployeeLevel(input: CreateEmployeeLevelInput!): EmployeeLevel!

  """
  Changes the label of a level that exists, or files it under another organization.
  
  **This is an upsert, not a replacement.** The delivered route hands the service the body with the
  path identifier beside it, and the service saves that row — loading the existing one first when the
  identifier names one. A member the caller leaves out is therefore left as it is, which is the
  opposite of the replacement semantics some other resources in this schema have; see
  `UpdateEmployeeLevelInput`.
  """
  updateEmployeeLevel(input: UpdateEmployeeLevelInput!): EmployeeLevel!

  """
  Removes a level outright. The answer says whether the removal happened rather than returning the
  removed row, because the delivered route answers the store's delete result.
  """
  deleteEmployeeLevel(id: ID!): Boolean!

  """
  Withdraws a level without removing it: the row keeps its identifier and the recovery below reads it
  back.
  """
  softDeleteEmployeeLevel(id: ID!): EmployeeLevel!

  """Puts a withdrawn level back, clearing the marker the withdrawal set."""
  recoverEmployeeLevel(id: ID!): EmployeeLevel!

  """Files an employee's notification preferences."""
  createEmployeeNotificationSetting(input: CreateEmployeeNotificationSettingInput!): EmployeeNotificationSetting!

  """
  Changes a notification setting that exists. A member the caller leaves out is left as it is,
  because the delivered update writes the members the body states over the row it verified rather
  than replacing it — see `UpdateEmployeeNotificationSettingInput`.
  """
  updateEmployeeNotificationSetting(input: UpdateEmployeeNotificationSettingInput!): EmployeeNotificationSetting!

  """
  Removes a notification setting outright: the row is gone and nothing reads it back.
  """
  deleteEmployeeNotificationSetting(id: ID!): Boolean!

  """
  Withdraws a notification setting without removing it: the row keeps its identifier, and the
  delivered list stops answering it.
  """
  softDeleteEmployeeNotificationSetting(id: ID!): EmployeeNotificationSetting!

  """Puts a withdrawn notification setting back."""
  recoverEmployeeNotificationSetting(id: ID!): EmployeeNotificationSetting!

  """
  Marks every unread notification of the caller's own employee record as read, and reports what the
  write did.
  
  **This answer is not a resource.** Nothing in the schema names a row it could be read back from: the
  write states how many notifications it changed and whether it succeeded, and both members are the
  delivered answer's own. It is a write, so it is a root field of its own under `Mutation` rather than
  a projection of `EmployeeNotification` — and it is separately named, so a client cannot mistake it
  for a row.
  
  The receiver is never an argument. The delivered service reads the caller's own employee record from
  the credential, so a caller can only mark its own notifications read; a caller with no employee
  record or no tenant is answered a successful no-op with a count of zero rather than an update over
  a scope it did not name.
  """
  markAllEmployeeNotificationsAsRead: EmployeeNotificationMarkAllAsReadResult!

  """
  Files a notification for a receiver.
  
  `PUT /:id` and this field's sibling below edit a notification that exists; filing one is the other
  capability, and it is the route that decides whether the receiver wants it at all: the delivered
  service consults the receiver's notification settings and, when that kind of notification has been
  switched off, creates nothing. **That is a write the delivered service declines to perform**, and it
  is stated here rather than carried in the answer: the field is the non-null row the write answers
  when it writes, and the decision not to write belongs to the receiver's own setting, so no payload
  type is invented to report it.
  """
  createEmployeeNotification(input: CreateEmployeeNotificationInput!): EmployeeNotification!

  """
  Edits a notification that exists.
  
  The delivered update is a partial column write rather than a replacement: a member the caller leaves
  out is left as it is, and the write answers the store's update result rather than a row — which is
  why this field answers the row the write left behind, read through the same read the node field
  performs. A notification of another tenant, or one that is not there, is answered with the miss
  rather than with a write under an identifier the caller does not own.
  
  This field mirrors the inherited `PUT /:id`. `PUT /mark-all-read` is declared by this resource's own
  controller and wins that literal path, and the inherited route still serves every other identifier,
  so both capabilities are served over REST and both are stated here.
  """
  updateEmployeeNotification(input: UpdateEmployeeNotificationInput!): EmployeeNotification!

  """
  Removes a notification outright. The delivered route answers the store's delete result — a statement
  about the write rather than a row — so this field answers `true` once the removal was performed, which
  is the fact a client branches on.
  """
  deleteEmployeeNotification(id: ID!): Boolean!

  """
  Withdraws a notification without removing it: the row keeps its identifier and the delivered list
  stops answering it.
  """
  softDeleteEmployeeNotification(id: ID!): EmployeeNotification!

  """Puts a withdrawn notification back."""
  recoverEmployeeNotification(id: ID!): EmployeeNotification!

  """
  Records a standing cost.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller,
  so an arrangement cannot be written into a tenant the caller is not acting in. The delivered handler
  sets the row's parent to the row's own identifier, which is what makes the arrangement a family.
  """
  createEmployeeRecurringExpense(input: CreateEmployeeRecurringExpenseInput!): EmployeeRecurringExpense!

  """
  Changes a standing cost from the month the caller states onward.
  
  The delivered edit decides what the change means — it may write the beginning and the value, close the
  arrangement and open its successor, or resolve a conflict — and answers whichever row that produced,
  which is what this field answers. A member the caller omits is left as it is; see
  `UpdateEmployeeRecurringExpenseInput`, which states what an absent employee means.
  """
  updateEmployeeRecurringExpense(input: UpdateEmployeeRecurringExpenseInput!): EmployeeRecurringExpense!

  """
  Removes a standing cost, in one of the three ways the delivered input names.
  
  The answer is the one fact the removal establishes, because the delivered handler's own answer is a
  delete result, an update result or a newly opened row depending on the deletion type — three shapes,
  none of them a row this field could state. A caller that needs the arrangement as it now stands reads
  it back from the connection.
  """
  deleteEmployeeRecurringExpense(id: ID!, input: EmployeeRecurringExpenseDeleteInput!): Boolean!

  """
  Withdraws a standing cost without removing the row.
  
  The delivered list stops answering it and the rows that reference it keep resolving the identifier
  they hold. The answer is the withdrawn row; it is recoverable until it is removed outright.
  """
  softDeleteEmployeeRecurringExpense(id: ID!): EmployeeRecurringExpense!

  """Puts a withdrawn standing cost back."""
  recoverEmployeeRecurringExpense(id: ID!): EmployeeRecurringExpense!

  """Files a setting."""
  createEmployeeSetting(input: CreateEmployeeSettingInput!): EmployeeSetting!

  """
  Changes a setting that exists. A member the caller leaves out is left as it is, because the
  delivered update writes the members the body states over the row it verified rather than replacing
  it — see `UpdateEmployeeSettingInput`.
  """
  updateEmployeeSetting(input: UpdateEmployeeSettingInput!): EmployeeSetting!

  """Removes a setting outright: the row is gone and nothing reads it back."""
  deleteEmployeeSetting(id: ID!): Boolean!

  """
  Withdraws a setting without removing it: the row keeps its identifier, and the delivered list stops
  answering it.
  """
  softDeleteEmployeeSetting(id: ID!): EmployeeSetting!

  """Puts a withdrawn setting back."""
  recoverEmployeeSetting(id: ID!): EmployeeSetting!

  """
  Files an engagement, and the account it belongs to when the caller states one instead of an
  existing account's identifier.
  """
  createEmployee(input: CreateEmployeeInput!): Employee!

  """
  Files several engagements in one call, each with the payload `createEmployee` takes.
  
  The delivered route answers the rows it created rather than a page of them, so the field does the
  same: a client that files five engagements and reads back four has learned something the page
  envelope would have hidden.
  """
  createEmployeesBulk(input: [CreateEmployeeInput!]!): [Employee!]!

  """
  Edits an engagement that exists. A member the caller leaves out is left as it is; the account
  behind the engagement is not a member, because it is edited through its own surface.
  """
  updateEmployee(input: UpdateEmployeeInput!): Employee!

  """
  Edits an engagement's own profile — the facts the employee owns — under the profile permission
  alone, which is what makes this a separate field from `updateEmployee` rather than the same one
  with a different guard.
  """
  updateEmployeeProfile(input: UpdateEmployeeProfileInput!): Employee!

  """
  Removes an engagement outright. The rows that point at it are the store's own constraint to answer
  for; this field does not pre-empt it.
  """
  deleteEmployee(id: ID!, organizationId: ID): Boolean!

  """
  Withdraws an engagement without removing it: the row keeps its identifier, and the times, expenses
  and sales attributed to it keep pointing at the same person.
  """
  softDeleteEmployee(id: ID!, organizationId: ID): Employee!

  """Puts a withdrawn engagement back."""
  recoverEmployee(id: ID!, organizationId: ID): Employee!

  """
  Subscribes the caller to an entity, through the command the delivered route dispatches.
  
  The tenant and the employee who is to be told are stamped from the credential and are never stated
  by the caller. A subscription for the same record and the same employee that already exists is
  answered with the row that exists rather than with a second one beside it, because the delivered
  command handler looks it up before it writes.
  """
  createEntitySubscription(input: CreateEntitySubscriptionInput!): EntitySubscription!

  """
  Changes a subscription that exists.
  
  **A member the caller leaves out is left as it is.** The delivered route reads the row the
  identifier names — proving it is the caller's own — and writes the members the body states to it,
  so this is a column update and not a replacement; see `UpdateEntitySubscriptionInput`.
  
  The answer is the row the write produced, read back through the read the node query performs. The
  delivered route answers the store's own update result — a statement about the write, `{ affected }`
  — which is not a row, and a field declared as the object type cannot answer it.
  """
  updateEntitySubscription(input: UpdateEntitySubscriptionInput!): EntitySubscription!

  """
  Unsubscribes the caller from one entity: the subscription for that record is removed outright.
  
  The three arguments are the members the delivered route binds out of its query string, and the
  employee and the tenant the criterion also carries are derived from the credential rather than
  stated. The answer says whether the removal happened, because the delivered route answers the
  store's delete result rather than the removed row.
  """
  unsubscribeFromEntity(id: ID!, entity: String, entityId: ID, organizationId: ID): Boolean!

  """
  Withdraws a subscription without removing it: the row keeps its identifier and the recovery below
  reads it back, and the delivered list stops answering it.
  """
  softDeleteEntitySubscription(id: ID!): EntitySubscription!

  """
  Puts a withdrawn subscription back, clearing the marker the withdrawal set.
  """
  recoverEntitySubscription(id: ID!): EntitySubscription!

  """
  Files a policy.
  
  The same service call the REST create route makes, with the same payload. The tenant the new row
  belongs to is stamped from the credential and is never stated by the caller, so a policy cannot be
  written into a tenant the caller is not acting in.
  """
  createEquipmentSharingPolicy(input: CreateEquipmentSharingPolicyInput!): EquipmentSharingPolicy!

  """
  Changes a policy that exists.
  
  The same service method the REST edit route calls, which reads the row before it writes: a policy of
  another tenant, or one that is not there, is answered with the miss rather than with a write against
  a row the caller does not own. A member the caller leaves out is left as it is — the delivered write
  updates the columns the body states rather than replacing the row — and the answer is the row read
  back, because the store's own answer to a partial update is a statement about the write rather than a
  row.
  """
  updateEquipmentSharingPolicy(input: UpdateEquipmentSharingPolicyInput!): EquipmentSharingPolicy!

  """
  Removes a policy outright.
  
  This is the hard removal, and it is the one to reach for only for a row filed in error: the sharing
  periods that point at a policy are the store's own to refuse or to cascade, and this field does not
  pre-empt that decision.
  """
  deleteEquipmentSharingPolicy(id: ID!): Boolean!

  """
  Withdraws a policy without removing the row.
  
  The delivered list stops answering it and the periods that point at it keep resolving the identifier
  they hold. The answer is the withdrawn row, whose `deletedAt` says when it happened.
  """
  softDeleteEquipmentSharingPolicy(id: ID!): EquipmentSharingPolicy!

  """Puts a withdrawn policy back."""
  recoverEquipmentSharingPolicy(id: ID!): EquipmentSharingPolicy!

  """
  Files a sharing period.
  
  The organization is stated because the delivered route reads it from its own path segment and the
  handler stamps the row with it; the caller's tenant is stamped by the same handler and is never
  stated here. The delivered write records the approval row that stands beside the sharing in the same
  call, so a period filed over this protocol is one an approver can act on exactly as a REST caller's
  is.
  """
  createEquipmentSharing(organizationId: ID!, input: CreateEquipmentSharingInput!): EquipmentSharing!

  """
  Changes a period that exists.
  
  The delivered edit is the handler's own read-then-replace, and the identifier is what it reads the row
  by: a period of another organization, or one that is not there, is answered with the miss rather than
  with a write under an identifier the caller does not own. See `UpdateEquipmentSharingInput` for what a
  member the caller leaves out does, and for why `status` is stated as required there.
  """
  updateEquipmentSharing(input: UpdateEquipmentSharingInput!): EquipmentSharing!

  """
  Approves a filed period.
  
  The delivered route dispatches the same status command the refusal below dispatches, with the
  approved value of the contracts' own vocabulary. The command moves the sharing row and the approval
  row together, so the two agree afterwards — which is why this is one field per decision rather than a
  single field taking a status number.
  """
  approveEquipmentSharing(id: ID!): EquipmentSharing!

  """
  Refuses a filed period.
  
  The same command as the approval above with the refused value; the answer is the row the command
  saved, whose `status` says which decision it was.
  """
  refuseEquipmentSharing(id: ID!): EquipmentSharing!

  """
  Removes a period outright, with the approval row recorded beside it.
  
  This is the hard removal, and it is the one to reach for only for a row filed in error: the delivered
  service removes the approval row in the same call, scoped to the caller's tenant, so a removal over
  this protocol leaves nothing behind for an approver to act on.
  """
  deleteEquipmentSharing(id: ID!): Boolean!

  """
  Withdraws a period without removing the row.
  
  The delivered list stops answering it and the asset it points at keeps resolving the identifier it
  holds. The answer is the withdrawn row, whose `deletedAt` says when it happened.
  """
  softDeleteEquipmentSharing(id: ID!): EquipmentSharing!

  """Puts a withdrawn period back."""
  recoverEquipmentSharing(id: ID!): EquipmentSharing!

  """
  Files an asset.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller,
  so an asset cannot be written into a tenant the caller is not acting in. The write is the same
  service call the REST create route makes, and it is a save rather than an insert because that is
  what the delivered route needs: the facets are a relation, and the relation is written by the save.
  """
  createEquipment(input: CreateEquipmentInput!): Equipment!

  """
  Changes an asset that exists.
  
  The delivered edit carries the identifier in the path and spreads the body beside it into the same
  save the create uses, and that save merges what it is given onto the row the store holds: a member the
  caller leaves out is left as it is, which is what `UpdateEquipmentInput` states at length. The
  delivered write refuses a row of another tenant before it persists anything, and that refusal reaches
  the caller as it is rather than being relabelled here.
  """
  updateEquipment(input: UpdateEquipmentInput!): Equipment!

  """
  Removes an asset outright, with the sharing rows that belong to it.
  
  This is the hard removal, and it is the one to reach for only for a row filed in error: the sharing
  periods that point at an asset are the store's own to cascade, and an asset the organization still
  hands out is withdrawn instead.
  """
  deleteEquipment(id: ID!): Boolean!

  """
  Withdraws an asset without removing the row.
  
  The delivered list stops answering it and the sharing rows that point at it keep resolving the
  identifier they hold. The answer is the withdrawn row, whose `deletedAt` says when it happened; it is
  recoverable until it is removed outright.
  """
  softDeleteEquipment(id: ID!): Equipment!

  """Puts a withdrawn asset back."""
  recoverEquipment(id: ID!): Equipment!

  """
  Re-drives one consumer's record for an event: the operator's first move.
  
  The attempt budget is reset to zero, the last error is cleared and the acknowledgement is
  withdrawn, so the retry scan claims the record exactly as it claims one whose consumer crashed
  mid-delivery. Nothing is re-published here and no consumer is invoked in this call — the record is
  the queue, and the scan that reads `PENDING` rows is what re-drives it — which is also why the move
  is safe to repeat. The answer is the record as it stands afterwards.
  """
  replayEventDelivery(id: ID!): EventDelivery!

  """
  Dead-letters one consumer's record for an event: the operator's second move.
  
  The record is stopped with the caller's reason, which is written onto its `lastError` and is the
  diagnosis the next operator reads. The move is a deliberate decision to lose one reaction to a
  real fact, so it is recorded rather than performed silently. The answer is the record as it stands
  afterwards.
  """
  markEventDeliveryDead(input: MarkEventDeliveryDeadInput!): EventDelivery!

  """
  Files a meeting length.
  
  The write is dispatched as the same command the REST create route dispatches, with the same payload —
  which is the only way to reach this write at all: the handler resolves the organization and the
  employee the body names before it builds the row, so calling the service beside the command would skip
  a step the REST route performs.
  """
  createEventType(input: CreateEventTypeInput!): EventType!

  """
  Changes a meeting length that exists.
  
  The delivered edit reaches the same service call the create route reaches — the entity spread beside
  the identifier the route read from its path — and that is *not* the command above: no handler runs on
  this route, so the write merges the stated members onto the row the store holds and leaves the rest as
  they are. See `UpdateEventTypeInput`.
  """
  updateEventType(input: UpdateEventTypeInput!): EventType!

  """
  Removes a meeting length outright.
  
  This is the hard removal, and the delivered store rather than this field decides what still pointing at
  the row means: the answer is the one fact the removal establishes, that it ran, and a constraint the
  store raises reaches the caller as it is. A length the organization means to stop offering is withdrawn
  instead, which leaves every row that names it resolvable.
  """
  deleteEventType(id: ID!): Boolean!

  """
  Withdraws a meeting length without removing the row.
  
  The delivered list stops answering it and the rows that reference it keep resolving the identifier
  they hold. The answer is the withdrawn row, whose `deletedAt` says when it happened.
  """
  softDeleteEventType(id: ID!): EventType!

  """Puts a withdrawn meeting length back."""
  recoverEventType(id: ID!): EventType!

  """
  Files a category into the tenant's vocabulary.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller,
  so a category cannot be written into a tenant the caller is not acting in. The name is the one
  member the delivered body validation requires.
  """
  createExpenseCategory(input: CreateExpenseCategoryInput!): ExpenseCategory!

  """
  Renames a category, or files it under another organization.
  
  The delivered edit reaches the same write the create does, carrying the identifier in the path and
  the body together, so a member the caller leaves out is left as it is — see
  `UpdateExpenseCategoryInput`, which also states what an identifier naming no row does.
  """
  updateExpenseCategory(input: UpdateExpenseCategoryInput!): ExpenseCategory!

  """
  Removes a category outright.
  
  This is the hard removal, and it is the one to reach for only for an entry filed in error: a category
  an expense still points at is the store's own constraint to refuse, and this field does not pre-empt
  it.
  """
  deleteExpenseCategory(id: ID!): Boolean!

  """
  Withdraws a category without removing the row.
  
  The delivered list stops answering it and the expenses filed under it keep resolving the identifier
  they hold. The answer is the withdrawn row; it is recoverable until it is removed outright.
  """
  softDeleteExpenseCategory(id: ID!): ExpenseCategory!

  """Puts a withdrawn category back."""
  recoverExpenseCategory(id: ID!): ExpenseCategory!

  """
  Records a cost.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller, so an expense cannot be written into a tenant the caller is not acting in. The amount is
  the exact decimal the column stores, and it and the currency are the two members the delivered body
  validation requires.
  """
  createExpense(input: CreateExpenseInput!): Expense!

  """
  Changes a cost that exists.
  
  The delivered edit reaches the same write the create does, carrying the identifier in the path and
  the body together, so a member the caller leaves out is left as it is — see `UpdateExpenseInput`,
  which also states what an identifier naming no row does. The delivered route relabels every failure
  of this write as a bad request; this field answers the failure the service raised instead, so a
  caller reads what actually happened rather than a label.
  """
  updateExpense(input: UpdateExpenseInput!): Expense!

  """
  Removes an expense outright.
  
  This is the hard removal, and it is the one to reach for only for a row recorded in error: a cost a
  document already accounts for is withdrawn instead. The employee is the optional query member the
  delivered route reads, and the row it names is the one whose recorded average the delivered handler
  refreshes beside the removal; a caller that states none gets the removal without the refresh.
  """
  deleteExpense(id: ID!, employeeId: ID): Boolean!

  """
  Withdraws an expense without removing the row.
  
  The delivered list stops answering it and the rows that reference it keep resolving the identifier
  they hold. The answer is the withdrawn row; it is recoverable until it is removed outright.
  """
  softDeleteExpense(id: ID!): Expense!

  """Puts a withdrawn expense back."""
  recoverExpense(id: ID!): Expense!

  """
  Marks a row as a favorite.
  
  The tenant the marker belongs to is stamped from the credential and is never stated by the caller;
  the employee is the caller's own record when the credential carries one, and `employeeId` is the
  fallback that files an organization-level marker.
  """
  createFavorite(input: CreateFavoriteInput!): Favorite!

  """
  Changes a marker that exists.
  
  A member the caller leaves out is left as it is, because the delivered edit is a partial update
  rather than a replacement — see `UpdateFavoriteInput`.
  """
  updateFavorite(input: UpdateFavoriteInput!): Favorite!

  """
  Removes a marker outright.
  
  The answer says whether the removal happened rather than returning the removed row, because the
  delivered route answers the store's delete result. No extra scope is stated here: the delivered
  service refuses a caller who does not own the row and lets an administrator through, and it decides
  that from the credential, so the field restates none of it.
  """
  deleteFavorite(id: ID!): Boolean!

  """
  Withdraws a marker without removing it: the row keeps its identifier and the marked row keeps
  pointing at it, and the delivered list stops answering it.
  """
  softDeleteFavorite(id: ID!): Favorite!

  """Puts a withdrawn marker back, clearing the marker the withdrawal set."""
  recoverFavorite(id: ID!): Favorite!

  """
  Switches a capability on or off for a scope, and answers whether the write was performed.
  
  The answer is the delivered route's own: the command behind it reports success as a boolean rather
  than returning the row, so this field states the same fact in the same shape.
  """
  toggleFeature(input: UpdateFeatureToggleInput!): Boolean!

  """Files a policy, and answers the row it wrote."""
  createGoalGeneralSetting(input: CreateGoalGeneralSettingInput!): GoalGeneralSetting!

  """
  Changes a policy. The delivered route is an update-through-create, so a member the caller leaves out
  is left as it is; see `UpdateGoalGeneralSettingInput`.
  """
  updateGoalGeneralSetting(input: UpdateGoalGeneralSettingInput!): GoalGeneralSetting!

  """
  Removes a policy outright. The delivered route answers the store's delete result rather than a row,
  so this field answers whether the removal was performed.
  """
  deleteGoalGeneralSetting(id: ID!): Boolean!

  """
  Withdraws a policy without removing it. The answer is the withdrawn row, whose `deletedAt` says when
  it happened.
  """
  softDeleteGoalGeneralSetting(id: ID!): GoalGeneralSetting!

  """Puts a withdrawn policy back."""
  recoverGoalGeneralSetting(id: ID!): GoalGeneralSetting!

  """Files a catalogue entry, and answers the row it wrote."""
  createGoalKpiTemplate(input: CreateGoalKPITemplateInput!): GoalKPITemplate!

  """
  Changes a catalogue entry, and answers the row the write produced. A member the caller leaves out is
  left as it is, because the delivered edit is a partial column update rather than a replacement; see
  `UpdateGoalKPITemplateInput`.
  """
  updateGoalKpiTemplate(input: UpdateGoalKPITemplateInput!): GoalKPITemplate!

  """
  Removes a catalogue entry outright. The delivered route answers the store's delete result rather
  than a row, so this field answers whether the removal was performed.
  """
  deleteGoalKpiTemplate(id: ID!): Boolean!

  """
  Withdraws a catalogue entry without removing it. The answer is the withdrawn row, whose `deletedAt`
  says when it happened.
  """
  softDeleteGoalKpiTemplate(id: ID!): GoalKPITemplate!

  """Puts a withdrawn catalogue entry back."""
  recoverGoalKpiTemplate(id: ID!): GoalKPITemplate!

  """Files a measure, and answers the row it wrote."""
  createGoalKpi(input: CreateGoalKPIInput!): GoalKPI!

  """
  Changes a measure. The delivered route is an update-through-create, so a member the caller leaves
  out is left as it is; see `UpdateGoalKPIInput`.
  """
  updateGoalKpi(input: UpdateGoalKPIInput!): GoalKPI!

  """
  Removes a measure outright. The delivered route answers the store's delete result rather than a row,
  so this field answers whether the removal was performed.
  """
  deleteGoalKpi(id: ID!): Boolean!

  """
  Withdraws a measure without removing it: the row keeps its identifier, the key results that track it
  keep pointing at it, and the delivered list stops answering it.
  """
  softDeleteGoalKpi(id: ID!): GoalKPI!

  """Puts a withdrawn measure back."""
  recoverGoalKpi(id: ID!): GoalKPI!

  """Files a catalogue entry, and answers the row it wrote."""
  createGoalTemplate(input: CreateGoalTemplateInput!): GoalTemplate!

  """
  Changes a catalogue entry, and answers the row the write produced. A member the caller leaves out is
  left as it is, because the delivered edit is a partial column update rather than a replacement; see
  `UpdateGoalTemplateInput`.
  """
  updateGoalTemplate(input: UpdateGoalTemplateInput!): GoalTemplate!

  """
  Removes a catalogue entry outright. The delivered route answers the store's delete result rather
  than a row, so this field answers whether the removal was performed.
  """
  deleteGoalTemplate(id: ID!): Boolean!

  """
  Withdraws a catalogue entry without removing it. The answer is the withdrawn row, whose `deletedAt`
  says when it happened.
  """
  softDeleteGoalTemplate(id: ID!): GoalTemplate!

  """Puts a withdrawn catalogue entry back."""
  recoverGoalTemplate(id: ID!): GoalTemplate!

  """Opens a period, and answers the row it wrote."""
  createGoalTimeFrame(input: CreateGoalTimeFrameInput!): GoalTimeFrame!

  """
  Changes a period. The delivered route is an update-through-create, so a member the caller leaves out
  is left as it is; see `UpdateGoalTimeFrameInput`.
  """
  updateGoalTimeFrame(input: UpdateGoalTimeFrameInput!): GoalTimeFrame!

  """
  Removes a period outright. The delivered route answers the store's delete result rather than a row,
  so this field answers whether the removal was performed.
  """
  deleteGoalTimeFrame(id: ID!): Boolean!

  """
  Withdraws a period without removing it. The answer is the withdrawn row, whose `deletedAt` says when
  it happened.
  """
  softDeleteGoalTimeFrame(id: ID!): GoalTimeFrame!

  """Puts a withdrawn period back."""
  recoverGoalTimeFrame(id: ID!): GoalTimeFrame!

  """Files an objective, and answers the row it wrote."""
  createGoal(input: CreateGoalInput!): Goal!

  """
  Edits an objective that exists. The delivered route reads the row before it writes — so an
  objective of another tenant, or one that is not there, is answered with the miss rather than with a
  write that would recreate the row under an identifier the caller does not own — and then merges the
  stated members onto it. A member the caller leaves out is left as it is; see
  `UpdateGoalInput`.
  """
  updateGoal(input: UpdateGoalInput!): Goal!

  """
  Removes an objective outright, with the key results the delivered cascade takes with it. A
  reference the store's own constraints refuse is the store's to refuse; this field does not
  pre-empt it, and the delivered route answers the store's delete result rather than a row, which is
  why this field answers whether the removal was performed.
  """
  deleteGoal(id: ID!): Boolean!

  """
  Withdraws an objective without removing it: the row keeps its identifier, its key results keep
  pointing at it, and the delivered list stops answering it. The answer is the withdrawn row, whose
  `deletedAt` says when it happened.
  """
  softDeleteGoal(id: ID!): Goal!

  """Puts a withdrawn objective back."""
  recoverGoal(id: ID!): Goal!

  """
  Releases a key, so the next attempt under it is a true first attempt.
  
  The row is removed rather than marked, because a marked row would still occupy the unique tuple and
  the retry the operator is unblocking would be refused as a reused key. The move is refused while a
  live claim holds the key: removing a claim whose work is still executing would let the retry start a
  second run of that work. An operator who genuinely needs that waits for the lease to go stale, which
  happens on its own — the row's `lockedAt` and the kernel's stale window say how long that is.
  
  The answer is the row as it stood, without the response it held.
  """
  releaseIdempotencyKey(id: ID!): IdempotencyKey!

  """
  Records an image whose bytes are already held. This is the delivered create route and not the
  upload: see the note above for why the upload has no field.
  """
  createImageAsset(input: CreateImageAssetInput!): ImageAsset!

  """
  Changes an image. A member the caller leaves out is left as it is, because the delivered edit is a
  partial column update rather than a replacement of the row.
  """
  updateImageAsset(input: UpdateImageAssetInput!): ImageAsset!

  """
  Removes an image outright, with the derived renditions that belong to it. A `400` is raised when a
  product still points at the image, and that refusal is let through.
  
  The two removals this resource serves are not the same operation, so both are stated: this one
  removes the row, and the withdrawal below keeps it and clears whatever marker it set.
  """
  deleteImageAsset(id: ID!): ImageAsset!

  """
  Withdraws an image without removing it. The answer is the row as it now stands, which is what the
  delivered route answers with.
  """
  softDeleteImageAsset(id: ID!): ImageAsset!

  """Puts a withdrawn image back, clearing the marker the withdrawal set."""
  recoverImageAsset(id: ID!): ImageAsset!

  """
  Records an income.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller, so
  an income cannot be written into a tenant the caller is not acting in. The amount is the exact decimal
  the column stores, and it, the party and the currency are the members the delivered body validation
  requires.
  """
  createIncome(input: CreateIncomeInput!): Income!

  """
  Changes an income that exists.
  
  The delivered edit reaches the same write the create does, carrying the identifier in the path and the
  body together, so a member the caller leaves out is left as it is — see `UpdateIncomeInput`, which also
  states what an identifier naming no row does.
  """
  updateIncome(input: UpdateIncomeInput!): Income!

  """
  Removes an income outright.
  
  This is the hard removal, and it is the one to reach for only for a row recorded in error: an amount a
  document already accounts for is withdrawn instead. The employee is the optional query member the
  delivered route reads, and the row it names is the one whose recorded averages the delivered handler
  refreshes beside the removal; a caller that states none gets the removal without the refresh.
  """
  deleteIncome(id: ID!, employeeId: ID): Boolean!

  """
  Withdraws an income without removing the row.
  
  The delivered list stops answering it and the rows that reference it keep resolving the identifier they
  hold. The answer is the withdrawn row; it is recoverable until it is removed outright.
  """
  softDeleteIncome(id: ID!): Income!

  """Puts a withdrawn income back."""
  recoverIncome(id: ID!): Income!

  """
  Stores the tied decisions of one configured integration: the rows that travel with a decision the
  caller already made. The delivered write stores what it is given rather than merging it, and it
  answers the rows it stored.
  """
  updateIntegrationEntitySettingsTied(
    """
    The configured integration whose tied decisions are written. The delivered route takes it in the path.
    """
    integrationId: ID!

    """The tied decisions to store, one member per row type."""
    input: [IntegrationEntitySettingTiedInput!]!
  ): [IntegrationEntitySettingTied!]!

  """
  Stores the synchronisation decisions of one configured integration: the rows the caller states, with
  the tied rows underneath them. The delivered write is a replacement of what it is given rather than
  a merge, so a row type the caller leaves out is a row type the integration no longer synchronises.
  """
  updateIntegrationEntitySettings(
    """
    The configured integration whose decisions are written. The delivered route takes it in the path.
    """
    integrationId: ID!

    """The decisions to store, one member per row type."""
    input: [IntegrationEntitySettingInput!]!
  ): [IntegrationEntitySetting!]!

  """
  Stores one credential of a configured integration under the identifier the caller states.
  
  The value travels one way: it is written and never answered back, because the row's value is what
  the delivered projection withholds.
  """
  updateIntegrationSetting(input: UpdateIntegrationSettingInput!): IntegrationSetting!

  """
  Connects an integration: the delivered creation, which stamps the caller's tenant and files the
  credentials and decisions the caller states with it.
  """
  createIntegrationTenant(input: CreateIntegrationTenantInput!): IntegrationTenant!

  """
  Changes a connection that exists. A member the caller leaves out is left as it is, because the
  delivered edit is a partial column update rather than a replacement of the row.
  """
  updateIntegrationTenant(input: UpdateIntegrationTenantInput!): IntegrationTenant!

  """
  Removes a connection outright, with the credentials and decisions that belong to it. The
  organization is required, as it is on the delivered route.
  """
  deleteIntegrationTenant(id: ID!, organizationId: ID!, tenantId: ID): Boolean!

  """
  Withdraws a connection without removing it: the row keeps its identifier and its synchronisation
  history, and the delivered reads stop answering it.
  """
  softDeleteIntegrationTenant(id: ID!): IntegrationTenant!

  """Puts a withdrawn connection back."""
  recoverIntegrationTenant(id: ID!): IntegrationTenant!

  """
  Sends invitations to a list of addresses.
  
  The same command the delivered route dispatches, carrying the same language: the route reads the
  `language` request header through its own decorator, and `RequestContext.getLanguageCode()` reads
  that same header off the same request — the bootstrap mounts a request context on the GraphQL
  endpoint as well as on the prefixed routes — so a caller asking the same question over either
  protocol is mailed the same message in the same language. Without a header it answers English, which
  is the decorator's own default.
  
  The answer is the envelope the delivered handler produces: the invitations it wrote, how many there
  were, and how many addresses it left alone because they were already invited. The last of the three
  is the reason the answer is a type of its own rather than a list of invitations — an address that was
  skipped is a fact about the call, and a client that received only the rows could not tell it from an
  address that was never named.
  """
  sendInvites(input: CreateInvitesInput!): InviteSendOutcome!

  """
  Mails a fresh credential for an invitation that is still pending.
  
  The same command the delivered route dispatches, in the caller's language, and the delivered route's
  declared answer is the store's update result or the invitation. **This field answers the invitation**
  — not the update result, which is a statement about the write rather than a row: it names how many
  rows the store touched and carries none of what they now hold, so a client could not read the new
  expiry or state of the invitation it just refreshed. The field therefore re-reads the row it named
  after the command has run, and answers that.
  
  The read is by identifier through the same service the register reads through, so an identifier that
  names nothing is a miss rather than an empty invitation — which is the refusal the delivered re-read
  would raise for the same call.
  """
  resendInvite(input: ResendInviteInput!): Invite!

  """
  Accepts an invitation: the invited person registers, and the credential is spent.
  
  The same command the delivered route dispatches, with the same two arguments: the body, and the
  origin the transport carried — the delivered route reads the request's `origin` header and states it
  as `originalUrl`, which is what the message links are composed from, so the field reads it off the
  same request rather than asking the caller to state it a second time.
  
  **The answer is the kernel's `JSON` scalar, and that is a statement rather than a shortcut.** What
  the command produces depends on the invitation's own kind: an account with its access and refresh
  tokens, an employee, or a candidate — three different rows, decided by the role the invitation
  carries rather than by anything the caller states. No single schema type can state that union, and
  naming one of the three would be a lie about the other two; the document is carried through
  unchanged instead, and a client reads the members it asked for.
  
  The credential is spent exactly once: the delivered handlers claim the invitation before they
  register anybody, so two acceptances racing on one invitation produce one account and one refusal
  rather than two accounts.
  """
  acceptInvite(input: AcceptInviteInput!): JSON!

  """
  Refuses an invitation.
  
  The same command the delivered route dispatches, with the same single argument — the body, and no
  language, because the delivered handler reads none. The answer is what the handler answers: the
  invitation it refused. A refusal that loses a race against an acceptance is a `409` rather than a
  write, and this field lets that refusal through unchanged.
  """
  rejectInvite(input: RejectInviteInput!): JSON!

  """
  Takes up a contact invitation and provisions the contact's own workspace.
  
  The same command the delivered route dispatches, with the origin the transport carried stated the
  way the route states it: the route writes the request's `Origin` header onto the body before it
  dispatches, so the field reads that same header off the same request.
  
  The answer is the kernel's `JSON` scalar. This acceptance provisions a tenant, its roles and
  features, an organization and the account that administers it, and returns the store's update result
  for the invitation — a shape that states nothing about the workspace that was built. The document is
  carried through unchanged, and a client that needs the invitation's own state reads it through
  `invites`.
  """
  acceptOrganizationContactInvite(input: AcceptOrganizationContactInviteInput!): JSON!

  """
  Invites an existing organization contact to take up a workspace of its own.
  
  The same command the delivered route dispatches, with the same body: the identifier from the path,
  the origin from the transport's own header, and the inviter read from the credential rather than
  stated by the caller — the delivered route passes `request.user` there, which is the account the
  request was authenticated as, and `RequestContext.currentUser()` reads that same account off the same
  request.
  
  **The answer is the kernel's `JSON` scalar.** The route answers an `IOrganizationContact` — a type
  this schema does not declare, because the contact is the organization-contact domain's resource and
  is read on that domain's own fields — so carrying it as a document says plainly that the member to
  read next is served there, rather than declaring a second, partial copy of that type here.
  """
  inviteOrganizationContact(id: ID!): JSON!

  """
  Removes an invitation outright.
  
  The answer says whether the removal happened rather than returning the removed row, because the
  delivered route answers the store's delete result — a statement about the write and not a row.
  """
  deleteInvite(id: ID!): Boolean!

  """
  Accepts or refuses an invitation on the caller's own behalf.
  
  The same service method the delivered route calls, with the same four arguments: the invitation, the
  move, the origin the transport carried, and the caller's language. The route reads the move from its
  path and the service narrows the row to the caller's own address before it writes anything, so this
  is not a way to answer somebody else's invitation.
  
  The action is carried as text rather than declared as an enum of this domain's own, because the
  vocabulary belongs to the contracts and the two members the delivered writer acts on are exactly the
  two the platform names — see `handleInvitationResponse`'s own service for the transition each makes.
  
  The answer is the invitation as it now stands: the delivered method re-reads the row after the move
  and answers it, which is the state a caller needs and not a statement about the write.
  """
  handleInvitationResponse(id: ID!, action: String!): Invite!

  """Files a log entry."""
  createInvoiceEstimateHistory(input: CreateInvoiceEstimateHistoryInput!): InvoiceEstimateHistory!

  """
  Changes a log entry that exists. A member the caller leaves out is left as it is.
  """
  updateInvoiceEstimateHistory(input: UpdateInvoiceEstimateHistoryInput!): InvoiceEstimateHistory!

  """
  Removes a log entry outright. The row is gone and nothing reads it back, which is why a caller that
  means "this entry should not count" reaches for the soft removal below instead.
  """
  deleteInvoiceEstimateHistory(id: ID!): Boolean!

  """
  Withdraws a log entry without removing it: the row keeps its identifier and the delivered reads stop
  answering it.
  """
  softDeleteInvoiceEstimateHistory(id: ID!): InvoiceEstimateHistory!

  """Puts a withdrawn log entry back."""
  recoverInvoiceEstimateHistory(id: ID!): InvoiceEstimateHistory!

  """
  Records one line.
  
  The write is the service call the REST route makes, and the tenant the new row belongs to is
  stamped from the credential rather than stated by the caller, so a line cannot be written into a
  tenant the caller is not acting in.
  """
  createInvoiceItem(input: CreateInvoiceItemInput!): InvoiceItem!

  """
  Changes a line that exists.
  
  The delivered update is a primitive statement over the columns the body states, never a
  replacement of the row, so a member the caller leaves out is left as it is — see
  `UpdateInvoiceItemInput`.
  """
  updateInvoiceItem(input: UpdateInvoiceItemInput!): InvoiceItem!

  """Removes a line outright."""
  deleteInvoiceItem(id: ID!): Boolean!

  """
  Withdraws a line without removing the row. The answer is the withdrawn line.
  """
  softDeleteInvoiceItem(id: ID!): InvoiceItem!

  """Puts a withdrawn line back."""
  recoverInvoiceItem(id: ID!): InvoiceItem!

  """
  Stores a document's whole line set at once.
  
  **This is a replacement rather than an append.** The delivered write removes the lines the named
  document already has and then stores the lines the caller states, so the body is the document's
  complete line set and a line that is left out of it is gone. The document is named twice for that
  reason, and neither statement is redundant: the identifier in the argument is what the removal
  narrows by, and each line's own `invoiceId` is what attaches it to the document it is stored under —
  the delivered client states both, which is why the member is part of the input rather than assumed
  from the argument.
  
  A document that does not exist is not refused by this field: nothing here reads the document, and
  the lines are stored as they are stated. The document's own surface is where its existence is
  established.
  """
  createInvoiceItemsInBulk(invoiceId: ID!, input: [CreateInvoiceItemInput!]!): [InvoiceItem!]!

  """
  Raises an invoice or an estimate.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller, so a document cannot be written into a tenant the caller is not acting in; the organization
  it is issued from and filed under is the one the caller states, which is what the delivered route
  reads.
  """
  createInvoice(input: CreateInvoiceInput!): Invoice!

  """
  Changes a document that exists.
  
  The members the delivered body validation requires are required here for the same reason; a member
  the caller leaves out is left as it is, because the delivered write is the platform's upsert — see
  `UpdateInvoiceInput`, which also states what an identifier naming no row does.
  """
  updateInvoice(input: UpdateInvoiceInput!): Invoice!

  """
  Records the buyer's answer to an estimate.
  
  The delivered route's body is exactly the one member, and it is required: an estimate is answered
  accepted or rejected, and a body that states neither has answered nothing.
  """
  updateInvoiceEstimate(id: ID!, isAccepted: Boolean!): Invoice!

  """
  Moves a document along: its status, its settlement, its note, the recipient it was sent to.
  
  The delivered route states only the members the caller names and writes those onto the document,
  so a step never clears a member the caller did not mention. The identifier is the path's, as it is
  on the route, and the body carries only the steps.
  """
  updateInvoiceAction(id: ID!, input: UpdateInvoiceActionInput!): Invoice!

  """
  Sends the document to a recipient, as an email carrying the rendered document.
  
  The answer is `true` once the dispatch has run, which is the whole of what the delivered route
  establishes: it answers an accepted status and no body. The delivered dispatcher logs a failure
  rather than raising it, so this field cannot claim delivery — a client that must know a message
  arrived has to hear it from the recipient. Its language and its origin are the request's own
  headers, as they are on the route.
  """
  sendInvoiceEmail(input: SendInvoiceEmailInput!): Boolean!

  """
  Generates the document's public link and answers the document with the token written onto it.
  
  The link is the pair of the document's identifier and its `token`, which the answer carries;
  reading the public document itself is the public share resource's own surface rather than this
  domain's.
  """
  generateInvoiceLink(id: ID!): Invoice!

  """
  Removes a document outright.
  
  This is the hard removal, and it is the one to reach for only for a document that was raised in
  error: the payments applied against a document and the lines that explain it are the store's own
  rows to refuse or to cascade, and a document a buyer holds is withdrawn instead.
  """
  deleteInvoice(id: ID!): Boolean!

  """
  Withdraws a document without removing the row.
  
  The rows that name the document keep resolving, and the delivered list stops answering it. The
  answer is the withdrawn document; it is recoverable until it is removed outright.
  """
  softDeleteInvoice(id: ID!): Invoice!

  """Puts a withdrawn document back."""
  recoverInvoice(id: ID!): Invoice!

  """Files a catalogue entry, and answers the row it wrote."""
  createKeyResultTemplate(input: CreateKeyResultTemplateInput!): KeyResultTemplate!

  """
  Changes a catalogue entry, and answers the row the write produced. A member the caller leaves out is
  left as it is, because the delivered edit is a partial column update rather than a replacement; see
  `UpdateKeyResultTemplateInput`.
  """
  updateKeyResultTemplate(input: UpdateKeyResultTemplateInput!): KeyResultTemplate!

  """
  Removes a catalogue entry outright. The delivered route answers the store's delete result rather
  than a row, so this field answers whether the removal was performed.
  """
  deleteKeyResultTemplate(id: ID!): Boolean!

  """
  Withdraws a catalogue entry without removing it. The answer is the withdrawn row, whose `deletedAt`
  says when it happened.
  """
  softDeleteKeyResultTemplate(id: ID!): KeyResultTemplate!

  """Puts a withdrawn catalogue entry back."""
  recoverKeyResultTemplate(id: ID!): KeyResultTemplate!

  """Records a move against a key result, and answers the row it wrote."""
  createKeyResultUpdate(input: CreateKeyResultUpdateInput!): KeyResultUpdate!

  """
  Corrects a move that was recorded, and answers the row the write produced.
  
  The delivered route catches whatever the write threw and answers an empty body, which a field that
  promises a row cannot do — an empty answer is indistinguishable from a row that is not there. This
  field lets the write's own refusal travel instead, which is the same fact stated where a caller can
  read it. A member the caller leaves out is left as it is; see `UpdateKeyResultUpdateInput`.
  """
  updateKeyResultUpdate(input: UpdateKeyResultUpdateInput!): KeyResultUpdate!

  """
  Removes one update outright, by its own identifier. The delivered route answers the store's delete
  result rather than a row, so this field answers whether the removal was performed.
  """
  deleteKeyResultUpdate(id: ID!): Boolean!

  """
  Removes every update of one key result in a single write — the delivered bulk removal — with the
  rows withdrawn and live alike taken, because the delivered handler deletes by identifier without
  regard to the withdrawal column. It answers whether the write ran; a caller that needs the rows
  themselves reads them back through the connection.
  """
  deleteKeyResultUpdates(keyResultId: ID!): Boolean!

  """
  Withdraws one update without removing it. The answer is the withdrawn row, whose `deletedAt` says
  when it happened.
  """
  softDeleteKeyResultUpdate(id: ID!): KeyResultUpdate!

  """Puts a withdrawn update back."""
  recoverKeyResultUpdate(id: ID!): KeyResultUpdate!

  """Authors a key result, and answers the row it wrote."""
  createKeyResult(input: CreateKeyResultInput!): KeyResult!

  """
  Authors several key results in one write — the delivered bulk route — and answers the rows it
  wrote. The rows are saved rather than inserted, so an entry that carries an identifier updates the
  row it names, which is what the delivered service's own save does.
  """
  createKeyResults(input: [CreateKeyResultInput!]!): [KeyResult!]!

  """
  Changes a key result. The delivered route is an update-through-create, so a member the caller
  leaves out is left as it is; see `UpdateKeyResultInput`.
  """
  updateKeyResult(input: UpdateKeyResultInput!): KeyResult!

  """
  Removes a key result outright, with the updates the delivered cascade takes with it. The delivered
  route answers the store's delete result rather than a row, so this field answers whether the
  removal was performed.
  """
  deleteKeyResult(id: ID!): Boolean!

  """
  Withdraws a key result without removing it: the row keeps its identifier, its updates keep pointing
  at it, and the delivered list stops answering it.
  """
  softDeleteKeyResult(id: ID!): KeyResult!

  """Puts a withdrawn key result back."""
  recoverKeyResult(id: ID!): KeyResult!
  createUnitCategory(input: CreateUnitCategoryInput!): UnitCategory!
  updateUnitCategory(input: UpdateUnitCategoryInput!): UnitCategory!
  createUnit(input: CreateUnitInput!): Unit!
  updateUnit(input: UpdateUnitInput!): Unit!
  archiveUnit(id: ID!): Boolean!
  archiveUnitCategory(id: ID!): Boolean!
  convertQuantity(value: String!, fromUnitId: ID!, toUnitId: ID!): UnitConversion!

  """
  Files a mention, and raises the subscription and the notification the delivered filing raises
  beside it.
  
  The tenant and the employee who did the mentioning are stamped from the credential and are never
  stated by the caller; the organization and the mentioned employee are, because the subscription and
  the notification are addressed to them.
  """
  createMention(input: CreateMentionInput!): Mention!

  """
  Changes a mention that exists.
  
  **A member the caller leaves out is left as it is.** The delivered route reads the row the
  identifier names — proving it is the caller's own — and writes the members the body states to it,
  so this is a column update and not a replacement; see `UpdateMentionInput`.
  
  The answer is the row the write produced, read back through the read the node query performs. The
  delivered route answers the store's own update result — a statement about the write, `{ affected }`
  — which is not a row, and a field declared as the object type cannot answer it.
  """
  updateMention(input: UpdateMentionInput!): Mention!

  """
  Removes a mention outright. The answer says whether the removal happened rather than returning the
  removed row, because the delivered route answers the store's delete result.
  """
  deleteMention(id: ID!): Boolean!

  """
  Withdraws a mention without removing it: the row keeps its identifier and the recovery below reads
  it back, and the delivered list stops answering it.
  """
  softDeleteMention(id: ID!): Mention!

  """Puts a withdrawn mention back, clearing the marker the withdrawal set."""
  recoverMention(id: ID!): Mention!

  """
  Files a store, with the contact, the logo and the two pivots it is given.
  """
  createMerchant(input: CreateMerchantInput!): Merchant!

  """
  Changes a store that exists. The columns the body carries are written and the ones it does not are
  left as they are — see `UpdateMerchantInput`.
  """
  updateMerchant(input: UpdateMerchantInput!): Merchant!

  """
  Removes a store outright. The two removals this resource serves are not the same operation, so both
  are stated rather than one of them being left to a client to discover on the other protocol: this
  one removes the row, and its warehouses and tags keep their own rows — the pivots that attached them
  to this store are what cascades.
  """
  deleteMerchant(id: ID!): Boolean!

  """
  Withdraws a store without removing it. The answer is the row as it now stands, which is what the
  delivered route answers with.
  """
  softDeleteMerchant(id: ID!): Merchant!

  """Puts a withdrawn store back, clearing the marker the withdrawal set."""
  recoverMerchant(id: ID!): Merchant!

  """Files a holiday."""
  createOfficialHoliday(input: CreateOfficialHolidayInput!): OfficialHoliday!

  """
  Changes a holiday. A member the caller leaves out is left as it is, because the delivered edit is a
  partial column update rather than a replacement of the row.
  """
  updateOfficialHoliday(input: UpdateOfficialHolidayInput!): OfficialHoliday!

  """
  Removes a holiday outright. This is the only removal this resource serves: there is no withdrawal
  and no restoration behind it, so a caller that means "this day no longer applies" reaches for the
  removal rather than for a soft one.
  """
  deleteOfficialHoliday(id: ID!): Boolean!

  """
  Requests cancellation, which compensates what the operation already applied.
  
  A cancellation is a request the runtime observes at its next checkpoint rather than a kill, so the
  answer is the operation as the request left it — already settled when it had applied nothing, and
  compensating when it had. A completed operation is refused: there is nothing left to cancel.
  """
  cancelOperation(id: ID!, reason: String): Operation!

  """
  Re-drives an operation that failed, under a fresh attempt budget.
  
  The steps a failed attempt left have their attempt counters reset, and where the retry continues
  falls out of the persisted step statuses: an operation whose compensation finished re-runs its
  plan on a clean aggregate, and one that still owes an undo is handed back to the compensation
  walk. A completed operation, and one a caller cancelled, are refused — this door does not resurrect
  work somebody deliberately abandoned.
  """
  retryOperation(id: ID!): Operation!

  """
  Continues an interrupted operation.
  
  Recovery needs no external state: the persisted step statuses say where to continue, so resuming
  is executing under a fresh lease. An operation that already reached a terminal status is answered
  unchanged, because there is no move left to make.
  """
  resumeOperation(id: ID!): Operation!

  """Files an award."""
  createOrganizationAward(input: CreateOrganizationAwardInput!): OrganizationAward!

  """
  Changes the stated facts of an award. A member the caller leaves out is left as it is, because the
  delivered edit is a partial column update rather than a replacement of the row.
  """
  updateOrganizationAward(input: UpdateOrganizationAwardInput!): OrganizationAward!

  """Removes an award outright."""
  deleteOrganizationAward(id: ID!): Boolean!

  """
  Withdraws an award without removing it: the row keeps its identifier and the organization keeps it,
  and the delivered list stops answering it.
  """
  softDeleteOrganizationAward(id: ID!): OrganizationAward!

  """Puts a withdrawn award back."""
  recoverOrganizationAward(id: ID!): OrganizationAward!

  """
  Records a party, together with the detail row it is stored with.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller. The organization is stated, because the delivered body validates it against the caller's
  own memberships and the handler stamps it onto the detail row as well as onto the party.
  """
  createOrganizationContact(input: CreateOrganizationContactInput!): OrganizationContact!

  """
  Changes a party that exists, and the detail row it carries.
  
  The delivered write reads the row first and writes the members stated onto it, so a member the
  caller leaves out is left as it is and a party that is not there is a miss rather than a write that
  creates one.
  """
  updateOrganizationContact(input: UpdateOrganizationContactInput!): OrganizationContact!

  """
  Moves a set of parties into or out of one employee's book, in one call.
  
  Both directions are one operation because they are one fact about one employee, and the delivered
  command applies both lists together; the answer is the write's own success, which is what the
  route answers with.
  """
  updateOrganizationContactsByEmployee(input: UpdateOrganizationContactsByEmployeeInput!): Boolean!

  """
  Removes a party outright: the row is gone and nothing reads it back.
  
  The two removals this resource serves are not the same operation, so both are stated rather than
  one of them being left to a client to discover on the other protocol. A party an order, an invoice
  or a payment already names is deactivated rather than removed — that rule is the service's, and it
  refuses the removal here exactly as it refuses it over REST.
  """
  deleteOrganizationContact(id: ID!): Boolean!

  """
  Withdraws a party without removing it: the row stays in the table, marked as removed, and the
  recovery below reads it back. The answer is the row as it now stands, which is what the delivered
  route answers with, and the parties that pointed at it keep resolving because the row is still
  there.
  """
  softDeleteOrganizationContact(id: ID!): OrganizationContact!

  """Puts a withdrawn party back, clearing the marker the withdrawal set."""
  recoverOrganizationContact(id: ID!): OrganizationContact!

  """
  Files a department, through the same call the delivered create route makes.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller; the organization is stated, because the route's body carries it and the column is the
  organization the row is filed under.
  """
  createOrganizationDepartment(input: CreateOrganizationDepartmentInput!): OrganizationDepartment!

  """
  Changes the stated facts of a department, through the command the delivered edit route dispatches.
  
  The route carries the identifier in the path and the facts in the body, and the command's handler
  writes through the create path with the identifier merged in — so this is an upsert rather than a
  replacement: a member the caller leaves out is left as it is, and the answer is the row as it now
  stands rather than a statement about the write.
  """
  updateOrganizationDepartment(input: UpdateOrganizationDepartmentInput!): OrganizationDepartment!

  """
  Moves a set of departments into or out of one employee's book, in one call.
  
  Both directions are one operation because they are one fact about one employee, and the delivered
  command applies both lists together. The answer is the write's own success: the route's declared
  answer is empty, and the command behind it answers the success of the write, which is what the
  field answers with — a refusal is the command's own failure and reaches the caller as one, rather
  than being flattened into a `false` that would read as "nothing to do".
  """
  updateOrganizationDepartmentByEmployee(input: UpdateOrganizationDepartmentByEmployeeInput!): Boolean!

  """
  Removes a department outright: the row is gone and nothing reads it back.
  """
  deleteOrganizationDepartment(id: ID!): Boolean!

  """
  Withdraws a department without removing it: the row keeps its identifier and the people filed
  under it keep pointing at it, and the delivered list stops answering it. The answer is the row as
  it now stands, which is what the delivered route answers with.
  """
  softDeleteOrganizationDepartment(id: ID!): OrganizationDepartment!

  """
  Puts a withdrawn department back, clearing the marker the withdrawal set.
  """
  recoverOrganizationDepartment(id: ID!): OrganizationDepartment!

  """Files a document."""
  createOrganizationDocument(input: CreateOrganizationDocumentInput!): OrganizationDocument!

  """
  Changes the stated facts of a document. A member the caller leaves out is left as it is, because the
  delivered edit is a partial column update rather than a replacement of the row.
  """
  updateOrganizationDocument(input: UpdateOrganizationDocumentInput!): OrganizationDocument!

  """
  Removes a document outright. The file the asset holds is not removed with it; the asset is a row of
  its own domain.
  """
  deleteOrganizationDocument(id: ID!): Boolean!

  """
  Withdraws a document without removing it: the row keeps its identifier and the organization keeps
  it, and the delivered list stops answering it.
  """
  softDeleteOrganizationDocument(id: ID!): OrganizationDocument!

  """Puts a withdrawn document back."""
  recoverOrganizationDocument(id: ID!): OrganizationDocument!

  """Files an employment type."""
  createOrganizationEmploymentType(input: CreateOrganizationEmploymentTypeInput!): OrganizationEmploymentType!

  """
  Changes the stated facts of an employment type. The delivered edit writes through the create path —
  it is an upsert rather than a replacement of the row — so a member the caller leaves out is left as
  it is.
  """
  updateOrganizationEmploymentType(input: UpdateOrganizationEmploymentTypeInput!): OrganizationEmploymentType!

  """
  Removes an employment type outright. Who was classified under it is the store's own constraint to
  refuse; this field does not pre-empt it.
  """
  deleteOrganizationEmploymentType(id: ID!): Boolean!

  """
  Withdraws an employment type without removing it: the row keeps its identifier and the people
  classified under it keep pointing at it, and the delivered list stops answering it.
  """
  softDeleteOrganizationEmploymentType(id: ID!): OrganizationEmploymentType!

  """Puts a withdrawn employment type back."""
  recoverOrganizationEmploymentType(id: ID!): OrganizationEmploymentType!

  """Files a language row."""
  createOrganizationLanguage(input: CreateOrganizationLanguageInput!): OrganizationLanguage!

  """
  Changes the stated facts of a language row. A member the caller leaves out is left as it is, because
  the delivered edit is a partial column update rather than a replacement of the row.
  """
  updateOrganizationLanguage(input: UpdateOrganizationLanguageInput!): OrganizationLanguage!

  """
  Removes a language row outright. Nothing points at it from this domain.
  """
  deleteOrganizationLanguage(id: ID!): Boolean!

  """
  Withdraws a language row without removing it: the row keeps its identifier and the delivered list
  stops answering it.
  """
  softDeleteOrganizationLanguage(id: ID!): OrganizationLanguage!

  """Puts a withdrawn language row back."""
  recoverOrganizationLanguage(id: ID!): OrganizationLanguage!

  """Files a position."""
  createOrganizationPosition(input: CreateOrganizationPositionInput!): OrganizationPosition!

  """
  Changes the stated facts of a position. The delivered edit writes through the create path — it is an
  upsert rather than a replacement of the row — so a member the caller leaves out is left as it is.
  """
  updateOrganizationPosition(input: UpdateOrganizationPositionInput!): OrganizationPosition!

  """
  Removes a position outright. Who is filed under it is the store's own constraint to refuse; this
  field does not pre-empt it.
  """
  deleteOrganizationPosition(id: ID!): Boolean!

  """
  Withdraws a position without removing it: the row keeps its identifier and the people filed under it
  keep pointing at it, and the delivered list stops answering it.
  """
  softDeleteOrganizationPosition(id: ID!): OrganizationPosition!

  """Puts a withdrawn position back."""
  recoverOrganizationPosition(id: ID!): OrganizationPosition!

  """
  Files a module, with the employees it is assigned to and the tasks filed under it in the same write.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  The organization is stated, because the delivered body validates it against the caller's own
  memberships and the write reads the employee lists as members of that organization.
  """
  createOrganizationProjectModule(input: CreateOrganizationProjectModuleInput!): OrganizationProjectModule!

  """
  Changes a module that exists, and the members, the managers and the tasks it carries.
  
  The identifier is the criterion and is not repeated in the payload, which is the shape the delivered
  route has: `:id` names the row and the body carries the facts. A member the caller leaves out is left
  as it is; a list the caller states is the set the write applies — see `UpdateOrganizationProjectModuleInput`.
  """
  updateOrganizationProjectModule(input: UpdateOrganizationProjectModuleInput!): OrganizationProjectModule!

  """
  Removes a module outright, together with the records the store cascades from it.
  
  The two removals this resource serves are not the same operation, so both are stated rather than one
  of them being left to a client to discover on the other protocol.
  """
  deleteOrganizationProjectModule(id: ID!): Boolean!

  """
  Withdraws a module without removing it: the row keeps its identifier, the tasks filed under it keep
  pointing at it, and the delivered list stops answering it. The answer is the row as it now stands,
  which is what the delivered route answers with.
  """
  softDeleteOrganizationProjectModule(id: ID!): OrganizationProjectModule!

  """Puts a withdrawn module back, clearing the marker the withdrawal set."""
  recoverOrganizationProjectModule(id: ID!): OrganizationProjectModule!

  """
  Files a project, together with the task statuses, priorities, sizes and issue types its first
  tasks need.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the
  caller. The organization is stated, because the delivered body validates it against the caller's
  own memberships and the handler files the row under it.
  """
  createOrganizationProject(input: CreateOrganizationProjectInput!): OrganizationProject!

  """
  Changes a project that exists.
  
  The identifier is the criterion and is not repeated in the payload, which is the shape the route
  itself has: `:id` names the row and the body carries the facts. The handler reads the row before it
  writes, so a project of another organization, or one that is not there, is answered with the miss
  rather than with a write under an identifier the caller does not own — and the member lists it
  carries replace the project's member pivot rather than adding to it.
  """
  updateOrganizationProject(input: UpdateOrganizationProjectInput!): OrganizationProject!

  """
  Changes which view a project's tasks open in.
  
  The same command the delivered route dispatches, with the same body: the list type and the
  organization that scopes the row. A caller that states a list type the column does not know is
  refused by the write, exactly as the route refuses it.
  """
  updateOrganizationProjectTaskViewMode(id: ID!, input: OrganizationProjectTaskViewModeInput!): OrganizationProject!

  """
  Changes how a project's tasks synchronise.
  
  The same command the delivered route dispatches, with the same body. The delivered route states no
  permission of its own, so this field resolves to the controller's class-level pair — the setting
  write is governed by the same grant as the edit beside it, on both protocols.
  """
  updateOrganizationProjectSetting(id: ID!, input: OrganizationProjectSettingInput!): OrganizationProject!

  """
  Moves a set of projects into or out of one employee's list, in one call.
  
  Both directions are one operation because they are one fact about one employee, and the delivered
  command applies both lists together; the answer is the write's own success, which is what the route
  answers with.
  """
  updateOrganizationProjectByEmployee(input: UpdateOrganizationProjectByEmployeeInput!): Boolean!

  """
  Removes a project outright.
  
  The store's own constraints are what refuse a row other records still point at; this field does not
  pre-empt them, so a project a task or an invoice line still names is answered with the same refusal
  the REST route answers with.
  """
  deleteOrganizationProject(id: ID!): Boolean!

  """
  Withdraws a project without removing it: the row keeps its identifier, the tasks and the records
  filed under it keep pointing at it, and the list stops answering it.
  """
  softDeleteOrganizationProject(id: ID!): OrganizationProject!

  """Puts a withdrawn project back, clearing the marker the withdrawal set."""
  recoverOrganizationProject(id: ID!): OrganizationProject!

  """
  Records a standing cost.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller, so
  an arrangement cannot be written into a tenant the caller is not acting in. The delivered handler sets
  the row's parent to the row's own identifier, which is what makes the arrangement a family.
  """
  createOrganizationRecurringExpense(input: CreateOrganizationRecurringExpenseInput!): OrganizationRecurringExpense!

  """
  Changes a standing cost from the month the caller states onward.
  
  The delivered edit decides what the change means — it may write the beginning and the value, close the
  arrangement and open its successor, or resolve a conflict — and answers whichever row that produced,
  which is what this field answers. A member the caller omits is left as it is.
  """
  updateOrganizationRecurringExpense(input: UpdateOrganizationRecurringExpenseInput!): OrganizationRecurringExpense!

  """
  Removes a standing cost, in one of the three ways the delivered input names.
  
  The answer is the one fact the removal establishes, because the delivered handler's own answer is a
  delete result, an update result or a newly opened row depending on the deletion type — three shapes,
  none of them a row this field could state. A caller that needs the arrangement as it now stands reads
  it back from the connection.
  """
  deleteOrganizationRecurringExpense(id: ID!, input: OrganizationRecurringExpenseDeleteInput!): Boolean!

  """
  Withdraws a standing cost without removing the row.
  
  The delivered list stops answering it and the rows that reference it keep resolving the identifier
  they hold. The answer is the withdrawn row; it is recoverable until it is removed outright.
  """
  softDeleteOrganizationRecurringExpense(id: ID!): OrganizationRecurringExpense!

  """Puts a withdrawn standing cost back."""
  recoverOrganizationRecurringExpense(id: ID!): OrganizationRecurringExpense!

  """
  Files a sprint under a project, with the people who work it.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  The organization and the project are stated, because the delivered body validates both and the
  handler files the row under them.
  """
  createOrganizationSprint(input: CreateOrganizationSprintInput!): OrganizationSprint!

  """
  Changes a sprint that exists.
  
  The identifier is the criterion and is not repeated in the payload, which is the shape the delivered
  route itself has: the path names the row and the body carries the facts. The delivered handler reads
  the row before it writes and answers it afterwards, so a sprint of another tenant, or one that is not
  there, is a miss rather than a write under an identifier the caller does not own.
  """
  updateOrganizationSprint(input: UpdateOrganizationSprintInput!): OrganizationSprint!

  """
  Removes a sprint outright: the row is gone and nothing reads it back.
  
  The two lifecycle moves this resource serves are not the same operation, so both are stated rather
  than one of them being left to a client to discover on the other protocol. What hangs off the
  removed row — the membership rows and the task links — is the store's own constraint to cascade or
  to refuse; this field does not pre-empt it.
  """
  deleteOrganizationSprint(id: ID!): Boolean!

  """
  Withdraws a sprint without removing it: the row stays in the table, marked as removed, and the
  recovery below reads it back. The answer is the row as it now stands, which is what the delivered
  route answers with.
  """
  softDeleteOrganizationSprint(id: ID!): OrganizationSprint!

  """Puts a withdrawn sprint back, clearing the marker the withdrawal set."""
  recoverOrganizationSprint(id: ID!): OrganizationSprint!

  """
  Files a strategic initiative. The caller's own employee is recorded as its steward when the caller
  states none.
  """
  createOrganizationStrategicInitiative(input: CreateOrganizationStrategicInitiativeInput!): OrganizationStrategicInitiative!

  """Changes the stated facts of a strategic initiative."""
  updateOrganizationStrategicInitiative(input: UpdateOrganizationStrategicInitiativeInput!): OrganizationStrategicInitiative!

  """
  Records an assessment of a strategic initiative. The stated signals are merged over the ones the row
  already carries and stamped with the assessment's time and the assessing employee, so the signals a
  previous assessment left behind are kept.
  """
  updateOrganizationStrategicInitiativeSignals(input: UpdateOrganizationStrategicSignalsInput!): OrganizationStrategicInitiative!

  """
  Removes a strategic initiative outright. What is aligned to it is the store's own constraint to
  refuse; this field does not pre-empt it.
  """
  deleteOrganizationStrategicInitiative(id: ID!): Boolean!

  """
  Withdraws a strategic initiative without removing it: the row keeps its identifier and everything
  aligned to it keeps pointing at it, and the delivered list stops answering it.
  """
  softDeleteOrganizationStrategicInitiative(id: ID!): OrganizationStrategicInitiative!

  """Puts a withdrawn strategic initiative back."""
  recoverOrganizationStrategicInitiative(id: ID!): OrganizationStrategicInitiative!

  """Files the task settings for an organization."""
  createOrganizationTaskSetting(input: CreateOrganizationTaskSettingInput!): OrganizationTaskSetting!

  """
  Changes the settings of one task-setting row. The identifier is the criterion; the body states the
  settings, and every setting is optional because every column behind it carries a default.
  """
  updateOrganizationTaskSetting(input: UpdateOrganizationTaskSettingInput!): OrganizationTaskSetting!

  """
  Edits a membership: the position of the member in the team's roster, whether the tracker records
  that member's time for the team, and the task the member is working on.
  """
  updateOrganizationTeamEmployee(id: ID!, input: UpdateOrganizationTeamEmployeeInput!): OrganizationTeamEmployee!

  """
  Moves a member onto a different task.
  
  The delivered method is also where the right to change one is decided — a caller that may select an
  employee may move any member of the scope, a manager of the team may move any of its members, and
  everyone else may move only themselves — and that decision is not restated here: the field reaches
  the delivered method and is refused by the same rules, with the same refusal shape the REST route
  answers with.
  """
  updateOrganizationTeamEmployeeActiveTask(id: ID!, input: OrganizationTeamEmployeeActiveTaskInput!): OrganizationTeamEmployee!

  """
  Removes a membership outright: the tasks the member held in this team are unassigned first, and the
  employee's own record is untouched. The membership is scoped by the identifier together with the
  organization and the team the delivered route takes from its query string, so the identifier alone
  does not name a row and a membership of another scope is not reached.
  
  The answer is `true` once the write has completed. A membership that is not there is the miss the
  delivered service raises rather than a `false` answered from here, so a caller never has to tell
  "nothing was removed" from "the removal did not happen".
  """
  deleteOrganizationTeamEmployee(id: ID!, options: OrganizationTeamEmployeeDeleteInput!): Boolean!

  """
  Asks to join one organization team, and mails a confirmation code to the address that asks.
  
  The same command the delivered route dispatches, carrying the same language: the route reads the
  `language` request header through its own decorator, and `RequestContext.getLanguageCode()` reads
  that same header off the same request — the bootstrap mounts a request context on the GraphQL
  endpoint as well as on the prefixed routes — so a caller asking the same question over either
  protocol is mailed the same message in the same language.
  
  The answer is the acknowledgement rather than the row, which is what the delivered handler answers
  and all it answers: it produces the pair in a `finally`, so it is answered for every outcome,
  including the ones the handler swallowed. A caller that learned from this answer whether the
  address had already asked to join the team would have learned it about an address that may not be
  its own, which is exactly what the delivered shape prevents.
  """
  createOrganizationTeamJoinRequest(input: CreateOrganizationTeamJoinRequestInput!): OrganizationTeamJoinRequestOutcome!

  """
  Presents the confirmation code or token mailed to the address that asked to join.
  
  The same service method the delivered route calls, with the route's own DTO as the input. The read
  it performs resolves the pending request of that address in that team — one that has not lapsed and
  whose state is still unset — matched by the code or by the token, and marks it `REQUESTED`; the
  answer is the row it resolved, with its identifier removed: the delivered reader deletes that member
  before it answers, so a caller holding a code cannot then look the row up by identifier. The address
  and the team are the two members every answer is guaranteed to carry — what travels beside them
  depends on the persistence layer an installation runs, because the read selects those two on one and
  serialises the row it loaded on the other — so a caller that needs the row itself reads it back
  through `organizationTeamJoinRequests`.
  
  A miss is a refusal rather than an empty answer. The delivered service answers a request that is not
  there, has lapsed, or has already moved with a bare `400`, and this field lets that refusal through
  rather than turning it into a null: "no such pending request" and "the code was wrong" are the same
  answer here on purpose, because telling them apart would confirm which codes are live.
  """
  validateOrganizationTeamJoinRequest(input: ValidateOrganizationTeamJoinRequestInput!): OrganizationTeamJoinRequest!

  """
  Mails a fresh confirmation code for a request that is still pending.
  
  The same service method the delivered route calls, and with the same single argument: the route
  passes the body and no language, so the message is written in whatever language the mailer falls back
  to rather than in the caller's. Passing the caller's language here would make this surface disagree
  with the REST one about the message a caller is sent, and the two-protocol rule is parity rather
  than improvement.
  
  The answer is the acknowledgement, produced by the delivered method's `finally`: like the ask, it is
  answered whatever happened, so a caller is told the request was received and is told nothing about
  whether a pending row was found or whether a message went out.
  """
  resendOrganizationTeamJoinRequestCode(input: ResendOrganizationTeamJoinRequestCodeInput!): OrganizationTeamJoinRequestOutcome!

  """
  Accepts or rejects one join request.
  
  The same service method the delivered route calls, with the same three arguments: the identifier
  names the row, the action names the move, and the language is read the way the route's own
  `@I18nLang()` reads it — the i18n resolver this installation configures is a header resolver over
  the `language` header, which is the header `RequestContext.getLanguageCode()` reads, with the same
  English fallback.
  
  The delivered method answers nothing at all, so this field answers whether the call ran, which is
  the most the route's answer carries. `true` is not a statement that the request moved: the delivered
  move is conditional, and leaves the row exactly as it was when the applicant is already a member of
  the team, or when the address has no employee record in the tenant. A caller that needs the row as
  it now stands reads it back through `organizationTeamJoinRequests`, which is where this surface
  reads the row.
  """
  acceptOrganizationTeamJoinRequest(id: ID!, action: OrganizationTeamJoinRequestAction!): Boolean!

  """
  Files a team, with the membership and the attachments the caller states.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  The organization is stated, because the delivered body checks it against the caller's own
  memberships. Filing a team is more than an insert: the delivered write stores the membership as the
  team-employee rows the caller's lists name, subscribes the people it added and provisions the team's
  task statuses, priorities, sizes and issue types.
  """
  createOrganizationTeam(input: CreateOrganizationTeamInput!): OrganizationTeam!

  """
  Changes a team that exists.
  
  The identifier names the row and the facts carry what changes, which is the shape the delivered
  route has: the path names the row and the body is the payload. The delivered write reads the row
  before it writes, and refuses a row the caller does not manage, so a team that is not there is
  answered with that refusal rather than with a write under an identifier the caller does not own.
  """
  updateOrganizationTeam(input: UpdateOrganizationTeamInput!): OrganizationTeam!

  """
  Removes a team outright, with the membership rows that belong to it.
  
  The delivered answer is the removal result — its signature admits the row beside it — and the one
  member a caller reads from either is that the removal happened, which is what this field answers. A
  team that is not there, or one the caller does not manage, is refused by the service before that
  line rather than answered with a negative.
  """
  deleteOrganizationTeam(id: ID!, options: OrganizationTeamDeleteInput): Boolean!

  """
  Removes one user from the teams they joined as a member.
  
  The delivered read resolves the user's employee, unassigns that employee from the tasks their teams
  carry and removes the membership rows they hold as a plain member — the facts a caller needs, the
  removal among them, being what the answer states. A caller naming a user other than itself is
  refused unless it holds the permission to change the selected employee, which is the same refusal
  the route gives.
  """
  removeUserFromOrganizationTeams(userId: ID!): Boolean!

  """
  Withdraws a team without removing it: the row keeps its identifier, the work filed under it keeps
  pointing at it, and the list stops answering it. The answer is the row as it now stands, which is
  what the delivered route answers with.
  """
  softDeleteOrganizationTeam(id: ID!): OrganizationTeam!

  """Puts a withdrawn team back, clearing the marker the withdrawal set."""
  recoverOrganizationTeam(id: ID!): OrganizationTeam!

  """
  Records a supplier.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller, so
  a supplier cannot be written into a tenant the caller is not acting in. The delivered creation binds
  the row itself as its body, so every member below is one the write stores.
  """
  createOrganizationVendor(input: CreateOrganizationVendorInput!): OrganizationVendor!

  """
  Changes a supplier that exists.
  
  The delivered edit reaches the same write the creation does, carrying the identifier in the path and the
  body beside it, so a member the caller leaves out is left as it is — see `UpdateOrganizationVendorInput`,
  which also states what an identifier naming no row does.
  """
  updateOrganizationVendor(input: UpdateOrganizationVendorInput!): OrganizationVendor!

  """
  Removes a supplier, unless an expense already names it.
  
  This is the master's own rule rather than the store's delete: a supplier the expense book points at is
  not removable, because removing it would leave those rows naming a party that no longer exists. The
  refusal names the dependency; a supplier nothing points at is removed outright.
  """
  deleteOrganizationVendor(id: ID!): Boolean!

  """
  Withdraws a supplier without removing the row.
  
  The delivered list stops answering it and the expenses that name it keep resolving the identifier they
  hold — which is the difference between this field and the removal above. The answer is the withdrawn
  row; it is recoverable until it is removed outright.
  """
  softDeleteOrganizationVendor(id: ID!): OrganizationVendor!

  """Puts a withdrawn supplier back."""
  recoverOrganizationVendor(id: ID!): OrganizationVendor!

  """
  Files an organization, together with the contact row the caller states beside it and the settings
  its first administrators need.
  """
  createOrganization(input: CreateOrganizationInput!): Organization!

  """
  Replaces the stated facts of an organization. The visibility flags of the public profile are
  written from the body as stated, so a member of that family the caller leaves out is turned off
  rather than left as it is — see `UpdateOrganizationInput`.
  """
  updateOrganization(input: UpdateOrganizationInput!): Organization!

  """
  Removes an organization outright. The store's own constraints are what refuse a row other records
  still point at; this field does not pre-empt them.
  """
  deleteOrganization(id: ID!): Boolean!

  """
  Withdraws an organization without removing it: the row keeps its identifier and the records filed
  under it keep pointing at it, and the delivered list stops answering it.
  """
  softDeleteOrganization(id: ID!): Organization!

  """Puts a withdrawn organization back."""
  recoverOrganization(id: ID!): Organization!
  createPaymentTerm(input: CreatePaymentTermInput!): PaymentTerm!
  updatePaymentTerm(input: UpdatePaymentTermInput!): PaymentTerm!
  updatePaymentTermLines(input: UpdatePaymentTermLinesInput!): PaymentTerm!
  archivePaymentTerm(id: ID!): Boolean!

  """
  Records a money movement.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller,
  so a payment cannot be written into a tenant the caller is not acting in. The amount is the exact
  decimal the column stores, which is the one member the delivered body validation requires beside the
  organization.
  """
  createPayment(input: CreatePaymentInput!): Payment!

  """
  Changes a movement that exists.
  
  The delivered edit reaches the same write the create does, carrying the identifier in the path and
  the body together, so a member the caller leaves out is left as it is — see `UpdatePaymentInput`,
  which also states what an identifier naming no row does. The delivered route relabels every failure
  of this write as a bad request; this field answers the failure the service raised instead, so a
  caller reads what actually happened rather than a label.
  """
  updatePayment(input: UpdatePaymentInput!): Payment!

  """
  Removes a movement outright.
  
  This is the hard removal, and it is the one to reach for only for a row recorded in error: the
  capture and refund rows that point at a movement are the store's own to refuse or to cascade, and a
  movement a document already accounts for is withdrawn instead.
  """
  deletePayment(id: ID!): Boolean!

  """
  Withdraws a movement without removing the row.
  
  The delivered list stops answering it and the rows that reference it keep resolving the identifier
  they hold. The answer is the withdrawn row; it is recoverable until it is removed outright.
  """
  softDeletePayment(id: ID!): Payment!

  """Puts a withdrawn movement back."""
  recoverPayment(id: ID!): Payment!

  """
  Sends a receipt for one movement, as an email carrying the amount it acknowledges.
  
  The answer is the boolean the delivered dispatch answers, which is the whole of what the route
  establishes: `true` once the message was handed to the mailer and `false` when the dispatch refused
  it, which the delivered method reports rather than raising. The language and the origin are the
  request's own headers, as they are on the route, so a caller cannot state one origin over one
  protocol and a different one over the other.
  """
  sendPaymentReceipt(input: SendPaymentReceiptInput!): Boolean!

  """
  Opens a run in `DRAFT`. The status and the three totals are outside this operation: the status moves
  only through the workflow below, and the totals are derived from the lines.
  """
  createPayrollRun(input: CreatePayrollRunInput!): PayrollRun!

  """
  Edits a run that has not been paid. A member the caller leaves out is left as it is.
  """
  updatePayrollRun(input: UpdatePayrollRunInput!): PayrollRun!

  """
  Moves a draft run to `PENDING_APPROVAL`, recomputing its totals first so the approver sees what they
  are signing off.
  """
  submitPayrollRun(id: ID!, organizationId: ID!): PayrollRun!

  """
  Approves a run that is pending approval, recording when it was approved and by whom. This is the
  operation the separate approve grant exists for.
  """
  approvePayrollRun(id: ID!, organizationId: ID!): PayrollRun!

  """
  Recomputes the totals from the run's lines and marks it paid.
  
  The totals and the status move together inside one transaction, and the transition is claimed in a
  single statement whose criterion still names the state that was read — so a second concurrent call is
  refused rather than paying the run twice. That is the delivered operation's own guarantee and not a
  simplification here.
  """
  processPayrollRun(id: ID!, organizationId: ID!): PayrollRun!

  """
  Cancels a run that has not been paid. Cancelling an already cancelled run answers the run unchanged,
  which is the delivered operation's own behaviour.
  """
  cancelPayrollRun(id: ID!, organizationId: ID!): PayrollRun!

  """
  Adds one earning or deduction line to a draft run and recomputes the run's totals from its lines.
  
  The run is the identifier, not an input member, so a caller cannot post a line into a run it did not
  name. The employee must belong to the same organization, which the service checks before it writes.
  """
  addPayrollRunItem(id: ID!, input: CreatePayrollItemInput!): PayrollItem!

  """
  Removes a line from a draft run and recomputes the run's totals.
  
  The delivered route answers the store's deletion result; this field answers the fact of the removal,
  which is the one member of that result a caller reads.
  """
  removePayrollRunItem(id: ID!, itemId: ID!, organizationId: ID!): Boolean!

  """Files a pipeline, with the stages it starts with."""
  createPipeline(input: CreatePipelineInput!): Pipeline!

  """
  Replaces a pipeline's facts and its whole stage set. A stage the body does not name is removed —
  see `UpdatePipelineInput`.
  """
  updatePipeline(input: UpdatePipelineInput!): Pipeline!

  """
  Removes a pipeline outright, with the stages that belong to it: the stage relation cascades at the
  store. The two removals this resource serves are stated separately rather than one of them being
  left to a client to discover on the other protocol.
  """
  deletePipeline(id: ID!): Boolean!

  """
  Withdraws a pipeline without removing it. The answer is the row as it now stands, which is what
  the delivered route answers with.
  """
  softDeletePipeline(id: ID!): Pipeline!

  """
  Puts a withdrawn pipeline back, clearing the marker the withdrawal set.
  """
  recoverPipeline(id: ID!): Pipeline!

  """
  Files a category, with the text of every language the caller states for it.
  """
  createProductCategory(input: CreateProductCategoryInput!): ProductCategory!

  """
  Edits a category in place. A member the caller leaves out is left as it is, and the categories
  below it stay below it — see `UpdateProductCategoryInput`.
  """
  updateProductCategory(input: UpdateProductCategoryInput!): ProductCategory!

  """
  Removes a category outright, together with the translation rows that belong to it. Its children
  become roots rather than being removed with it, on every database; a category a product still
  points at is the store's own constraint to answer, and this field does not pre-empt it.
  """
  deleteProductCategory(id: ID!): Boolean!

  """
  Withdraws a category without removing it: the row keeps its identifier and its products keep
  pointing at it, and the delivered list stops answering it.
  """
  softDeleteProductCategory(id: ID!): ProductCategory!

  """Puts a withdrawn category back."""
  recoverProductCategory(id: ID!): ProductCategory!

  """Files an option, with the group it answers for."""
  createProductOption(input: CreateProductOptionInput!): ProductOption!

  """
  Changes the descriptive facts of an option. A member the caller leaves out is left as it is.
  """
  updateProductOption(input: UpdateProductOptionInput!): ProductOption!

  """
  Removes an option outright: the row is gone and nothing reads it back.
  
  The two removals this resource serves are not the same operation, so both are stated rather than
  one of them being left to a client to discover on the other protocol.
  """
  deleteProductOption(id: ID!): Boolean!

  """
  Softly removes an option: the row stays in the table, marked as removed, and the restore below
  reads it back. The answer is the row as it now stands, which is what the delivered route answers
  with.
  """
  softDeleteProductOption(id: ID!): ProductOption!

  """
  Restores an option that was softly removed, clearing the marker the removal set.
  """
  recoverProductOption(id: ID!): ProductOption!

  """Opens a capability record for a variant."""
  createProductVariantSetting(input: CreateProductVariantSettingInput!): ProductVariantSetting!

  """Changes the capabilities a variant is recorded under."""
  updateProductVariantSetting(input: UpdateProductVariantSettingInput!): ProductVariantSetting!

  """
  Removes a setting outright: the row is gone and nothing reads it back.
  
  The two removals this resource serves are not the same operation, so both are stated rather than
  one of them being left to a client to discover on the other protocol.
  """
  deleteProductVariantSetting(id: ID!): Boolean!

  """
  Softly removes a setting: the row stays in the table, marked as removed, and the restore below
  reads it back. The answer is the row as it now stands, which is what the delivered route answers
  with.
  """
  softDeleteProductVariantSetting(id: ID!): ProductVariantSetting!

  """
  Restores a setting that was softly removed, clearing the marker the removal set.
  """
  recoverProductVariantSetting(id: ID!): ProductVariantSetting!

  """
  Declares a type, with the text of every language the caller states for it.
  """
  createProductType(input: CreateProductTypeInput!): ProductType!

  """
  Replaces a type. A column member the caller leaves out is written as its default rather than left
  as it is, because the delivered update removes the row and persists the stated body under the
  verified path identifier; `translations` is the exception the delivered service carries over — see
  `UpdateProductTypeInput`.
  """
  updateProductType(input: UpdateProductTypeInput!): ProductType!

  """
  Removes a type outright, together with the translation rows that belong to it. A product still
  classified under it is the store's own constraint to refuse; this field does not pre-empt it.
  """
  deleteProductType(id: ID!): Boolean!

  """
  Withdraws a type without removing it: the row keeps its identifier and its products keep their
  classification, and the delivered list stops answering it.
  """
  softDeleteProductType(id: ID!): ProductType!

  """Puts a withdrawn type back."""
  recoverProductType(id: ID!): ProductType!

  """
  Prices a variant: an amount and the currency it is stated in, for the cost and for the sale.
  """
  createProductVariantPrice(input: CreateProductVariantPriceInput!): ProductVariantPrice!

  """Changes a price. A member the caller leaves out is left as it is."""
  updateProductVariantPrice(input: UpdateProductVariantPriceInput!): ProductVariantPrice!

  """
  Removes a price outright: the row is gone and nothing reads it back.
  
  The two removals this resource serves are not the same operation, so both are stated rather than
  one of them being left to a client to discover on the other protocol.
  """
  deleteProductVariantPrice(id: ID!): Boolean!

  """
  Softly removes a price: the row stays in the table, marked as removed, and the restore below reads
  it back. The answer is the row as it now stands, which is what the delivered route answers with.
  """
  softDeleteProductVariantPrice(id: ID!): ProductVariantPrice!

  """
  Restores a price that was softly removed, clearing the marker the removal set.
  """
  recoverProductVariantPrice(id: ID!): ProductVariantPrice!

  """
  Generates one variant per option combination. Each variant is stored with its own setting row and
  its own price row, because a variant without them cannot be priced, sold or counted.
  """
  createProductVariants(input: CreateProductVariantsInput!): [ProductVariant!]!

  """Changes the descriptive facts of a variant."""
  updateProductVariant(input: UpdateProductVariantInput!): ProductVariant!

  """
  Removes a variant, together with the setting and price rows that belong to it.
  """
  deleteProductVariant(id: ID!): Boolean!

  """
  Withdraws a variant without removing its rows.
  
  The routes of this resource are the ones a `CrudController` delivers, and its two softer write routes are
  among them: a withdrawal is recoverable, which is what makes it the operation to reach for when a variant
  stops being sold but its order lines must keep resolving.
  """
  softDeleteProductVariant(id: ID!): ProductVariant!

  """Brings a withdrawn variant back."""
  recoverProductVariant(id: ID!): ProductVariant!

  """
  Withdraws the variant's featured image. The asset itself is kept: it may still be the product's
  cover, another variant's image, or a member of the product's gallery.
  """
  deleteProductVariantFeaturedImage(variantId: ID!): ProductVariant!

  """
  Records a product, with the translations and the option groups the delivered create path builds
  rows from. The tenant the new row belongs to is stamped from the credential and is never stated by
  the caller, so a product cannot be written into a tenant the caller is not acting in; the
  organization it is filed under is the one the caller states, which is what the delivered route
  reads.
  """
  createProduct(input: CreateProductInput!): Product!

  """
  Changes the facts of a product that exists, and the option groups and options it offers.
  
  The members are the ones the delivered edit path carries into the row. It reads the row first and
  writes the members stated here onto it, so a product that is not there is a miss rather than a
  write.
  """
  updateProduct(input: UpdateProductInput!): Product!

  """
  Applies a batch of products — written, changed and archived — and answers one outcome per item.
  
  The batch is the one `POST /products/bulk` applies, run by the same executor from the same
  declaration, so the resource name, the cap, the permission and the members an item must carry are
  the route's rather than a second statement of them. `atomic` is the whole point of the flag: an
  atomic batch applies every item or none of them, and a batch that is not atomic applies what it can
  and reports the rest.
  
  `idempotencyKey` is the retry key of this batch. A GraphQL request may carry many mutations in one
  document and a header cannot say which of them a key belongs to, so the key travels beside the
  input it qualifies. The kernel that stores and replays keys reads it there, which is why a key it
  cannot use is refused with the same code the route answers — and why a batch that states no key is
  served exactly as before.
  """
  bulkCreateProducts(input: BulkCreateProductsInput!): BulkCreateProductsPayload!

  """
  Removes a product, together with the rows only it explains: its variants and their setting and
  price rows, and its option groups with their options and translations. A variant left behind
  without its setting and price cannot be priced, sold or counted.
  """
  deleteProduct(id: ID!): Boolean!

  """
  Withdraws a product without removing the row.
  
  Soft removal is a different operation from the removal above and is the one to reach for when the
  product has been sold: the order lines that name it keep resolving, and the row is what they are
  explained by. `softDeleteProduct` answers the withdrawn row; the product is recoverable until it
  is removed outright.
  """
  softDeleteProduct(id: ID!): Product!

  """
  Puts a withdrawn product back. A product that is not withdrawn is refused rather than silently
  left as it is.
  """
  recoverProduct(id: ID!): Product!

  """
  Adds images to the product's gallery.
  
  An image is named by the identifier of its asset row: the asset itself is stored by the image
  domain, and the delivered gallery write stores the membership — which is the pair of identifiers —
  so a member carrying a whole asset would offer members nothing honours.
  """
  addProductGalleryImages(productId: ID!, imageIds: [ID!]!): Product!

  """
  Makes one image the product's cover. The image is named by its asset identifier, on the same terms
  as the gallery write above.
  """
  setProductAsFeatured(productId: ID!, imageId: ID!): Product!

  """
  Takes one image out of the product's gallery. The asset itself is kept: it may still be another
  product's cover, or a member of another product's gallery. An image a variant of this product
  carries is refused rather than detached.
  """
  deleteProductGalleryImage(productId: ID!, imageId: ID!): Product!

  """
  Withdraws the product's cover. The asset itself is kept, as it is for a gallery image.
  """
  deleteProductFeaturedImage(productId: ID!): Product!

  """
  Leaves a reaction, through the command the delivered create route dispatches.
  
  **This is a toggle rather than a filing.** The handler looks for a mark that is already there — by
  domain, row, emoji, organization and the acting employee — and withdraws that one instead of
  creating a second, so a caller that repeats the call takes the reaction back. The tenant and the
  acting employee are read from the credential and never stated here.
  """
  createReaction(input: CreateReactionInput!): Reaction!

  """
  Changes a reaction that exists, through the command the delivered edit route dispatches.
  
  A member the caller leaves out is left as it is: the handler's read narrows the row to the caller's
  own employee and its write persists only the members a body states — see `UpdateReactionInput`.
  """
  updateReaction(input: UpdateReactionInput!): Reaction!

  """
  Removes a reaction outright, narrowed to the caller's own employee and tenant by the delivered
  service. The answer says whether the removal happened rather than returning the removed row, because
  the delivered route answers the store's delete result.
  """
  deleteReaction(id: ID!): Boolean!

  """
  Withdraws a reaction without removing it: the row keeps its identifier and the delivered list stops
  answering it.
  """
  softDeleteReaction(id: ID!): Reaction!

  """
  Puts a withdrawn reaction back, clearing the marker the withdrawal set.
  """
  recoverReaction(id: ID!): Reaction!

  """
  Opens a commercial geography. The currency is checked against the platform's currency master
  before the row is written.
  """
  createRegion(input: CreateRegionInput!): Region!

  """
  Changes the descriptive facts of a region. A new currency is re-checked against the master.
  """
  updateRegion(input: UpdateRegionInput!): Region!

  """
  Retires a region: it keeps its rows and is no longer offered by its channels. The supported path,
  because a cart, a tax rate and a price list may all name it.
  """
  deleteRegion(id: ID!): Region!

  """
  Claims the organization's default region, releasing the flag from the previous holder.
  """
  setDefaultRegion(id: ID!): Region!

  """
  Replaces the served-country set. The operation is a set: the members stated are written and the
  ones left out are withdrawn in one transaction.
  """
  replaceRegionCountries(input: ReplaceRegionCountriesInput!): [RegionCountry!]!

  """
  Switches a report on or off in an organization's menu.
  
  The delivered write is a find-or-create: it looks the menu row up by the report and the
  organization of the caller's tenant and saves the stated flag onto it, and it creates the row when
  there is none. The answer is that row, so a caller that switched a report on reads back the
  membership it just wrote rather than a statement about the write.
  
  A body that names no report or no organization is refused by the delivered write with a `400`, which
  is why both members are required here: an input that let a caller omit them would let it make a
  request the route can only refuse.
  """
  updateReportMenu(input: UpdateReportMenuInput!): ReportOrganization!

  """
  Raises a request for approval.
  
  The same service method the delivered route calls, with the same body: the delivered create builds
  the row member by member, stamps the caller's tenant onto it and resolves the employees and teams
  the caller names into one pivot row each. The state the new request is in is the write's decision
  rather than the caller's, which is why no input states one.
  """
  createRequestApproval(input: CreateRequestApprovalInput!): RequestApproval!

  """
  Changes a request that exists.
  
  The same service method the delivered route calls, with the same two arguments: the identifier
  travels inside the input because the REST route carries it in the path, and the same body follows it.
  See `UpdateRequestApprovalInput` for which members the delivered write acts on — the two sets behave
  differently, and the edit returns every request it touches to the waiting state.
  """
  updateRequestApproval(input: UpdateRequestApprovalInput!): RequestApproval!

  """
  Approves one request.
  
  The same command the delivered route dispatches, carrying the same decision, and the delivered
  handler's answer is the row: it records the approval, and then moves the document the request is
  about — a time-off request or an equipment sharing — through the vocabulary that document's own
  status column speaks. Refusing a request a caller may not answer is the delivered service's own
  check, and this field lets that refusal through rather than restating it.
  """
  approveRequestApproval(id: ID!): RequestApproval!

  """
  Refuses one request.
  
  The same command the delivered route dispatches with the other decision, and the delivered handler's
  answer is the row. A refusal is a single approver's answer rather than a threshold's: the delivered
  writer sets the request to refused outright, which is what makes one refusal enough.
  """
  refuseRequestApproval(id: ID!): RequestApproval!

  """
  Removes a request outright.
  
  The answer says whether the removal happened rather than returning the removed row, because the
  delivered route answers the store's delete result. No permission is stated: the delivered
  `DELETE /:id` is inherited from the CRUD base without one of its own.
  """
  deleteRequestApproval(id: ID!): Boolean!

  """
  Withdraws a request without removing it: the row keeps its identifier and the recovery below reads
  it back. The answer is the withdrawn row, whose `deletedAt` is the whole of what the write set. No
  permission is stated, for the same reason the removal states none.
  """
  softDeleteRequestApproval(id: ID!): RequestApproval!

  """
  Puts a withdrawn request back, clearing the marker the withdrawal set.
  
  The tenant and organization the write runs under are read from the credential, because the delivered
  route validates its query string against the tenant-organization body the base controller applies.
  No permission is stated, for the same reason the removal states none.
  """
  recoverRequestApproval(id: ID!): RequestApproval!

  """
  Files a link against one record, attributed to the caller's own employee where the credential
  carries one.
  """
  createResourceLink(input: CreateResourceLinkInput!): ResourceLink!

  """
  Changes a link. A member the caller leaves out is left as it is — see `UpdateResourceLinkInput` for
  what the delivered edit expects a caller not to restate.
  """
  updateResourceLink(input: UpdateResourceLinkInput!): ResourceLink!

  """
  Removes a link outright. The two removals this resource serves are not the same operation, so both
  are stated rather than one of them being left to a client to discover on the other protocol.
  """
  deleteResourceLink(id: ID!): Boolean!

  """
  Withdraws a link without removing it. The answer is the row as it now stands, which is what the
  delivered route answers with.
  """
  softDeleteResourceLink(id: ID!): ResourceLink!

  """Puts a withdrawn link back, clearing the marker the withdrawal set."""
  recoverResourceLink(id: ID!): ResourceLink!

  """
  Adds a permission row for a role.
  
  The write is the delivered one, with its own refusal rules: nobody, not even a super
  administrator, may grant themselves a permission through this field, and an administrator may not
  touch the super administrator's own rows. Those rules are the service's, and it refuses here
  exactly as it refuses over REST. The row is filed under the caller's tenant, which the service
  stamps from the credential rather than reading from the input.
  """
  createRolePermission(input: CreateRolePermissionInput!): RolePermission!

  """
  Changes the permission a row names and whether the role carries it.
  
  The delivered route names the row in its path and carries the members in its body, and its service
  applies the same refusal rules as the create above; the answer is the row the write produced, read
  back through the same service, because the delivered result is the store's own statement about the
  write rather than a row.
  """
  updateRolePermission(input: UpdateRolePermissionInput!): RolePermission!

  """
  Removes a role permission outright: the row is gone and nothing reads it back.
  
  The delivered removal applies the same rules the two writes above do — the super administrator's own
  rows are refused — and that refusal is the service's rather than this field's.
  """
  deleteRolePermission(id: ID!): Boolean!

  """
  Withdraws a role permission without removing it: the row keeps its identifier, and the delivered
  reads stop answering it, because every one of them excludes a row a withdrawal has marked.
  """
  softDeleteRolePermission(id: ID!): RolePermission!

  """
  Puts a withdrawn role permission back, clearing the marker the withdrawal set.
  """
  recoverRolePermission(id: ID!): RolePermission!
  createRole(input: CreateRoleInput!): Role!
  updateRole(input: UpdateRoleInput!): Role!

  """
  Removes a role outright.
  
  The delivered removal narrows its own criteria before it runs — a system role is not selected, and
  neither is a name of the platform's own set — so this field does not pre-empt that rule: it reports
  that the removal ran, which is the same fact the REST route states with its own result, and a
  caller that must know whether a row went reads it back through `tenantRoles`.
  """
  deleteRole(id: ID!): Boolean!

  """
  Withdraws a role without removing it: the row keeps its identifier and the permission rows that
  point at it keep resolving, and the delivered list stops answering it.
  """
  softDeleteRole(id: ID!): Role!

  """Puts a withdrawn role back."""
  recoverRole(id: ID!): Role!

  """
  Opens a numbering series.
  
  `key` and the channel are the series' identity and are stated here once; the counter is stated here
  too, which is the one write that may state it — a series an installation adopts continues numbering
  an external system has already begun, and it has to know where to continue from. The tenant and the
  organization are stamped from the credential.
  """
  createSequence(input: CreateSequenceInput!): Sequence!

  """
  Changes the configuration of a series: the shape of the numbers it produces, the note kept beside
  it, and whether it numbers documents at all.
  
  The counter is not among the members this write accepts — see `UpdateSequenceInput` for why — so an
  edit can never renumber a document the installation has already issued. Stating `isActive: false`
  retires the series: the counter stays and an allocation answers with a reason instead of a number.
  """
  updateSequence(input: UpdateSequenceInput!): Sequence!

  """
  Restarts a series, when its own policy says a restart is due.
  
  **This is the move the allocator performs, performed on demand — not a column write.** The counter
  is rewound to the value a period starts at and the moment is recorded, under the same row lock the
  allocator takes and inside the same kind of transaction, so an allocation running beside this one
  is serialised against it exactly as it is against the allocator's own restart.
  
  It declines with the kernel's own reason when no restart is due: the series' policy is `NEVER`, it
  has already restarted inside the period this moment falls in, or no period is recorded for it yet —
  in which case its next allocation records one without discarding the configured counter. Nothing
  here counts the documents that carry the series' numbers, because nothing in the kernel does: a
  series is a key, and the domains that number their documents from it are not known to it.
  """
  resetSequence(id: ID!): Sequence!

  """
  Creates a share over one record of the caller's tenant, minting its token.
  """
  createSharedEntity(input: CreateSharedEntityInput!): SharedEntity!

  """
  Changes a share's rules. The target and the token are pinned at creation and are not members of the
  input — see `UpdateSharedEntityInput`.
  """
  updateSharedEntity(input: UpdateSharedEntityInput!): SharedEntity!

  """
  Removes a share outright. A token that has been removed no longer resolves, and there is nothing to
  restore; the withdrawal below is the removal that can be undone.
  """
  deleteSharedEntity(id: ID!): Boolean!

  """
  Withdraws a share without removing it: the row keeps its token, and a withdrawn share's token stops
  resolving while the row is withdrawn. The answer is the row as it now stands.
  """
  softDeleteSharedEntity(id: ID!): SharedEntity!

  """
  Puts a withdrawn share back, clearing the marker the withdrawal set and restoring what its token
  resolves.
  """
  recoverSharedEntity(id: ID!): SharedEntity!

  """
  Files a skill. The name and the colour are required because the store requires them: both columns
  are `NOT NULL` and carry no default.
  """
  createSkill(input: CreateSkillInput!): Skill!

  """
  Changes a skill. A member the caller leaves out is left as it is, because the delivered edit is a
  partial column update rather than a replacement of the row.
  """
  updateSkill(input: UpdateSkillInput!): Skill!

  """
  Removes a skill outright. The employees and organizations that pointed at it keep their own rows —
  the pivots that attached them are what cascades — which is why a caller that means "stop offering
  this skill" reaches for the soft removal below instead.
  """
  deleteSkill(id: ID!): Boolean!

  """
  Withdraws a skill without removing it. The answer is the row as it now stands, which is what the
  delivered route answers with.
  """
  softDeleteSkill(id: ID!): Skill!

  """Puts a withdrawn skill back, clearing the marker the withdrawal set."""
  recoverSkill(id: ID!): Skill!

  """Declares a group."""
  createTagType(input: CreateTagTypeInput!): TagType!

  """
  Changes a group that exists. A member the caller leaves out is left as it is: the delivered write
  updates the columns the body states rather than replacing the row — see `UpdateTagTypeInput`.
  """
  updateTagType(input: UpdateTagTypeInput!): TagType!

  """
  Removes a group outright. What becomes of the tags that were filed under it is the store's own
  constraint to decide — the relation is declared to clear rather than to cascade — and this field
  does not pre-empt it.
  """
  deleteTagType(id: ID!): Boolean!

  """
  Withdraws a group without removing it: the row keeps its identifier and the tags filed under it keep
  pointing at it, and the delivered reader stops answering it.
  """
  softDeleteTagType(id: ID!): TagType!

  """Puts a withdrawn group back."""
  recoverTagType(id: ID!): TagType!

  """Files a tag."""
  createTag(input: CreateTagInput!): Tag!

  """
  Changes the facts of a tag that exists. A member the caller leaves out is left as it is: the
  delivered write updates the columns the body states rather than replacing the row — see
  `UpdateTagInput`.
  """
  updateTag(input: UpdateTagInput!): Tag!

  """
  Removes a tag outright, with the pivot rows that attach it to the records it labelled. A tag is a
  label rather than a record of its own, so removing it unlabels whatever carried it; that cascade is
  the store's, and this field does not pre-empt it.
  """
  deleteTag(id: ID!): Boolean!

  """
  Withdraws a tag without removing it: the row keeps its identifier and the records that carry it
  keep pointing at it, and the delivered readers stop answering it.
  """
  softDeleteTag(id: ID!): Tag!

  """Puts a withdrawn tag back."""
  recoverTag(id: ID!): Tag!

  """Files a task."""
  createTask(input: CreateTaskInput!): Task!

  """Edits a task that exists."""
  updateTask(input: UpdateTaskInput!): Task!

  """Removes a task outright."""
  deleteTask(id: ID!): Boolean!

  """
  Takes one employee off every task of one team.
  
  The delivered route answers nothing at all — it is a statement about the tasks it touched rather
  than about a row — so this field answers whether it ran, which is the most the route's own answer
  carries.
  """
  unassignEmployeeFromTeamTasks(employeeId: ID!, organizationTeamId: ID!): Boolean!

  """Records an estimate for a task."""
  createTaskEstimation(input: CreateTaskEstimationInput!): TaskEstimation!

  """Changes an estimate."""
  updateTaskEstimation(input: UpdateTaskEstimationInput!): TaskEstimation!

  """
  Removes an estimate outright, with the activity log the delivered service writes beside it.
  """
  deleteTaskEstimation(id: ID!): Boolean!

  """Withdraws an estimate without removing it."""
  softDeleteTaskEstimation(id: ID!): TaskEstimation!

  """Puts a withdrawn estimate back."""
  recoverTaskEstimation(id: ID!): TaskEstimation!

  """Links two tasks."""
  createTaskLinkedIssue(input: CreateTaskLinkedIssueInput!): TaskLinkedIssue!

  """Changes an existing link."""
  updateTaskLinkedIssue(input: UpdateTaskLinkedIssueInput!): TaskLinkedIssue!

  """
  Removes a link outright, with the activity log the delivered service writes beside it.
  """
  deleteTaskLinkedIssue(id: ID!): Boolean!

  """Withdraws a link without removing it."""
  softDeleteTaskLinkedIssue(id: ID!): TaskLinkedIssue!

  """Puts a withdrawn link back."""
  recoverTaskLinkedIssue(id: ID!): TaskLinkedIssue!

  """Saves a view."""
  createTaskView(input: CreateTaskViewInput!): TaskView!

  """Changes a saved view."""
  updateTaskView(input: UpdateTaskViewInput!): TaskView!

  """Removes a saved view outright."""
  deleteTaskView(id: ID!): Boolean!

  """Withdraws a saved view without removing it."""
  softDeleteTaskView(id: ID!): TaskView!

  """Puts a withdrawn view back."""
  recoverTaskView(id: ID!): TaskView!

  """Opens a day's plan."""
  createDailyPlan(input: CreateDailyPlanInput!): DailyPlan!

  """Changes a day's plan."""
  updateDailyPlan(input: UpdateDailyPlanInput!): DailyPlan!

  """Removes a planned day outright."""
  deleteDailyPlan(id: ID!): Boolean!

  """Withdraws a planned day without removing it."""
  softDeleteDailyPlan(id: ID!): DailyPlan!

  """Puts a withdrawn planned day back."""
  recoverDailyPlan(id: ID!): DailyPlan!

  """Files one task into one planned day."""
  addTaskToDailyPlan(planId: ID!, input: DailyPlanTaskInput!): DailyPlan!

  """Takes one task out of one planned day."""
  removeTaskFromDailyPlan(planId: ID!, input: DailyPlanTaskInput!): DailyPlan!

  """Takes one task out of the plans it is in, for one employee."""
  removeTaskFromDailyPlans(taskId: ID!, input: DailyPlanTaskRemovalInput!): [DailyPlan!]!

  """
  Puts a task up for screening, filing the task itself in the same write.
  """
  createScreeningTask(input: CreateScreeningTaskInput!): ScreeningTask!

  """
  Records where a screening decision stands, and moves the task with it: a declined or duplicated
  decision cancels the task, and a pending or snoozed one keeps it in screening.
  """
  updateScreeningTask(input: UpdateScreeningTaskInput!): ScreeningTask!

  """Removes a screening decision outright."""
  deleteScreeningTask(id: ID!): Boolean!

  """Withdraws a screening decision without removing it."""
  softDeleteScreeningTask(id: ID!): ScreeningTask!

  """Puts a withdrawn screening decision back."""
  recoverScreeningTask(id: ID!): ScreeningTask!

  """Files a status."""
  createTaskStatus(input: CreateTaskStatusInput!): TaskStatus!

  """Changes a status."""
  updateTaskStatus(input: UpdateTaskStatusInput!): TaskStatus!

  """
  Removes a status outright. A status the platform maintains is the store's own constraint to
  refuse; this field does not pre-empt it.
  """
  deleteTaskStatus(id: ID!): Boolean!

  """Withdraws a status without removing it."""
  softDeleteTaskStatus(id: ID!): TaskStatus!

  """Puts a withdrawn status back."""
  recoverTaskStatus(id: ID!): TaskStatus!

  """
  Writes the board's column order for a set of statuses, one row per step.
  """
  reorderTaskStatuses(reorder: [TaskStatusOrderInput!]!): TaskStatusReorderResult!

  """
  Makes one status the default of one scope, clearing the mark from every other status of that
  scope and answering them all back.
  """
  markTaskStatusAsDefault(id: ID!, scope: TaskMetadataScopeInput!): [TaskStatus!]!

  """Files a size."""
  createTaskSize(input: CreateTaskSizeInput!): TaskSize!

  """Changes a size."""
  updateTaskSize(input: UpdateTaskSizeInput!): TaskSize!

  """
  Removes a size outright. A size the platform maintains is the store's own constraint to refuse.
  """
  deleteTaskSize(id: ID!): Boolean!

  """Withdraws a size without removing it."""
  softDeleteTaskSize(id: ID!): TaskSize!

  """Puts a withdrawn size back."""
  recoverTaskSize(id: ID!): TaskSize!

  """Files a priority."""
  createTaskPriority(input: CreateTaskPriorityInput!): TaskPriority!

  """Changes a priority."""
  updateTaskPriority(input: UpdateTaskPriorityInput!): TaskPriority!

  """
  Removes a priority outright. A priority the platform maintains is the store's own constraint to
  refuse.
  """
  deleteTaskPriority(id: ID!): Boolean!

  """Withdraws a priority without removing it."""
  softDeleteTaskPriority(id: ID!): TaskPriority!

  """Puts a withdrawn priority back."""
  recoverTaskPriority(id: ID!): TaskPriority!

  """Files a version."""
  createTaskVersion(input: CreateTaskVersionInput!): TaskVersion!

  """Changes a version."""
  updateTaskVersion(input: UpdateTaskVersionInput!): TaskVersion!

  """
  Removes a version outright. A version the platform maintains is the store's own constraint to
  refuse.
  """
  deleteTaskVersion(id: ID!): Boolean!

  """Withdraws a version without removing it."""
  softDeleteTaskVersion(id: ID!): TaskVersion!

  """Puts a withdrawn version back."""
  recoverTaskVersion(id: ID!): TaskVersion!

  """Files an issue type."""
  createIssueType(input: CreateIssueTypeInput!): IssueType!

  """Changes an issue type."""
  updateIssueType(input: UpdateIssueTypeInput!): IssueType!

  """
  Removes an issue type outright. An issue type the platform maintains is the store's own
  constraint to refuse.
  """
  deleteIssueType(id: ID!): Boolean!

  """Withdraws an issue type without removing it."""
  softDeleteIssueType(id: ID!): IssueType!

  """Puts a withdrawn issue type back."""
  recoverIssueType(id: ID!): IssueType!

  """
  Makes one issue type the default of one scope, clearing the mark from every other issue type of
  that scope and answering them all back.
  """
  markIssueTypeAsDefault(id: ID!, scope: TaskMetadataScopeInput!): [IssueType!]!

  """Files a related-issue type."""
  createTaskRelatedIssueType(input: CreateTaskRelatedIssueTypeInput!): TaskRelatedIssueType!

  """Changes a related-issue type."""
  updateTaskRelatedIssueType(input: UpdateTaskRelatedIssueTypeInput!): TaskRelatedIssueType!

  """
  Removes a related-issue type outright. One the platform maintains is the store's own constraint
  to refuse.
  """
  deleteTaskRelatedIssueType(id: ID!): Boolean!

  """Withdraws a related-issue type without removing it."""
  softDeleteTaskRelatedIssueType(id: ID!): TaskRelatedIssueType!

  """Puts a withdrawn related-issue type back."""
  recoverTaskRelatedIssueType(id: ID!): TaskRelatedIssueType!

  """
  Has an API key pair issued for the caller's own tenant, and answers it in clear text — the one and
  only time the secret is readable. A tenant that already holds a pair is refused, as the delivered
  route refuses it: the resource issues a pair for a tenant that has none.
  """
  generateTenantApiKeyPair(input: GenerateTenantApiKeyInput!): TenantApiKeyPair!

  """
  Files a tenant and onboards the caller into it: the delivered creation, which stamps the caller as
  the new tenant's owner. A caller that already belongs to a tenant is refused, as the delivered
  route refuses it.
  """
  createTenant(input: CreateTenantInput!): Tenant!

  """
  Replaces the caller's own tenant. The delivered edit is dispatched under the installation owner's
  role, so this field carries the same role guard its route does; the answer is the row read back,
  because the delivered route answers the store's own update result, which is a statement about the
  write rather than the tenant.
  """
  updateTenant(input: UpdateTenantInput!): Tenant!

  """
  Removes the caller's own tenant outright. The delivered removal is dispatched under the same
  installation owner's role, so this field carries the same role guard its route does.
  """
  deleteTenant: Boolean!

  """
  Saves the tenant's settings document: the file storage provider and the provider credentials the
  caller states. The answer is the document as it now stands, which is what the delivered route
  answers with.
  """
  updateTenantSetting(input: UpdateTenantSettingInput!): JSON!

  """
  Saves settings the caller names itself, as key/value pairs — the same write the delivered dynamic
  route performs, through the same command.
  """
  updateDynamicTenantSetting(input: UpdateDynamicTenantSettingInput!): JSON!

  """
  Saves the installation-wide defaults (`tenantId IS NULL`). This is the one write of the resource
  that is not scoped to the caller's tenant, which is why it is the one that asks for
  `GLOBAL_SETTING`.
  """
  saveGlobalTenantSetting(input: UpdateDynamicTenantSettingInput!): JSON!

  """
  Replaces the writable columns of one settings row. The delivered edit is the CRUD base's own — the
  row is read, then written under the verified identifier — so a caller naming a row that is not
  there is answered with the miss rather than with a write that recreates it.
  """
  updateTenantSettingRow(input: UpdateTenantSettingRowInput!): TenantSetting!

  """
  Removes a settings row outright: nothing reads it back, and a row that is not there is the
  service's own miss rather than a false answered from here.
  """
  deleteTenantSetting(id: ID!): Boolean!

  """
  Withdraws a settings row without removing it, so the resolution falls back to the next scope. The
  answer is the row as it now stands, which is what the delivered route answers with.
  """
  softDeleteTenantSetting(id: ID!): TenantSetting!

  """Puts a withdrawn settings row back."""
  recoverTenantSetting(id: ID!): TenantSetting!

  """
  Asks the platform to verify a Wasabi configuration by using it. The delivered route answers with the
  provider's own report and refuses with a 400 when the credentials are missing, so this field
  answers the report as a document and surfaces the refusal as an error.
  """
  validateWasabiFileStorage(input: WasabiFileStorageConfigInput!): JSON!

  """Changes the UI flavour this tenant's users are served."""
  updateTenantUiPreferences(input: UpdateTenantUiPreferencesInput!): TenantUiPreferences!

  """
  Sets the accrued days of one employee's balance under one policy for one year, creating the row when
  this is the first allocation.
  
  The delivered write **replaces** `accrued` rather than adding to it, so re-running an allocation for
  a period is idempotent instead of compounding, and `remaining` is recomputed from it. An employee or
  a policy that is not the caller's own is refused by the delivered write before anything is stored.
  """
  allocateTimeOffBalance(input: AllocateTimeOffBalanceInput!): TimeOffBalance!

  """
  Spends days from one balance, which is what approving a request does.
  
  The delivered write performs the subtraction and the balance check in one conditional statement, so
  two approvals landing at the same moment cannot each read the same `taken` and lose one of the two
  deductions. A request for more days than are left is refused as a request rather than answered with a
  clamped row.
  """
  deductTimeOffBalance(input: AdjustTimeOffBalanceInput!): TimeOffBalance!

  """
  Gives days back to one balance, which is what cancelling an approved request does.
  
  `days` is positive on both this field and the deduction above: the operation decides the direction,
  which is what stops a negative number from turning a spend into a grant. The delivered write clamps
  at zero, so reversing twice can never push `taken` below it.
  """
  reverseTimeOffBalance(input: AdjustTimeOffBalanceInput!): TimeOffBalance!

  """
  Rolls the unused days of one policy from one year into the next.
  
  The answer is **how many employee balances were rolled over** — a count rather than a row, because
  the delivered write touches two rows per employee: it records what left the source year in
  `carriedOut` and what arrived in the target year's `carriedForward`, so the same day is never
  available in two years at once. It is safe to run again: both sides are set to the recomputed value
  rather than added to. A policy that does not allow carry-forward, or a target year that is not later
  than the source year, is refused as a request.
  """
  carryForwardTimeOffBalances(input: CarryForwardTimeOffBalanceInput!): TimeOffCarryForwardResult!

  """
  Files a policy.
  
  The membership is stated as identifiers, and the delivered write resolves them against the caller's
  own organization before it stores anything, so an employee of another organization is not a member
  however it is named.
  """
  createTimeOffPolicy(input: CreateTimeOffPolicyInput!): TimeOffPolicy!

  """
  Edits a policy that exists.
  
  The delivered write verifies the row against the caller's own tenant and organization and edits it in
  place rather than removing and re-creating it, which is what keeps the identifier every balance and
  every request points at. A body without an organization is refused by the write, which is why the
  input requires one.
  """
  updateTimeOffPolicy(input: UpdateTimeOffPolicyInput!): TimeOffPolicy!

  """
  Removes a policy outright.
  
  The delivered route answers the store's own delete result rather than a row, so this field answers
  whether the removal ran — the one fact that call establishes.
  """
  deleteTimeOffPolicy(id: ID!): Boolean!

  """
  Withdraws a policy without removing it: the row keeps its identifier and the delivered list stops
  answering it.
  """
  softDeleteTimeOffPolicy(id: ID!): TimeOffPolicy!

  """Puts a withdrawn policy back."""
  recoverTimeOffPolicy(id: ID!): TimeOffPolicy!

  """
  Files a request, and the approval record that carries it forward, in one call.
  
  The delivered write stores the request and then files the approval row that says who has to decide it,
  so a request never exists without one. The status a caller states seeds that record; an absent status
  is the delivered write's own default, which is "requested".
  """
  createTimeOffRequest(input: CreateTimeOffRequestInput!): TimeOffRequest!

  """
  Edits a filed request.
  
  The permission is the delivered route's own and is the delete grant rather than the edit one — see
  the module comment above. A request that does not belong to the caller's tenant is refused by the
  delivered write before anything is stored, and the identifier it verifies is the one it writes.
  """
  updateTimeOffRequest(input: UpdateTimeOffRequestInput!): TimeOffRequest!

  """
  Approves a filed request.
  
  The transition is the delivered handler's: a request that is not awaiting a decision is refused, and
  the refusal is not restated here. The caller has to hold one of the two administrative roles, which
  is the route's own guard restated on this field.
  """
  approveTimeOffRequest(id: ID!): TimeOffRequest!

  """
  Denies a filed request, under the same guard, the same roles and the same permission as the approval
  above — the two routes differ in the decision they dispatch and in nothing else.
  """
  denyTimeOffRequest(id: ID!): TimeOffRequest!

  """
  Removes a request outright.
  
  The delivered route answers the store's own delete result rather than a row, so this field answers
  whether the removal ran — the one fact that call establishes.
  """
  deleteTimeOffRequest(id: ID!): Boolean!

  """
  Withdraws a request without removing it: the row keeps its identifier, the approval record filed
  beside it still points at it, and the delivered list stops answering it.
  """
  softDeleteTimeOffRequest(id: ID!): TimeOffRequest!

  """Puts a withdrawn request back."""
  recoverTimeOffRequest(id: ID!): TimeOffRequest!

  """
  Files several activities in one call, and answers the rows the write persisted.
  
  The delivered write dispatches a command that stamps the organization, the tenant, the engagement and
  the project of the request onto every row before it saves them, so the envelope's members win over
  anything a row states — which is why the rows do not state them. The rows come back in the order they
  were saved, and a body that states no activity files none rather than failing.
  """
  bulkSaveActivities(input: ActivityBulkInput!): [Activity!]!

  """
  Submits one encoded tracking payload.
  
  The delivered write decodes the payload to learn which session it belongs to, finds the slot the
  observation falls into — creating it when the range holds none — and files both forms of the payload
  inside it. The answer says which session and which slot it landed in, and carries the session as it
  stands after the write, which is the reading a client that has just submitted wants back.
  
  The instant the payload belongs to is the caller's to state and is never invented from the payload
  alone: the delivered write refuses a request that states no readable instant, so the field states the
  instant as required rather than accepting a body the write would refuse.
  """
  submitTrackingData(input: CustomTrackingSubmissionInput!): CustomTrackingSubmission!

  """
  Submits several encoded tracking payloads in one call.
  
  Each payload is processed on its own and answered on its own, with the position it held in the list, so
  a call that filed nineteen payloads and refused one says which one was refused rather than failing the
  whole call. A list that is empty is refused rather than answered empty, which is the delivered
  validation's own choice.
  """
  submitBulkTrackingData(input: CustomTrackingBulkInput!): CustomTrackingBulkSubmission!

  """
  Records time by hand, and answers the row that was written.
  
  The delivered write resolves the conflicts of the interval first — trimming the conflicting slots
  of the logs it finds — and files the interval into the payroll period it belongs to. That is the
  handler's work and is not restated here; what this field states is the same body the route states,
  with the employee replaced by the caller's own and the log type and source set as the delivered
  body sets them.
  """
  addManualTime(input: CreateManualTimeLogInput!): TimeLog!

  """
  Edits a hand-recorded interval, and answers the row as it stands after the write.
  
  The delivered write resolves the interval's conflicts again, stamps the instant it was edited and
  files the row into the period its new start belongs to. A body that states an employee the caller
  may not choose has it replaced by the caller's own, exactly as the create does.
  """
  updateManualTime(input: UpdateManualTimeLogInput!): TimeLog!

  """
  Removes time logs, outright or by withdrawal, and answers whether the removal ran.
  
  The delivered write refuses a caller that names no row, and refuses one that may not change the
  selected employee when the named rows belong to employees it does not manage; both refusals are the
  handler's and are not restated here.
  """
  deleteTimeLogs(input: DeleteTimeLogsInput!): Boolean!

  """
  Records a slot for an instant, or answers the slot that instant already belongs to. The delivered
  write merges the result into the ten-minute window the instant falls in, so the row it answers is
  the merged slot rather than the body that was posted.
  """
  createTimeSlot(
    """The slot to record."""
    input: CreateTimeSlotInput!
  ): TimeSlot!

  """
  Edits a slot that exists, or answers null when there is none. A caller whose body withdraws a member
  the delivered write does not state leaves that column as it was, because the write persists exactly
  the body it is handed.
  """
  updateTimeSlot(
    """The slot to edit, and the body to edit it with."""
    input: UpdateTimeSlotInput!
  ): TimeSlot

  """
  Removes the slots a caller names, as a withdrawal or as a hard delete. The identifiers are required
  and must not be empty, and the removal spans the logs the slots cover unless the force flag is
  stated.
  """
  deleteTimeSlots(
    """The slots to remove."""
    ids: [ID!]!

    """
    Whether the removal is a hard delete rather than a withdrawal. Defaults to a withdrawal.
    """
    forceDelete: Boolean

    """The organization the slots are read under."""
    organizationId: ID
  ): Boolean!

  """
  Starts the timer when none is running and stops it when one is, and answers the log it touched.
  
  Nullable, because a toggle that stops the timer ends where the stop write ends — a re-read that
  answers nothing when the row it wrote cannot be found — which is the possibility the route's own
  `log or null` declares.
  """
  toggleTimer(input: TimerToggleInput!): TimeLog

  """
  Starts the timer, and answers the log it created.
  
  Non-null, and the write cannot break that: the handler ends by reading back the row it wrote through
  a finder that raises on a miss, so its outcomes are a row and a raised refusal, never an empty
  answer.
  """
  startTimer(input: TimerToggleInput!): TimeLog!

  """
  Stops the running timer, and answers the log it stopped — the log itself, not a projection of it.
  
  Nullable for the same reason the toggle is, and declared as such by its own route: the handler's
  last statement re-reads the row it wrote and answers nothing when the row is not there. A caller
  whose timer is already stopped is refused by the delivered handler instead — the same refusal the
  REST route raises, in this protocol's vocabulary.
  """
  stopTimer(input: TimerToggleInput!): TimeLog

  """
  Raises a request to move the time booked to one project over to another. The request is filed in
  the pending state; nothing moves until an approver accepts it.
  """
  requestTimesheetProjectChange(input: RequestTimesheetProjectChangeInput!): TimesheetProjectChangeRequest!

  """
  Approves or rejects a pending request. Approving is what actually moves the time, inside one
  transaction that claims the request first so two approvers cannot both act on it.
  """
  reviewTimesheetProjectChange(id: ID!, input: ReviewTimesheetProjectChangeInput!): TimesheetProjectChangeRequest!

  """
  Moves one or more timesheets to a new status. Approving one records the approver and the instant,
  which the delivered handler owns.
  """
  updateTimesheetStatus(input: UpdateTimesheetStatusInput!): [Timesheet!]!

  """
  Submits one or more timesheets to their approver, or withdraws them again: the delivered write is
  one route whose status member selects the direction.
  """
  submitTimesheet(input: SubmitTimesheetInput!): [Timesheet!]!

  """
  Puts a person inside an organization.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller,
  so a membership cannot be written into a tenant the caller is not acting in. Both identifiers are
  required: a membership is the pivot between exactly two rows.
  """
  createUserOrganization(input: CreateUserOrganizationInput!): UserOrganization!

  """
  Changes the flags of a membership that exists.
  
  The delivered edit is a partial column update against the identifier the path names, so a member the
  caller leaves out is left as it is. The answer is the row the write produced, read back through the
  same service: the delivered route answers the store's own update result — a statement about the
  write — which is not a row and not what a field named `updateUserOrganization` may return.
  """
  updateUserOrganization(input: UpdateUserOrganizationInput!): UserOrganization!

  """
  Removes a person from an organization.
  
  The same command the REST route dispatches, and the delivered handler is deliberately more than a
  row deletion: it removes the person outright when this was their last membership, refuses the
  platform's own default accounts in a demo environment, and reserves removing a super administrator
  for a super administrator. None of that is restated here — the command is the whole of it on both
  protocols.
  
  Answering whether the removal happened rather than the removed row: which of the handler's two
  outcomes occurred is not a row either way, and a client that needs the row reads it before the call.
  """
  deleteUserOrganization(id: ID!): Boolean!

  """
  Withdraws a membership without removing it: the row keeps its identifier, and the recovery below
  reads it back. The answer is the row as it now stands, which is what the delivered route answers
  with.
  """
  softDeleteUserOrganization(id: ID!): UserOrganization!

  """
  Puts a withdrawn membership back, clearing the marker the withdrawal set.
  """
  recoverUserOrganization(id: ID!): UserOrganization!

  """
  Files an account.
  
  The tenant the new row belongs to is stamped from the credential and is never stated by the caller.
  The role is resolved from the database rather than trusted from the body, and granting one the caller
  may not grant is refused by the delivered handler rather than here.
  """
  createUser(input: CreateUserInput!): User!

  """
  Edits an account that exists.
  
  The same write the delivered route performs, with the identifier in both places the route carries it
  — the path and the body — because the service reads the body's. The delivered service answers the
  row it read back after the write, so this field answers a row and not the store's update result.
  """
  updateUser(input: UpdateUserInput!): User!

  """
  Writes the caller's own preferred language.
  
  The account written is the caller's: the route carries no identifier and the service takes the one
  the credential names, so this field cannot be pointed at anybody else. The delivered route answers
  the store's update result — a statement about the write rather than a row — so the answer here is the
  caller's own row read back, which is the fact a client asked for.
  """
  updatePreferredLanguage(preferredLanguage: String!): User!

  """
  Writes the caller's own preferred list layout, for the same reason and with the same answer as the
  language above.
  """
  updatePreferredComponentLayout(preferredComponentLayout: String!): User!

  """
  Merges one or more per-feature interface patches into the caller's own stored preferences and answers
  the result.
  
  The patch is a document keyed by feature — a feature that is already stored keeps every key the patch
  does not name, and a feature stated as null is removed — so it is carried as the kernel's `JSON`
  scalar rather than as an input object: a free-form map keyed by feature is not a shape an input type
  can state, and an input type that listed today's feature keys would refuse tomorrow's.
  """
  updateUserUiPreferences(patch: JSON!): JSON!

  """
  Removes an account outright.
  
  Answering whether the removal happened rather than the removed row: the delivered route answers the
  store's delete result, which is a statement about the write. The delivered service refuses a caller
  that names somebody else's account unless it holds the permission that may, so the check is not
  restated here.
  """
  deleteUser(id: ID!): Boolean!

  """
  Withdraws an account without removing it: the row keeps its identifier, and the recovery below reads
  it back. The answer is the row as it now stands, which is what the delivered route answers with.
  """
  softDeleteUser(id: ID!): User!

  """Puts a withdrawn account back, clearing the marker the withdrawal set."""
  recoverUser(id: ID!): User!

  """
  Erases the caller's data from every table of the tenant and leaves them in their first organization.
  
  **This is not a withdrawal.** The delivered service deletes the rows of every organization the caller
  belongs to except the first — including the rows of other people that belong to those organizations
  — and clears the caller's own third-party identity and preferences. It is refused outright in a demo
  environment. The field exists because the route delivers it and carries the same permission the
  route carries; nothing about reaching it over this protocol is wider than reaching it over the other.
  
  The delivered route answers the organization the caller is left in, which is a type this schema does
  not declare, so the field answers whether the reset happened. The organization itself is read from
  the organization's own surface.
  """
  factoryReset: Boolean!

  """
  Files a stock location, with the contact, the logo and the tag pivot it is given.
  """
  createWarehouse(input: CreateWarehouseInput!): Warehouse!

  """
  Changes a stock location that exists: the columns the body carries are written and the ones it does
  not are left as they are.
  """
  updateWarehouse(input: UpdateWarehouseInput!): Warehouse!

  """
  Removes a stock location outright. The stock levels held there are rows of their own and cascade at
  the store, which is why a caller that means "stop using this location" reaches for the soft removal
  below instead.
  """
  deleteWarehouse(id: ID!): Boolean!

  """
  Withdraws a stock location without removing it. The answer is the row as it now stands, which is
  what the delivered route answers with.
  """
  softDeleteWarehouse(id: ID!): Warehouse!

  """
  Puts a withdrawn stock location back, clearing the marker the withdrawal set.
  """
  recoverWarehouse(id: ID!): Warehouse!

  """
  Opens a stock level for each product named, at one location, with a variant level under it for every
  variant the product has.
  
  The answer is the level rows the write created. The delivered route answers the page envelope the
  bulk write produces — a list of rows and their count — and the list's own length is that count, so
  the field answers the rows rather than restating it.
  
  A product that is not there, or a location that is not, is refused: the delivered write raises a
  miss rather than answering an empty set, so the field lets that refusal through.
  """
  addWarehouseProducts(warehouseId: ID!, input: AddWarehouseProductsInput!): [WarehouseProduct!]!

  """
  Sets the quantity held at one product level of a location.
  
  The delivered write loads the level, sets the quantity and saves it back — it does not add to it —
  so the argument is the quantity the level is to hold, not a change to apply.
  """
  updateWarehouseProductQuantity(warehouseProductId: ID!, count: Decimal!): WarehouseProduct!

  """
  Sets the quantity held at one variant level, and rolls the sum of the level's variants back up into
  the product level above it — one write moves both rows, which is what keeps the cached product
  quantity and the variant levels it is the sum of from drifting apart.
  """
  updateWarehouseProductVariantQuantity(warehouseProductVariantId: ID!, count: Decimal!): WarehouseProductVariant!

  """
  Subscribes an endpoint and generates its signing secret.
  
  The secret is answered here and never again: every later read of the subscription answers a
  fingerprint. The endpoint must use HTTPS, and the events must name at least one pattern, because a
  subscription that could never deliver anything is refused rather than stored.
  """
  createWebhookSubscription(input: CreateWebhookSubscriptionInput!): WebhookSubscriptionCredential!

  """
  Changes the mutable facts of a subscription: its endpoint, its event selection, its channel and the
  descriptive members. A member the caller omits is left as it is.
  
  The switch, the counters and the secret are not among them — each has an operation of its own,
  because each is a different decision from editing an endpoint.
  """
  updateWebhookSubscription(input: UpdateWebhookSubscriptionInput!): WebhookSubscription!

  """
  Removes a subscription and, by the table's own cascade, its delivery log.
  
  Removal is the only way to stop a subscription for good: disabling keeps the row, its counters and
  its history, which is what an incident needs.
  """
  deleteWebhookSubscription(id: ID!): Boolean!

  """
  Generates a new signing secret.
  
  The previous secret stays valid for a grace window, so the endpoint can be reconfigured without
  losing deliveries; the instant it stops being accepted is answered beside the new one. This is the
  second and last moment a plaintext secret exists.
  """
  rotateWebhookSecret(id: ID!): WebhookSubscriptionCredential!

  """
  Switches a subscription back on and resets its failure counter.
  
  The reset is the point: keeping the count that switched the endpoint off would switch it off again
  on its first hiccup. Events produced while it was off are not replayed — a gap is redelivered
  deliberately, through `redeliverWebhook`, rather than by accident.
  """
  enableWebhookSubscription(id: ID!): WebhookSubscription!

  """
  Switches a subscription off.
  
  Disabling is immediate and loses nothing already queued: the deliveries already written walk their
  retry schedule to its end and dead-letter on their own. The switch is announced to subscribers of
  `webhookSubscriptionDisabled`, whichever protocol threw it.
  """
  disableWebhookSubscription(id: ID!, reason: String): WebhookSubscription!

  """
  Requeues one delivery for another attempt.
  
  The row is reset rather than duplicated and the payload it already holds is what goes out again, so
  a redelivery reproduces the original request rather than a newer projection of the same event. A
  delivery is requeued whatever status it reached, the terminal one included: a dead delivery is
  precisely the row an operator redelivers once the receiver has been fixed.
  
  The answer is the requeued row, not an attempt's outcome: the retry worker is what calls an
  endpoint, so neither this field nor the route behind it ever blocks on a partner's server.
  """
  redeliverWebhook(id: ID!): WebhookDelivery!
  createCollection(input: CreateCollectionInput!): Collection!
  updateCollection(id: ID!, input: UpdateCollectionInput!): Collection!
  deleteCollection(id: ID!): Boolean!

  """
  Retires a collection recoverably, so the membership and the channels that name it keep their class.
  """
  softDeleteCollection(id: ID!): Collection!

  """Restores a soft-deleted collection."""
  recoverCollection(id: ID!): Collection!
  addCollectionProducts(collectionId: ID!, productIds: [ID!]!): [CollectionProduct!]!
  removeCollectionProducts(collectionId: ID!, productIds: [ID!]!): [CollectionProduct!]!

  """
  Retires one collection membership recoverably, so the product stays curated into the collection.
  """
  softDeleteCollectionProduct(id: ID!): CollectionProduct!

  """Restores a soft-deleted collection membership."""
  recoverCollectionProduct(id: ID!): CollectionProduct!
  addCollectionVariants(collectionId: ID!, variantIds: [ID!]!): [CollectionVariant!]!

  """
  Writes the whole variant set of a collection in one call, which is the operation the service defines.
  """
  replaceCollectionVariants(collectionId: ID!, variantIds: [ID!]!): [CollectionVariant!]!
  removeCollectionVariants(collectionId: ID!, variantIds: [ID!]!): [CollectionVariant!]!

  """
  Retires one collection variant membership recoverably, so the variant keeps its place in the collection.
  """
  softDeleteCollectionVariant(id: ID!): CollectionVariant!

  """Restores a soft-deleted collection variant membership."""
  recoverCollectionVariant(id: ID!): CollectionVariant!
  attachProductVariantFacets(variantId: ID!, tagIds: [ID!]!): [TagProductVariant!]!
  detachProductVariantFacets(variantId: ID!, tagIds: [ID!]!): [TagProductVariant!]!

  """
  Retires one facet recoverably, so the variant keeps the tag it is filtered under.
  """
  softDeleteTagProductVariant(id: ID!): TagProductVariant!

  """Restores a soft-deleted facet."""
  recoverTagProductVariant(id: ID!): TagProductVariant!
  assignCollectionChannel(collectionId: ID!, input: CollectionChannelInput!): [CollectionChannel!]!
  unassignCollectionChannel(collectionId: ID!, channelId: ID!): [CollectionChannel!]!

  """
  Retires one collection publication recoverably, so the placement keeps its status and its date.
  """
  softDeleteCollectionChannel(id: ID!): CollectionChannel!

  """Restores a soft-deleted collection publication."""
  recoverCollectionChannel(id: ID!): CollectionChannel!
  publishProduct(productId: ID!, channelIds: [ID!]!, publishedAt: DateTime): [ProductPublication!]!
  unpublishProduct(productId: ID!, channelIds: [ID!]!, unpublishedAt: DateTime): [ProductPublication!]!

  """
  Updates one product publication: its status, its dates and its place in the channel's listing.
  """
  updateProductChannel(id: ID!, input: UpdateProductPublicationInput!): ProductPublication!

  """
  Retires one product publication recoverably, so the placement keeps its status and its date.
  """
  softDeleteProductChannel(id: ID!): ProductPublication!

  """Restores a soft-deleted product publication."""
  recoverProductChannel(id: ID!): ProductPublication!
  publishProductVariant(variantId: ID!, input: [ProductVariantPublicationInput!]!): [ProductVariantPublication!]!
  unpublishProductVariant(variantId: ID!, channelIds: [ID!]!): [ProductVariantPublication!]!

  """
  Updates one variant publication: its status, its dates and its place in the channel's listing.
  """
  updateProductVariantChannel(id: ID!, input: UpdateProductVariantPublicationInput!): ProductVariantPublication!

  """
  Retires one variant publication recoverably, so the placement keeps its status and its date.
  """
  softDeleteProductVariantChannel(id: ID!): ProductVariantPublication!

  """Restores a soft-deleted variant publication."""
  recoverProductVariantChannel(id: ID!): ProductVariantPublication!
  createProductRelation(input: CreateProductRelationInput!): ProductRelation!
  updateProductRelation(id: ID!, input: UpdateProductRelationInput!): ProductRelation!
  deleteProductRelation(id: ID!): Boolean!

  """
  Retires a relation recoverably, so the two products keep the recommendation between them.
  """
  softDeleteProductRelation(id: ID!): ProductRelation!

  """Restores a soft-deleted relation."""
  recoverProductRelation(id: ID!): ProductRelation!
  attachProductVariantMedia(variantId: ID!, input: [ProductVariantMediaInput!]!): [ProductVariantMedia!]!
  reorderProductVariantMedia(variantId: ID!, imageAssetIds: [ID!]!): [ProductVariantMedia!]!
  detachProductVariantMedia(variantId: ID!, imageAssetId: ID!): [ProductVariantMedia!]!

  """
  Retires one gallery row recoverably, so the gallery keeps its order and its thumbnail.
  """
  softDeleteProductVariantMedia(id: ID!): ProductVariantMedia!

  """Restores a soft-deleted gallery row."""
  recoverProductVariantMedia(id: ID!): ProductVariantMedia!

  """Create a price list."""
  createPriceList(input: CreatePriceListInput!): PriceList!

  """Update a price list."""
  updatePriceList(input: UpdatePriceListInput!): PriceList!

  """Delete a price list, softly unless `force` is set."""
  deletePriceList(id: ID!, force: Boolean): DeletePriceListPayload!

  """
  Retire a price list recoverably, keeping it and the prices it carries queryable.
  """
  softDeletePriceList(id: ID!): PriceList!

  """Restore a soft-deleted price list."""
  recoverPriceList(id: ID!): PriceList!

  """Publish a built price list, so that its prices begin to resolve."""
  activatePriceList(id: ID!): PriceList!

  """Withdraw a price list without deleting it or the prices it carries."""
  expirePriceList(id: ID!): PriceList!

  """
  Dry-run one price list against a context without writing anything.
  
  The resolution is the storefront's own, restricted to the list named, so a draft list can be
  previewed before it is published. A variant the list does not price is omitted rather than
  resolved to zero, exactly as `resolvePrice` omits it.
  """
  simulatePriceList(id: ID!, input: ResolvePriceInput!): [ResolvedPrice!]!

  """
  Create a price row: a default price, a tiered price, a list price or a scheduled one.
  """
  createProductPrice(input: CreateProductPriceInput!): ProductPrice!

  """Update a price row."""
  updateProductPrice(input: UpdateProductPriceInput!): ProductPrice!

  """Delete a price row, softly unless `force` is set."""
  deleteProductPrice(id: ID!, force: Boolean): DeleteProductPricePayload!

  """
  Retire a price row recoverably, keeping the amount a past resolution used queryable.
  """
  softDeleteProductPrice(id: ID!): ProductPrice!

  """Restore a soft-deleted price row."""
  recoverProductPrice(id: ID!): ProductPrice!

  """Write a price matrix, reporting the rows that were refused."""
  bulkUpsertProductPrices(input: BulkUpsertProductPricesInput!): BulkUpsertProductPricesPayload!

  """Create the preference a scope answers with, for a scope that has none."""
  createPricePreference(input: CreatePricePreferenceInput!): PricePreference!

  """Change the answer a scope gives about tax-inclusive presentation."""
  updatePricePreference(input: UpdatePricePreferenceInput!): PricePreference!

  """Delete a preference, softly unless `force` is set."""
  deletePricePreference(id: ID!, force: Boolean): DeletePricePreferencePayload!

  """
  Retire a preference recoverably, so that its scope falls back to the next answer.
  """
  softDeletePricePreference(id: ID!): PricePreference!

  """Restore a soft-deleted preference."""
  recoverPricePreference(id: ID!): PricePreference!

  """Create an exchange rate."""
  createExchangeRate(input: CreateExchangeRateInput!): ExchangeRate!

  """Update an exchange rate."""
  updateExchangeRate(input: UpdateExchangeRateInput!): ExchangeRate!

  """Delete an exchange rate, softly unless `force` is set."""
  deleteExchangeRate(id: ID!, force: Boolean): DeleteExchangeRatePayload!

  """
  Retire a rate recoverably, keeping a historical conversion reproducible.
  """
  softDeleteExchangeRate(id: ID!): ExchangeRate!

  """Restore a soft-deleted rate."""
  recoverExchangeRate(id: ID!): ExchangeRate!
  createTaxCategory(input: CreateTaxCategoryInput!): TaxCategory!
  updateTaxCategory(input: UpdateTaxCategoryInput!): TaxCategory!

  """
  Retires a category; the row is kept, because its rates are what placed tax lines point at.
  """
  deleteTaxCategory(id: ID!): TaxCategory!

  """
  Retires a category recoverably, so the rates that name it keep their class.
  """
  softDeleteTaxCategory(id: ID!): TaxCategory!

  """Restores a soft-deleted category."""
  recoverTaxCategory(id: ID!): TaxCategory!
  createTaxRate(input: CreateTaxRateInput!): TaxRate!
  updateTaxRate(input: UpdateTaxRateInput!): TaxRate!

  """Retires a rate; the row is kept, because placed tax lines name it."""
  deleteTaxRate(id: ID!): TaxRate!

  """
  Retires a rate recoverably, so the tax lines that name it stay explainable.
  """
  softDeleteTaxRate(id: ID!): TaxRate!

  """Restores a soft-deleted rate."""
  recoverTaxRate(id: ID!): TaxRate!

  """
  Replaces the ordered parts of a rate; an empty list returns it to its implied part.
  """
  setTaxRateParts(input: SetTaxRatePartsInput!): [TaxRatePart!]!
  createTaxRegime(input: CreateTaxRegimeInput!): TaxRegime!
  updateTaxRegime(input: UpdateTaxRegimeInput!): TaxRegime!

  """
  Retires a regime; the row is kept, because placed tax lines record the set they were taxed under.
  """
  deleteTaxRegime(id: ID!): TaxRegime!

  """
  Retires a regime recoverably, so the rates it selects can be selected again.
  """
  softDeleteTaxRegime(id: ID!): TaxRegime!

  """Restores a soft-deleted regime."""
  recoverTaxRegime(id: ID!): TaxRegime!

  """
  Sets which rates a regime selects. A regime that selects nothing is refused.
  """
  setTaxRegimeRates(input: SetTaxRegimeRatesInput!): [TaxRegimeRate!]!
  reconcileStockLevels(input: StockLevelReconciliationInput): StockLevelReconciliation!
  adjustStock(input: StockAdjustmentInput!): StockAdjustment!

  """
  Applies a drafted correction. The key is the resolver's own argument here because the mutation
  has no input object to carry it; the kernel reads both spellings, so the two protocols answer
  identically.
  """
  applyStockAdjustment(id: ID!, idempotencyKey: String): StockAdjustment!

  """
  Cancels a drafted correction, so that it can never be applied.
  
  The counterpart of the apply beside it and not a variant of it: an apply writes the correction's
  ledger row and closes the instruction as applied, while a cancel closes it without writing
  anything, because a correction that was never made has no movement to explain.
  """
  cancelStockAdjustment(id: ID!): StockAdjustment!
  createStockReservation(input: StockReservationInput!): StockReservation!
  releaseStockReservation(id: ID!, reason: String, idempotencyKey: String): StockReservation!
  consumeStockReservation(id: ID!): StockReservation!

  """
  Pushes the expiry of every active hold of a document.
  
  The path member is the document, not the kind of document: the kind is what a caller states
  beside it, and a caller that states none is answered for every active hold of that document. What
  comes back is how many holds were pushed out, which is what the route answers.
  """
  extendStockReservation(id: ID!, expiresAt: DateTime!, referenceType: String): Int!
  createStockTransfer(input: StockTransferInput!): StockTransfer!
  updateStockTransfer(id: ID!, note: String): StockTransfer!
  requestStockTransfer(id: ID!): StockTransfer!
  approveStockTransfer(id: ID!): StockTransfer!
  shipStockTransfer(id: ID!, lines: [StockTransferShipLineInput!]!, idempotencyKey: String): StockTransfer!
  receiveStockTransfer(id: ID!, lines: [StockTransferReceiveLineInput!]!, idempotencyKey: String): StockTransfer!
  cancelStockTransfer(id: ID!, reason: String): StockTransfer!
  addStockTransferLine(input: StockTransferLineInput!): StockTransferLine!
  createStockAlert(input: StockAlertInput!): StockAlert!
  updateStockAlert(id: ID!, input: StockAlertInput!): StockAlert!
  deleteStockAlert(id: ID!): Boolean!
  createStockCount(input: StockCountInput!): StockCount!
  openStockCount(id: ID!): StockCount!
  recordStockCountLine(id: ID!, lines: [StockCountLineInput!]!, idempotencyKey: String): StockCount!
  closeStockCount(id: ID!, idempotencyKey: String): StockCount!

  """
  Cancels a session without writing its corrections.
  
  The counterpart of the close beside it and not a variant of it: a close writes one correction per
  counted line, while a cancel ends the session and leaves the levels exactly where they were — the
  session was refused, not performed. The domain states both transitions in one row of its own
  surface table, and this is the second of them.
  """
  cancelStockCount(id: ID!): StockCount!
  assignChannelWarehouse(input: ChannelWarehouseInput!): ChannelWarehouse!
  unassignChannelWarehouse(channelId: ID!, warehouseId: ID!): Boolean!

  """Creates a zone inside a location."""
  createWarehouseZone(input: WarehouseZoneInput!): WarehouseZonePayload!

  """Updates a zone."""
  updateWarehouseZone(id: ID!, input: WarehouseZoneInput!): WarehouseZonePayload!

  """Rewrites the visiting order of a location's zones."""
  reorderWarehouseZones(warehouseId: ID!, zones: [WarehouseZonePriorityInput!]!): [WarehouseZone!]!

  """Blocks a zone, or puts it back into service."""
  setWarehouseZoneBlocked(id: ID!, isBlocked: Boolean!): WarehouseZonePayload!

  """Deletes a zone that holds no bin."""
  deleteWarehouseZone(id: ID!): WarehouseZonePayload!

  """
  Retires a zone recoverably, so the bins inside it keep the area they were addressed in.
  """
  softDeleteWarehouseZone(id: ID!): WarehouseZonePayload!

  """Restores a soft-deleted zone."""
  recoverWarehouseZone(id: ID!): WarehouseZonePayload!

  """Creates a bin."""
  createWarehouseBin(input: WarehouseBinInput!): WarehouseBinPayload!

  """Creates a consecutive range of bins."""
  createWarehouseBinRange(input: WarehouseBinRangeInput!): WarehouseBinRangePayload!

  """Updates a bin."""
  updateWarehouseBin(id: ID!, input: WarehouseBinInput!): WarehouseBinPayload!

  """Moves a bin and its subtree inside its zone."""
  reparentWarehouseBin(id: ID!, parentId: ID, idempotencyKey: String): WarehouseBinPayload!

  """Blocks a bin, or puts it back into service."""
  setWarehouseBinBlocked(id: ID!, isBlocked: Boolean!): WarehouseBinPayload!

  """Deletes a bin that is empty and holds no position under it."""
  deleteWarehouseBin(id: ID!): WarehouseBinPayload!

  """
  Retires a bin recoverably, so the rows that name it as their address stay readable.
  """
  softDeleteWarehouseBin(id: ID!): WarehouseBinPayload!

  """Restores a soft-deleted bin."""
  recoverWarehouseBin(id: ID!): WarehouseBinPayload!

  """Reconciles the bins of a location against the movement ledger."""
  reconcileWarehouseBins(input: ReconcileBinsInput!): BinReconciliationPayload!

  """
  Declares a bin as the home bin of a variant at a location. No movement is written.
  """
  assignWarehouseBinHome(id: ID!, input: AssignWarehouseBinInput!): AssignWarehouseBinPayload!

  """Walks received units from the receiving area into a bin."""
  putAwayWarehouseBin(id: ID!, input: PutAwayWarehouseBinInput!): PutAwayWarehouseBinPayload!

  """Creates a wave and the picking work it covers."""
  createPickWave(input: PickWaveInput!): PickWavePayload!

  """
  Updates a wave that has not been released, while its contents may still be amended.
  """
  updatePickWave(id: ID!, input: UpdatePickWaveInput!): PickWavePayload!

  """Releases a wave to the floor."""
  releasePickWave(id: ID!, pickerUserId: ID, idempotencyKey: String): PickWavePayload!

  """Marks a released wave as being walked."""
  startPickWave(id: ID!): PickWavePayload!

  """Completes a wave whose lists are all done."""
  completePickWave(id: ID!): PickWavePayload!

  """Closes a wave whose output was packed and manifested."""
  closePickWave(id: ID!, idempotencyKey: String): PickWavePayload!

  """Closes a wave short, releasing the work that will not be done."""
  closePickWaveShort(id: ID!, reason: String): PickWavePayload!

  """Cancels a wave nothing has been picked from."""
  cancelPickWave(id: ID!, reason: String): PickWavePayload!

  """
  Deletes a wave outright, destroying the work it records. `softDeletePickWave` retires it recoverably instead.
  """
  deletePickWave(id: ID!): PickWavePayload!

  """
  Retires a wave recoverably, so its lists and the work they record stay explainable.
  """
  softDeletePickWave(id: ID!): PickWavePayload!

  """Restores a soft-deleted wave."""
  recoverPickWave(id: ID!): PickWavePayload!

  """Creates a pick list from shipments."""
  createPickList(input: PickListInput!): PickListPayload!

  """
  Updates a pick list: the assignee, the priority and the picker-facing note a dispatcher still owns.
  """
  updatePickList(id: ID!, input: UpdatePickListInput!): PickListPayload!

  """Assigns a pick list to a picker."""
  assignPickList(id: ID!, assignedToUserId: ID!): PickListPayload!

  """Marks a pick list as being walked."""
  startPickList(id: ID!): PickListPayload!

  """Completes a pick list whose lines all reached an outcome."""
  completePickList(id: ID!): PickListPayload!

  """Cancels a pick list nothing has been picked from."""
  cancelPickList(id: ID!, reason: String): PickListPayload!

  """
  Deletes a pick list outright, destroying the lines and outcomes it holds. `softDeletePickList` retires it recoverably instead.
  """
  deletePickList(id: ID!): PickListPayload!

  """
  Retires a pick list recoverably, so the lines it was walked with keep their list.
  """
  softDeletePickList(id: ID!): PickListPayload!

  """Restores a soft-deleted pick list."""
  recoverPickList(id: ID!): PickListPayload!

  """Records what was taken from the bin."""
  pickPickListLine(
    pickListId: ID!
    lineId: ID!
    pickedQuantity: Decimal!
    binId: ID
    lotNumber: String
    serialNumbers: [String!]
    note: String

    """
    The level counter the outcome is conditioned on, stated as the input member `version`.
    """
    version: Int
    idempotencyKey: String
  ): PickListLinePayload!

  """Records a substitute: a different unit was taken."""
  substitutePickListLine(pickListId: ID!, lineId: ID!, substituteVariantId: ID!, substituteQuantity: Decimal!, substitutionReason: String, binId: ID, note: String, version: Int, idempotencyKey: String): PickListLinePayload!

  """Records a line the picker deliberately did not collect."""
  skipPickListLine(pickListId: ID!, lineId: ID!, note: String): PickListLinePayload!

  """
  Retires a line recoverably, so a packing record that names it stays complete.
  """
  softDeletePickListLine(id: ID!): PickListLinePayload!

  """Restores a soft-deleted pick line."""
  recoverPickListLine(id: ID!): PickListLinePayload!

  """Creates a pack slip from a picked list."""
  createPackSlip(warehouseId: ID!, pickListId: ID, fulfillmentId: ID, packageCount: Int): PackSlipPayload!

  """
  Updates an open pack slip: its parcel count, its weight and volume of record, its carrier and its label.
  """
  updatePackSlip(id: ID!, input: UpdatePackSlipInput!): PackSlipPayload!

  """Records the packing and seals the slip."""
  packPackSlip(id: ID!, input: PackSlipContentInput!): PackSlipPayload!

  """Voids a slip that was never packed."""
  voidPackSlip(id: ID!, reason: String): PackSlipPayload!

  """
  Deletes a pack slip outright, destroying the packing record a carrier claim reads. `softDeletePackSlip` retires it recoverably instead.
  """
  deletePackSlip(id: ID!): PackSlipPayload!

  """
  Retires a slip recoverably, so the lines it covers keep the parcel they went into.
  """
  softDeletePackSlip(id: ID!): PackSlipPayload!

  """Restores a soft-deleted pack slip."""
  recoverPackSlip(id: ID!): PackSlipPayload!

  """Builds a draft manifest for a carrier."""
  createCarrierManifest(input: CarrierManifestInput!): CarrierManifestPayload!

  """
  Updates a draft manifest: the dispatch day, the collection window and the note. Membership is never stated.
  """
  updateCarrierManifest(id: ID!, input: UpdateCarrierManifestInput!): CarrierManifestPayload!

  """Submits a draft manifest, freezing its membership."""
  submitCarrierManifest(id: ID!): CarrierManifestPayload!

  """Hands the parcels over to the carrier."""
  handOverCarrierManifest(id: ID!, input: HandOverManifestInput): CarrierManifestPayload!

  """Cancels a manifest before hand-over."""
  cancelCarrierManifest(id: ID!, reason: String): CarrierManifestPayload!

  """
  Deletes a manifest outright, destroying the hand-over record the carrier accepted. `softDeleteCarrierManifest` retires it recoverably instead.
  """
  deleteCarrierManifest(id: ID!): CarrierManifestPayload!

  """
  Retires a manifest recoverably, so the shipments it covered keep their hand-over record.
  """
  softDeleteCarrierManifest(id: ID!): CarrierManifestPayload!

  """Restores a soft-deleted manifest."""
  recoverCarrierManifest(id: ID!): CarrierManifestPayload!
  createCart(input: CreateCartInput!): Cart!
  updateCart(id: ID!, input: UpdateCartInput!): Cart!
  deleteCart(id: ID!, version: Int): Boolean!
  associateCartWithContact(id: ID!, contactId: ID!, version: Int): Cart!
  mergeCarts(targetCartId: ID!, sourceCartId: ID!, version: Int, idempotencyKey: String): Cart!
  addCartLine(input: AddCartLineInput!): Cart!
  updateCartLine(input: UpdateCartLineInput!): Cart!
  removeCartLine(cartId: ID!, lineId: ID!, version: Int): Cart!
  setCartShippingMethod(input: SetCartShippingMethodInput!): Cart!
  removeCartShippingMethod(cartId: ID!, version: Int): Cart!
  applyCartPromotion(input: ApplyCartPromotionInput!): Cart!
  removeCartPromotion(cartId: ID!, code: String!, version: Int): Cart!
  startCheckout(input: StartCheckoutInput!): CheckoutSession!
  completeCheckout(input: CompleteCheckoutInput!): CheckoutResult!
  abandonCheckout(cartId: ID!, version: Int): Cart!

  """
  Recomputes a cart's prices, promotions and totals. The version argument is the one the caller read the cart at.
  """
  recalculateCommerceCart(id: ID!, version: Int): Cart!

  """
  Amends a checkout session's own record, as its update route's body states it.
  """
  updateCommerceCheckoutSession(id: ID!, input: UpdateCheckoutSessionInput!): CheckoutSession!

  """
  Records that a step of a checkout session completed, appending it to the session's path.
  """
  completeStepCommerceCheckoutSession(id: ID!, step: String!, data: JSON, idempotencyKey: String): CheckoutSession!

  """
  Deletes a checkout session outright. `softDeleteCommerceCheckoutSession` retires it recoverably instead.
  """
  deleteCommerceCheckoutSession(id: ID!): Boolean!

  """
  Retires a cart recoverably, keeping its lines, delivery choices and promotions.
  """
  softDeleteCommerceCart(id: ID!): Cart!

  """Restores a soft-deleted cart."""
  recoverCommerceCart(id: ID!): Cart!

  """Retires a cart line recoverably, keeping its price snapshot."""
  softDeleteCommerceCartLine(id: ID!): CartLine!

  """Restores a soft-deleted cart line."""
  recoverCommerceCartLine(id: ID!): CartLine!

  """
  Retires an applied promotion recoverably, keeping the discount it recorded.
  """
  softDeleteCommerceCartPromotion(id: ID!): CartPromotion!

  """Restores a soft-deleted applied promotion."""
  recoverCommerceCartPromotion(id: ID!): CartPromotion!

  """
  Retires a delivery choice recoverably, keeping the amount it was quoted at.
  """
  softDeleteCommerceCartShippingMethod(id: ID!): CartShippingMethod!

  """Restores a soft-deleted delivery choice."""
  recoverCommerceCartShippingMethod(id: ID!): CartShippingMethod!

  """
  Retires a checkout session recoverably, keeping the progress it recorded.
  """
  softDeleteCommerceCheckoutSession(id: ID!): CheckoutSession!

  """Restores a soft-deleted checkout session."""
  recoverCommerceCheckoutSession(id: ID!): CheckoutSession!
  createOrder(input: CreateOrderInput!): Order!
  updateOrder(id: ID!, input: UpdateOrderInput!): Order!
  cancelOrder(id: ID!, reason: String, version: Int, idempotencyKey: String): Order!
  archiveOrder(id: ID!, version: Int): Order!
  placeOrder(id: ID!, version: Int, idempotencyKey: String): Order!
  confirmOrder(id: ID!, version: Int): Order!
  recalculateOrder(id: ID!, version: Int): Order!

  """Create a change: the only way a placed order is modified."""
  requestOrderEdit(input: RequestOrderEditInput!): OrderChange!

  """
  Applies a change. The version argument is the version of the ORDER the change belongs to — the
  value the order itself carries, not the version of the change, which is the order version the
  change produces — and an order that has moved past it answers a conflict rather than applying
  the change.
  """
  confirmOrderChange(id: ID!, version: Int, idempotencyKey: String): OrderChange!

  """
  Declines a change. The version argument is the version of the order the change belongs to.
  """
  declineOrderChange(id: ID!, reason: String, version: Int): OrderChange!

  """
  Cancels a change. The version argument is the version of the order the change belongs to.
  """
  cancelOrderChange(id: ID!, reason: String, version: Int): OrderChange!

  """
  Records a link and moves the line's counters with it, in one transaction.
  """
  recordOrderLineInvoice(input: OrderLineInvoiceInput!): OrderLineInvoicePayload!

  """
  Amends a link's tenant extras; the rest of the row describes an issued document.
  """
  updateOrderLineInvoice(id: ID!, metadata: JSON): OrderLineInvoice!

  """Removes a link and re-derives the line's counters from what remains."""
  deleteOrderLineInvoice(id: ID!): DeleteOrderLineInvoicePayload!

  """
  Re-derives the counters of a line from its links — the reconciliation read.
  """
  recomputeOrderLineInvoices(orderLineId: ID!, basisQuantity: Decimal): OrderLine!

  """Records one refund against a line, in as many parts as it was paid in."""
  recordOrderLineRefund(input: OrderLineRefundInput!): OrderLine!

  """Retires an order recoverably, keeping the row and its satellites."""
  softDeleteOrder(id: ID!): Order!

  """Restores a soft-deleted order."""
  recoverOrder(id: ID!): Order!

  """Retires a frozen address of an order recoverably."""
  softDeleteOrderAddress(id: ID!): OrderAddress!

  """Restores a soft-deleted address of an order."""
  recoverOrderAddress(id: ID!): OrderAddress!

  """
  Retires a change recoverably, keeping what it asked for and what it did.
  """
  softDeleteOrderChange(id: ID!): OrderChange!

  """Restores a soft-deleted change."""
  recoverOrderChange(id: ID!): OrderChange!

  """Retires one action of a change recoverably."""
  softDeleteOrderChangeAction(id: ID!): OrderChangeAction!

  """Restores a soft-deleted action of a change."""
  recoverOrderChangeAction(id: ID!): OrderChangeAction!

  """
  Retires a credit line recoverably, keeping what the order was reduced by.
  """
  softDeleteOrderCreditLine(id: ID!): OrderCreditLine!

  """Restores a soft-deleted credit line."""
  recoverOrderCreditLine(id: ID!): OrderCreditLine!

  """Retires one timeline entry recoverably."""
  softDeleteOrderHistory(id: ID!): OrderHistory!

  """Restores a soft-deleted timeline entry."""
  recoverOrderHistory(id: ID!): OrderHistory!

  """
  Retires an order line recoverably, keeping its price snapshot and its counters.
  """
  softDeleteOrderLine(id: ID!): OrderLine!

  """Restores a soft-deleted order line."""
  recoverOrderLine(id: ID!): OrderLine!

  """
  Retires a line-to-invoice link recoverably, keeping the evidence of what was billed.
  """
  softDeleteOrderLineInvoice(id: ID!): OrderLineInvoice!

  """Restores a soft-deleted line-to-invoice link."""
  recoverOrderLineInvoice(id: ID!): OrderLineInvoice!

  """
  Retires a delivery choice recoverably, keeping the amount the order was placed with.
  """
  softDeleteOrderShippingMethod(id: ID!): OrderShippingMethod!

  """Restores a soft-deleted delivery choice."""
  recoverOrderShippingMethod(id: ID!): OrderShippingMethod!

  """Retires one totals summary recoverably."""
  softDeleteOrderSummary(id: ID!): OrderSummary!

  """Restores a soft-deleted totals summary."""
  recoverOrderSummary(id: ID!): OrderSummary!

  """Retires one ledger transaction recoverably."""
  softDeleteOrderTransaction(id: ID!): OrderTransaction!

  """Restores a soft-deleted ledger transaction."""
  recoverOrderTransaction(id: ID!): OrderTransaction!

  """Deletes an order outright, dropping the row and every satellite of it."""
  deleteOrder(id: ID!): OrderDeleteResult!

  """
  Deletes a frozen address outright. `softDeleteOrderAddress` retires it recoverably instead.
  """
  deleteOrderAddress(id: ID!): OrderDeleteResult!

  """
  Amends a pending change's own columns. The version argument is the version of the ORDER the change
  belongs to — a change has no version of its own, because the `version` column on it is the order
  version the change produces — and an order that has moved past it answers a conflict.
  """
  updateOrderChange(id: ID!, input: UpdateOrderChangeInput!, version: Int): OrderChange!

  """Deletes a change outright, with the actions it carries."""
  deleteOrderChange(id: ID!): OrderDeleteResult!

  """Amends one action of a pending change, before the change is applied."""
  updateOrderChangeAction(id: ID!, input: UpdateOrderChangeActionInput!): OrderChangeAction!

  """Deletes one action of a pending change outright."""
  deleteOrderChangeAction(id: ID!): OrderDeleteResult!

  """Amends a credit line that was applied to an order."""
  updateOrderCreditLine(id: ID!, input: UpdateOrderCreditLineInput!): OrderCreditLine!

  """
  Deletes a credit line outright. `softDeleteOrderCreditLine` retires it recoverably instead.
  """
  deleteOrderCreditLine(id: ID!): OrderDeleteResult!
  createPaymentProvider(input: CreatePaymentProviderInput!): CreatePaymentProviderPayload!
  updatePaymentProvider(input: UpdatePaymentProviderInput!): UpdatePaymentProviderPayload!
  deletePaymentProvider(id: ID!): DeletePaymentProviderPayload!
  createPaymentCollection(input: CreatePaymentCollectionInput!): CreatePaymentCollectionPayload!
  updatePaymentCollection(input: UpdatePaymentCollectionInput!): UpdatePaymentCollectionPayload!
  openPaymentSession(input: OpenPaymentSessionInput!): OpenPaymentSessionPayload!
  authorizePaymentSession(input: AuthorizePaymentSessionInput!): AuthorizePaymentSessionPayload!
  voidPaymentSession(input: VoidPaymentSessionInput!): VoidPaymentSessionPayload!

  """
  Corrects the recorded fields of an attempt, without running any of its four verbs.
  """
  updatePaymentSession(input: UpdatePaymentSessionInput!): UpdatePaymentSessionPayload!

  """
  Re-reads the state of an attempt and closes one that has outlived its lifetime. It does not
  re-poll the provider: what the provider answered is written by the call that reached it, and
  this operation is the package's own expiry half, which a caller otherwise waits for the sweep to
  perform.
  """
  refreshPaymentSession(id: ID!): RefreshPaymentSessionPayload!
  capturePayment(input: CapturePaymentInput!): CapturePaymentPayload!
  createRefund(input: CreateRefundInput!): CreateRefundPayload!
  updateRefund(input: UpdateRefundInput!): UpdateRefundPayload!
  approveRefund(input: ApproveRefundInput!): ApproveRefundPayload!
  cancelRefund(input: CancelRefundInput!): CancelRefundPayload!
  createRefundReason(input: CreateRefundReasonInput!): CreateRefundReasonPayload!
  updateRefundReason(input: UpdateRefundReasonInput!): UpdateRefundReasonPayload!
  deleteRefundReason(id: ID!): DeleteRefundReasonPayload!
  createRefundLine(input: CreateRefundLineInput!): CreateRefundLinePayload!
  updateRefundLine(input: UpdateRefundLineInput!): UpdateRefundLinePayload!
  deleteRefundLine(id: ID!): DeleteRefundLinePayload!
  reprocessPaymentWebhookEvent(input: ReprocessPaymentWebhookEventInput!): ReprocessPaymentWebhookEventPayload!
  createPaymentAccountHolder(input: CreatePaymentAccountHolderInput!): CreatePaymentAccountHolderPayload!
  updatePaymentAccountHolder(input: UpdatePaymentAccountHolderInput!): UpdatePaymentAccountHolderPayload!
  verifyPaymentAccountHolder(input: VerifyPaymentAccountHolderInput!): VerifyPaymentAccountHolderPayload!
  deletePaymentAccountHolder(id: ID!): DeletePaymentAccountHolderPayload!
  createPaymentMethodToken(input: CreatePaymentMethodTokenInput!): CreatePaymentMethodTokenPayload!

  """
  Corrects the display facts of a saved instrument. The stored reference is not among them.
  """
  updatePaymentMethodToken(input: UpdatePaymentMethodTokenInput!): UpdatePaymentMethodTokenPayload!
  setDefaultPaymentMethodToken(id: ID!): SetDefaultPaymentMethodTokenPayload!
  revokePaymentMethodToken(id: ID!): RevokePaymentMethodTokenPayload!

  """
  Retires a provider registration recoverably, keeping the sessions it served.
  """
  softDeletePaymentProvider(id: ID!): SoftDeletePaymentProviderPayload!

  """Restores a soft-deleted provider registration."""
  recoverPaymentProvider(id: ID!): RecoverPaymentProviderPayload!

  """Retires a collection recoverably, keeping the amounts it reconciled."""
  softDeletePaymentCollection(id: ID!): SoftDeletePaymentCollectionPayload!

  """Restores a soft-deleted collection."""
  recoverPaymentCollection(id: ID!): RecoverPaymentCollectionPayload!

  """
  Retires a payment attempt recoverably, keeping the authorisation it recorded.
  """
  softDeletePaymentSession(id: ID!): SoftDeletePaymentSessionPayload!

  """Restores a soft-deleted payment attempt."""
  recoverPaymentSession(id: ID!): RecoverPaymentSessionPayload!

  """
  Retires a capture recoverably, keeping the ledger row the money was taken against.
  """
  softDeletePaymentCapture(id: ID!): SoftDeletePaymentCapturePayload!

  """Restores a soft-deleted capture."""
  recoverPaymentCapture(id: ID!): RecoverPaymentCapturePayload!

  """
  Retires a refund recoverably, keeping what it explains about the money given back.
  """
  softDeleteRefund(id: ID!): SoftDeleteRefundPayload!

  """Restores a soft-deleted refund."""
  recoverRefund(id: ID!): RecoverRefundPayload!

  """
  Retires a governed refund reason recoverably, so the refunds citing it stay explainable.
  """
  softDeleteRefundReason(id: ID!): SoftDeleteRefundReasonPayload!

  """Restores a soft-deleted refund reason."""
  recoverRefundReason(id: ID!): RecoverRefundReasonPayload!

  """
  Retires a line of a refund's breakdown recoverably, keeping the refund it accounted for.
  """
  softDeleteRefundLine(id: ID!): SoftDeleteRefundLinePayload!

  """Restores a soft-deleted line of a refund's breakdown."""
  recoverRefundLine(id: ID!): RecoverRefundLinePayload!

  """
  Retires an inbound callback recoverably, keeping the record that it arrived.
  """
  softDeletePaymentWebhookEvent(id: ID!): SoftDeletePaymentWebhookEventPayload!

  """Restores a soft-deleted inbound callback."""
  recoverPaymentWebhookEvent(id: ID!): RecoverPaymentWebhookEventPayload!

  """
  Retires a party's account at a provider recoverably, keeping the charges that point at it.
  """
  softDeletePaymentAccountHolder(id: ID!): SoftDeletePaymentAccountHolderPayload!

  """Restores a soft-deleted account at a provider."""
  recoverPaymentAccountHolder(id: ID!): RecoverPaymentAccountHolderPayload!

  """
  Retires a saved instrument recoverably, keeping the charge history that references it.
  """
  softDeletePaymentMethodToken(id: ID!): SoftDeletePaymentMethodTokenPayload!

  """Restores a soft-deleted saved instrument."""
  recoverPaymentMethodToken(id: ID!): RecoverPaymentMethodTokenPayload!

  """Creates a promotion in draft."""
  createPromotion(input: CreatePromotionInput!): CreatePromotionPayload!

  """
  Changes a promotion. The status is moved by the activation and expiry mutations.
  """
  updatePromotion(id: ID!, input: UpdatePromotionInput!): UpdatePromotionPayload!

  """Deletes a promotion."""
  deletePromotion(id: ID!): DeletePromotionPayload!

  """Starts a promotion, so its rules may match."""
  activatePromotion(id: ID!): ActivatePromotionPayload!

  """
  Stops a promotion, so its rules no longer match anything.
  
  Deactivation and expiry are separate acts, not one field with a status argument: a promotion an
  operator pulled is `INACTIVE` and can be started again, while one whose window closed is
  `EXPIRED`.
  """
  deactivatePromotion(id: ID!, input: DeactivatePromotionInput): DeactivatePromotionPayload!

  """
  Replaces the whole action set of a promotion, rather than merging into it.
  """
  replacePromotionActions(id: ID!, input: ReplacePromotionActionsInput!): ReplacePromotionActionsPayload!

  """
  Dry-runs one promotion against a basket, writing nothing.
  
  The evaluation the checkout runs, restricted to the promotion named, so "why did this offer not
  fire" is answerable before it is published.
  """
  simulatePromotion(id: ID!, input: PromotionSimulationInput!): SimulatePromotionPayload!

  """
  Retires a promotion recoverably: its counters and its redemption ledger are kept.
  """
  softDeletePromotion(id: ID!): SoftDeletePromotionPayload!

  """Restores a soft-deleted promotion."""
  recoverPromotion(id: ID!): RecoverPromotionPayload!

  """
  Retires one action of a promotion recoverably, keeping its place in the application order.
  """
  softDeletePromotionAction(id: ID!): SoftDeletePromotionActionPayload!

  """Restores a soft-deleted action of a promotion."""
  recoverPromotionAction(id: ID!): RecoverPromotionActionPayload!

  """
  Retires one application of a promotion recoverably, keeping the counters it was counted in.
  """
  softDeletePromotionUsage(id: ID!): SoftDeletePromotionUsagePayload!

  """Restores a soft-deleted application of a promotion."""
  recoverPromotionUsage(id: ID!): RecoverPromotionUsagePayload!

  """Closes a promotion before its window ends."""
  expirePromotion(id: ID!, input: ExpirePromotionInput): ExpirePromotionPayload!

  """Creates a campaign."""
  createCampaign(input: CreateCampaignInput!): CreateCampaignPayload!

  """Changes a campaign."""
  updateCampaign(id: ID!, input: UpdateCampaignInput!): UpdateCampaignPayload!

  """Deletes a campaign."""
  deleteCampaign(id: ID!): DeleteCampaignPayload!

  """
  Retires a campaign recoverably, so the promotions that name it keep their window.
  """
  softDeleteCampaign(id: ID!): SoftDeleteCampaignPayload!

  """Restores a soft-deleted campaign."""
  recoverCampaign(id: ID!): RecoverCampaignPayload!

  """Sets or replaces the single budget of a campaign."""
  updateCampaignBudget(campaignId: ID!, input: UpdateCampaignBudgetInput!): UpdateCampaignBudgetPayload!

  """
  Resets the consumption of a campaign's ceiling, re-opening a budget that has been spent.
  
  It is the one act that makes a ceiling forget what it paid out, which is why it is a field of its
  own rather than a member of `updateCampaignBudget`: moving a ceiling leaves the consumption
  recorded against it alone, and an operator has to be able to tell the two acts apart.
  """
  resetCampaignBudget(campaignId: ID!, input: ResetCampaignBudgetInput): ResetCampaignBudgetPayload!

  """
  Retires a campaign's ceiling recoverably, so the spend behind it stays attributable.
  """
  softDeleteCampaignBudget(id: ID!): SoftDeleteCampaignBudgetPayload!

  """Restores a soft-deleted campaign ceiling."""
  recoverCampaignBudget(id: ID!): RecoverCampaignBudgetPayload!

  """
  Retires one per-value consumption row recoverably, without giving its value back.
  """
  softDeleteCampaignBudgetUsage(id: ID!): SoftDeleteCampaignBudgetUsagePayload!

  """Restores a soft-deleted per-value consumption row."""
  recoverCampaignBudgetUsage(id: ID!): RecoverCampaignBudgetUsagePayload!

  """Creates one coupon."""
  createCoupon(input: CreateCouponInput!): CreateCouponPayload!

  """
  Mints a batch of codes that share one promotion, one window and one set of limits.
  
  The batch is all-or-nothing, because a mailing that quietly comes back a thousand codes short is
  worse than a request that fails and is retried: the service refuses a shortfall rather than
  reporting one, and a refusal reaches the caller as a `userError` on this payload.
  """
  createCouponBatch(input: CreateCouponBatchInput!): CreateCouponBatchPayload!

  """Changes a coupon's promotion, window or limits."""
  updateCoupon(id: ID!, input: UpdateCouponInput!): UpdateCouponPayload!

  """Deletes a coupon."""
  deleteCoupon(id: ID!): DeleteCouponPayload!

  """
  Retires a coupon recoverably, so the redemptions it granted stay explainable.
  """
  softDeleteCoupon(id: ID!): SoftDeleteCouponPayload!

  """Restores a soft-deleted coupon."""
  recoverCoupon(id: ID!): RecoverCouponPayload!

  """
  Issues a gift card, crediting its face value as the ledger's first row.
  """
  issueGiftCard(input: IssueGiftCardInput!): IssueGiftCardPayload!

  """Spends part of a card's balance against an order."""
  redeemGiftCard(id: ID!, input: RedeemGiftCardInput!): RedeemGiftCardPayload!

  """
  Returns value to a card, on a refund that was paid back onto it.
  
  The amount returned is the smaller of what was asked for and what the card paid out and has not
  already been given back, so a card can never be refunded more than it was spent from. The payload
  carries the figure actually applied rather than the one requested, because the two differ whenever
  the request exceeds what is returnable.
  """
  refundGiftCard(id: ID!, input: RefundGiftCardInput!): RefundGiftCardPayload!

  """
  Corrects a card's balance by hand, in either direction.
  
  A correction is this field and never an edit of a ledger row, which is what keeps the chain of
  balances a card is explained by replayable. The note is required: the movement written here is the
  only place the correction is ever explained, and an adjustment without a reason is
  indistinguishable from a defect.
  """
  adjustGiftCard(id: ID!, input: AdjustGiftCardInput!): AdjustGiftCardPayload!

  """Withdraws a card from circulation, keeping its ledger."""
  voidGiftCard(id: ID!, input: VoidGiftCardInput): VoidGiftCardPayload!

  """
  Changes what a card says about itself: its code, its placement, its status and its window.
  
  The ledger is not editable through this field. `initialAmount` is the base the balance is derived
  from, `balance` is the materialised cache of that derivation whose correction is `adjustGiftCard`,
  and `pin` is the second factor — none of the three is on the input, because none of the three is
  an act the card's own rules offer.
  """
  updateGiftCard(id: ID!, input: UpdateGiftCardInput!): UpdateGiftCardPayload!

  """Retires a card recoverably; its ledger and its balance are kept."""
  softDeleteGiftCard(id: ID!): SoftDeleteGiftCardPayload!

  """Restores a soft-deleted gift card."""
  recoverGiftCard(id: ID!): RecoverGiftCardPayload!

  """
  Retires one gift-card movement recoverably, so a correction can be undone.
  """
  softDeleteGiftCardTransaction(id: ID!): SoftDeleteGiftCardTransactionPayload!

  """Restores a soft-deleted gift-card movement."""
  recoverGiftCardTransaction(id: ID!): RecoverGiftCardTransactionPayload!
  createShippingProfile(input: CreateShippingProfileInput!): ShippingProfile!
  updateShippingProfile(id: ID!, input: CreateShippingProfileInput!): ShippingProfile!
  deleteShippingProfile(id: ID!): Boolean!

  """
  Retire a shipping profile recoverably, so the variants that ship under it keep their group.
  """
  softDeleteShippingProfile(id: ID!): ShippingProfile!

  """Restore a soft-deleted shipping profile."""
  recoverShippingProfile(id: ID!): ShippingProfile!
  assignShippingProfileVariant(input: ShippingOptionAssignmentInput!): [ShippingProfileVariant!]!

  """
  Retire a variant attachment recoverably, so the variant keeps the group it was placed in.
  """
  softDeleteShippingProfileVariant(id: ID!): ShippingProfileVariant!

  """Restore a soft-deleted variant attachment."""
  recoverShippingProfileVariant(id: ID!): ShippingProfileVariant!
  createShippingOption(input: CreateShippingOptionInput!): ShippingOption!
  updateShippingOption(id: ID!, input: CreateShippingOptionInput!): ShippingOption!
  deleteShippingOption(id: ID!): Boolean!

  """
  Retire a shipping option recoverably, so the carts that selected it keep their record.
  """
  softDeleteShippingOption(id: ID!): ShippingOption!

  """Restore a soft-deleted shipping option."""
  recoverShippingOption(id: ID!): ShippingOption!
  createFulfillment(input: CreateFulfillmentInput!): Fulfillment!
  updateFulfillment(id: ID!, input: UpdateFulfillmentInput!): Fulfillment!

  """
  Correct one shipment line: the quantity, the location, the order line it covers, or its payload.
  """
  updateFulfillmentLine(id: ID!, input: UpdateFulfillmentLineInput!): FulfillmentLine!
  shipFulfillment(id: ID!, input: ShipFulfillmentInput): Fulfillment!
  markFulfillmentInTransit(id: ID!): Fulfillment!
  deliverFulfillment(id: ID!): Fulfillment!
  cancelFulfillment(id: ID!, reason: String): Fulfillment!

  """
  Request a carrier label for a shipment, or re-fetch the one the carrier already issued.
  """
  requestFulfillmentLabel(id: ID!, input: RequestFulfillmentLabelInput!): Fulfillment!

  """
  Retire a shipment recoverably, so the order it satisfies keeps its shipping record.
  """
  softDeleteFulfillment(id: ID!): Fulfillment!

  """Restore a soft-deleted shipment."""
  recoverFulfillment(id: ID!): Fulfillment!

  """
  Remove a shipment outright. The recoverable withdrawal is softDeleteFulfillment.
  """
  deleteFulfillment(id: ID!): Boolean!

  """
  Retire a shipment line recoverably, so what the shipment covered stays readable.
  """
  softDeleteFulfillmentLine(id: ID!): FulfillmentLine!

  """Restore a soft-deleted shipment line."""
  recoverFulfillmentLine(id: ID!): FulfillmentLine!

  """
  Remove a shipment line outright. The recoverable withdrawal is softDeleteFulfillmentLine.
  """
  deleteFulfillmentLine(id: ID!): Boolean!

  """Requests a return against an order."""
  requestOrderReturn(input: RequestOrderReturnInput!): RequestOrderReturnPayload!

  """
  Edits a requested return: its receiving location, its reason, its note, and the line set that
  replaces the old one.
  
  The route this mirrors is `PUT /order-returns/:id`, and without it a caller could raise a
  return over GraphQL and then not correct it on the protocol that raised it: a line requested in
  the wrong quantity could only be withdrawn and re-requested, which allocates a second return
  number for one correction and leaves the first in the caller's history. The line set is replaced
  wholesale rather than added to, because the service refuses a return whose goods have started
  arriving — the quantities the movements were written against are what a partial rewrite would
  put underneath them.
  """
  updateOrderReturn(id: ID!, input: UpdateOrderReturnInput!): RequestOrderReturnPayload!

  """
  Approves a requested return. The version the caller read rides as its own argument rather than
  in an input, because deciding a status takes no other input and one GraphQL request may carry
  several mutations, so a version cannot be stated once for all of them.
  """
  approveOrderReturn(id: ID!, note: String, version: Int): RequestOrderReturnPayload!

  """Rejects a requested return, under the version the caller read."""
  rejectOrderReturn(id: ID!, reason: String, version: Int): RequestOrderReturnPayload!

  """
  Receives returned goods, writing the stock movements and issuing the refund.
  """
  receiveOrderReturn(id: ID!, input: ReceiveOrderReturnInput!): ReceiveOrderReturnPayload!

  """
  Refunds a received return, moving its running refund total on.
  
  It is a capability of its own and not a second door to the receipt: the receipt issues a refund
  when `returns.refundTrigger` is `ON_RECEIVE` or `ON_APPROVAL`, and is skipped when it is
  `MANUAL` — which is the setting under which this field is the only way a return's money goes
  back. It also reaches a return the receipt cannot: the service accepts `RECEIVED`,
  `PARTIALLY_RECEIVED` and `CLOSED`, while the receipt is refused on anything that is not
  `APPROVED` or `PARTIALLY_RECEIVED`, so a refund decided after the last parcel arrived had no
  door at all.
  
  The grant is the route's own, `RETURNS_RECEIVE`, and not the `REFUNDS_CREATE` that
  `06-api-specification.md` §7.14 states for the route: the two disagree in the repository, and a
  field mirrors what its route *does*. Stating the documented grant here would let a caller holding
  it move money over GraphQL while the same caller is refused the same act over REST — the one
  direction §3.1 forbids. The divergence is recorded rather than resolved, because settling it
  changes the route as well as the field.
  
  No retry key is declared, because the route declares none. §7.14 writes "Yes (Idempotency-Key,
  required)" for it and the controller's own `@Idempotent` decorators sit on the request and the
  receipt only; inventing a scope here would dedupe a GraphQL retry that the REST route lets
  through, which is a difference in behaviour rather than a difference in transport.
  """
  refundOrderReturn(id: ID!, input: RefundOrderReturnInput!): RefundOrderReturnPayload!

  """
  Creates the return leg: the shipment that brings the goods back.
  
  It is not the fulfillment domain's create. `createFulfillment` raises an outbound shipment
  whose lines are what a picking list is built from and refuses one without lines; a return leg
  carries none, because goods coming back are not fetched from a bin — they arrive, and the
  quantity that arrived is recorded against the return's own lines. The leg is raised through the
  return, which is what lets the service require the return to be `APPROVED` before a label
  exists, stamp the return's identifier onto the shipment, and record the chosen option back on
  the return it belongs to. Without this field a caller could approve a return over GraphQL and
  then have to leave the protocol to send it.
  
  The grant, the missing retry key and the version member are the route's: `RETURNS_CREATE`,
  no `Idempotent-Key` despite §7.14 stating one, and the version the caller read.
  """
  shipOrderReturn(id: ID!, input: ShipOrderReturnInput!): ShipOrderReturnPayload!

  """Cancels a return, under the version the caller read."""
  cancelOrderReturn(id: ID!, reason: String, version: Int): RequestOrderReturnPayload!

  """Closes a fully received return, under the version the caller read."""
  closeOrderReturn(id: ID!, version: Int): RequestOrderReturnPayload!

  """
  Retires a return recoverably, keeping the receipt, the stock movements and the refund it wrote.
  """
  softDeleteOrderReturn(id: ID!): RequestOrderReturnPayload!

  """Restores a soft-deleted return."""
  recoverOrderReturn(id: ID!): RequestOrderReturnPayload!

  """
  Removes a return destructively.
  
  Two facts meet here and a reader should meet both rather than one. The withdrawal this domain
  wants is `softDeleteOrderReturn`: the receipt wrote stock movements and the refund wrote money,
  and both point back at the row, so retiring a return recoverably keeps them explainable while
  taking it out of every live read. This field mirrors the *destructive* route the controller
  inherits from `CrudController` — `06-api-specification.md` §2 declares `DELETE /:id` in the
  inherited route set for every entity resource §7 lists, returns' row does not say otherwise, and
  the marketplace row names six `delete*` fields for its own — and it is delivered so the two
  protocols offer the same acts rather than the safe one twice. What it costs is stated rather
  than hidden: `order_return_line` cascades from the return, so the rows it removes include the
  lines the receipt recorded against, and the refunds raised for it keep their identifier but lose
  the return they were attributed to.
  """
  deleteOrderReturn(id: ID!): DeleteOrderReturnPayload!

  """
  Retires a return line recoverably, so the quantities the return was received against survive.
  """
  softDeleteOrderReturnLine(id: ID!): OrderReturnLine!

  """Restores a soft-deleted return line."""
  recoverOrderReturnLine(id: ID!): OrderReturnLine!

  """Creates a governed return reason."""
  createOrderReturnReason(input: OrderReturnReasonInput!): OrderReturnReasonPayload!

  """Updates a governed return reason."""
  updateOrderReturnReason(id: ID!, input: OrderReturnReasonInput!): OrderReturnReasonPayload!

  """Deactivates a governed return reason."""
  deleteOrderReturnReason(id: ID!): DeleteOrderReturnReasonPayload!

  """
  Removes a return reason that was never used, which is a different act from deactivating it.
  
  `deleteOrderReturnReason` deactivates: the row stays, and the returns already filed under it
  keep explaining themselves in a report. This field is the physical removal, and it is what
  `DELETE /order-return-reasons/:id` serves — §7.14 declares that route's answer as
  `DeleteResult`, which is a deletion's shape rather than a row's, while the controller answers
  the same path by deactivating and keeps this one at `/:id/hard`. The reason the two must not be
  confused is the one the service does not enforce: `order_return.reasonId` is `SET NULL`, so
  removing a reason that *has* explained a return nulls the reason on every return filed under it.
  """
  hardDeleteOrderReturnReason(id: ID!): DeleteOrderReturnReasonPayload!

  """
  Retires a governed return reason recoverably, so the returns filed under it stay explainable.
  """
  softDeleteOrderReturnReason(id: ID!): OrderReturnReasonPayload!

  """Restores a soft-deleted return reason."""
  recoverOrderReturnReason(id: ID!): OrderReturnReasonPayload!

  """Raises a claim against an order."""
  requestOrderClaim(input: RequestOrderClaimInput!): RequestOrderClaimPayload!

  """
  Edits an open claim: what explains it, and the line set that replaces the old one.
  
  The service refuses the edit once the claim is decided, because changing a claim after a refund
  was issued would leave the refund explaining a claim that no longer says what it said. Without
  this field a claim raised over GraphQL in error could only be withdrawn, which loses the
  complaint the corrected one would have recorded.
  """
  updateOrderClaim(id: ID!, input: UpdateOrderClaimInput!): RequestOrderClaimPayload!

  """Approves and settles a claim."""
  approveOrderClaim(id: ID!, refundAmount: Decimal, note: String): RequestOrderClaimPayload!

  """Rejects a claim."""
  rejectOrderClaim(id: ID!, reason: String): RequestOrderClaimPayload!

  """
  Cancels a claim.
  
  Cancelling and rejecting are separate acts on the same row and the plugin grants them
  separately: a rejection is a decision — the complaint was examined and refused — while a
  cancellation is the caller abandoning its own, which is why the route states `CLAIMS_CREATE`
  where the rejection states `CLAIMS_RESOLVE`. Without this field a claim could reach `CANCELED`
  over REST and only `REJECTED` over GraphQL, so a customer withdrawing a claim would be recorded
  as having had it refused.
  """
  cancelOrderClaim(id: ID!, reason: String): RequestOrderClaimPayload!

  """
  Retires a claim recoverably, keeping the refund it settled and the lines it raised.
  """
  softDeleteOrderClaim(id: ID!): RequestOrderClaimPayload!

  """Restores a soft-deleted claim."""
  recoverOrderClaim(id: ID!): RequestOrderClaimPayload!

  """
  Removes a claim destructively.
  
  `softDeleteOrderClaim` is the withdrawal this domain wants — a claim that settled in money is
  the record of that money — and this field mirrors the destructive route `CrudController`
  inherits (`06-api-specification.md` §2), delivered so both protocols offer the same acts. It
  takes the claim's lines with it, which is what `order_claim_line` cascading from `order_claim`
  means.
  """
  deleteOrderClaim(id: ID!): DeleteOrderClaimPayload!

  """
  Retires a claim line recoverably, keeping the complaint the claim records.
  """
  softDeleteOrderClaimLine(id: ID!): OrderClaimLine!

  """Restores a soft-deleted claim line."""
  recoverOrderClaimLine(id: ID!): OrderClaimLine!

  """Requests an exchange against an order."""
  requestOrderExchange(input: RequestOrderExchangeInput!): RequestOrderExchangePayload!

  """
  Edits an open exchange: its backorder allowance, a note, and the line set that replaces the old
  one.
  
  A replacement line's price is snapshotted when the set is written, so an exchange edited after
  approval would re-price lines the customer was already charged the difference on — which is why
  the service refuses the edit then, and why the capability exists at all: before approval the
  difference is not settled and the set is still the caller's to state.
  """
  updateOrderExchange(id: ID!, input: UpdateOrderExchangeInput!): RequestOrderExchangePayload!

  """Approves an exchange and prices the difference."""
  approveOrderExchange(id: ID!, settleDifference: Boolean, note: String): RequestOrderExchangePayload!

  """Rejects an exchange."""
  rejectOrderExchange(id: ID!, reason: String): RequestOrderExchangePayload!

  """
  Cancels an exchange.
  
  A cancellation releases the reservations the exchange holds for its replacement units, which a
  rejection does not: rejecting decides the exchange was not warranted, while cancelling abandons
  one that was approved and reserved stock that nobody is going to ship. The route states
  `EXCHANGES_CREATE` for that reason, and a GraphQL caller without this field could reserve stock
  and then have no way to give it back.
  """
  cancelOrderExchange(id: ID!, reason: String): RequestOrderExchangePayload!

  """
  Retires an exchange recoverably, keeping the difference it priced and the customer was charged.
  """
  softDeleteOrderExchange(id: ID!): RequestOrderExchangePayload!

  """Restores a soft-deleted exchange."""
  recoverOrderExchange(id: ID!): RequestOrderExchangePayload!

  """
  Removes an exchange destructively.
  
  `softDeleteOrderExchange` is the withdrawal this domain wants — the difference was priced once
  and charged, and the row is the explanation of that charge — and this field mirrors the
  destructive route `CrudController` inherits (`06-api-specification.md` §2), delivered so both
  protocols offer the same acts. It takes the exchange's outbound lines with it.
  """
  deleteOrderExchange(id: ID!): DeleteOrderExchangePayload!

  """
  Retires an exchange line recoverably, so the priced difference stays explainable.
  """
  softDeleteOrderExchangeLine(id: ID!): OrderExchangeLine!

  """Restores a soft-deleted exchange line."""
  recoverOrderExchangeLine(id: ID!): OrderExchangeLine!

  """Creates a plan."""
  createSubscriptionPlan(input: CreateSubscriptionPlanInput!): SubscriptionPlanPayload!

  """Updates a plan."""
  updateSubscriptionPlan(id: ID!, input: UpdateSubscriptionPlanInput!): SubscriptionPlanPayload!

  """Deactivates a plan, refusing while it still has live subscriptions."""
  deleteSubscriptionPlan(id: ID!): DeleteSubscriptionPlanPayload!

  """
  Retires a plan recoverably: the subscriptions already on it keep billing.
  """
  softDeleteSubscriptionPlan(id: ID!): SubscriptionPlanPayload!

  """Puts a retired plan back on offer."""
  recoverSubscriptionPlan(id: ID!): SubscriptionPlanPayload!

  """Puts a customer on a plan."""
  createSubscription(input: CreateSubscriptionInput!): CustomerSubscriptionPayload!

  """Moves a subscription's payer, quantity or metadata."""
  updateSubscription(id: ID!, input: UpdateSubscriptionInput!): CustomerSubscriptionPayload!

  """Starts billing a pending subscription."""
  activateSubscription(id: ID!, version: Int, idempotencyKey: String): CustomerSubscriptionPayload!

  """Suspends billing."""
  pauseSubscription(id: ID!, input: PauseSubscriptionInput): CustomerSubscriptionPayload!

  """Resumes a paused subscription."""
  resumeSubscription(id: ID!, version: Int, idempotencyKey: String): CustomerSubscriptionPayload!

  """Ends a subscription."""
  cancelSubscription(id: ID!, input: CancelSubscriptionInput): CustomerSubscriptionPayload!

  """Ends a subscription because it ran out."""
  expireSubscription(id: ID!, input: ExpireSubscriptionInput): CustomerSubscriptionPayload!

  """
  Moves a subscription to another plan, settling the remainder of the current period.
  """
  changeSubscriptionPlan(id: ID!, input: ChangeSubscriptionPlanInput!): SubscriptionPlanChangePayload!

  """Adds a recurring line mid-cycle, settling the remainder of the period."""
  addSubscriptionItem(id: ID!, input: SubscriptionItemInput!, version: Int, idempotencyKey: String): SubscriptionPlanChangePayload!

  """
  Changes a recurring line's quantity mid-cycle, settling the remainder of the period.
  """
  changeSubscriptionItemQuantity(id: ID!, variantId: ID!, quantity: Decimal!, version: Int, idempotencyKey: String): SubscriptionPlanChangePayload!

  """
  Removes a recurring line mid-cycle, settling the remainder of the period.
  """
  removeSubscriptionItem(id: ID!, variantId: ID!): SubscriptionPlanChangePayload!

  """
  Deletes a subscription outright.
  
  The row leaves the database rather than being retired, which is why every read that has to keep
  answering for what was sold reaches the recoverable pair below instead. It is mirrored because
  `DELETE /subscriptions/:id` serves it and §3.1 requires one mutation per REST write route including
  the routes the CRUD base contributes; a caller that reaches it over REST must be able to reach it
  over GraphQL, and the answer says which of the two removals happened.
  """
  deleteSubscription(id: ID!): DeleteSubscriptionPayload!

  """
  Retires a subscription recoverably.
  
  The rows stay as the record of what was sold, which is why this is the retirement a caller
  reaches for rather than a hard delete, and why it is recoverable by the field below.
  """
  softDeleteSubscription(id: ID!): CustomerSubscriptionPayload!

  """Restores a soft-deleted subscription."""
  recoverSubscription(id: ID!): CustomerSubscriptionPayload!

  """Adds a recurring line without settling a proration."""
  createSubscriptionItem(input: CreateSubscriptionItemInput!): SubscriptionItemPayload!

  """Corrects a recurring line's quantity or price."""
  updateSubscriptionItem(id: ID!, input: UpdateSubscriptionItemInput!): SubscriptionItemPayload!

  """Removes a recurring line without settling a proration."""
  deleteSubscriptionItem(id: ID!): DeleteSubscriptionItemPayload!

  """
  Retires a recurring line recoverably, so that the next cycle stops billing it.
  """
  softDeleteSubscriptionItem(id: ID!): SubscriptionItemPayload!

  """Restores a soft-deleted recurring line."""
  recoverSubscriptionItem(id: ID!): SubscriptionItemPayload!

  """Opens a billing cycle by hand, for a backfill."""
  createSubscriptionBilling(input: CreateSubscriptionBillingInput!): SubscriptionBillingPayload!

  """Corrects a billing cycle that has not been charged."""
  updateSubscriptionBilling(id: ID!, input: UpdateSubscriptionBillingInput!): SubscriptionBillingPayload!

  """Bills one cycle of one subscription."""
  billSubscription(id: ID!, input: BillSubscriptionInput): SubscriptionBillingOutcomePayload!

  """Runs a billing pass over every due subscription."""
  runSubscriptionBilling(input: RunSubscriptionBillingInput): SubscriptionBillingRunPayload!

  """Records that a cycle's money arrived."""
  paySubscriptionBilling(id: ID!, input: PaySubscriptionBillingInput): SubscriptionBillingPayload!

  """Records that a cycle was deliberately not charged."""
  waiveSubscriptionBilling(id: ID!, input: WaiveSubscriptionBillingInput!): SubscriptionBillingPayload!

  """Records that a paid cycle was refunded."""
  refundSubscriptionBilling(id: ID!): SubscriptionBillingPayload!

  """
  Retires a billing cycle recoverably, taking it out of the billing history.
  """
  softDeleteSubscriptionBilling(id: ID!): SubscriptionBillingPayload!

  """Restores a soft-deleted billing cycle."""
  recoverSubscriptionBilling(id: ID!): SubscriptionBillingPayload!

  """Raises a purchase order against an existing supplier."""
  createPurchaseOrder(input: CreatePurchaseOrderInput!): PurchaseOrderPayload!

  """
  Amends a draft purchase order, replacing its line set when one is supplied.
  """
  updatePurchaseOrder(id: ID!, input: UpdatePurchaseOrderInput!): PurchaseOrderPayload!

  """Deletes a draft purchase order."""
  deletePurchaseOrder(id: ID!): DeletePurchaseOrderPayload!

  """Sends an approved purchase order to the supplier."""
  sendPurchaseOrder(id: ID!, email: String, note: String): PurchaseOrderPayload!

  """
  Records the supplier's acknowledgement of a sent order, revising its expected date when one is stated.
  """
  acknowledgePurchaseOrder(id: ID!, expectedAt: DateTime, note: String): PurchaseOrderPayload!

  """
  Approves a purchase order internally, which is what permits it to be sent.
  """
  approvePurchaseOrder(id: ID!, note: String): PurchaseOrderPayload!

  """Closes a purchase order short of the ordered quantity."""
  closePurchaseOrder(id: ID!, reason: String): PurchaseOrderPayload!

  """Cancels a purchase order before anything arrived."""
  cancelPurchaseOrder(id: ID!, reason: String): PurchaseOrderPayload!

  """
  Receives goods against a purchase order, writing the stock movements they produce. The order is
  the one the caller is already looking at, so the field carries it as its own argument and the
  delivery is the input. The input's `idempotencyKey` is required, as it is on
  `createGoodsReceipt`: both book a delivery, and one booked twice counts the stock twice.
  """
  receivePurchaseOrder(id: ID!, input: ReceivePurchaseOrderInput!): GoodsReceiptPayload!

  """
  Withdraws a purchase order without removing the row, so the recovery below can read it back. The
  answer is the withdrawn document, as it is on every other soft removal of the platform.
  """
  softDeletePurchaseOrder(id: ID!): PurchaseOrder!

  """Puts a withdrawn purchase order back."""
  recoverPurchaseOrder(id: ID!): PurchaseOrder!

  """
  Withdraws one line of a purchase order without removing the row. A line is read through its order
  rather than by id, so this pair is the only root field the line answers — and it answers them because
  the line's own controller serves both routes under the order-edit grant.
  """
  softDeletePurchaseOrderLine(id: ID!): PurchaseOrderLine!

  """Puts a withdrawn purchase order line back."""
  recoverPurchaseOrderLine(id: ID!): PurchaseOrderLine!

  """
  Receives goods against a purchase order, writing the stock movements they produce.
  """
  createGoodsReceipt(input: CreateGoodsReceiptInput!): GoodsReceiptPayload!

  """Records one further line against a receipt that was already posted."""
  recordGoodsReceiptLine(receiptId: ID!, input: RecordGoodsReceiptLineInput!): GoodsReceiptPayload!

  """
  Ends a receipt: its quantities are taken back out of stock and off the order's lines.
  """
  closeGoodsReceipt(id: ID!, reason: String): GoodsReceiptPayload!

  """
  Deletes a goods receipt outright.
  
  The row leaves the database rather than being withdrawn, so the movements it wrote stay in the
  ledger with nothing left to explain them — which is why the withdrawal below and the reversal above
  are the removals a caller reaches for. It is mirrored because `DELETE /goods-receipts/:id` serves
  it and §3.1 requires one mutation per REST write route; the answer carries the identity, because a
  removed row is not there to answer with.
  """
  deleteGoodsReceipt(id: ID!): DeleteGoodsReceiptPayload!

  """
  Withdraws a goods receipt without removing the row, so the recovery below can read it back. The
  receipt controller serves `DELETE /goods-receipts/:id/soft` and overrides it only to state a
  permission the inherited declaration leaves unstated — `GOODS_RECEIPTS_CREATE`, the grant
  recording a delivery carries — and this field states the same one. It answers the payload the
  resource's own mutations answer rather than the receipt itself, so a client generated from the
  composed schema sees one shape per resource.
  """
  softDeleteGoodsReceipt(id: ID!): GoodsReceiptPayload!

  """Puts a withdrawn goods receipt back, under the grant its route states."""
  recoverGoodsReceipt(id: ID!): GoodsReceiptPayload!

  """
  Withdraws one line of a goods receipt without removing the row. A receipt line is read through its
  receipt, so this pair is the only root field the line answers — and it answers them because the
  line's own controller serves both routes under the receiving grant. It answers the line, which is
  what those routes answer and what no payload of this document carries.
  """
  softDeleteGoodsReceiptLine(id: ID!): GoodsReceiptLine!

  """Puts a withdrawn goods receipt line back."""
  recoverGoodsReceiptLine(id: ID!): GoodsReceiptLine!

  """
  Deletes a goods receipt line outright.
  
  The row leaves the database rather than being withdrawn, so the movement the line was written from
  stays in the ledger with nothing left to explain it — the withdrawal below is the removal a caller
  reaches for. It is mirrored because `DELETE /goods-receipt-lines/:id` serves it and §3.1 requires
  one mutation per REST write route; the answer carries the identity, because a removed row is not
  there to answer with.
  """
  deleteGoodsReceiptLine(id: ID!): DeleteGoodsReceiptLinePayload!

  """
  Writes a term, which is the standing agreement a purchase line is priced from.
  """
  createVendorProductTerm(input: VendorProductTermInput!): VendorProductTermPayload!

  """Amends a term."""
  updateVendorProductTerm(id: ID!, input: VendorProductTermInput!): VendorProductTermPayload!

  """
  Writes several terms in one call, which is how a product-wide agreement is recorded.
  """
  bulkVendorProductTerms(input: BulkVendorProductTermInput!): VendorProductTermPayload!

  """Retires a term. One a placed order used is kept and moved to INACTIVE."""
  deleteVendorProductTerm(id: ID!): DeleteVendorProductTermPayload!

  """
  Withdraws a term without removing the row, so the recovery below can read it back — the recoverable
  half of the lifecycle, where the field above retires a term the organization no longer wants. The
  term controller serves `DELETE /vendor-product-terms/:id/soft` and overrides it only to state
  `VENDOR_TERMS_EDIT`, the grant every write route of this resource states, and this field states
  the same one. It answers the payload the resource's write mutations answer.
  """
  softDeleteVendorProductTerm(id: ID!): VendorProductTermPayload!

  """Puts a withdrawn term back, under the grant its route states."""
  recoverVendorProductTerm(id: ID!): VendorProductTermPayload!

  """Grants a right. Idempotent on the order line or subscription it names."""
  grantEntitlement(input: GrantEntitlementInput!): GrantEntitlementPayload!

  """
  Withdraws a right, terminally. Its keys and its live activations go with it. The caller states
  the 'version' it read, and a right that has moved on since is refused.
  """
  revokeEntitlement(id: ID!, reason: String!, version: Int, idempotencyKey: String): EntitlementPayload!

  """
  Extends the term of a right, which is what a successful renewal does. The caller states the
  'version' it read, and a right that has moved on since is refused.
  """
  extendEntitlement(id: ID!, endsAt: DateTime!, quantity: Int, version: Int, idempotencyKey: String): EntitlementPayload!

  """
  Edits a right: its ceiling, its term, its grace, its activation limit, its extras and its
  conditions. The caller states the 'version' it read, and a right that has moved on since is
  refused — an edit is the one act here that overwrites fields another operator may be looking at.
  """
  updateEntitlement(id: ID!, input: UpdateEntitlementInput!, version: Int): EntitlementPayload!

  """
  Suspends a right temporarily: a failed renewal, a dispute, an operator pause. Its activations
  are retained and refused at use, because a suspended right is expected to come back. The caller
  states the 'version' it read, and a right that has moved on since is refused.
  """
  suspendEntitlement(id: ID!, reason: String, version: Int, idempotencyKey: String): EntitlementPayload!

  """
  Returns a suspended right to force, clearing the reason the suspension recorded. A right whose
  term ran out while it was suspended is expired rather than resumed, and the payload carries the
  right as it ends up. The caller states the 'version' it read.
  """
  resumeEntitlement(id: ID!, version: Int, idempotencyKey: String): EntitlementPayload!

  """
  Lowers the ceiling a right carries, which is what a partial refund does. The surplus
  activations are revoked newest-first and a right reduced to zero is revoked outright, so this is
  not an edit of the quantity column: it is the act `10-orders-payments-and-returns-spec.md` gives
  the refund path, and it is deliberately not answered by `extendEntitlement`, which moves the term
  forward and returns a suspended right to force. The caller states the 'version' it read.
  """
  reduceEntitlement(id: ID!, quantity: Int!, reason: String, version: Int): EntitlementPayload!

  """
  Retires a right recoverably: the row is kept, with its keys and its activation history, unlike
  `revokeEntitlement`, which is terminal.
  """
  softDeleteEntitlement(id: ID!): EntitlementPayload!

  """Restores a right that was retired recoverably."""
  recoverEntitlement(id: ID!): EntitlementPayload!

  """Occupies a slot of a right for a device or a named seat."""
  activateEntitlement(input: ActivateEntitlementInput!): ActivateEntitlementPayload!

  """Gives a slot back: released by the holder, or revoked by support."""
  deactivateEntitlement(id: ID!, reason: String, revoked: Boolean): DeactivateEntitlementPayload!

  """
  Corrects the recorded fields of a slot, without giving it back and without taking it away. The
  input is the route's own body type member for member. A slot's state, its right, its key, its device
  and its revocation reason are not in it and are refused by the service: a revoked slot set back to
  `ACTIVE` by an edit would sit past the activation limit, because the limit is only counted when a slot
  is taken.
  """
  updateEntitlementActivation(id: ID!, input: UpdateEntitlementActivationInput!): EntitlementActivationPayload!

  """
  Retires an activation recoverably, leaving the row and the slot's history in place.
  """
  softDeleteEntitlementActivation(id: ID!): EntitlementActivationPayload!

  """Restores an activation that was retired recoverably."""
  recoverEntitlementActivation(id: ID!): EntitlementActivationPayload!

  """
  Issues a licence key. Its plaintext is returned once, in this response.
  """
  issueEntitlementKey(input: IssueEntitlementKeyInput!): IssueEntitlementKeyPayload!

  """
  Records who holds an issued credential. A key that already names a different holder is refused
  rather than reassigned, and the documented answer to a genuine reassignment is a new key.
  """
  assignEntitlementKey(id: ID!, input: AssignEntitlementKeyInput!): EntitlementKeyPayload!

  """Withdraws a credential, releasing the activations it was used for."""
  revokeEntitlementKey(id: ID!, reason: String!): EntitlementKeyPayload!

  """
  Replaces a credential with a freshly generated one: the recovery path for a lost key, and the
  only one. The credential it replaces is revoked in the same transaction, the activations it was
  used for are released, and the pair are linked so "what happened to the key this customer was
  sent" has one answer.
  """
  reissueEntitlementKey(id: ID!, input: ReissueEntitlementKeyInput!): ReissueEntitlementKeyPayload!

  """Retires a licence key recoverably, leaving the row in place."""
  softDeleteEntitlementKey(id: ID!): EntitlementKeyPayload!

  """Restores a licence key that was retired recoverably."""
  recoverEntitlementKey(id: ID!): EntitlementKeyPayload!

  """
  Opens a seller account on the seller's behalf. The applicant is born `DRAFT` and `UNVERIFIED`.
  
  Mirrors `POST /sellers` and declares the same `seller.create` scope, so an applicant that re-sends
  an application it never saw acknowledged is answered from its first attempt over either protocol
  rather than with the collision its own code would cause.
  """
  createSeller(input: CreateSellerInput!, idempotencyKey: String): Seller!

  """
  Amends a seller's profile, commission defaults, payout terms and tax identifiers.
  """
  updateSeller(id: ID!, input: UpdateSellerInput!): Seller!

  """
  Submits a seller application for review. The seller becomes `SUBMITTED` and no further.
  """
  submitSeller(id: ID!): Seller!

  """
  Records one verification kind's result.
  
  Mirrors `POST /sellers/:id/verify` and declares the same `seller.verify` scope, so a verifier that
  re-sends a verdict it never saw acknowledged is answered from its first attempt rather than stamping
  a fresh verification date over the one already recorded.
  """
  verifySeller(id: ID!, input: VerifySellerInput!, idempotencyKey: String): Seller!

  """Activates an approved seller. `ACTIVE` is never reached implicitly."""
  activateSeller(id: ID!): Seller!

  """
  Suspends a seller, with a reason the seller can read. Balances are held.
  """
  suspendSeller(id: ID!, reason: String!): Seller!

  """Returns a suspended seller to active."""
  reinstateSeller(id: ID!): Seller!

  """Refuses an application, with a reason the applicant can read."""
  rejectSeller(id: ID!, reason: String!): Seller!

  """
  Starts winding a seller down. The seller becomes `OFFBOARDING` and no further.
  
  Mirrors `POST /sellers/:id/offboard` and declares the same `seller.offboard` scope, so a retry of
  one transition is a retry whichever protocol it arrives on.
  """
  offboardSeller(id: ID!, idempotencyKey: String): Seller!

  """
  Removes a seller account, answering the count the deletion reports.
  
  Mirrors `DELETE /sellers/:id`, which takes `SELLERS_DELETE` — the grant a caller that may merely
  amend a seller does not hold.
  """
  deleteSeller(id: ID!): SellerDeleteResult!

  """
  Archives a seller account, keeping the row.
  
  Mirrors `DELETE /sellers/:id/soft` and answers the archived seller, as that route does.
  """
  softDeleteSeller(id: ID!): Seller!

  """
  Restores a soft-deleted seller account.
  
  Mirrors `PUT /sellers/:id/recover`: restoring is the same destructive authority read backwards,
  so it takes `SELLERS_DELETE` and not the edit grant.
  """
  restoreSeller(id: ID!): Seller!

  """
  Offers a variant. The offering is born `DRAFT`; publishing it is a separate act.
  
  Mirrors `POST /seller-offerings`, which takes `SELLER_OFFERINGS_EDIT` — the grant a caller that may merely see the listings does not hold — and declares the same `seller_offering.create` scope, so a seller that re-sends an offer it never saw acknowledged is answered with the offering its first attempt created over either protocol rather than with the collision the same variant causes.
  """
  createSellerOffering(input: CreateSellerOfferingInput!, idempotencyKey: String): SellerOffering!

  """
  Amends an offering's price, window, commission and publication set.
  
  Mirrors `PUT /seller-offerings/:id`, which takes `SELLER_OFFERINGS_EDIT`, and hands the service the same seller scope the route hands it: a seller-scoped caller reaches its own listings and no others.
  """
  updateSellerOffering(id: ID!, input: UpdateSellerOfferingInput!): SellerOffering!

  """
  Submits an offering for moderation: the door a marketplace that does not let its sellers publish opens.
  
  `marketplace.sellerSelfPublish` decides whether publishing is a seller's act or a moderator's, and where it is the moderator's this is the only way an offering reaches `PENDING_REVIEW` — so without this field a seller could author and amend an offering here and never advance it, while a REST caller could. Mirrors `POST /seller-offerings/:id/submit`, which takes `SELLER_OFFERINGS_EDIT`. It is not `submitSeller`: that field serves the seller resource's own submit route, and the two are different capabilities on different rows.
  """
  submitSellerOffering(id: ID!): SellerOffering!

  """
  Publishes an offering to the given channels, materialising its authored price into a price row.
  
  Mirrors the publish route and declares the same `seller_offering.publish` scope, so a retry of one
  publication is a retry whichever protocol it arrives on.
  """
  publishSellerOffering(id: ID!, channelIds: [String!], idempotencyKey: String): SellerOffering!

  """Pauses an offering, so it stops being sellable and stays resumable."""
  pauseSellerOffering(id: ID!): SellerOffering!

  """Withdraws an offering; terminal."""
  withdrawSellerOffering(id: ID!): SellerOffering!

  """
  Publishes, pauses, withdraws or re-prices a page of listings in one request.
  
  Mirrors `POST /seller-offerings/bulk` and declares the same `seller_offering.bulk` scope, so a retry of one batch is a retry whichever protocol it arrives on. The batch is applied by the platform's executor from the route's own declaration, which is what makes the two surfaces one operation: the same cap, the same single authorisation decision, the same per-item report, and an atomic batch that writes nothing when one item fails.
  """
  bulkSellerOfferings(input: BulkSellerOfferingsInput!): BulkSellerOfferingsPayload!

  """
  Archives an offering, keeping the row.
  
  Mirrors `DELETE /seller-offerings/:id/soft`, which takes `SELLERS_DELETE`: an offering is a child row
  of the seller, and the catalogue declares the destructive grant on the seller rather than one per child.
  """
  softDeleteSellerOffering(id: ID!): SellerOffering!

  """
  Restores a soft-deleted offering.
  
  Mirrors `PUT /seller-offerings/:id/recover`: restoring is the same destructive authority read
  backwards, so it takes `SELLERS_DELETE` and not the edit grant.
  """
  recoverSellerOffering(id: ID!): SellerOffering!

  """
  Advances a ledger row to settleable. The state is advanced, never the amount.
  
  Mirrors the settle route and declares the same `seller.transaction.settle` scope, so a retry of one
  advance is a retry whichever protocol it arrives on.
  """
  settleSellerTransaction(id: ID!, note: String, idempotencyKey: String): SellerTransaction!

  """Holds a ledger row out of payouts, with a reason a seller can read."""
  holdSellerTransaction(id: ID!, reason: String!): SellerTransaction!

  """
  Removes a ledger row, answering the count the deletion reports.
  
  Mirrors `DELETE /seller-transactions/:id`, which takes `SELLERS_DELETE` rather than the settle grant beside it, and removes where the archive keeps: the ledger is kept because it is the truth about what a seller earned, so a caller that removes one is destroying that record rather than retiring it.
  """
  deleteSellerTransaction(id: ID!): SellerDeleteResult!

  """
  Archives a ledger row, keeping it.
  
  Mirrors `DELETE /seller-transactions/:id/soft`, which takes `SELLERS_DELETE`: a ledger row is a child
  row of the seller, and the ledger is kept because it is the truth about what a seller earned.
  """
  softDeleteSellerTransaction(id: ID!): SellerTransaction!

  """
  Restores a soft-deleted ledger row.
  
  Mirrors `PUT /seller-transactions/:id/recover`: restoring is the same destructive authority read
  backwards, so it takes `SELLERS_DELETE` and not the settle grant beside it.
  """
  recoverSellerTransaction(id: ID!): SellerTransaction!

  """
  Creates a payout from named ledger rows, or from the settleable rows of a period.
  
  Mirrors the create route and declares the same `seller.payout.create` scope, so a client that
  presents one key over either protocol receives the payout its first attempt built rather than a
  second one over the same ledger rows.
  """
  createSellerPayout(sellerId: ID!, currency: String!, transactionIds: [String!], note: String, idempotencyKey: String): SellerPayout!

  """
  Amends what a payout states about itself: its note, its provider references, its period and schedule and its metadata. Its status and its figures are not in the input and are refused by the service: they move through the approve, pay, cancel and retry fields.
  
  Mirrors `PUT /seller-payouts/:id`, which takes `SELLER_PAYOUTS_CREATE` and not the approve grant beside it — preparing a payout is creating one, while approving it is what moves money. The route threads no seller scope into this write, so neither does the field: a divergence in either direction would be one protocol serving a caller the other refused.
  """
  updateSellerPayout(id: ID!, input: UpdateSellerPayoutInput!): SellerPayout!

  """
  Runs the payout pass for the sellers whose schedule is due.
  
  Mirrors `POST /seller-payouts/run` and declares the same `seller.payout.run` scope, so a scheduler that re-sends a pass it never received an answer for is answered from the first attempt's result over either protocol rather than repeating the pass.
  """
  runSellerPayout(input: RunSellerPayoutInput, idempotencyKey: String): [SellerPayoutRunResult!]!

  """
  Re-drives a failed payout.
  
  Mirrors `POST /seller-payouts/:id/retry`, which takes `SELLER_PAYOUTS_APPROVE` — re-driving a failed transfer is the authority approving one is — and declares the same `seller.payout.retry` scope, so a retry of one re-drive is a retry whichever protocol it arrives on.
  """
  retrySellerPayout(id: ID!, idempotencyKey: String): SellerPayout!

  """
  Removes a payout, answering the count the deletion reports.
  
  Mirrors `DELETE /seller-payouts/:id`, which takes `SELLERS_DELETE` and neither payout grant beside it: a caller that may approve or cancel a payout is not thereby a caller that may remove one from the table.
  """
  deleteSellerPayout(id: ID!): SellerDeleteResult!

  """
  Approves a payout. Approving is a separate permission from creating, because approving one moves money.
  """
  approveSellerPayout(id: ID!): SellerPayout!

  """
  Records the provider's execution of a payout, as reported.
  
  Mirrors the execution route, and carries that route's scope and its requirement with it. This is
  the operation that moves money to the seller, so a mutation sent without a key is refused with
  `IDEMPOTENCY_KEY_REQUIRED` rather than executed, exactly as the route refuses it. A repeat of one
  key and one body receives the first attempt's payout instead of instructing the provider again.
  """
  markSellerPayoutPaid(id: ID!, providerKey: String, providerTransferId: String, idempotencyKey: String): SellerPayout!

  """Cancels an unpaid payout, returning its ledger rows to settleable."""
  cancelSellerPayout(id: ID!, reason: String!): SellerPayout!

  """
  Archives a payout, keeping the row.
  
  Mirrors `DELETE /seller-payouts/:id/soft`, which takes `SELLERS_DELETE` and neither of the payout
  grants beside it: a caller that may approve or cancel a payout is not thereby a caller that may retire
  one from every read that resolves it.
  """
  softDeleteSellerPayout(id: ID!): SellerPayout!

  """
  Restores a soft-deleted payout.
  
  Mirrors `PUT /seller-payouts/:id/recover`: restoring is the same destructive authority read backwards,
  and it puts the payout back in front of every read that decides what a seller is still owed.
  """
  recoverSellerPayout(id: ID!): SellerPayout!

  """
  Removes a payout line, answering the count the deletion reports.
  
  Mirrors `DELETE /seller-payout-lines/:id`, which takes `SELLERS_DELETE`: a line is the join row of one payout and one ledger row, and every row under the seller belongs to the seller.
  """
  deleteSellerPayoutLine(id: ID!): SellerDeleteResult!

  """
  Archives a payout line, keeping the row.
  
  Mirrors `DELETE /seller-payout-lines/:id/soft`, which takes `SELLERS_DELETE`: a line is the join row
  of one payout and one ledger row, and every row under the seller belongs to the seller.
  """
  softDeleteSellerPayoutLine(id: ID!): SellerPayoutLine!

  """
  Restores a soft-deleted payout line.
  
  Mirrors `PUT /seller-payout-lines/:id/recover`: restoring the join row is what makes its ledger row
  payable at most once again, so it takes `SELLERS_DELETE` and not the payout grants.
  """
  recoverSellerPayoutLine(id: ID!): SellerPayoutLine!

  """
  Records a settlement reported by a provider, as reported: the ledger is never edited to agree with it.
  
  Mirrors the recording route and declares the same `seller.settlement.record` scope, so a
  signature-verified callback delivered twice records one settlement over either protocol.
  """
  createSellerSettlement(sellerId: ID!, providerKey: String!, currency: String!, grossAmount: String!, commissionAmount: String, feeAmount: String, idempotencyKey: String): SellerSettlement!

  """
  Amends what a settlement states about itself: its period, its report and external references, the holder it pays, its note and its metadata. Its status and its figures are not in the input and are refused by the service: they move through the reconcile, close and dispute fields.
  
  Mirrors `PUT /seller-settlements/:id`, which takes `SELLER_SETTLEMENTS_EDIT`. The route threads no seller scope into this write, so neither does the field.
  """
  updateSellerSettlement(id: ID!, input: UpdateSellerSettlementInput!): SellerSettlement!

  """
  Reconciles a settlement against the platform's lines for its period.
  
  Mirrors `POST /seller-settlements/:id/reconcile` and declares the same `seller.settlement.reconcile` scope, so a client that re-sends a reconciliation it never saw the answer to is answered from its first attempt rather than stamping a second reconciled date over the first. A discrepancy is recorded and reported, never repaired: the settlement is answered `DISPUTED`, with the lines the comparison was made over.
  """
  reconcileSellerSettlement(id: ID!, input: ReconcileSellerSettlementInput, idempotencyKey: String): SellerSettlementReconciliation!

  """
  Closes a settlement, which accepts no further lines.
  
  Mirrors `POST /seller-settlements/:id/close`, which takes `SELLER_SETTLEMENTS_EDIT`.
  """
  closeSellerSettlement(id: ID!, note: String): SellerSettlement!

  """
  Marks a settlement disputed, which requires a reason.
  
  Mirrors `POST /seller-settlements/:id/dispute`, which takes `SELLER_SETTLEMENTS_EDIT`. The reason is a required argument because the service refuses a dispute without one, and a document that let it be omitted would let a client reach a `BAD_REQUEST` the schema could have made unreachable.
  """
  disputeSellerSettlement(id: ID!, reason: String!): SellerSettlement!

  """
  Removes a settlement, answering the count the deletion reports.
  
  Mirrors `DELETE /seller-settlements/:id`, which takes `SELLERS_DELETE` rather than the settlement edit grant the recording, reconciling and closing fields carry: the ledger is never edited to agree with a report, so removing one is the destructive authority rather than another way to correct it.
  """
  deleteSellerSettlement(id: ID!): SellerDeleteResult!

  """
  Archives a settlement, keeping the row.
  
  Mirrors `DELETE /seller-settlements/:id/soft`, which takes `SELLERS_DELETE` rather than the settlement
  edit grant the recording and closing routes state: the ledger is never edited to agree with a report, so
  retiring one is the destructive authority rather than another way to correct it.
  """
  softDeleteSellerSettlement(id: ID!): SellerSettlement!

  """
  Restores a soft-deleted settlement.
  
  Mirrors `PUT /seller-settlements/:id/recover`: restoring is the same destructive authority read
  backwards, and a restored report is one the discrepancy of a period is read against again.
  """
  recoverSellerSettlement(id: ID!): SellerSettlement!

  """Re-weights or deactivates an index definition."""
  updateSearchIndexDefinition(id: ID!, input: SearchIndexDefinitionUpdateInput!): SearchIndexDefinitionPayload!

  """
  Removes a definition an operator authored; a shipped definition is refused.
  """
  deleteSearchIndexDefinition(id: ID!): DeleteSearchIndexDefinitionPayload!

  """Rebuilds one entity's index."""
  reindexEntity(entity: String!, input: SearchReindexInput): SearchReindexPayload!

  """Rebuilds every index, or every index of one channel."""
  reindexAll(input: SearchReindexInput): SearchReindexPayload!

  """Drops the index of an entity, or of one channel."""
  dropSearchIndex(entity: String, channelId: ID): DropSearchIndexPayload!
}

"""
One accounting template: the document one type of record is rendered through, in one language, for one
organization.
"""
type AccountingTemplate {
  id: ID!

  """The template's name. The delivered save writes the template type here."""
  name: String

  """The language this copy is written in."""
  languageCode: String

  """
  Which record this template renders: `invoice`, `estimate` or `receipt`. The value set is the
  platform's own accounting template vocabulary, shared with the delivered body validation.
  """
  templateType: String

  """The MJML source of the document — what an editor reads and writes."""
  mjml: String

  """
  The HTML the platform compiled from the source — what a renderer reads.
  """
  hbs: String

  """
  The organization this copy belongs to, or null on a copy that belongs to no organization at all.
  An organization-less copy with an organization-less tenant is one of the templates the platform
  ships, and the delivered lookup falls back to it for every tenant.
  """
  organizationId: ID

  """
  The tenant this copy belongs to, or null on a copy the platform itself ships.
  """
  tenantId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are the CRUD base's
  own routes and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of accounting templates."""
type AccountingTemplateEdge {
  node: AccountingTemplate!
  cursor: String!
}

"""
One page of accounting templates, with the total the filters select and the boundary cursors.
"""
type AccountingTemplateConnection {
  nodes: [AccountingTemplate!]!
  edges: [AccountingTemplateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an accounting template list may be sorted by."""
enum AccountingTemplateSortField {
  createdAt
  updatedAt
  name
  languageCode
  templateType
}

"""
The order the accounting template list is returned in. At most three keys are accepted.
"""
input AccountingTemplateSort {
  field: AccountingTemplateSortField!
  direction: SortDirection!
}

"""
The fields an accounting template list may be narrowed by. `and` is the default; `or` requires any of
its members and `not` excludes the rows that match it.

`organizationId: { isNull: true }` is the copy that belongs to no organization, and
`tenantId: { isNull: true }` is the copy the platform itself ships — the pair the delivered lookup falls
back to for every tenant.

`mjml` and `hbs` are not filterable. They are document columns, and a condition on one would scan a
whole template's source for a pattern: a question no delivered read asks, answered against markup rather
than against a template.
"""
input AccountingTemplateFilter {
  id: IDFilter
  name: StringFilter
  languageCode: StringFilter
  templateType: StringFilter
  organizationId: IDFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [AccountingTemplateFilter!]
  or: [AccountingTemplateFilter!]
  not: AccountingTemplateFilter
}

"""
What a caller states when it files a template.

The tenant is not a member: the tenant a row is written into comes from the credential on every write on
this platform, and a member naming one would promise a scope the write does not honour. `organizationId`
is a member and is nullable, because its absence is a statement rather than an omission — a copy that
names no organization is a candidate the delivered lookup falls back to.
"""
input CreateAccountingTemplateInput {
  """The template's name, as the delivered list read answers it."""
  name: String

  """The language this copy is written in."""
  languageCode: String

  """
  Which record this template renders: `invoice`, `estimate` or `receipt`.
  """
  templateType: String

  """The MJML source of the document."""
  mjml: String

  """
  The compiled HTML, when the caller states one rather than a source to compile.
  """
  hbs: String

  """
  The organization this copy belongs to. Omit for a copy that belongs to none.
  """
  organizationId: ID
}

"""
What a caller states when it changes a template that exists.

The members are the delivered body's own, and the delivered edit does not patch: it hands the store the
stated columns beside the path identifier, so **a member the caller leaves out is written as the
column's default rather than left as it is**. Reading the row first and then stating every member is
what a caller that means "change one thing" has to do.
"""
input UpdateAccountingTemplateInput {
  id: ID!
  name: String
  languageCode: String
  templateType: String
  mjml: String
  organizationId: ID
}

"""
What a caller states when it saves the document of one template.

The three addressing members are required because the delivered body requires them: the save resolves
the copy to write by its type, its language and its organization, and a request that named none of them
would have no row to resolve. The tenant is not a member, for the same reason it is on no other write
here.
"""
input SaveAccountingTemplateInput {
  """
  Which record this template renders: `invoice`, `estimate` or `receipt`.
  """
  templateType: String!

  """The language this copy is written in."""
  languageCode: String!

  """The organization this copy belongs to."""
  organizationId: ID!

  """The MJML source to store. The compiled body is derived from it."""
  mjml: String!
}

"""
A document converted for preview, as the delivered preview route answers it.

The conversion is answered rather than kept: there is no row behind this type, which is why it carries
the rendered document and nothing else.
"""
type AccountingTemplatePreview {
  """The document with its markup compiled and its sample values rendered."""
  html: String!
}

"""One recorded action against one platform record."""
type ActivityLog {
  id: ID!

  """
  The kind of record the action was performed on, as the kernel's own entity vocabulary names it.
  """
  entity: String!

  """
  The row the action was performed on, by identifier. The record behind it is read from the domain that owns it.
  """
  entityId: ID!

  """
  The action that was performed, as the kernel's own action vocabulary names it.
  """
  action: String!

  """Who performed the action: a person or the system itself."""
  actorType: String

  """
  The sentence the writer composed for this row, describing what happened.
  """
  description: String

  """
  The names of the fields an update changed. Empty on a creation or a removal.
  """
  updatedFields: JSON

  """
  The values those fields held before the update, one document per changed row.
  """
  previousValues: JSON

  """
  The values those fields hold after the update, one document per changed row.
  """
  updatedValues: JSON

  """The records as they stood before a bulk update."""
  previousEntities: JSON

  """The records as they stand after a bulk update."""
  updatedEntities: JSON

  """The record the action produced, as it was written."""
  data: JSON

  """
  The person the action is attributed to, by identifier. The employee row is not joined by this read.
  """
  employeeId: ID

  """The tenant the action was taken in, by identifier."""
  tenantId: ID

  """The organization the action was taken in, by identifier."""
  organizationId: ID

  """
  Whether the row is active. A flag the writer sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the row is archived. A flag the writer sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the row was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the row was withdrawn, or null while it is live. The delivered list read selects on this
  column rather than offering an argument for it, so a client that must see withdrawn rows states the
  `deletedAt` filter below.
  """
  deletedAt: DateTime

  """When the action was recorded."""
  createdAt: DateTime

  """
  When the row was last changed. A log row is written once, so this is normally the same instant.
  """
  updatedAt: DateTime
}

"""The boundary of one page of log rows."""
type ActivityLogEdge {
  node: ActivityLog!
  cursor: String!
}

"""
One page of log rows, with the total the filters select and the boundary cursors.
"""
type ActivityLogConnection {
  nodes: [ActivityLog!]!
  edges: [ActivityLogEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a log list may be sorted by.

Exactly the four the delivered read accepts an order for — `createdAt`, `updatedAt`, `entity` and
`action` are that reader's own allow-list, and a value outside it is answered with the field's own
default rather than refused. Stating the same four here keeps the two surfaces' order vocabularies one
vocabulary: a REST caller that sorts by `actorType` is silently given `createdAt`, and a schema that
offered `actorType` would make GraphQL promise an order the route does not apply.
"""
enum ActivityLogSortField {
  createdAt
  updatedAt
  entity
  action
}

"""
The order the log rows are returned in. At most three keys are accepted.
"""
input ActivityLogSort {
  field: ActivityLogSortField!
  direction: SortDirection!
}

"""
The fields a log list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the log row, because the connection narrows the rows the delivered read
returned rather than restating the criterion it was handed. `entity` and `entityId` are the pair that
makes this a per-record history — one record's timeline is this connection narrowed by both — and
`employeeId` is the per-actor view of the same rows.

The five document members carry the kernel's `JSON` operators: a `contains` is a substring match over
the serialised document rather than a query into it, which is what the query protocol's own `JSON`
condition means on every resource that declares one.
"""
input ActivityLogFilter {
  id: IDFilter
  entity: StringFilter
  entityId: IDFilter
  action: StringFilter
  actorType: StringFilter
  description: StringFilter
  updatedFields: JSONFilter
  previousValues: JSONFilter
  updatedValues: JSONFilter
  previousEntities: JSONFilter
  updatedEntities: JSONFilter
  data: JSONFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [ActivityLogFilter!]
  or: [ActivityLogFilter!]
  not: ActivityLogFilter
}

type AddressRole {
  id: ID!
  organizationId: ID
  addressId: ID!
  role: AddressRoleEnum!
  isDefault: Boolean!
}

enum AddressRoleEnum {
  SHIPPING
  BILLING
  REGISTERED
  PAYOUT
  RETURN
  REMIT_TO
}

input CreateAddressRoleInput {
  organizationId: ID!
  addressId: ID!
  role: AddressRoleEnum!
  isDefault: Boolean
}

"""
One row of the address book: a reusable postal address with the roles it plays and the two defaults
it may carry.
"""
type Address {
  id: ID!
  tenantId: ID
  organizationId: ID

  """Customer-facing nickname ("Home", "Warehouse 2")."""
  label: String

  """The person to address at this location."""
  contactName: String

  """Company name as it should appear on a label or an invoice."""
  company: String
  firstName: String
  lastName: String
  phone: String
  email: String

  """
  Street address. Always present: an address that names no street names nothing.
  """
  line1: String!

  """Second line of the street address — a unit, a floor, a building."""
  line2: String

  """City or locality. Always present."""
  city: String

  """Free-text province name as entered."""
  province: String

  """
  Normalised province or state code — the member a rate table is keyed by.
  """
  provinceCode: String

  """Postal or ZIP code, as entered."""
  postalCode: String

  """
  ISO 3166-1 alpha-2 country code, upper case. The member tax and shipping rules match on.
  """
  countryCode: String!

  """
  The country lookup row, non-null exactly when `countryCode` matched one.
  """
  countryId: ID
  latitude: Float
  longitude: Float

  """
  Whether this is the party's default shipping address.
  
  A derived mirror, not the authority: the party's own column is authoritative and the two are
  written in one transaction by the set-default and clear-default operations, so a write that
  disagrees is refused rather than reconciled.
  """
  isDefaultShipping: Boolean!

  """The billing counterpart of the above, on the same terms."""
  isDefaultBilling: Boolean!

  """Whether an address-validation strategy has confirmed this address."""
  isValidated: Boolean!

  """Which validator produced the verdict, when one did."""
  validationProviderKey: String

  """What kind of thing the address belongs to."""
  ownerType: AddressOwnerType!

  """
  The id of the row `ownerType` names. No foreign key: the target table depends on the type.
  """
  ownerId: ID!

  """
  The buyer this address is scoped to, when it is a party's. Null on an anonymous cart address.
  """
  customerId: ID

  """
  Tenant extras, including the raw provider response once a validator has answered.
  """
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""What kind of thing a row of the address book belongs to."""
enum AddressOwnerType {
  """
  The address belongs to an organization contact; `customerId` equals `ownerId`.
  """
  CONTACT

  """
  The address belongs to a stock location — the ship-from a carrier label prints.
  """
  WAREHOUSE

  """The address belongs to a marketplace seller."""
  SELLER

  """
  The address belongs to a supplier — what a purchase order prints as its remit-to.
  """
  VENDOR

  """The address belongs to the organization itself, not to any party."""
  ORGANIZATION
}

"""The boundary of one page of addresses."""
type AddressEdge {
  node: Address!
  cursor: String!
}

"""
One page of addresses, with the total the filters select and the boundary cursors.
"""
type AddressConnection {
  nodes: [Address!]!
  edges: [AddressEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an address may be sorted by."""
enum AddressSortField {
  createdAt
  updatedAt
  label
  city
  countryCode
  postalCode
  isDefaultShipping
  isDefaultBilling
}

"""
The order the addresses are returned in. At most three keys are accepted.
"""
input AddressSort {
  field: AddressSortField!
  direction: SortDirection!
}

"""Comparison on the kind of thing an address belongs to."""
input AddressOwnerTypeFilter {
  eq: AddressOwnerType
  ne: AddressOwnerType
  in: [AddressOwnerType!]
  nin: [AddressOwnerType!]
  isNull: Boolean
}

"""
The fields an address list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.
"""
input AddressFilter {
  id: IDFilter
  label: StringFilter
  contactName: StringFilter
  company: StringFilter
  phone: StringFilter
  email: StringFilter
  line1: StringFilter
  line2: StringFilter
  city: StringFilter
  province: StringFilter
  provinceCode: StringFilter
  postalCode: StringFilter
  countryCode: StringFilter
  countryId: IDFilter
  isDefaultShipping: BooleanFilter
  isDefaultBilling: BooleanFilter
  isValidated: BooleanFilter
  ownerType: AddressOwnerTypeFilter
  ownerId: IDFilter
  customerId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [AddressFilter!]
  or: [AddressFilter!]
  not: AddressFilter
}

"""
What a caller states when it records an address in the book.

The validation verdict is absent by construction — it is the address-validation strategy's own
statement — and a body that states one is refused rather than ignored. The two default members are
accepted and are routed to the set-default operation, so a create never writes a default flag itself.
"""
input CreateAddressInput {
  organizationId: ID!
  label: String
  contactName: String
  company: String
  firstName: String
  lastName: String
  phone: String
  email: String
  line1: String!
  line2: String
  city: String!
  province: String
  provinceCode: String
  postalCode: String
  countryCode: String!
  countryId: ID
  latitude: Float
  longitude: Float
  isDefaultShipping: Boolean
  isDefaultBilling: Boolean
  ownerType: AddressOwnerType
  ownerId: ID!
  customerId: ID
  metadata: JSON
}

"""
What a caller may change on an address that exists.

Every member is optional, and the owner pair is among them: an address legitimately moves from one
warehouse to another, and the service re-runs the owner-consistency check and the default rule on the
new owner's book when it does.
"""
input UpdateAddressInput {
  id: ID!
  organizationId: ID!
  label: String
  contactName: String
  company: String
  firstName: String
  lastName: String
  phone: String
  email: String
  line1: String
  line2: String
  city: String
  province: String
  provinceCode: String
  postalCode: String
  countryCode: String
  countryId: ID
  latitude: Float
  longitude: Float
  isDefaultShipping: Boolean
  isDefaultBilling: Boolean
  ownerType: AddressOwnerType
  ownerId: ID
  customerId: ID
  metadata: JSON
}

"""
The role a default operation names, and which way the fact moves.

One input for both directions because the fact is one: the address either becomes the party's default
for the role or stops being it, and a client that had to choose between two root fields would be
choosing a spelling rather than a capability. `isDefault` defaults to true, so the common call states
only the role.
"""
input SetDefaultAddressInput {
  id: ID!
  role: AddressRoleEnum!
  isDefault: Boolean
}

"""One request the platform answered, as the middleware recorded it."""
type ApiCallLog {
  id: ID!

  """The identifier that ties every row one operation produced together."""
  correlationId: ID!

  """The path that was called."""
  url: String!

  """The HTTP method the request used."""
  method: String!

  """The status the platform answered with."""
  statusCode: Int!

  """When the request arrived."""
  requestTime: DateTime!

  """When the response was completed."""
  responseTime: DateTime!

  """The address the request came from."""
  ipAddress: String

  """The protocol the request used."""
  protocol: String

  """
  The client's own identification of itself. A browser, a desktop application or a script.
  """
  userAgent: String

  """Where the request was initiated from, when the client states it."""
  origin: String

  """
  The account the request was made as, by identifier. The account row is not joined by this read.
  """
  userId: ID

  """The headers the request carried."""
  requestHeaders: JSON!

  """The body the request carried."""
  requestBody: JSON!

  """The body the platform answered with."""
  responseBody: JSON!

  """The tenant the request was made in, by identifier."""
  tenantId: ID

  """The organization the request was made in, by identifier."""
  organizationId: ID

  """
  Whether the row is active. A flag the writer sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the row is archived. A flag the writer sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the row was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the row was withdrawn, or null while it is live. Withdrawing is one of the two removals this
  resource serves, and this column is the whole of what it writes — so without it a caller could not
  tell a live row from a withdrawn one, nor read back a removal that was asked for as a withdrawal.
  """
  deletedAt: DateTime

  """When the request was recorded."""
  createdAt: DateTime

  """
  When the row was last changed. A log row is written once, so this is normally the same instant.
  """
  updatedAt: DateTime
}

"""The boundary of one page of log rows."""
type ApiCallLogEdge {
  node: ApiCallLog!
  cursor: String!
}

"""
One page of log rows, with the total the filters select and the boundary cursors.
"""
type ApiCallLogConnection {
  nodes: [ApiCallLog!]!
  edges: [ApiCallLogEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a call log list may be sorted by.

The two instants the row records, the two columns an operator triages by — the status and the path —
the method, and the row's own timestamps. The identifier is not here: a page of one caller's calls is
`userId` stated as a filter, which is both narrower and cheaper than an order.
"""
enum ApiCallLogSortField {
  requestTime
  responseTime
  statusCode
  url
  method
  createdAt
  updatedAt
}

"""
The order the log rows are returned in. At most three keys are accepted.
"""
input ApiCallLogSort {
  field: ApiCallLogSortField!
  direction: SortDirection!
}

"""
The fields a call log list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, because the connection narrows the rows the delivered read
returned. The delivered read narrows by `correlationId`, `statusCode`, `ipAddress`, `method`, `userId`
and a `requestTime` range; the remaining members are narrowing the connection performs over the rows
that read answered, which is the same set the route answers.

The three document members carry the kernel's `JSON` operators. A `contains` is a substring match over
the serialised document rather than a query into it, which is what the query protocol's own `JSON`
condition means on every resource that declares one — and the document is answered in full on the same
row, so filtering by it discloses nothing the row does not already carry.
"""
input ApiCallLogFilter {
  id: IDFilter
  correlationId: IDFilter
  url: StringFilter
  method: StringFilter
  statusCode: NumberFilter
  requestTime: DateTimeFilter
  responseTime: DateTimeFilter
  ipAddress: StringFilter
  protocol: StringFilter
  userAgent: StringFilter
  origin: StringFilter
  userId: IDFilter
  requestHeaders: JSONFilter
  requestBody: JSONFilter
  responseBody: JSONFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [ApiCallLogFilter!]
  or: [ApiCallLogFilter!]
  not: ApiCallLogFilter
}

"""One employee's invitation to one appointment."""
type AppointmentEmployee {
  id: ID!

  """
  The appointment the employee is invited to, as the plain column this table stores. It is the field the
  delivered `GET /appointment/:appointmentId` narrows by, and therefore the filter that answers an
  appointment's invitees.
  """
  appointmentId: ID!

  """
  The employee invited. Not joined by the list read — the member is the identifier that always travels,
  and the row behind it is read from the employees resource.
  """
  employeeId: ID

  """
  The relation id of `employeeAppointment`: the appointment row this invitation belongs to, as opposed
  to the plain `appointmentId` column above. Either one names the appointment; this is the one the
  entity's many-to-one is declared with.
  """
  employeeAppointmentId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the invitation was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two lifecycle removals this resource serves — and this column is the whole of what they
  write, so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of invitations."""
type AppointmentEmployeeEdge {
  node: AppointmentEmployee!
  cursor: String!
}

"""
One page of invitations, with the total the filters select and the boundary cursors.
"""
type AppointmentEmployeeConnection {
  nodes: [AppointmentEmployee!]!
  edges: [AppointmentEmployeeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an invitation list may be sorted by."""
enum AppointmentEmployeeSortField {
  createdAt
  updatedAt
}

"""
The order the invitations are returned in. At most three keys are accepted.
"""
input AppointmentEmployeeSort {
  field: AppointmentEmployeeSortField!
  direction: SortDirection!
}

"""
The fields an invitation list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

**The two identifier filters are how the delivered sub-routes are stated here.** `appointmentId` answers
what `GET /appointment/:appointmentId` answers — `findAll({ where: { appointmentId } })` — and
`employeeId` answers what `GET /employee-appointments/:employeeId` answers, which is the same read with
the same table and one more field named. Both are narrowings of this connection and therefore filters,
never root fields of their own.

Every member is a column of the row the read answers, because the connection narrows the rows the service
returned: a member the read does not select would be a filter that silently selects nothing. `tenantId`
is absent because the tenant a read runs under comes from the credential and never from a filter, and the
four bookkeeping members — `isActive`, `isArchived`, `archivedAt` and `deletedAt` — are absent because the
delivered read already answers live rows through the soft-delete column.
"""
input AppointmentEmployeeFilter {
  id: IDFilter
  appointmentId: IDFilter
  employeeId: IDFilter
  employeeAppointmentId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [AppointmentEmployeeFilter!]
  or: [AppointmentEmployeeFilter!]
  not: AppointmentEmployeeFilter
}

"""
What a caller states when it invites an employee to an appointment.

Every member is a column of the row, because the delivered `POST /` is inherited from the CRUD base and
hands the service the entity as the store's own partial: there is no body type of its own to mirror, so
this input mirrors the row. The tenant is deliberately not a member — the service stamps the caller's own
tenant from the credential and overwrites whatever a body states — and so are the base entity's lifecycle
flags, its soft-delete marker and its audit-user columns: the two lifecycle routes are what move the
marker, and an input that could set an audit column would be a way to write a fact the platform's own
bookkeeping owns.

A caller that may not select an employee has the employee stamped from the credential by the same service:
the invitation is then the caller's own, which is the service's scope decision rather than a member here.
"""
input CreateAppointmentEmployeeInput {
  """The appointment the employee is invited to."""
  appointmentId: ID!

  """The employee invited."""
  employeeId: ID!

  """The organization the invitation is filed under."""
  organizationId: ID
}

"""
What a caller states when it changes an invitation.

**A member the caller leaves out is left as it is.** The delivered edit is a partial column update against
the criterion the path names — the inherited `PUT /:id` hands the store the body it received — so the input
is the create input with the criterion stated and every other member optional. Moving an invitation is
therefore stating either identifier and nothing else; the tenant is absent for the reason it is absent
above.
"""
input UpdateAppointmentEmployeeInput {
  id: ID!
  appointmentId: ID
  employeeId: ID
  organizationId: ID
}

"""
One approval type an organization offers, as an administrator named it.
"""
type ApprovalPolicy {
  id: ID!

  """The label the policy is known by, as the administrator wrote it."""
  name: String!

  """The note shown beside the label, when one was written."""
  description: String

  """
  The code the platform matches on: the name with its whitespace folded to underscores and its letters
  upper-cased, derived by the delivered write. Carried as a value rather than a schema enum because the
  vocabulary is open — it is whatever the organization's administrators have named.
  """
  approvalType: String

  """The tenant the policy belongs to, by identifier."""
  tenantId: ID

  """
  The organization whose request forms the policy belongs to, by identifier.
  """
  organizationId: ID

  """
  Whether the policy is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the policy is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the policy was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the policy was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime

  """When the policy was filed."""
  createdAt: DateTime

  """When the policy was last changed."""
  updatedAt: DateTime
}

"""The boundary of one page of policies."""
type ApprovalPolicyEdge {
  node: ApprovalPolicy!
  cursor: String!
}

"""
One page of policies, with the total the filters select and the boundary cursors.
"""
type ApprovalPolicyConnection {
  nodes: [ApprovalPolicy!]!
  edges: [ApprovalPolicyEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a policy list may be sorted by."""
enum ApprovalPolicySortField {
  id
  name
  approvalType
  createdAt
  updatedAt
  deletedAt
}

"""
The order the policies are returned in. At most three keys are accepted.
"""
input ApprovalPolicySort {
  field: ApprovalPolicySortField!
  direction: SortDirection!
}

"""
The fields a policy list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the policy row, because the delivered list read answers the rows the
command's own `where` selected and joins only the relations a REST caller names — which this surface
does not name. `approvalType` is the member the request-approval reader's condition is expressed
with: the codes an organization's request forms must not offer are excluded by naming them, which is
the `nin` operator rather than a second read. The tenant comes from the credential on every read, so
the two identifier members narrow the row's own columns rather than restating a scope decision the
caller does not have.
"""
input ApprovalPolicyFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  approvalType: StringFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [ApprovalPolicyFilter!]
  or: [ApprovalPolicyFilter!]
  not: ApprovalPolicyFilter
}

"""
What a caller states when it files a policy.

The organization is stated because the delivered create does not derive one — the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.

**`approvalType` is deliberately not a member, and its absence is a statement about the write rather
than an omission.** The delivered service derives the code from the name — the name with its whitespace
folded to underscores, upper-cased — so a body stating a code would be accepted by a schema and then
overwritten by the service. A caller that wants a particular code names the policy accordingly.
"""
input CreateApprovalPolicyInput {
  """The label the policy is known by. The code is derived from it."""
  name: String!

  """The note shown beside the label."""
  description: String

  """The organization whose request forms the policy belongs to."""
  organizationId: ID!
}

"""
What a caller states when it edits a policy that exists.

The delivered update DTO is not a partial one — it declares the same members as the create — so the
label is required here as well, and the code is derived from it again: renaming a policy renames its
code. Everything the create does not accept is absent, for the reason the create states.
"""
input UpdateApprovalPolicyInput {
  id: ID!

  """The label the policy is known by. The code is derived from it."""
  name: String!

  """The note shown beside the label."""
  description: String

  """The organization whose request forms the policy belongs to."""
  organizationId: ID!
}

"""
One registered OAuth client: a third party allowed to obtain authorization codes and exchange them for
tokens, together with what it may ask for.
"""
type OAuthClient {
  id: ID!

  """
  The public identifier the third party presents. Generated by the server, never stated by a caller.
  """
  clientId: String!

  """The name an administrator reads in the registry."""
  name: String!

  """A short description shown beside the name."""
  description: String

  """
  Which kind of client this is: `confidential` (holds a secret, authenticates server-to-server) or
  `public` (cannot keep one, authenticates with PKCE). Only confidential clients may be registered at
  present, so every row this surface answers currently carries `confidential`.
  """
  clientType: String!

  """
  The redirect URIs the authorization code flow may return to. Exact matches only.
  """
  redirectUris: JSON!

  """
  The scopes the client may request. A client that requests one outside this list is refused.
  """
  allowedScopes: JSON!

  """
  The grant types the client may use: `authorization_code`, `refresh_token` or
  `client_credentials`. Only `authorization_code` is implemented end to end; the others are accepted
  by the registry and refused at the token exchange until they ship.
  """
  allowedGrantTypes: JSON!

  """Whether the client states that PKCE is required of it."""
  pkceRequired: Boolean!

  """The lifetime of an access token this client is issued, in seconds."""
  accessTokenTtl: Int!

  """The lifetime of a refresh token this client is issued, in seconds."""
  refreshTokenTtl: Int!

  """
  Whether the client is still resolvable. A withdrawn client answers `false`.
  """
  isActive: Boolean

  """
  The tenant the client belongs to, or null when it is an installation-wide client. A null tenant is
  the row the delivered lookup resolves for any tenant, which is how the legacy single-client
  configuration is represented.
  """
  tenantId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""
The client a registration or a rotation produced, beside the plaintext secret those two operations
answer and no read repeats.
"""
type OAuthClientSecret {
  """
  The client the secret belongs to, with every member the registry read answers.
  """
  client: OAuthClient!

  """
  The plaintext client secret. Answered once here and never again: it is not a member of
  `OAuthClient` and no read of the registry carries it.
  """
  clientSecret: String!
}

"""The boundary of one page of registered clients."""
type OAuthClientEdge {
  node: OAuthClient!
  cursor: String!
}

"""
One page of registered clients, with the total the filters select and the boundary cursors.
"""
type OAuthClientConnection {
  nodes: [OAuthClient!]!
  edges: [OAuthClientEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a registry list may be sorted by."""
enum OAuthClientSortField {
  createdAt
  updatedAt
  name
  clientId
  clientType
  accessTokenTtl
  refreshTokenTtl
}

"""
The order the registry list is returned in. At most three keys are accepted.
"""
input OAuthClientSort {
  field: OAuthClientSortField!
  direction: SortDirection!
}

"""
The fields a registry list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`tenantId: { isNull: true }` selects the installation-wide clients — the rows the delivered lookup
resolves for every tenant. The three secret columns are not filterable, for the same reason they are not
members: a filter is a comparison against the row's own value, so a condition on a hash would answer
whether a guessed credential is the right one, one character at a time. `redirectUris`, `allowedScopes`
and `allowedGrantTypes` are not filterable either. They are documents, and the one operator the protocol
offers on a document is a substring match against its text form: a condition on a redirect URI would
answer a match on part of a longer URL, and one on a scope would answer a match on the beginning of a
longer scope name. Neither is the question a caller means to ask.
"""
input OAuthClientFilter {
  id: IDFilter
  clientId: StringFilter
  name: StringFilter
  description: StringFilter
  clientType: StringFilter
  pkceRequired: BooleanFilter
  accessTokenTtl: NumberFilter
  refreshTokenTtl: NumberFilter
  isActive: BooleanFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OAuthClientFilter!]
  or: [OAuthClientFilter!]
  not: OAuthClientFilter
}

"""
What a caller states when it registers a client.

`clientId`, the client secret and the code-signing secret are members of no input, at any point in the
resource's life. All three are generated server-side: a caller that could name its own client identifier
could collide with another registration, and a caller that could name its own secret would choose a
value the platform's own generator is there to make unguessable. The answer carries the secret; the
request never states it.

The tenant is not a member either, and that is a security statement rather than an omission. The
delivered create stamps the caller's own tenant, and it only writes an installation-wide row (a null
tenant) for a super administrator — a privilege the service checks from the credential, which is a
decision this surface cannot and must not restate as an argument.
"""
input CreateOAuthClientInput {
  """The name an administrator reads in the registry."""
  name: String!

  """A short description shown beside the name."""
  description: String

  """
  Which kind of client to register. Omit for `confidential`, which is the delivered default. `public`
  is accepted by this input because the delivered body accepts it, and the service refuses it with a
  stated reason until PKCE enforcement ships — which is the same answer the REST body gets.
  """
  clientType: String

  """
  The redirect URIs the flow may return to. At least one is required, and each is matched exactly.
  """
  redirectUris: JSON!

  """The scopes the client may request. Omit to grant none."""
  allowedScopes: JSON

  """Omit for `authorization_code`, which is the delivered default."""
  allowedGrantTypes: JSON

  """
  Whether the client states that PKCE is required of it. Omit for the delivered default, `false`.
  """
  pkceRequired: Boolean

  """
  The access token lifetime in seconds. Omit for the delivered default of one day.
  """
  accessTokenTtl: Int

  """
  The refresh token lifetime in seconds. Omit for the delivered default of thirty days.
  """
  refreshTokenTtl: Int
}

"""
What a caller states when it changes a client that exists.

The members are the delivered edit's own: `clientId`, the client secret and the code-signing secret are
absent because the route that changes a client cannot touch them, and the rotation below is the only
route that replaces a secret.

**A member that is absent is left as it is**, which is what the delivered edit does: it hands the store
the members the body carried and nothing else. Stating a member as `null` states it explicitly, which is
what clears a nullable column such as `description`.
"""
input UpdateOAuthClientInput {
  id: ID!
  name: String
  description: String
  redirectUris: JSON
  allowedScopes: JSON
  allowedGrantTypes: JSON
  pkceRequired: Boolean
  accessTokenTtl: Int
  refreshTokenTtl: Int

  """
  Whether the client is still resolvable. Setting it false is a second way to disable a client.
  """
  isActive: Boolean
}

"""One window of bookable hours."""
type AvailabilitySlot {
  id: ID!

  """The instant the window opens."""
  startTime: DateTime!

  """The instant the window closes."""
  endTime: DateTime!

  """
  Whether the window is the whole day rather than the hours the two instants bound.
  """
  allDay: Boolean!

  """
  Which kind of window this is, as `AvailabilitySlotType` states it: `Default` or `Recurring`. The value
  is the string the delivered vocabulary carries, and it is deliberately not redeclared as a schema enum
  here.
  """
  type: String!

  """
  The employee the window belongs to, or null when it belongs to the organization rather than to one
  person. Read the employee itself from its own surface.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """When the row was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of slots."""
type AvailabilitySlotEdge {
  node: AvailabilitySlot!
  cursor: String!
}

"""
One page of slots, with the total the filters select and the boundary cursors.
"""
type AvailabilitySlotConnection {
  nodes: [AvailabilitySlot!]!
  edges: [AvailabilitySlotEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a slot list may be sorted by."""
enum AvailabilitySlotSortField {
  createdAt
  updatedAt
  startTime
  endTime
  type
}

"""The order the slots are returned in. At most three keys are accepted."""
input AvailabilitySlotSort {
  field: AvailabilitySlotSortField!
  direction: SortDirection!
}

"""
The fields a slot list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

The two bounds of the window compare as instants, which is what makes "which slots overlap this fortnight"
a question this input can express; `allDay` compares as the flag it is, so narrowing to the whole-day rows
is a read of the same shape. `type` compares as text, because the value is the vocabulary's own string.
"""
input AvailabilitySlotFilter {
  id: IDFilter
  startTime: DateTimeFilter
  endTime: DateTimeFilter
  allDay: BooleanFilter
  type: StringFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [AvailabilitySlotFilter!]
  or: [AvailabilitySlotFilter!]
  not: AvailabilitySlotFilter
}

"""
What a caller states when it files a slot.

The two instants and the flag are what make the window, and `type` is what says which kind of window it
is, so all four are required — that is the delivered `IAvailabilitySlotsCreateInput` stated in the schema's
vocabulary.

`employeeId` is optional because the column is nullable: a slot may be filed for an organization rather
than for one person, and a required member here would refuse a row the delivered write accepts.

`organizationId` is optional because the delivered input type carries it as the write is
organization-scoped. The tenant is never a member: the handler stamps it from the credential for every
row, exactly as it does over REST.
"""
input CreateAvailabilitySlotInput {
  """
  Which kind of window this is, as `AvailabilitySlotType` states it: `Default` or `Recurring`.
  """
  type: String!

  """
  Whether the window is the whole day rather than the hours the two instants bound.
  """
  allDay: Boolean!

  """The instant the window opens."""
  startTime: DateTime!

  """The instant the window closes."""
  endTime: DateTime!

  """
  The employee the window belongs to. Omit for a slot that belongs to the organization.
  """
  employeeId: ID
  organizationId: ID
}

"""
What a caller states when it writes a slot under an identifier it names.

The delivered route takes the row's own interface as its body rather than a partial of the create input,
so the four members that make a window are stated here too: the write is an upsert, and the columns the
body states are the ones the row is written with. Every member but the identifier is the same member the
create above declares, with the same optionality, because the two writes reach the same service call.
"""
input UpdateAvailabilitySlotInput {
  id: ID!
  type: String!
  allDay: Boolean!
  startTime: DateTime!
  endTime: DateTime!
  employeeId: ID
  organizationId: ID
}

"""One published message, filed against the record it is about."""
type Broadcast {
  id: ID!

  """The message's title, which is what a reader sees before opening it."""
  title: String!

  """
  The message body, as the document column the platform stores rich content in.
  """
  content: JSON!

  """
  What kind of message this is, as the contracts' own value: `STATUS_REPORT`, `MILESTONE`,
  `ANNOUNCEMENT`, `ALERT`, `DECISION` or `CHANGELOG`.
  """
  category: String!

  """
  Who may read it, as the delivered reader's own value: `ENTITY_MEMBERS` — the members of the record
  below; `ORGANIZATION` — everyone in the organization; `RESTRICTED` — the audience rules below; or
  `EXTERNAL_VIEW` — a share link, which is not a credential and which no read here answers.
  """
  visibilityMode: String!

  """
  The audience constraints of a restricted message: the users, employees, teams and roles it names.
  """
  audienceRules: JSON

  """
  When the message was published. Null on a row written without one, which is a row no reading order can place.
  """
  publishedAt: DateTime

  """
  The kind of record the message is about, as the contracts' own entity-type value.
  """
  entity: String!

  """
  The record the message is about, as the identifier of that record's own row.
  """
  entityId: ID!

  """
  The employee who published it, as the identifier of the employee's own row.
  """
  employeeId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live row from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of broadcasts."""
type BroadcastEdge {
  node: Broadcast!
  cursor: String!
}

"""
One page of broadcasts, with the total the filters select and the boundary cursors.
"""
type BroadcastConnection {
  nodes: [Broadcast!]!
  edges: [BroadcastEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a broadcast list may be sorted by.

The publication instant is the reading order and the delivered read's own order beside it; the title and
the two vocabularies are how a list is taken in groups — a work list by kind, an audience report by
visibility; and the two timestamps are how a change is found again. The identifiers and the document
columns are not sort keys: an identifier order is not an order a message list is read in, and the
questions those columns answer are filters.
"""
enum BroadcastSortField {
  createdAt
  updatedAt
  publishedAt
  title
  category
  visibilityMode
}

"""
The order the broadcasts are returned in. At most three keys are accepted.
"""
input BroadcastSort {
  field: BroadcastSortField!
  direction: SortDirection!
}

"""
The fields a broadcast list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered read answers, and the read answers it after its own
visibility rule has been applied — so each condition below narrows the rows the caller may see rather
than the rows the store holds.

Three absences are deliberate and each is a statement:

- `audienceRules` is not filterable. The column is a document type on one dialect and plain text on
  another, so the value a read produces differs by installation and a condition over it would answer
  differently on two installations running the same query.
- `isArchived` and `isActive` are not filterable, although the object carries both. The delivered list
  read applies them itself — it answers live, unarchived rows — so a condition on either could only ever
  restate what the read already did, and a caller stating the other value would be answered the empty set
  rather than the rows they asked for.
- The organization is not filterable, because the read takes it from the credential rather than from the
  caller: it is the organization the caller is acting in, and a member here would suggest a scope
  decision the caller does not have.
"""
input BroadcastFilter {
  id: IDFilter
  title: StringFilter
  category: StringFilter
  visibilityMode: StringFilter
  entity: StringFilter
  entityId: IDFilter
  employeeId: IDFilter
  publishedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [BroadcastFilter!]
  or: [BroadcastFilter!]
  not: BroadcastFilter
}

"""
What a caller states when it publishes a message.

The three vocabularies are stated as their own values rather than as schema enums, and the delivered
body validation is what requires them: it validates `category` and `visibilityMode` against the
contracts' enums and requires a title and a body.

The organization is stated as an identifier and is *not* required, because the delivered validation
requires an organization only when the body names neither the organization object nor a `sentTo` — and
the object spelling is a type this schema does not declare, so the identifier is what this surface
offers and the condition stays the delivered validator's rather than being restated as a non-null
member that would refuse a body the route serves.

`audienceRules` is the body of a restricted message — the users, employees, teams and roles it names —
and the delivered write stores the document it is given.
"""
input CreateBroadcastInput {
  """The message's title."""
  title: String!

  """The message body, as the platform's document shape."""
  content: JSON!

  """What kind of message this is."""
  category: String!

  """Who may read it."""
  visibilityMode: String!

  """The audience constraints of a restricted message."""
  audienceRules: JSON

  """
  When the message was published. Omitted, the delivered write publishes it now.
  """
  publishedAt: DateTime

  """The kind of record the message is about."""
  entity: String!

  """The record the message is about, by identifier."""
  entityId: ID!

  """The organization the message is filed under."""
  organizationId: ID
}

"""
What a caller states when it changes a message that exists.

**A member the caller leaves out is left as it is.** The delivered edit reads the row it is allowed to
edit, then merges the stated members onto it through the same write the create uses, so omitting `title`
keeps the title and omitting `content` keeps the body.

**`entity` and `entityId` are not members.** The delivered edit body removes the pair, so a message
cannot be re-pointed at another record: what it is about is fixed when it is published, and everything
that reads it — the membership rule that decides who may see it among them — is read from that pair.

**The edit is the publisher's, or a permission holder's.** The delivered write refuses a caller who is
neither the row's publisher nor a holder of `CHANGE_SELECTED_EMPLOYEE`, and refuses an employee-less
caller outright rather than dropping the missing criterion from its query. That refusal is the write's
own and reaches the caller as it is; this input does not restate it as a member.

The identifier is the criterion and is not repeated in the payload, which is the shape the route itself
has.
"""
input UpdateBroadcastInput {
  id: ID!
  title: String
  content: JSON
  category: String
  visibilityMode: String
  audienceRules: JSON
  publishedAt: DateTime
  organizationId: ID
}

"""
One person's candidacy with one organization: where they are in the pipeline, what they would be paid,
and which account, contact and employee row they are tied to.
"""
type Candidate {
  id: ID!

  """
  The rating filed on this row. A rating, and therefore an exact decimal.
  """
  rating: Decimal

  """
  The instant the row is dated to, where an administration screen dates it.
  """
  valueDate: DateTime

  """When the candidacy was filed with the organization."""
  appliedDate: DateTime

  """
  When the candidate was hired. `status` is the pipeline move; this is when it happened.
  """
  hiredDate: DateTime

  """When the candidate was declined."""
  rejectDate: DateTime

  """
  The pipeline state, as the contracts package's own value: `APPLIED`, `HIRED` or `REJECTED`. Written
  by the filing, by the two pipeline moves and by the delivered subscriber when a rejection date is
  stated.
  """
  status: String

  """
  The level's label, as this row stores it. It is the candidate's own column and not the engagement's `employeeLevel`.
  """
  candidateLevel: String

  """
  Recurring weekly limit, in hours. A whole number of hours rather than an amount.
  """
  reWeeklyLimit: Int

  """
  The currency the two rates are stated in, as the contracts package's own `CurrenciesEnum` value.
  """
  billRateCurrency: String

  """
  The billing rate per hour the candidate would be engaged at. Money, and therefore an exact decimal.
  """
  minimumBillingRate: Decimal

  """
  How often the candidate would be paid, as the contracts package's own `PayPeriodEnum` value.
  """
  payPeriod: String

  """
  The billing rate per hour the candidate is filed at. Money, and therefore an exact decimal.
  """
  billRateValue: Decimal

  """The URL of the candidate's curriculum vitae, as filed."""
  cvUrl: String

  """
  The account this candidacy belongs to. The account's own columns are read from `user(id)`; this surface carries the identifier alone because no delivered read of this domain selects an account column.
  """
  userId: ID!

  """
  The contact row the candidacy keeps its address and fiscal facts in, where it has one. A contact is
  shared — an employee and a customer point at the same row — so its columns are read from the contact
  surface and this member only says which row.
  """
  contactId: ID

  """
  The organization position the candidate is being considered for, where one is recorded. The position is read from the position surface.
  """
  organizationPositionId: ID

  """
  The row describing where the candidate came from. A source is the *vocabulary* of origins — one row
  per origin, shared by every candidacy that came from it — so its name is read from `candidateSource(id)`
  and this member says which row.
  """
  sourceId: ID

  """
  The engagement this candidacy became, once it was hired. The engagement is read from `employee(id)`.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """
  When the candidacy was withdrawn, or null while it is live. `DELETE /:id/soft` and `PUT /:id/recover`
  are delivered routes and this column is the whole of what they write, so without it a caller could
  not tell a live row from a withdrawn one on the answer to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of candidates."""
type CandidateEdge {
  node: Candidate!
  cursor: String!
}

"""
One page of candidates, with the total the filters select and the boundary cursors.
"""
type CandidateConnection {
  nodes: [Candidate!]!
  edges: [CandidateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a candidate list may be sorted by. Every member is a column the row always has.
"""
enum CandidateSortField {
  createdAt
  updatedAt
  appliedDate
  hiredDate
  rejectDate
  candidateLevel
  status
  rating
  billRateValue
}

"""
The order the candidates are returned in. At most three keys are accepted.
"""
input CandidateSort {
  field: CandidateSortField!
  direction: SortDirection!
}

"""
The fields a candidate list may be narrowed by: exactly the columns the delivered read returns.

The account is deliberately not filterable, although the paginated spelling of the list narrows by the
account's name and address. That spelling does so with a join predicate the store evaluates, and the
rows the connection protocol narrows are the rows that came back — which carry no account column. A
filter here would be advertising a narrowing this surface cannot perform; the way to ask for one
person's candidacy is `userId`, and the way to narrow by a name is to read the account surface first.
"""
input CandidateFilter {
  id: IDFilter
  rating: DecimalFilter
  valueDate: DateTimeFilter
  appliedDate: DateTimeFilter
  hiredDate: DateTimeFilter
  rejectDate: DateTimeFilter
  status: StringFilter
  candidateLevel: StringFilter
  reWeeklyLimit: NumberFilter
  billRateCurrency: StringFilter
  billRateValue: DecimalFilter
  minimumBillingRate: DecimalFilter
  payPeriod: StringFilter
  cvUrl: StringFilter
  userId: IDFilter
  contactId: IDFilter
  organizationPositionId: IDFilter
  sourceId: IDFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateFilter!]
  or: [CandidateFilter!]
  not: CandidateFilter
}

"""
The account a candidacy signs in as.

**Only the four members the delivered write persists are here.** The delivered body's account also
carries a role and a preferred language, and the delivered handler overwrites both: it resolves the
candidate role itself, passes it beside the language of the request, and the handler behind that
resolves the relation from the database where the relation wins on persist. A stated role would
therefore be checked and then ignored — the only effect a caller could have with it is to be refused
for naming a role it may not assign, which is not a capability — and a stated language would be
overwritten by the language of the request. An argument whose effect is invisible is worse than an
argument that is absent, so neither is stated.

`password` is a plaintext member on the way in and is hashed before the account row is written; it is
never a member of an answer, because the answer is the row's projection and the projection carries no
digest.
"""
input CandidateUserInput {
  """
  The address the account is created with, and the address it signs in with.
  """
  email: String!

  """The given name to file on the account."""
  firstName: String

  """The family name to file on the account."""
  lastName: String

  """The image to show for the account."""
  imageUrl: String
}

"""
What a caller states when it files a candidacy.

**Only the members the delivered write persists are here.** The delivered create body inherits the
engagement's employment terms — `startedWorkOn`, `endWork`, `short_description`, `description`,
`anonymousBonus` and `employeeLevel` — and a candidate row has no column for any of them: the level
this row carries is `candidateLevel`, which the delivered create body does not accept. An argument
whose effect is invisible is worse than an argument that is absent, so none of the six is stated; the
level is written through `updateCandidate`, which is the route that carries it.

The collections the delivered body also accepts — the documents, the employment types and the
departments — are refused for the same reason at one remove: each is a collection of rows rather than
of identifiers, the delivered write cascades none of them, and the rows themselves are written through
the surface that owns them (`createCandidateDocument` for a document). The position and the tags are
the exception, because both are written from an identifier: `organizationPositionId` and `tagIds`.
"""
input CreateCandidateInput {
  """The organization the candidacy is filed under."""
  organizationId: ID!

  """
  The account to file together with the candidacy. The delivered write always files one.
  """
  user: CandidateUserInput!

  """The new account's password, hashed before the account row is written."""
  password: String!

  """The organization position the candidate is being considered for."""
  organizationPositionId: ID

  """
  The facets to attach to the candidate, as the identifiers the pivot is written from.
  """
  tagIds: [ID!]
}

"""
What a caller states when it edits a candidacy.

Every member is optional and a member that is absent is left as it is, because the delivered edit
persists the stated members over the row it read.

The same discipline as the create applies to the members: `offerDate`, `acceptDate` and `isAway` are
accepted by the delivered body and are columns of an *engagement*, not of a candidacy, so they are not
stated here. `status` is not stated either: it is written by the two pipeline moves and by the
subscriber's own reading of `rejectDate`, and a member for it would be a second way to write the
pipeline that the delivered routes leave to those two.

The contact is stated as the identifier the relation is written from rather than as the row the
delivered body accepts. A contact is shared with the employee and customer surfaces, so its own columns
are written through the contact surface; this member moves the pointer and nothing else.
"""
input UpdateCandidateInput {
  id: ID!

  """The organization position the candidate is being considered for."""
  organizationPositionId: ID

  """
  The facets to attach to the candidate, as the identifiers the pivot is written from.
  """
  tagIds: [ID!]

  """The contact row the candidacy keeps its address and fiscal facts in."""
  contactId: ID

  """When the candidacy was filed with the organization."""
  appliedDate: DateTime

  """When the candidate was hired."""
  hiredDate: DateTime

  """When the candidate was declined."""
  rejectDate: DateTime

  """The label the pipeline shows for this candidate's seniority."""
  candidateLevel: String

  """The URL of the candidate's curriculum vitae."""
  cvUrl: String

  """
  The billing rate per hour the candidate is filed at. Money, and therefore an exact decimal.
  """
  billRateValue: Decimal

  """The currency the two rates are stated in."""
  billRateCurrency: String

  """
  The floor below which the candidate would not be engaged. A rate, and therefore an exact decimal.
  """
  minimumBillingRate: Decimal

  """How often the candidate would be paid."""
  payPeriod: String

  """
  Recurring weekly limit, in hours. A whole number of hours rather than an amount.
  """
  reWeeklyLimit: Int

  """The candidate's LinkedIn profile."""
  linkedInUrl: String

  """The candidate's Facebook profile."""
  facebookUrl: String

  """The candidate's Instagram profile."""
  instagramUrl: String

  """The candidate's Twitter profile."""
  twitterUrl: String

  """The candidate's GitHub profile."""
  githubUrl: String

  """The candidate's GitLab profile."""
  gitlabUrl: String

  """The candidate's Upwork profile."""
  upworkUrl: String

  """The candidate's Stack Overflow profile."""
  stackoverflowUrl: String

  """The candidate's identifier at the external marketplace."""
  upworkId: String

  """The candidate's identifier at LinkedIn."""
  linkedInId: String

  """The external marketplace's public profile link."""
  profile_link: String
}

"""
The two pipeline moves the delivered route serves.

The vocabulary is the controller's own — it reads the move out of its path — and it is declared as an
enum here rather than carried as a string because the delivered `switch` has no default branch: a move
it does not name answers nothing at all, so a schema that accepted any string would advertise a
capability that does not exist. The values are stated as the route spells them, so the two protocols
name the same move with the same word.
"""
enum CandidatePipelineAction {
  hired
  rejected
}

"""
A paper the candidate filed with the candidacy: a curriculum vitae, a certificate, a reference.
"""
type CandidateDocument {
  id: ID!

  """The document's name, as the caller filed it."""
  name: String!

  """The URL the document is served from."""
  documentUrl: String

  """The candidacy this document belongs to."""
  candidateId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of documents."""
type CandidateDocumentEdge {
  node: CandidateDocument!
  cursor: String!
}

"""
One page of documents, with the total the filters select and the boundary cursors.
"""
type CandidateDocumentConnection {
  nodes: [CandidateDocument!]!
  edges: [CandidateDocumentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a document list may be sorted by."""
enum CandidateDocumentSortField {
  createdAt
  updatedAt
  name
}

"""
The order the documents are returned in. At most three keys are accepted.
"""
input CandidateDocumentSort {
  field: CandidateDocumentSortField!
  direction: SortDirection!
}

"""The fields a document list may be narrowed by."""
input CandidateDocumentFilter {
  id: IDFilter
  name: StringFilter
  documentUrl: StringFilter
  candidateId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateDocumentFilter!]
  or: [CandidateDocumentFilter!]
  not: CandidateDocumentFilter
}

"""What a caller states when it files a document under a candidacy."""
input CreateCandidateDocumentInput {
  """The candidacy the document belongs to."""
  candidateId: ID

  """The document's name."""
  name: String!

  """The URL the document is served from."""
  documentUrl: String
  organizationId: ID
}

"""What a caller states when it edits a document."""
input UpdateCandidateDocumentInput {
  id: ID!

  """The candidacy the document belongs to."""
  candidateId: ID

  """The document's name."""
  name: String

  """The URL the document is served from."""
  documentUrl: String
  organizationId: ID
}

"""One course of study the candidate completed."""
type CandidateEducation {
  id: ID!

  """The school or institution."""
  schoolName: String!

  """The degree the candidate was awarded."""
  degree: String!

  """The field the degree is in."""
  field: String!

  """When the course of study was completed."""
  completionDate: DateTime!

  """Free-text notes the caller filed with the row."""
  notes: String

  """The candidacy this row belongs to."""
  candidateId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of courses of study."""
type CandidateEducationEdge {
  node: CandidateEducation!
  cursor: String!
}

"""
One page of courses of study, with the total the filters select and the boundary cursors.
"""
type CandidateEducationConnection {
  nodes: [CandidateEducation!]!
  edges: [CandidateEducationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a course-of-study list may be sorted by."""
enum CandidateEducationSortField {
  createdAt
  updatedAt
  schoolName
  completionDate
}

"""
The order the courses of study are returned in. At most three keys are accepted.
"""
input CandidateEducationSort {
  field: CandidateEducationSortField!
  direction: SortDirection!
}

"""The fields a course-of-study list may be narrowed by."""
input CandidateEducationFilter {
  id: IDFilter
  schoolName: StringFilter
  degree: StringFilter
  field: StringFilter
  completionDate: DateTimeFilter
  notes: StringFilter
  candidateId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateEducationFilter!]
  or: [CandidateEducationFilter!]
  not: CandidateEducationFilter
}

"""What a caller states when it files a course of study."""
input CreateCandidateEducationInput {
  """The candidacy the row belongs to."""
  candidateId: ID
  schoolName: String!
  degree: String!
  field: String!
  completionDate: DateTime!
  notes: String
  organizationId: ID!
}

"""What a caller states when it edits a course of study."""
input UpdateCandidateEducationInput {
  id: ID!
  schoolName: String
  degree: String
  field: String
  completionDate: DateTime
  notes: String
  organizationId: ID
}

"""One engagement the candidate held before applying."""
type CandidateExperience {
  id: ID!

  """The role the candidate held."""
  occupation: String!

  """How long the candidate held it, as the caller stated it."""
  duration: String!

  """What the candidate did there."""
  description: String

  """The candidacy this row belongs to."""
  candidateId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of prior engagements."""
type CandidateExperienceEdge {
  node: CandidateExperience!
  cursor: String!
}

"""
One page of prior engagements, with the total the filters select and the boundary cursors.
"""
type CandidateExperienceConnection {
  nodes: [CandidateExperience!]!
  edges: [CandidateExperienceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a prior-engagement list may be sorted by."""
enum CandidateExperienceSortField {
  createdAt
  updatedAt
  occupation
}

"""
The order the prior engagements are returned in. At most three keys are accepted.
"""
input CandidateExperienceSort {
  field: CandidateExperienceSortField!
  direction: SortDirection!
}

"""The fields a prior-engagement list may be narrowed by."""
input CandidateExperienceFilter {
  id: IDFilter
  occupation: StringFilter
  duration: StringFilter
  description: StringFilter
  candidateId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateExperienceFilter!]
  or: [CandidateExperienceFilter!]
  not: CandidateExperienceFilter
}

"""What a caller states when it files a prior engagement."""
input CreateCandidateExperienceInput {
  """The candidacy the row belongs to."""
  candidateId: ID
  occupation: String!
  duration: String!
  description: String
  organizationId: ID!
}

"""What a caller states when it edits a prior engagement."""
input UpdateCandidateExperienceInput {
  id: ID!
  occupation: String
  duration: String
  description: String
  organizationId: ID
}

"""
One skill the candidate claims. A skill is a name on this row and not a row in a shared vocabulary.
"""
type CandidateSkill {
  id: ID!

  """The skill's name, as the caller filed it."""
  name: String!

  """The candidacy this row belongs to."""
  candidateId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of skills."""
type CandidateSkillEdge {
  node: CandidateSkill!
  cursor: String!
}

"""
One page of skills, with the total the filters select and the boundary cursors.
"""
type CandidateSkillConnection {
  nodes: [CandidateSkill!]!
  edges: [CandidateSkillEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a skill list may be sorted by."""
enum CandidateSkillSortField {
  createdAt
  updatedAt
  name
}

"""The order the skills are returned in. At most three keys are accepted."""
input CandidateSkillSort {
  field: CandidateSkillSortField!
  direction: SortDirection!
}

"""The fields a skill list may be narrowed by."""
input CandidateSkillFilter {
  id: IDFilter
  name: StringFilter
  candidateId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateSkillFilter!]
  or: [CandidateSkillFilter!]
  not: CandidateSkillFilter
}

"""What a caller states when it files a skill."""
input CreateCandidateSkillInput {
  """The candidacy the row belongs to."""
  candidateId: ID

  """The skill's name."""
  name: String!
  organizationId: ID!
}

"""What a caller states when it edits a skill."""
input UpdateCandidateSkillInput {
  id: ID!

  """The skill's name."""
  name: String
  organizationId: ID
}

"""
Where a candidacy came from: a job board, a referral, an agency.

A source is a **vocabulary** rather than a fact about one person: one row per origin, pointed at by
every candidacy that came from it. That is why this row carries no `candidateId` — the relation is
written from the candidate's own `sourceId` column — and why the list is the vocabulary itself rather
than one person's origins.
"""
type CandidateSource {
  id: ID!

  """The origin's name."""
  name: String!
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of sources."""
type CandidateSourceEdge {
  node: CandidateSource!
  cursor: String!
}

"""
One page of sources, with the total the filters select and the boundary cursors.
"""
type CandidateSourceConnection {
  nodes: [CandidateSource!]!
  edges: [CandidateSourceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a source list may be sorted by."""
enum CandidateSourceSortField {
  createdAt
  updatedAt
  name
}

"""
The order the sources are returned in. At most three keys are accepted.
"""
input CandidateSourceSort {
  field: CandidateSourceSortField!
  direction: SortDirection!
}

"""The fields a source list may be narrowed by."""
input CandidateSourceFilter {
  id: IDFilter
  name: StringFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateSourceFilter!]
  or: [CandidateSourceFilter!]
  not: CandidateSourceFilter
}

"""What a caller states when it files a source."""
input CreateCandidateSourceInput {
  """The origin's name."""
  name: String!
  organizationId: ID!
}

"""What a caller states when it edits a source."""
input UpdateCandidateSourceInput {
  id: ID!

  """The origin's name."""
  name: String
  organizationId: ID
}

"""
One sitting of the pipeline's interview stage.

The row carries the schedule, the place, the note and the average the platform computed from the
feedbacks of the sitting. Its four collections — the feedbacks, the interviewers, the technologies and
the personal qualities — are each read back through this domain's own root fields filtered by
`interviewId`, which is the identifier this row is pointed at by.
"""
type CandidateInterview {
  id: ID!

  """The interview's title."""
  title: String!

  """When the sitting begins."""
  startTime: DateTime

  """When the sitting ends."""
  endTime: DateTime

  """Where the sitting is held."""
  location: String

  """The note the caller filed with the sitting."""
  note: String

  """
  The average the platform computed from this sitting's feedbacks. A rating, and therefore an exact decimal.
  """
  rating: Decimal

  """The candidacy this sitting belongs to."""
  candidateId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of interviews."""
type CandidateInterviewEdge {
  node: CandidateInterview!
  cursor: String!
}

"""
One page of interviews, with the total the filters select and the boundary cursors.
"""
type CandidateInterviewConnection {
  nodes: [CandidateInterview!]!
  edges: [CandidateInterviewEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an interview list may be sorted by."""
enum CandidateInterviewSortField {
  createdAt
  updatedAt
  title
  startTime
  endTime
  rating
}

"""
The order the interviews are returned in. At most three keys are accepted.
"""
input CandidateInterviewSort {
  field: CandidateInterviewSortField!
  direction: SortDirection!
}

"""
The fields an interview list may be narrowed by. `candidateId` is the narrowing the delivered candidate-scoped route performs.
"""
input CandidateInterviewFilter {
  id: IDFilter
  title: StringFilter
  startTime: DateTimeFilter
  endTime: DateTimeFilter
  location: StringFilter
  note: StringFilter
  rating: DecimalFilter
  candidateId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateInterviewFilter!]
  or: [CandidateInterviewFilter!]
  not: CandidateInterviewFilter
}

"""What a caller states when it schedules a sitting."""
input CreateCandidateInterviewInput {
  """The candidacy the sitting belongs to."""
  candidateId: ID
  title: String!
  startTime: DateTime!
  endTime: DateTime!
  location: String
  note: String

  """
  The average the platform computed, where a caller states one. The delivered write persists what it is given.
  """
  rating: Decimal
  organizationId: ID
}

"""
What a caller states when it edits a sitting. The delivered edit route takes the create body's shape.
"""
input UpdateCandidateInterviewInput {
  id: ID!
  title: String
  startTime: DateTime
  endTime: DateTime
  location: String
  note: String
  rating: Decimal
  isArchived: Boolean
  organizationId: ID
}

"""
One employee sitting on one interview panel.

The row is the pair itself — an interview and an employee — and it is the pair that the panel reads, so
the employee's own columns are read from `employee(id)` and this member carries the identifier.
"""
type CandidateInterviewer {
  id: ID!

  """The sitting this interviewer sits on."""
  interviewId: ID!

  """
  The engagement of the person on the panel. The engagement is read from `employee(id)`.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of interviewers."""
type CandidateInterviewerEdge {
  node: CandidateInterviewer!
  cursor: String!
}

"""
One page of interviewers, with the total the filters select and the boundary cursors.
"""
type CandidateInterviewerConnection {
  nodes: [CandidateInterviewer!]!
  edges: [CandidateInterviewerEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an interviewer list may be sorted by."""
enum CandidateInterviewerSortField {
  createdAt
  updatedAt
}

"""
The order the interviewers are returned in. At most three keys are accepted.
"""
input CandidateInterviewerSort {
  field: CandidateInterviewerSortField!
  direction: SortDirection!
}

"""
The fields an interviewer list may be narrowed by. `interviewId` is the narrowing the delivered interview-scoped route performs.
"""
input CandidateInterviewerFilter {
  id: IDFilter
  interviewId: IDFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateInterviewerFilter!]
  or: [CandidateInterviewerFilter!]
  not: CandidateInterviewerFilter
}

"""What a caller states when it puts one employee on one panel."""
input CreateCandidateInterviewerInput {
  """The sitting the interviewer sits on."""
  interviewId: ID!

  """The engagement of the person on the panel."""
  employeeId: ID
  organizationId: ID
}

"""What a caller states when it edits one panel seat."""
input UpdateCandidateInterviewerInput {
  id: ID!
  interviewId: ID
  employeeId: ID
  organizationId: ID
}

"""
What a caller states when it puts several employees on one panel in one call.

The delivered body states the panel as a list of employee identifiers beside the interview, and the
delivered handler writes one row per identifier; this input states the same thing in the same way.
"""
input CreateCandidateInterviewersBulkInput {
  """The sitting every row of the call belongs to."""
  interviewId: ID!

  """The engagements to seat on the panel, one row each."""
  employeeIds: [ID!]!
  organizationId: ID
}

"""
One technology the panel assesses.

The row belongs to one sitting and carries the name the panel filed and the average the platform
computed from the criteria the feedbacks rated. The criteria themselves are `CandidateCriterionsRating`
rows, read back by `technologyId`.
"""
type CandidateTechnology {
  id: ID!

  """The technology's name, as the panel filed it."""
  name: String!

  """
  The average the platform computed from the criteria rated against this row. A rating, and therefore an exact decimal.
  """
  rating: Decimal

  """The sitting this row belongs to."""
  interviewId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of technologies."""
type CandidateTechnologyEdge {
  node: CandidateTechnology!
  cursor: String!
}

"""
One page of technologies, with the total the filters select and the boundary cursors.
"""
type CandidateTechnologyConnection {
  nodes: [CandidateTechnology!]!
  edges: [CandidateTechnologyEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a technology list may be sorted by."""
enum CandidateTechnologySortField {
  createdAt
  updatedAt
  name
  rating
}

"""
The order the technologies are returned in. At most three keys are accepted.
"""
input CandidateTechnologySort {
  field: CandidateTechnologySortField!
  direction: SortDirection!
}

"""
The fields a technology list may be narrowed by. `interviewId` is the narrowing the delivered interview-scoped route performs.
"""
input CandidateTechnologyFilter {
  id: IDFilter
  name: StringFilter
  rating: DecimalFilter
  interviewId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateTechnologyFilter!]
  or: [CandidateTechnologyFilter!]
  not: CandidateTechnologyFilter
}

"""What a caller states when it files one technology under a sitting."""
input CreateCandidateTechnologyInput {
  interviewId: ID
  name: String!
  rating: Decimal
  organizationId: ID
}

"""What a caller states when it edits a technology."""
input UpdateCandidateTechnologyInput {
  id: ID!
  interviewId: ID
  name: String
  rating: Decimal
  organizationId: ID
}

"""
What a caller states when it rates a technology in one call. The delivered handler reads the row's identifier and its rating.
"""
input CandidateTechnologyRatingInput {
  """The technology row."""
  id: ID!

  """The rating to write on it."""
  rating: Decimal
  organizationId: ID
}

"""
One personal quality the panel assesses.

The row is the technology's twin: it belongs to one sitting, it carries the name the panel filed, and
the criteria rated against it are `CandidateCriterionsRating` rows read back by `personalQualityId`.
The two are one shape and are served by one resolver for that reason.
"""
type CandidatePersonalQuality {
  id: ID!

  """The quality's name, as the panel filed it."""
  name: String!

  """
  The average the platform computed from the criteria rated against this row. A rating, and therefore an exact decimal.
  """
  rating: Decimal

  """The sitting this row belongs to."""
  interviewId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of personal qualities."""
type CandidatePersonalQualityEdge {
  node: CandidatePersonalQuality!
  cursor: String!
}

"""
One page of personal qualities, with the total the filters select and the boundary cursors.
"""
type CandidatePersonalQualityConnection {
  nodes: [CandidatePersonalQuality!]!
  edges: [CandidatePersonalQualityEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a personal-quality list may be sorted by."""
enum CandidatePersonalQualitySortField {
  createdAt
  updatedAt
  name
  rating
}

"""
The order the personal qualities are returned in. At most three keys are accepted.
"""
input CandidatePersonalQualitySort {
  field: CandidatePersonalQualitySortField!
  direction: SortDirection!
}

"""
The fields a personal-quality list may be narrowed by. `interviewId` is the narrowing the delivered interview-scoped route performs.
"""
input CandidatePersonalQualityFilter {
  id: IDFilter
  name: StringFilter
  rating: DecimalFilter
  interviewId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidatePersonalQualityFilter!]
  or: [CandidatePersonalQualityFilter!]
  not: CandidatePersonalQualityFilter
}

"""
What a caller states when it files one personal quality under a sitting.
"""
input CreateCandidatePersonalQualityInput {
  interviewId: ID
  name: String!
  rating: Decimal
  organizationId: ID
}

"""What a caller states when it edits a personal quality."""
input UpdateCandidatePersonalQualityInput {
  id: ID!
  interviewId: ID
  name: String
  rating: Decimal
  organizationId: ID
}

"""
One panel member's verdict on one sitting.

The row carries the verdict, its rating and the pipeline state it argues for, and it points at three
rows: the candidacy, the sitting and the panel seat. Each is carried as its identifier.
"""
type CandidateFeedback {
  id: ID!

  """The verdict, as the panel member wrote it."""
  description: String

  """
  The rating the panel member gave. A rating, and therefore an exact decimal.
  """
  rating: Decimal

  """
  The pipeline state the verdict argues for, as the contracts package's own value.
  """
  status: String

  """The candidacy the verdict is about."""
  candidateId: ID

  """The sitting the verdict belongs to."""
  interviewId: ID

  """The panel seat that filed the verdict."""
  interviewerId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of feedbacks."""
type CandidateFeedbackEdge {
  node: CandidateFeedback!
  cursor: String!
}

"""
One page of feedbacks, with the total the filters select and the boundary cursors.
"""
type CandidateFeedbackConnection {
  nodes: [CandidateFeedback!]!
  edges: [CandidateFeedbackEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a feedback list may be sorted by."""
enum CandidateFeedbackSortField {
  createdAt
  updatedAt
  rating
  status
}

"""
The order the feedbacks are returned in. At most three keys are accepted.
"""
input CandidateFeedbackSort {
  field: CandidateFeedbackSortField!
  direction: SortDirection!
}

"""
The fields a feedback list may be narrowed by. `interviewId` is the narrowing the delivered interview-scoped route performs.
"""
input CandidateFeedbackFilter {
  id: IDFilter
  description: StringFilter
  rating: DecimalFilter
  status: StringFilter
  candidateId: IDFilter
  interviewId: IDFilter
  interviewerId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateFeedbackFilter!]
  or: [CandidateFeedbackFilter!]
  not: CandidateFeedbackFilter
}

"""What a caller states when it files a verdict."""
input CreateCandidateFeedbackInput {
  """The candidacy the verdict is about."""
  candidateId: ID

  """The sitting the verdict belongs to."""
  interviewId: ID

  """The panel seat that files the verdict."""
  interviewerId: ID

  """The verdict."""
  description: String

  """The rating. The delivered body requires it."""
  rating: Decimal!

  """The pipeline state the verdict argues for."""
  status: String
  organizationId: ID
}

"""
What a caller states when it edits a verdict.

`interviewId` is stated as well as `interviewerId`, and that is the one place in this domain where an
identifier is carried beside the relation it addresses. The delivered handler does not read the
feedback's own interview column when it edits a verdict: it reads the interview out of the nested panel
seat the REST body carries, and recomputes that sitting's average from the verdicts that remain. This
input states the same fact as the identifier the seating is written from, and the resolver hands the
handler the nested shape it reads, so the two protocols leave the sitting in the same state.
"""
input UpdateCandidateFeedbackInput {
  id: ID!
  candidateId: ID

  """The panel seat the verdict belongs to."""
  interviewerId: ID

  """The sitting whose average the edit recomputes."""
  interviewId: ID
  description: String
  rating: Decimal
  status: String
  organizationId: ID
}

"""
One rating the panel gave to one criterion of one verdict.

The row is a join with a number on it: it points at the verdict that carries it and at exactly one of
the two things the sitting is assessed on — a technology or a personal quality. The rating is a whole
number on this row rather than a decimal, and the schema says so.
"""
type CandidateCriterionsRating {
  id: ID!

  """The rating the panel gave this criterion."""
  rating: Int!

  """
  The technology the rating is about, where the criterion is a technology.
  """
  technologyId: ID

  """
  The personal quality the rating is about, where the criterion is a quality.
  """
  personalQualityId: ID

  """The verdict this rating belongs to."""
  feedbackId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of criterion ratings."""
type CandidateCriterionsRatingEdge {
  node: CandidateCriterionsRating!
  cursor: String!
}

"""
One page of criterion ratings, with the total the filters select and the boundary cursors.
"""
type CandidateCriterionsRatingConnection {
  nodes: [CandidateCriterionsRating!]!
  edges: [CandidateCriterionsRatingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a criterion-rating list may be sorted by."""
enum CandidateCriterionsRatingSortField {
  createdAt
  updatedAt
  rating
}

"""
The order the criterion ratings are returned in. At most three keys are accepted.
"""
input CandidateCriterionsRatingSort {
  field: CandidateCriterionsRatingSortField!
  direction: SortDirection!
}

"""
The fields a criterion-rating list may be narrowed by. `feedbackId` is the narrowing the delivered verdict-scoped route performs.
"""
input CandidateCriterionsRatingFilter {
  id: IDFilter
  rating: NumberFilter
  technologyId: IDFilter
  personalQualityId: IDFilter
  feedbackId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CandidateCriterionsRatingFilter!]
  or: [CandidateCriterionsRatingFilter!]
  not: CandidateCriterionsRatingFilter
}

"""What a caller states when it files one criterion rating."""
input CreateCandidateCriterionsRatingInput {
  rating: Int!
  technologyId: ID
  personalQualityId: ID
  feedbackId: ID
  organizationId: ID
}

"""What a caller states when it edits one criterion rating."""
input UpdateCandidateCriterionsRatingInput {
  id: ID!
  rating: Int
  technologyId: ID
  personalQualityId: ID
  feedbackId: ID
  organizationId: ID
}

"""
What a caller states when it rates a verdict's technologies in one call.

The delivered body states each criterion as the row it rates plus the number to write on it, and the
delivered handler reads exactly those two members; this input states the same two.
"""
input CandidateTechnologyRatingBulkInput {
  """The technology row the rating is about."""
  technologyId: ID!

  """The rating to write on it."""
  rating: Int!
  organizationId: ID
}

"""
What a caller states when it rates a verdict's personal qualities in one call.
"""
input CandidatePersonalQualityRatingBulkInput {
  """The personal-quality row the rating is about."""
  personalQualityId: ID!

  """The rating to write on it."""
  rating: Int!
  organizationId: ID
}

"""
What a caller states when it files a whole verdict's criteria in one call.
"""
input CreateCandidateCriterionsRatingsBulkInput {
  """The verdict every rating of the call belongs to."""
  feedbackId: ID!

  """The technologies the verdict rated, with the number given to each."""
  technologies: [CandidateTechnologyRatingBulkInput!]

  """
  The personal qualities the verdict rated, with the number given to each.
  """
  qualities: [CandidatePersonalQualityRatingBulkInput!]
}

"""
What a caller states when it edits a whole verdict's criteria in one call.
"""
input UpdateCandidateCriterionsRatingsBulkInput {
  """The rows to write, as the delivered body states them."""
  criterionsRating: [UpdateCandidateCriterionsRatingInput!]!

  """
  The numbers to write on the technology rows, in the order the rows are stated.
  """
  technologies: [Int!]

  """
  The numbers to write on the personal-quality rows, in the order the rows are stated.
  """
  personalQualities: [Int!]
}

"""
One hostname that resolves to one channel.

Exactly one row per channel is primary, and `hostname` is unique across the whole deployment rather
than per tenant: the header is global, and two tenants claiming one host would make the resolution
ambiguous in exactly the request that cannot ask a question first.
"""
type ChannelDomain {
  id: ID!
  channelId: ID!
  channel: Channel

  """Lower-cased hostname, without scheme and without a trailing slash."""
  hostname: String!

  """
  Whether this is the channel's canonical host, which the others redirect to.
  """
  isPrimary: Boolean!

  """
  When false the host is served over plain HTTP — development and internal deployments.
  """
  isSslEnabled: Boolean!

  """
  Whether a request to this host is redirected to the channel's primary host.
  """
  redirectToPrimary: Boolean!

  """
  Tenant extras: the certificate reference, a content-delivery identifier.
  """
  metadata: JSON
  tenantId: ID
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""
What a verification of one hostname answers.

`verified` is true when the hostname resolves to the channel it is bound to, `resolvedTo` names the
channel it resolved to, and `checkedAt` is when the check ran. Whether a certificate is installed in
front of the host is a deployment question this database does not hold.
"""
type ChannelDomainVerification {
  verified: Boolean!
  resolvedTo: ID
  checkedAt: DateTime!
}

"""The boundary of one page of hostnames."""
type ChannelDomainEdge {
  node: ChannelDomain!
  cursor: String!
}

"""
One page of hostnames, with the total the filters select and the boundary cursors.
"""
type ChannelDomainConnection {
  nodes: [ChannelDomain!]!
  edges: [ChannelDomainEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a hostname may be sorted by."""
enum ChannelDomainSortField {
  createdAt
  updatedAt
  hostname
  isPrimary
  isSslEnabled
  redirectToPrimary
}

"""
The order the hostnames are returned in. At most three keys are accepted.
"""
input ChannelDomainSort {
  field: ChannelDomainSortField!
  direction: SortDirection!
}

"""The fields a hostname list may be narrowed by."""
input ChannelDomainFilter {
  id: IDFilter
  channelId: IDFilter
  hostname: StringFilter
  isPrimary: BooleanFilter
  isSslEnabled: BooleanFilter
  redirectToPrimary: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ChannelDomainFilter!]
  or: [ChannelDomainFilter!]
  not: ChannelDomainFilter
}

"""
What a caller states when it binds a hostname to a channel.

`channelId` and `hostname` are both required. The three flags carry their column defaults when they
are absent, so a plain binding is a primary, TLS-served, non-redirecting host.
"""
input CreateChannelDomainInput {
  channelId: ID!
  hostname: String!
  isPrimary: Boolean
  isSslEnabled: Boolean
  redirectToPrimary: Boolean
  metadata: JSON
}

"""
What a caller may change on a hostname that exists.

The hostname and the channel are absent: a binding that could be re-pointed is a binding whose
resolution changes under the requests that already resolved it. Setting `isPrimary` moves the flag
from the current holder rather than being refused, because an update is the deliberate operation.
"""
input UpdateChannelDomainInput {
  id: ID!
  isPrimary: Boolean
  isSslEnabled: Boolean
  redirectToPrimary: Boolean
  metadata: JSON
}

type Subscription {
  """
  Every change to a channel of the caller's tenant: a creation, an edit, a status move, a default
  change, a region-set replacement, a retirement, or a hostname bound to or withdrawn from it. The
  stream is narrowed by `channelId` and by `action`; both only ever narrow what the credential may
  already read.
  
  A subscription is a notification and never a system of record: an event produced while the client
  is disconnected is not replayed, and a client that must not miss one registers a webhook.
  """
  channelChanged(channelId: ID, action: String): Channel!

  """
  Every change to a contact group of the caller's tenant: the group being created, edited or
  soft-deleted, and its membership being granted or withdrawn. The stream is narrowed by `groupId`
  and by `action`; both only ever narrow what the credential may already read.
  
  A subscription is a notification and never a system of record: an event produced while the client
  is disconnected is not replayed, and a client that must not miss one registers a webhook.
  """
  contactGroupChanged(groupId: ID, action: String): ContactGroup!

  """
  Every operator move on a delivery record of the caller's tenant.
  
  The stream carries the record in its post-move state and the move that produced it (`replayed` or
  `marked-dead`), and it is narrowed by `deliveryId` and by `action`; both only ever narrow what the
  credential may already read. Its producer is the service both protocols move a record through, so
  a subscriber cannot tell which protocol made the move.
  
  **This is an operator's stream and not a consumer's.** It exists for the staff who watch the
  reliability machinery, and no subscription subscribes to another subscription's delivery. A
  subscription is a notification and never a system of record: a move made while the client is
  disconnected is not replayed, and a client that must not miss one reads the delivery records.
  """
  eventDeliveryChanged(deliveryId: ID, action: String): EventDelivery!

  """
  A stream of the platform's events, selected by name. One event is delivered per message, so a
  payload never merges unrelated facts. A subscription is a notification and never a system of
  record: an event produced while a client is disconnected is not replayed, and a client that must
  not miss one registers a webhook.
  """
  events(
    """
    The event names to receive, exactly as the platform names them (`order.placed`) or as a
    one-segment prefix pattern (`order.*`). At least one name is required, and a selection that
    matches no known event is refused at subscribe time so a typo is visible immediately.
    """
    names: [String!]!

    """
    Narrows the stream to one aggregate. It can only narrow what the credential may read.
    """
    aggregateId: ID

    """Narrows the stream to one channel of the credential's own scope."""
    channelId: ID
  ): EventEnvelope

  """
  A step of an operation of the caller's tenant moving: it started, succeeded, failed, or was
  compensated during the undo walk.
  
  The payload is the operation in its state at delivery, so a subscriber follows a run without
  polling. Narrowing by `operationId` and by `stepName` only ever narrows what the credential may
  already read: the caller without `OPERATIONS_VIEW` is refused by the guard before the stream is
  opened, and the tenant is taken from the credential rather than from an argument.
  """
  operationStepChanged(operationId: ID, stepName: String): Operation!

  """
  An operation of the caller's tenant reaching `COMPLETED`: every step of its plan ran.
  """
  operationCompleted(operationId: ID): Operation!

  """
  An operation of the caller's tenant that will not complete: it entered the compensation walk, or it
  settled compensated, failed or cancelled.
  
  The payload says which of the four it was — its `status` — and the step list says what was left
  behind, so a client never has to infer the outcome from the stream's name.
  
  A subscription is a notification and never a system of record: an event produced while the client
  is disconnected is not replayed, and a client that must not miss one reads the operation by id.
  """
  operationFailed(operationId: ID): Operation!

  """
  Every change to a region of the caller's tenant: a creation, an edit, a default change, a
  retirement, or a country-set replacement. The stream is narrowed by `regionId` and by `action`;
  both only ever narrow what the credential may already read.
  
  A subscription is a notification and never a system of record: an event produced while the client
  is disconnected is not replayed, and a client that must not miss one registers a webhook.
  """
  regionChanged(regionId: ID, action: String): Region!

  """
  Every attempt an endpoint of the caller's tenant has refused, as it is refused.
  
  The stream is narrowed by `subscriptionId`, which only ever narrows what the credential may already
  read. A delivery that reaches `DEAD` is announced too, so an integration owner that is only told
  about the retryable failures does not miss the moment its events stop arriving.
  
  A subscription is a notification and never a system of record: an event produced while the client
  is disconnected is not replayed, and a client that must not miss one registers a webhook — which is
  the resource this one is about.
  """
  webhookDeliveryFailed(subscriptionId: ID): WebhookDelivery!

  """
  Every subscription of the caller's tenant that has been switched off, whether by an operator or by
  the auto-disable rule.
  
  This is the fact an integration owner cannot poll for cheaply, which is why it is one of the two
  facts this domain streams. The row is answered with its secret replaced by a fingerprint, as it is
  on every other read.
  """
  webhookSubscriptionDisabled(subscriptionId: ID): WebhookSubscription!
  collectionChanged(id: ID): Collection!
  productPublished(productId: ID, channelId: ID): ProductPublication!
  productUnpublished(productId: ID, channelId: ID): ProductPublication!
  stockLevelChanged(warehouseId: ID, variantId: ID): StockLevel!
  stockLevelLow(warehouseId: ID): StockLevel!
  stockLevelOutOfStock(warehouseId: ID): StockLevel!
  stockReservationChanged(referenceId: ID): StockReservation!
  stockTransferChanged(id: ID): StockTransfer!
  paymentAuthorized(organizationId: ID): PaymentSession!
  paymentCaptured(organizationId: ID): PaymentCapture!
  paymentFailed(organizationId: ID): PaymentSession!
  paymentCanceled(organizationId: ID): PaymentSession!
  paymentRefunded(organizationId: ID): Refund!
  refundCreated(organizationId: ID): Refund!

  """
  A promotion was created, activated or expired.
  
  The stream is the promotion domain's own event stream, so a subscriber sees exactly the facts
  the domain already publishes for its outbox rather than a second, transport-shaped copy of
  them. `promotionId` narrows the stream to one promotion; it can only narrow what the caller is
  already allowed to read.
  """
  promotionChanged(promotionId: ID): PromotionChangedPayload!

  """
  A campaign budget reached its ceiling and its promotions stopped being applied.
  """
  promotionBudgetExhausted(promotionId: ID, budgetId: ID): PromotionBudgetExhaustedPayload!

  """
  A code was redeemed, whether by a basket being checked out or by an order being placed.
  """
  couponRedeemed(couponId: ID): CouponRedeemedPayload!

  """Stored value was spent with a card."""
  giftCardRedeemed(giftCardId: ID): GiftCardRedeemedPayload!

  """Streams rights that were granted or changed."""
  entitlementChanged(entitlementId: ID): Entitlement!

  """Streams rights that had a slot taken."""
  entitlementActivated(entitlementId: ID): Entitlement!

  """Streams rights that were withdrawn."""
  entitlementRevoked(entitlementId: ID): Entitlement!
}

"""
The sales context: a storefront, a marketplace listing, a point of sale, a B2B portal.
"""
type Channel {
  id: ID!
  tenantId: ID
  organizationId: ID
  name: String!

  """
  Stable key, unique per organization among the live rows. Written once: it is what a storefront
  URL, a seed file and an import mapping name.
  """
  code: String!
  description: String

  """Only `ACTIVE` serves requests. `ARCHIVED` is terminal."""
  status: ChannelStatus!

  """
  Whether this is the organization's default channel. At most one live row carries it.
  """
  isDefault: Boolean!

  """
  Currency of a cart created without an explicit currency. A three-letter ISO code.
  """
  defaultCurrency: String!

  """
  The region applied when a request does not resolve one. When set, it is also a member of `regions`.
  """
  defaultRegionId: ID
  defaultRegion: Region

  """BCP-47 locale for translated content."""
  defaultLocale: String

  """
  Prefix handed to the numbering series this channel numbers its documents with.
  """
  orderNumberPrefix: String

  """Zero-padding width of the numeric part of an order number."""
  orderNumberPadding: Int!

  """
  Channel-scoped overrides read by the checkout, tax and fulfilment strategies.
  """
  settings: JSON

  """Tenant extras. Read whole; nothing here is filtered on."""
  metadata: JSON

  """
  The hostnames that resolve to this channel, primary first when they are expanded.
  """
  domains: [ChannelDomain!]

  """
  The regions published to this channel, fallback region first when they are expanded.
  """
  regions: [ChannelRegion!]
  createdAt: DateTime
  updatedAt: DateTime
}

"""Where a channel and a region stand in their life."""
enum ChannelStatus {
  DRAFT
  ACTIVE
  INACTIVE
  ARCHIVED
}

"""The publication of one region to one channel."""
type ChannelRegion {
  id: ID!
  channelId: ID!
  channel: Channel
  regionId: ID!
  region: Region

  """
  Whether this is the region the channel falls back to when its own default region is unset.
  Distinct from `Channel.defaultRegionId`, which is the region a request that resolved none is
  priced in.
  """
  isDefault: Boolean!
  tenantId: ID
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of channels."""
type ChannelEdge {
  node: Channel!
  cursor: String!
}

"""
One page of channels, with the total the filters select and the boundary cursors.
"""
type ChannelConnection {
  nodes: [Channel!]!
  edges: [ChannelEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a channel may be sorted by."""
enum ChannelSortField {
  createdAt
  updatedAt
  name
  code
  status
  isDefault
}

"""
The order the channels are returned in. At most three keys are accepted.
"""
input ChannelSort {
  field: ChannelSortField!
  direction: SortDirection!
}

"""Comparison on the lifecycle status."""
input ChannelStatusFilter {
  eq: ChannelStatus
  ne: ChannelStatus
  in: [ChannelStatus!]
  nin: [ChannelStatus!]
  isNull: Boolean
}

"""
The fields a channel list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.
"""
input ChannelFilter {
  id: IDFilter
  name: StringFilter
  code: StringFilter
  status: ChannelStatusFilter
  isDefault: BooleanFilter
  defaultCurrency: StringFilter
  defaultLocale: StringFilter
  defaultRegionId: IDFilter
  orderNumberPrefix: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ChannelFilter!]
  or: [ChannelFilter!]
  not: ChannelFilter
}

"""
What a caller states when it opens a sales context.

The lifecycle members are absent by construction: the channel is created `DRAFT` by the service, its
default flag is claimed through `setDefaultChannel`, and its default region is named by the write
that publishes it. `code` is required here and never editable afterwards.
"""
input CreateChannelInput {
  organizationId: ID!
  name: String!
  code: String!
  description: String
  defaultCurrency: String
  defaultLocale: String
  orderNumberPrefix: String
  orderNumberPadding: Int
  settings: JSON
  metadata: JSON
}

"""
What a caller may change on a channel that exists.

`code` is absent, and a write that carries it is refused rather than silently ignored.
"""
input UpdateChannelInput {
  id: ID!
  organizationId: ID!
  name: String
  description: String
  defaultCurrency: String
  defaultLocale: String
  orderNumberPrefix: String
  orderNumberPadding: Int
  settings: JSON
  metadata: JSON
}

"""The lifecycle move a caller asks for."""
input SetChannelStatusInput {
  id: ID!
  status: ChannelStatus!
}

"""One member of a channel's region set."""
input ChannelRegionInput {
  regionId: ID!
  isDefault: Boolean
}

"""
The region set a channel sells into afterwards.

A set rather than a member-per-call pair: the regions stated are published and the ones left out are
withdrawn in one transaction, so a half-applied replacement is not a state a client can observe.
"""
input ReplaceChannelRegionsInput {
  id: ID!
  items: [ChannelRegionInput!]!
}

"""
One comment: a note attached to a row of another resource, with the author and the reply it answers.
"""
type Comment {
  id: ID!

  """The text of the comment, as the author wrote it."""
  comment: String!

  """
  Whether a person or the system acted, as the kernel's own `ActorTypeEnum` value.
  """
  actorType: String

  """Whether the conversation this comment belongs to has been resolved."""
  resolved: Boolean

  """When the conversation was resolved, or null while it is open."""
  resolvedAt: DateTime

  """
  When the text was last edited, or null while it stands as first written.
  """
  editedAt: DateTime

  """
  The resource type the comment is attached to, as the kernel's own `BaseEntityEnum` value.
  """
  entity: String!

  """The row of that resource the comment is attached to."""
  entityId: ID!

  """
  The comment this one answers, by identifier. Null on a comment that starts a conversation. The
  comment behind the identifier is not joined by this read; it is another node of this connection.
  """
  parentId: ID

  """
  The employee who resolved the conversation, by identifier. The employee row is not joined here.
  """
  resolvedByEmployeeId: ID

  """
  The employee who wrote the comment, by identifier. The employee row is not joined here.
  """
  employeeId: ID

  """The tenant the comment belongs to, by identifier."""
  tenantId: ID

  """The organization the comment was posted in, by identifier."""
  organizationId: ID

  """
  Whether the comment is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the comment is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the comment was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the comment was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime

  """When the comment was posted."""
  createdAt: DateTime

  """When the comment was last changed."""
  updatedAt: DateTime
}

"""The boundary of one page of comments."""
type CommentEdge {
  node: Comment!
  cursor: String!
}

"""
One page of comments, with the total the filters select and the boundary cursors.
"""
type CommentConnection {
  nodes: [Comment!]!
  edges: [CommentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a comment list may be sorted by."""
enum CommentSortField {
  id
  entity
  entityId
  resolved
  editedAt
  resolvedAt
  createdAt
  updatedAt
  deletedAt
}

"""
The order the comments are returned in. At most three keys are accepted.
"""
input CommentSort {
  field: CommentSortField!
  direction: SortDirection!
}

"""
The fields a comment list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the comment row, because the delivered list read answers the row itself
and joins only what its REST caller names in `relations` — which this surface does not name. The
parent/child column is among them on purpose: narrowing by `parentId` is how the replies of one
comment are read here, and `parentId: { isNull: true }` is how the comments that start a conversation
are read. The tenant comes from the credential on every read, so the two identifier members narrow the
row's own columns rather than restating a scope decision the caller does not have.
"""
input CommentFilter {
  id: IDFilter
  comment: StringFilter
  actorType: StringFilter
  resolved: BooleanFilter
  resolvedAt: DateTimeFilter
  editedAt: DateTimeFilter
  entity: StringFilter
  entityId: IDFilter
  parentId: IDFilter
  resolvedByEmployeeId: IDFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [CommentFilter!]
  or: [CommentFilter!]
  not: CommentFilter
}

"""
What a caller states when it posts a comment.

The row the comment is attached to is stated as the pair the delivered write stores: the resource type
in `entity` and the row in `entityId`, both required because both columns are. The organization is
stated because the delivered create does not derive one — the tenant is stamped from the credential,
never from an argument, but the organization is a member the caller names and the delivered route
validates against the caller's own memberships. There is no member for the tenant, for the same reason
there is none on any other write here.

The author is the credential's employee wherever the credential carries one, so `employeeId` is what a
caller holding the kernel's change-selected-employee permission states on behalf of somebody else, and
what the delivered write falls back to when the credential names no employee at all.

`parentId` makes the comment a reply. `mentionEmployeeIds` is the one member the delivered write reads
without storing it on the row: each identifier named there is published a mention against the comment
this call creates. A collection is not a member — `replies`, `members` and `teams` are relations this
write does not build, for the reason the object type states.
"""
input CreateCommentInput {
  """The text of the comment."""
  comment: String!

  """The resource type the comment is attached to."""
  entity: String!

  """The row of that resource the comment is attached to."""
  entityId: ID!

  """The organization the comment is posted in."""
  organizationId: ID!

  """Whether a person or the system acted. Omit for the delivered default."""
  actorType: String

  """Whether the conversation is already resolved."""
  resolved: Boolean

  """When the conversation was resolved."""
  resolvedAt: DateTime

  """When the text was last edited. Omit while it stands as first written."""
  editedAt: DateTime

  """
  The employee to record as the author. Omit to record the caller's own employee.
  """
  employeeId: ID

  """The employee to record as the one who resolved the conversation."""
  resolvedByEmployeeId: ID

  """The comment this one answers. Omit to start a conversation."""
  parentId: ID

  """
  The employees to mention in the comment. Each is published a mention; none is stored on the row.
  """
  mentionEmployeeIds: [ID!]

  """
  A denormalised name for the attached row, read by the mention publisher and not stored.
  """
  entityName: String

  """Whether the comment is active."""
  isActive: Boolean

  """Whether the comment is archived."""
  isArchived: Boolean

  """When the comment was archived."""
  archivedAt: DateTime
}

"""
What a caller states when it edits a comment that exists.

**The row the comment is attached to is deliberately not a member.** The delivered update DTO omits
`entity` and `entityId` — `OmitType(CreateCommentDTO, ['entity', 'entityId'])` — so the route refuses a
body stating them rather than moving a comment to another resource, and a member here would promise a
move neither protocol performs. Everything else the create accepts is accepted here, and every one of
them is optional: the delivered update saves the row it is given, so a member the caller leaves out is
left as it is.

There is no member for the tenant either, for the reason the create states. The put body carries the
path identifier, and that is why `id` is the one required member.
"""
input UpdateCommentInput {
  id: ID!

  """The text of the comment."""
  comment: String

  """The organization the comment is posted in."""
  organizationId: ID

  """Whether a person or the system acted."""
  actorType: String

  """Whether the conversation is resolved."""
  resolved: Boolean

  """When the conversation was resolved."""
  resolvedAt: DateTime

  """When the text was last edited."""
  editedAt: DateTime

  """The employee recorded as the author."""
  employeeId: ID

  """The employee recorded as the one who resolved the conversation."""
  resolvedByEmployeeId: ID

  """The comment this one answers."""
  parentId: ID

  """
  The employees to mention in the comment. Each is published a mention; none is stored on the row.
  """
  mentionEmployeeIds: [ID!]

  """
  A denormalised name for the attached row, read by the mention publisher and not stored.
  """
  entityName: String

  """Whether the comment is active."""
  isActive: Boolean

  """Whether the comment is archived."""
  isArchived: Boolean

  """When the comment was archived."""
  archivedAt: DateTime
}

"""
One company-account membership: who may buy for an organization contact, in what role, and up to what.
"""
type ContactBuyer {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The company account. A contact whose kind is `COMPANY`."""
  companyCustomerId: ID!

  """The individual buyer. A different contact from the account, always."""
  buyerCustomerId: ID!

  """
  What the buyer may do inside the account. The membership's authority, not a tenant role.
  """
  role: ContactBuyerRole!

  """
  The per-order ceiling for this buyer.
  
  When both this and the account's credit facility are set, the effective ceiling is the **lower** of
  the two: a buyer limit narrows what the account allows and never widens it.
  """
  spendingLimit: Decimal

  """
  The ceiling over one rolling period, evaluated as spent plus total against it.
  """
  periodSpendingLimit: Decimal

  """Orders at or above this amount require an approval."""
  approvalThreshold: Decimal

  """
  The day of the month the rolling period restarts, 1–28 so that every month has it.
  """
  periodStartDay: Int

  """When the buyer was attached to the account."""
  assignedAt: DateTime

  """
  The staff user who invited the buyer, when a member of staff did. History, not an authority.
  """
  invitedByUserId: ID

  """Whether the membership currently grants what its role allows."""
  isActive: Boolean

  """Tenant-defined extras."""
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""
What a buyer may do inside the company account it belongs to.

The role is the membership discriminator and not a capability: it says what kind of member this is,
while the approval capability of an approver is the platform permission the staff side already evaluates.
"""
enum ContactBuyerRole {
  """
  Administers the account: its buyer list, its limits and its approval thresholds.
  """
  ADMIN

  """
  May approve or reject an order a purchaser raised above that purchaser's threshold.
  """
  APPROVER

  """
  May place orders on the account, inside its own per-order and rolling-period limits.
  """
  PURCHASER

  """May read the account's orders and invoices, and may not place one."""
  VIEWER
}

"""The boundary of one page of memberships."""
type ContactBuyerEdge {
  node: ContactBuyer!
  cursor: String!
}

"""
One page of memberships, with the total the filters select and the boundary cursors.
"""
type ContactBuyerConnection {
  nodes: [ContactBuyer!]!
  edges: [ContactBuyerEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a membership may be sorted by."""
enum ContactBuyerSortField {
  createdAt
  updatedAt
  assignedAt
  role
  spendingLimit
  periodSpendingLimit
  approvalThreshold
}

"""
The order the memberships are returned in. At most three keys are accepted.
"""
input ContactBuyerSort {
  field: ContactBuyerSortField!
  direction: SortDirection!
}

"""Comparison on the role a buyer holds."""
input ContactBuyerRoleFilter {
  eq: ContactBuyerRole
  ne: ContactBuyerRole
  in: [ContactBuyerRole!]
  nin: [ContactBuyerRole!]
  isNull: Boolean
}

"""
The fields a membership list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.
"""
input ContactBuyerFilter {
  id: IDFilter
  companyCustomerId: IDFilter
  buyerCustomerId: IDFilter
  role: ContactBuyerRoleFilter
  periodStartDay: NumberFilter
  invitedByUserId: IDFilter
  isActive: BooleanFilter
  assignedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ContactBuyerFilter!]
  or: [ContactBuyerFilter!]
  not: ContactBuyerFilter
}

"""
What a caller states when a buyer is attached to a company account.

Both sides of the pivot are required and neither is mutable afterwards: a membership that could be
re-pointed would be a way to move purchasing authority without a trace.
"""
input CreateContactBuyerInput {
  organizationId: ID!
  companyCustomerId: ID!
  buyerCustomerId: ID!
  role: ContactBuyerRole
  spendingLimit: Decimal
  periodSpendingLimit: Decimal
  approvalThreshold: Decimal
  periodStartDay: Int
  assignedAt: DateTime
  invitedByUserId: ID
  metadata: JSON
}

"""
A party's login — the only resource of the kernel that holds a secret, and one that answers without it.
"""
type ContactCredential {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The contact the credential authenticates. One contact is one login."""
  customerId: ID!

  """
  The login identifier, always stored normalised. It resolves one credential per tenant.
  """
  email: String!

  """
  Whether the e-mail address has been confirmed by redeeming its verification token.
  """
  isVerified: Boolean!

  """When the contact last authenticated successfully."""
  lastLoginAt: DateTime

  """
  The instant the credential stops refusing logins, when it is locked.
  
  The lockout ladder counts consecutive failures, so this is a statement about them rather than a
  permanent state: a successful authentication clears both it and the counter.
  """
  lockedUntil: DateTime

  """
  Whether an authenticator factor has been enrolled.
  
  Answered as a flag rather than as the secret: the value is encrypted at rest and is read by nothing
  that answers a caller.
  """
  hasMfa: Boolean!
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of credentials."""
type ContactCredentialEdge {
  node: ContactCredential!
  cursor: String!
}

"""
One page of credentials, with the total the filters select and the boundary cursors.
"""
type ContactCredentialConnection {
  nodes: [ContactCredential!]!
  edges: [ContactCredentialEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a credential may be sorted by."""
enum ContactCredentialSortField {
  createdAt
  updatedAt
  email
  isVerified
  lastLoginAt
}

"""
The order the credentials are returned in. At most three keys are accepted.
"""
input ContactCredentialSort {
  field: ContactCredentialSortField!
  direction: SortDirection!
}

"""
The fields a credential list may be narrowed by.

Every member is a member of the readable shape, because the shape is what the connection filters: a
secret member is not filterable here for the same reason it is not selectable.
"""
input ContactCredentialFilter {
  id: IDFilter
  customerId: IDFilter
  email: StringFilter
  isVerified: BooleanFilter
  hasMfa: BooleanFilter
  lastLoginAt: DateTimeFilter
  lockedUntil: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ContactCredentialFilter!]
  or: [ContactCredentialFilter!]
  not: ContactCredentialFilter
}

"""
What a caller states when it records a party's login.

`password` is the password the party chose, hashed by the platform's password hasher before the service
is reached — the contract the service states when it demands a hash and refuses a plaintext. The four
secrets the row stores are absent by construction, and because an input type is closed, a document that
names one is refused by the schema itself.
"""
input CreateContactCredentialInput {
  organizationId: ID!
  customerId: ID!
  email: String!
  password: String!
  metadata: JSON
}

"""One explicit group membership — the pivot between a party and a group."""
type ContactGroupMember {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The party that is a member."""
  customerId: ID!

  """The group the party belongs to."""
  groupId: ID!

  """When the membership was granted."""
  assignedAt: DateTime!

  """
  When the membership lapses, for a temporary one. Null means it does not lapse.
  
  An expired row is treated as absent by every reader even before the cleanup job removes it, so a
  lapsed membership never grants anything.
  """
  expiresAt: DateTime

  """
  Who wrote the row, which is also what may remove it: an operator removes the hand-written
  membership, the segment materialiser replaces its own, and an import replaces its own.
  """
  source: ContactGroupSource!
  createdAt: DateTime
  updatedAt: DateTime
}

"""How one membership row came to exist."""
enum ContactGroupSource {
  """
  Written by an operator. Never removed by the segment materialiser or by an import.
  """
  MANUAL

  """
  Written by the segment materialiser; may be replaced wholesale by the next evaluation.
  """
  RULE

  """
  Written by a bulk import; removed or superseded by a later import of the same source.
  """
  IMPORT
}

"""One party joining a group."""
input ContactGroupMemberInput {
  contactId: ID!

  """
  When the membership lapses. Omitted means it does not lapse, and a stated instant in the past is
  refused: a membership that lapses immediately grants nothing, and the caller would be told it had
  granted something.
  """
  expiresAt: DateTime
}

"""One party leaving a group, and which of its memberships is meant."""
input ContactGroupMemberRemoveInput {
  contactId: ID!

  """Which membership row is meant. Defaults to the hand-written one."""
  source: ContactGroupSource
}

"""
The parties joining one group.

A list rather than a member-per-call mutation for the reason the service states: the whole list is
validated before any of it is written, so a body naming a party twice, or one that already holds a live
membership, is refused as a whole rather than half-applied.
"""
input AddContactGroupMembersInput {
  groupId: ID!
  members: [ContactGroupMemberInput!]!
}

"""
The parties leaving one group, each of the provenance the caller states.
"""
input RemoveContactGroupMembersInput {
  groupId: ID!
  members: [ContactGroupMemberRemoveInput!]!
}

"""A named set of parties that prices and promotions can target."""
type ContactGroup {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The display name of the group, as an operator reads it."""
  name: String!

  """
  The stable key an integration addresses the group by, unique per organization among live rows.
  Written once for a group the platform maintains, whose code its own logic addresses it by.
  """
  code: String!

  """What the group is for, in the tenant's own words."""
  description: String

  """
  Whether membership is explicit or computed. A rule-based group's membership is never written down.
  """
  type: ContactGroupType!

  """The price list granted to every member of this group."""
  priceListId: ID

  """
  The group-wide discount, as a fraction between 0 and 1. Applied when no price list wins.
  
  A value a resolution branches on, so it is a column rather than a key of `metadata`.
  """
  discountPercent: Float

  """
  Whether the platform maintains this group. Such a group is neither deletable nor re-codable, and
  the flag is written by the platform's own seeding path rather than by a request.
  """
  isSystem: Boolean!

  """Tenant-defined extras that nothing filters on."""
  metadata: JSON

  """
  The live membership of this group, oldest first. Resolved by the membership domain, which owns the
  pivot and the rule that an expired row is absent to every reader.
  """
  members: [ContactGroupMember!]

  """
  How many parties are live members. Resolved from the pivot rather than counted as rows, because a
  membership whose window has closed is not a membership.
  """
  memberCount: Int!
  createdAt: DateTime
  updatedAt: DateTime
}

"""
What kind of membership a group has.

Static and rule-based segmentation live in one concept, because both are "a set of contacts that prices
and promotions can target". The kind is not a label: it decides who may write a membership row.
"""
enum ContactGroupType {
  """
  Membership is exactly the live member rows of this group, and they may be written by hand.
  """
  STATIC

  """
  Membership is computed from the group's rules on every evaluation and is never materialised.
  """
  RULE_BASED
}

"""The boundary of one page of groups."""
type ContactGroupEdge {
  node: ContactGroup!
  cursor: String!
}

"""
One page of groups, with the total the filters select and the boundary cursors.
"""
type ContactGroupConnection {
  nodes: [ContactGroup!]!
  edges: [ContactGroupEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a group may be sorted by."""
enum ContactGroupSortField {
  createdAt
  updatedAt
  name
  code
  type
  isSystem
  discountPercent
}

"""The order the groups are returned in. At most three keys are accepted."""
input ContactGroupSort {
  field: ContactGroupSortField!
  direction: SortDirection!
}

"""Comparison on the kind of membership."""
input ContactGroupTypeFilter {
  eq: ContactGroupType
  ne: ContactGroupType
  in: [ContactGroupType!]
  nin: [ContactGroupType!]
  isNull: Boolean
}

"""
The fields a group list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.
"""
input ContactGroupFilter {
  id: IDFilter
  name: StringFilter
  code: StringFilter
  description: StringFilter
  type: ContactGroupTypeFilter
  priceListId: IDFilter
  isSystem: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ContactGroupFilter!]
  or: [ContactGroupFilter!]
  not: ContactGroupFilter
}

"""
What a caller states when it creates a group.

`isSystem` is absent by construction: a caller that could promote its own group to undeletable would be
deciding a platform matter. A body that states it is refused rather than ignored.
"""
input CreateContactGroupInput {
  organizationId: ID!
  name: String!
  code: String!
  description: String
  type: ContactGroupType
  priceListId: ID
  discountPercent: Float
  metadata: JSON
}

"""
What a caller may change on a group that exists.

The kind is among the mutable fields, because a tenant that outgrows a hand-kept list turns it into a
rule-based segment; the service refuses that change while hand-written member rows exist.
"""
input UpdateContactGroupInput {
  id: ID!
  organizationId: ID!
  name: String
  code: String
  description: String
  type: ContactGroupType
  priceListId: ID
  discountPercent: Float
  metadata: JSON
}

"""
A party's person-or-place detail row: the name, the address and the coordinates documents print.
"""
type Contact {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The display name of the person or the place, as entered."""
  name: String
  firstName: String
  lastName: String

  """The country as text, as entered. It is not the country master's code."""
  country: String
  city: String

  """
  Street address. A contact carries exactly one: the address book holds the many.
  """
  address: String

  """Second line of the street address — a unit, a floor, a building."""
  address2: String
  postcode: String

  """Latitude of the address, from -90 to 90. A coordinate, so a `Float`."""
  latitude: Float

  """
  Longitude of the address, from -180 to 180. A coordinate, so a `Float`.
  """
  longitude: Float

  """The region or state code the country's own vocabulary uses."""
  regionCode: String
  fax: String

  """
  The registration or tax identity as printed on the party's own document. Reverse charge and
  intra-community zero-rating are conditional on the **buyer's** registration being present, and this
  is where a legacy row keeps it — the party row carries its own, more specific pair.
  """
  fiscalInformation: String
  website: String

  """
  Whether the row is active. A removal sets `deletedAt`; it does not clear this flag.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime

  """
  When the row was withdrawn, or null while it is live. The two lifecycle routes write this alone.
  """
  deletedAt: DateTime
}

"""The boundary of one page of contacts."""
type ContactEdge {
  node: Contact!
  cursor: String!
}

"""
One page of contacts, with the total the filters select and the boundary cursors.
"""
type ContactConnection {
  nodes: [Contact!]!
  edges: [ContactEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a contact list may be sorted by."""
enum ContactSortField {
  createdAt
  updatedAt
  name
  firstName
  lastName
  city
  country
}

"""
The order the contacts are returned in. At most three keys are accepted.
"""
input ContactSort {
  field: ContactSortField!
  direction: SortDirection!
}

"""
The fields a contact list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned: a member the read does not select would be a filter that silently
selects nothing. Three members of the type are deliberately not here: `latitude` and `longitude` are
coordinates, not a narrowing a list of parties is ever read by, and `isActive` is a flag no delivered
read of this resource narrows on — the list answers live rows through the soft-delete column, so a
filter on the flag would be a second, weaker way to ask the question the read already answers.
"""
input ContactFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  firstName: StringFilter
  lastName: StringFilter
  country: StringFilter
  city: StringFilter
  address: StringFilter
  address2: StringFilter
  postcode: StringFilter
  regionCode: StringFilter
  fax: StringFilter
  fiscalInformation: StringFilter
  website: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ContactFilter!]
  or: [ContactFilter!]
  not: ContactFilter
}

"""
What a caller states when it records a contact.

**Every member is optional, and that is the delivered route's own contract.** `POST /contact` binds
its body to a partial entity and validates nothing beyond the credential's tenant, so the input
states the row's descriptive columns — what a contact *is* — and adds no member the row does not
have. The organization is among them because it is a column of this row and the route writes the
value it is given; the tenant is not, because the service stamps the caller's own tenant and
overwrites whatever a body states.

The base entity's lifecycle flags (`isActive`, `isArchived`, `archivedAt`), its soft-delete marker
and its three audit-user columns are deliberately not members: the two lifecycle routes are what move
the marker, and an input that could set an audit column would be a way to write a fact the platform's
own bookkeeping owns.
"""
input CreateContactInput {
  organizationId: ID
  name: String
  firstName: String
  lastName: String
  country: String
  city: String
  address: String
  address2: String
  postcode: String
  latitude: Float
  longitude: Float
  regionCode: String
  fax: String
  fiscalInformation: String
  website: String
}

"""
What a caller states when it changes a contact.

**A member the caller leaves out is left as it is.** The delivered edit is a partial column update
against the criterion the path names, not a replacement of the row, so the input is the create input
with the criterion stated and every other member optional. The tenant is absent for the reason it is
absent above: the service takes it from the credential and overwrites whatever a body states.
"""
input UpdateContactInput {
  id: ID!
  organizationId: ID
  name: String
  firstName: String
  lastName: String
  country: String
  city: String
  address: String
  address2: String
  postcode: String
  latitude: Float
  longitude: Float
  regionCode: String
  fax: String
  fiscalInformation: String
  website: String
}

"""
One country of the platform's master: its ISO 3166-1 code and the name it is written as.
"""
type Country {
  id: ID!

  """
  The ISO 3166-1 alpha-2 code: the row's identity as a country, and the code an address states.
  """
  isoCode: String!

  """The country's name, as the platform writes it."""
  country: String!
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of countries."""
type CountryEdge {
  node: Country!
  cursor: String!
}

"""
One page of countries, with the total the filters select and the boundary cursors.
"""
type CountryConnection {
  nodes: [Country!]!
  edges: [CountryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a country list may be sorted by.

The code and the name, which are the two orders this table is read in: by code, which is the row's
identity and the order a form's country picker offers a counterparty the code for, and by name, which
is the order a person reads a picker in. The two timestamps are how a change to the master is found
again.

There is nothing else to offer. A country row is a code and a name; the two lifecycle flags below are
a state a row is in rather than a quantity, and a list of the countries that have been retired is that
state stated as a filter.
"""
enum CountrySortField {
  createdAt
  updatedAt
  isoCode
  country
}

"""
The order the countries are returned in. At most three keys are accepted.
"""
input CountrySort {
  field: CountrySortField!
  direction: SortDirection!
}

"""
The fields a country list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Both facts the object type carries are filterable — the code identifies one country and the name is
how a searcher reaches it without knowing the code — and so are the two lifecycle flags. A client that
can read a flag and cannot narrow by it has to page the whole master to find the retired rows, and
"what is still in use?" is the question this table is most often opened to answer. Neither flag is
written by a route of this resource: a caller reads them here and they are set by the path that
maintains the master.
"""
input CountryFilter {
  id: IDFilter
  isoCode: StringFilter
  country: StringFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CountryFilter!]
  or: [CountryFilter!]
  not: CountryFilter
}

"""
One currency of the platform's master: its ISO 4217 code, the name it is written as, and the facts an
amount in it is rendered and rounded by.
"""
type Currency {
  id: ID!

  """
  The ISO 4217 alpha-3 code: the row's identity as a currency, and the code every amount names.
  """
  isoCode: String!

  """The currency's name, as the platform writes it."""
  currency: String!

  """
  How many minor-unit digits the currency has: `2` for most, `0` for the currencies with no minor unit
  and `3` for the few with a thousandth. A count, so it is an `Int` and never a decimal.
  """
  decimalPlaces: Int!

  """
  The display symbol, absent for a currency this table has not been told a symbol for.
  """
  symbol: String

  """
  Whether the symbol precedes or follows the amount — `PREFIX` or `SUFFIX`. Carried as its own value
  rather than declared as an enum: the vocabulary is the money layer's, and it is the same one the
  column is typed with.
  """
  symbolPosition: String!

  """Whether a space separates the symbol from the amount."""
  symbolSpace: Boolean!

  """
  How a value that is not exact at the currency's scale is resolved — `HALF_UP`, `HALF_EVEN`, `UP`,
  `DOWN` or `NONE`, the last of which makes an inexact value an error rather than a silent truncation.
  Carried as its own value, for the same reason as `symbolPosition`.
  """
  roundingMode: String!

  """
  The cash-rounding step applied at the payment boundary, as an exact decimal; `0` disables it. The
  column is `numeric(20,6)`, which is the shape the platform's exact-decimal scalar is the wire form
  of, so a step read here and the same step read over REST are the same decimal.
  """
  roundingIncrement: Decimal!

  """
  Whether the currency can settle a payment. False for the unit-of-account and metal codes, which are
  quoted against but never tendered — the distinction a checkout needs before it offers a currency.
  """
  isTender: Boolean!
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of currencies."""
type CurrencyEdge {
  node: Currency!
  cursor: String!
}

"""
One page of currencies, with the total the filters select and the boundary cursors.
"""
type CurrencyConnection {
  nodes: [Currency!]!
  edges: [CurrencyEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a currency list may be sorted by.

The code and the name are the two orders a currency master is actually read in: by code, which is the
row's identity and the order an operator reconciles a report against, and by name, which is the order
a picker shows. The two timestamps are how a change to the master is found again.

The formatting members are not sort keys. The order of a symbol, of a rounding mode or of a
cash-rounding step is the order of a spelling, which is not a fact about a currency; a list of the
currencies that round a particular way, or that can be tendered at all, is that fact stated as a
filter.
"""
enum CurrencySortField {
  createdAt
  updatedAt
  isoCode
  currency
}

"""
The order the currencies are returned in. At most three keys are accepted.
"""
input CurrencySort {
  field: CurrencySortField!
  direction: SortDirection!
}

"""
The fields a currency list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every fact the object type carries is filterable, because each of them is one a caller selects
currencies by: the code and the name identify one, `decimalPlaces`, `symbolPosition`, `symbolSpace`
and `roundingMode` say how an amount in it is written and rounded, and `isTender` says whether it can
settle a payment at all. `roundingIncrement` is stated through the decimal family, which is the family
money is compared in.

`isActive` and `isArchived` are filterable too, and `isActive` is the sharpest case of why: the table
declares `(isActive, decimalPlaces)` and `(isActive, isTender)` as composite indexes, which are
exactly the two reads a checkout makes — the currencies still in use, and how many decimals and
whether each can be tendered — so narrowing the master to its live rows is a read this resource exists
to serve rather than one a client should have to page the whole table to perform.
"""
input CurrencyFilter {
  id: IDFilter
  isoCode: StringFilter
  currency: StringFilter
  decimalPlaces: NumberFilter
  symbol: StringFilter
  symbolPosition: StringFilter
  symbolSpace: BooleanFilter
  roundingMode: StringFilter
  roundingIncrement: DecimalFilter
  isTender: BooleanFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [CurrencyFilter!]
  or: [CurrencyFilter!]
  not: CurrencyFilter
}

"""
The SMTP transport an organization's mail is sent through, or the installation's own when the
organization has none.
"""
type SmtpSetting {
  """
  The configuration row's identifier, absent when the answer is the installation's own transport
  rather than a row.
  """
  id: ID

  """The address messages are sent from."""
  fromAddress: String

  """
  The transport host. Absent only when the installation's own configuration does not name one.
  """
  host: String

  """The transport port."""
  port: Int

  """
  Whether the connection is secured from the first byte rather than upgraded to it.
  """
  secure: Boolean

  """Whether an operator has validated this configuration by using it."""
  isValidate: Boolean

  """
  The organization the configuration belongs to. Absent on the installation-wide answer.
  """
  organizationId: ID

  """
  The tenant the configuration belongs to. Stamped from the credential, never stated by a caller.
  """
  tenantId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are the CRUD base's
  own routes and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of configurations."""
type SmtpSettingEdge {
  node: SmtpSetting!
  cursor: String!
}

"""
One page of configurations, with the total the filters select and the boundary cursors.
"""
type SmtpSettingConnection {
  nodes: [SmtpSetting!]!
  edges: [SmtpSettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a configuration list may be sorted by."""
enum SmtpSettingSortField {
  createdAt
  updatedAt
  host
  port
  isValidate
}

"""
The order the configurations are returned in. At most three keys are accepted.
"""
input SmtpSettingSort {
  field: SmtpSettingSortField!
  direction: SortDirection!
}

"""
The fields a configuration list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

`organizationId: { isNull: true }` is the installation-wide configuration — the row that answers for
every organization that has none of its own, and the one every new installation is seeded with. The
credentials are not filterable, for the same reason they are not members: a filter is a comparison
against the row's own value, so `username: { like: '%…%' }` would answer whether a credential matches,
one character at a time.
"""
input SmtpSettingFilter {
  id: IDFilter
  fromAddress: StringFilter
  host: StringFilter
  port: NumberFilter
  secure: BooleanFilter
  isValidate: BooleanFilter
  organizationId: IDFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [SmtpSettingFilter!]
  or: [SmtpSettingFilter!]
  not: SmtpSettingFilter
}

"""
What a caller states when it files a configuration.

The credentials are members here and nowhere else: the delivered body requires them — a transport that
cannot authenticate is not a configuration — and they travel one way, into the store.
"""
input CreateSmtpSettingInput {
  """The address messages are sent from."""
  fromAddress: String

  """The transport host."""
  host: String!

  """The transport port."""
  port: Int!

  """
  Whether the connection is secured from the first byte rather than upgraded to it.
  """
  secure: Boolean!

  """Whether the caller states the configuration as already validated."""
  isValidate: Boolean

  """The account the transport authenticates as."""
  username: String!

  """The account's secret."""
  password: String!

  """
  The organization the configuration belongs to. Omit for the installation-wide one.
  """
  organizationId: ID
}

"""
What a caller states when it changes a configuration that exists.

The delivered edit binds the creation body and the identifier from the path, so every member is stated
here as the creation input states it and the identifier is added: the delivered edit replaces the
writable columns rather than patching around them.
"""
input UpdateSmtpSettingInput {
  id: ID!
  fromAddress: String
  host: String!
  port: Int!
  secure: Boolean!
  isValidate: Boolean
  username: String!
  password: String!
  organizationId: ID
}

"""
A configuration as the validator states it: the same members a creation states, because verifying a
transport is using one.

Every member is required here for the same reason it is on the delivered body: the validator opens a
connection with what it is given, and a configuration missing its credentials is refused by the
delivered route's own validation rather than verified and reported as failing.
"""
input ValidateSmtpSettingInput {
  fromAddress: String
  host: String!
  port: Int!
  secure: Boolean!
  username: String!
  password: String!
}

"""
One widget placed on a dashboard: what it renders, where it sits and how it is configured.
"""
type DashboardWidget {
  id: ID!

  """The widget's display name, as the dashboard prints it."""
  name: String!

  """The placement's position among the dashboard's widgets."""
  order: Int

  """The placement's size, in the units the dashboard's own grid uses."""
  size: Int

  """The colour the placement is rendered in."""
  color: String

  """
  Whether the placement is shown. A hidden placement keeps its row and its configuration.
  """
  isVisible: Boolean

  """
  The placement's own settings. Its shape belongs to the widget the placement renders, so it travels
  as the document it is.
  """
  options: JSON

  """The dashboard the placement sits on."""
  dashboardId: ID

  """
  The employee the placement is scoped to, stamped from the credential when the caller names none.
  """
  employeeId: ID

  """The project the placement reports on, when it reports on one."""
  projectId: ID

  """The team the placement reports on, when it reports on one."""
  organizationTeamId: ID
  tenantId: ID
  organizationId: ID

  """
  Whether the row is active. A removal sets `deletedAt`; it does not clear this flag.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime

  """
  When the placement was withdrawn, or null while it is live. The two lifecycle routes write this alone.
  """
  deletedAt: DateTime
}

"""The boundary of one page of dashboard widgets."""
type DashboardWidgetEdge {
  node: DashboardWidget!
  cursor: String!
}

"""
One page of dashboard widgets, with the total the filters select and the boundary cursors.
"""
type DashboardWidgetConnection {
  nodes: [DashboardWidget!]!
  edges: [DashboardWidgetEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a widget list may be sorted by."""
enum DashboardWidgetSortField {
  createdAt
  updatedAt
  name
  order
  isVisible
}

"""
The order the widgets are returned in. At most three keys are accepted.
"""
input DashboardWidgetSort {
  field: DashboardWidgetSortField!
  direction: SortDirection!
}

"""
The fields a widget list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

`dashboardId` is the member a client assembles a dashboard's canvas with, which is the read this
resource exists for: the widgets of one dashboard are this connection narrowed by that column rather
than a second root field over the same rows.
"""
input DashboardWidgetFilter {
  id: IDFilter
  name: StringFilter
  order: NumberFilter
  size: NumberFilter
  color: StringFilter
  isVisible: BooleanFilter
  options: JSONFilter
  dashboardId: IDFilter
  employeeId: IDFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [DashboardWidgetFilter!]
  or: [DashboardWidgetFilter!]
  not: DashboardWidgetFilter
}

"""
What a caller states when it places a widget.

The members are the ones the delivered create DTO declares. The employee is stated only to place a
widget for somebody else: the delivered write stamps the caller's own employee when the member is
absent. The tenant is not a member — the write stamps the caller's own — and neither are the audit
columns, which the platform's own bookkeeping owns.
"""
input CreateDashboardWidgetInput {
  """The widget's display name."""
  name: String!

  """The position among the dashboard's widgets."""
  order: Int
  size: Int
  color: String

  """Whether the placement is shown."""
  isVisible: Boolean

  """The placement's own settings, as the widget's own document."""
  options: JSON

  """The dashboard to place the widget on."""
  dashboardId: ID

  """
  The employee to place the widget for. Defaults to the caller's own employee.
  """
  employeeId: ID
  projectId: ID
  organizationTeamId: ID
  organizationId: ID
}

"""
What a caller states when it changes a placement that exists.

The identifier is the path's, so it is stated once and only here. Every other member is optional, and
**a member the caller leaves out is left as it is**: the delivered edit is the create's own service
call carrying the identifier, which updates the columns the body names rather than replacing the row.
"""
input UpdateDashboardWidgetInput {
  id: ID!
  name: String
  order: Int
  size: Int
  color: String
  isVisible: Boolean
  options: JSON
  dashboardId: ID
  employeeId: ID
  projectId: ID
  organizationTeamId: ID
  organizationId: ID
}

"""
One dashboard: a named arrangement of widgets, owned by the person who created it.
"""
type Dashboard {
  id: ID!

  """The dashboard's display name."""
  name: String!

  """
  The dashboard's identifier, unique within the organization. It is what the layout and the personal
  preferences are keyed by.
  """
  identifier: String!

  """One line describing what the dashboard is for."""
  description: String

  """
  The serialised layout: which widgets the dashboard shows, where they sit and what each is
  configured with. Either document version the builder has shipped, carried through unchanged.
  """
  contentHtml: JSON

  """
  Whether this is the employee's default dashboard.
  
  At most one dashboard of one person is the default: the delivered update demotes the others in the
  same step it promotes this one.
  """
  isDefault: Boolean!

  """The employee the dashboard was built for."""
  employeeId: ID
  tenantId: ID
  organizationId: ID

  """
  The user who created the dashboard. Only that user may change or remove it, which is what the
  delivered service refuses everyone else with.
  """
  createdByUserId: ID

  """
  Whether the row is active. A removal sets `deletedAt`; it does not clear this flag.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime

  """
  When the dashboard was withdrawn, or null while it is live. The two lifecycle routes write this alone.
  """
  deletedAt: DateTime
}

"""The boundary of one page of dashboards."""
type DashboardEdge {
  node: Dashboard!
  cursor: String!
}

"""
One page of dashboards, with the total the filters select and the boundary cursors.
"""
type DashboardConnection {
  nodes: [Dashboard!]!
  edges: [DashboardEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a dashboard list may be sorted by."""
enum DashboardSortField {
  createdAt
  updatedAt
  name
  identifier
  isDefault
}

"""
The order the dashboards are returned in. At most three keys are accepted.
"""
input DashboardSort {
  field: DashboardSortField!
  direction: SortDirection!
}

"""
The fields a dashboard list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`contentHtml` is filterable as a document, which is what the `JSON` kind's `contains` operator is for:
a client asking which dashboards place a given widget asks it of the layout rather than of a column
the dashboard does not have.
"""
input DashboardFilter {
  id: IDFilter
  name: StringFilter
  identifier: StringFilter
  description: StringFilter
  contentHtml: JSONFilter
  isDefault: BooleanFilter
  employeeId: IDFilter
  createdByUserId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [DashboardFilter!]
  or: [DashboardFilter!]
  not: DashboardFilter
}

"""
What a caller states when it creates a dashboard.

The members are the ones the delivered create DTO declares, and two absences are deliberate.
`isDefault` is excluded because the DTO excludes it — creation never promotes a dashboard — and it is
the update that re-allows it. The tenant is excluded because the delivered service stamps the caller's
own tenant onto the row; stating one here would promise a scope the write does not honour.
"""
input CreateDashboardInput {
  """The dashboard's display name."""
  name: String!

  """The identifier the layout and the preferences are keyed by."""
  identifier: String!
  description: String

  """
  The serialised layout document, for a dashboard created with its arrangement already known.
  """
  contentHtml: JSON

  """The employee the dashboard is built for."""
  employeeId: ID

  """The organization to file the dashboard under."""
  organizationId: ID
}

"""
What a caller states when it changes a dashboard that exists.

The identifier is the path's, so it is stated once and only here. Every other member is optional, and
**a member the caller leaves out is left as it is**: the delivered edit is the create's own service
call carrying the identifier, which updates the columns the body names rather than replacing the row.
"""
input UpdateDashboardInput {
  id: ID!
  name: String
  identifier: String
  description: String
  contentHtml: JSON

  """
  Whether this becomes the employee's default dashboard. Promoting one demotes the person's other
  defaults in the same step, so at most one is ever default.
  """
  isDefault: Boolean
  employeeId: ID
  organizationId: ID
}

"""
One opportunity being pursued: its value, the stage it sits at, and the party it is for.
"""
type Deal {
  id: ID!

  """The working title of the deal, as entered."""
  title: String!

  """
  How likely the deal is to close, as an integer on the deal's own five-point scale: `0` is the
  weakest and `5` the strongest. It is a bucketed judgement, not a percentage — the delivered
  column is an `int` and the delivered validator bounds it to that scale.
  """
  probability: Int!

  """
  The stage of a pipeline the deal currently sits at. The stage row owns the pipeline it belongs to.
  """
  stageId: ID!

  """The party the deal is for. Null on a deal that is not attached to one."""
  clientId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the deal was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of deals."""
type DealEdge {
  node: Deal!
  cursor: String!
}

"""
One page of deals, with the total the filters select and the boundary cursors.
"""
type DealConnection {
  nodes: [Deal!]!
  edges: [DealEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a deal list may be sorted by."""
enum DealSortField {
  createdAt
  updatedAt
  title
  probability
  stageId
  clientId
}

"""The order the deals are returned in. At most three keys are accepted."""
input DealSort {
  field: DealSortField!
  direction: SortDirection!
}

"""
The fields a deal list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned rather than translating the filter into a query: a member the read does
not select would be a filter that silently selects nothing. `deletedAt` is filterable so a caller can
ask the one question the list read cannot — which rows have been withdrawn — while the read itself
answers live rows only, exactly as the delivered route does.
"""
input DealFilter {
  id: IDFilter
  organizationId: IDFilter
  title: StringFilter
  probability: NumberFilter
  stageId: IDFilter
  clientId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [DealFilter!]
  or: [DealFilter!]
  not: DealFilter
}

"""
What a caller states when it files a deal.

The organization is required because the delivered create validates it: `POST /deals` binds a DTO
that intersects the organization base, and that base refuses a body naming neither an organization nor
a sent-to destination. There is no member for the tenant, because the service stamps the caller's own
tenant onto the row and overwrites whatever a body states.

The party and the stage are stated as identifiers. The delivered body accepts the whole relation
object as well, but the row is what holds the foreign key: an identifier is what the write actually
persists, and a client that has the stage or the party in hand already has its id.
"""
input CreateDealInput {
  organizationId: ID!
  title: String!

  """The deal's probability on its own five-point scale, 0 to 5."""
  probability: Int!

  """The stage of a pipeline to file the deal at."""
  stageId: ID!
  clientId: ID
  isActive: Boolean
  isArchived: Boolean
}

"""
What a caller states when it changes a deal that exists.

**A member the caller leaves out is left as it is.** The delivered edit is the CRUD base's partial
column update — it hands the store the members the body carries and writes them — so the identifier
is the only required member and the rest are stated only when they change. That is the opposite of
the catalogue's own category write, which replaces the row, and the difference is the delivered
service's rather than this surface's.
"""
input UpdateDealInput {
  id: ID!
  organizationId: ID
  title: String
  probability: Int
  stageId: ID
  clientId: ID
  isActive: Boolean
  isArchived: Boolean
}

"""Whether an account with the given address exists on this installation."""
type EmailCheckResult {
  """True when at least one account carries the address that was checked."""
  exists: Boolean!
}

"""
One message the platform sent, or tried to send: its recipient, its subject, its rendered body and what
became of it.
"""
type EmailHistory {
  id: ID!

  """The subject the message carried, as the delivered send wrote it."""
  name: String

  """The rendered body of the message, as it was sent."""
  content: String

  """The address the message was sent to."""
  email: String!

  """
  What became of the message: `SENT` when the transport accepted it, `FAILED` when it did not. The
  resend route writes this column, which is what makes it a state a caller reads rather than a
  by-product.
  """
  status: String

  """
  The user the message was sent to, when it was addressed to one. A column, not the joined relation.
  """
  userId: ID

  """
  The message template the body was rendered from, when it was rendered from one.
  """
  emailTemplateId: ID

  """
  The organization the message belongs to, or null for one sent outside any organization.
  """
  organizationId: ID

  """The tenant the message belongs to."""
  tenantId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of sent messages."""
type EmailHistoryEdge {
  node: EmailHistory!
  cursor: String!
}

"""
One page of sent messages, with the total the filters select and the boundary cursors.
"""
type EmailHistoryConnection {
  nodes: [EmailHistory!]!
  edges: [EmailHistoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a message list may be sorted by."""
enum EmailHistorySortField {
  createdAt
  updatedAt
  name
  email
  status
}

"""
The order the message list is returned in. At most three keys are accepted.
"""
input EmailHistorySort {
  field: EmailHistorySortField!
  direction: SortDirection!
}

"""
The fields a message list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

`status` is a `StringFilter` because the vocabulary belongs to the platform's own enum rather than to
this file: `eq: "FAILED"` is how an operator asks for what did not go out, and `isNull: true` selects the
rows a send recorded before any outcome was known.

The two relations are not filterable — neither is a member — and `content` is not filterable either: a
condition on a rendered body would scan the whole of every message for a pattern, which is a question
the delivered read never asks.
"""
input EmailHistoryFilter {
  id: IDFilter
  name: StringFilter
  email: StringFilter
  status: StringFilter
  userId: IDFilter
  emailTemplateId: IDFilter
  organizationId: IDFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmailHistoryFilter!]
  or: [EmailHistoryFilter!]
  not: EmailHistoryFilter
}

"""
What a caller states when it changes a sent message.

One member, and it is the whole of what the delivered body declares: a message that has been sent is a
record, and the only fact about it the API lets a caller restate is whether it is still active. The
delivered route carries the identifier in its path, so it is not repeated here as a body member — and the
organization the row belongs to is not a member either, because the delivered update reaches the row by
its identifier under the caller's own scope.
"""
input UpdateEmailHistoryInput {
  id: ID!

  """
  Whether the message is still active. Withdrawing a sent message marks it rather than removing it.
  """
  isArchived: Boolean
}

"""
What a caller states when it resends a sent message.

The delivered body is the address the message was filed under, and the delivered handler reads one member
out of it: the organization to resolve the transport for. The tenant is never stated — the resend reads
it from the credential, and the delivered implementation states why — and the recipient is not stated
either, because the message that is resent is the row the caller named, addressed to the address that row
carries.
"""
input ResendEmailHistoryInput {
  """The organization whose transport the message is sent through."""
  organizationId: ID
}

"""
What the request and the verification both answer.

The two members are always the same pair, and that is the design rather than an accident: the delivered
handlers answer this shape in a `finally`, so a caller is told the request was received and is told
nothing about what was done with it. A caller that could tell "the code was right" from "the code was
wrong" by the answer alone could use this endpoint to probe which addresses exist and which codes are
live, which is exactly what the shape exists to prevent.
"""
type EmailResetOutcome {
  """
  The status the delivered handler states in the body: `200` on both routes.
  """
  status: Int!

  """
  The message the delivered handler states in the body: `OK` on both routes.
  """
  message: String!
}

"""
What a caller states when it asks for the address on its account to be changed.

The subject of the request is not a member. Both delivered routes act on the caller — the user is read
from the credential, never from the body — so an address is the only thing there is to state, and a
member naming the account would promise a scope the write does not honour. The language the
acknowledgement and the mailed code are written in is the request's own language rather than a member,
exactly as the delivered route reads it from the `language` header.
"""
input RequestEmailResetInput {
  """
  The address the account is to be changed to. The code is mailed to this address.
  """
  email: String!
}

"""
What a caller states when it presents the code mailed to the new address.

The code is the only member for the same reason: the delivered read matches it against the caller's own
address and identifier, so naming either of those would be naming something the write already knows.
"""
input VerifyEmailResetInput {
  """The code mailed to the address the change was requested for."""
  code: String!
}

"""One part of one message, in one language."""
type EmailTemplate {
  id: ID!

  """
  The address of the part: `<template>/<type>`, for example `password/html` or `password/subject`.
  The surname is not decoration — the two parts of one message are two rows, and this member is how a
  reader tells them apart.
  """
  name: String!

  """The language this copy is written in."""
  languageCode: String!

  """
  The label the delivered surface derives from `name` for display. It is nullable because it is not a
  column: the entity produces it from `name` on load, and a row read through a path that does not run
  that hook carries none.
  """
  title: String

  """The compiled part: the text of a subject, or the HTML of a body."""
  hbs: String!

  """
  The MJML source `hbs` was compiled from, or null on a part filed without one.
  """
  mjml: String

  """
  The tenant this copy belongs to, or null on a copy that belongs to no tenant at all.
  """
  tenantId: ID

  """
  The organization this copy belongs to, or null when it is the tenant-wide copy: the platform
  answers an organization-less row to every organization of the tenant, and the copies seeded from the
  platform's own template folder carry no organization and no tenant either. A caller states one with
  `organizationId: { isNull: true }` in the filter below.
  """
  organizationId: ID

  """
  When the copy was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live copy from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of templates."""
type EmailTemplateEdge {
  node: EmailTemplate!
  cursor: String!
}

"""
One page of templates, with the total the filters select and the boundary cursors.
"""
type EmailTemplateConnection {
  nodes: [EmailTemplate!]!
  edges: [EmailTemplateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a template list may be sorted by."""
enum EmailTemplateSortField {
  createdAt
  updatedAt
  name
  languageCode
}

"""
The order the templates are returned in. At most three keys are accepted.
"""
input EmailTemplateSort {
  field: EmailTemplateSortField!
  direction: SortDirection!
}

"""
The fields a template list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

**`organizationId: { isNull: true }` is how a caller states the tenant-wide copy.** Such a row is the
copy the platform answers to every organization of its tenant, and a filter that could not say so
would leave the row that decides what most messages actually say unstateable — which is why the member
is an `IDFilter` rather than an `ID`.

`tenantId` is filterable for the same reason in the other direction. The copies seeded from the
platform's own template folder carry no tenant either, so `tenantId: { isNull: true }` separates the
copy that belongs to no tenant at all from an organization-less copy a tenant filed for itself, and a
caller that means one of the two no longer has to read both and guess. Neither key reaches outside the
set the reader answered: the tenant a read runs under comes from the credential, never from a filter,
so these two members narrow that set rather than widening it.

`deletedAt` is deliberately absent: the delivered list read answers live rows only, so the column is
absent on every row this connection holds and a condition on it could only ever be a condition no row
matches. The member is carried on the object type because the two lifecycle writes answer the row they
changed.
"""
input EmailTemplateFilter {
  id: IDFilter
  name: StringFilter
  languageCode: StringFilter
  tenantId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmailTemplateFilter!]
  or: [EmailTemplateFilter!]
  not: EmailTemplateFilter
}

"""
One message part as the delivered lookup answers it: the subject text and the body source that will be
used for a name, in a language, for an organization.

The two members are the two parts of one message, resolved separately and each through its own
fallback: the organization's own copy of that part when it has one, then the tenant-wide copy, then the
English copy of either. They are nullable because a part the fallback chain could not reach any row for
is answered as no text at all rather than as a fabricated empty one — the difference between "this
message has no body" and "this message has an empty body" is one a sender has to be able to see.
"""
type EmailTemplateContent {
  """The text of the `<name>/subject` part of the message."""
  subject: String

  """
  The MJML source of the `<name>/html` part — the source the platform compiles the HTML body from,
  not the compiled HTML itself. It is null on a copy that was filed from a source carrying only the
  compiled body.
  """
  template: String
}

"""
A document converted for preview, as the delivered preview route answers it.
"""
type EmailTemplatePreview {
  """
  The document with its markup compiled and its placeholders rendered with sample values.
  """
  html: String!
}

"""
What a caller states when it files a template part.

The tenant is not a member: the tenant a row is written into comes from the credential on every write
on this platform, and a member naming one would promise a scope the write does not honour.
`organizationId` is a member and is nullable, because its absence is a statement rather than an
omission — an absent organization files the tenant-wide copy the whole tenant falls back to.
"""
input CreateEmailTemplateInput {
  """The address of the part: `<template>/<type>`."""
  name: String!

  """The language this copy is written in."""
  languageCode: String!

  """
  The compiled part. The delivered create stores what it is given and compiles nothing, so a caller
  holding only MJML source wants `saveEmailTemplate` rather than this field.
  """
  hbs: String!

  """The MJML source, when the caller holds one."""
  mjml: String

  """
  The organization the copy is filed for. Omit it to file the tenant-wide copy.
  """
  organizationId: ID
}

"""
What a caller states when it edits a template part that exists.

**A member that is absent is left as it is, and a member stated as null is written as null.** The
delivered edit is a partial column update — the service writes the members it is given and touches no
other column — so a body that omits `mjml` keeps the source that is there. GraphQL keeps the two apart
for exactly this reason: omitting `organizationId` leaves the copy where it is, while stating
`organizationId: null` moves it to the tenant-wide copy every organization of the tenant is answered
with, which is a different request from saying nothing about it.
"""
input UpdateEmailTemplateInput {
  id: ID!
  name: String
  languageCode: String
  hbs: String
  mjml: String
  organizationId: ID
}

"""
What a caller states when it saves a part of a message through the delivered save route.

`subject` and `mjml` are the two halves that route compiles: `subject` is stored as the part's compiled
text and `mjml` as its source, with the compiled body derived from the source. That is the difference
between this write and `createEmailTemplate` above, which stores what it is given.
"""
input SaveEmailTemplateInput {
  """
  The template the part belongs to, without the `/subject` or `/html` surname.
  """
  name: String!
  languageCode: String!

  """The MJML source of the body. The compiled HTML is derived from it."""
  mjml: String!

  """The text of the subject part."""
  subject: String!

  """
  The organization the parts are saved for. Omit it to write the tenant-wide copies.
  """
  organizationId: ID
}

"""One booked slot on an employee's calendar."""
type EmployeeAppointment {
  id: ID!

  """
  What the slot is for. Required: an appointment with no agenda is not a booking.
  """
  agenda: String!

  """Free text about the slot, for the people the invitation reaches."""
  description: String

  """
  Where the meeting happens, as free text — a room, an address or a link.
  """
  location: String

  """When the slot begins."""
  startDateTime: DateTime!

  """When the slot ends."""
  endDateTime: DateTime!

  """Whether the calendar reserves a buffer before the slot."""
  bufferTimeStart: Boolean

  """Whether the calendar reserves a buffer after the slot."""
  bufferTimeEnd: Boolean

  """The length of each buffer, in minutes."""
  bufferTimeInMins: Int

  """The length of the break the slot contains, in minutes."""
  breakTimeInMins: Int

  """When the break begins."""
  breakStartTime: DateTime

  """
  The addresses an invitation is sent to, as one comma-separated text column. The delivered handler
  splits it on `, ` when it sends, so a caller that writes it states the same text the route's own body
  states.
  """
  emails: String

  """
  Where the slot stands in its lifecycle, as the contracts package's own value: `Scheduled`,
  `Cancelled` or `Completed`. The member is text, and the value set deliberately stays in the package
  that owns it — see the note above.
  """
  status: String

  """
  The employee the slot is filed for. The relation is not joined by either read this surface mirrors —
  the list read names only the relations its own caller named, and this surface names none — so the
  member is the identifier that always travels; the row behind it is read from the employees resource.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the appointment was withdrawn, or null while it is live. Withdrawing and restoring are
  delivered routes — the two lifecycle removals this resource serves — and this column is the whole of
  what they write, so without it a caller could not tell a live row from a withdrawn one on the answer
  to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of appointments."""
type EmployeeAppointmentEdge {
  node: EmployeeAppointment!
  cursor: String!
}

"""
One page of appointments, with the total the filters select and the boundary cursors.
"""
type EmployeeAppointmentConnection {
  nodes: [EmployeeAppointment!]!
  edges: [EmployeeAppointmentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an appointment list may be sorted by."""
enum EmployeeAppointmentSortField {
  createdAt
  updatedAt
  startDateTime
  endDateTime
  status
  agenda
}

"""
The order the appointments are returned in. At most three keys are accepted.
"""
input EmployeeAppointmentSort {
  field: EmployeeAppointmentSortField!
  direction: SortDirection!
}

"""
The fields an appointment list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows the
rows the service returned: a member the read does not select would be a filter that silently selects
nothing. `startDateTime` and `endDateTime` are the pair a calendar is read by, and `employeeId` is the
narrowing that turns the tenant's whole calendar into one employee's.

`tenantId` is absent because the tenant a read runs under comes from the credential and never from a
filter. The four bookkeeping members — `isActive`, `isArchived`, `archivedAt` and `deletedAt` — are
absent as well: the delivered read already answers live rows through the soft-delete column, so a filter
on one of the markers would be a second, weaker way to ask the question the read has answered.
"""
input EmployeeAppointmentFilter {
  id: IDFilter
  employeeId: IDFilter
  organizationId: IDFilter
  agenda: StringFilter
  description: StringFilter
  location: StringFilter
  emails: StringFilter
  status: StringFilter
  startDateTime: DateTimeFilter
  endDateTime: DateTimeFilter
  breakStartTime: DateTimeFilter
  bufferTimeStart: BooleanFilter
  bufferTimeEnd: BooleanFilter
  bufferTimeInMins: NumberFilter
  breakTimeInMins: NumberFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeAppointmentFilter!]
  or: [EmployeeAppointmentFilter!]
  not: EmployeeAppointmentFilter
}

"""
What a caller states when it files an appointment.

The organization is required, because the delivered create resolves it by identifier before the write
and there is nothing to file the row under without it. The tenant is not a member: the handler stamps
the caller's own tenant from the credential and never reads one from the body.

The invitation list is not a member either. The delivered create does not read it — the handler builds
the appointment from the named columns and returns, so a body stating invitees would be accepted and
then dropped — and an invitee is a row of its own resource, filed through `createAppointmentEmployee`
with the appointment's identifier and read back as `appointmentEmployees`. An input that silently
discarded a member would be worse than one that does not offer it.
"""
input CreateEmployeeAppointmentInput {
  """The organization the appointment is filed under."""
  organizationId: ID!
  agenda: String!
  employeeId: ID
  description: String
  location: String
  startDateTime: DateTime!
  endDateTime: DateTime!
  bufferTimeStart: Boolean
  bufferTimeEnd: Boolean
  bufferTimeInMins: Int
  breakTimeInMins: Int
  breakStartTime: DateTime

  """
  The addresses to invite, as the comma-separated text column the delivered handler splits.
  """
  emails: String
}

"""
What a caller states when it changes an appointment that exists.

**A member the caller leaves out is left as it is.** The delivered handler builds the write from the
members that are present and skips the ones that are not, so an absent member is not a column default —
which is why every member but the criterion is optional here.

`emails` is deliberately not among them. The delivered handler does read `data['emails']` off the body
it is handed, but the request type the route publishes for this operation does not declare the member:
it is part of the filing above, and the update's own vocabulary is the lifecycle `status`. Stating it
here would publish a member the route's own contract withholds, and the address list of an appointment
is written where it is sent from.

`invitees` is absent for the reason the filing states: the update does not read it either, and an
invitation is filed through its own resource's write.
"""
input UpdateEmployeeAppointmentInput {
  id: ID!
  organizationId: ID
  agenda: String
  employeeId: ID
  description: String
  location: String
  startDateTime: DateTime
  endDateTime: DateTime
  bufferTimeStart: Boolean
  bufferTimeEnd: Boolean
  bufferTimeInMins: Int
  breakTimeInMins: Int
  breakStartTime: DateTime
  status: String
}

"""One recurring window of an employee's availability."""
type EmployeeAvailability {
  id: ID!

  """The instant the arrangement takes effect."""
  startDate: DateTime!

  """The instant the arrangement stops holding."""
  endDate: DateTime!

  """
  The day of the week the window belongs to, as the delivered vocabulary states it: `0` is Sunday and
  `6` is Saturday.
  """
  dayOfWeek: Int!

  """
  The employee's state for this window, as `AvailabilityStatusEnum` states it — `Available`, `Partial`
  or `Unavailable`. The stored column is an integer and the label is what the mapping in
  `employee-availability-status.pipe.ts` produces; the vocabulary is the contracts package's and is
  deliberately not redeclared as a schema enum here.
  """
  availabilityStatus: String!

  """
  Free-form notes about the window, for example why a partial day is partial.
  """
  availabilityNotes: String

  """
  The employee the window belongs to. Read the employee itself from its own surface.
  """
  employeeId: ID!
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """When the row was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of availability rows."""
type EmployeeAvailabilityEdge {
  node: EmployeeAvailability!
  cursor: String!
}

"""
One page of availability rows, with the total the filters select and the boundary cursors.
"""
type EmployeeAvailabilityConnection {
  nodes: [EmployeeAvailability!]!
  edges: [EmployeeAvailabilityEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an availability list may be sorted by."""
enum EmployeeAvailabilitySortField {
  createdAt
  updatedAt
  startDate
  endDate
  dayOfWeek
  availabilityStatus
}

"""
The order the availability rows are returned in. At most three keys are accepted.
"""
input EmployeeAvailabilitySort {
  field: EmployeeAvailabilitySortField!
  direction: SortDirection!
}

"""
The fields an availability list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

`dayOfWeek` compares as a whole number, so a range over it is a range over the week as the delivered
vocabulary numbers it. The two window bounds compare as instants, which is what makes "whose
arrangement covers next week" a question this input can express. `availabilityStatus` compares as
text, because the label is the value the row carries by the time the connection sees it.
"""
input EmployeeAvailabilityFilter {
  id: IDFilter
  startDate: DateTimeFilter
  endDate: DateTimeFilter
  dayOfWeek: NumberFilter
  availabilityStatus: StringFilter
  availabilityNotes: StringFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeAvailabilityFilter!]
  or: [EmployeeAvailabilityFilter!]
  not: EmployeeAvailabilityFilter
}

"""
What a caller states when it files an availability window.

The three members that make the window are required, because the row is meaningless without them: a day
of the week, and the two instants the arrangement holds between. `availabilityStatus` is required for the
same reason — a row whose state is unstated says nothing about whether the employee can be booked — and
it is stated as the vocabulary's label.

`employeeId` is required because the entity's relation identifier is not nullable: an availability row
that belongs to nobody is not a row this resource has. It is stated by the caller and then resolved
against the credential by the delivered service — an actor that is itself an employee and does not hold
the permission that lets it select one writes its own row, whatever the body said — so the member names
the employee whose availability is being filed for a caller that may choose, and is answered by the
credential for one that may not.

`organizationId` is optional, and that is the delivered body's own statement rather than a relaxation:
the DTO validates the organization against the caller's own memberships whichever way it is named, and
accepts a body that names the organization by identifier or as an embedded object. The identifier is the
member this surface carries because it is the one that always travels and the one the write stores;
the tenant is never a member, because the handler stamps it from the credential.
"""
input CreateEmployeeAvailabilityInput {
  """The day of the week, `0` (Sunday) through `6` (Saturday)."""
  dayOfWeek: Int!

  """The instant the arrangement takes effect."""
  startDate: DateTime!

  """The instant the arrangement stops holding."""
  endDate: DateTime!

  """
  The state for this window, as `AvailabilityStatusEnum` states it: `Available`, `Partial` or
  `Unavailable`.
  """
  availabilityStatus: String!

  """The employee the window belongs to."""
  employeeId: ID!
  availabilityNotes: String
  organizationId: ID
}

"""
What a caller states when it changes an availability row that exists.

Every member but the identifier is optional, because the delivered DTO is the create DTO with all of
its members made optional — and the optionality is a fact about the write rather than a convenience:
**a member that is absent is left as it is.** The delivered update reads the row under the caller's own
tenant and then states the columns the caller gave, so a body that omits `endDate` keeps the row's
existing end date. Stating this here is what keeps a client from reading the create and the update as
one operation.
"""
input UpdateEmployeeAvailabilityInput {
  id: ID!
  dayOfWeek: Int
  startDate: DateTime
  endDate: DateTime
  availabilityStatus: String
  employeeId: ID
  availabilityNotes: String
  organizationId: ID
}

"""One recognition recorded against an employee."""
type EmployeeAward {
  id: ID!

  """The award's title, as the organization wrote it."""
  name: String!

  """
  The period the award belongs to, as the organization wrote it. Text rather than a number: the column
  is text and holds whatever was stated, a financial year as readily as a calendar one.
  """
  year: String!

  """
  The employee the award is filed on, by identifier. The employee row is not joined by this surface's
  reads; the person behind the identifier is read from `employee(id: ID!)`.
  """
  employeeId: ID

  """The organization the award was filed under, by identifier."""
  organizationId: ID

  """The tenant the award belongs to, by identifier."""
  tenantId: ID

  """
  When the award was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the award was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the award was filed."""
  createdAt: DateTime

  """When the award was last changed."""
  updatedAt: DateTime

  """
  Whether the award is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the award is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the award, by identifier. The account behind it is not joined by this read.
  """
  createdByUserId: ID

  """
  The person who last changed the award, by identifier. The account behind it is not joined here.
  """
  updatedByUserId: ID

  """
  The person who withdrew the award, by identifier. The account behind it is not joined here.
  """
  deletedByUserId: ID
}

"""The boundary of one page of awards."""
type EmployeeAwardEdge {
  node: EmployeeAward!
  cursor: String!
}

"""
One page of awards, with the total the filters select and the boundary cursors.
"""
type EmployeeAwardConnection {
  nodes: [EmployeeAward!]!
  edges: [EmployeeAwardEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an award list may be sorted by."""
enum EmployeeAwardSortField {
  id
  name
  year
  employeeId
  tenantId
  organizationId
  isActive
  isArchived
  createdAt
  updatedAt
  deletedAt
}

"""The order the awards are returned in. At most three keys are accepted."""
input EmployeeAwardSort {
  field: EmployeeAwardSortField!
  direction: SortDirection!
}

"""
The fields an award list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the award row, because the delivered list read joins nothing: a reader
answers these columns themselves, so each one narrows the rows the connection was handed rather than a
collection that reader would have had to load. `employeeId` is the one that matters most — it is the
column the row carries in place of the relation it does not join, so narrowing an award list to one
person is a question this surface can answer.

No member narrows by the relation itself: the tenant comes from the credential on every read and the
organization is the one the caller is acting in, so the two identifier members above narrow the row's
own columns rather than restating a scope decision the caller does not have.
"""
input EmployeeAwardFilter {
  id: IDFilter
  name: StringFilter
  year: StringFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [EmployeeAwardFilter!]
  or: [EmployeeAwardFilter!]
  not: EmployeeAwardFilter
}

"""
What a caller states when it files an award.

The organization and the employee are stated because the delivered create derives neither: the tenant
is stamped from the credential, never from an argument, but the organization and the employee are the
two members the delivered body declares and validates against the caller's own memberships — and there
is no member for the tenant, for the same reason there is none on any other write here.

Both of them are required, which is the delivered body's own shape rather than a choice made here: the
create DTO validates the organization as one of the caller's and the employee as one of that
organization's, and a body omitting either is refused before the write.
"""
input CreateEmployeeAwardInput {
  """
  The organization the award is filed under. It must be one of the caller's own.
  """
  organizationId: ID!

  """
  The employee the award belongs to. It must be one of that organization's.
  """
  employeeId: ID!

  """The award's title."""
  name: String!

  """The period the award belongs to, as the caller writes it."""
  year: String!
}

"""
What a caller states when it changes an award that exists.

The identifier is the one member stated outside the body, and it is stated because the delivered route
carries it in the path. The delivered edit is a partial column update rather than a replacement of the
row, so a member the caller leaves out is left as it is.

**The employee is not a member.** The delivered update body carries no employee, and the route's
validation pipe strips what its DTO does not declare, so an award's employee is fixed when it is filed
— moving one to another person is a removal and a filing, not an edit.
"""
input UpdateEmployeeAwardInput {
  id: ID!

  """The organization the award is filed under."""
  organizationId: ID!

  """The award's title."""
  name: String!

  """The period the award belongs to, as the caller writes it."""
  year: String!
}

"""
One label in the vocabulary an organization offers for employee seniority.
"""
type EmployeeLevel {
  id: ID!

  """The label the level is known by, as the organization wrote it."""
  level: String!

  """
  The organization whose vocabulary this level belongs to, by identifier.
  """
  organizationId: ID

  """The tenant the level belongs to, by identifier."""
  tenantId: ID

  """
  When the level was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the level was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the level was filed."""
  createdAt: DateTime

  """When the level was last changed."""
  updatedAt: DateTime

  """
  Whether the level is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the level is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the level, by identifier. The account behind it is not joined by this read.
  """
  createdByUserId: ID

  """
  The person who last changed the level, by identifier. The account behind it is not joined here.
  """
  updatedByUserId: ID

  """
  The person who withdrew the level, by identifier. The account behind it is not joined here.
  """
  deletedByUserId: ID
}

"""The boundary of one page of levels."""
type EmployeeLevelEdge {
  node: EmployeeLevel!
  cursor: String!
}

"""
One page of levels, with the total the filters select and the boundary cursors.
"""
type EmployeeLevelConnection {
  nodes: [EmployeeLevel!]!
  edges: [EmployeeLevelEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a level list may be sorted by."""
enum EmployeeLevelSortField {
  id
  level
  tenantId
  organizationId
  isActive
  isArchived
  createdAt
  updatedAt
  deletedAt
}

"""The order the levels are returned in. At most three keys are accepted."""
input EmployeeLevelSort {
  field: EmployeeLevelSortField!
  direction: SortDirection!
}

"""
The fields a level list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the level row, because the delivered list read answers the row itself and
joins only what its REST caller names: the pivot this row owns is not among the members, since no read
behind this connection loads it, and a condition on a collection that is never loaded could only ever
match the empty set.

No member narrows by the relation behind an identifier either. The tenant comes from the credential on
every read, so the two identifier members above narrow the row's own columns rather than restating a
scope decision the caller does not have.
"""
input EmployeeLevelFilter {
  id: IDFilter
  level: StringFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [EmployeeLevelFilter!]
  or: [EmployeeLevelFilter!]
  not: EmployeeLevelFilter
}

"""
What a caller states when it files a level.

The label is required because the column is: the delivered route performs no validation of its own, so
the one member a filing cannot succeed without is the one stated as required here, and the failure a
body omitting it would otherwise meet is the store's own. The organization is stated because the level
is one organization's vocabulary and the column is nullable — a level filed without one belongs to the
tenant's whole vocabulary, which is what the delivered write does with a body that omits it.

There is no member for the tenant: the delivered write stamps the caller's own tenant onto the row and
overwrites whatever a body states, so a member here would promise a scope the write refuses.
"""
input CreateEmployeeLevelInput {
  """The label the level is known by."""
  level: String!

  """
  The organization whose vocabulary the level belongs to. Omit for the whole tenant's.
  """
  organizationId: ID
}

"""
What a caller states when it changes a level that exists.

**A member the caller leaves out is left as it is.** The delivered route is an upsert rather than a
replacement: the service loads the row the identifier names and assigns the members the body states to
it, so omitting `level` keeps the label and omitting `organizationId` keeps the organization. The
identifier is the one required member, and it is stated because the delivered route carries it in the
path.
"""
input UpdateEmployeeLevelInput {
  id: ID!

  """The label the level is known by."""
  level: String

  """The organization whose vocabulary the level belongs to."""
  organizationId: ID
}

"""One employee's notification preferences."""
type EmployeeNotificationSetting {
  id: ID!

  """Whether payment-related notifications are sent to the employee."""
  payment: Boolean

  """Whether assignment-related notifications are sent to the employee."""
  assignment: Boolean

  """Whether invitation-related notifications are sent to the employee."""
  invitation: Boolean

  """Whether notifications for a mention of the employee are sent."""
  mention: Boolean

  """Whether notifications for a comment relevant to the employee are sent."""
  comment: Boolean

  """Whether direct messages are sent to the employee."""
  message: Boolean

  """
  Everything the six flags above do not cover, as a document the caller stores.
  """
  preferences: JSON

  """
  The employee these preferences belong to. The row behind it is read from the employee surface.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the preferences were withdrawn, or null while they are live."""
  archivedAt: DateTime

  """
  When the preferences were withdrawn, or null while they are live. Withdrawing and restoring are
  delivered routes whose whole effect is this column, so it is carried: without it the answer to the
  write that withdrew a row would not say so.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of notification settings."""
type EmployeeNotificationSettingEdge {
  node: EmployeeNotificationSetting!
  cursor: String!
}

"""
One page of notification settings, with the total the filters select and the boundary cursors.
"""
type EmployeeNotificationSettingConnection {
  nodes: [EmployeeNotificationSetting!]!
  edges: [EmployeeNotificationSettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a notification setting list may be sorted by.

The two timestamps, the employee and the withdrawal stamp. The six flags are deliberately not here:
each is a column with two values, so half the rows carry it and half do not, and an order over that
split is a question the filter beside it already answers per flag — which is why the flags are
filterable and not sortable rather than both.
"""
enum EmployeeNotificationSettingSortField {
  createdAt
  updatedAt
  employeeId
  deletedAt
}

"""
The order the notification settings are returned in. At most three keys are accepted.
"""
input EmployeeNotificationSettingSort {
  field: EmployeeNotificationSettingSortField!
  direction: SortDirection!
}

"""
The fields a notification setting list may be narrowed by. `and` is the default; `or` requires any of
its members and `not` excludes the rows that match it.

`preferences` is narrowed as a document, which is what the connection protocol's JSON kind is: a caller
asks whether a value is present inside the document rather than for the document as a whole, because
two callers never write byte-identical documents.
"""
input EmployeeNotificationSettingFilter {
  id: IDFilter
  payment: BooleanFilter
  assignment: BooleanFilter
  invitation: BooleanFilter
  mention: BooleanFilter
  comment: BooleanFilter
  message: BooleanFilter
  preferences: JSONFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeNotificationSettingFilter!]
  or: [EmployeeNotificationSettingFilter!]
  not: EmployeeNotificationSettingFilter
}

"""
What a caller states when it files notification preferences.

The members are the scalar ones the delivered `CreateEmployeeNotificationSettingDTO` validates — the
intersection of the tenant-and-organization base body with the entity's own columns, less the relation.
The tenant is not among them: the delivered write stamps the tenant of the credential onto the row and
never reads one from a body. The organization is among them, because the delivered base body carries it
and the write takes it from there; an organization the caller states is checked against the caller's own
memberships by that body's validation rather than trusted.

**There is no member for the employee, and its absence is the delivered body's own statement.** The
delivered `CreateEmployeeNotificationSettingDTO` omits both the relation and its identifier, and the
delivered service derives the employee from the credential when the body names none — so these
preferences belong to the caller acting as an employee, and no caller states whose they are. A member
here would name a person the delivered write never reads.

`preferences` is the one required member, because the entity declares that column as one a row must
carry. The six flags are optional: each column has a default, so a caller that states none files the
platform's own defaults for the kinds it did not mention, which is what the delivered route does with
the same body.
"""
input CreateEmployeeNotificationSettingInput {
  """Whether payment-related notifications are sent."""
  payment: Boolean

  """Whether assignment-related notifications are sent."""
  assignment: Boolean

  """Whether invitation-related notifications are sent."""
  invitation: Boolean

  """Whether notifications for a mention are sent."""
  mention: Boolean

  """Whether notifications for a relevant comment are sent."""
  comment: Boolean

  """Whether direct messages are sent."""
  message: Boolean

  """Everything the six flags do not cover."""
  preferences: JSON!

  """
  The organization to file the preferences under. The delivered base body carries it.
  """
  organizationId: ID
}

"""
What a caller states when it changes notification preferences that exist.

The delivered update body is the create body with every member optional, so this input carries the same
members — plus the identifier the route carries in its path, because this surface's mutation takes one
argument and the resolver passes it on both ways.

**A member that is absent is left as it is.** The delivered update reads the row by the identifier to
prove it is the caller's, then writes the members the body states over that row; a member the body does
not state is not part of that write, so the column keeps what the row has. A client that means to switch
one kind of notification off states that flag as `false` rather than leaving it out, and the employee
is not a member at all: preferences are read and written for the employee the credential names.
"""
input UpdateEmployeeNotificationSettingInput {
  """The notification setting to change."""
  id: ID!
  payment: Boolean
  assignment: Boolean
  invitation: Boolean
  mention: Boolean
  comment: Boolean
  message: Boolean
  preferences: JSON
  organizationId: ID
}

"""One in-app notification addressed to one employee."""
type EmployeeNotification {
  id: ID!

  """The notification's heading, as the sender wrote it."""
  title: String

  """The notification's body, as the sender wrote it."""
  message: String

  """
  The kind of notification, as the vocabulary's own value: `Payment`, `Assignment`, `Invitation`,
  `Mention`, `Comment`, `Message` or `Broadcast`. The value is the column's own, which is why it is
  carried as text rather than as an enum declared here.
  """
  type: String

  """Whether the receiver has read it."""
  isRead: Boolean

  """When the receiver read it, or null while it is unread."""
  readAt: DateTime

  """
  When the notification stops being held back — the snooze column. A time in the past means it is not
  being held at all.
  """
  onHoldUntil: DateTime

  """
  The employee who sent it. The relation is not joined; the sender is read from the employee surface.
  """
  sentByEmployeeId: ID

  """
  The employee who is to read it. The relation is not joined; the receiver is read from the employee surface.
  """
  receiverEmployeeId: ID

  """The kind of record the notification is about."""
  entity: String

  """The record the notification is about, by identifier."""
  entityId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """When the notification was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the notification was withdrawn, or null while it is live. Withdrawing and restoring are
  delivered routes — the two removals this resource serves — and this column is the whole of what they
  write, so without it a caller could not tell a live row from a withdrawn one on the answer to the
  write that withdrew it.
  """
  deletedAt: DateTime
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of notifications."""
type EmployeeNotificationEdge {
  node: EmployeeNotification!
  cursor: String!
}

"""
One page of notifications, with the total the filters select and the boundary cursors.
"""
type EmployeeNotificationConnection {
  nodes: [EmployeeNotification!]!
  edges: [EmployeeNotificationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a notification list may be sorted by."""
enum EmployeeNotificationSortField {
  createdAt
  updatedAt
  readAt
  onHoldUntil
  isRead
  title
  type
}

"""
The order the notifications are returned in. At most three keys are accepted.
"""
input EmployeeNotificationSort {
  field: EmployeeNotificationSortField!
  direction: SortDirection!
}

"""
The fields a notification list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

`type` is a text filter over the vocabulary's own value, for the reason the object type states: the
column reads back as that value, so a caller narrows an inbox to `Assignment` by naming it, exactly as
it would over the delivered query string.
"""
input EmployeeNotificationFilter {
  id: IDFilter
  title: StringFilter
  message: StringFilter
  type: StringFilter
  isRead: BooleanFilter
  readAt: DateTimeFilter
  onHoldUntil: DateTimeFilter
  sentByEmployeeId: IDFilter
  receiverEmployeeId: IDFilter
  entity: StringFilter
  entityId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeNotificationFilter!]
  or: [EmployeeNotificationFilter!]
  not: EmployeeNotificationFilter
}

"""
What a caller states when it files a notification.

There is no member for the tenant: the delivered create stamps the caller's own tenant from the
credential and overwrites whatever a body states, so an argument here would be one that is accepted and
then ignored. The organization is a member, because the delivered create reads it — it is half of the key
the receiver's notification settings are looked up under, and it decides whether the notification is
wanted at all.

**The read state is not a member either.** `isRead` and `readAt` are set by the platform when a
notification is read, never by the caller filing one: the delivered write takes the payload minus those
two columns, and a body that states them would be accepted and dropped. The delivered input type states
the same omission.
"""
input CreateEmployeeNotificationInput {
  """
  The notification's heading. A notification with none is one the platform generated without a title.
  """
  title: String

  """The notification's body."""
  message: String

  """
  The kind of notification, as the vocabulary's own value — `Payment`, `Assignment`, `Invitation`, `Mention`, `Comment`, `Message` or `Broadcast`.
  """
  type: String

  """
  When the notification should stop being held back, when the sender wants it snoozed.
  """
  onHoldUntil: DateTime

  """
  The employee sending it. Omitted for a notification the platform itself raises.
  """
  sentByEmployeeId: ID

  """The employee who is to read it."""
  receiverEmployeeId: ID

  """The kind of record the notification is about."""
  entity: String!

  """The record the notification is about, by identifier."""
  entityId: ID!

  """
  The organization the notification belongs to. The receiver's settings are looked up under it.
  """
  organizationId: ID!

  """
  The tenant to file it under. The delivered create overwrites this with the caller's own tenant.
  """
  tenantId: ID
}

"""
What a caller states when it edits a notification that exists.

**The sender and the receiver are not members.** The delivered create fixes both when the notification is
filed, and the delivered update writes columns of the row rather than re-addressing it: a notification
that could be re-pointed at another employee would be a different notification wearing the same
identifier, and the two relations being immutable after creation is the delivered input type's own
statement.

**A member that is absent is left as it is.** The delivered update is a partial column write — it reads
the row to prove it is the caller's and then updates the columns the caller stated — so an inbox client
can set the read state without restating the title, the message or the record the notification is about.
The two members the row is keyed by and the organization are the exception: they are stated because the
delivered input type requires them, and a caller sends the values the notification already carries.
"""
input UpdateEmployeeNotificationInput {
  id: ID!
  title: String
  message: String
  type: String

  """
  The read state, which this write does set: an inbox marks one notification read through it.
  """
  isRead: Boolean

  """When it was read. The platform sets both this and `isRead` together."""
  readAt: DateTime
  onHoldUntil: DateTime
  entity: String!
  entityId: ID!
  organizationId: ID!
  tenantId: ID
}

"""
What the mark-all write answers.

Both members are the delivered answer's own, and neither is a row: `success` says whether the write was
performed — a caller with no unread notifications, or none it could name, is answered a successful no-op
— and `count` says how many notifications it changed. There is deliberately no node member: the write
reports what it did rather than answering a row, and a resource projection here would be a second answer
to a question the inbox already answers.
"""
type EmployeeNotificationMarkAllAsReadResult {
  """Whether the write was performed."""
  success: Boolean!

  """How many notifications it changed."""
  count: Int!
}

"""One record an employee recently opened."""
type EmployeeRecentVisit {
  id: ID!

  """
  When the employee opened it. This is the order the delivered read answers in, most recent first.
  """
  visitedAt: DateTime!

  """The document the visit carries, as the row stores it."""
  data: JSON

  """The kind of record the employee opened."""
  entity: String

  """The record the employee opened, by identifier."""
  entityId: ID

  """
  The employee who opened it. The relation is not joined; the employee is read from the employee surface.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the row was withdrawn, or null while it is live. The delivered surface has no route that
  withdraws a visit, so this column is not written by anything this domain serves — it is carried
  because it is a column of the row, and a reader that could not see it could not tell a live row from
  one a migration or a neighbouring domain withdrew.
  """
  deletedAt: DateTime
}

"""The boundary of one page of recent visits."""
type EmployeeRecentVisitEdge {
  node: EmployeeRecentVisit!
  cursor: String!
}

"""
One page of recent visits, with the total the filters select and the boundary cursors.

**The total is the count of the rows the delivered read answered, not of every visit in the store.** That
read fixes its own page — a hundred rows unless the caller states otherwise — so the connection is applied
to exactly that set, and a total that counted beyond it would be a second, disagreeing count of the same
question. A caller that needs the whole history pages through it with the read's own parameters over REST
or narrows this connection by `visitedAt`.
"""
type EmployeeRecentVisitConnection {
  nodes: [EmployeeRecentVisit!]!
  edges: [EmployeeRecentVisitEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a recent-visit list may be sorted by."""
enum EmployeeRecentVisitSortField {
  visitedAt
  createdAt
  updatedAt
  entity
}

"""
The order the recent visits are returned in. At most three keys are accepted.

The order the caller states none in is the delivered read's own — `visitedAt` descending, then the
identifier — so a walk that states no sort resumes over the sequence the REST route answers.
"""
input EmployeeRecentVisitSort {
  field: EmployeeRecentVisitSortField!
  direction: SortDirection!
}

"""
The fields a recent-visit list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

The first six are the parameters the delivered route binds from its query string — `organizationId`,
`entity`, `entityId`, `employeeId`, `isActive` and `isArchived` — stated as filters so that each of them
narrows this one connection rather than claiming a root field of its own. `skip`, `take` and `relations`,
the rest of what that route binds, are not members: the first two are the page and the last is the join,
and both are what a connection answers with rather than what it narrows by.
"""
input EmployeeRecentVisitFilter {
  id: IDFilter
  visitedAt: DateTimeFilter

  """
  An operator over the document column, for example `contains` on a value the payload carries.
  """
  data: JSONFilter
  entity: StringFilter
  entityId: IDFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeRecentVisitFilter!]
  or: [EmployeeRecentVisitFilter!]
  not: EmployeeRecentVisitFilter
}

"""
One standing cost an employee carries from one month until an end that may be open.
"""
type EmployeeRecurringExpense {
  id: ID!

  """The day of the month the arrangement begins, from 1 to 31."""
  startDay: Int!

  """The month the arrangement begins, from 1 to 12."""
  startMonth: Int!

  """The four-digit year the arrangement begins in."""
  startYear: Int!

  """
  The instant the arrangement is dated to, which is the beginning above as a date.
  """
  startDate: DateTime!

  """The day of the month the arrangement ends; null while the end is open."""
  endDay: Int

  """The month the arrangement ends; null while the end is open."""
  endMonth: Int

  """The year the arrangement ends; null while the end is open."""
  endYear: Int

  """
  The instant the arrangement ends; null while the end is open, which is what makes the cost standing.
  """
  endDate: DateTime

  """
  The category the standing cost is filed under, in the tenant's own vocabulary.
  """
  categoryName: String!

  """The standing cost, in the row's own `currency`."""
  value: Decimal!

  """The ISO 4217 code `value` is expressed in."""
  currency: String!

  """
  The arrangement this row belongs to. The delivered creation sets it to the row's own identifier, so
  a standing arrangement is a family of rows whose later members are the ones an edit opened when it
  moved the beginning forward — which is what the verdict read below looks a conflict up among.
  """
  parentRecurringExpenseId: ID

  """
  The employee the standing cost is recorded against; null on an arrangement the whole organization carries.
  """
  employeeId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of standing costs."""
type EmployeeRecurringExpenseEdge {
  node: EmployeeRecurringExpense!
  cursor: String!
}

"""
One page of standing costs, with the total the filters select and the boundary cursors.
"""
type EmployeeRecurringExpenseConnection {
  nodes: [EmployeeRecurringExpense!]!
  edges: [EmployeeRecurringExpenseEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
What moving one arrangement's beginning would mean, and the arrangements in the way.

**This is a computed verdict rather than a row.** The delivered read compares the beginning the caller
proposes with the one the arrangement has, and when the change would move the beginning forward or
backward it reads the sibling arrangements of the same parent that fall inside the gap. So the answer
has two parts that belong together: what the change would be, and the rows that make it that.

`value` is the contracts' own verdict rather than a schema enum, because the vocabulary is shared with
the organization's own arrangements: `NO_CHANGE` when the two beginnings are the same instant,
`WITHIN_MONTH` when they fall in one month, `REDUCE_SAFE` and `REDUCE_CONFLICT` when the change moves the
beginning backward with and without a sibling in the way, `INCREASE_SAFE_WITHIN_LIMIT` and
`INCREASE_SAFE_OUTSIDE_LIMIT` when it moves forward without a conflict, and `INCREASE_CONFLICT` when a
sibling stands between the two. The delivered edit asks this same question of itself and refuses the
last of the seven outright.
"""
type EmployeeRecurringExpenseStartDateUpdateType {
  """What moving the beginning would be, as the contracts' own verdict."""
  value: String!

  """
  The sibling arrangements of the same parent that fall inside the gap the change would leave.
  """
  conflicts: [EmployeeRecurringExpense!]!
}

"""
The fields a standing-cost list may be sorted by.

`startDate` is the payroll's own order, which is the column the delivered month route narrows on; the
two timestamps are how a change is found again; `value` is what a list of standing costs is taken by;
and `categoryName` is offered because a payroll is read in category groups.
"""
enum EmployeeRecurringExpenseSortField {
  createdAt
  updatedAt
  startDate
  endDate
  value
  categoryName
}

"""
The order the standing costs are returned in. At most three keys are accepted.
"""
input EmployeeRecurringExpenseSort {
  field: EmployeeRecurringExpenseSortField!
  direction: SortDirection!
}

"""
The fields a standing-cost list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the row, and the four that describe the beginning and the end are what
makes the delivered month route a filter of this list rather than a read of its own:

```
filter: {
  or: [
    { startDate: { between: [$start, $end] }, endDate: { isNull: true } },
    { startDate: { lte: $end }, endDate: { gte: $start } }
  ]
}
```

That is the delivered criterion exactly — an arrangement that begins inside the range and has no end
yet, or one that begins no later than the range's start and ends no earlier than its end — stated in the
connection's own vocabulary, with the two bounds being the instants the delivered handler computes from
the range its route was given (one month's bounds, as the delivered clients send them). An employee's own
arrangements are the same disjunction with `employeeId: { eq: … }` beside it, and a family of rows is
`parentRecurringExpenseId: { eq: … }`.

The amount is stated through the decimal family, which is the family money is compared in. `deletedAt`
is deliberately absent although the object carries it: the delivered list read answers live rows only,
so a condition on that column could only ever match the empty set. The tenant is absent because it is
applied to the criterion from the credential rather than from the caller.
"""
input EmployeeRecurringExpenseFilter {
  id: IDFilter
  startDay: NumberFilter
  startMonth: NumberFilter
  startYear: NumberFilter
  startDate: DateTimeFilter
  endDay: NumberFilter
  endMonth: NumberFilter
  endYear: NumberFilter
  endDate: DateTimeFilter
  categoryName: StringFilter
  value: DecimalFilter
  currency: StringFilter
  parentRecurringExpenseId: IDFilter
  employeeId: IDFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeRecurringExpenseFilter!]
  or: [EmployeeRecurringExpenseFilter!]
  not: EmployeeRecurringExpenseFilter
}

"""
What a caller states when it records a standing cost.

`value`, `categoryName`, the two beginnings — the date and the day, month and year — and the currency
are the members the delivered body validation requires; a body without the currency is refused, which is
why the member is non-null here.

The employee is optional, and its absence is meaningful rather than incomplete: an arrangement the whole
organization carries has no employee at all. The delivered handler additionally stamps the caller's own
employee onto the row when the caller has no permission to choose one, so the member a caller states is
not always the member the row ends up with — which is the delivered creation's own scope rule and is not
narrowed here.
"""
input CreateEmployeeRecurringExpenseInput {
  """The standing cost, in the currency below."""
  value: Decimal!

  """The ISO 4217 code the value is expressed in."""
  currency: String!

  """The category the standing cost is filed under."""
  categoryName: String!

  """The day of the month the arrangement begins, from 1 to 31."""
  startDay: Int!

  """The month the arrangement begins, from 1 to 12."""
  startMonth: Int!

  """The four-digit year the arrangement begins in."""
  startYear: Int!

  """The instant the arrangement is dated to."""
  startDate: DateTime!

  """
  The day of the month the arrangement ends, where the caller states one.
  """
  endDay: Int

  """The month the arrangement ends, where the caller states one."""
  endMonth: Int

  """The year the arrangement ends, where the caller states one."""
  endYear: Int

  """
  The instant the arrangement ends, where the caller states one. Omit it and the cost is standing.
  """
  endDate: DateTime

  """
  The arrangement this row belongs to, where the caller states one. The delivered creation sets it to the row's own identifier.
  """
  parentRecurringExpenseId: ID

  """The organization the standing cost is filed under."""
  organizationId: ID

  """
  The employee the standing cost is recorded against. Omit it for an arrangement the whole organization carries.
  """
  employeeId: ID
}

"""
What a caller states when it changes a standing cost.

**These are the members the delivered edit reads, and nothing else.** Its body validates more — the end
of the arrangement, the parent, the currency — and the delivered handler writes none of them: the plain
edit writes the beginning and the value, and the branch that closes one arrangement and opens its
successor takes the currency and the parent from the row it replaces. An input member the write ignores
would be worse than no member at all, so they are not offered.

An absent `employeeId` is not "leave it as it is" in every case: the delivered handler reads an absent
member, an explicit `null` and an identifier as three different instructions — leave the assignment
alone, switch the arrangement to the whole organization, and assign it to the named employee — and a
caller without permission to choose an employee has their own stamped on instead. Stating that here
rather than narrowing it is the delivered edit's own rule.

The verdict member the delivered handler computes is not a member of this input: it asks the
`employeeRecurringExpenseStartDateUpdateType` field's question of itself and overwrites whatever a body
carried.
"""
input UpdateEmployeeRecurringExpenseInput {
  id: ID!

  """The day of the month the arrangement now begins, from 1 to 31."""
  startDay: Int!

  """The month the arrangement now begins, from 1 to 12."""
  startMonth: Int!

  """The four-digit year the arrangement now begins in."""
  startYear: Int!

  """The category the standing cost is filed under."""
  categoryName: String!

  """The standing cost."""
  value: Decimal!

  """
  The employee the standing cost is recorded against; `null` switches it to the whole organization.
  """
  employeeId: ID
}

"""
How much of a standing arrangement a removal takes out.

**This is this domain's own spelling of a shape the platform states once**, because the contracts'
`IRecurringExpenseDeleteInput` is what both standing-cost resources' removals read and a document
declares the concepts it serves rather than reaching into a neighbour's for one: the organization's own
arrangements declare the same three members under their own name.

`deletionType` is the contracts' own value: `all` removes every row of the arrangement, `future` moves
its end back to the month before the one named so that nothing later remains, and `current` takes the
named month out of the middle — closing the arrangement before it and opening its successor after it.
`month` and `year` name the month all three are measured from; `all` is the one that does not read them.
"""
input EmployeeRecurringExpenseDeleteInput {
  """Which of the three removals this is: `all`, `future` or `current`."""
  deletionType: String!

  """The month the removal is measured from, from 1 to 12."""
  month: Int!

  """The four-digit year the removal is measured from."""
  year: Int!
}

"""One employee's stored settings."""
type EmployeeSetting {
  id: ID!

  """
  What kind of setting this is, as the contracts package's own value: `Normal`, `Task-View` or
  `Custom`. Carried as its value rather than declared as a schema enum, because the value set belongs
  to the column that stores it.
  """
  settingType: String

  """
  The record this setting is about. Absent on a setting that is about no particular record.
  """
  entityId: ID

  """
  The kind of record `entityId` names, as the contracts package's own value. The column is a varchar
  holding that value, so it travels as text rather than as an enum declared here.
  """
  entity: String

  """The setting's document, as the caller stored it."""
  data: JSON

  """
  The document the platform falls back to when the employee has stated nothing.
  """
  defaultData: JSON

  """
  The employee this setting belongs to. The row behind it is read from the employee surface.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the setting was withdrawn, or null while it is live."""
  archivedAt: DateTime

  """
  When the setting was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes whose whole effect is this column, so it is carried: without it the answer to the write that
  withdrew a row would not say so.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of settings."""
type EmployeeSettingEdge {
  node: EmployeeSetting!
  cursor: String!
}

"""
One page of settings, with the total the filters select and the boundary cursors.
"""
type EmployeeSettingConnection {
  nodes: [EmployeeSetting!]!
  edges: [EmployeeSettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a setting list may be sorted by.

The two timestamps, the two vocabulary values and the relation key, and `deletedAt` beside them: the
withdrawal is a delivered operation, so a reviewer asking which of an employee's settings are
withdrawn has an order to ask it in. `data` and `defaultData` are deliberately absent — a document has
no order a caller could predict, which is what the filter beside them is for.
"""
enum EmployeeSettingSortField {
  createdAt
  updatedAt
  settingType
  entity
  employeeId
  deletedAt
}

"""
The order the settings are returned in. At most three keys are accepted.
"""
input EmployeeSettingSort {
  field: EmployeeSettingSortField!
  direction: SortDirection!
}

"""
The fields a setting list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`data` and `defaultData` are narrowed as documents, which is what the connection protocol's JSON kind
is: a caller asks whether a stored value is present inside the document rather than for the document
as a whole, because two callers never write byte-identical documents.
"""
input EmployeeSettingFilter {
  id: IDFilter
  settingType: StringFilter
  entityId: IDFilter
  entity: StringFilter
  data: JSONFilter
  defaultData: JSONFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeSettingFilter!]
  or: [EmployeeSettingFilter!]
  not: EmployeeSettingFilter
}

"""
What a caller states when it files a setting.

The members are the scalar ones the delivered `CreateEmployeeSettingDTO` validates — the intersection
of the tenant-and-organization base body with the entity's own columns. The tenant is not among them:
the delivered write stamps the tenant of the credential onto the row and never reads one from a body,
so a caller cannot file into a scope it is not acting in. The organization is among them, because the
delivered base body carries it and the write takes it from there; an organization the caller states is
checked against the caller's own memberships by that body's validation rather than trusted.

The employee is optional, because the delivered write prefers the employee the credential names and
falls back to the one the caller states — so a caller acting as an employee files its own setting by
stating nothing.

**A row already filed for the same employee, entity and organization is overwritten rather than added
to.** The delivered create looks the setting up by those three columns first and, when it finds one,
writes the stated body over that row under the identifier it already has — so a caller that files
twice for the same employee and record has one setting, not two, and the columns that tell two
settings apart are the ones it must state.

There is no member for the employee relation. The relation is not a column of this row, and this
schema declares no type for it; the employee behind the identifier is read from the employee surface.
"""
input CreateEmployeeSettingInput {
  """What kind of setting this is, as the contracts package's own value."""
  settingType: String

  """The record this setting is about."""
  entityId: ID

  """
  The kind of record `entityId` names, as the contracts package's own value.
  """
  entity: String

  """The setting's document."""
  data: JSON

  """
  The document the platform falls back to when the employee has stated nothing.
  """
  defaultData: JSON

  """
  The employee to file the setting for. Omit it and the delivered write uses the employee the
  credential names.
  """
  employeeId: ID

  """
  The organization to file the setting under. The delivered base body carries it.
  """
  organizationId: ID
}

"""
What a caller states when it changes a setting that exists.

The delivered `PUT /:id` body is the create body with the employee removed, so this input carries the
same members minus the employee and gains the identifier the route carries in its path: this surface's
mutation takes one argument, and the identifier travels in it — the resolver passes it on both ways,
exactly as the delivered route does with its path and its body.

**A member that is absent is left as it is.** The delivered update reads the row by the identifier to
prove it is the caller's, then writes the members the body states over that row; a member the body
does not state is not part of that write, so the column keeps what the row has. This is what keeps a
client from reading "absent" as "cleared" — and from reading it as "defaulted", which is what the
resources that replace the whole row would do with the same request.
"""
input UpdateEmployeeSettingInput {
  """The setting to change."""
  id: ID!
  settingType: String
  entityId: ID
  entity: String
  data: JSON
  defaultData: JSON
  organizationId: ID
}

"""
One sum of money over a period: what came in, what went out, what was left and what is owed on it.
"""
type EmployeeStatisticTotals {
  """The income recorded over the period. Money."""
  income: Decimal!

  """
  The expenses recorded over the period, the employee's own and the organization's split share alike. Money.
  """
  expense: Decimal!

  """The bonus the organization's policy computes over the period. Money."""
  bonus: Decimal!

  """
  Income less expense over the period. Money, and negative when the period cost more than it earned.
  """
  profit: Decimal!
}

"""One employee's sum over the period, as the aggregate answers it."""
type EmployeeStatisticByEmployee {
  """
  The employee the sum belongs to. The account behind it is read from the user surface by this identifier.
  """
  employeeId: ID!

  """The income recorded against this employee over the period. Money."""
  income: Decimal!

  """The expenses recorded against this employee over the period. Money."""
  expense: Decimal!

  """The bonus computed for this employee over the period. Money."""
  bonus: Decimal!

  """Income less expense for this employee over the period. Money."""
  profit: Decimal!
}

"""
One day of the aggregate's chart, with the whole organization's sum for it.
"""
type EmployeeStatisticChartPoint {
  """
  The day, formatted by the delivered answer in its own locale-aware spelling.
  """
  dates: String!

  """The organization's sum for that day."""
  statistics: EmployeeStatisticTotals!
}

"""
What the aggregate route answers for one organization over one range: the total, the same figures per
employee, and the same figures per day.
"""
type EmployeeStatisticsAggregate {
  """The organization's total over the range."""
  total: EmployeeStatisticTotals!

  """The same total, once per employee the range's read considered working."""
  employees: [EmployeeStatisticByEmployee!]!

  """The same total, once per day of the range."""
  chart: [EmployeeStatisticChartPoint!]!
}

"""
One employee's twelve-month series, as the per-employee route answers them.

The four lists are **parallel**: they are indexed by the same month walk, so the fourth entry of
`incomeStatistics` and the fourth of `expenseStatistics` are the same month. The delivered answer does
not carry the month names beside them, and this type does not invent any: the walk starts at the
current month and runs forward eleven, which is what makes the lists readable positionally.
"""
type EmployeeStatisticsSeries {
  """Expenses, one entry per month of the walk. Money."""
  expenseStatistics: [Decimal!]!

  """Income, one entry per month of the walk. Money."""
  incomeStatistics: [Decimal!]!

  """Income less expense, one entry per month of the walk. Money."""
  profitStatistics: [Decimal!]!

  """
  The bonus computed from the organization's policy, one entry per month of the walk. Money.
  """
  bonusStatistics: [Decimal!]!
}

"""
One month of an employee's statistics, as the monthly route answers them.

`month` is the calendar month's zero-based position — January is `0` — which is the value the
delivered answer carries rather than a name, and it is zero-based because the store's own month
arithmetic is.
"""
type EmployeeMonthlyStatistic {
  """The month, zero-based: January is `0`."""
  month: Int!

  """The year, as a four-digit calendar year."""
  year: Int!

  """The income recorded in the month. Money."""
  income: Decimal!

  """
  Every expense of the month, the employee's own, the recurring ones and the organization's split share alike. Money.
  """
  expense: Decimal!

  """
  The month's expenses less the recurring salary lines, which are the part of a payroll an operator reads the run-rate without. Money.
  """
  expenseWithoutSalary: Decimal!

  """Income less expense for the month. Money."""
  profit: Decimal!

  """The bonus computed for the month. Money."""
  bonus: Decimal!

  """
  The part of `bonus` that came from income the platform recorded as a bonus directly, rather than from the organization's policy. Money.
  """
  directIncomeBonus: Decimal!
}

"""
One split expense an entry was divided from, kept beside the entry so a reader can see why the amount
is what it is.
"""
type EmployeeStatisticsSplitExpense {
  """The whole expense before it was divided. Money."""
  originalValue: Decimal!

  """How many people it was divided among."""
  employeeCount: Int!
}

"""
One line of an employee's income or expense history.

The lines are heterogeneous on purpose, and the flags are what tell them apart: an entry may be an
income or an expense, it may have come from the employee's own record or from the organization's split
of a shared cost, and it may be recurring, a bonus or a salary line. That is the delivered answer's own
shape rather than a normalisation of it — the four sources are aggregated differently and a client that
wanted them merged would lose the ability to ask which one a line came from.
"""
type EmployeeStatisticsHistoryEntry {
  """The instant the line is dated to."""
  valueDate: DateTime

  """
  The amount, as the source recorded it. `amount` is already the employee's share for a split expense, and `splitExpense` carries the whole it was divided from. Money.
  """
  amount: Decimal

  """What the source recorded about the line."""
  notes: String

  """
  The vendor on an expense line. Present on an employee's own expense and absent on the other three sources.
  """
  vendorName: String

  """
  The client on an income line. Present on an income and absent on an expense; the client row is read from the party surface by this identifier.
  """
  clientId: ID

  """The expense or income category. Present on the lines that carry one."""
  categoryName: String

  """
  Whether the line came from a recurring expense rather than a one-off one.
  """
  isRecurring: Boolean

  """Whether the line is income the platform recorded as a bonus."""
  isBonus: Boolean

  """
  Whether the line is a recurring salary line, which is what `EXPENSES_WITHOUT_SALARY` filters out.
  """
  isSalary: Boolean

  """
  Which of the four sources the line came from: the employee's own record, or the organization's split of a shared cost. Carried as its value rather than declared as a schema enum because the vocabulary belongs to the contracts package.
  """
  source: String

  """
  The split the line was divided from, present only on the two sources that divide a shared cost.
  """
  splitExpense: EmployeeStatisticsSplitExpense
}

"""
One person's engagement by one organization: what they are paid, on what terms, what they are allowed
to do in the tracking agent, and which account they sign in as.
"""
type Employee {
  id: ID!

  """
  The instant the row is dated to, where an administration screen dates it. Not the hire date — that is `startedWorkOn`.
  """
  valueDate: DateTime

  """
  A one-line summary written for the lists, kept beside the long description rather than derived from it.
  """
  short_description: String

  """
  The long description, sanitised through the server-side allowlist before it is stored, because a public organization page renders it.
  """
  description: String

  """
  When the engagement began. The delivered working-employees read is defined against it and against `endWork`.
  """
  startedWorkOn: DateTime

  """
  When the engagement ended, or null while it is current. Null is what the working-employees read treats as still working.
  """
  endWork: DateTime

  """
  How often this employee is paid, as the contracts package's own `PayPeriodEnum` value.
  """
  payPeriod: String

  """
  The billing rate per hour, in `billRateCurrency`. Money, and therefore an exact decimal.
  """
  billRateValue: Decimal

  """
  The floor below which tracked time on this employee is not billed. A rate, and therefore an exact decimal.
  """
  minimumBillingRate: Decimal

  """
  The currency `billRateValue` and `minimumBillingRate` are stated in, as the contracts package's own `CurrenciesEnum` value.
  """
  billRateCurrency: String

  """
  Recurring weekly limit, in hours. A whole number of hours rather than an amount.
  """
  reWeeklyLimit: Int

  """When the offer was made, for an engagement that is being hired."""
  offerDate: DateTime

  """When the offer was accepted."""
  acceptDate: DateTime

  """When the offer was declined."""
  rejectDate: DateTime

  """
  The engagement level's label, as the row stores it. The vocabulary it is drawn from is read from `employeeLevels`.
  """
  employeeLevel: String

  """
  Whether the bonus computed for this employee is hidden from the employee's own view.
  """
  anonymousBonus: Boolean

  """The average income the platform computed for this employee. Money."""
  averageIncome: Decimal

  """The average bonus the platform computed for this employee. Money."""
  averageBonus: Decimal

  """Total hours the platform recorded against this employee."""
  totalWorkHours: Decimal

  """The average expenses the platform computed for this employee. Money."""
  averageExpenses: Decimal

  """Whether the employee's own view shows the anonymous-bonus column."""
  show_anonymous_bonus: Boolean

  """Whether the employee's own view shows the average-bonus column."""
  show_average_bonus: Boolean

  """Whether the employee's own view shows the average-expenses column."""
  show_average_expenses: Boolean

  """Whether the employee's own view shows the average-income column."""
  show_average_income: Boolean

  """Whether the employee's own view shows the billing-rate column."""
  show_billrate: Boolean

  """Whether the employee's own view shows the pay-period column."""
  show_payperiod: Boolean

  """Whether the employee's own view shows the start date column."""
  show_start_work_on: Boolean

  """
  Whether the employee is open to being put forward for other engagements.
  """
  isJobSearchActive: Boolean

  """The employee's LinkedIn profile."""
  linkedInUrl: String

  """The employee's Facebook profile."""
  facebookUrl: String

  """The employee's Instagram profile."""
  instagramUrl: String

  """The employee's Twitter profile."""
  twitterUrl: String

  """The employee's GitHub profile."""
  githubUrl: String

  """The employee's GitLab profile."""
  gitlabUrl: String

  """The employee's Upwork profile."""
  upworkUrl: String

  """The employee's Stack Overflow profile."""
  stackoverflowUrl: String

  """Whether the platform has verified this engagement."""
  isVerified: Boolean

  """Whether the platform has vetted this engagement."""
  isVetted: Boolean

  """
  How many jobs the external marketplace reports for this employee. A number, and an exact decimal because the column is one.
  """
  totalJobs: Decimal

  """
  The external marketplace's success score for this employee. An exact decimal for the same reason.
  """
  jobSuccess: Decimal

  """The external marketplace's public profile link."""
  profile_link: String

  """Whether the tracking agent records time for this employee."""
  isTrackingEnabled: Boolean

  """
  Whether the employee is currently online. Written by the tracking agent, not by an administration screen.
  """
  isOnline: Boolean

  """Whether the employee has stepped away."""
  isAway: Boolean

  """
  Whether the tracking agent currently has a running timer for this employee.
  """
  isTrackingTime: Boolean

  """
  Whether the agent may capture screenshots. A per-employee policy the agent reads.
  """
  allowScreenshotCapture: Boolean

  """Whether the employee may enter time by hand."""
  allowManualTime: Boolean

  """Whether the employee may change a time entry that exists."""
  allowModifyTime: Boolean

  """Whether the employee may remove a time entry."""
  allowDeleteTime: Boolean

  """Whether the employee may quit the agent application."""
  allowAgentAppExit: Boolean

  """Whether the employee may sign out of the agent application."""
  allowLogoutFromAgentApp: Boolean

  """
  Whether the agent tracks keyboard and mouse activity, or only elapsed time.
  """
  trackKeyboardMouseActivity: Boolean

  """Whether the agent captures every display, or only the primary one."""
  trackAllDisplays: Boolean

  """The employee's identifier at the external marketplace."""
  upworkId: String

  """The employee's identifier at LinkedIn."""
  linkedInId: String

  """
  The account this engagement belongs to. The account row is read from this surface's own `user(id)` by this identifier.
  """
  userId: ID

  """
  The contact row the engagement keeps its postal and fiscal facts in, where it has one. The contact is read from the contact surface.
  """
  contactId: ID

  """
  The organization position this employee holds, where one is recorded. The position is read from the position surface.
  """
  organizationPositionId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """
  When the engagement was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — `DELETE /:id/soft` and `PUT /:id/recover` — and this column is what they write, so without
  it a caller could not tell a live row from a withdrawn one on the answer to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of employees."""
type EmployeeEdge {
  node: Employee!
  cursor: String!
}

"""
One page of employees, with the total the filters select and the boundary cursors.
"""
type EmployeeConnection {
  nodes: [Employee!]!
  edges: [EmployeeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields the employee list may be sorted by.

The set is narrower than the columns the type carries, and that is a decision rather than an oversight
in one direction only: every member here is a column the row always has, and the columns a list is
read in an order for are the dates, the level and the flags. Sorting a roster by a description or by a
profile URL is not a question an administration screen asks.
"""
enum EmployeeSortField {
  createdAt
  updatedAt
  startedWorkOn
  endWork
  short_description
  employeeLevel
  isActive
  isArchived
  isOnline
  isTrackingEnabled
}

"""
The order the employees are returned in. At most three keys are accepted.
"""
input EmployeeSort {
  field: EmployeeSortField!
  direction: SortDirection!
}

"""
The fields the employee list may be narrowed by.

Every member is a column of the row, which is what makes it answerable: the delivered read joins
nothing, so a member for a relation would narrow by a value the rows this surface holds never carry.
The identifiers a relation is carried as are filterable for the reason they exist — selecting the
employees of one organization, or the engagements of one account, is the read these columns are for.
"""
input EmployeeFilter {
  id: IDFilter
  valueDate: DateTimeFilter
  short_description: StringFilter
  description: StringFilter
  startedWorkOn: DateTimeFilter
  endWork: DateTimeFilter
  payPeriod: StringFilter
  billRateValue: DecimalFilter
  minimumBillingRate: DecimalFilter
  billRateCurrency: StringFilter
  reWeeklyLimit: NumberFilter
  offerDate: DateTimeFilter
  acceptDate: DateTimeFilter
  rejectDate: DateTimeFilter
  employeeLevel: StringFilter
  anonymousBonus: BooleanFilter
  averageIncome: DecimalFilter
  averageBonus: DecimalFilter
  totalWorkHours: DecimalFilter
  averageExpenses: DecimalFilter
  totalJobs: DecimalFilter
  jobSuccess: DecimalFilter
  profile_link: StringFilter
  isVerified: BooleanFilter
  isVetted: BooleanFilter
  isJobSearchActive: BooleanFilter
  isTrackingEnabled: BooleanFilter
  isOnline: BooleanFilter
  isAway: BooleanFilter
  isTrackingTime: BooleanFilter
  allowScreenshotCapture: BooleanFilter
  allowManualTime: BooleanFilter
  allowModifyTime: BooleanFilter
  allowDeleteTime: BooleanFilter
  allowAgentAppExit: BooleanFilter
  allowLogoutFromAgentApp: BooleanFilter
  trackKeyboardMouseActivity: BooleanFilter
  trackAllDisplays: BooleanFilter
  upworkId: StringFilter
  linkedInId: StringFilter
  userId: IDFilter
  contactId: IDFilter
  organizationPositionId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeFilter!]
  or: [EmployeeFilter!]
  not: EmployeeFilter
}

"""The fields the working-employees list may be sorted by."""
enum WorkingEmployeeSortField {
  createdAt
  updatedAt
  startedWorkOn
  billRateValue
  isOnline
  isTrackingEnabled
}

"""
The order the working employees are returned in. At most three keys are accepted.
"""
input WorkingEmployeeSort {
  field: WorkingEmployeeSortField!
  direction: SortDirection!
}

"""
The fields the working-employees list may be narrowed by: exactly the columns that read selects.
"""
input WorkingEmployeeFilter {
  id: IDFilter
  short_description: StringFilter
  description: StringFilter
  startedWorkOn: DateTimeFilter
  averageIncome: DecimalFilter
  averageExpenses: DecimalFilter
  averageBonus: DecimalFilter
  isTrackingEnabled: BooleanFilter
  billRateCurrency: StringFilter
  billRateValue: DecimalFilter
  minimumBillingRate: DecimalFilter
  userId: IDFilter
  isAway: BooleanFilter
  isOnline: BooleanFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [WorkingEmployeeFilter!]
  or: [WorkingEmployeeFilter!]
  not: WorkingEmployeeFilter
}

"""The fields the organization-members list may be sorted by."""
enum EmployeeMemberSortField {
  createdAt
  updatedAt
  isOnline
  isAway
}

"""
The order the organization members are returned in. At most three keys are accepted.
"""
input EmployeeMemberSort {
  field: EmployeeMemberSortField!
  direction: SortDirection!
}

"""
The fields the organization-members list may be narrowed by: exactly the columns that read selects.
"""
input EmployeeMemberFilter {
  id: IDFilter
  userId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  isOnline: BooleanFilter
  isAway: BooleanFilter
  tenantId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EmployeeMemberFilter!]
  or: [EmployeeMemberFilter!]
  not: EmployeeMemberFilter
}

"""
The account a new employee signs in as, when the caller is filing an account together with the
engagement.

The members are the ones the delivered body validates for a nested account. The role is stated as an
identifier here rather than as the row the delivered body accepts, because that body resolves the role
from the database and refuses one the caller may not grant — the row's other columns are not read from
this member at all, so stating them would promise an effect the write does not have.

`password` is a plaintext member on the way in and is hashed before the row is written; it is never a
member of the answer, because the answer is the account's projection and the projection has no digest
in it.
"""
input EmployeeUserInput {
  """
  The address the account is created with, and the address it signs in with.
  """
  email: String!

  """The given name to file on the account."""
  firstName: String

  """The family name to file on the account."""
  lastName: String

  """The image to show for the account."""
  imageUrl: String

  """The language the account's own interface is read in."""
  preferredLanguage: String

  """
  The role the account acts under, resolved from the database and refused when the caller may not grant it.
  """
  roleId: ID
}

"""
What a caller states when it files an engagement.

The organization is stated and required because the delivered create does not derive one: the tenant is
stamped from the credential, never from an argument, but the organization is a member the caller names
and the delivered handler reads it to file the row and to add the account to the organization. There is
no member for the tenant, for the same reason there is none on any other write here.

Either `userId` names an account that already exists or `user` states one to file with the engagement,
and the delivered create refuses a body that states neither: an engagement is a fact about a person, so
there has to be a person. `password` is read only on the branch that files a new account.

The nested row collections the delivered body also accepts — the employment types, the departments and
the skills — are not members here. Each is a collection of rows rather than of identifiers, the
delivered write cascades the rows themselves, and a GraphQL input that stated a half-row would be a
second and weaker way to write the same relation than the domain that owns it already offers. The
position is the exception, because it is one relation and it has an identifier: `organizationPositionId`.
"""
input CreateEmployeeInput {
  """The organization the engagement is filed under."""
  organizationId: ID!

  """The account that already exists and is to be engaged."""
  userId: ID

  """
  The account to file together with the engagement, when none exists yet.
  """
  user: EmployeeUserInput

  """The new account's password, read only on the branch that files one."""
  password: String

  """When the engagement begins."""
  startedWorkOn: DateTime

  """When the engagement ended, if it already has."""
  endWork: DateTime

  """A one-line summary for the lists."""
  short_description: String

  """
  The long description. It is sanitised through the server-side allowlist before it is stored.
  """
  description: String

  """Whether the computed bonus is hidden from the employee's own view."""
  anonymousBonus: Boolean

  """The engagement level's label, as this row stores it."""
  employeeLevel: String

  """The organization position this employee holds."""
  organizationPositionId: ID

  """
  The facets to attach to the employee, as the identifiers the pivot is written from.
  """
  tagIds: [ID!]
}

"""
What a caller states when it edits an engagement.

Every member is optional and a member that is absent is left as it is *for the columns of this row*,
because the delivered edit reads the row and persists the stated members over it. The account is the
exception and is not a member at all: the edit route reads the engagement's own columns, and an
account is edited through the account's own surface.

The identifier is carried in the body as well as in the field's own argument, because the delivered
handler reads the body's and treats the path's as authoritative — see `updateEmployee`.
"""
input UpdateEmployeeInput {
  id: ID!

  """When the engagement begins."""
  startedWorkOn: DateTime

  """When the engagement ended, if it already has."""
  endWork: DateTime

  """A one-line summary for the lists."""
  short_description: String

  """
  The long description. It is sanitised through the server-side allowlist before it is stored.
  """
  description: String

  """Whether the computed bonus is hidden from the employee's own view."""
  anonymousBonus: Boolean

  """The engagement level's label, as this row stores it."""
  employeeLevel: String

  """The organization position this employee holds."""
  organizationPositionId: ID

  """
  The facets to attach to the employee, as the identifiers the pivot is written from.
  """
  tagIds: [ID!]

  """The employee's LinkedIn profile."""
  linkedInUrl: String

  """The employee's Facebook profile."""
  facebookUrl: String

  """The employee's Instagram profile."""
  instagramUrl: String

  """The employee's Twitter profile."""
  twitterUrl: String

  """The employee's GitHub profile."""
  githubUrl: String

  """The employee's GitLab profile."""
  gitlabUrl: String

  """The employee's Upwork profile."""
  upworkUrl: String

  """The employee's Stack Overflow profile."""
  stackoverflowUrl: String

  """
  The billing rate per hour, in `billRateCurrency`. A rate, and therefore an exact decimal.
  """
  billRateValue: Decimal

  """The currency the rates are stated in."""
  billRateCurrency: String

  """The floor below which tracked time is not billed."""
  minimumBillingRate: Decimal

  """How often the employee is paid."""
  payPeriod: String

  """Recurring weekly limit, in hours."""
  reWeeklyLimit: Int

  """When the offer was made."""
  offerDate: DateTime

  """When the offer was accepted."""
  acceptDate: DateTime

  """When the offer was declined."""
  rejectDate: DateTime

  """The employee's identifier at the external marketplace."""
  upworkId: String

  """The employee's identifier at LinkedIn."""
  linkedInId: String

  """The external marketplace's public profile link."""
  profile_link: String

  """Whether the employee has stepped away."""
  isAway: Boolean

  """Whether the employee's own view shows the anonymous-bonus column."""
  show_anonymous_bonus: Boolean

  """Whether the employee's own view shows the average-bonus column."""
  show_average_bonus: Boolean

  """Whether the employee's own view shows the average-expenses column."""
  show_average_expenses: Boolean

  """Whether the employee's own view shows the average-income column."""
  show_average_income: Boolean

  """Whether the employee's own view shows the billing-rate column."""
  show_billrate: Boolean

  """Whether the employee's own view shows the pay-period column."""
  show_payperiod: Boolean

  """Whether the employee's own view shows the start date column."""
  show_start_work_on: Boolean

  """Whether the engagement is active."""
  isActive: Boolean

  """Whether the engagement is archived."""
  isArchived: Boolean

  """Whether the platform has verified this engagement."""
  isVerified: Boolean

  """Whether the platform has vetted this engagement."""
  isVetted: Boolean

  """Whether the employee is currently online."""
  isOnline: Boolean

  """Whether the tracking agent records time for this employee."""
  isTrackingEnabled: Boolean

  """
  Whether the tracking agent currently has a running timer for this employee.
  """
  isTrackingTime: Boolean

  """
  Whether the employee is open to being put forward for other engagements.
  """
  isJobSearchActive: Boolean

  """Whether the agent may capture screenshots."""
  allowScreenshotCapture: Boolean

  """Whether the employee may enter time by hand."""
  allowManualTime: Boolean

  """Whether the employee may change a time entry that exists."""
  allowModifyTime: Boolean

  """Whether the employee may remove a time entry."""
  allowDeleteTime: Boolean

  """Whether the employee may quit the agent application."""
  allowAgentAppExit: Boolean

  """Whether the employee may sign out of the agent application."""
  allowLogoutFromAgentApp: Boolean

  """Whether the agent tracks keyboard and mouse activity."""
  trackKeyboardMouseActivity: Boolean

  """Whether the agent captures every display, or only the primary one."""
  trackAllDisplays: Boolean
}

"""
What a caller states when an employee edits their own profile.

This is the same class of write as `UpdateEmployeeInput` and a narrower statement of it: the delivered
profile body carries the facts an employee owns — the networks, the rates, the hiring dates and the
employment terms — and none of the administrative columns, which is why the two inputs are two
declarations rather than one. Both are carried because both routes are served, and a resolver that
merged them would let a profile edit reach columns its own route cannot.
"""
input UpdateEmployeeProfileInput {
  id: ID!

  """When the engagement begins."""
  startedWorkOn: DateTime

  """When the engagement ended, if it already has."""
  endWork: DateTime

  """A one-line summary for the lists."""
  short_description: String

  """
  The long description. It is sanitised through the server-side allowlist before it is stored.
  """
  description: String

  """Whether the computed bonus is hidden from the employee's own view."""
  anonymousBonus: Boolean

  """The engagement level's label, as this row stores it."""
  employeeLevel: String

  """The organization position this employee holds."""
  organizationPositionId: ID

  """
  The facets to attach to the employee, as the identifiers the pivot is written from.
  """
  tagIds: [ID!]

  """The employee's LinkedIn profile."""
  linkedInUrl: String

  """The employee's Facebook profile."""
  facebookUrl: String

  """The employee's Instagram profile."""
  instagramUrl: String

  """The employee's Twitter profile."""
  twitterUrl: String

  """The employee's GitHub profile."""
  githubUrl: String

  """The employee's GitLab profile."""
  gitlabUrl: String

  """The employee's Upwork profile."""
  upworkUrl: String

  """The employee's Stack Overflow profile."""
  stackoverflowUrl: String

  """
  The billing rate per hour, in `billRateCurrency`. A rate, and therefore an exact decimal.
  """
  billRateValue: Decimal

  """The currency the rates are stated in."""
  billRateCurrency: String

  """The floor below which tracked time is not billed."""
  minimumBillingRate: Decimal

  """How often the employee is paid."""
  payPeriod: String

  """Recurring weekly limit, in hours."""
  reWeeklyLimit: Int

  """When the offer was made."""
  offerDate: DateTime

  """When the offer was accepted."""
  acceptDate: DateTime

  """When the offer was declined."""
  rejectDate: DateTime

  """The employee's identifier at the external marketplace."""
  upworkId: String

  """The employee's identifier at LinkedIn."""
  linkedInId: String

  """The external marketplace's public profile link."""
  profile_link: String

  """Whether the employee has stepped away."""
  isAway: Boolean
}

"""One employee's standing request to be told when one record changes."""
type EntitySubscription {
  id: ID!

  """
  The type of the entity being watched, as the platform's own entity-type value. It is stored on this
  row as text, so it is carried as text rather than as a declared enum.
  """
  entity: String!

  """The record being watched, by identifier."""
  entityId: ID!

  """
  The kind of subscription this is, as the contract's own value: `manual`, `mention`, `assignment`,
  `comment` or `created-entity`. Carried as its value rather than declared as an enum, because the
  vocabulary belongs to the contract and is written by rows this surface does not file.
  """
  type: String!

  """
  Who performed the subscribing, as the contract's own name for it: `System` or `User`. Nullable
  because the column is — a row written before the actor was recorded carries neither.
  """
  actorType: String

  """
  The employee who is to be told, by identifier. The delivered filing stamps it from the credential
  rather than from the body, and it is nullable for rows written outside a request. The account behind
  it is not joined by this read.
  """
  employeeId: ID

  """The tenant the subscription belongs to, by identifier."""
  tenantId: ID

  """The organization the subscription belongs to, by identifier."""
  organizationId: ID

  """
  Whether the subscription is active. A flag an operator sets, not a statement about its lifecycle.
  """
  isActive: Boolean

  """
  Whether the subscription is archived. A flag an operator sets, not a statement about its lifecycle.
  """
  isArchived: Boolean

  """When the subscription was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the subscription was withdrawn, or null while it is live. Withdrawing and restoring are
  delivered routes — two of the nine this resource serves — and this column is the whole of what they
  write, so without it a caller could not tell a live row from a withdrawn one on the answer to the
  write that withdrew it.
  """
  deletedAt: DateTime

  """When the subscription was filed."""
  createdAt: DateTime

  """When the subscription was last changed."""
  updatedAt: DateTime
}

"""The boundary of one page of subscriptions."""
type EntitySubscriptionEdge {
  node: EntitySubscription!
  cursor: String!
}

"""
One page of subscriptions, with the total the filters select and the boundary cursors.
"""
type EntitySubscriptionConnection {
  nodes: [EntitySubscription!]!
  edges: [EntitySubscriptionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a subscription list may be sorted by."""
enum EntitySubscriptionSortField {
  id
  entity
  entityId
  type
  employeeId
  createdAt
  updatedAt
  deletedAt
}

"""
The order the subscriptions are returned in. At most three keys are accepted.
"""
input EntitySubscriptionSort {
  field: EntitySubscriptionSortField!
  direction: SortDirection!
}

"""
The fields a subscription list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the subscription row, because the delivered list read answers the row
itself and joins only the relations its REST caller names — and this connection's read names none. The
employee is therefore an identifier rather than a relation path: narrowing by who is to be told is a
condition on the identifier this row stores.

No member narrows by the tenant on the caller's behalf. The tenant comes from the credential on every
read, so the identifier member above narrows the row's own column rather than restating a scope
decision the caller does not have.
"""
input EntitySubscriptionFilter {
  id: IDFilter
  entity: StringFilter
  entityId: IDFilter
  type: StringFilter
  actorType: StringFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [EntitySubscriptionFilter!]
  or: [EntitySubscriptionFilter!]
  not: EntitySubscriptionFilter
}

"""
What a caller states when it subscribes to an entity.

The members are the ones the delivered create DTO states: the two that name the record being watched,
the kind of subscription, and the actor who performed the subscribing. The record's type and its
identifier are required because the subscription is *for* that record — a row without them watches
nothing — and the kind is required because the column is not nullable.

There is no member for the tenant, and none for the employee: the delivered service stamps both from
the credential and overwrites whatever a body states. The organization is stated because it is a
member of the delivered DTO and because the criterion the service looks an existing subscription up by
carries it: a caller that states the organization it is acting in is answered the subscription it
already has rather than a second one beside it.
"""
input CreateEntitySubscriptionInput {
  """The organization the subscription belongs to."""
  organizationId: ID

  """The kind of subscription, as the contract's own value."""
  type: String!

  """
  The type of the entity being watched, as the platform's own entity-type value.
  """
  entity: String!

  """The record being watched, by identifier."""
  entityId: ID!

  """
  Who performed the subscribing, as the contract's own name for it: `System` or `User`.
  """
  actorType: String
}

"""
What a caller states when it changes a subscription that exists.

**A member the caller leaves out is left as it is.** The delivered route reads the row the identifier
names — proving it is the caller's own — and then writes the members the body states to it, so this is
a column update and not a replacement of the row. The identifier is the one required member, and it is
stated because the delivered route carries it in the path rather than in the body.
"""
input UpdateEntitySubscriptionInput {
  id: ID!

  """The organization the subscription belongs to."""
  organizationId: ID

  """The kind of subscription, as the contract's own value."""
  type: String

  """The type of the entity being watched."""
  entity: String

  """The record being watched, by identifier."""
  entityId: ID

  """
  Who performed the subscribing, as the contract's own name for it: `System` or `User`.
  """
  actorType: String
}

"""One of the organization's own terms for handing equipment out."""
type EquipmentSharingPolicy {
  id: ID!

  """
  The policy's name, which is how it is chosen from the organization's vocabulary.
  """
  name: String!

  """What the policy means, for the operator rather than for the caller."""
  description: String

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live row from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of sharing policies."""
type EquipmentSharingPolicyEdge {
  node: EquipmentSharingPolicy!
  cursor: String!
}

"""
One page of sharing policies, with the total the filters select and the boundary cursors.
"""
type EquipmentSharingPolicyConnection {
  nodes: [EquipmentSharingPolicy!]!
  edges: [EquipmentSharingPolicyEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a policy list may be sorted by.

The name is what a vocabulary is scanned by, and the two timestamps are how a change is found again.
Nothing else is offered: the row has no other member an order would be meaningful on.
"""
enum EquipmentSharingPolicySortField {
  createdAt
  updatedAt
  name
}

"""
The order the sharing policies are returned in. At most three keys are accepted.
"""
input EquipmentSharingPolicySort {
  field: EquipmentSharingPolicySortField!
  direction: SortDirection!
}

"""
The fields a policy list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row, because the connection narrows the rows the delivered read
returned. `deletedAt` is deliberately absent although the object carries it: the delivered list read
answers live rows only, so a condition on that column could only ever match the empty set. The tenant is
absent for a different reason — it is applied to the criterion from the credential — and the
organization is present, because the delivered read takes its whole query DTO from the query string and
a caller may legitimately narrow to one organization of its tenant. The periods filed under a policy are
in neither list: the delivered list read joins no relation, so a condition on that collection could
only ever match the empty set.
"""
input EquipmentSharingPolicyFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EquipmentSharingPolicyFilter!]
  or: [EquipmentSharingPolicyFilter!]
  not: EquipmentSharingPolicyFilter
}

"""
What a caller states when it files a policy.

`name` is required because the delivered create is the one route of the two that runs its validation
pipe: the body's name is validated as a non-empty string there, and the organization is validated
against the caller's own memberships. The organization is stated rather than stamped because it is a
member the delivered DTO carries, while the tenant is not a member at all — the delivered write stamps
the caller's own tenant onto the row and refuses a row that belongs to another one.
"""
input CreateEquipmentSharingPolicyInput {
  """The policy's name."""
  name: String!

  """What the policy means, for the operator."""
  description: String

  """The organization the policy belongs to."""
  organizationId: ID!
}

"""
What a caller states when it changes a policy that exists.

**A member the caller leaves out is left as it is, and `name` is not required here although the create
requires it.** The two routes bind the same DTO, but only the create runs the validation pipe over it,
and the delivered edit is a column-wise update rather than a replacement: it reads the row to prove it
is the caller's and then writes the columns the body states. A body that states no name therefore keeps
the policy's name, which is why the member is optional on this input and required on the one above —
stating it as required here would refuse a body the delivered route serves and writes.

The identifier is the criterion and is not repeated in the body, which is the shape the route itself
has. An identifier that names no row, or one of another tenant, is refused by the delivered read the
edit performs before it writes anything.
"""
input UpdateEquipmentSharingPolicyInput {
  id: ID!
  name: String
  description: String
  organizationId: ID!
}

"""
One period for which an asset is handed to one or more employees or teams.
"""
type EquipmentSharing {
  id: ID!

  """The label the period is filed under."""
  name: String

  """When the request for this period was filed."""
  shareRequestDay: DateTime

  """
  The first instant the asset is handed over. With `shareEndDay`, this is the period's own window.
  """
  shareStartDay: DateTime

  """The last instant the asset is due back."""
  shareEndDay: DateTime

  """
  Where the request stands, as the contracts' own value: `1` requested, `2` approved, `3` refused. It
  is carried as its value rather than declared as a schema enum because the vocabulary is shared with
  the approval row the delivered write records beside this one.
  """
  status: Int!

  """
  The asset whose period this is, as the identifier of the asset's own row.
  """
  equipmentId: ID

  """
  The policy the period was filed under, as the identifier of the policy's own row.
  """
  equipmentSharingPolicyId: ID

  """
  The employees the asset is handed to. Joined by the two by-organization and by-employee reads, which
  is why the members are nullable: the list connection's own rows are not joined.
  """
  employees: [Employee!]

  """
  The teams the asset is handed to. Joined by the same two reads, and null on the connection's rows for
  the same reason.
  """
  teams: [OrganizationTeam!]

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered routes
  — the two removals this resource serves — and this column is the whole of what they write, so without
  it a caller could not tell a live row from a withdrawn one on the answer to the write that withdrew
  it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID

  """
  The user who filed the period, as the identifier of the user's own row.
  """
  createdByUserId: ID
}

"""The boundary of one page of sharing periods."""
type EquipmentSharingEdge {
  node: EquipmentSharing!
  cursor: String!
}

"""
One page of sharing periods, with the total the filters select and the boundary cursors.
"""
type EquipmentSharingConnection {
  nodes: [EquipmentSharing!]!
  edges: [EquipmentSharingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a sharing list may be sorted by.

The window's two bounds are the orders this resource is read in — what is handed out next, and what is
due back next; the request day is how the queue of pending requests is worked; the name is what a
period is found by; `status` is offered because a work list is taken in status groups, and the values
sort as the numbers they are, which groups like with like; and the two timestamps are how a change is
found again.
"""
enum EquipmentSharingSortField {
  createdAt
  updatedAt
  name
  shareRequestDay
  shareStartDay
  shareEndDay
  status
}

"""
The order the sharing periods are returned in. At most three keys are accepted.
"""
input EquipmentSharingSort {
  field: EquipmentSharingSortField!
  direction: SortDirection!
}

"""
The fields a sharing list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers. The three interval members are
date filters, which is what makes the period questions stateable: **a period in force now is
`shareStartDay: { lte: <now> }` beside `shareEndDay: { gte: <now> }`**, and `isNull` on either bound is
a row whose window was never recorded. `isActive` is the base entity's own marker and answers a
different question — whether the row is part of the working set — so the two are not the same filter and
neither replaces the other.

The collections the row is joined with by the two by-organization and by-employee reads are not
filterable here, and the reason is the read rather than a preference: the list connection's rows are not
joined, so a condition on a membership could only ever match the empty set. The membership questions are
answered by narrowing those two fields instead.

`deletedAt` is absent although the object carries it: the delivered list read answers live rows only.
The tenant is absent because the read applies it from the credential; the organization is present,
because the two sub-route reads take it and a caller may legitimately narrow to one organization.
"""
input EquipmentSharingFilter {
  id: IDFilter
  name: StringFilter
  shareRequestDay: DateTimeFilter
  shareStartDay: DateTimeFilter
  shareEndDay: DateTimeFilter
  status: NumberFilter
  equipmentId: IDFilter
  equipmentSharingPolicyId: IDFilter
  createdByUserId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EquipmentSharingFilter!]
  or: [EquipmentSharingFilter!]
  not: EquipmentSharingFilter
}

"""
What a caller states when it files a sharing period.

The organization is an argument of the field rather than a member here, because the delivered route
reads it from its own path segment and the handler stamps the row with it.
"""
input CreateEquipmentSharingInput {
  """The label the period is filed under."""
  name: String

  """When the request for this period was filed."""
  shareRequestDay: DateTime

  """The first instant the asset is handed over."""
  shareStartDay: DateTime

  """The last instant the asset is due back."""
  shareEndDay: DateTime

  """Where the request starts: `1` requested, `2` approved, `3` refused."""
  status: Int!

  """The asset whose period this is, by identifier."""
  equipmentId: ID

  """The policy the period is filed under, by identifier."""
  equipmentSharingPolicyId: ID

  """
  The employees the asset is handed to, by identifier. The pivot is written from these.
  """
  employeeIds: [ID!]

  """
  The teams the asset is handed to, by identifier. The pivot is written from these.
  """
  teamIds: [ID!]
}

"""
What a caller states when it changes a period that exists.

**The delivered edit is a delete and a re-insert under the same identifier, not a column-wise update.**
The handler reads the row to prove it is in the caller's scope, removes it together with its approval
row, and then persists the stated body under the identifier the route read from its path. Two
consequences follow from that same fact and are worth stating plainly:

- A member the caller leaves out is written as the column's default rather than left as it is.
- `status` is therefore **not optional in effect** even though a partial body is what the delivered DTO
  describes: the column is `NOT NULL`, so a body that omits it removes the row and then fails the insert
  that was to replace it. It is stated as required here so that the refusal arrives at the schema rather
  than after the row is gone.

The identifier names the row and is not repeated in the body, which is the shape the route itself has.
An identifier naming no row is refused rather than recreated: the handler's own read is what proves the
row exists.
"""
input UpdateEquipmentSharingInput {
  id: ID!
  name: String
  shareRequestDay: DateTime
  shareStartDay: DateTime
  shareEndDay: DateTime

  """Where the request stands: `1` requested, `2` approved, `3` refused."""
  status: Int!
  equipmentId: ID
  equipmentSharingPolicyId: ID
  employeeIds: [ID!]
  teamIds: [ID!]
}

"""One tracked asset of an organization."""
type Equipment {
  id: ID!

  """The name the organization tracks the asset under."""
  name: String!

  """The kind of asset, as the organization's own vocabulary spells it."""
  type: String!

  """
  The manufacturer's serial number, which is how the asset is identified in the field.
  """
  serialNumber: String!

  """The year the asset was manufactured."""
  manufacturedYear: Decimal

  """The amount the asset cost when it was acquired, in `currency`."""
  initialCost: Decimal

  """The ISO 4217 code `initialCost` is expressed in."""
  currency: String!

  """
  The longest period, in days, the asset may be shared for. Null means the organization caps nothing.
  """
  maxSharePeriod: Decimal

  """
  Whether a sharing request for this asset is approved without an approver acting on it.
  """
  autoApproveShare: Boolean!

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of equipment."""
type EquipmentEdge {
  node: Equipment!
  cursor: String!
}

"""
One page of equipment, with the total the filters select and the boundary cursors.
"""
type EquipmentConnection {
  nodes: [Equipment!]!
  edges: [EquipmentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an equipment list may be sorted by.

The name and the kind are how a stock list is scanned; the two numbers are what it is taken by when an
operator is looking for the expensive or the oldest assets; and the two timestamps are how a change is
found again. The identifier of the organization is not a sort key: it groups the list rather than
ordering it, and the questions it answers are filters.
"""
enum EquipmentSortField {
  createdAt
  updatedAt
  name
  type
  manufacturedYear
  initialCost
}

"""
The order the equipment is returned in. At most three keys are accepted.
"""
input EquipmentSort {
  field: EquipmentSortField!
  direction: SortDirection!
}

"""
The fields an equipment list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, because the connection narrows the rows the delivered read
returned. `name`, `type` and `serialNumber` are the three columns the delivered paginated spelling
turns into a partial match before it queries, and that narrowing survives the fold as `ilike` on the
same three members. The three numeric columns are stated through the decimal family, which is the
family they are stored and compared in.

`deletedAt` is deliberately absent although the object carries it: the delivered list read answers live
rows only, so a condition on that column could only ever match the empty set, and an argument that
cannot be honoured is worse than an argument that is not offered. The tenant is absent for a different
reason — it is applied to the criterion from the credential, so a member here would suggest a scope
decision the caller does not have. The organization is present, because the delivered list route hands
its own criterion through and a caller may legitimately narrow to one organization of its tenant.
"""
input EquipmentFilter {
  id: IDFilter
  name: StringFilter
  type: StringFilter
  serialNumber: StringFilter
  manufacturedYear: DecimalFilter
  initialCost: DecimalFilter
  currency: StringFilter
  maxSharePeriod: DecimalFilter
  autoApproveShare: BooleanFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EquipmentFilter!]
  or: [EquipmentFilter!]
  not: EquipmentFilter
}

"""
What a caller states when it files an asset.

`name` and `currency` are the two members the delivered body validation requires: the name is what the
asset is tracked under and the currency is what `initialCost` is expressed in.
"""
input CreateEquipmentInput {
  """The name the organization tracks the asset under."""
  name: String!

  """The kind of asset."""
  type: String

  """The asset to show for the equipment, by identifier."""
  imageId: ID

  """The manufacturer's serial number."""
  serialNumber: String

  """
  The year the asset was manufactured, as the exact decimal the column stores.
  """
  manufacturedYear: Decimal

  """The amount the asset cost when it was acquired."""
  initialCost: Decimal

  """The longest period, in days, the asset may be shared for."""
  maxSharePeriod: Decimal

  """
  Whether a sharing request for this asset is approved without an approver acting on it.
  """
  autoApproveShare: Boolean

  """The ISO 4217 code `initialCost` is expressed in."""
  currency: String!

  """The organization the asset is filed under."""
  organizationId: ID!

  """
  The facets attached to the asset, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it changes an asset that exists.

**A member the caller leaves out is left as it is.** The delivered edit spreads the body beside the
identifier the route read from its path and hands the result to the same save the create uses, and that
save merges what it is given onto the row the store holds rather than replacing the row — so omitting
`serialNumber` keeps the serial number and omitting `initialCost` keeps the cost. The three members the
delivered body validation requires are the exception in the other direction: a body that states no
`name`, no `currency` and no organization is refused before the write runs, so those are effectively
always stated.

An identifier that names a row of another tenant is refused outright, which is the ownership guard the
delivered write applies before it persists anything. An identifier that names no row at all is left to
the store: the write is a save, and what a save does with an identifier it has never seen is the store's
own behaviour rather than this surface's promise.
"""
input UpdateEquipmentInput {
  id: ID!
  name: String!
  type: String
  imageId: ID
  serialNumber: String
  manufacturedYear: Decimal
  initialCost: Decimal
  maxSharePeriod: Decimal
  autoApproveShare: Boolean
  currency: String!
  organizationId: ID!
  tagIds: [ID!]
}

"""
One invitation to accept or reject an estimate, as the delivered validation read answers it.
"""
type EstimateEmail {
  id: ID!

  """
  The address the invitation was sent to. Part of what the token is verified against.
  """
  email: String!

  """
  When the invitation stops being valid. The validation read refuses a row past this instant.
  """
  expireDate: DateTime!

  """
  The organization's policy on accepted estimates, as it stood when the invitation was sent — the
  recipient's page reads this to decide what to offer.
  """
  convertAcceptedEstimates: Boolean

  """The organization the estimate belongs to."""
  organizationId: ID

  """The tenant the estimate belongs to."""
  tenantId: ID

  """
  The tenant, joined only when the caller names it in `relations`. It carries the two members the
  delivered select names — the tenant's name and its logo — because those are what the invitation page
  renders above the document.
  """
  tenant: Tenant

  """
  The organization, joined only when the caller names it in `relations`. It carries the three members
  the delivered select names: the name and official name the document is headed with, and the brand
  colour it is styled in.
  """
  organization: Organization
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
The relations the delivered validation read will join.

An enumeration rather than a bare string list, because the delivered query DTO validates the same two
names with the same enum and refuses anything else: only relations whose columns the read's own `select`
constrains may be named, so a caller cannot ask for a relation the answer would then carry in full.
"""
enum EstimateEmailRelation {
  """
  The tenant the invitation was sent under, with the name and logo the page renders.
  """
  tenant

  """
  The organization the estimate belongs to, with the names and brand colour the page renders.
  """
  organization
}

"""
One durable event: the state change it describes, projected, and where its dispatch stands.
"""
type EventOutboxRecord {
  id: ID!

  """
  Stable event identity, generated by the writer rather than by the database. It is what the delivery
  records, the webhook signature and a replay all carry, and it is *not* the row's primary key: a
  delivery record outlives the event row and names this one.
  """
  eventId: ID!

  """`<aggregate>.<action>`, for example `order.placed`."""
  eventName: String!

  """The aggregate that changed, for example `order`."""
  aggregateType: String!

  """The instance of the aggregate that changed."""
  aggregateId: ID!

  """
  The event body: the projection the catalogue declares for this event name, never an entity dump.
  Non-null, because the column is non-null with an empty-object default.
  """
  payload: JSON!

  """
  What travelled beside the body: the correlation id, the causation id, the actor and the channel.
  Null when the row was written outside a request, which is the only case that produces one.
  """
  headers: JSON

  """
  Where dispatch stands, as the contracts' own value: `PENDING`, `PUBLISHED`, `FAILED` or `DEAD`.
  `FAILED` means a further attempt is scheduled, not that the event was abandoned — `DEAD` is the
  terminal state.
  """
  status: String!

  """
  How many dispatch attempts were made. The claim that leases the row increments it.
  """
  attemptCount: Int!

  """
  When dispatch is next due. It is the lease and the backoff in one column: claiming a row pushes it
  forward for the lease window, so a dispatcher that dies mid-pass leaves rows another worker picks
  up when the window expires.
  """
  availableAt: DateTime!

  """When dispatch completed; null while the event has not been dispatched."""
  publishedAt: DateTime

  """The last dispatch error, as the dispatcher recorded it."""
  lastError: String

  """The ordering key, normally `<aggregateType>:<aggregateId>`."""
  partitionKey: String

  """
  The position of this event inside its partition, allocated inside the writing transaction. It is
  monotonic within the key above and meaningless across keys.
  """
  sequence: Int!
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of outbox rows."""
type EventOutboxRecordEdge {
  node: EventOutboxRecord!
  cursor: String!
}

"""
One page of outbox rows, with the total the filters select and the boundary cursors.
"""
type EventOutboxRecordConnection {
  nodes: [EventOutboxRecord!]!
  edges: [EventOutboxRecordEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an outbox list may be sorted by.

`availableAt` is the queue's own order — the row whose turn comes first — and it is the order the
listing means when the caller states none. The two timestamps are how a change is found again,
`sequence` is how one aggregate's events are put back in order, and `attemptCount` is how the events
that keep failing are found. The flags and the identifiers are not sort keys: an identifier or a
boolean order is not an order a queue is read in, and the questions those columns answer are filters.
"""
enum EventOutboxRecordSortField {
  createdAt
  updatedAt
  availableAt
  publishedAt
  sequence
  attemptCount
}

"""
The order the outbox rows are returned in. At most three keys are accepted.
"""
input EventOutboxRecordSort {
  field: EventOutboxRecordSortField!
  direction: SortDirection!
}

"""
The fields an outbox list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row. The two documents are stated through the JSON family, which is
the only family that can compare them: `contains` answers "which events mention this aggregate"
without the surface claiming to know the body's shape.

`deletedAt` is deliberately absent although the row carries it: the delivered read answers live rows
only, so a condition on that column could only ever match the empty set, and an argument that cannot
be honoured is worse than an argument that is not offered. The tenant and the organization are absent
for a different reason — both are applied to the criterion from the credential rather than from the
caller, so a member here would suggest a scope decision the caller does not have.
"""
input EventOutboxRecordFilter {
  id: IDFilter
  eventId: IDFilter
  eventName: StringFilter
  aggregateType: StringFilter
  aggregateId: IDFilter
  payload: JSONFilter
  headers: JSONFilter
  status: StringFilter
  attemptCount: NumberFilter
  availableAt: DateTimeFilter
  publishedAt: DateTimeFilter
  lastError: StringFilter
  partitionKey: StringFilter
  sequence: NumberFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EventOutboxRecordFilter!]
  or: [EventOutboxRecordFilter!]
  not: EventOutboxRecordFilter
}

"""What one consumer did with one event."""
type EventDelivery {
  id: ID!

  """
  The `eventId` of the outbox row this record is about. There is deliberately no foreign key behind
  it: the record outlives the event row, and it is the record — not the event — that says what a
  consumer actually did.
  """
  eventId: ID!

  """
  The consumer, namespaced by its kind: `subscriber:<key>`, `job:<queueName>` or
  `webhook:<subscriptionId>`. The prefix is what keeps a subscriber, a queued job and an outbound
  endpoint from ever colliding on one event.
  """
  consumerKey: String!

  """
  Where this consumer stands, in the same vocabulary the outbox row uses: `PENDING` before the
  consumer is invoked, `PUBLISHED` once it acknowledged the event, `FAILED` while a further attempt
  is scheduled, and `DEAD` when the budget is spent or an operator stopped it.
  """
  status: String!

  """
  How many times the consumer was invoked. Reset by a replay, which is what a replay is for.
  """
  attemptCount: Int!

  """When the consumer acknowledged the event; null while it has not."""
  deliveredAt: DateTime

  """
  The last error the consumer raised, or the reason an operator stopped it.
  """
  lastError: String

  """
  Copied from the outbox row, so the retry scan keeps per-aggregate order without a join.
  """
  partitionKey: String

  """
  Copied from the outbox row; a strict consumer compares it against the last it processed.
  """
  sequence: Int
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of delivery records."""
type EventDeliveryEdge {
  node: EventDelivery!
  cursor: String!
}

"""
One page of delivery records, with the total the filters select and the boundary cursors.
"""
type EventDeliveryConnection {
  nodes: [EventDelivery!]!
  edges: [EventDeliveryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a delivery list may be sorted by.

`createdAt` is the order the listing means — the newest record first, which is the record an operator
opens to read what just broke — and it is the instant the record was written, before the consumer
ran. `deliveredAt` is when one finally succeeded, `attemptCount` is how hard the machinery tried, and
`sequence` is the position the record carries from its event.
"""
enum EventDeliverySortField {
  createdAt
  updatedAt
  deliveredAt
  attemptCount
  sequence
}

"""
The order the delivery records are returned in. At most three keys are accepted.
"""
input EventDeliverySort {
  field: EventDeliverySortField!
  direction: SortDirection!
}

"""
The fields a delivery list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`consumerKey` is the member the dead-letter runbook groups by — one consumer failing broadly is a
regression in that consumer, many consumers failing on one event is a bad payload — and `eventId` is
the other half of that question. Every other member is a column of the record; the tenant and the
organization are applied from the credential, and a soft-deleted record is not answered at all,
which is why neither that column nor a condition on it appears here.
"""
input EventDeliveryFilter {
  id: IDFilter
  eventId: IDFilter
  consumerKey: StringFilter
  status: StringFilter
  attemptCount: NumberFilter
  deliveredAt: DateTimeFilter
  lastError: StringFilter
  partitionKey: StringFilter
  sequence: NumberFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EventDeliveryFilter!]
  or: [EventDeliveryFilter!]
  not: EventDeliveryFilter
}

"""
What a caller states when it stops one delivery record.

The reason is the operator's own words and is written onto the record's `lastError`. A record
belonging to another tenant is answered exactly as a record that does not exist.
"""
input MarkEventDeliveryDeadInput {
  """The record to stop."""
  id: ID!

  """Why it is being stopped."""
  reason: String!
}

"""
One length of meeting the organization offers, with the name it offers it under.
"""
type EventType {
  id: ID!

  """The length of the meeting, as the exact number the column stores."""
  duration: Decimal!

  """
  The unit `duration` is stated in — the organization's own spelling of minutes, hours or days.
  """
  durationUnit: String

  """The name the meeting is offered under."""
  title: String

  """What the meeting is for, for the person choosing it."""
  description: String

  """
  The employee the meeting length is recorded against, as the identifier of the employee's own row.
  """
  employeeId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live row from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of event types."""
type EventTypeEdge {
  node: EventType!
  cursor: String!
}

"""
One page of event types, with the total the filters select and the boundary cursors.
"""
type EventTypeConnection {
  nodes: [EventType!]!
  edges: [EventTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an event-type list may be sorted by.

The title is what a vocabulary of meetings is scanned by and the length is what it is chosen by — a
scheduler picks "the half-hour one" as readily as it picks a name — and the unit groups the lengths that
are stated on one scale. The two timestamps are how a change is found again. Nothing else is offered: the
remaining columns are identifiers and flags, whose questions are filters rather than orders.
"""
enum EventTypeSortField {
  createdAt
  updatedAt
  title
  duration
  durationUnit
}

"""
The order the event types are returned in. At most three keys are accepted.
"""
input EventTypeSort {
  field: EventTypeSortField!
  direction: SortDirection!
}

"""
The fields an event-type list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, because the connection narrows the rows the delivered read
returned. `title` and `description` are the two columns the delivered paginated spelling turns into a
partial match before it queries, and that narrowing survives the fold as `ilike` on the same two members.
`duration` is stated through the decimal family, which is the family the column is stored and compared
in, so a window on it — "everything between half an hour and an hour" — selects the rows the column
actually holds.

`deletedAt` is deliberately absent although the object carries it: the delivered list read answers live
rows only, so a condition on that column could only ever match the empty set. The tenant is absent for a
different reason — it is applied to the criterion from the credential — and the organization is present,
because the delivered list route hands its own criterion through and a caller may legitimately narrow to
one organization of its tenant. The facets are in neither list: the delivered list read loads no
relations, so a condition on that collection could only ever match the empty set.
"""
input EventTypeFilter {
  id: IDFilter
  title: StringFilter
  description: StringFilter
  duration: DecimalFilter
  durationUnit: StringFilter
  employeeId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [EventTypeFilter!]
  or: [EventTypeFilter!]
  not: EventTypeFilter
}

"""
What a caller states when it files a meeting length.

The organization is required because the delivered handler resolves it before it writes — it is the
organization the row is filed under, and the write does not proceed without it. The four members the
delivered create input declares are required here as well: the delivered route binds that input directly
and reads each of its members straight onto the row, and `duration` is the one of the four whose column
the store refuses to do without.
"""
input CreateEventTypeInput {
  """The name the meeting is offered under."""
  title: String!

  """What the meeting is for."""
  description: String

  """The length of the meeting, as the exact decimal the column stores."""
  duration: Decimal!

  """The unit `duration` is stated in."""
  durationUnit: String!

  """Whether the meeting length is offered at all."""
  isActive: Boolean!

  """The employee the meeting length is recorded against, by identifier."""
  employeeId: ID

  """The organization the row is filed under."""
  organizationId: ID!

  """
  The facets attached to the row, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it changes a meeting length that exists.

**A member the caller leaves out is left as it is.** The delivered edit spreads the body beside the
identifier the route read from its path and hands the result to the same write the create uses, and that
write merges what it is given onto the row the store holds rather than replacing it — so omitting
`durationUnit` keeps the unit and omitting `title` keeps the name. The delivered edit body is the entity
itself rather than the create input, and it runs no validation pipe, which is why every member here is
optional where the create requires four of them.

The organization is a member because the entity the delivered body binds carries it, and omitting it
leaves the row where it is. The identifier is the criterion and is not repeated in the payload, which is
the shape the route itself has.
"""
input UpdateEventTypeInput {
  id: ID!
  title: String
  description: String
  duration: Decimal
  durationUnit: String
  isActive: Boolean
  employeeId: ID
  organizationId: ID
  tagIds: [ID!]
}

"""One label an expense is filed under."""
type ExpenseCategory {
  id: ID!

  """The category's name, as the tenant states it."""
  name: String!

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of categories."""
type ExpenseCategoryEdge {
  node: ExpenseCategory!
  cursor: String!
}

"""
One page of categories, with the total the filters select and the boundary cursors.
"""
type ExpenseCategoryConnection {
  nodes: [ExpenseCategory!]!
  edges: [ExpenseCategoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a category list may be sorted by.

The two timestamps are how a change is found again, and the name is the vocabulary's own order — the
order an operator scans a filing list in.
"""
enum ExpenseCategorySortField {
  createdAt
  updatedAt
  name
}

"""
The order the categories are returned in. At most three keys are accepted.
"""
input ExpenseCategorySort {
  field: ExpenseCategorySortField!
  direction: SortDirection!
}

"""
The fields a category list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is: the delivered list read
answers those columns, so each one narrows rows the connection was handed. `deletedAt` is deliberately
absent although the object carries it — the delivered list read answers live rows only, so a condition
on that column could only ever match the empty set. The tenant and the organization are absent because
both are applied to the criterion from the credential rather than from the caller, so a member here
would suggest a scope decision the caller does not have.
"""
input ExpenseCategoryFilter {
  id: IDFilter
  name: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isArchived: BooleanFilter
  and: [ExpenseCategoryFilter!]
  or: [ExpenseCategoryFilter!]
  not: ExpenseCategoryFilter
}

"""What a caller states when it files a category."""
input CreateExpenseCategoryInput {
  """
  The category's name, which the tenant's vocabulary must not already carry.
  """
  name: String!

  """The organization the category is filed under."""
  organizationId: ID

  """
  The facets attached to the category, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it renames a category.

The delivered edit is the platform's own upsert: the route reads the row's identifier from the path,
spreads the body beside it and calls the same write the create reaches, so a member the caller leaves
out is left as it is — and an identifier that names no row at all is **stored as a new row** rather
than refused. A client that means "rename this category" reads it first.
"""
input UpdateExpenseCategoryInput {
  id: ID!

  """The category's name."""
  name: String!

  """The organization the category is filed under."""
  organizationId: ID

  """
  The facets attached to the category, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
One cost the organization incurred: what it cost, when it belonged to the book and what it was for.
"""
type Expense {
  id: ID!

  """The cost, in the row's own `currency`."""
  amount: Decimal!

  """
  What kind of expense this is, in the tenant's own vocabulary. Null on a row written without one.
  """
  typeOfExpense: String

  """
  The note kept with the cost, for the operator rather than for the ledger.
  """
  notes: String

  """The ISO 4217 code `amount` and `rateValue` are expressed in."""
  currency: String!

  """
  The date the cost belongs to. Null on a row written without one, which is a row no book order can place.
  """
  valueDate: DateTime

  """What the cost was incurred for."""
  purpose: String

  """
  How `rateValue` is to be read: `PERCENTAGE` when it is a share of the cost and `VALUE` when it is an
  amount in the row's own currency. Null on a row that carries no tax.
  """
  taxType: String

  """The label the tax is printed under. Null on a row that carries no tax."""
  taxLabel: String

  """
  The tax rate or tax value the row carries, read as `taxType` states. Null on a row that carries no tax.
  """
  rateValue: Decimal

  """
  The stored receipt reference, which a client resolves to a document through its own asset surface.
  """
  receipt: String

  """
  Whether the platform divides this cost among the organization's employees. A divided row is what the
  split reads below answer a share of, and the flag is what tells a share from a stored amount.
  """
  splitExpense: Boolean

  """
  The human reference for the cost, such as the supplier's document number.
  """
  reference: String

  """
  Where the cost stands in the billing cycle, as the contracts' own value: `INVOICED`, `UNINVOICED`,
  `PAID` or `NOT_BILLABLE`. Null on a row written without one.
  """
  status: String

  """
  The employee the cost is recorded against, as the identifier of the employee's own row.
  """
  employeeId: ID

  """
  The vendor the cost was paid to, as the identifier of the vendor's own row.
  """
  vendorId: ID

  """
  The category the cost is filed under, as the identifier of the category's own row.
  """
  categoryId: ID

  """
  The project the cost belongs to, as the identifier of the project's own row.
  """
  projectId: ID

  """
  The contact the cost is billed to, as the identifier of the contact's own row.
  """
  organizationContactId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live row from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""
One row of a split read: the expense, and the whole a divided cost was divided from.

The read behind `splitExpensesByEmployee` and `mySplitExpenses` answers the employee's own expenses
beside the organization's shared ones, and it divides the amount of exactly the rows the platform
splits. On a divided row the expense's own `amount` is the employee's share — the read replaces the
member before it answers — and `originalValue` and `employeeCount` carry the whole it was divided from
and the number of people it was divided among. On every other row the amount is the stored cost and
both members are null, which is why they are nullable here: the delivered answer leaves a row it does
not divide exactly as it found it, and a figure this surface fabricated for those rows would be a
share nobody computed.
"""
type ExpenseSplitRow {
  """
  The expense, as the split read answered it. Its `amount` is the employee's share on a row whose
  `splitExpense` is true, and the stored cost on every other row.
  """
  expense: Expense!

  """
  The whole the divided amount was divided from; null on a row the read did not divide.
  """
  originalValue: Decimal

  """
  How many people the divided amount was divided among; null on a row the read did not divide.
  """
  employeeCount: Int
}

"""The boundary of one page of expenses."""
type ExpenseEdge {
  node: Expense!
  cursor: String!
}

"""
One page of expenses, with the total the filters select and the boundary cursors.
"""
type ExpenseConnection {
  nodes: [Expense!]!
  edges: [ExpenseEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an expense list may be sorted by.

`valueDate` is the book's own order, which is the order a cost report is read in and the column the
delivered report read orders the same rows by; `amount` is what a list of costs is taken by; `status`
is offered because a work list is taken in status groups, and the values sort as their spelling, which
groups like with like.

The flags and the identifiers are not sort keys: a boolean or an identifier order is not an order an
expense book is read in, and the questions those columns answer are filters. `rateValue` is filtered
rather than sorted for the same reason — a tax rate is a question about one row, not an order for a
page of them.
"""
enum ExpenseSortField {
  createdAt
  updatedAt
  valueDate
  amount
  status
}

"""
The order the expenses are returned in. At most three keys are accepted.
"""
input ExpenseSort {
  field: ExpenseSortField!
  direction: SortDirection!
}

"""
The fields an expense list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is: the delivered list read
answers those columns, so each one narrows rows the connection was handed. The amounts are stated
through the decimal family, which is the family money is compared in; **the values are the row's own**,
so an equality states the amount as the read produces it (`150.5`, not a padded string), while the
range operators — which are what a book is actually searched with — need no such care. The two
patterns the paginated spelling of the list interprets are stated here in the connection's own
vocabulary: `ilike` on `notes` and `ilike` on `purpose`.

`deletedAt` is deliberately absent although the object carries it: the delivered list read answers live
rows only, so a condition on that column could only ever match the empty set, and an argument that
cannot be honoured is worse than an argument that is not offered. The tenant and the organization are
absent for a different reason — both are applied to the criterion from the credential rather than from
the caller, so a member here would suggest a scope decision the caller does not have. The relations are
absent for a third: the delivered list read loads none, so a condition on the tags, the category or
the vendor row could only ever select the empty set. Which expenses belong to one employee is the
split-read root fields' question rather than a filter's, because that read answers a figure these rows
do not carry.
"""
input ExpenseFilter {
  id: IDFilter
  valueDate: DateTimeFilter
  amount: DecimalFilter
  currency: StringFilter
  typeOfExpense: StringFilter
  notes: StringFilter
  purpose: StringFilter
  taxType: StringFilter
  taxLabel: StringFilter
  rateValue: DecimalFilter
  receipt: StringFilter
  reference: StringFilter
  splitExpense: BooleanFilter
  status: StringFilter
  employeeId: IDFilter
  vendorId: IDFilter
  categoryId: IDFilter
  projectId: IDFilter
  organizationContactId: IDFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ExpenseFilter!]
  or: [ExpenseFilter!]
  not: ExpenseFilter
}

"""
What a caller states when it records a cost.

`amount` and `currency` are the two members the delivered body validation requires: the amount column
is `numeric`, and a JSON number — which is what the REST body carries the same quantity as — is a
binary fraction that cannot hold a cent, while the currency is the code the amount is read in.

The organization is stated as an identifier and is *not* required, because the delivered validation
requires an organization only when the body names neither the organization object nor a `sentTo` — and
the object spelling is a type this schema does not declare, so the identifier is what this surface
offers and the condition stays the delivered validator's rather than being restated as a non-null
member that would refuse a body the route serves.
"""
input CreateExpenseInput {
  """The cost, in the currency below."""
  amount: Decimal!

  """The ISO 4217 code the amount and the rate are expressed in."""
  currency: String!

  """What kind of expense this is."""
  typeOfExpense: String

  """The date the cost belongs to."""
  valueDate: DateTime

  """The note kept with the cost."""
  notes: String

  """The human reference for the cost."""
  reference: String

  """What the cost was incurred for."""
  purpose: String

  """How `rateValue` is to be read."""
  taxType: String

  """The label the tax is printed under."""
  taxLabel: String

  """The tax rate or tax value the row carries."""
  rateValue: Decimal

  """The stored receipt reference."""
  receipt: String

  """
  Whether the platform divides this cost among the organization's employees.
  """
  splitExpense: Boolean

  """Where the cost stands in the billing cycle."""
  status: String

  """The organization the cost is filed under."""
  organizationId: ID

  """The employee the cost is recorded against."""
  employeeId: ID

  """The vendor the cost was paid to."""
  vendorId: ID

  """The category the cost is filed under."""
  categoryId: ID

  """The project the cost belongs to."""
  projectId: ID

  """The contact the cost is billed to."""
  organizationContactId: ID

  """
  The facets attached to the cost, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it changes a cost that exists.

**The delivered edit is the platform's own upsert, and it is reached through the same write the create
is.** The route reads the row's identifier from the path, spreads the body beside it and calls the
delivered create, so a member the caller leaves out is left as it is — the write touches the members
the body states and no others — and two consequences follow from that same fact and are worth stating
plainly:

- An identifier that names a row of another tenant is refused outright, which is the ownership guard
  the delivered write applies before it persists anything.
- An identifier that names no row at all is **stored as a new row** rather than refused, because an
  upsert is what the delivered write is. A client that means "change this cost" reads it first, or
  reaches for the mutation above only with an identifier it obtained from this surface.

`employeeId` is not a member, because the delivered edit body does not carry it — the delivered DTO
drops that member on the edit and keeps it on the create.
"""
input UpdateExpenseInput {
  id: ID!

  """The cost, in the currency below."""
  amount: Decimal!

  """The ISO 4217 code the amount and the rate are expressed in."""
  currency: String!

  """What kind of expense this is."""
  typeOfExpense: String

  """The date the cost belongs to."""
  valueDate: DateTime

  """The note kept with the cost."""
  notes: String

  """The human reference for the cost."""
  reference: String

  """What the cost was incurred for."""
  purpose: String

  """How `rateValue` is to be read."""
  taxType: String

  """The label the tax is printed under."""
  taxLabel: String

  """The tax rate or tax value the row carries."""
  rateValue: Decimal

  """The stored receipt reference."""
  receipt: String

  """
  Whether the platform divides this cost among the organization's employees.
  """
  splitExpense: Boolean

  """Where the cost stands in the billing cycle."""
  status: String

  """The organization the cost is filed under."""
  organizationId: ID

  """The vendor the cost was paid to."""
  vendorId: ID

  """The category the cost is filed under."""
  categoryId: ID

  """The project the cost belongs to."""
  projectId: ID

  """The contact the cost is billed to."""
  organizationContactId: ID

  """
  The facets attached to the cost, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
One ledger entry for one import: the archive that was taken in, and what became of it.
"""
type ImportHistory {
  id: ID!

  """
  The name of the archive that was uploaded, as the caller's own upload named it.
  """
  file: String!

  """How large the archive was, in bytes."""
  size: Int

  """
  What became of the import: `Success` when the archive was parsed into the tenant's tables, `Failed`
  when it was not, and the two reserved values the vocabulary also declares. The delivered import
  records either outcome as a row rather than refusing the request, so this member is the answer rather
  than a detail beside it.
  """
  status: String!

  """When the platform took the archive in."""
  importDate: DateTime

  """
  The URL of the stored archive, resolved by the configured storage provider as the row is loaded. It
  points at the tenant's own uploaded file; the storage key behind it is not a member.
  """
  fullUrl: String

  """
  The tenant the entry belongs to. Stamped from the credential when the import ran.
  """
  tenantId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of ledger entries."""
type ImportHistoryEdge {
  node: ImportHistory!
  cursor: String!
}

"""
One page of ledger entries, with the total the filters select and the boundary cursors.
"""
type ImportHistoryConnection {
  nodes: [ImportHistory!]!
  edges: [ImportHistoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a ledger list may be sorted by."""
enum ImportHistorySortField {
  importDate
  createdAt
  updatedAt
  file
  size
  status
}

"""
The order the ledger list is returned in. At most three keys are accepted.
"""
input ImportHistorySort {
  field: ImportHistorySortField!
  direction: SortDirection!
}

"""
The fields a ledger list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

`status: { eq: "Failed" }` is how an operator asks what did not come in. `path` is not filterable for the
same reason it is not a member — a condition on a storage key is a comparison against a value the
delivered projection withholds — and `fullUrl` is not filterable because it is not stored: it is resolved
from the key as the row is loaded, so the delivered read has no criterion for it.
"""
input ImportHistoryFilter {
  id: IDFilter
  file: StringFilter
  size: NumberFilter
  status: StringFilter
  importDate: DateTimeFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ImportHistoryFilter!]
  or: [ImportHistoryFilter!]
  not: ImportHistoryFilter
}

"""One row of another domain, marked as a favorite of one employee."""
type Favorite {
  id: ID!

  """
  The name of the domain the marked row belongs to, as the contract's own vocabulary states it.
  """
  entity: String!

  """
  The marked row, by identifier. The row itself is not joined by this read.
  """
  entityId: ID!

  """
  The employee whose list the marker sits in, by identifier. An organization-level favorite has none,
  which is what the delivered create writes when the caller has no employee identity of its own. The
  employee row is not joined by this read.
  """
  employeeId: ID

  """The tenant the marker belongs to, by identifier."""
  tenantId: ID

  """The organization whose list the marker sits in, by identifier."""
  organizationId: ID

  """
  Whether the marker is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the marker is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the marker was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the marker was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the row was marked."""
  createdAt: DateTime

  """When the row was last changed."""
  updatedAt: DateTime
}

"""The boundary of one page of favorites."""
type FavoriteEdge {
  node: Favorite!
  cursor: String!
}

"""
One page of favorites, with the total the filters select and the boundary cursors.
"""
type FavoriteConnection {
  nodes: [Favorite!]!
  edges: [FavoriteEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a favorite list may be sorted by."""
enum FavoriteSortField {
  id
  entity
  entityId
  employeeId
  createdAt
  updatedAt
  deletedAt
}

"""
The order the favorites are returned in. At most three keys are accepted.
"""
input FavoriteSort {
  field: FavoriteSortField!
  direction: SortDirection!
}

"""
The fields a favorite list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the favorite row, because the delivered list read answers the row itself
and joins only the relations a REST caller names in its query string — which this surface never names.
A condition on the marked row or on the employee behind `employeeId` is therefore not a member: neither
is loaded by the read this connection is built over, and a condition on a relation that is never
loaded could only ever match the empty set.

The tenant comes from the credential on every read, so `tenantId` narrows the row's own column rather
than restating a scope decision the caller does not have. `deletedAt` is a member for the same reason
the object type carries it — it is the column the withdrawal routes write — and because the delivered
list read answers live rows only, `isNull: true` selects every row this connection can return.
"""
input FavoriteFilter {
  id: IDFilter
  entity: StringFilter
  entityId: IDFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [FavoriteFilter!]
  or: [FavoriteFilter!]
  not: FavoriteFilter
}

"""
What a caller states when it marks a row as a favorite.

`entity` and `entityId` are required because they are what the marker names: the delivered column pair
is non-null, and a marker that named nothing could not be de-duplicated against the row it marks — the
delivered create looks for an existing marker on the same entity, entity identifier, organization and
employee before it files a new one.

`employeeId` is optional, and omitting it is a statement rather than an omission. The delivered create
prefers the caller's own employee record from the credential and falls back to the member a body
states; a caller with no employee identity at all — an administrator acting for the organization
rather than for a person — files a marker that belongs to no employee, which is what an
organization-level favorite is. The de-duplication pins that case to the rows whose employee column is
absent, so an organization-level marker is never the same row as somebody's personal one.

There is no member for the tenant: the delivered create reads it from the credential and stamps it,
overwriting whatever a body states, so a member here would promise a scope the write refuses.
"""
input CreateFavoriteInput {
  """The domain the marked row belongs to."""
  entity: String!

  """The row to mark, by identifier."""
  entityId: ID!

  """
  The employee whose list to file the marker in. Omit for an organization-level marker.
  """
  employeeId: ID

  """The organization whose list to file the marker in."""
  organizationId: ID
}

"""
What a caller states when it changes a marker that exists.

**A member the caller leaves out is left as it is.** The delivered edit is a partial update rather
than a replacement: the service reads the row the identifier names — which is how a marker of another
tenant, or one that is not there, is answered with the miss rather than with a write — and then
persists only the members a body states. Stating this here is what keeps a client from reading
"absent" as "written as the column's default".

The identifier is required and is stated because the delivered route carries it in the path, and the
same member is what the write is keyed on.
"""
input UpdateFavoriteInput {
  id: ID!

  """The domain the marked row belongs to."""
  entity: String

  """The row to mark, by identifier."""
  entityId: ID

  """The employee whose list the marker sits in."""
  employeeId: ID

  """The organization whose list the marker sits in."""
  organizationId: ID
}

"""
One catalogue entry: a capability the platform can describe and a tenant can switch.

The row is installation-wide — it has no tenant and no organization column — so the same entry is
answered to every tenant, and the members it carries are descriptive only. Whether the capability is
*served* is a `FeatureToggle` row, and that row is per tenant and per organization.

Deliberately absent, each for its own reason:

- `isEnabled` is a virtual column. Nothing persists it, the delivered list read does not populate it,
  and the delivered toggle write does not write it, so a member here would be `null` on every row this
  surface answers while appearing to be the answer. The enablement is `FeatureToggle.isEnabled`.
- `imageUrl` is a virtual column too, derived from `image` for the administration UI, and it is
  likewise not populated by the read this surface mirrors. `image` carries the file name.
- `parent`, `children` and `featureOrganizations` are relations. A REST caller loads them by naming
  them in `relations`; the GraphQL reads pass none, so the members would always be absent. The roots
  of the catalogue are reachable as `features(filter: { parentId: { isNull: true } })` — the delivered
  parent route stated as the filter it is — and the toggles of an entry as
  `featureToggles(filter: { featureId: { eq: … } })`.
- `deletedAt` is absent because no delivered route withdraws a catalogue entry: the reads answer live
  rows and nothing on this surface writes that column.
"""
type Feature {
  id: ID!

  """Human readable name, shown wherever features are listed."""
  name: String!

  """
  The feature code. Upper snake case, `FEATURE_` prefixed — the same text `FeatureToggle` rows and
  `FeatureFlagGuard` compare, and the text `FEATURE_GRAPHQL` gates this surface with.
  """
  code: String!

  """One line explaining what the flag enables."""
  description: String

  """
  Artwork file name, or null. The delivered catalogue ships no artwork for the commerce codes, so
  this is null on every entry introduced by the programme and set only on the ones it reuses.
  """
  image: String

  """
  Where an administrator is taken when the feature is selected. NOT NULL on the row.
  """
  link: String!

  """Presentation only: `info` / `primary` / `success` / `warning`."""
  status: String

  """Presentation only."""
  icon: String

  """
  Whether the capability is a paid one, which the catalogue marks for the administration UI.
  """
  isPaid: Boolean

  """The parent entry, for a catalogue that is a tree. Null on a root."""
  parentId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
One tenant's answer for one catalogue entry, at one scope.

**The scope members are the whole point of this type**, so each is stated:

- `tenantId` — the tenant whose answer this is. It is stamped from the credential by the toggle write
  and is never stated by a caller, so a row cannot be written into another tenant's scope.
- `organizationId` — **the second, more specific scope**. `null` means the row is the tenant-wide
  answer, which is the row a toggle written without an organization produces and the row a tenant
  provisioned from scratch is given. Set, it is that one organization's answer. A resolution layers
  the two, most specific first, and falls back to the deployment's own configured state when neither
  exists — which is why a missing row is never an error.
- `featureId` — which catalogue entry is switched. The entry itself is reachable as
  `features(filter: { id: { eq: … } })`; the relation is not a member here for the reason the
  catalogue's own header states.

`isEnabled` is the answer: whether the capability is served for this scope.
"""
type FeatureToggle {
  id: ID!

  """The catalogue entry this row switches."""
  featureId: ID!

  """Whether the capability is served for this scope."""
  isEnabled: Boolean!

  """The tenant whose answer this row is."""
  tenantId: ID

  """
  The organization the row is scoped to, or null for the tenant-wide row. A tenant-wide row is the
  answer for every organization of the tenant that has no row of its own.
  """
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of catalogue entries."""
type FeatureEdge {
  node: Feature!
  cursor: String!
}

"""
One page of catalogue entries, with the total the filters select and the boundary cursors.
"""
type FeatureConnection {
  nodes: [Feature!]!
  edges: [FeatureEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The boundary of one page of toggle rows."""
type FeatureToggleEdge {
  node: FeatureToggle!
  cursor: String!
}

"""
One page of toggle rows, with the total the filters select and the boundary cursors.
"""
type FeatureToggleConnection {
  nodes: [FeatureToggle!]!
  edges: [FeatureToggleEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a catalogue list may be sorted by."""
enum FeatureSortField {
  createdAt
  updatedAt
  name
  code
  status
}

"""
The order the catalogue entries are returned in. At most three keys are accepted.
"""
input FeatureSort {
  field: FeatureSortField!
  direction: SortDirection!
}

"""The fields a catalogue list may be narrowed by."""
input FeatureFilter {
  id: IDFilter
  name: StringFilter
  code: StringFilter
  description: StringFilter
  image: StringFilter
  link: StringFilter
  status: StringFilter
  icon: StringFilter
  isPaid: BooleanFilter
  parentId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [FeatureFilter!]
  or: [FeatureFilter!]
  not: FeatureFilter
}

"""The fields a toggle list may be sorted by."""
enum FeatureToggleSortField {
  createdAt
  updatedAt
  isEnabled
}

"""
The order the toggle rows are returned in. At most three keys are accepted.
"""
input FeatureToggleSort {
  field: FeatureToggleSortField!
  direction: SortDirection!
}

"""
The fields a toggle list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`tenantId` is filterable because the delivered list route binds one — the guard has already refused a
tenant that is not the caller's, so the member narrows what the credential may read and never widens
it. `organizationId` and `featureId` are the route's own two parameters and they are stated here as
what they are: filters over the list, not root fields of their own.
"""
input FeatureToggleFilter {
  id: IDFilter
  featureId: IDFilter
  isEnabled: BooleanFilter
  tenantId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [FeatureToggleFilter!]
  or: [FeatureToggleFilter!]
  not: FeatureToggleFilter
}

"""
What a caller states when it switches a capability on or off for a scope.

The write is an upsert, which is why there is one input and not a create and an update beside each
other: the route writes the row if the scope has none and rewrites it if it has one, so a caller
states the answer it wants and not which operation it is performing.

`tenantId` is deliberately **not** a member. The delivered handler reads the tenant from the
credential and stamps it onto the row, overwriting whatever a body states — so a member here would
promise a scope the write refuses. `organizationId` is stated because that is the scope the caller
chooses: omit it for the tenant-wide answer, state it for one organization's.
"""
input UpdateFeatureToggleInput {
  """The catalogue entry to switch."""
  featureId: ID!

  """Whether the capability is served for this scope."""
  isEnabled: Boolean!

  """The organization to switch it for. Omit for the tenant-wide answer."""
  organizationId: ID
}

"""
One toggle definition, as the deployment's own toggle source declares it.

This is not a stored row: it is what the configured toggle provider answers for the codes this
platform knows, filtered to them. It therefore has no identifier — an entry is named by its `name`,
which is the code — and no cursor can be built over it, which is why the root field that answers it
is a bare list rather than a connection: the connection contract names every row by its identifier,
and inventing one here would make a cursor mean something the provider never said.
"""
type FeatureToggleDefinition {
  """The toggle name, which is the feature code."""
  name: String!
  description: String

  """
  The provider's own toggle type — `release`, `experiment`, `kill-switch` and so on. Carried as its
  value rather than declared as a schema enum: the vocabulary belongs to the toggle provider, not to
  this domain, and a second domain that reads the same provider would then collide with this file
  rather than reuse it.
  """
  type: String!

  """The provider project the toggle belongs to."""
  project: String!

  """
  Whether the provider itself considers the toggle on. It is not the tenant's answer — see `FeatureToggle`.
  """
  enabled: Boolean!

  """Whether the provider has marked the toggle stale."""
  stale: Boolean!

  """Whether the provider records impression data for the toggle."""
  impressionData: Boolean!

  """The provider's strategies, as the document it declares them in."""
  strategies: JSON!

  """The provider's variants, as the document it declares them in."""
  variants: JSON!
}

"""One organization's policy for the objective programme."""
type GoalGeneralSetting {
  id: ID!

  """How many objectives an organization may hold at once."""
  maxObjectives: Int!

  """How many key results one objective may carry."""
  maxKeyResults: Int!

  """
  Whether an employee may open an objective without an administrator doing it for them.
  """
  employeeCanCreateObjective: Boolean!

  """
  Who may own an objective. The value set belongs to the domain's own contract, so it is carried as
  its value rather than declared here as an enum.
  """
  canOwnObjectives: String!

  """
  Who may own a key result. Carried as its value for the same reason `canOwnObjectives` is.
  """
  canOwnKeyResult: String!

  """Whether the organization tracks key results as measures."""
  krTypeKPI: Boolean!

  """Whether the organization tracks key results as pieces of work."""
  krTypeTask: Boolean!

  """
  When the policy was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the policy belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the policy is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of policies."""
type GoalGeneralSettingEdge {
  node: GoalGeneralSetting!
  cursor: String!
}

"""
One page of policies, with the total the filters select and the boundary cursors.
"""
type GoalGeneralSettingConnection {
  nodes: [GoalGeneralSetting!]!
  edges: [GoalGeneralSettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a policy list may be sorted by."""
enum GoalGeneralSettingSortField {
  createdAt
  updatedAt
  maxObjectives
  maxKeyResults
}

"""
The order the policies are returned in. At most three keys are accepted.
"""
input GoalGeneralSettingSort {
  field: GoalGeneralSettingSortField!
  direction: SortDirection!
}

"""
The fields a policy list may be narrowed by. Every member is a column of the setting row.
"""
input GoalGeneralSettingFilter {
  id: IDFilter
  maxObjectives: NumberFilter
  maxKeyResults: NumberFilter
  employeeCanCreateObjective: BooleanFilter
  canOwnObjectives: StringFilter
  canOwnKeyResult: StringFilter
  krTypeKPI: BooleanFilter
  krTypeTask: BooleanFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [GoalGeneralSettingFilter!]
  or: [GoalGeneralSettingFilter!]
  not: GoalGeneralSettingFilter
}

"""
What a caller states when it files a policy.

The delivered DTO requires the objective ceiling and leaves the rest optional at validation, while
every one of these columns is non-nullable — so a body that omits one is refused by the store rather
than by validation, and the members are optional here for the same reason they are optional there. The
tenant is never a member — the service stamps it from the credential — and `organizationId` is,
because it is the row's own column and the delivered validation does not strip it.
"""
input CreateGoalGeneralSettingInput {
  maxObjectives: Int!
  maxKeyResults: Int
  employeeCanCreateObjective: Boolean
  canOwnObjectives: String
  canOwnKeyResult: String
  krTypeKPI: Boolean
  krTypeTask: Boolean
  organizationId: ID
}

"""
What a caller states when it changes a policy.

**A member that is absent is left as it is.** The delivered edit is an update-through-create: the
stated members are merged onto the row the store holds, and an identifier that names a row of another
tenant is refused by the service rather than overwritten.
"""
input UpdateGoalGeneralSettingInput {
  id: ID!
  maxObjectives: Int
  maxKeyResults: Int
  employeeCanCreateObjective: Boolean
  canOwnObjectives: String
  canOwnKeyResult: String
  krTypeKPI: Boolean
  krTypeTask: Boolean
  organizationId: ID
}

"""One catalogue entry a measure can be authored from."""
type GoalKPITemplate {
  id: ID!

  """The suggested measure's name."""
  name: String!

  """What the suggested measure is for."""
  description: String

  """
  The kind of metric the entry suggests. The value set belongs to the domain's own contract, so it is
  carried as its value rather than declared here as an enum.
  """
  type: String!

  """The unit the suggested measure is read in, as text."""
  unit: String

  """
  How the reading is compared with the target: at least, at most, equal to.
  """
  operator: String!

  """The reading the entry suggests starting from, when it suggests one."""
  currentValue: Int

  """The reading the entry suggests aiming at, when it suggests one."""
  targetValue: Int

  """
  When the entry was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the entry belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the entry is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of catalogue entries."""
type GoalKPITemplateEdge {
  node: GoalKPITemplate!
  cursor: String!
}

"""
One page of catalogue entries, with the total the filters select and the boundary cursors.
"""
type GoalKPITemplateConnection {
  nodes: [GoalKPITemplate!]!
  edges: [GoalKPITemplateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a catalogue list may be sorted by."""
enum GoalKPITemplateSortField {
  createdAt
  updatedAt
  name
  type
  currentValue
  targetValue
}

"""
The order the catalogue is returned in. At most three keys are accepted.
"""
input GoalKPITemplateSort {
  field: GoalKPITemplateSortField!
  direction: SortDirection!
}

"""
The fields a catalogue list may be narrowed by. Every member is a column of the template row.
"""
input GoalKPITemplateFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  type: StringFilter
  unit: StringFilter
  operator: StringFilter
  currentValue: NumberFilter
  targetValue: NumberFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [GoalKPITemplateFilter!]
  or: [GoalKPITemplateFilter!]
  not: GoalKPITemplateFilter
}

"""
What a caller states when it files a catalogue entry.

The delivered creation takes the row's own shape rather than a DTO, so the members the store requires
are the members the columns require: a name, the metric, and the operator. The two readings are
optional because the columns are. The tenant is never a member — the service stamps it from the
credential — and `organizationId` is, because it is the row's own column and the delivered validation
does not strip it.
"""
input CreateGoalKPITemplateInput {
  name: String!
  description: String
  type: String!
  unit: String
  operator: String!
  currentValue: Int
  targetValue: Int
  organizationId: ID
}

"""
What a caller states when it changes a catalogue entry.

**A member that is absent is left as it is.** The delivered edit is the CRUD base's partial column
update rather than a replacement: it reads the row first — so an entry of another tenant, or one that
is not there, is a miss rather than a write — and then updates the stated columns and no others.
"""
input UpdateGoalKPITemplateInput {
  id: ID!
  name: String
  description: String
  type: String
  unit: String
  operator: String
  currentValue: Int
  targetValue: Int
  organizationId: ID
}

"""One measure a key result can track."""
type GoalKPI {
  id: ID!

  """The measure's name."""
  name: String!

  """What the measure is for."""
  description: String

  """
  The kind of metric the measure is: a number, a percentage or a currency figure. The value set
  belongs to the domain's own contract, so it is carried as its value rather than declared here as an
  enum.
  """
  type: String!

  """
  The unit the measure is read in, as text: a count, a score, a ratio, an abbreviation.
  """
  unit: String

  """
  How the reading is compared with the target: at least, at most, equal to.
  """
  operator: String!

  """The reading as it stands."""
  currentValue: Int!

  """The reading the measure is aiming at."""
  targetValue: Int!

  """The employee who leads the measure, by identifier."""
  leadId: ID

  """
  When the measure was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the measure belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the measure is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of measures."""
type GoalKPIEdge {
  node: GoalKPI!
  cursor: String!
}

"""
One page of measures, with the total the filters select and the boundary cursors.
"""
type GoalKPIConnection {
  nodes: [GoalKPI!]!
  edges: [GoalKPIEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a measure list may be sorted by."""
enum GoalKPISortField {
  createdAt
  updatedAt
  name
  type
  currentValue
  targetValue
}

"""
The order the measures are returned in. At most three keys are accepted.
"""
input GoalKPISort {
  field: GoalKPISortField!
  direction: SortDirection!
}

"""
The fields a measure list may be narrowed by.

Every member is a column of the KPI row, which is what the connection protocol evaluates.
"""
input GoalKPIFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  type: StringFilter
  unit: StringFilter
  operator: StringFilter
  currentValue: NumberFilter
  targetValue: NumberFilter
  leadId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [GoalKPIFilter!]
  or: [GoalKPIFilter!]
  not: GoalKPIFilter
}

"""
What a caller states when it files a measure.

The delivered creation takes the row's own shape rather than a DTO — the controller declares no
validation pipe on that route — so the members the store requires are the members the columns
require: a name, the metric it is read as, the operator, the reading and the target. `leadId` and
`organizationId` are the two relations the row carries, and the tenant is never a member because the
service stamps it from the credential.
"""
input CreateGoalKPIInput {
  name: String!
  description: String
  type: String!
  unit: String
  operator: String!
  currentValue: Int!
  targetValue: Int!
  leadId: ID
  organizationId: ID
}

"""
What a caller states when it changes a measure.

**A member that is absent is left as it is.** The delivered edit is the delivered creation called with
the stated identifier — the controller's route is an update-through-create, because the write has to
go through the save that a partial column update would bypass — so the stated members are merged onto
the row the store holds. An identifier that names no row of the caller's tenant is an insert rather
than a miss, which is the delivered route's own behaviour and not something this surface narrows.
"""
input UpdateGoalKPIInput {
  id: ID!
  name: String
  description: String
  type: String
  unit: String
  operator: String
  currentValue: Int
  targetValue: Int
  leadId: ID
  organizationId: ID
}

"""One catalogue entry an objective can be authored from."""
type GoalTemplate {
  id: ID!

  """The name of the shape this entry offers."""
  name: String!

  """
  The level the objective is normally taken at. The value set belongs to the domain's own contract,
  so it is carried as its value rather than declared here as an enum.
  """
  level: String!

  """
  The category the catalogue files the objective under. Carried as its value for the same reason
  `level` is.
  """
  category: String!

  """
  When the entry was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the entry belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the entry is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of catalogue entries."""
type GoalTemplateEdge {
  node: GoalTemplate!
  cursor: String!
}

"""
One page of catalogue entries, with the total the filters select and the boundary cursors.
"""
type GoalTemplateConnection {
  nodes: [GoalTemplate!]!
  edges: [GoalTemplateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a catalogue list may be sorted by."""
enum GoalTemplateSortField {
  createdAt
  updatedAt
  name
  level
  category
}

"""
The order the catalogue is returned in. At most three keys are accepted.
"""
input GoalTemplateSort {
  field: GoalTemplateSortField!
  direction: SortDirection!
}

"""
The fields a catalogue list may be narrowed by. Every member is a column of the template row.
"""
input GoalTemplateFilter {
  id: IDFilter
  name: StringFilter
  level: StringFilter
  category: StringFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [GoalTemplateFilter!]
  or: [GoalTemplateFilter!]
  not: GoalTemplateFilter
}

"""
What a caller states when it files a catalogue entry.

The delivered creation takes the row's own shape rather than a DTO, so the members the store requires
are the members the columns require: a name, a level and a category. The tenant is never a member —
the service stamps it from the credential — and `organizationId` is, because it is the row's own
column and the delivered validation does not strip it.

The key-result templates the entry holds are not members. They are the sibling catalogue's rows, and
they name this entry by their own `goalId`, so they are filed from their own surface.
"""
input CreateGoalTemplateInput {
  name: String!
  level: String!
  category: String!
  organizationId: ID
}

"""
What a caller states when it changes a catalogue entry.

**A member that is absent is left as it is.** The delivered edit is the CRUD base's partial column
update rather than a replacement: it reads the row first — so an entry of another tenant, or one that
is not there, is a miss rather than a write — and then updates the stated columns and no others.
"""
input UpdateGoalTemplateInput {
  id: ID!
  name: String
  level: String
  category: String
  organizationId: ID
}

"""One period an objective can be set for."""
type GoalTimeFrame {
  id: ID!

  """The period's name: the quarter, the half, the year."""
  name: String!

  """
  The state the period is in. The value set belongs to the domain's own contract, so it is carried as
  its value rather than declared here as an enum.
  """
  status: String!

  """When the period opens."""
  startDate: DateTime!

  """When the period closes."""
  endDate: DateTime!

  """
  When the period was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the period belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the period is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of periods."""
type GoalTimeFrameEdge {
  node: GoalTimeFrame!
  cursor: String!
}

"""
One page of periods, with the total the filters select and the boundary cursors.
"""
type GoalTimeFrameConnection {
  nodes: [GoalTimeFrame!]!
  edges: [GoalTimeFrameEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a period list may be sorted by."""
enum GoalTimeFrameSortField {
  createdAt
  updatedAt
  name
  status
  startDate
  endDate
}

"""
The order the periods are returned in. At most three keys are accepted.
"""
input GoalTimeFrameSort {
  field: GoalTimeFrameSortField!
  direction: SortDirection!
}

"""
The fields a period list may be narrowed by. Every member is a column of the time-frame row.
"""
input GoalTimeFrameFilter {
  id: IDFilter
  name: StringFilter
  status: StringFilter
  startDate: DateTimeFilter
  endDate: DateTimeFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [GoalTimeFrameFilter!]
  or: [GoalTimeFrameFilter!]
  not: GoalTimeFrameFilter
}

"""
What a caller states when it opens a period.

The delivered DTO requires all four of these, and so does this input: a period with no name, no state
or no boundary is not a row the delivered validation accepts. The tenant is never a member — the
service stamps it from the credential — and `organizationId` is, because it is the row's own column
and the delivered validation does not strip it.
"""
input CreateGoalTimeFrameInput {
  name: String!
  status: String!
  startDate: DateTime!
  endDate: DateTime!
  organizationId: ID
}

"""
What a caller states when it changes a period.

**A member that is absent is left as it is.** The delivered edit is an update-through-create: the
stated members are merged onto the row the store holds, and the identifiers the row carries are
untouched. See `CreateGoalTimeFrameInput` for why all four members are stated here too — the input is
the delivered DTO's own shape, made partial by the identifier the path carries.
"""
input UpdateGoalTimeFrameInput {
  id: ID!
  name: String
  status: String
  startDate: DateTime
  endDate: DateTime
  organizationId: ID
}

"""One objective an organization committed to."""
type Goal {
  id: ID!

  """The objective's name."""
  name: String!

  """What the objective is for."""
  description: String

  """
  When the objective is due. The column is text, so this is carried as text; see the note above the
  type.
  """
  deadline: String!

  """
  The level the objective was taken at: the organization, the team or the individual. The value set
  belongs to the domain's own contract, so it is carried as its value rather than declared here as an
  enum.
  """
  level: String!

  """
  How far the objective has come, as the whole number of percent the column holds.
  """
  progress: Int!

  """The team that owns the objective, by identifier."""
  ownerTeamId: ID

  """The employee who owns the objective, by identifier."""
  ownerEmployeeId: ID

  """The employee who leads the objective, by identifier."""
  leadId: ID

  """The key result this objective is aligned to, by identifier."""
  alignedKeyResultId: ID

  """The strategic initiative this objective supports, by identifier."""
  organizationStrategicInitiativeId: ID

  """
  When the objective was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two lifecycle moves this resource serves — and this column is the whole of what they
  write, so without it a caller could not tell a live row from a withdrawn one on the answer to the
  write that withdrew it.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the objective belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the objective is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of objectives."""
type GoalEdge {
  node: Goal!
  cursor: String!
}

"""
One page of objectives, with the total the filters select and the boundary cursors.
"""
type GoalConnection {
  nodes: [Goal!]!
  edges: [GoalEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an objective list may be sorted by."""
enum GoalSortField {
  createdAt
  updatedAt
  name
  level
  progress
}

"""
The order the objectives are returned in. At most three keys are accepted.
"""
input GoalSort {
  field: GoalSortField!
  direction: SortDirection!
}

"""
The fields an objective list may be narrowed by.

Every member is a column of the goal row, because that is what the connection protocol evaluates. The
key results under an objective are **not** here: they are the sibling aggregate's rows and a key
result names its objective by `goalId`, so they are read from their own surface with that identifier
in `filter`.
"""
input GoalFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  deadline: StringFilter
  level: StringFilter
  progress: NumberFilter
  ownerTeamId: IDFilter
  ownerEmployeeId: IDFilter
  leadId: IDFilter
  alignedKeyResultId: IDFilter
  organizationStrategicInitiativeId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [GoalFilter!]
  or: [GoalFilter!]
  not: GoalFilter
}

"""
What a caller states when it files an objective.

The tenant is never a member: it is stamped from the credential by the delivered write, which is the
one value a caller must not be able to misstate. `organizationId` is a member even though the
delivered body's own DTO does not declare it, because the delivered validation pipe does not strip
members it does not know and the column is the row's own; it is optional rather than required, since
a body that omits it is one the delivered route accepts.

`deadline`, `level` and `progress` are members the delivered DTO marks optional while the columns
behind them are not nullable, so a body that omits one is refused by the store rather than by
validation. They are optional here for the same reason they are optional there: a surface that
demanded them would refuse a body the delivered route accepts.
"""
input CreateGoalInput {
  name: String!
  description: String
  deadline: String
  level: String
  progress: Int
  ownerTeamId: ID
  ownerEmployeeId: ID
  leadId: ID
  alignedKeyResultId: ID
  organizationStrategicInitiativeId: ID
  organizationId: ID
}

"""
What a caller states when it edits an objective that exists.

**A member that is absent is left as it is.** The delivered edit is the delivered creation called with
the verified identifier: the row is read first, so an objective of another tenant or one that is not
there is a miss rather than a write, and the stated members are then merged onto the row the store
holds. This is therefore a partial write rather than a replacement, and stating so is what keeps a
client from reading "absent" as "cleared".
"""
input UpdateGoalInput {
  id: ID!
  name: String
  description: String
  deadline: String
  level: String
  progress: Int
  ownerTeamId: ID
  ownerEmployeeId: ID
  leadId: ID
  alignedKeyResultId: ID
  organizationStrategicInitiativeId: ID
  organizationId: ID
}

"""
An instant, served as RFC 3339 in UTC with milliseconds: "2026-09-21T10:15:30.000Z", the form the REST
surface writes for the same value. Send an RFC 3339 date-time, or a date.
"""
scalar DateTime

"""
An exact decimal, served as a string in plain notation with six fractional digits, or up to twelve
when the value has more: "20.010000". Never read it as a Float. Send it as a string; a JSON number is
accepted when it has at most fourteen integer and twelve fractional digits.
"""
scalar Decimal

"""An arbitrary JSON document, carried through unchanged."""
scalar JSON

"""One event of the platform catalogue, as a subscriber receives it."""
type EventEnvelope {
  """
  The event identity: stable, and the idempotency key a consumer applies its effects under.
  """
  eventId: ID!

  """The event name, `<aggregate>.<action>`."""
  eventName: String!

  """When the fact happened, as opposed to when it was dispatched."""
  occurredAt: DateTime!

  """The aggregate that changed."""
  aggregate: EventAggregate!

  """
  Monotonic within the tenant. It correlates an event with the delivery ledger; it is not a cursor,
  because a subscription does not resume.
  """
  sequence: Int

  """
  The event body: the catalogued payload of that event, never an entity dump.
  """
  data: JSON!
}

"""The aggregate an event belongs to."""
type EventAggregate {
  """The aggregate type in PascalCase, for example `Order`."""
  type: String!

  """The aggregate instance."""
  id: ID!
}

"""
The boundary of a page.

One shared type, so a client learns the shape once and a cursor means the same thing on every
connection. `startCursor` and `endCursor` are opaque: they are produced and consumed by the same
codec the REST surface uses, so a cursor obtained over one surface resumes on the other.
"""
type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

"""
An expected, caller-correctable outcome of a mutation.

This is a payload member, not an error transport: a business rejection is reported here with the
operation succeeding, and only a request the caller could not have made correctly becomes a GraphQL
error. `path` names the input member the outcome belongs to, as a field path.
"""
type UserError {
  """
  The stable platform error code, the same string the REST surface returns for the condition.
  """
  code: String!

  """A message written for the caller."""
  message: String!

  """The input members this outcome belongs to, as a field path."""
  path: [String!]

  """Structured detail: which values were refused and why."""
  details: JSON
}

"""How far a durable multi-step operation has come."""
enum OperationStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  COMPENSATING
  COMPENSATED
  CANCELED
}

"""How far one step of a durable operation has come."""
enum OperationStepStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  SKIPPED
  COMPENSATING
  COMPENSATED
  COMPENSATION_FAILED
}

"""
A durable multi-step operation: a write that spans more than one step, whose progress survives a
restart and whose partial effects are undone when a later step fails.
"""
type Operation {
  id: ID!

  """
  The operation type, for example `CHECKOUT_COMPLETE`. Declared as a string rather than an enum
  because an operation type is registered by the package that owns the workflow, and a closed enum
  would make a new workflow a schema change.
  """
  type: String!
  status: OperationStatus!

  """
  The aggregate type the operation acts on, or null when the operation is not about one.
  Nullable because the delivered column is: the exclusivity rule is a *partial* unique index —
  `WHERE "aggregateType" IS NOT NULL AND "status" IN ('PENDING','RUNNING','COMPENSATING')` — so an
  operation that spans several aggregates, or that runs for the platform rather than for a row, is a
  stored operation with no aggregate at all. Declaring the member non-null would make reading such a
  row a schema violation, which is the one thing a nullable column must never become.
  """
  aggregateType: String

  """
  The aggregate instance the operation acts on, or null for the same reason as `aggregateType`.
  """
  aggregateId: ID

  """Completion in the range 0 to 1."""
  progress: Float!
  steps: [OperationStep!]!
  startedAt: DateTime
  finishedAt: DateTime

  """When the operation is abandoned if it has not finished."""
  deadlineAt: DateTime

  """Why it failed, when it did."""
  failureReason: String
}

"""One step of a durable operation."""
type OperationStep {
  id: ID!

  """The step name from the operation definition."""
  name: String!
  status: OperationStepStatus!

  """How many times this step has been attempted."""
  attempt: Int!
  startedAt: DateTime
  finishedAt: DateTime

  """
  The state of this step's compensating action, when the operation had to undo its work.
  """
  compensationStatus: OperationStepStatus
}

"""Equality and set membership on an identifier."""
input IDFilter {
  """The value is equal to this."""
  eq: ID

  """
  The value is not equal to this. Rows where the column is absent are excluded.
  """
  ne: ID

  """The value is one of these."""
  in: [ID!]

  """
  The value is none of these. Rows where the column is absent are excluded.
  """
  nin: [ID!]

  """
  Whether the column is absent. `true` selects absent values, `false` selects present ones.
  """
  isNull: Boolean
}

"""Comparison and pattern matching on text."""
input StringFilter {
  eq: String
  ne: String
  in: [String!]
  nin: [String!]

  """A case-sensitive pattern. `%` and `_` are wildcards."""
  like: String

  """A case-insensitive pattern. `%` and `_` are wildcards."""
  ilike: String
  isNull: Boolean

  """A value contained in a document or array column."""
  contains: [String!]
}

"""Comparison on a whole number."""
input NumberFilter {
  eq: Int
  ne: Int
  in: [Int!]
  nin: [Int!]
  gt: Int
  gte: Int
  lt: Int
  lte: Int

  """Exactly two bounds, inclusive at both ends, lower first."""
  between: [Int!]
  isNull: Boolean
}

"""
Comparison on an exact decimal, such as a money amount or a rate.

Values are decimal strings rather than floating-point numbers, for the same reason the output is:
`100.10` has no exact binary representation, and a filter that rounds is a filter that returns the
wrong rows.
"""
input DecimalFilter {
  eq: Decimal
  ne: Decimal
  in: [Decimal!]
  nin: [Decimal!]
  gt: Decimal
  gte: Decimal
  lt: Decimal
  lte: Decimal

  """Exactly two bounds, inclusive at both ends, lower first."""
  between: [Decimal!]
  isNull: Boolean
}

"""Comparison on a flag."""
input BooleanFilter {
  eq: Boolean
  ne: Boolean
  isNull: Boolean
}

"""Comparison on an instant."""
input DateTimeFilter {
  eq: DateTime
  ne: DateTime
  gt: DateTime
  gte: DateTime
  lt: DateTime
  lte: DateTime

  """Exactly two bounds, inclusive at both ends, earlier first."""
  between: [DateTime!]
  isNull: Boolean
}

"""Comparison on a document column."""
input JSONFilter {
  eq: JSON
  ne: JSON
  contains: [String!]
  isNull: Boolean
}

"""
A tenant filter, with exactly one member.

The tenant a read runs under comes from the credential, never from a filter, so there is nothing for
a caller to choose here: the single member exists so that a resource whose rows are shared across
organizations inside one tenant can say so in its generated input, and its value is checked against
the caller's own tenant before it is used.
"""
input TenantFilter {
  """
  The tenant the caller is acting in. It must be the caller's own tenant.
  """
  id: ID!
}

"""
Cursor pagination. `first`/`after` walk forwards, `last`/`before` walk backwards, and a request that
states both directions is refused.
"""
input PageInput {
  """The page size, walking forwards from `after`."""
  first: Int

  """Resume after this cursor. Exclusive."""
  after: String

  """The page size, walking backwards from `before`."""
  last: Int

  """Resume before this cursor. Exclusive."""
  before: String
}

"""The direction of one sort key."""
enum SortDirection {
  ASC
  DESC
}

"""
One stored idempotency key: the claim a client's retry is answered from.
"""
type IdempotencyKey {
  id: ID!

  """
  The tenant the key belongs to, which the caller's credential states and never the caller.
  """
  tenantId: ID

  """
  The organization the key was presented in. Keys are scoped by it, so one tenant's key can never
  replay another's response — not even inside the same tenant across two organizations.
  """
  organizationId: ID

  """
  The key the client presented, exactly as it sent it. It is the client's own string and is stored so
  the client can find its own row; it is never generated and never rewritten.
  """
  key: String!

  """
  The operation the key was presented to, for example `checkout.complete`. Two operations may
  legitimately be called with the same client key, which is why the scope is part of the identity
  rather than a filter applied after the lookup.
  """
  scope: String!

  """
  The first eight characters of the SHA-256 of the canonicalised request.
  A replay with a different body is a conflict rather than a replay, and the stored hash is what makes
  the two distinguishable — without keeping the body itself, which is why only the prefix is carried
  here. Eight hex characters tell two requests apart and reconstruct neither.
  """
  requestHash: String!

  """
  How far the first attempt got: `IN_PROGRESS` while it runs, `COMPLETED` when its response was
  recorded, `FAILED` when it was refused. A terminal row is what a retry is answered from, and the
  distinction is what an operator reads to decide whether to release it.
  """
  status: IdempotencyStatus!

  """
  The status the first attempt answered with, or null while the attempt is still running. A replay
  returns this status verbatim, which is why it is recorded rather than recomputed.
  """
  responseStatus: Int

  """
  What the operation was recorded as creating, for example `order`. Null while the attempt runs.
  """
  resourceType: String

  """
  Id of what was created, when the response carried one. It is how an operator finds the row.
  """
  resourceId: ID

  """
  The moment past which the stored response is no longer replayable and the row is swept. The lookup
  refuses an expired row on its own, so the horizon holds even if the sweep is behind.
  """
  expiresAt: DateTime!

  """
  When the claim was taken. A claim whose lease has gone stale is treated as abandoned and may be
  taken over by another attempt, which is what stops a request whose process died mid-flight from
  holding its key forever — and what decides whether a release is allowed to remove the row.
  """
  lockedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The lifecycle of a stored key."""
enum IdempotencyStatus {
  """A first attempt owns the key and its work is running."""
  IN_PROGRESS

  """The attempt finished; its response is what a retry is answered from."""
  COMPLETED

  """
  The attempt was refused; a retry receives the refusal rather than a second run.
  """
  FAILED
}

"""The boundary of one page of keys."""
type IdempotencyKeyEdge {
  node: IdempotencyKey!
  cursor: String!
}

"""
One page of keys, with the total the filters select and the boundary cursors.
"""
type IdempotencyKeyConnection {
  nodes: [IdempotencyKey!]!
  edges: [IdempotencyKeyEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a key list may be sorted by.

`createdAt` is what an operator chasing a stuck client reaches for — the newest rows are the retries in
flight — and `expiresAt` is what a retention question is read in. The identifiers are not sort keys: an
order by identifier is not an order anybody reads a key table in, and the questions those columns
answer are filters.
"""
enum IdempotencyKeySortField {
  createdAt
  expiresAt
  lockedAt
  scope
  status
}

"""The order the keys are returned in. At most three keys are accepted."""
input IdempotencyKeySort {
  field: IdempotencyKeySortField!
  direction: SortDirection!
}

"""Comparison on the lifecycle of a key."""
input IdempotencyStatusFilter {
  eq: IdempotencyStatus
  ne: IdempotencyStatus
  in: [IdempotencyStatus!]
  nin: [IdempotencyStatus!]
  isNull: Boolean
}

"""
The fields a key list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row, and the set is deliberately the columns an operator actually
searches by: the operation, the client's own key, the lifecycle and the resource it points at. A key
table is not browsed — an operator arrives at it already knowing which operation and which key the
stuck client is holding — and a pattern match over the request hash, or a range over the whole table,
would invite the scan the unique index exists to avoid. The tenant and the organization are absent
because both are applied to the read from the credential rather than from the caller.
"""
input IdempotencyKeyFilter {
  id: IDFilter
  key: StringFilter
  scope: StringFilter
  requestHash: StringFilter
  status: IdempotencyStatusFilter
  responseStatus: NumberFilter
  resourceType: StringFilter
  resourceId: IDFilter
  expiresAt: DateTimeFilter
  lockedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IdempotencyKeyFilter!]
  or: [IdempotencyKeyFilter!]
  not: IdempotencyKeyFilter
}

"""A stored image."""
type ImageAsset {
  id: ID!

  """The original file name, kept so an operator can recognise the asset."""
  name: String

  """The path the asset is served from."""
  url: String!

  """The smaller rendition's path, when one was generated."""
  thumb: String

  """The pixel width, when the asset was measured."""
  width: Int

  """The pixel height, when the asset was measured."""
  height: Int

  """The size in bytes, when the storage provider reported it."""
  size: Float

  """Whether an operator marked the asset as featured in its gallery."""
  isFeatured: Boolean

  """The identifier the storage provider knows the asset by."""
  externalProviderId: String

  """Which provider currently holds the bytes."""
  storageProvider: String

  """The absolute URL, resolved against the configured host."""
  fullUrl: String

  """The absolute thumbnail URL, resolved against the configured host."""
  thumbUrl: String
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """
  When the image was withdrawn, or null while it is live. The withdrawal and the restoration are
  delivered routes; this is the column they write. A withdrawn image is still pointed at by whatever
  referenced it — withdrawing it does not remove those references, and the removal is what refuses an
  image still in use.
  """
  deletedAt: DateTime
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of images."""
type ImageAssetEdge {
  node: ImageAsset!
  cursor: String!
}

"""
One page of images, with the total the filters select and the boundary cursors.
"""
type ImageAssetConnection {
  nodes: [ImageAsset!]!
  edges: [ImageAssetEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an image list may be sorted by."""
enum ImageAssetSortField {
  createdAt
  updatedAt
  name
  size
  isFeatured
}

"""The order the images are returned in. At most three keys are accepted."""
input ImageAssetSort {
  field: ImageAssetSortField!
  direction: SortDirection!
}

"""
The fields an image list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned rather than translating the filter into a query. `size` is a
`DecimalFilter` because the column behind it is `numeric`: a bound compared through a binary fraction
would select the wrong rows, and a gallery is routinely narrowed to "everything under a limit".
"""
input ImageAssetFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  url: StringFilter
  thumb: StringFilter
  width: NumberFilter
  height: NumberFilter
  size: DecimalFilter
  isFeatured: BooleanFilter
  externalProviderId: StringFilter
  storageProvider: StringFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ImageAssetFilter!]
  or: [ImageAssetFilter!]
  not: ImageAssetFilter
}

"""
What a caller states when it records an image whose bytes are already somewhere.

The address is required because the row is the address: this write records where an image lives, and
the store marks the column non-empty. The organization is optional, which is the delivered route's
own contract — the creation is inherited from the CRUD base and binds its body to a partial entity,
so nothing validates a member the row does not itself require — and there is no member for the tenant,
because the service stamps the caller's own tenant.

**This is not the upload.** The delivered upload route produces the address, the thumbnail, the byte
count and the provider name from a multipart body, and none of them can be stated here; a caller
reaching this input is recording an image the platform already holds, or one held elsewhere.
"""
input CreateImageAssetInput {
  organizationId: ID

  """The original file name, for an operator to recognise the asset by."""
  name: String

  """The path the asset is served from."""
  url: String!

  """The smaller rendition's path, when one exists."""
  thumb: String
  width: Int
  height: Int

  """The size in bytes, when it is known."""
  size: Float
  isFeatured: Boolean

  """The identifier the storage provider knows the asset by."""
  externalProviderId: String

  """Which provider holds the bytes."""
  storageProvider: String
}

"""
What a caller states when it changes an image that exists.

**A member the caller leaves out is left as it is.** The delivered edit is the CRUD base's partial
column update — it hands the store the members the body carries and writes them — so the identifier
is the only required member and the rest are stated only when they change.
"""
input UpdateImageAssetInput {
  id: ID!
  organizationId: ID
  name: String
  url: String
  thumb: String
  width: Int
  height: Int
  size: Float
  isFeatured: Boolean
  externalProviderId: String
  storageProvider: String
}

"""One amount the organization earned."""
type Income {
  id: ID!

  """The amount earned, in the row's own `currency`."""
  amount: Decimal!

  """The ISO 4217 code `amount` is expressed in."""
  currency: String!

  """
  The date the income belongs to. Null on a row written without one, which is a row no book order can place.
  """
  valueDate: DateTime

  """
  The note kept with the income, for the operator rather than for the ledger.
  """
  notes: String

  """
  Whether the platform records this amount as a bonus rather than as ordinary income. The two are
  averaged separately for the employee the row names, so the flag is what tells them apart.
  """
  isBonus: Boolean

  """
  The human reference for the income, such as the document it was billed through.
  """
  reference: String

  """
  The party the income is billed to, as the identifier of the party's own row.
  """
  clientId: ID

  """
  The employee the income is recorded against, as the identifier of the employee's own row.
  """
  employeeId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered routes —
  the two removals this resource serves — and this column is the whole of what they write, so without it
  a caller could not tell a live row from a withdrawn one on the answer to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of incomes."""
type IncomeEdge {
  node: Income!
  cursor: String!
}

"""
One page of incomes, with the total the filters select and the boundary cursors.
"""
type IncomeConnection {
  nodes: [Income!]!
  edges: [IncomeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an income list may be sorted by.

`valueDate` is the book's own order, which is the order an earnings report is read in; the two timestamps
are how a change is found again; and `amount` is what a list of earnings is taken by. The flags and the
identifiers are not sort keys: a boolean or an identifier order is not an order a book is read in, and
the questions those columns answer are filters.
"""
enum IncomeSortField {
  createdAt
  updatedAt
  valueDate
  amount
}

"""
The order the incomes are returned in. At most three keys are accepted.
"""
input IncomeSort {
  field: IncomeSortField!
  direction: SortDirection!
}

"""
The fields an income list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is: the delivered list read answers
those columns, so each one narrows rows the connection was handed. The amount is stated through the
decimal family, which is the family money is compared in; **the value is the row's own**, so an equality
states the amount as the read produces it (`1200.5`, not a padded string), while the range operators —
which are what a book is actually searched with — need no such care. The pattern the paginated spelling
of the list interprets is stated here in the connection's own vocabulary: `ilike` on `notes`.

`deletedAt` is deliberately absent although the object carries it: the delivered list read answers live
rows only, so a condition on that column could only ever match the empty set. The tenant and the
organization are absent for a different reason — both are applied to the criterion from the credential
rather than from the caller, so a member here would suggest a scope decision the caller does not have.
The tags are absent for a third: the delivered list read loads no relations, so a condition on them could
only ever select the empty set. Which incomes are the caller's own is the `myIncomes` root field's
question rather than a filter's, because that read narrows by who is asking.
"""
input IncomeFilter {
  id: IDFilter
  valueDate: DateTimeFilter
  amount: DecimalFilter
  currency: StringFilter
  notes: StringFilter
  isBonus: BooleanFilter
  reference: StringFilter
  clientId: IDFilter
  employeeId: IDFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IncomeFilter!]
  or: [IncomeFilter!]
  not: IncomeFilter
}

"""
What a caller states when it records an income.

`amount`, `clientId` and `currency` are the members the delivered body validation requires; a body
without any of them is refused, which is why none of them is nullable here. The amount is stated as the
exact decimal: the column is `numeric`, and a JSON number — which is what the REST body carries the same
quantity as — is a binary fraction that cannot hold a cent.

The organization is stated as an identifier and is *not* required, because the delivered validation
requires an organization only when the body names neither the organization object nor a `sentTo` — and
the object spelling is a type this schema does not declare, so the identifier is what this surface offers
and the condition stays the delivered validator's rather than being restated as a non-null member that
would refuse a body the route serves.
"""
input CreateIncomeInput {
  """The amount earned, in the currency below."""
  amount: Decimal!

  """The party the income is billed to."""
  clientId: ID!

  """The ISO 4217 code the amount is expressed in."""
  currency: String!

  """The date the income belongs to."""
  valueDate: DateTime

  """The note kept with the income."""
  notes: String

  """Whether the amount is a bonus rather than ordinary income."""
  isBonus: Boolean

  """The human reference for the income."""
  reference: String

  """The organization the income is filed under."""
  organizationId: ID

  """The employee the income is recorded against."""
  employeeId: ID

  """
  The facets attached to the income, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it changes an income that exists.

**The delivered edit is the platform's own upsert, and it is reached through the same write the create
is.** The route reads the row's identifier from the path, spreads the body beside it and calls the
delivered create, so a member the caller leaves out is left as it is — and an identifier that names no row
at all is **stored as a new row** rather than refused, because an upsert is what the delivered write is. A
client that means "change this income" reads it first, or reaches for the mutation above only with an
identifier it obtained from this surface.

`employeeId` is not a member, because the delivered edit body does not carry it — the delivered DTO drops
that member on the edit and keeps it on the create.
"""
input UpdateIncomeInput {
  id: ID!

  """The amount earned, in the currency below."""
  amount: Decimal!

  """The party the income is billed to."""
  clientId: ID!

  """The ISO 4217 code the amount is expressed in."""
  currency: String!

  """The date the income belongs to."""
  valueDate: DateTime

  """The note kept with the income."""
  notes: String

  """Whether the amount is a bonus rather than ordinary income."""
  isBonus: Boolean

  """The human reference for the income."""
  reference: String

  """The organization the income is filed under."""
  organizationId: ID

  """
  The facets attached to the income, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
One row type that is synchronised because the row type it hangs off is.
"""
type IntegrationEntitySettingTied {
  id: ID!

  """The kind of row that travels with the decision it belongs to."""
  entity: IntegrationEntity!

  """Whether rows of that kind travel with the decision."""
  sync: Boolean!

  """The decision this row hangs off."""
  integrationEntitySettingId: ID

  """
  The tenant the row belongs to. Stamped from the credential, never stated by a caller.
  """
  tenantId: ID

  """The organization the tied decision applies to."""
  organizationId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are routes every
  resource on this platform inherits, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
One tied decision as a write states it.

The delivered write accepts one of these or a list of them and stores what it is given, so the surface
states the list — a GraphQL input is one shape, and the list is the shape the delivered handler folds a
single object into anyway. The decision the row hangs off is stated by the write's own argument when it
is not stated here, which is what the delivered body does with the identifier it receives.
"""
input IntegrationEntitySettingTiedInput {
  """The kind of row that travels with the decision it belongs to."""
  entity: IntegrationEntity!

  """Whether rows of that kind travel with the decision."""
  sync: Boolean!

  """
  The decision to hang the row off. The delivered write also takes an integration in its path.
  """
  integrationEntitySettingId: ID

  """The organization the tied decision applies to."""
  organizationId: ID
}

"""
One row type's synchronisation decision for one configured integration.
"""
type IntegrationEntitySetting {
  id: ID!

  """The kind of row this decision is about."""
  entity: IntegrationEntity!

  """Whether rows of that kind are synchronised through this integration."""
  sync: Boolean!

  """
  The configured integration this decision belongs to, as the delivered read loads it.
  """
  integration: IntegrationTenant

  """The same integration, by identifier."""
  integrationId: ID

  """
  The rows that are synchronised because this one is. Loaded by the delivered read.
  """
  tiedEntities: [IntegrationEntitySettingTied!]!

  """
  The tenant the row belongs to. Stamped from the credential, never stated by a caller.
  """
  tenantId: ID

  """The organization the decision applies to."""
  organizationId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are routes every
  resource on this platform inherits, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of synchronisation decisions."""
type IntegrationEntitySettingEdge {
  node: IntegrationEntitySetting!
  cursor: String!
}

"""
One page of synchronisation decisions, with the total the filters select and the boundary cursors.
"""
type IntegrationEntitySettingConnection {
  nodes: [IntegrationEntitySetting!]!
  edges: [IntegrationEntitySettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a decision list may be sorted by."""
enum IntegrationEntitySettingSortField {
  createdAt
  updatedAt
  entity
  sync
}

"""
The order the decisions are returned in. At most three keys are accepted.
"""
input IntegrationEntitySettingSort {
  field: IntegrationEntitySettingSortField!
  direction: SortDirection!
}

"""
The fields a decision list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`integrationId` is the member a reader of one configured integration states; the delivered read takes
it as its own path parameter and this surface states it as the argument it is, so the narrowing is
applied by the store rather than to its answer. It is carried here as well because the rows this
connection holds are the rows of one integration, and a caller that wants to be sure of that can say
so — the two agree by construction.
"""
input IntegrationEntitySettingFilter {
  id: IDFilter
  entity: StringFilter
  sync: BooleanFilter
  integrationId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IntegrationEntitySettingFilter!]
  or: [IntegrationEntitySettingFilter!]
  not: IntegrationEntitySettingFilter
}

"""
One synchronisation decision as a write states it.

The delivered write accepts one of these or a list of them and stores what it is given, so the surface
states the list — a GraphQL input is one shape, and the list is the shape the delivered handler folds a
single object into anyway. `tiedEntities` is a member because the delivered write cascades it: a
decision and the rows that hang off it are written together.
"""
input IntegrationEntitySettingInput {
  """The kind of row this decision is about."""
  entity: IntegrationEntity!

  """Whether rows of that kind are synchronised through this integration."""
  sync: Boolean!

  """
  The configured integration the decision belongs to. The delivered write also takes it in the path.
  """
  integrationId: ID

  """The organization the decision applies to."""
  organizationId: ID

  """The rows that are synchronised because this one is."""
  tiedEntities: [IntegrationEntitySettingTiedInput!]
}

"""One credential of a configured integration, with its value withheld."""
type IntegrationSetting {
  id: ID!

  """
  The key the value is stored under. The column is named `settingsName` and it stays named that:
  renaming a core column is not an additive change, and the delivered rules name it.
  """
  settingsName: String!

  """The configured integration this credential belongs to."""
  integrationId: ID

  """
  The tenant the row belongs to. Stamped from the credential, never stated by a caller.
  """
  tenantId: ID

  """
  The organization the row belongs to, when the integration is configured per organization.
  """
  organizationId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are routes every
  resource on this platform inherits, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
One credential as a write states it: the key, and the value to store under it.

This input is what the configured-integration write composes, and it is the one place the value
appears — it travels into the store and is never answered back.
"""
input IntegrationSettingInput {
  """The key the value is stored under."""
  settingsName: String!

  """
  The value to store. Sent, withheld on the way out, and never returned in clear text.
  """
  settingsValue: String!
}

"""
What a caller states when it rotates one credential.

The delivered route binds this body and nothing else: the identifier comes from the path, the
organization scopes the row, and the value replaces what was stored. `id` is required here because the
delivered route takes the identifier from the path; stating it in the input as well is what keeps the
two readings of one identifier from disagreeing.
"""
input UpdateIntegrationSettingInput {
  id: ID!

  """The organization the credential belongs to."""
  organizationId: ID

  """The value to store under the row's existing key."""
  settingsValue: String!
}

"""One integration a tenant has connected."""
type IntegrationTenant {
  id: ID!

  """
  The provider this connection is for, in the contract's own provider vocabulary. Carried as text
  because a provider a plugin introduces is a row this resource must still answer.
  """
  name: String!

  """When this connection last synchronised, or null while it never has."""
  lastSyncedAt: DateTime

  """The catalogue entry the connection is configured against."""
  integration: Integration

  """The same catalogue entry, by identifier."""
  integrationId: ID

  """
  The credentials this connection authenticates with. Loaded when the read is asked for them; each
  row's stored value is never answered, which is the decision the credential type states at length.
  """
  settings: [IntegrationSetting!]

  """
  The tenant the connection belongs to. Stamped from the credential, never stated by a caller.
  """
  tenantId: ID

  """The organization the connection belongs to."""
  organizationId: ID

  """
  When the connection was withdrawn, or null while it is live. Withdrawing and restoring are the CRUD
  base's own routes and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of connections."""
type IntegrationTenantEdge {
  node: IntegrationTenant!
  cursor: String!
}

"""
One page of connections, with the total the filters select and the boundary cursors.
"""
type IntegrationTenantConnection {
  nodes: [IntegrationTenant!]!
  edges: [IntegrationTenantEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a connection list may be sorted by."""
enum IntegrationTenantSortField {
  createdAt
  updatedAt
  name
  lastSyncedAt
}

"""
The order the connections are returned in. At most three keys are accepted.
"""
input IntegrationTenantSort {
  field: IntegrationTenantSortField!
  direction: SortDirection!
}

"""
The fields a connection list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`name` narrows by the provider the connection is for, which is the question a caller actually asks of
this list — and `lastSyncedAt: { isNull: true }` is the connections that have never run, which is the
set an operator looks at when a sync is expected and nothing has happened.
"""
input IntegrationTenantFilter {
  id: IDFilter
  name: StringFilter
  integrationId: IDFilter
  lastSyncedAt: DateTimeFilter
  organizationId: IDFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IntegrationTenantFilter!]
  or: [IntegrationTenantFilter!]
  not: IntegrationTenantFilter
}

"""
What a caller states when it connects an integration.

The members are the delivered creation body's own: the provider, the catalogue entry, the organization
the connection belongs to, and the credentials and per-row-type decisions to file with it. There is no
member for the tenant — the delivered service stamps the caller's own and overwrites whatever a body
states — and none for `lastSyncedAt`, which is a fact the synchronisation itself produces rather than
something a caller announces.
"""
input CreateIntegrationTenantInput {
  """The provider to connect, in the contract's own provider vocabulary."""
  name: String!

  """The catalogue entry this connection is for."""
  integrationId: ID

  """The organization the connection belongs to."""
  organizationId: ID

  """The credentials to file with the connection, one member per key."""
  settings: [IntegrationSettingInput!]

  """
  The per-row-type synchronisation decisions to file with the connection.
  """
  entitySettings: [IntegrationEntitySettingInput!]
}

"""
What a caller states when it changes a connection that exists.

Every member but the identifier is optional and a member the caller leaves out is left as it is: the
delivered edit is a partial column update against the identifier the path names, not a replacement of
the row. The provider is not among the mutable members — a connection that could change provider would
be a different connection under the same identifier, with the decisions and credentials of the old one
still attached to it.
"""
input UpdateIntegrationTenantInput {
  id: ID!

  """The organization the connection belongs to."""
  organizationId: ID

  """Whether the connection is active."""
  isActive: Boolean

  """Whether the connection is archived."""
  isArchived: Boolean

  """The credentials to write with the edit, one member per key."""
  settings: [IntegrationSettingInput!]
}

"""
One entry of the integration catalogue: a provider this installation can be connected to.
"""
type Integration {
  id: ID!

  """The provider's display name, for example `Github`."""
  name: String!

  """
  The provider's own identifier — `github`, `jira`, `hubstaff` — which is what a configured
  integration is looked up by. Null on a catalogue entry that is not wired to a provider yet.
  """
  provider: String

  """
  Where the provider sends a caller back to once it has authorised the connection.
  """
  redirectUrl: String

  """The stored path of the catalogue image."""
  imgSrc: String

  """
  The same image, resolved through the file storage provider, as a client can load it.
  """
  fullImgUrl: String

  """Whether the catalogue entry is advertised before it can be connected."""
  isComingSoon: Boolean

  """Whether connecting requires a paid provider plan."""
  isPaid: Boolean

  """A free trial is offered, and for how long."""
  isFreeTrial: Boolean

  """The length of the free trial, in whole days."""
  freeTrialPeriod: Int

  """The provider's version this catalogue entry was written against."""
  version: String

  """Where the provider's own documentation is."""
  docUrl: String

  """The position of the entry in the catalogue listing."""
  order: Int
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
One facet of the integration catalogue: a group an integration is filed under.
"""
type IntegrationType {
  id: ID!

  """The facet's name, for example `Project Management`."""
  name: String!

  """The facet's explanation, shown under its name."""
  description: String

  """The facet's icon."""
  icon: String

  """The group the facet is presented in, for example `Categories`."""
  groupName: String!

  """The position of the facet among the others."""
  order: Int!
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
The kinds of row an integration synchronises.

The vocabulary is this domain's own and is closed: a value outside it names a row type the platform
has no sync for. It is declared once here and referenced by the two settings resources that write it,
so the sync flags and the catalogue cannot drift apart.
"""
enum IntegrationEntity {
  Project
  Organization
  Note
  Client
  Task
  Issue
  Label
  Activity
  User
  Employee
  TimeLog
  TimeSlot
  Screenshot
  Income
  Expense
  Proposal
  JOB_MATCHING
  EMPLOYEE_PERFORMANCE
}

"""The boundary of one page of catalogue entries."""
type IntegrationEdge {
  node: Integration!
  cursor: String!
}

"""
One page of catalogue entries, with the total the filters select and the boundary cursors.
"""
type IntegrationConnection {
  nodes: [Integration!]!
  edges: [IntegrationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The boundary of one page of catalogue facets."""
type IntegrationTypeEdge {
  node: IntegrationType!
  cursor: String!
}

"""
One page of catalogue facets, with the total the filters select and the boundary cursors.
"""
type IntegrationTypeConnection {
  nodes: [IntegrationType!]!
  edges: [IntegrationTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a catalogue list may be sorted by."""
enum IntegrationSortField {
  createdAt
  updatedAt
  name
  provider
  order
  isComingSoon
  isFreeTrial
  version
}

"""
The order the catalogue is returned in. At most three keys are accepted.
"""
input IntegrationSort {
  field: IntegrationSortField!
  direction: SortDirection!
}

"""The fields a catalogue list may be sorted by, for the facets."""
enum IntegrationTypeSortField {
  createdAt
  updatedAt
  name
  groupName
  order
}

"""The order the facets are returned in. At most three keys are accepted."""
input IntegrationTypeSort {
  field: IntegrationTypeSortField!
  direction: SortDirection!
}

"""
The fields a catalogue list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Two of the row's columns are deliberately absent. `name` and `provider` are here, but the delivered
read's own *search term* is a root-field argument rather than a member of this input: it is the string
the delivered command hands the store as a prefix match, and it is stated where the delivered filter
object is stated. `isPaid` is absent for the same reason — the delivered read takes it as a member of
its own filter object, and this surface states it as the argument it is. One narrowing stated twice in
one field would be two spellings that could disagree.
"""
input IntegrationFilter {
  id: IDFilter
  name: StringFilter
  provider: StringFilter
  redirectUrl: StringFilter
  version: StringFilter
  docUrl: StringFilter
  order: NumberFilter
  isComingSoon: BooleanFilter
  isFreeTrial: BooleanFilter
  freeTrialPeriod: NumberFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IntegrationFilter!]
  or: [IntegrationFilter!]
  not: IntegrationFilter
}

"""The fields a facet list may be narrowed by."""
input IntegrationTypeFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  icon: StringFilter
  groupName: StringFilter
  order: NumberFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IntegrationTypeFilter!]
  or: [IntegrationTypeFilter!]
  not: IntegrationTypeFilter
}

"""One invitation, as the delivered reads answer it."""
type Invite {
  id: ID!

  """
  The address the invitation was mailed to, and the address the credential is checked against.
  """
  email: String!

  """
  The name the inviter stated for the invited person. Null when none was stated.
  """
  fullName: String

  """
  The state of the invitation, in the contracts' own `InviteStatusEnum` vocabulary: `INVITED` while
  the credential may still be spent, and `ACCEPTED`, `REJECTED` or `EXPIRED` once it may not.
  Carried as its value rather than declared as a schema enum, because the same vocabulary types the
  column on the row and the transitions the delivered writers make on it.
  """
  status: String!

  """
  When the invitation lapses. Null means it does not lapse, which is the delivered vocabulary's own
  `Never` rather than a missing value — the entity's load subscriber turns the two cases into the
  liveness flag below.
  """
  expireDate: DateTime

  """
  The instant the invitation was answered, or the date the invited person is to start work on: the
  delivered write stores the stated start date in this column, so an invitation read before it is
  answered may already carry one.
  """
  actionDate: DateTime

  """
  Whether the invitation has lapsed. Derived on load by the entity's own subscriber from the instant
  above, which is the liveness fact a manager reads — an invitation that has lapsed is one no
  credential can spend.
  """
  isExpired: Boolean

  """
  The role the invited person is given on acceptance, by identifier. The row is joined only when a
  REST caller names it, and is read on the role domain.
  """
  roleId: ID

  """
  The account the invitation produced, by identifier. Null while the invitation is pending, and read
  on the user domain.
  """
  userId: ID

  """
  The person who issued the invitation, by identifier. The account is read on the user domain.
  """
  invitedByUserId: ID

  """
  The tenant the invitation belongs to, by identifier. Stamped by the delivered write.
  """
  tenantId: ID

  """The organization the invitation is into, by identifier."""
  organizationId: ID

  """When the invitation was issued."""
  createdAt: DateTime

  """When the row last changed."""
  updatedAt: DateTime

  """
  When the invitation was withdrawn, or null while it is live. A removal this resource serves is a
  withdrawal rather than an erasure, and this column is the whole of what it writes.
  """
  deletedAt: DateTime

  """When the invitation was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  Whether the invitation is active. A flag an operator sets, not a statement about its lifecycle.
  """
  isActive: Boolean

  """
  Whether the invitation is archived. A flag an operator sets, not a statement about its lifecycle.
  """
  isArchived: Boolean
}

"""The boundary of one page of invitations."""
type InviteEdge {
  node: Invite!
  cursor: String!
}

"""
One page of invitations, with the total the filters select and the boundary cursors.
"""
type InviteConnection {
  nodes: [Invite!]!
  edges: [InviteEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an invitation list may be sorted by."""
enum InviteSortField {
  id
  email
  fullName
  status
  expireDate
  actionDate
  createdAt
  updatedAt
  deletedAt
}

"""
The order the invitations are returned in. At most three keys are accepted.
"""
input InviteSort {
  field: InviteSortField!
  direction: SortDirection!
}

"""
The fields an invitation list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

**The narrowing the delivered register performs on its own is stated nowhere here.** That read always
adds the caller's tenant to its criterion, and it reads `isExpired` — a flag the entity derives on load
rather than a column — as a range on `expireDate`, treating an absent instant as an invitation that
never lapses. Both belong to the read rather than to the caller: the tenant comes from the credential,
so a filter naming another one narrows to nothing rather than reading across tenants, and a caller that
wants only the live rows states the range on `expireDate` that means so. Every member below is
therefore a column of the row the read actually answers, which is what keeps a filter that silently
selects the empty set out of the schema.
"""
input InviteFilter {
  id: IDFilter
  email: StringFilter
  fullName: StringFilter
  status: StringFilter
  expireDate: DateTimeFilter
  actionDate: DateTimeFilter
  roleId: IDFilter
  userId: IDFilter
  invitedByUserId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [InviteFilter!]
  or: [InviteFilter!]
  not: InviteFilter
}

"""
What the bulk send answers: the invitations it wrote, how many there were, and how many addresses it
left alone.

This is a type of its own rather than the `Invite` type, because that is what the delivered handler
produces — `ICreateEmailInvitesOutput` is an envelope of the rows it saved beside a count of the
addresses it skipped, and flattening it into a list of invitations would lose the count, which is the
only way a caller learns that an address it named was already invited.
"""
type InviteSendOutcome {
  """
  The invitations this call wrote. An address already invited to every team it names is not here.
  """
  items: [Invite!]!

  """How many invitations were written."""
  total: Int!

  """
  How many addresses were left alone because the invitation for them had already been sent and none
  of the teams named was still to be joined. They are not failures: each is an address the register
  already covers.
  """
  ignored: Int!
}

"""
The kind of invitation being issued. The vocabulary is the contracts' own `InvitationTypeEnum`, and it
decides which message is composed: an account invitation, an employee invitation, a candidate
invitation or a team membership.
"""
enum InvitationType {
  USER
  EMPLOYEE
  CANDIDATE
  TEAM
}

"""
The three claims that make a password reset possible on an account whose address nobody has confirmed.
"""
input TermsAcceptanceClaimInput {
  """The stable document identifier, `<document>:<product>`."""
  documentId: String!

  """The published version of the document the person was shown."""
  version: String!

  """The lowercase hex sha256 of the exact document text that was rendered."""
  sha256: String!

  """The BCP-47 locale of the rendering that was shown."""
  locale: String!
}

"""
What a caller states when it sends invitations to a list of addresses.

**Every member here is one the delivered write reads**, and the members it does not read are absent
rather than accepted and discarded: the delivered body type carries a `sentTo` member that no part of
the write touches, and an organization that may be stated as a row instead of an identifier — this
surface states the identifier, which is the member the write actually resolves.

The collections are stated as identifiers because the delivered write resolves them to rows itself: it
reads the teams, the projects, the departments and the contacts the caller names, keeps only those of
the caller's own tenant and organization, and files the resolved rows onto the invitation. The
addresses are addresses rather than identifiers — `emailIds` is a list of e-mail addresses, and it is
required and non-empty, because a send that names no address has nothing to send.

`invitationExpirationPeriod` is stated as text because the delivered vocabulary mixes the two: four
counts of days and the word `Never`. An enum here would have to spell members the delivered write does
not recognise, and a number could not carry the word, so the member is the wire form the write reads.

There is no member for the tenant: the delivered write stamps the caller's own and overwrites whatever
a body states.
"""
input CreateInvitesInput {
  """The addresses to invite. Required, and never empty."""
  emailIds: [String!]!

  """
  The kind of invitation to issue. The write composes its message from it.
  """
  inviteType: InvitationType!

  """
  The role the invited people are given on acceptance. Required by the delivered body.
  """
  roleId: ID!

  """The organization the invitations are into."""
  organizationId: ID!

  """
  The teams to invite into. Required when the kind is `TEAM`, which no input type can state.
  """
  teamIds: [ID!]

  """The projects the invited people are filed under on acceptance."""
  projectIds: [ID!]

  """The departments the invited people are filed under on acceptance."""
  departmentIds: [ID!]

  """The contacts the invited people are attached to on acceptance."""
  organizationContactIds: [ID!]

  """
  The date the invited person is to start work on, stored in the invitation's action column.
  """
  startedWorkOn: DateTime

  """
  The date an invited candidate applied, stored the same way when no start date is stated.
  """
  appliedDate: DateTime

  """
  How long the invitation stays valid: a count of days as text, or `Never` for one that does not
  lapse. Omit to take the organization's own expiry period.
  """
  invitationExpirationPeriod: String

  """
  The name to greet the invited people by. Omit to greet the address alone.
  """
  fullName: String

  """
  The link the message points at instead of the platform's own acceptance page. When it is stated the
  message carries this address with the invitation's own query parameters appended.
  """
  callbackUrl: String

  """
  The query parameters the callback link is composed with, as a document: each key is the parameter
  to append and each value names an invitation member to take it from.
  """
  queryParams: JSON
}

"""
What a caller states when it mails a fresh credential for an invitation that is still pending.

The identifier is the only required member and names the row; the kind selects the message, and the
callback link is the same override the send states. The organization members the delivered body type
carries are absent because the delivered write reads neither: the row already belongs to an
organization, and it is the invitation's own that the message is composed with.
"""
input ResendInviteInput {
  """The invitation to mail again."""
  inviteId: ID!

  """
  The kind of message to compose. The row states what was invited; the caller states how to say it.
  """
  inviteType: InvitationType!

  """
  The link the message points at instead of the platform's own acceptance page.
  """
  callbackUrl: String
}

"""
What a caller states when it refuses an invitation.

The address and the credential that was mailed to it — both halves of the pair, because the delivered
reader matches on the address and then verifies the credential against it. A miss is a refusal rather
than an empty answer on the route this mirrors, for the same reason the two validation reads refuse
one: telling "no such invitation" apart from "the credential was wrong" would confirm which
credentials are live.
"""
input RejectInviteInput {
  """The address the invitation was mailed to."""
  email: String!

  """The token from the message's link."""
  token: String!

  """The code from the message."""
  code: String!
}

"""
The account an acceptance registers, as much of it as the caller may state.

**Five members of the user body are deliberately absent, and the delivered handler is why.** It
overwrites the invited address, the role and its identifier, the tenant and the tenant's identifier
from the invitation before it registers anything — this route is public, and a body-supplied address
would let an invitee register an account for somebody else's address, a body-supplied role would let
one grant itself an administrator's, and a body-supplied tenant would let one register into a tenant
the invitation does not name. The registration then strips the members that identify or privilege an
*existing* account before it persists — an identifier, a password hash, the verification marks and the
refresh token — so none of those is offered either.

What remains is what the account is: the two name parts, an avatar and the language the person reads.
The password is not here but beside it on the acceptance inputs, because that is where the delivered
registration reads it from.
"""
input AcceptInviteUserInput {
  """The given name of the person the account is for."""
  firstName: String

  """The family name of the person the account is for."""
  lastName: String

  """A URL the account's avatar is taken from."""
  imageUrl: String

  """
  The language the account's interface is shown in, in the platform's own vocabulary.
  """
  preferredLanguage: String
}

"""
What a caller states when it accepts an invitation and registers.

The address and one of the two credentials the message carried — the token its link carries, or the
code a person retypes. **Exactly one of `token` and `code` is required**, which is the rule the
delivered reader applies: it branches on which of the two the body carries, the token first and the
code second, so neither can be non-null here — a caller that stated both would take the token's branch
whatever it meant, and the code's branch would be a path no caller could reach over this protocol. A
body stating neither reaches neither branch and is refused before anything is matched, which is why
neither is stated as required and the field's own description carries the rule.

The account body is the one the registration creates. `password` is optional because the delivered
contract marks it so: an acceptance by a person who already has an account in the invited tenant does
not register one, and a password stated for that case is simply not read.

Four members of the delivered body are absent, each because the delivered handler settles it before the
write: `inviteId` is pinned to the invitation the credential was validated against — the delivered
reader resolves it and overwrites whatever the body says, so a caller cannot accept one invitation
while naming another; `originalUrl` is taken from the request's own origin header, which the transport
supplies rather than the caller; `organizationId` is overridden with the invitation's own organization;
and `createdByUserId` is deleted outright unless it names the authenticated caller, which on a public
route it never does. `confirmPassword` is absent as well, because no part of the delivered registration
reads it — the confirmation is the form's check, not the server's.
"""
input AcceptInviteInput {
  """
  The address the invitation was mailed to, and the address the credential is checked against.
  """
  email: String!

  """The token from the message's link. Required when no code is stated."""
  token: String

  """The code from the message. Required when no token is stated."""
  code: String

  """The account to register for the invited person."""
  user: AcceptInviteUserInput!

  """
  The password the new account signs in with. Hashed by the service before it is stored.
  """
  password: String

  """
  Whether the acceptance also files the new account as an employee of the organization.
  """
  featureAsEmployee: Boolean

  """
  The documents the person ticked the box for, exactly as they were shown. Absent for a
  machine-driven acceptance, where a checkbox never existed.
  """
  terms: [TermsAcceptanceClaimInput!]

  """
  Whether the registration is part of an import, which records its source beside the new account.
  """
  isImporting: Boolean

  """The identifier the imported row had in the system it came from."""
  sourceId: ID
}

"""
What a caller states when it takes up a contact invitation and provisions the contact's own workspace.

This is the one acceptance that builds a tenant: the delivered handler creates the tenant the contact
is named by, the roles and features that belong to it, the organization described here, and the
account that administers it. The identifier names the invitation, the account body is the
administrator, and `contactOrganization` is the workspace itself — typed by the organization domain's
own create input rather than restated here, because the delivered write hands that body straight to
that domain's create and the two have to agree member for member.

`originalUrl` is absent for the same reason it is absent above: the transport's origin header supplies
it, and the delivered handler overwrites a stated one.
"""
input AcceptOrganizationContactInviteInput {
  """The invitation to take up."""
  inviteId: ID!

  """
  The workspace the contact is accepting: the tenant's name and the organization built under it.
  """
  contactOrganization: CreateOrganizationInput!

  """The account that administers the new workspace."""
  user: AcceptInviteUserInput!

  """
  The password the new account signs in with. Hashed by the service before it is stored.
  """
  password: String!
}

"""One entry in the log of what happened to an invoice or an estimate."""
type InvoiceEstimateHistory {
  id: ID!

  """
  What was done: the free-text action the platform's invoicing paths record.
  """
  action: String!

  """An optional heading for the entry."""
  title: String

  """The user who performed the action. A column, not the joined relation."""
  userId: ID

  """
  The invoice or estimate the action was performed on. A column, not the joined relation.
  """
  invoiceId: ID

  """The organization the document belongs to, when the entry names one."""
  organizationId: ID

  """The tenant the entry belongs to, stamped from the credential."""
  tenantId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are the CRUD base's
  own routes and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of log entries."""
type InvoiceEstimateHistoryEdge {
  node: InvoiceEstimateHistory!
  cursor: String!
}

"""
One page of log entries, with the total the filters select and the boundary cursors.
"""
type InvoiceEstimateHistoryConnection {
  nodes: [InvoiceEstimateHistory!]!
  edges: [InvoiceEstimateHistoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a log list may be sorted by."""
enum InvoiceEstimateHistorySortField {
  createdAt
  updatedAt
  action
  title
}

"""
The order the log list is returned in. At most three keys are accepted.
"""
input InvoiceEstimateHistorySort {
  field: InvoiceEstimateHistorySortField!
  direction: SortDirection!
}

"""
The fields a log list may be narrowed by. `and` is the default; `or` requires any of its members and `not`
excludes the rows that match it.

`invoiceId` is the narrowing the resource exists for: the log is read one document at a time, and the
entries of that document are the entries whose column names it.
"""
input InvoiceEstimateHistoryFilter {
  id: IDFilter
  action: StringFilter
  title: StringFilter
  userId: IDFilter
  invoiceId: IDFilter
  organizationId: IDFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [InvoiceEstimateHistoryFilter!]
  or: [InvoiceEstimateHistoryFilter!]
  not: InvoiceEstimateHistoryFilter
}

"""
What a caller states when it files a log entry.

The members are the row's own writable columns: what was done, an optional heading, the user it is
attributed to, the document it concerns and the organization that owns it. The tenant is not a member,
because the tenant a row is written into comes from the credential on every write on this platform and a
member naming one would promise a scope the write does not honour.
"""
input CreateInvoiceEstimateHistoryInput {
  """What was done."""
  action: String!

  """An optional heading for the entry."""
  title: String

  """The user the entry is attributed to."""
  userId: ID

  """The invoice or estimate the entry concerns."""
  invoiceId: ID

  """The organization the document belongs to."""
  organizationId: ID
}

"""
What a caller states when it changes a log entry that exists.

The delivered edit is the CRUD base's own: it hands the store the identifier from the path and the body
beside it, so a member the caller leaves out is left as it is. An entry is a record of something that
happened, so an edit is a correction rather than a change of history — and a caller that means "withdraw
this entry" reaches for the soft removal below instead.
"""
input UpdateInvoiceEstimateHistoryInput {
  id: ID!
  action: String
  title: String
  userId: ID
  invoiceId: ID
  organizationId: ID
}

"""
One billed line of a document: what it charges for, at what price, for how many units.
"""
type InvoiceItem {
  id: ID!

  """The line's text — what the buyer reads on the document."""
  description: String

  """The price of one unit, in the currency the document states."""
  price: Decimal!

  """
  How many units the line bills, as an exact decimal.
  
  Not an integer: time and weight are billed in parts of a unit, and the line's total is this factor's
  product with the price, so a rounded quantity would make the total wrong by more than the rounding.
  """
  quantity: Decimal!

  """The line's total, in the currency the document states."""
  totalValue: Decimal!

  """Whether the document's first tax applies to this line."""
  applyTax: Boolean

  """Whether the document's discount applies to this line."""
  applyDiscount: Boolean

  """The document this line belongs to."""
  invoiceId: ID

  """The task this line bills, when it bills time on one."""
  taskId: ID

  """The employee this line bills, when it bills their time."""
  employeeId: ID

  """The project this line bills, when it bills time on one."""
  projectId: ID

  """The catalogue product this line bills, when it bills a stocked item."""
  productId: ID

  """The expense this line bills, when it bills a reimbursable cost."""
  expenseId: ID

  """
  The purchase order line this bill line settles.
  
  The bill line's own provenance, and the third of the three quantities a three-way match needs. Null
  on a sales line and on a bill imported before the column existed, which is reconciled by the
  purchase-side audit rather than backfilled with a guessed match.
  """
  purchaseOrderLineId: ID

  """
  When the line was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of lines."""
type InvoiceItemEdge {
  node: InvoiceItem!
  cursor: String!
}

"""
One page of lines, with the total the filters select and the boundary cursors.
"""
type InvoiceItemConnection {
  nodes: [InvoiceItem!]!
  edges: [InvoiceItemEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a line list may be sorted by.

The two timestamps are how a line is found again, and the three amounts and quantities are what a
line list is read by: what a document's lines cost, and how much of each was billed. `description` is
not a sort key, because the order of a text is the order of its spelling rather than a fact about the
line, and `invoiceId` is not one either — a document's lines are read by narrowing the list to that
document, which is a filter, and the order they come back in is then the order above.
"""
enum InvoiceItemSortField {
  createdAt
  updatedAt
  price
  quantity
  totalValue
}

"""The order the lines are returned in. At most three keys are accepted."""
input InvoiceItemSort {
  field: InvoiceItemSortField!
  direction: SortDirection!
}

"""
The fields a line list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the line's own row, which is why the set is what it is: the delivered list
read answers those columns, so each one narrows rows the connection was handed. `invoiceId` is the
member a document's own screen reads its lines by.

`deletedAt` is deliberately absent although the object carries it: the delivered list reads answer
live rows only, so a condition on that column could only ever match the empty set, and an argument
that cannot be honoured is worse than an argument that is not offered. The tenant and the organization
are absent for a different reason — both are applied to the criterion from the credential rather than
from the caller, so a member here would suggest a scope decision the caller does not have.

The amounts and the quantity are stated through the decimal family, which is the family money and
exact quantities are compared in. **The values are the row's own**: the columns are `numeric` read
through the platform's numeric transformer, so an equality states the value as the read produces it,
while the range operators — which are what a line list is actually searched with — need no such care.
"""
input InvoiceItemFilter {
  id: IDFilter
  description: StringFilter
  price: DecimalFilter
  quantity: DecimalFilter
  totalValue: DecimalFilter
  applyTax: BooleanFilter
  applyDiscount: BooleanFilter
  invoiceId: IDFilter
  taskId: IDFilter
  employeeId: IDFilter
  projectId: IDFilter
  productId: IDFilter
  expenseId: IDFilter
  purchaseOrderLineId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [InvoiceItemFilter!]
  or: [InvoiceItemFilter!]
  not: InvoiceItemFilter
}

"""
What a caller states when it records one line.

The three members the store will not accept a row without — the price, the quantity and the total —
are required here too. `invoiceId` is not: it is the identifier that attaches the line to its document
and the delivered bulk write's own validation requires it on every line of its body, while the single
create route validates nothing and the column is nullable, so the member is declared as the looser of
the two and the requirement stays where it is enforced.
"""
input CreateInvoiceItemInput {
  """The line's text — what the buyer reads on the document."""
  description: String

  """The price of one unit, in the currency the document states."""
  price: Decimal!

  """How many units the line bills."""
  quantity: Decimal!

  """The line's total, in the currency the document states."""
  totalValue: Decimal!

  """The document this line belongs to."""
  invoiceId: ID

  """The organization the line is filed under."""
  organizationId: ID

  """The task this line bills, when it bills time on one."""
  taskId: ID

  """The employee this line bills, when it bills their time."""
  employeeId: ID

  """The project this line bills, when it bills time on one."""
  projectId: ID

  """The catalogue product this line bills, when it bills a stocked item."""
  productId: ID

  """The expense this line bills, when it bills a reimbursable cost."""
  expenseId: ID

  """The purchase order line this bill line settles."""
  purchaseOrderLineId: ID

  """Whether the document's first tax applies to this line."""
  applyTax: Boolean

  """Whether the document's discount applies to this line."""
  applyDiscount: Boolean
}

"""
What a caller states when it changes a line that exists.

**A member the caller leaves out is left as it is.** The delivered write is a primitive statement over
the columns the body states, never a replacement of the row, so omitting `price` keeps the price and
omitting `description` keeps the text. A caller that means to clear an optional member states it as
null.

Every column is optional here, and that is the delivered route's own shape rather than a promise made
here: it validates nothing beyond the identifier the path names, and a body that states one member is
a body it serves.
"""
input UpdateInvoiceItemInput {
  id: ID!
  description: String
  price: Decimal
  quantity: Decimal
  totalValue: Decimal
  invoiceId: ID
  organizationId: ID
  taskId: ID
  employeeId: ID
  projectId: ID
  productId: ID
  expenseId: ID
  purchaseOrderLineId: ID
  applyTax: Boolean
  applyDiscount: Boolean
}

"""
The finance document: what one buyer owes, in one currency, under one number of the tenant's series.
"""
type Invoice {
  id: ID!

  """
  The number this document carries, from the tenant's series.
  
  Unique across the table, which is the constraint the delivered schema declares — a series is what
  makes a document quotable, and two documents under one number cannot be told apart. Nullable
  because the column is: a row written outside the numbering path carries none.
  """
  invoiceNumber: Decimal

  """The date the document is issued. The delivered write requires it."""
  invoiceDate: DateTime

  """The date the document falls due. The delivered write requires it."""
  dueDate: DateTime

  """The ISO 4217 code every amount on this document is expressed in."""
  currency: String!

  """
  The discount, as an amount in the document's own currency or as a percentage of the subtotal —
  `discountType` states which of the two it is.
  """
  discountValue: Decimal!

  """Whether the document is marked settled."""
  paid: Boolean

  """
  The first tax applied, as an amount in the document's own currency or as a rate — `taxType`.
  """
  tax: Decimal

  """The second tax applied, on the same terms as the first."""
  tax2: Decimal

  """The payment terms as text, as they are printed on the document."""
  terms: String!

  """The document's total, in the document's own currency."""
  totalValue: Decimal

  """
  Where the document stands, as the contracts' own value: `DRAFT`, `SENT`, `VIEWED`, `FULLY_PAID`,
  `PARTIALLY_PAID`, `OVERPAID` or `VOID` for an invoice, and `ACCEPTED` or `REJECTED` in place of the
  payment states for an estimate. One column serves both readings, which is why the member is the
  value rather than a schema enum.
  """
  status: String!

  """
  Whether this row is an estimate rather than an invoice. The same table holds both.
  """
  isEstimate: Boolean

  """
  Whether the buyer accepted the estimate. An estimate that is not accepted is not billed.
  """
  isAccepted: Boolean

  """
  The discount's kind: `PERCENT` of the subtotal, or `FLAT` as an amount.
  """
  discountType: String

  """
  The first tax's kind: `PERCENT` of the subtotal, or `FLAT` as an amount.
  """
  taxType: String

  """The second tax's kind, on the same two values as the first."""
  tax2Type: String

  """
  How the document's lines are chosen, when the client says so: by employee hours, by project
  hours, by task hours, by products, by expenses, or as detailed items. Stated when the document is
  recorded and not changed afterwards — the delivered edit body does not carry it.
  """
  invoiceType: String

  """The recipient the document was sent to, when it was sent."""
  sentTo: String

  """
  The contact the document is addressed to, as the identifier of the contact's own row.
  """
  organizationContactId: String

  """The note kept for the operator rather than printed for the buyer."""
  internalNote: String

  """
  The amount already received against this document, in the document's own currency.
  """
  alreadyPaid: Decimal

  """The amount still owed, in the document's own currency."""
  amountDue: Decimal

  """
  Whether the buyer still has something to be invoiced for on the agreement behind this document.
  """
  hasRemainingAmountInvoiced: Boolean

  """
  The capability the document's public link is made of. Written by the link route and carried here,
  so a caller that generated the link reads it off the answer to that write.
  """
  token: String

  """
  The settlement schedule this document is settled against.
  
  Its instalments are derived from the term and the document's own date, so `dueDate` becomes the
  last instalment's date rather than a fixed offset, and the schedule is served by the term's own
  resource rather than stored here.
  """
  paymentTermId: ID

  """
  The supplier this document bills *from*, when it is a vendor bill rather than a sales invoice.
  Null on a sales invoice, whose issuing organization is `fromOrganizationId`.
  """
  vendorId: ID

  """The organization that issued the document."""
  fromOrganizationId: ID

  """The contact the document is addressed to, as an identifier."""
  toContactId: ID

  """
  When the document was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live document from a withdrawn one on the answer to the
  write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""
The highest number the tenant's invoice series has reached.

The delivered look-up answers the aggregate row its own statement selects — the SQL alias it computes
the maximum under is `max`, and that is the whole of what it returns — rather than an invoice, which
is what its own return type says. This type is that answer stated as what it is, so a client taking
the next number reads it without mistaking the row for a document.

The number is carried as the exact decimal the column holds rather than as a `Float`, for the reason
the numbering series is carried that way on `Invoice`; and rather than as an `Int`, because the
series is the column's own and a 32-bit integer would refuse a series the column holds.
"""
type InvoiceNumberSeries {
  """The highest number issued, or zero when the tenant has issued none."""
  max: Decimal!
}

"""
One rendered document: the bytes the delivered renderer produced, and what they are.

The REST routes stream a PDF and state its media type and its length in the response headers. A
GraphQL answer has no binary scalar, so the same bytes travel base64-encoded with the media type and
the exact length beside them — which is what lets a client write the document it asked for to disk
without guessing either. The delivered renderer answers nothing when it fails (it logs the failure
rather than raising it), and the REST routes answer an empty body in that case; the fields that
answer this type are therefore nullable, and `null` is that same fact in this protocol's vocabulary.
"""
type InvoiceDocument {
  """The media type of the bytes. The delivered renderer produces PDFs."""
  contentType: String!

  """
  The document's exact length in bytes, as the REST routes state in `Content-Length`.
  """
  byteLength: Int!

  """The document's bytes, base64-encoded."""
  content: String!
}

"""The boundary of one page of invoices."""
type InvoiceEdge {
  node: Invoice!
  cursor: String!
}

"""
One page of invoices, with the total the filters select and the boundary cursors.
"""
type InvoiceConnection {
  nodes: [Invoice!]!
  edges: [InvoiceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an invoice list may be sorted by.

The two timestamps are how a change is found again; the number and the two dates are the document's
own order, which is the order a ledger is read in; `totalValue` and `amountDue` are what a receivable
list is taken by; and `status` is offered because a work list is taken in status groups — the values
sort as their spelling, which groups like with like.

`paid` and `isEstimate` are not sort keys: a boolean order is not an order a document list is read
in, and the questions they answer are filters.
"""
enum InvoiceSortField {
  createdAt
  updatedAt
  invoiceNumber
  invoiceDate
  dueDate
  totalValue
  amountDue
  status
}

"""
The order the invoices are returned in. At most three keys are accepted.
"""
input InvoiceSort {
  field: InvoiceSortField!
  direction: SortDirection!
}

"""
The fields an invoice list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the document's own row, which is why the set is what it is: the delivered
list read answers those columns, so each one narrows rows the connection was handed.

`deletedAt` is deliberately absent although the object carries it: the delivered list reads answer
live rows only, so a condition on that column could only ever match the empty set, and an argument
that cannot be honoured is worse than an argument that is not offered. The tenant and the
organization are absent for a different reason — both are applied to the criterion from the credential
rather than from the caller, so a member here would suggest a scope decision the caller does not have.

The amounts are stated through the decimal family, which is the family money is compared in. **The
values are the row's own**: the columns are `numeric` read through the platform's numeric transformer,
so an equality states the amount as the read produces it (`100.5`, not a padded string), while the
range operators — which are what a receivable list is actually searched with — need no such care.
"""
input InvoiceFilter {
  id: IDFilter
  invoiceNumber: DecimalFilter
  invoiceDate: DateTimeFilter
  dueDate: DateTimeFilter
  currency: StringFilter
  discountValue: DecimalFilter
  discountType: StringFilter
  paid: BooleanFilter
  tax: DecimalFilter
  taxType: StringFilter
  tax2: DecimalFilter
  tax2Type: StringFilter
  terms: StringFilter
  totalValue: DecimalFilter
  status: StringFilter
  isEstimate: BooleanFilter
  isAccepted: BooleanFilter
  invoiceType: StringFilter
  sentTo: StringFilter
  organizationContactId: StringFilter
  internalNote: StringFilter
  alreadyPaid: DecimalFilter
  amountDue: DecimalFilter
  hasRemainingAmountInvoiced: BooleanFilter
  token: StringFilter
  paymentTermId: IDFilter
  vendorId: IDFilter
  fromOrganizationId: IDFilter
  toContactId: IDFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [InvoiceFilter!]
  or: [InvoiceFilter!]
  not: InvoiceFilter
}

"""
What a caller states when it raises an invoice or an estimate.

The members the delivered body validation requires are required here too, so that a request this
surface accepts is a request the delivered write serves: the number, the two dates, the status, the
currency and the issuing organization. The organization the document is filed under is stated as an
identifier and is *not* required, because the delivered validation requires an organization only when
the body names neither the organization object nor a `sentTo` — and the object spelling is a type this
schema does not declare, so the identifier is what this surface offers and the condition stays the
delivered validator's rather than being restated as a non-null member that would refuse a body the
route serves.
"""
input CreateInvoiceInput {
  """The number from the tenant's series."""
  invoiceNumber: Decimal!

  """The date the document is issued."""
  invoiceDate: DateTime!

  """The date the document falls due."""
  dueDate: DateTime!

  """The status the document starts in."""
  status: String!

  """The ISO 4217 code every amount on the document is expressed in."""
  currency: String!

  """
  The organization that issues the document. The delivered write requires it.
  """
  fromOrganizationId: ID!

  """The organization the document is filed under."""
  organizationId: ID

  """The recipient, when the document is recorded as already sent."""
  sentTo: String

  """How the document's lines are chosen."""
  invoiceType: String

  """The total, in the document's own currency."""
  totalValue: Decimal

  """
  The discount, as an amount or as a percentage of the subtotal — `discountType` states which.
  """
  discountValue: Decimal

  """The discount's kind: `PERCENT` or `FLAT`."""
  discountType: String

  """The first tax, as an amount or as a rate — `taxType` states which."""
  tax: Decimal

  """The first tax's kind: `PERCENT` or `FLAT`."""
  taxType: String

  """The second tax, on the same terms as the first."""
  tax2: Decimal

  """The second tax's kind, on the same two values as the first."""
  tax2Type: String

  """Whether the document is recorded as already settled."""
  paid: Boolean

  """The payment terms as text, as they are printed on the document."""
  terms: String

  """The contact the document is addressed to."""
  organizationContactId: String

  """The contact the document is addressed to, as an identifier."""
  toContactId: ID

  """Whether this row is an estimate rather than an invoice."""
  isEstimate: Boolean

  """Whether the buyer has already accepted the estimate."""
  isAccepted: Boolean

  """The note kept for the operator rather than printed for the buyer."""
  internalNote: String

  """The amount already received against the document."""
  alreadyPaid: Decimal

  """The amount still owed."""
  amountDue: Decimal

  """
  Whether the agreement behind the document still has something to be invoiced for.
  """
  hasRemainingAmountInvoiced: Boolean

  """Whether the document is filed away rather than live."""
  isArchived: Boolean

  """The settlement schedule the document is settled against."""
  paymentTermId: ID

  """
  The facets attached to the document, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it edits a document that exists.

The delivered edit dispatches the same command the create dispatches, with the identifier path and the
body together, so the members the delivered validation requires are required here for the same reason
they are required on the create.

**A member the caller leaves out is left as it is.** The delivered write is the platform's own upsert:
it writes the members the body states onto the row the identifier names and touches no others, so
"absent" and "unchanged" coincide and a caller that means to clear an optional member states it as
null. Two consequences follow from the same fact and are worth stating plainly:

- An identifier that names a row of another tenant is refused outright, which is the ownership guard
  the delivered write applies before it persists anything.
- An identifier that names no row at all is **stored as a new row** rather than refused, because an
  upsert is what the delivered write is. A client that means "change this document" reads it first, or
  reaches for the mutation above only with an identifier it obtained from this surface.

`invoiceType` is not a member: the delivered edit body does not carry it, and the way the document's
lines were chosen is fixed when the document is raised.
"""
input UpdateInvoiceInput {
  id: ID!
  invoiceNumber: Decimal!
  invoiceDate: DateTime!
  dueDate: DateTime!
  status: String!
  currency: String!
  organizationId: ID
  sentTo: String
  totalValue: Decimal
  discountValue: Decimal
  discountType: String
  tax: Decimal
  taxType: String
  tax2: Decimal
  tax2Type: String
  paid: Boolean
  terms: String
  organizationContactId: String
  toContactId: ID
  isEstimate: Boolean
  isAccepted: Boolean
  internalNote: String
  alreadyPaid: Decimal
  amountDue: Decimal
  hasRemainingAmountInvoiced: Boolean
  isArchived: Boolean
  paymentTermId: ID

  """
  The facets attached to the document, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it moves a document along.

Every member is optional, because every one of them is a step rather than a replacement: the delivered
route states only the members the caller names, writes them onto the document, and leaves the rest as
they are. The statuses the two flags move between are the contracts' own vocabulary, which is why
`status` is a `String` here — see the note on the object type.
"""
input UpdateInvoiceActionInput {
  """The status to move the document to."""
  status: String

  """Whether the row is an estimate rather than an invoice."""
  isEstimate: Boolean

  """The note kept for the operator rather than printed for the buyer."""
  internalNote: String

  """Whether the document is filed away rather than live."""
  isArchived: Boolean

  """Whether the document is marked settled."""
  paid: Boolean

  """The amount received against the document."""
  alreadyPaid: Decimal

  """The amount still owed."""
  amountDue: Decimal

  """The recipient the document has been sent to."""
  sentTo: String
}

"""
What a caller states when it sends a document to a recipient.

The delivered route takes the recipient in its path, a body whose `params` member names the document,
and the language and the origin from the request's own headers. Those headers are read here from the
same request — the language is the one the delivered `language` header names and the origin the one
the `origin` header carries — so a message sent over either protocol is the same message.
"""
input SendInvoiceEmailInput {
  """The recipient's email address."""
  email: String!

  """The document the message is about."""
  invoiceId: ID!

  """The number of the document the message is about."""
  invoiceNumber: Decimal!

  """Whether the document is an estimate rather than an invoice."""
  isEstimate: Boolean!

  """The organization that issued the document."""
  organizationId: ID!
}

"""One catalogue entry a key result can be authored from."""
type KeyResultTemplate {
  id: ID!

  """The name of the key result this entry offers."""
  name: String!

  """
  The kind of key result the entry offers. The value set belongs to the domain's own contract, so it
  is carried as its value rather than declared here as an enum.
  """
  type: String!

  """The unit the reading is expressed in, as text."""
  unit: String

  """The reading the entry suggests aiming at, when it suggests one."""
  targetValue: Int

  """The reading the entry suggests starting from, when it suggests one."""
  initialValue: Int

  """
  Which deadline the entry suggests. Carried as its value rather than declared here as an enum, for
  the reason `type` is.
  """
  deadline: String!

  """The KPI template this entry suggests tracking, by identifier."""
  kpiId: ID

  """
  The goal template this entry belongs to, by identifier. A goal template's key results are this
  connection narrowed by it.
  """
  goalId: ID

  """
  When the entry was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the entry belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the entry is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of catalogue entries."""
type KeyResultTemplateEdge {
  node: KeyResultTemplate!
  cursor: String!
}

"""
One page of catalogue entries, with the total the filters select and the boundary cursors.
"""
type KeyResultTemplateConnection {
  nodes: [KeyResultTemplate!]!
  edges: [KeyResultTemplateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a catalogue list may be sorted by."""
enum KeyResultTemplateSortField {
  createdAt
  updatedAt
  name
  type
  targetValue
  initialValue
  deadline
}

"""
The order the catalogue is returned in. At most three keys are accepted.
"""
input KeyResultTemplateSort {
  field: KeyResultTemplateSortField!
  direction: SortDirection!
}

"""The fields a catalogue list may be narrowed by."""
input KeyResultTemplateFilter {
  id: IDFilter
  name: StringFilter
  type: StringFilter
  unit: StringFilter
  targetValue: NumberFilter
  initialValue: NumberFilter
  deadline: StringFilter
  kpiId: IDFilter
  goalId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [KeyResultTemplateFilter!]
  or: [KeyResultTemplateFilter!]
  not: KeyResultTemplateFilter
}

"""
What a caller states when it files a catalogue entry.

The delivered body is validated by a DTO that joins this row's own members to the two relations it
carries, and it requires a name, a kind and a deadline. The tenant is never a member — the service
stamps it from the credential — and `organizationId` is, because it is the row's own column and the
delivered validation does not strip it.
"""
input CreateKeyResultTemplateInput {
  name: String!
  type: String!
  unit: String
  targetValue: Int
  initialValue: Int
  deadline: String!
  kpiId: ID

  """The goal template to file the entry under."""
  goalId: ID
  organizationId: ID
}

"""
What a caller states when it changes a catalogue entry.

**A member that is absent is left as it is.** The delivered edit is the CRUD base's partial column
update rather than a replacement: it reads the row first — so an entry of another tenant, or one that
is not there, is a miss rather than a write — and then updates the stated columns and no others.
"""
input UpdateKeyResultTemplateInput {
  id: ID!
  name: String
  type: String
  unit: String
  targetValue: Int
  initialValue: Int
  deadline: String
  kpiId: ID
  goalId: ID
  organizationId: ID
}

"""One move a key result made, as it was recorded."""
type KeyResultUpdate {
  id: ID!

  """The reading the key result moved to."""
  update: Int!

  """The progress that reading puts the key result on."""
  progress: Int!

  """The employee or process that recorded the move, as the row states it."""
  owner: String!

  """
  The state the key result was left in. The value set belongs to the domain's own contract, so it is
  carried as its value rather than declared here as an enum.
  """
  status: String!

  """The key result this move was recorded against, by identifier."""
  keyResultId: ID

  """
  When the update was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the update belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the update is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of updates."""
type KeyResultUpdateEdge {
  node: KeyResultUpdate!
  cursor: String!
}

"""
One page of updates, with the total the filters select and the boundary cursors.
"""
type KeyResultUpdateConnection {
  nodes: [KeyResultUpdate!]!
  edges: [KeyResultUpdateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an update list may be sorted by."""
enum KeyResultUpdateSortField {
  createdAt
  updatedAt
  progress
  status
}

"""
The order the updates are returned in. At most three keys are accepted.
"""
input KeyResultUpdateSort {
  field: KeyResultUpdateSortField!
  direction: SortDirection!
}

"""The fields an update list may be narrowed by."""
input KeyResultUpdateFilter {
  id: IDFilter
  update: NumberFilter
  progress: NumberFilter
  owner: StringFilter
  status: StringFilter
  keyResultId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [KeyResultUpdateFilter!]
  or: [KeyResultUpdateFilter!]
  not: KeyResultUpdateFilter
}

"""
What a caller states when it records a move.

The delivered body declares the organization as required — the DTO refuses a body that names neither
an organization, an organization object nor a recipient, and checks the value against the caller's own
memberships — so the member is required here too rather than optional as it is on the sibling
resources. The tenant is never a member: the service stamps it from the credential. `keyResultId` is
the key result the move is recorded against.
"""
input CreateKeyResultUpdateInput {
  owner: String!
  progress: Int!
  update: Int!
  status: String!
  keyResultId: ID
  organizationId: ID!
}

"""
What a caller states when it corrects a move.

**A member that is absent is left as it is.** The delivered edit is an update-through-create: the
stated members are merged onto the row the store holds. The delivered route answers nothing at all —
it catches whatever the write threw and returns an empty body — so this surface answers the write's
own refusal instead of a silence; see the field's own note.
"""
input UpdateKeyResultUpdateInput {
  id: ID!
  owner: String
  progress: Int
  update: Int
  status: String
  keyResultId: ID
  organizationId: ID
}

"""One measurable outcome an objective is judged by."""
type KeyResult {
  id: ID!

  """The outcome's name."""
  name: String!

  """What the outcome is for."""
  description: String

  """
  The kind of key result this is: a measure, a piece of work, or a project's own result. The value set
  belongs to the domain's own contract, so it is carried as its value rather than declared here as an
  enum.
  """
  type: String!

  """The reading the outcome is aiming at, when one is stated."""
  targetValue: Int

  """The reading the outcome started from, when one is stated."""
  initialValue: Int

  """The unit the readings are expressed in, as text."""
  unit: String

  """The latest reading recorded against the outcome."""
  update: Int!

  """
  How far the outcome has come, as the whole number of percent the column holds.
  """
  progress: Int!

  """
  Which deadline the outcome runs on. Carried as its value rather than declared here as an enum, for
  the reason `type` is.
  """
  deadline: String!

  """
  The instant the outcome must be met by, when the deadline is a hard one.
  """
  hardDeadline: DateTime

  """The instant the outcome is aimed at, when the deadline is a soft one."""
  softDeadline: DateTime

  """The state the outcome is in, as the row states it."""
  status: String

  """
  The weight the outcome carries among its siblings. The column is text, so this is carried as text;
  see the note above the type.
  """
  weight: String

  """The employee who owns the outcome, by identifier."""
  ownerId: ID!

  """The employee who leads the outcome, by identifier."""
  leadId: ID

  """The project the outcome is filed under, by identifier."""
  projectId: ID

  """The task the outcome is measured by, by identifier."""
  taskId: ID

  """The measure the outcome tracks, by identifier."""
  kpiId: ID

  """
  The objective this outcome belongs to, by identifier. An objective's key results are this
  connection narrowed by it.
  """
  goalId: ID

  """
  When the outcome was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """The tenant the outcome belongs to, stamped from the credential."""
  tenantId: ID

  """The organization the outcome is scoped to."""
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of key results."""
type KeyResultEdge {
  node: KeyResult!
  cursor: String!
}

"""
One page of key results, with the total the filters select and the boundary cursors.
"""
type KeyResultConnection {
  nodes: [KeyResult!]!
  edges: [KeyResultEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a key result list may be sorted by."""
enum KeyResultSortField {
  createdAt
  updatedAt
  name
  type
  progress
  targetValue
  deadline
  status
}

"""
The order the key results are returned in. At most three keys are accepted.
"""
input KeyResultSort {
  field: KeyResultSortField!
  direction: SortDirection!
}

"""
The fields a key result list may be narrowed by.

Every member is a column of the key-result row, which is what the connection protocol evaluates. An
objective's key results are this connection narrowed by `goalId`; one employee's are the same
connection narrowed by `ownerId`.
"""
input KeyResultFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  type: StringFilter
  targetValue: NumberFilter
  initialValue: NumberFilter
  unit: StringFilter
  update: NumberFilter
  progress: NumberFilter
  deadline: StringFilter
  hardDeadline: DateTimeFilter
  softDeadline: DateTimeFilter
  status: StringFilter
  weight: StringFilter
  ownerId: IDFilter
  leadId: IDFilter
  projectId: IDFilter
  taskId: IDFilter
  kpiId: IDFilter
  goalId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [KeyResultFilter!]
  or: [KeyResultFilter!]
  not: KeyResultFilter
}

"""
What a caller states when it authors a key result.

**No member is required, and that is the delivered DTO's own shape rather than an oversight.** The
body the delivered route validates marks every member optional; the columns behind six of them —
`name`, `type`, `update`, `progress`, `deadline` and `ownerId` — are not nullable, so a body that
omits one is refused by the store rather than by validation. Stating them as required here would
refuse a body the delivered route accepts, which is the one thing a second surface may not do.

The tenant is never a member: the service stamps it from the credential. `organizationId` is, because
it is the row's own column and the delivered validation does not strip it. The updates the outcome
will carry are not a member either — they are filed from their own surface against this row's
identifier.
"""
input CreateKeyResultInput {
  name: String
  description: String
  type: String
  targetValue: Int
  initialValue: Int
  unit: String
  update: Int
  progress: Int
  deadline: String
  hardDeadline: DateTime
  softDeadline: DateTime
  status: String
  weight: String
  ownerId: ID
  leadId: ID
  projectId: ID
  taskId: ID
  kpiId: ID
  goalId: ID
  organizationId: ID
}

"""
What a caller states when it changes a key result.

**A member that is absent is left as it is.** The delivered edit is the delivered creation called with
the stated identifier spread over the body — the controller's route is an update-through-create,
because the write has to go through the save a partial column update would bypass — so the stated
members are merged onto the row the store holds. An identifier that names no row of the caller's
tenant is an insert rather than a miss, which is the delivered route's own behaviour and not
something this surface narrows.
"""
input UpdateKeyResultInput {
  id: ID!
  name: String
  description: String
  type: String
  targetValue: Int
  initialValue: Int
  unit: String
  update: Int
  progress: Int
  deadline: String
  hardDeadline: DateTime
  softDeadline: DateTime
  status: String
  weight: String
  ownerId: ID
  leadId: ID
  projectId: ID
  taskId: ID
  kpiId: ID
  goalId: ID
  organizationId: ID
}

"""One language the platform knows, as installation-wide reference data."""
type Language {
  id: ID!

  """The language's own name."""
  name: String

  """The language's code, unique across the platform."""
  code: String

  """
  Whether the platform itself seeded the row, as opposed to an installation adding one.
  """
  is_system: Boolean
  description: String

  """The colour the language is shown in, where a client renders one."""
  color: String

  """Whether the row is active."""
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of languages."""
type LanguageEdge {
  node: Language!
  cursor: String!
}

"""
One page of languages, with the total the filters select and the boundary cursors.
"""
type LanguageConnection {
  nodes: [Language!]!
  edges: [LanguageEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a language list may be sorted by."""
enum LanguageSortField {
  createdAt
  updatedAt
  name
  code
}

"""
The order the languages are returned in. At most three keys are accepted.
"""
input LanguageSort {
  field: LanguageSortField!
  direction: SortDirection!
}

"""
The fields a language list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned rather than translating the filter into a query. `is_system` is spelled
as the column is, for the reason the object type states.
"""
input LanguageFilter {
  id: IDFilter
  name: StringFilter
  code: StringFilter
  is_system: BooleanFilter
  description: StringFilter
  color: StringFilter
  isActive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [LanguageFilter!]
  or: [LanguageFilter!]
  not: LanguageFilter
}

type UnitCategory {
  id: ID!
  organizationId: ID
  code: String!
  name: String!
  isSystem: Boolean!
  units: [Unit!]
}

type Unit {
  id: ID!
  organizationId: ID
  categoryId: ID!
  category: UnitCategory
  code: String!
  name: String!
  symbol: String
  factor: String!
  isReference: Boolean!
  decimalPlaces: Int!
  isSystem: Boolean!
}

input CreateUnitCategoryReferenceInput {
  code: String!
  name: String!
  symbol: String
  decimalPlaces: Int
}

input CreateUnitCategoryInput {
  organizationId: ID!
  code: String!
  name: String!
  isSystem: Boolean
  reference: CreateUnitCategoryReferenceInput!
}

input UpdateUnitCategoryInput {
  id: ID!
  organizationId: ID!
  name: String
}

input CreateUnitInput {
  organizationId: ID!
  categoryId: ID!
  code: String!
  name: String!
  symbol: String
  factor: String
  isReference: Boolean
  decimalPlaces: Int
}

input UpdateUnitInput {
  id: ID!
  organizationId: ID!
  name: String
  symbol: String
  factor: String
  decimalPlaces: Int
}

type UnitConversion {
  value: String!
  fromUnitId: ID!
  toUnitId: ID!
}

"""One employee named another in the text of one entity."""
type Mention {
  id: ID!

  """
  Who performed the mentioning, as the contract's own name for it: `System` or `User`. Nullable
  because the column is — a row written before the actor was recorded carries neither.
  """
  actorType: String

  """
  The type of the entity the text belongs to, as the platform's own entity-type value. It is stored
  on this row as text, so it is carried as text rather than as a declared enum.
  """
  entity: String!

  """The entity the text belongs to, by identifier."""
  entityId: ID!

  """
  The record the mention is about when the text sits one level below it — the task a comment was
  written on, for instance — by identifier. Null on a mention written directly on its entity.
  """
  parentEntityId: ID

  """
  The type of that parent record, as the platform's own entity-type value.
  """
  parentEntityType: String

  """
  The employee who was named, by identifier. This is the one relation the row cannot be written
  without, which is what makes it the member a filing is stated for. The account behind it is not
  joined by this read.
  """
  mentionedEmployeeId: ID!

  """
  The employee who did the naming, by identifier. The delivered filing stamps it from the credential
  rather than from the body, and it is nullable for rows written outside a request. The account behind
  it is not joined by this read.
  """
  employeeId: ID

  """The tenant the mention belongs to, by identifier."""
  tenantId: ID

  """The organization the mention belongs to, by identifier."""
  organizationId: ID

  """
  Whether the mention is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the mention is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the mention was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the mention was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — two of the nine this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live row from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime

  """When the mention was filed."""
  createdAt: DateTime

  """When the mention was last changed."""
  updatedAt: DateTime
}

"""The boundary of one page of mentions."""
type MentionEdge {
  node: Mention!
  cursor: String!
}

"""
One page of mentions, with the total the filters select and the boundary cursors.
"""
type MentionConnection {
  nodes: [Mention!]!
  edges: [MentionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a mention list may be sorted by."""
enum MentionSortField {
  id
  entity
  entityId
  parentEntityType
  mentionedEmployeeId
  createdAt
  updatedAt
  deletedAt
}

"""
The order the mentions are returned in. At most three keys are accepted.
"""
input MentionSort {
  field: MentionSortField!
  direction: SortDirection!
}

"""
The fields a mention list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the mention row, because the delivered list read answers the row itself
and joins only the relations its REST caller names — and this connection's read names none. The two
employee members are therefore identifiers rather than relation paths: narrowing by who was mentioned,
or by who did the mentioning, is a condition on the identifier this row stores.

No member narrows by the tenant on the caller's behalf. The tenant comes from the credential on every
read, so the identifier member above narrows the row's own column rather than restating a scope
decision the caller does not have.
"""
input MentionFilter {
  id: IDFilter
  actorType: StringFilter
  entity: StringFilter
  entityId: IDFilter
  parentEntityId: IDFilter
  parentEntityType: StringFilter
  mentionedEmployeeId: IDFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [MentionFilter!]
  or: [MentionFilter!]
  not: MentionFilter
}

"""
What a caller states when it files a mention.

The four members beside the parent pair are the ones a mention cannot be filed without, and each is
required for its own reason. `entity` and `entityId` say which text the mention belongs to, and both
are written into columns the row cannot be stored without. `mentionedEmployeeId` is the relation the
row is *for* — the one relation column that is not nullable — so a filing without it is not a mention
at all. `organizationId` is required because the delivered filing threads it through the two effects
it raises beside the row: the subscription it publishes for the mentioned employee and the
notification it raises for them, each of which is addressed to that organization. A body that omitted
it would file a mention nobody is notified of, which is not a filing this surface offers.

The parent pair is optional and is what makes a mention about a record other than the one its text sits
on: state it when the text is one level below the record the subscription belongs to.

There is no member for the tenant, and none for the employee who did the mentioning: the delivered
service stamps both from the credential and overwrites whatever a body states, so a member here would
promise a scope and an authorship the write refuses.

`entityName` is a member although it is no column of this row: the delivered filing reads it off the
input to title the notification it raises, so a caller that wants the notification to name the thing
being discussed states it here.
"""
input CreateMentionInput {
  """
  The type of the entity the mention's text belongs to, as the platform's own entity-type value.
  """
  entity: String!

  """The entity the mention's text belongs to, by identifier."""
  entityId: ID!

  """The employee who was named, by identifier."""
  mentionedEmployeeId: ID!

  """
  The organization the mention — and the subscription and notification it raises — belongs to.
  """
  organizationId: ID!

  """
  The record the mention is about, when the text sits one level below it.
  """
  parentEntityId: ID

  """The type of that parent record."""
  parentEntityType: String

  """
  Who performed the mentioning, as the contract's own name for it: `System` or `User`.
  """
  actorType: String

  """The name the notification shows for the entity being discussed."""
  entityName: String
}

"""
What a caller states when it changes a mention that exists.

**A member the caller leaves out is left as it is.** The delivered route reads the row the identifier
names — proving it is the caller's own — and then writes the members the body states to it, so this is
a column update and not a replacement of the row. The identifier is the one required member, and it is
stated because the delivered route carries it in the path rather than in the body.

`entityName` is deliberately absent here, although the filing states it: the delivered update never
reads it, so a member here could only be accepted and dropped.
"""
input UpdateMentionInput {
  id: ID!

  """The type of the entity the mention's text belongs to."""
  entity: String

  """The entity the mention's text belongs to, by identifier."""
  entityId: ID

  """The employee who was named, by identifier."""
  mentionedEmployeeId: ID

  """The organization the mention belongs to."""
  organizationId: ID

  """
  The record the mention is about, when the text sits one level below it.
  """
  parentEntityId: ID

  """The type of that parent record."""
  parentEntityType: String

  """
  Who performed the mentioning, as the contract's own name for it: `System` or `User`.
  """
  actorType: String
}

"""
The store a sale is attributed to: its trading identity, its currency and its logo.
"""
type Merchant {
  id: ID!

  """The trading name of the store, as entered."""
  name: String!

  """
  The store's own code, unique within the organization among the live rows.
  """
  code: String!

  """The address the store is written to."""
  email: String!
  phone: String
  description: String

  """
  Whether the store is trading. A removal sets `deletedAt`; it does not clear this flag.
  """
  active: Boolean!

  """
  The currency the store's amounts are read in, as the money layer's own value — for example `USD`.
  It is a code rather than an amount, so it is text and not a `Decimal`.
  """
  currency: String!

  """The store's own contact row. Null on a store filed without one."""
  contactId: ID

  """The asset the store is shown with. Null on a store filed without one."""
  logoId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the store was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of stores."""
type MerchantEdge {
  node: Merchant!
  cursor: String!
}

"""
One page of stores, with the total the filters select and the boundary cursors.
"""
type MerchantConnection {
  nodes: [Merchant!]!
  edges: [MerchantEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a store list may be sorted by."""
enum MerchantSortField {
  createdAt
  updatedAt
  name
  code
  email
}

"""The order the stores are returned in. At most three keys are accepted."""
input MerchantSort {
  field: MerchantSortField!
  direction: SortDirection!
}

"""
The fields a store list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned rather than translating the filter into a query. `deletedAt` is
filterable so a caller can ask which rows have been withdrawn, while the read itself answers live rows
only.
"""
input MerchantFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  code: StringFilter
  email: StringFilter
  phone: StringFilter
  description: StringFilter
  active: BooleanFilter
  currency: StringFilter
  contactId: IDFilter
  logoId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [MerchantFilter!]
  or: [MerchantFilter!]
  not: MerchantFilter
}

"""
What a caller states when it files a store.

The organization is required because the delivered create validates it: `POST /merchants` binds a DTO
that intersects the organization base, and that base refuses a body naming neither an organization nor
a sent-to destination. It requires the name, the code and the email for the same reason — the DTO
marks all three non-empty — so a store cannot be filed half-identified. There is no member for the
tenant, because the service stamps the caller's own tenant onto the row.

**The two pivots are stated as identifier lists**, which is this protocol's spelling of the same
write: the delivered body carries whole `tags` and `warehouses` arrays and the store persists the
pivot rows for them, and a caller that has the rows in hand already has their identifiers. The
delivered writer cascades the owning side, so a list that names rows of another organization is
refused by the store's own constraints rather than by this surface.
"""
input CreateMerchantInput {
  organizationId: ID!
  name: String!
  code: String!
  email: String!
  phone: String
  description: String
  active: Boolean

  """The store's currency, as the money layer's own value."""
  currency: String

  """The store's own contact row, by identifier."""
  contactId: ID

  """The asset the store is shown with, by identifier."""
  logoId: ID

  """The tags to classify the store with."""
  tagIds: [ID!]

  """The warehouses to attach to the store."""
  warehouseIds: [ID!]
}

"""
What a caller states when it changes a store that exists.

**The organization, the name, the code and the email are required, and the write is nevertheless a
merge rather than a replacement.** The delivered route validates those four on the body and then hands
the parsed body to a service that saves it beside the path identifier: the columns the body carries
are written and the ones it does not are left as they are. The two statements are not in conflict —
the route is simply stricter about what a caller must restate than the write is about what it
overwrites — and stating only one of them here would tell a client something the delivered pair does
not.
"""
input UpdateMerchantInput {
  id: ID!
  organizationId: ID!
  name: String!
  code: String!
  email: String!
  phone: String
  description: String
  active: Boolean
  currency: String
  contactId: ID
  logoId: ID
  tagIds: [ID!]
  warehouseIds: [ID!]
}

"""One publicly recognised day off, for a country, kept per organization."""
type OfficialHoliday {
  id: ID!

  """The holiday's display name, for example `Christmas Day`."""
  name: String!

  """
  The ISO 3166-1 alpha-2 code of the country the holiday belongs to, for example `US`.
  """
  countryCode: String!

  """
  The holiday's date, or the first day of a multi-day holiday. A calendar day: the columns are
  `date` rather than `timestamp`, so no timezone shifts it.
  """
  date: DateTime!

  """The last day of a multi-day holiday, or null when it lasts one day."""
  endDate: DateTime

  """Whether the holiday falls on the same date every year."""
  isRecurring: Boolean
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """
  When the row was marked as removed, or null while it is live. This resource serves no withdrawal
  route — its controller is not a CRUD controller and declares no lifecycle pair — so the column is
  carried because the delivered edit body can write it rather than because a route of this domain
  moves it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of holidays."""
type OfficialHolidayEdge {
  node: OfficialHoliday!
  cursor: String!
}

"""
One page of holidays, with the total the filters select and the boundary cursors.
"""
type OfficialHolidayConnection {
  nodes: [OfficialHoliday!]!
  edges: [OfficialHolidayEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a holiday list may be sorted by."""
enum OfficialHolidaySortField {
  createdAt
  updatedAt
  name
  countryCode
  date
  endDate
}

"""
The order the holidays are returned in. At most three keys are accepted.
"""
input OfficialHolidaySort {
  field: OfficialHolidaySortField!
  direction: SortDirection!
}

"""
The fields a holiday list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

**The delivered calendar-year narrowing is a filter rather than an argument, and the `or` group is
what states it.** The delivered query DTO carries a `year` member, which the read turns into a
disjunction: the holiday falls inside that year, **or** the holiday recurs. That is not a column and
cannot be an equality, but it is exactly what the connection protocol's `or` means, so a caller asks
the same question the route answers with:

```graphql
filter: { or: [{ date: { between: ["2026-01-01", "2026-12-31"] } }, { isRecurring: { eq: true } }] }
```

The delivered `countryCode` narrowing upper-cases what it is given before it matches, so a caller
matching a stored code matches the code as it is stored — `eq: "DE"`, or `ilike: "de"` for the
case-insensitive spelling of the same question.
"""
input OfficialHolidayFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  countryCode: StringFilter
  date: DateTimeFilter
  endDate: DateTimeFilter
  isRecurring: BooleanFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OfficialHolidayFilter!]
  or: [OfficialHolidayFilter!]
  not: OfficialHolidayFilter
}

"""
What a caller states when it files a holiday.

The organization is required because the delivered create validates it: `POST /official-holiday`
binds a DTO that intersects the organization base, and that base refuses a body naming neither an
organization nor a sent-to destination. The name, the country code and the date are required for the
same reason — the DTO marks all three non-empty. There is no member for the tenant, because the
service stamps the caller's own tenant onto the row.

A holiday that is not recurring is stored once, against whatever year it was entered for; a recurring
one is stored once and read by every year. That is the delivered read's own rule rather than a member
of this input — see the filter above for how a year narrows a list.
"""
input CreateOfficialHolidayInput {
  organizationId: ID!
  name: String!
  countryCode: String!

  """The holiday's date, or the first day of a multi-day holiday."""
  date: DateTime!

  """
  The last day of a multi-day holiday. Omit for a holiday that lasts one day.
  """
  endDate: DateTime
  isRecurring: Boolean
  isActive: Boolean
  isArchived: Boolean
}

"""
What a caller states when it changes a holiday that exists.

**Every member but the identifier is optional**, because the delivered edit binds a partial type: a
member the caller leaves out is left as it is, and the delivered write is the CRUD base's partial
column update rather than a replacement of the row.
"""
input UpdateOfficialHolidayInput {
  id: ID!
  organizationId: ID
  name: String
  countryCode: String
  date: DateTime
  endDate: DateTime
  isRecurring: Boolean
  isActive: Boolean
  isArchived: Boolean
}

"""
The boundary of one page of operations.

The cursor is the platform's own: it is produced and consumed by the same codec the REST list route
uses, so a cursor obtained over one surface resumes on the other.
"""
type OperationEdge {
  node: Operation!
  cursor: String!
}

"""
One page of operations, with the total the filters select and the boundary cursors.
"""
type OperationConnection {
  nodes: [Operation!]!
  edges: [OperationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an operation may be sorted by."""
enum OperationSortField {
  createdAt
  updatedAt
  startedAt
  finishedAt
  deadlineAt
  type
  status
}

"""
The order the operations are returned in. At most three keys are accepted.
"""
input OperationSort {
  field: OperationSortField!
  direction: SortDirection!
}

"""Comparison on a status."""
input OperationStatusFilter {
  eq: OperationStatus
  ne: OperationStatus
  in: [OperationStatus!]
  nin: [OperationStatus!]
  isNull: Boolean
}

"""
The fields an operation list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

The lease columns are deliberately absent: `lockedBy` is an internal worker identity and
`leaseExpiresAt` answers "is this operation stuck", which the deadline and the status already say in
the vocabulary an operator reads. The tenant is absent for the reason every resource's filter omits
it — it is applied to the criterion from the credential, never from the caller — and the
organization is absent because the reads are not scoped by one: an operation a step of another
operation started carries no organization, and the resource's scope is the tenant's.
"""
input OperationFilter {
  id: IDFilter
  type: StringFilter
  status: OperationStatusFilter
  aggregateType: StringFilter
  aggregateId: IDFilter
  parentOperationId: IDFilter
  correlationId: IDFilter
  idempotencyKey: StringFilter
  deadlineAt: DateTimeFilter
  startedAt: DateTimeFilter
  finishedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OperationFilter!]
  or: [OperationFilter!]
  not: OperationFilter
}

"""One award an organization records about itself."""
type OrganizationAward {
  id: ID!

  """The award's title, as the organization wrote it."""
  name: String!

  """
  The period the award belongs to, as the organization wrote it. Text rather than a number: the
  column is text and holds whatever the organization stated, a financial year as readily as a
  calendar one.
  """
  year: String!

  """The organization the award is filed under, by identifier."""
  organizationId: ID

  """The tenant the award belongs to, by identifier."""
  tenantId: ID

  """
  When the award was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the award was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the award was filed."""
  createdAt: DateTime

  """When the award was last changed."""
  updatedAt: DateTime

  """
  Whether the award is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the award is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the award, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the award, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the award, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of awards."""
type OrganizationAwardEdge {
  node: OrganizationAward!
  cursor: String!
}

"""
One page of awards, with the total the filters select and the boundary cursors.
"""
type OrganizationAwardConnection {
  nodes: [OrganizationAward!]!
  edges: [OrganizationAwardEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an award list may be sorted by."""
enum OrganizationAwardSortField {
  createdAt
  updatedAt
  name
}

"""The order the awards are returned in. At most three keys are accepted."""
input OrganizationAwardSort {
  field: OrganizationAwardSortField!
  direction: SortDirection!
}

"""
The fields an award list may be narrowed by.

`year` is filterable and not sortable: an equality or a pattern on it is a question a caller asks, and
an ordering over text that only sometimes looks like a year is not one this surface can answer
honestly.
"""
input OrganizationAwardFilter {
  id: IDFilter
  name: StringFilter
  year: StringFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationAwardFilter!]
  or: [OrganizationAwardFilter!]
  not: OrganizationAwardFilter
}

"""
What a caller states when it files an award.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.
"""
input CreateOrganizationAwardInput {
  organizationId: ID!
  name: String!

  """The period the award belongs to, as the organization writes it."""
  year: String!
}

"""
What a caller states when it edits an award that exists.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path. The delivered edit is a partial column update rather than a replacement
of the row, so a member the caller leaves out is left as it is.
"""
input UpdateOrganizationAwardInput {
  id: ID!
  organizationId: ID
  name: String
  year: String
}

"""
One commercial counterparty: the party an order, an invoice and a subscription all point at.
"""
type OrganizationContact {
  id: ID!
  tenantId: ID
  organizationId: ID

  """
  The display name of the party. A party that names nothing names no counterparty.
  """
  name: String!

  """
  The address a document is mailed to and a guest order is matched on. Not unique, by design.
  """
  primaryEmail: String
  primaryPhone: String

  """How far the invitation to the party's own portal has come."""
  inviteStatus: String
  notes: String

  """
  What the party is to the organization. The discriminator every list of parties is read by.
  """
  contactType: ContactType!

  """
  A denormalised avatar URL, kept for lists that must not join the asset table.
  """
  imageUrl: String

  """
  The amount the relationship was budgeted for. Its unit is `budgetType`: money when that is `cost`,
  hours when it is `hours`.
  """
  budget: Decimal

  """Whether `budget` is an amount of money or a number of hours."""
  budgetType: String

  """
  The staff user the party is also linked to, when it is. Null for a party who never logs into the
  back office, which is the common case.
  """
  userId: ID

  """The channel the relationship was acquired through."""
  channelId: ID

  """The commercial lifecycle: `GUEST`, `ACTIVE`, `BLOCKED` or `ARCHIVED`."""
  status: String

  """
  The price list assigned to this party. It wins over a group list and over the default list.
  """
  priceListId: ID

  """Party-wide tax exemption: an export customer, an exempt institution."""
  taxExempt: Boolean

  """
  The tax category applied to this party's lines when the variant's own category does not govern.
  """
  taxCategoryId: ID

  """Ceiling on outstanding credit. Null means no credit facility."""
  creditLimit: Decimal

  """
  Outstanding credit, re-derived from the order and refund ledgers by the nightly reconciliation.
  The ceiling check reads both members, which is why they are on one row.
  """
  creditUsed: Decimal

  """
  Net-N terms for invoiced orders; null means due on receipt or the assigned term's schedule.
  """
  paymentTermsDays: Int

  """
  The loyalty balance: the cached figure a checkout reads without summing the movement ledger.
  """
  loyaltyPoints: Decimal

  """
  The address pre-selected at checkout. A reference into the address book, never a copy.
  """
  defaultShippingAddressId: ID

  """The billing counterpart of the above, on the same terms."""
  defaultBillingAddressId: ID

  """Whether the party consented to marketing, with its own audit value."""
  acceptsMarketing: Boolean

  """
  Free-text acquisition source for reporting (`organic`, `campaign-x`, `import`).
  """
  acquiredChannel: String

  """
  The normalised (trimmed, lower-cased) form of `primaryEmail`, maintained by a subscriber. It is
  the key duplicate detection and guest-order matching compare on.
  """
  emailKey: String

  """
  The party's key in an upstream CRM or ERP, used as the upsert key by the import jobs.
  """
  externalId: String

  """
  Whether the party is a natural person (`INDIVIDUAL`) or a legal entity (`COMPANY`).
  """
  partyKind: String

  """
  The currency this party contracts in. Null means the region's currency.
  """
  currency: String

  """
  The instalment schedule this party is settled against. It supersedes `paymentTermsDays`.
  """
  paymentTermId: ID

  """
  The party's VAT / GST / TIN number. Reverse charge and intra-community zero-rating are
  conditional on the **buyer's** registration being present.
  """
  taxRegistrationNumber: String

  """Which registration scheme the number belongs to."""
  taxRegistrationScheme: String

  """
  The tax regime assigned to this party by hand. It always wins over automatic matching.
  """
  taxRegimeId: ID

  """Tenant-defined, non-indexed extras."""
  metadata: JSON

  """
  The party's person-or-place detail row. The identifier always travels with this row: the detail
  itself is joined only by the reads whose caller names it, and is read as `contact(id)` on the
  contact domain.
  """
  contactId: ID

  """
  The avatar asset. The identifier travels; the asset row is joined on request.
  """
  imageId: ID

  """
  Whether the row is active. The commercial lifecycle is `status`, not this flag.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime

  """
  When the row was withdrawn, or null while it is live. The two lifecycle routes write this alone.
  """
  deletedAt: DateTime
}

"""
What a party is to the organization.

It is the resource's own discriminator and the narrowing every list of parties is read by, so it is
declared here as a closed set. The three values are the ones the column stores.
"""
enum ContactType {
  """A party the organization sells to on account."""
  CLIENT

  """A party that buys, typically a consumer rather than an account."""
  CUSTOMER

  """A party that has not bought yet."""
  LEAD
}

"""The boundary of one page of parties."""
type OrganizationContactEdge {
  node: OrganizationContact!
  cursor: String!
}

"""
One page of parties, with the total the filters select and the boundary cursors.
"""
type OrganizationContactConnection {
  nodes: [OrganizationContact!]!
  edges: [OrganizationContactEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a party list may be sorted by."""
enum OrganizationContactSortField {
  createdAt
  updatedAt
  name
  primaryEmail
  contactType
  status
  creditLimit
  loyaltyPoints
}

"""
The order the parties are returned in. At most three keys are accepted.
"""
input OrganizationContactSort {
  field: OrganizationContactSortField!
  direction: SortDirection!
}

"""Comparison on what a party is to the organization."""
input ContactTypeFilter {
  eq: ContactType
  ne: ContactType
  in: [ContactType!]
  nin: [ContactType!]
  isNull: Boolean
}

"""
The fields a party list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned: a member the read does not select would be a filter that silently
selects nothing. The collections are absent for the same reason — a filter on `members` would be
evaluated against a row the list read never joined, and would select nothing at all rather than
selecting the wrong rows, which is the worst answer a filter can give.
"""
input OrganizationContactFilter {
  id: IDFilter
  name: StringFilter
  primaryEmail: StringFilter
  primaryPhone: StringFilter
  inviteStatus: StringFilter
  contactType: ContactTypeFilter
  imageUrl: StringFilter
  userId: IDFilter
  channelId: IDFilter
  status: StringFilter
  priceListId: IDFilter
  taxExempt: BooleanFilter
  taxCategoryId: IDFilter
  budget: DecimalFilter
  budgetType: StringFilter
  creditLimit: DecimalFilter
  creditUsed: DecimalFilter
  paymentTermsDays: NumberFilter
  loyaltyPoints: DecimalFilter
  defaultShippingAddressId: IDFilter
  defaultBillingAddressId: IDFilter
  acceptsMarketing: BooleanFilter
  acquiredChannel: StringFilter
  emailKey: StringFilter
  externalId: StringFilter
  partyKind: StringFilter
  currency: StringFilter
  paymentTermId: IDFilter
  taxRegistrationNumber: StringFilter
  taxRegistrationScheme: StringFilter
  taxRegimeId: IDFilter
  contactId: IDFilter
  imageId: IDFilter
  metadata: JSONFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OrganizationContactFilter!]
  or: [OrganizationContactFilter!]
  not: OrganizationContactFilter
}

"""
The party's person-or-place detail row, as the two delivered writes carry it.

It is not a type of its own here: the detail row's type lives on the contact domain, and this input
states only the members the create and the edit copy into the row they hand the contact service. A
party is stored with its detail in one call rather than two, which is why the nested member exists at
all; stating the detail's identifier updates that same row instead of writing a second one.
"""
input ContactDetailInput {
  id: ID
  name: String
  firstName: String
  lastName: String
  country: String
  city: String
  address: String
  address2: String
  postcode: String
  latitude: Float
  longitude: Float
  regionCode: String
  fax: String
  fiscalInformation: String
  website: String
}

"""
What a caller states when it records a party.

Every member is one the delivered body declares, and that is the whole of the list: the input states
the descriptive facts the route carries and nothing else. The commercial terms that live on the same
row — `creditLimit`, `status`, `partyKind`, the registration pair, the two default addresses — are
readable and filterable on the type above but are written by no declared body of this resource, so a
member for one would be a promise this surface makes and the REST contract does not.

`name` is required because the delivered body is validated from the entity's own members and the
column is not nullable: a party that names nothing names no counterparty. `contactType` is optional
here and defaulted by the column, and a stated one must be a member of the closed set above.

The three identifier lists are the relations the delivered write maps onto the row it stores: the
detail row, the employees the party is worked by and the projects it is attached to. A related row is
named by its identifier and never by the row beside it, because that is what the delivered write
persists — and a list the caller does not state is left out of the payload rather than sent empty,
because the delivered handler reads "no members stated" as "derive them from the projects".
"""
input CreateOrganizationContactInput {
  organizationId: ID!
  name: String!
  primaryEmail: String
  primaryPhone: String
  inviteStatus: String
  contactType: ContactType
  notes: String
  budget: Decimal
  budgetType: String
  imageId: ID

  """
  The detail row to store with the party. Stating its identifier updates the row it names.
  """
  contact: ContactDetailInput

  """The employees the party is worked by, by identifier."""
  memberIds: [ID!]

  """
  The projects the party is attached to, by identifier. The delivered write derives the member set
  from them when no member is stated.
  """
  projectIds: [ID!]

  """The tags to attach, by identifier."""
  tagIds: [ID!]
}

"""
What a caller states when it changes a party that exists.

The members are the create input's, with the criterion stated. **A member the caller leaves out is
left as it is**: the delivered edit reads the row, writes the members stated onto it and reads it
back, so an absent member is not an instruction to clear anything — unlike a relation list, which is
replaced by the set the caller states.
"""
input UpdateOrganizationContactInput {
  id: ID!
  organizationId: ID!
  name: String!
  primaryEmail: String
  primaryPhone: String
  inviteStatus: String
  contactType: ContactType
  notes: String
  budget: Decimal
  budgetType: String
  imageId: ID
  contact: ContactDetailInput
  memberIds: [ID!]
  projectIds: [ID!]
  tagIds: [ID!]
}

"""
What a caller states when it moves a set of parties into or out of one employee's book.

One payload for both directions because the fact is one: the employee either gains the named parties
or loses them, and the delivered write applies both lists in the one call. The employee is named by
its identifier — the delivered command takes the row the identifier names — and a list the caller
does not state is left out rather than sent empty.
"""
input UpdateOrganizationContactsByEmployeeInput {
  organizationId: ID!

  """The employee whose book of parties changes."""
  memberId: ID!

  """The parties the employee gains, by identifier."""
  addedEntityIds: [ID!]

  """The parties the employee loses, by identifier."""
  removedEntityIds: [ID!]
}

"""One unit an organization files its people and its recruiting under."""
type OrganizationDepartment {
  id: ID!

  """
  The department's name, as the organization writes it. The column is not nullable.
  """
  name: String!

  """The organization the department is filed under, by identifier."""
  organizationId: ID

  """The tenant the department belongs to, by identifier."""
  tenantId: ID

  """
  When the department was withdrawn, or null while it is live. Withdrawing and restoring are
  delivered routes whose whole effect is this column, so the answer to either write says which
  state the row is in.
  """
  deletedAt: DateTime

  """When the department was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the department was filed."""
  createdAt: DateTime

  """When the department was last changed."""
  updatedAt: DateTime

  """
  Whether the department is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the department is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the department, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the department, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the department, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of departments."""
type OrganizationDepartmentEdge {
  node: OrganizationDepartment!
  cursor: String!
}

"""
One page of departments, with the total the filters select and the boundary cursors.
"""
type OrganizationDepartmentConnection {
  nodes: [OrganizationDepartment!]!
  edges: [OrganizationDepartmentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a department list may be sorted by."""
enum OrganizationDepartmentSortField {
  createdAt
  updatedAt
  name
}

"""
The order the departments are returned in. At most three keys are accepted.
"""
input OrganizationDepartmentSort {
  field: OrganizationDepartmentSortField!
  direction: SortDirection!
}

"""
The fields a department list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned: a member the read does not select would be a filter that silently
selects nothing. `name` is here for the obvious reason — a name is how a department is chosen from a
list of them — and `organizationId` because a department is filed under an organization and a list
of them is read one organization at a time.

The three pivots are in neither, and that is a statement rather than an omission. `members`, `tags`
and `candidates` are joined only when a REST caller names the relation in its query string, and this
surface names none, so a filter on one of them would be evaluated against a row that carries none of
it and would select nothing at all — the worst answer a filter can give. Who works a department is
the employee read's question, and that read is a root field of its own precisely because the pivot it
joins is not a relation the list read loads.
"""
input OrganizationDepartmentFilter {
  id: IDFilter
  name: StringFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationDepartmentFilter!]
  or: [OrganizationDepartmentFilter!]
  not: OrganizationDepartmentFilter
}

"""
What a caller states when it files a department.

`name` is required because the column is not nullable: a department that names nothing names no unit
of the organization. `organizationId` is stated by the caller and is not stamped from the credential
— the tenant is, and is therefore never a member here — and it is stated without a `!` because the
relation column is nullable in the store and a caller that files a department without naming an
organization is answered rather than refused, exactly as its REST counterpart is.

The two identifier lists are the relations the delivered write persists: the employees the
department is worked by and the tags attached to it. A related row is named by its identifier and
never by the row beside it, because the write stores the pivot the identifier names — and a list the
caller does not state is left out of the payload rather than sent empty, because an absent list is
not an instruction and an empty one is the instruction to clear the pivot.
"""
input CreateOrganizationDepartmentInput {
  name: String!
  organizationId: ID

  """The employees the department is worked by, by identifier."""
  memberIds: [ID!]

  """The tags attached to the department, by identifier."""
  tagIds: [ID!]
}

"""
What a caller states when it edits a department that exists.

The identifier is stated because the delivered route carries it in the path. **The delivered edit
writes through the create path** — it hands the service the stated body with the path identifier
merged in, so the row is written where the members are stated and left alone where they are not. A
member the caller leaves out is therefore left as it is, and a relation list it states replaces the
pivot.
"""
input UpdateOrganizationDepartmentInput {
  id: ID!
  name: String
  organizationId: ID
  memberIds: [ID!]
  tagIds: [ID!]
}

"""
What a caller states when it moves a set of departments into or out of one employee's book.

One payload for both directions because the fact is one: the employee either gains the named
departments or loses them, and the delivered write applies both lists together in the one call. The
employee is named by its identifier — the delivered command takes the row the identifier names — and
a list the caller does not state is left out rather than sent empty, because the handler reads
"nothing stated" and "nothing to change" as the same instruction.
"""
input UpdateOrganizationDepartmentByEmployeeInput {
  """
  The organization the departments belong to. The delivered write narrows the rows it touches by it.
  """
  organizationId: ID!

  """The employee whose book of departments changes."""
  memberId: ID!

  """The departments the employee gains, by identifier."""
  addedEntityIds: [ID!]

  """The departments the employee loses, by identifier."""
  removedEntityIds: [ID!]
}

"""One piece of paperwork an organization keeps about itself."""
type OrganizationDocument {
  id: ID!

  """The document's name, as the organization wrote it."""
  name: String!

  """
  A denormalised link to the file, kept for lists that must not join the asset table.
  """
  documentUrl: String

  """The asset the file lives in, by identifier."""
  documentId: ID

  """The asset the file lives in."""
  document: ImageAsset

  """The organization the document is filed under, by identifier."""
  organizationId: ID

  """The tenant the document belongs to, by identifier."""
  tenantId: ID

  """
  When the document was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the document was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the document was filed."""
  createdAt: DateTime

  """When the document was last changed."""
  updatedAt: DateTime

  """
  Whether the document is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the document is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the document, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the document, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the document, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of documents."""
type OrganizationDocumentEdge {
  node: OrganizationDocument!
  cursor: String!
}

"""
One page of documents, with the total the filters select and the boundary cursors.
"""
type OrganizationDocumentConnection {
  nodes: [OrganizationDocument!]!
  edges: [OrganizationDocumentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a document list may be sorted by."""
enum OrganizationDocumentSortField {
  createdAt
  updatedAt
  name
}

"""
The order the documents are returned in. At most three keys are accepted.
"""
input OrganizationDocumentSort {
  field: OrganizationDocumentSortField!
  direction: SortDirection!
}

"""
The fields a document list may be narrowed by.

`documentUrl` is filterable because a caller looks a document up by where it is served from;
`documentId` is filterable because the same question is asked by asset identifier. The asset's own
members are not filterable here — the asset is a row of another domain, and narrowing a document list
by the bytes behind it is a question that surface answers.
"""
input OrganizationDocumentFilter {
  id: IDFilter
  name: StringFilter
  documentUrl: StringFilter
  documentId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationDocumentFilter!]
  or: [OrganizationDocumentFilter!]
  not: OrganizationDocumentFilter
}

"""
What a caller states when it files a document.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.
"""
input CreateOrganizationDocumentInput {
  organizationId: ID!
  name: String!

  """The link to the file, when the caller already knows it."""
  documentUrl: String

  """The asset the file lives in, by identifier."""
  documentId: ID
}

"""
What a caller states when it edits a document that exists.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path. The delivered edit is a partial column update rather than a replacement
of the row, so a member the caller leaves out is left as it is.
"""
input UpdateOrganizationDocumentInput {
  id: ID!
  organizationId: ID
  name: String
  documentUrl: String
  documentId: ID
}

"""One classification an organization files its people under."""
type OrganizationEmploymentType {
  id: ID!

  """The type's name, as the organization writes it."""
  name: String!

  """The organization the type is filed under, by identifier."""
  organizationId: ID

  """The tenant the type belongs to, by identifier."""
  tenantId: ID

  """
  When the type was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the type was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the type was filed."""
  createdAt: DateTime

  """When the type was last changed."""
  updatedAt: DateTime

  """
  Whether the type is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the type is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the type, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the type, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the type, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of employment types."""
type OrganizationEmploymentTypeEdge {
  node: OrganizationEmploymentType!
  cursor: String!
}

"""
One page of employment types, with the total the filters select and the boundary cursors.
"""
type OrganizationEmploymentTypeConnection {
  nodes: [OrganizationEmploymentType!]!
  edges: [OrganizationEmploymentTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an employment-type list may be sorted by."""
enum OrganizationEmploymentTypeSortField {
  createdAt
  updatedAt
  name
}

"""
The order the employment types are returned in. At most three keys are accepted.
"""
input OrganizationEmploymentTypeSort {
  field: OrganizationEmploymentTypeSortField!
  direction: SortDirection!
}

"""
The fields an employment-type list may be narrowed by.

The collections are not among them, and that is the point rather than an omission: the delivered list
read does not join the tag pivot, the member pivot or the candidate pivot unless a REST caller names
it, and this surface names none — so a filter on one of them would be evaluated against a row that
carries none and would select nothing at all, which is the worst answer a filter can give.
"""
input OrganizationEmploymentTypeFilter {
  id: IDFilter
  name: StringFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationEmploymentTypeFilter!]
  or: [OrganizationEmploymentTypeFilter!]
  not: OrganizationEmploymentTypeFilter
}

"""
What a caller states when it files an employment type.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.
"""
input CreateOrganizationEmploymentTypeInput {
  organizationId: ID!
  name: String!
}

"""
What a caller states when it edits an employment type that exists.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path. **The delivered edit writes through the create path** — it hands the
service the stated body with the path identifier merged in — so it is an upsert rather than a
replacement: a member the caller leaves out is left as it is, and the answer is the row rather than a
statement about the write.
"""
input UpdateOrganizationEmploymentTypeInput {
  id: ID!
  organizationId: ID
  name: String
}

"""
One language an organization works in, as the organization describes it.
"""
type OrganizationLanguage {
  id: ID!

  """
  The language's code: the identifier every other surface names the language by.
  """
  languageCode: String!

  """The name the organization calls the language."""
  name: String!

  """
  The organization's own description of how well it works in the language.
  """
  level: String!

  """The organization the row is filed under, by identifier."""
  organizationId: ID

  """The tenant the row belongs to, by identifier."""
  tenantId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the row was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the row was filed."""
  createdAt: DateTime

  """When the row was last changed."""
  updatedAt: DateTime

  """
  Whether the row is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the row is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the row, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the row, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the row, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of organization languages."""
type OrganizationLanguageEdge {
  node: OrganizationLanguage!
  cursor: String!
}

"""
One page of organization languages, with the total the filters select and the boundary cursors.
"""
type OrganizationLanguageConnection {
  nodes: [OrganizationLanguage!]!
  edges: [OrganizationLanguageEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an organization-language list may be sorted by."""
enum OrganizationLanguageSortField {
  createdAt
  updatedAt
  name
  languageCode
  level
}

"""
The order the organization languages are returned in. At most three keys are accepted.
"""
input OrganizationLanguageSort {
  field: OrganizationLanguageSortField!
  direction: SortDirection!
}

"""
The fields an organization-language list may be narrowed by.

Everything the row carries is here, because everything the row carries is a column of it: the register
is a short list read by code or by name, and there is no relation behind any of these three members.
"""
input OrganizationLanguageFilter {
  id: IDFilter
  languageCode: StringFilter
  name: StringFilter
  level: StringFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationLanguageFilter!]
  or: [OrganizationLanguageFilter!]
  not: OrganizationLanguageFilter
}

"""
What a caller states when it files a language.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.

The relation is stated as `languageCode` rather than as a nested `language` object, which is the shape
the delivered column has and the shape a caller already holds: the code is the identity of the language,
and the reference row behind it is the platform's.
"""
input CreateOrganizationLanguageInput {
  organizationId: ID!
  languageCode: String!
  name: String!
  level: String!
}

"""
What a caller states when it edits a row that exists.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path. The delivered edit is a partial column update rather than a replacement
of the row, so a member the caller leaves out is left as it is.
"""
input UpdateOrganizationLanguageInput {
  id: ID!
  organizationId: ID
  languageCode: String
  name: String
  level: String
}

"""One job title an organization files its people under."""
type OrganizationPosition {
  id: ID!

  """The position's title, as the organization writes it."""
  name: String!

  """The organization the position is filed under, by identifier."""
  organizationId: ID

  """The tenant the position belongs to, by identifier."""
  tenantId: ID

  """
  When the position was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the position was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the position was filed."""
  createdAt: DateTime

  """When the position was last changed."""
  updatedAt: DateTime

  """
  Whether the position is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the position is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the position, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the position, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the position, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of positions."""
type OrganizationPositionEdge {
  node: OrganizationPosition!
  cursor: String!
}

"""
One page of positions, with the total the filters select and the boundary cursors.
"""
type OrganizationPositionConnection {
  nodes: [OrganizationPosition!]!
  edges: [OrganizationPositionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a position list may be sorted by."""
enum OrganizationPositionSortField {
  createdAt
  updatedAt
  name
}

"""
The order the positions are returned in. At most three keys are accepted.
"""
input OrganizationPositionSort {
  field: OrganizationPositionSortField!
  direction: SortDirection!
}

"""
The fields a position list may be narrowed by.

`name` is here for the obvious reason: a title is how a position is chosen from a list of them. The tag
pivot is not, and that is a statement rather than an omission — the delivered list read joins it only
when a REST caller names it, and this surface names none, so a filter on it would be evaluated against
a row that carries none and would select nothing at all.
"""
input OrganizationPositionFilter {
  id: IDFilter
  name: StringFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationPositionFilter!]
  or: [OrganizationPositionFilter!]
  not: OrganizationPositionFilter
}

"""
What a caller states when it files a position.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.
"""
input CreateOrganizationPositionInput {
  organizationId: ID!
  name: String!
}

"""
What a caller states when it edits a position that exists.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path. **The delivered edit writes through the create path** — it hands the
service the stated body with the path identifier merged in — so it is an upsert rather than a
replacement: a member the caller leaves out is left as it is, and the answer is the row rather than a
statement about the write.
"""
input UpdateOrganizationPositionInput {
  id: ID!
  organizationId: ID
  name: String
}

"""
One part of a project: a named, dated unit of work that employees are assigned to and teams share.
"""
type OrganizationProjectModule {
  id: ID!

  """
  The module's name, as its author wrote it. A module that names nothing is not a module.
  """
  name: String!

  """
  The module's description, as the delivered write stores it: the brief's rich text, sanitised
  through the shared server-side allowlist before it is persisted.
  """
  description: String

  """
  The lifecycle of the module, as the platform's own value: `backlog`, `planned`, `in-progress`,
  `paused`, `completed` or `cancelled`. Carried as its value rather than declared here as an enum,
  because the value set belongs to the contracts package and the delivered write validates against it.
  """
  status: String

  """
  The day the module starts. Null on a module that has not been scheduled.
  """
  startDate: DateTime

  """The day the module is due to end. Null on a module with no end stated."""
  endDate: DateTime

  """Whether the module is visible beyond the people it is assigned to."""
  public: Boolean

  """
  The favourite marker of the row: whether the caller's own module list keeps it to hand.
  """
  isFavorite: Boolean

  """
  The module this one is filed under — the self-reference that turns a flat list into a tree. The
  parent row is not joined by the delivered reads, which join a relation only when a REST caller names
  it; the identifier travels, and the parent itself is read as `organizationProjectModule`.
  """
  parentId: ID

  """
  The project the module belongs to. The row is joined only when a REST caller names it in `relations`,
  which this surface never does, so the identifier is what is carried and the project itself is read
  from the project domain, whose own type it is.
  """
  projectId: ID

  """The organization the module is filed under, by identifier."""
  organizationId: ID

  """The tenant the module belongs to, by identifier."""
  tenantId: ID

  """
  When the module was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it the answer to the write that withdrew a row would not say so.
  """
  deletedAt: DateTime

  """When the module was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the module was filed."""
  createdAt: DateTime

  """When the module was last changed."""
  updatedAt: DateTime

  """
  Whether the module is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the module is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the module, by identifier. The user row is not joined by this surface's reads.
  """
  createdByUserId: ID

  """
  The person who last changed the module, by identifier. The user row is not joined here.
  """
  updatedByUserId: ID

  """
  The person who withdrew the module, by identifier. The user row is not joined here.
  """
  deletedByUserId: ID
}

"""The boundary of one page of modules."""
type OrganizationProjectModuleEdge {
  node: OrganizationProjectModule!
  cursor: String!
}

"""
One page of modules, with the total the filters select and the boundary cursors.
"""
type OrganizationProjectModuleConnection {
  nodes: [OrganizationProjectModule!]!
  edges: [OrganizationProjectModuleEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a module list may be sorted by.

The two timestamps, the name and the two dates of the module's own plan. `status` is filterable and not
sortable: the vocabulary is the platform's, and its alphabetical order is not a lifecycle — a board
orders modules by the dates it planned them for, and by the day they were written when it has no plan.
"""
enum OrganizationProjectModuleSortField {
  createdAt
  updatedAt
  name
  startDate
  endDate
}

"""
The order the modules are returned in. At most three keys are accepted.
"""
input OrganizationProjectModuleSort {
  field: OrganizationProjectModuleSortField!
  direction: SortDirection!
}

"""
The fields a module list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered reads answer, because the connection narrows the rows
the service returned: a member a read does not select would be a filter that silently selects nothing.

`members`, `teams` and `tasks` are absent for that reason and one more: the list read hydrates none of
them onto the rows it answers, so a filter on one would be evaluated against a row that carries none of
them and would select nothing at all — the worst answer a filter can give. Who works a module, which
teams share it and which tasks are filed under it are the questions the reads of their own domains
answer.

`tenantId` is absent too: the delivered read is scoped to the caller's own tenant by the service, from
the credential rather than from the request, so every row the connection narrows already carries the
caller's tenant and a member for it could only select all of them or none.
"""
input OrganizationProjectModuleFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  status: StringFilter
  startDate: DateTimeFilter
  endDate: DateTimeFilter
  public: BooleanFilter
  isFavorite: BooleanFilter

  """
  The parent module, by identifier. `isNull: true` selects the roots of the tree.
  """
  parentId: IDFilter
  projectId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OrganizationProjectModuleFilter!]
  or: [OrganizationProjectModuleFilter!]
  not: OrganizationProjectModuleFilter
}

"""
What a caller states when it files a module.

The organization is stated because the delivered create does not derive one: the tenant is stamped from
the credential and is never a member, but the organization is a member the caller names and the delivered
route validates against the caller's own memberships. It is carried as its identifier rather than as the
organization row, which is the shape this surface has for every relation.

`name` is required because the column is not nullable: a module that names nothing names no unit of work.
`description` is the brief's rich text and is sanitised through the shared server-side allowlist by the
delivered write, so what a caller reads back is the sanitised text rather than the text it sent.

`memberIds` and `managerIds` are the two lists the delivered write turns into the module-employee pivot:
the service reads each identifier as an active employee of the module's own organization and tenant,
writes one pivot row per employee and marks the managers among them. The pivot row itself is never
stated — its own columns are the write's to derive rather than a caller's to assert — and `members`, the
relation column the delivered body also validates, is not a member here at all: the write replaces it
with the pivot rows it builds from these two lists, so a caller that stated it would have its list
silently discarded.

`tasks` are the tasks to file under the module, by identifier. The pairing is not stored on this row: the
delivered write reads each identifier as an existing task and adds the module to that task's own module
list, which is where the pairing lives. The identifiers are stated rather than the task rows because an
identifier is what the write reads off each entry; a task's own list is written from the task's side.

The two lifecycle markers are not members. `archivedAt` and `deletedAt` are what a lifecycle route writes
about a row that exists rather than facts a caller states when it files one, and a create that carried
them would file a module that is already withdrawn. `isActive` and `isArchived` are members, because they
are flags an operator sets on the row rather than markers of a lifecycle the routes perform. There is no
member for the tenant, for the same reason there is none on any other write on this platform.
"""
input CreateOrganizationProjectModuleInput {
  organizationId: ID!
  name: String!
  description: String
  status: String
  startDate: DateTime
  endDate: DateTime
  public: Boolean
  isFavorite: Boolean
  isActive: Boolean
  isArchived: Boolean

  """The parent module to file this one under. Omit for a root of the tree."""
  parentId: ID

  """The project the module belongs to, by identifier."""
  projectId: ID

  """The employees the module is assigned to, by identifier."""
  memberIds: [ID!]

  """
  The employees who manage the module, by identifier. They are written into the same pivot, marked.
  """
  managerIds: [ID!]

  """The tasks to file under the module, by identifier."""
  tasks: [ID!]
}

"""
What a caller states when it edits a module that exists.

The members are the create input's with the criterion stated, and every one of them but `id` is optional,
which is the shape the delivered body has: it is validated from a partial type, the write persists the
members it carries and a member the caller leaves out is left as it is.

Two of the lists are the exception that proves the reading of "left out" and "stated empty". `memberIds`,
`managerIds` and `tasks` are read as a set whenever they are stated: an empty `tasks` list is the
instruction to take every task out of the module, and leaving it out is the instruction to leave the task
set as it is.
"""
input UpdateOrganizationProjectModuleInput {
  id: ID!
  organizationId: ID
  name: String
  description: String
  status: String
  startDate: DateTime
  endDate: DateTime
  public: Boolean
  isFavorite: Boolean
  isActive: Boolean
  isArchived: Boolean
  parentId: ID
  projectId: ID
  memberIds: [ID!]
  managerIds: [ID!]
  tasks: [ID!]
}

"""
One project: the unit of work a task, a time log, an expense, an invoice line, a sprint and a payment
are filed under, together with the settings that govern how its tasks behave.
"""
type OrganizationProject {
  id: ID!

  """
  The tenant the project belongs to, by identifier. The row is read from the tenant surface.
  """
  tenantId: ID

  """
  The organization the project is filed under, by identifier. The row is read from the organization surface.
  """
  organizationId: ID

  """
  The project's name. A project that names nothing names no unit of work, so the column is not nullable.
  """
  name: String!

  """When the project is planned to start, when a schedule was stated."""
  startDate: DateTime

  """When the project is planned to end, when a schedule was stated."""
  endDate: DateTime

  """
  Which basis the project is billed on. The vocabulary is the platform's `ProjectBillingEnum`.
  """
  billing: String

  """The ISO 4217 code of the currency `budget` is stated in."""
  currency: String

  """
  Whether the project is visible to everyone in the organization rather than to the people working it.
  """
  public: Boolean

  """
  Whose project it is taken to be. The vocabulary is the platform's `ProjectOwnerEnum`.
  """
  owner: String

  """
  How the project's tasks are listed. The vocabulary is the platform's `TaskListTypeEnum`.
  """
  taskListType: String

  """A short code the project is known by inside the organization."""
  code: String

  """
  The long-form description. The delivered writes sanitise this member through the platform's
  server-side allowlist before it is stored, so the answer here is the sanitised text rather than
  what the caller sent.
  """
  description: String

  """The colour the project is shown with in lists and calendars."""
  color: String

  """Whether the time recorded against the project is billable."""
  billable: Boolean

  """
  Whether the project is billed as a flat amount rather than by the time recorded against it.
  """
  billingFlat: Boolean

  """
  Whether the project is open source, and therefore tracked as such in the organization's reports.
  """
  openSource: Boolean

  """The project's own web address, when it publishes one."""
  projectUrl: String

  """The project's repository or home page, when it is open source."""
  openSourceProjectUrl: String

  """
  The amount the project was budgeted for. Its unit is `budgetType`: money when that is `cost`, hours
  when it is `hours`. An amount, so it is an exact decimal and never a float.
  """
  budget: Decimal

  """
  Whether `budget` is an amount of money (`cost`) or a number of hours (`hours`).
  """
  budgetType: String

  """
  A denormalised avatar URL, kept for the lists that must not join the asset table.
  """
  imageUrl: String

  """
  The avatar asset, by identifier. The identifier always travels; the asset row is joined eagerly beside it.
  """
  imageId: ID

  """
  The avatar asset itself. The relation is declared eager on the entity, so it travels on every read of the row.
  """
  image: ImageAsset

  """
  The icon the project is shown with, when a client asks for one rather than for the avatar.
  """
  icon: String

  """
  The project's lifecycle: `open`, `in-progress`, `completed`, `cancelled` or `custom`, as
  `ProjectStatusEnum` names them. Carried as its value: the time-tracking and invoicing paths read
  the same column, so the vocabulary is not this domain's to close.
  """
  status: String

  """
  Whether the project's tasks are kept in step with the linked repository automatically.
  """
  isTasksAutoSync: Boolean

  """Whether a task label triggers the synchronisation of that task."""
  isTasksAutoSyncOnLabel: Boolean

  """
  The label a task must carry for the synchronisation on label to pick it up.
  """
  syncTag: String

  """
  How many days after their last change the project's tasks are archived automatically. A duration in
  days, and the column is an exact decimal, so it is carried as a `Float` — a fractional interval a
  write accepts is one this surface has to be able to answer.
  """
  archiveTasksIn: Float

  """
  How many days after their last change the project's tasks are closed automatically, in days.
  """
  closeTasksIn: Float

  """
  How many people work the project, as the row records it. A figure the row carries rather than one
  this surface counts, which is why it is an `Int` and not a connection's `totalCount`.
  """
  membersCount: Int

  """
  The party the project is delivered for, by identifier. The row carries the column; the party itself
  is read as `organizationContact(id)`, because the delivered list read does not join it.
  """
  organizationContactId: ID

  """
  The employee a task of this project is assigned to when its own assignee is not stated, by identifier.
  """
  defaultAssigneeId: ID

  """
  The tenant-defined extras the platform registers against this entity, as the row stores them.
  """
  customFields: JSON

  """
  Whether the row is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the row is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are two of the
  delivered writes this domain serves, and this column is the whole of what they change: without it
  the answer to the write that withdrew a row could not say that it did.
  """
  deletedAt: DateTime

  """When the project was filed."""
  createdAt: DateTime

  """When the project was last changed."""
  updatedAt: DateTime

  """
  The person who filed the project, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the project, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the project, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of projects."""
type OrganizationProjectEdge {
  node: OrganizationProject!
  cursor: String!
}

"""
One page of projects, with the total the filters select and the boundary cursors.

**On the synced page the total counts the rows that read answered, not every linked project in the
store.** That read fixes a page of its own — ten rows unless the caller states otherwise — so the
connection's `totalCount` is the size of the set it returned after the filters select from it, which
is the same statement the connection's page makes over the same rows.
"""
type OrganizationProjectConnection {
  nodes: [OrganizationProject!]!
  edges: [OrganizationProjectEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a project list may be sorted by."""
enum OrganizationProjectSortField {
  createdAt
  updatedAt
  name
  startDate
  endDate
  budget
  status
  taskListType
}

"""
The order the projects are returned in. At most three keys are accepted.
"""
input OrganizationProjectSort {
  field: OrganizationProjectSortField!
  direction: SortDirection!
}

"""
The fields a project list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned: a member the read does not select would be a filter that silently
selects nothing, which is the worst answer a filter can give. The relations the row points at are
absent for the same reason — `members`, `teams`, `tags` and `organizationContact` are pivots and rows
this read never joins, so a filter on one of them would be evaluated against a row that carries none
and would select nothing at all. The tenant is not a member either: the read applies it to the
criterion from the credential, so a filter on it would be a second statement of the same scope.

`description` is absent although it is a column: it is the long-form rich text the delivered writes
sanitise, and a list is not narrowed by a fragment of markup. It is carried on the type, and a caller
that needs it reads the row.
"""
input OrganizationProjectFilter {
  id: IDFilter
  name: StringFilter
  startDate: DateTimeFilter
  endDate: DateTimeFilter
  billing: StringFilter
  currency: StringFilter
  public: BooleanFilter
  owner: StringFilter
  taskListType: StringFilter
  code: StringFilter
  color: StringFilter
  billable: BooleanFilter
  billingFlat: BooleanFilter
  openSource: BooleanFilter
  projectUrl: StringFilter
  openSourceProjectUrl: StringFilter
  budget: DecimalFilter
  budgetType: StringFilter
  imageUrl: StringFilter
  icon: StringFilter
  status: StringFilter
  isTasksAutoSync: BooleanFilter
  isTasksAutoSyncOnLabel: BooleanFilter
  syncTag: StringFilter
  archiveTasksIn: NumberFilter
  closeTasksIn: NumberFilter
  membersCount: NumberFilter
  organizationId: IDFilter
  organizationContactId: IDFilter
  imageId: IDFilter
  defaultAssigneeId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationProjectFilter!]
  or: [OrganizationProjectFilter!]
  not: OrganizationProjectFilter
}

"""
What a caller states when it files a project.

Every member is one the delivered create body validates, and that is the whole of the list: the input
states the descriptive facts the route carries and nothing else. Three groups are deliberately
absent:

- the tenant is never stated, because a write's tenant comes from the credential;
- `tagIds` is not declared. The delivered body does not validate it — the members it validates for
  relations are the two employee lists below — so a tag list here would be a member this surface
  accepts and the REST contract does not. Tags are attached from the tag surface, which owns the
  pivot;
- the commercial terms that live on the same row — `budget`, `currency`, `status`, `owner`, the two
  dates and the settings beside them — are readable and filterable on the type above but are written
  by no declared body of this resource, so a member for one would be a promise the REST contract does
  not make.

`name` is required because the delivered body is validated from the entity's own members and the
column is not nullable. `organizationId` is required because the member that stands for the caller's
organization is validated against the caller's own memberships, and the row cannot be filed into a
scope the caller does not state.

The two employee lists are the relations the delivered write maps onto the row it stores: an employee
named as a manager is written to the project's member pivot with the manager role, and one named as a
member is written with none. A related row is named by its identifier and never by the row beside it,
because an identifier is what the write persists.
"""
input CreateOrganizationProjectInput {
  """The organization the project is filed under, by identifier."""
  organizationId: ID!

  """
  The project's name. A project that names nothing names no unit of work.
  """
  name: String!

  """The avatar asset to file the project with, by identifier."""
  imageId: ID

  """
  Which basis the project is billed on. The vocabulary is the platform's `ProjectBillingEnum`.
  """
  billing: String

  """
  Whether the budget to be stated later is money (`cost`) or hours (`hours`).
  """
  budgetType: String

  """
  How the project's tasks are listed. The vocabulary is the platform's `TaskListTypeEnum`.
  """
  taskListType: String

  """The employees the project is worked by, by identifier."""
  memberIds: [ID!]

  """
  The employees who manage the project, by identifier. A manager is also a member of the project.
  """
  managerIds: [ID!]
}

"""
What a caller states when it changes a project that exists.

The members are the create input's, with the criterion stated, and every one of them is optional
because the delivered edit body is the create body with each member relaxed: a member the caller
leaves out is left as it is rather than cleared. `organizationId` stays required, because the read
that finds the row is scoped by it and the write would otherwise have no scope to find the row in.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path: `:id` names the row and the body carries the facts.

The two employee lists are read together by the delivered write: a body that states one of them and
not the other is refused there, because the write walks the pair to rebuild the project's member
pivot. This surface states the pair as the route reads it and does not invent an empty list for the
member that was left out — papering over that here would be a second write path with a different
meaning from the one REST performs.
"""
input UpdateOrganizationProjectInput {
  id: ID!

  """The organization the project is filed under, by identifier."""
  organizationId: ID!
  name: String
  imageId: ID
  billing: String
  budgetType: String
  taskListType: String
  memberIds: [ID!]
  managerIds: [ID!]
}

"""
What a caller states when it changes which view a project's tasks open in.

The body the delivered route reads is the task-mode DTO, and these are its members: the project's
organization, which scopes the row the write finds, and the list type itself. The list type is
required there because the column's own validation states it, so it is required here — a write that
stated no view would be refused over REST and must not be accepted here.
"""
input OrganizationProjectTaskViewModeInput {
  """The organization the project is filed under, by identifier."""
  organizationId: ID!

  """
  How the project's tasks are listed. The vocabulary is the platform's `TaskListTypeEnum`.
  """
  taskListType: String!
}

"""
What a caller states when it changes how a project's tasks synchronise.

The body the delivered route reads is the project-setting DTO, and these are its members: the
organization that scopes the write, the two synchronisation flags, the label the second of them
matches on, and the tenant-defined extras. Every member is optional except the organization, because
the delivered body leaves each of them relaxed and the scope is what the read that finds the row
needs.

A member the caller leaves out is left as it is: the delivered write reads the row, writes the
members stated onto it and answers with the row read back, so an absent flag is not an instruction to
turn anything off.
"""
input OrganizationProjectSettingInput {
  """The organization the project is filed under, by identifier."""
  organizationId: ID!

  """
  Whether the project's tasks are kept in step with the linked repository automatically.
  """
  isTasksAutoSync: Boolean

  """Whether a task label triggers the synchronisation of that task."""
  isTasksAutoSyncOnLabel: Boolean

  """
  The label a task must carry for the synchronisation on label to pick it up.
  """
  syncTag: String

  """
  The tenant-defined extras to write onto the row, as the registered custom fields name them.
  """
  customFields: JSON
}

"""
What a caller states when it moves a set of projects into or out of one employee's list.

One payload for both directions because the fact is one: the employee either gains the named projects
or loses them, and the delivered write applies both lists in the one call. The employee is named by
its identifier — the write keys the member pivot by it — and the organization is stated because the
write is scoped by it and the service refuses a call that states none.

A list the caller does not state is left out rather than sent empty: the delivered write reads
"nothing to add" and "an empty list to add" as the same instruction, and leaving it out says so once
rather than twice.
"""
input UpdateOrganizationProjectByEmployeeInput {
  """The organization the projects are filed under, by identifier."""
  organizationId: ID!

  """The employee whose list of projects changes."""
  memberId: ID!

  """The projects the employee gains, by identifier."""
  addedProjectIds: [ID!]

  """The projects the employee loses, by identifier."""
  removedProjectIds: [ID!]
}

"""
One standing cost the organization carries from one month until an end that may be open.
"""
type OrganizationRecurringExpense {
  id: ID!

  """The day of the month the arrangement begins, from 1 to 31."""
  startDay: Int!

  """The month the arrangement begins, from 1 to 12."""
  startMonth: Int!

  """The four-digit year the arrangement begins in."""
  startYear: Int!

  """
  The instant the arrangement is dated to, which is the beginning above as a date.
  """
  startDate: DateTime!

  """The day of the month the arrangement ends; null while the end is open."""
  endDay: Int

  """The month the arrangement ends; null while the end is open."""
  endMonth: Int

  """The year the arrangement ends; null while the end is open."""
  endYear: Int

  """
  The instant the arrangement ends; null while the end is open, which is what makes the cost standing.
  """
  endDate: DateTime

  """
  The category the standing cost is filed under, in the tenant's own vocabulary.
  """
  categoryName: String!

  """The standing cost, in the row's own `currency`."""
  value: Decimal!

  """The ISO 4217 code `value` is expressed in."""
  currency: String!

  """
  Whether the platform divides this cost among the organization's employees. A shared arrangement is
  what the share read below answers a quotient of, and the flag is what tells a quotient from a stored
  amount.
  """
  splitExpense: Boolean

  """
  The arrangement this row belongs to. The delivered creation sets it to the row's own identifier, so a
  standing arrangement is a family of rows whose later members are the ones an edit opened when it moved
  the beginning forward — which is what the verdict read below looks a conflict up among.
  """
  parentRecurringExpenseId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered routes
  — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of standing costs."""
type OrganizationRecurringExpenseEdge {
  node: OrganizationRecurringExpense!
  cursor: String!
}

"""
One page of standing costs, with the total the filters select and the boundary cursors.
"""
type OrganizationRecurringExpenseConnection {
  nodes: [OrganizationRecurringExpense!]!
  edges: [OrganizationRecurringExpenseEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
What moving one arrangement's beginning would mean, and the arrangements in the way.

**This is a computed verdict rather than a row.** The delivered read compares the beginning the caller
proposes with the one the arrangement has, and when the change would move the beginning forward or
backward it reads the sibling arrangements of the same parent that fall inside the gap. So the answer has
two parts that belong together: what the change would be, and the rows that make it that.

`value` is the contracts' own verdict rather than a schema enum, because the vocabulary is shared with
the employee's own arrangements: `NO_CHANGE`, `WITHIN_MONTH`, `REDUCE_SAFE`, `REDUCE_CONFLICT`,
`INCREASE_SAFE_WITHIN_LIMIT`, `INCREASE_SAFE_OUTSIDE_LIMIT` or `INCREASE_CONFLICT`. The delivered edit
asks this same question of itself and refuses the last of the seven outright.
"""
type OrganizationRecurringExpenseStartDateUpdateType {
  """What moving the beginning would be, as the contracts' own verdict."""
  value: String!

  """
  The sibling arrangements of the same parent that fall inside the gap the change would leave.
  """
  conflicts: [OrganizationRecurringExpense!]!
}

"""
One shared arrangement as a single employee's share of it, for one month.

The read behind `organizationRecurringExpenseShares` selects the arrangements the organization shares
and that are in force in the month, counts the people it employs, and divides each arrangement's amount
by that count. **The quotient is not a stored figure**: the read replaces the arrangement's own amount
with it before it answers, which is why the row below carries the share under the arrangement and the
whole it came from beside it. Every member is present on every row this type is answered for — the read
selects only shared arrangements, so a row that was not divided is not one of its answers.
"""
type OrganizationRecurringExpenseShare {
  """
  The arrangement, as the share read answered it: its `value` is the employee's share of the standing
  cost rather than the stored amount, and `originalValue` below is the stored amount it was divided
  from.
  """
  recurringExpense: OrganizationRecurringExpense!

  """
  The standing cost before it was divided, which is the amount the arrangement stores.
  """
  originalValue: Decimal!

  """
  How many people the standing cost was divided among, which is the organization's head count.
  """
  employeeCount: Int!
}

"""
The fields a standing-cost list may be sorted by.

`startDate` is the ledger's own order, which is the column the delivered month route narrows on; the two
timestamps are how a change is found again; `value` is what a list of standing costs is taken by; and
`categoryName` is offered because a ledger is read in category groups.
"""
enum OrganizationRecurringExpenseSortField {
  createdAt
  updatedAt
  startDate
  endDate
  value
  categoryName
}

"""
The order the standing costs are returned in. At most three keys are accepted.
"""
input OrganizationRecurringExpenseSort {
  field: OrganizationRecurringExpenseSortField!
  direction: SortDirection!
}

"""
The fields a standing-cost list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the row, and the four that describe the beginning and the end are what makes
the delivered month route a filter of this list rather than a read of its own:

```
filter: {
  or: [
    { startDate: { between: [$start, $end] }, endDate: { isNull: true } },
    { startDate: { lte: $end }, endDate: { gte: $start } }
  ]
}
```

That is the delivered criterion exactly — an arrangement that begins inside the range and has no end
yet, or one that begins no later than the range's start and ends no earlier than its end — stated in the
connection's own vocabulary, with the two bounds being the instants the delivered handler computes from
the range its route was given (one month's bounds, as the delivered clients send them). The shared
arrangements alone are the same disjunction with `splitExpense: { eq: true }` beside it, and a family of
rows is `parentRecurringExpenseId: { eq: … }`.

The amount is stated through the decimal family, which is the family money is compared in. `deletedAt`
is deliberately absent although the object carries it: the delivered list read answers live rows only, so
a condition on that column could only ever match the empty set. The tenant is absent because it is
applied to the criterion from the credential rather than from the caller.
"""
input OrganizationRecurringExpenseFilter {
  id: IDFilter
  startDay: NumberFilter
  startMonth: NumberFilter
  startYear: NumberFilter
  startDate: DateTimeFilter
  endDay: NumberFilter
  endMonth: NumberFilter
  endYear: NumberFilter
  endDate: DateTimeFilter
  categoryName: StringFilter
  value: DecimalFilter
  currency: StringFilter
  splitExpense: BooleanFilter
  parentRecurringExpenseId: IDFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OrganizationRecurringExpenseFilter!]
  or: [OrganizationRecurringExpenseFilter!]
  not: OrganizationRecurringExpenseFilter
}

"""
What a caller states when it records a standing cost.

The delivered creation binds the row itself as its body — the controller declares no DTO and no
validation pipe — so these are the entity's own columns and nothing beside them. `value`, `currency`,
`categoryName` and the two beginnings are what a standing arrangement cannot be read without; the three
the delivered creation stores are the amount, the currency and the category, and the beginning written
as both the date and the day, month and year.

`splitExpense` is a member because the delivered creation stores it: whether the cost is shared is the
caller's decision when it records the arrangement, not a figure the platform derives.
"""
input CreateOrganizationRecurringExpenseInput {
  """The standing cost, in the currency below."""
  value: Decimal!

  """The ISO 4217 code the value is expressed in."""
  currency: String!

  """The category the standing cost is filed under."""
  categoryName: String!

  """The day of the month the arrangement begins, from 1 to 31."""
  startDay: Int!

  """The month the arrangement begins, from 1 to 12."""
  startMonth: Int!

  """The four-digit year the arrangement begins in."""
  startYear: Int!

  """The instant the arrangement is dated to."""
  startDate: DateTime!

  """
  The day of the month the arrangement ends, where the caller states one.
  """
  endDay: Int

  """The month the arrangement ends, where the caller states one."""
  endMonth: Int

  """The year the arrangement ends, where the caller states one."""
  endYear: Int

  """
  The instant the arrangement ends, where the caller states one. Omit it and the cost is standing.
  """
  endDate: DateTime

  """
  Whether the platform divides this cost among the organization's employees.
  """
  splitExpense: Boolean

  """
  The arrangement this row belongs to, where the caller states one. The delivered creation sets it to the row's own identifier.
  """
  parentRecurringExpenseId: ID

  """The organization the standing cost is filed under."""
  organizationId: ID
}

"""
What a caller states when it changes a standing cost.

**These are the members the delivered edit reads, and nothing else.** Its body also accepts a currency —
the contract's own edit input requires one — and the delivered handler never writes it: the plain edit
writes the beginning, the value and the category, and the branch that closes one arrangement and opens
its successor takes the currency and the parent from the row it replaces. An input member the write
ignores would be worse than no member at all, so it is not offered.

The verdict member the delivered handler computes is not a member of this input either: it asks the
`organizationRecurringExpenseStartDateUpdateType` field's question of itself and overwrites whatever a
body carried.
"""
input UpdateOrganizationRecurringExpenseInput {
  id: ID!

  """The day of the month the arrangement now begins, from 1 to 31."""
  startDay: Int!

  """The month the arrangement now begins, from 1 to 12."""
  startMonth: Int!

  """The four-digit year the arrangement now begins in."""
  startYear: Int!

  """The category the standing cost is filed under."""
  categoryName: String!

  """The standing cost."""
  value: Decimal!
}

"""
How much of a standing arrangement a removal takes out.

**This is this domain's own spelling of a shape the platform states once**, because the contracts'
`IRecurringExpenseDeleteInput` is what both standing-cost resources' removals read and a document
declares the concepts it serves rather than reaching into a neighbour's for one: the employee's own
arrangements declare the same three members under their own name.

`deletionType` is the contracts' own value: `all` removes every row of the arrangement, `future` moves
its end back to the month before the one named so that nothing later remains, and `current` takes the
named month out of the middle — closing the arrangement before it and opening its successor after it.
`month` and `year` name the month all three are measured from; `all` is the one that does not read them.
"""
input OrganizationRecurringExpenseDeleteInput {
  """Which of the three removals this is: `all`, `future` or `current`."""
  deletionType: String!

  """The month the removal is measured from, from 1 to 12."""
  month: Int!

  """The four-digit year the removal is measured from."""
  year: Int!
}

"""One window of work inside one project."""
type OrganizationSprint {
  id: ID!

  """
  The sprint's name, as it is spoken of in the project. The column is not nullable, so a row always
  names itself.
  """
  name: String!

  """
  What the window is for: the outcome the team is trying to reach before it closes.
  """
  goal: String

  """
  The length of the sprint's window **in days**. The column carries a default of seven, which is the
  length a sprint is filed with when the caller states none.
  """
  length: Int!

  """
  When the sprint's window opens, or null while the row records no opening date.
  """
  startDate: DateTime

  """
  When the sprint's window closes, or null while the row records no closing date.
  """
  endDate: DateTime

  """
  Where the sprint is in its life — `draft`, `upcoming`, `active` or `completed`. Carried as its own
  value rather than declared as an enum: the vocabulary is the platform's
  `OrganizationSprintStatusEnum`, which types the column and which the delivered write validates
  against, so a second declaration here could disagree with it.
  """
  status: String

  """
  Which day the sprint's own week starts on, in the platform's numbering: 1 is Sunday through 7 is
  Saturday. Carried as its own value for the same reason `status` is — the vocabulary is the
  platform's `SprintStartDayEnum` and the delivered write validates against it.
  """
  dayStart: Int

  """
  The sprint's progress as one structured document: the state and the metrics a board reads without
  recounting the tasks of the sprint. Authored and updated as a whole document rather than derived
  from the task rows, which is why it is carried whole.
  """
  sprintProgress: JSON

  """
  The project the sprint is filed under, by identifier. The project row itself is a row of the
  project domain, the delivered reads do not join it, and the identifier is what this row carries —
  so the row is read from the project surface rather than through a member here that would answer
  null.
  """
  projectId: ID

  """
  The organization the sprint is filed under, by identifier. The organization row is read from the organization surface.
  """
  organizationId: ID

  """
  The tenant the sprint belongs to, by identifier. The tenant row is read from the tenant surface.
  """
  tenantId: ID

  """
  When the sprint was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two lifecycle moves this resource serves — and this column is the whole of what they
  write, so without it a caller could not tell a live row from a withdrawn one on the answer to the
  write that withdrew it.
  """
  deletedAt: DateTime

  """When the sprint was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the sprint was filed."""
  createdAt: DateTime

  """When the sprint was last changed."""
  updatedAt: DateTime

  """
  Whether the sprint is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the sprint is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the sprint, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the sprint, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the sprint, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of sprints."""
type OrganizationSprintEdge {
  node: OrganizationSprint!
  cursor: String!
}

"""
One page of sprints, with the total the filters select and the boundary cursors.
"""
type OrganizationSprintConnection {
  nodes: [OrganizationSprint!]!
  edges: [OrganizationSprintEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a sprint list may be sorted by."""
enum OrganizationSprintSortField {
  createdAt
  updatedAt
  name
  startDate
  endDate
  status
  length
}

"""
The order the sprints are returned in. At most three keys are accepted.
"""
input OrganizationSprintSort {
  field: OrganizationSprintSortField!
  direction: SortDirection!
}

"""
The fields a sprint list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered read answers, because the connection narrows the rows
the service returned: a member the read does not select would be a filter that silently selects
nothing. The membership pivot and the task links are in neither, and that is a statement rather than an
omission — the delivered list read joins the relations its `data` query parameter names, this surface
names none, so a filter on the people working a sprint or on the tasks planned into it would be
evaluated against a row that carries none of them and would select nothing at all, which is the worst
answer a filter can give. `status` and `dayStart` are here because they are the two questions a list of
sprints is actually narrowed by, and both are compared as the values the columns store.
"""
input OrganizationSprintFilter {
  id: IDFilter
  name: StringFilter
  goal: StringFilter

  """How many days long the sprint's window is."""
  length: NumberFilter

  """When the sprint's window opens."""
  startDate: DateTimeFilter

  """When the sprint's window closes."""
  endDate: DateTimeFilter
  status: StringFilter

  """
  Which day the sprint's own week starts on: 1 is Sunday through 7 is Saturday.
  """
  dayStart: NumberFilter
  sprintProgress: JSONFilter
  projectId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationSprintFilter!]
  or: [OrganizationSprintFilter!]
  not: OrganizationSprintFilter
}

"""
What a caller states when it files a sprint.

The members are the ones the delivered create body validates, and the handler reads every one of them.
Five of them are **required**, which is the delivered body's own statement rather than this file's:
`name` because the column is not nullable, `projectId` because the validator behind it refuses an absent
value outright, and `organizationId`, `length` and `status` because the validation the delivered body
runs is conditioned only on the caller naming the organization row or a recipient instead — which this
surface offers no member for — so a REST caller that leaves one of them out is refused before the
handler runs, even though the columns behind `length` and `status` carry defaults. `goal`, the two
dates, `dayStart` and the progress document are optional, and a caller that states none of them files a
sprint whose columns are as the store defaults them.

`memberIds` and `managerIds` are the two relations the delivered write reads: the employees named are
looked up in the caller's own organization and stored as the sprint's membership rows, with the ones
named as managers marked as such. They are stated as identifiers because that is what the delivered
write persists — the pivot is written from the identifiers rather than from rows carried beside them.
`projectId` is the other relation column of this row, and naming it is what files the sprint under a
project.

The tenant is never a member: the delivered handler takes it from the credential and overwrites
whatever a body carries, so there is no way for a caller to file a sprint into a tenant it is not acting
in. The base row's own bookkeeping — the withdrawal marker, the archive date, the two lifecycle flags
and the record's own timestamps — is not stated either: withdrawing and restoring are delivered routes
whose whole effect is that one column, and the rest are columns the platform maintains rather than
facts a caller files a sprint with.
"""
input CreateOrganizationSprintInput {
  organizationId: ID!
  projectId: ID!
  name: String!
  goal: String

  """
  The length of the sprint's window **in days**. The delivered body requires it; the column defaults to seven.
  """
  length: Int!
  startDate: DateTime
  endDate: DateTime

  """
  Where the sprint starts in its life: `draft`, `upcoming`, `active` or `completed`. The vocabulary is
  the platform's `OrganizationSprintStatusEnum`, and the delivered body requires the member.
  """
  status: String!

  """
  Which day the sprint's own week starts on: 1 is Sunday through 7 is Saturday.
  """
  dayStart: Int

  """The progress document, as the caller states it."""
  sprintProgress: JSON

  """The employees the sprint is worked by, by identifier."""
  memberIds: [ID!]

  """
  The employees who manage the sprint, by identifier. A manager is a member as well.
  """
  managerIds: [ID!]
}

"""
What a caller states when it changes a sprint that exists.

Every member is optional here, which is the shape the delivered edit body has: it is the create body
made partial, so a member the caller leaves out is not validated and not stated. The identifier is the
one member stated outside the body, because the delivered route carries it in the path — and the
delivered handler reads the row it names before it writes, so a sprint of another tenant, or one that
is not there, is a miss rather than a write under an identifier the caller does not own.

**The delivered write reaches the row only when a member or a manager is stated.** The handler resolves
the named employees first and writes the stated facts and the membership set together in the one call it
makes once that set is non-empty, so a body that states a name and nobody is answered with the row
unchanged rather than written — and the employees a caller leaves out of a stated set are removed from
the sprint, because a stated set replaces the membership rather than adding to it. The answer is the row
as it stands afterwards either way, which is what the handler reads back once the write has run.
"""
input UpdateOrganizationSprintInput {
  id: ID!
  organizationId: ID
  projectId: ID
  name: String
  goal: String

  """The length of the sprint's window **in days**."""
  length: Int
  startDate: DateTime
  endDate: DateTime

  """
  The lifecycle state. The vocabulary is the platform's `OrganizationSprintStatusEnum`.
  """
  status: String

  """
  Which day the sprint's own week starts on: 1 is Sunday through 7 is Saturday.
  """
  dayStart: Int
  sprintProgress: JSON

  """
  The employees the sprint is worked by, by identifier. Stating any member replaces the set.
  """
  memberIds: [ID!]

  """
  The employees who manage the sprint, by identifier. A manager is a member as well.
  """
  managerIds: [ID!]
}

"""One direction an organization has decided to move in."""
type OrganizationStrategicInitiative {
  id: ID!

  """A short human-readable name for the initiative."""
  title: String!

  """
  The long-form statement of what the organization is deliberately trying to move forward, and why.
  """
  intent: String

  """
  Where the initiative has got to — `draft`, `active`, `resolved` or `retired`. Carried as its own
  value rather than declared as an enum: the vocabulary is the delivered service's, and it is the same
  one the column is typed with.
  """
  state: String!

  """
  Who may read the initiative — `leadership`, `organization` or `team`. Carried as its own value for
  the same reason `state` is: the service switches on this vocabulary when it decides what a caller
  may see, and a second declaration here could disagree with it.
  """
  visibilityScope: String!

  """
  The qualitative signals the initiative carries, as one structured document: the confidence level,
  the perceived momentum, the known risks, the strategic notes, and when and by whom it was last
  assessed. Human-authored rather than derived, so it is carried whole.
  """
  signals: JSON

  """
  The steward who owns the clarity of the initiative's intent, by identifier. The person is a row of
  the employee domain and the delivered reads do not join it, so the identifier is carried rather
  than a member that would answer null.
  """
  stewardId: ID

  """The organization the initiative is filed under, by identifier."""
  organizationId: ID

  """The tenant the initiative belongs to, by identifier."""
  tenantId: ID

  """
  When the initiative was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the initiative was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the initiative was filed."""
  createdAt: DateTime

  """When the initiative was last changed."""
  updatedAt: DateTime

  """
  Whether the initiative is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the initiative is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the initiative, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the initiative, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the initiative, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of strategic initiatives."""
type OrganizationStrategicInitiativeEdge {
  node: OrganizationStrategicInitiative!
  cursor: String!
}

"""
One page of strategic initiatives, with the total the filters select and the boundary cursors.
"""
type OrganizationStrategicInitiativeConnection {
  nodes: [OrganizationStrategicInitiative!]!
  edges: [OrganizationStrategicInitiativeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a strategic-initiative list may be sorted by."""
enum OrganizationStrategicInitiativeSortField {
  createdAt
  updatedAt
  title
  state
  visibilityScope
}

"""
The order the strategic initiatives are returned in. At most three keys are accepted.
"""
input OrganizationStrategicInitiativeSort {
  field: OrganizationStrategicInitiativeSortField!
  direction: SortDirection!
}

"""
The fields a strategic-initiative list may be narrowed by.

`state` and `visibilityScope` are here because they are the two questions a list of directions is
actually narrowed by. The aligned projects and goals are not, and that is a statement rather than an
omission — the connection is applied to the rows the delivered read decided the caller may see, and an
alignment is read from the project or the goal that names this initiative.
"""
input OrganizationStrategicInitiativeFilter {
  id: IDFilter
  title: StringFilter
  intent: StringFilter
  state: StringFilter
  visibilityScope: StringFilter
  signals: JSONFilter
  stewardId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationStrategicInitiativeFilter!]
  or: [OrganizationStrategicInitiativeFilter!]
  not: OrganizationStrategicInitiativeFilter
}

"""
What a caller states when it files a strategic initiative.

The organization is required because the delivered create body requires it, and the steward is
optional because the delivered handler answers with the caller's own employee when none is stated —
which is what makes a person who files a direction its first steward without having to say so.

The aligned projects and goals are not members: the delivered create does not cascade them, so a body
stating them would be accepted and then dropped. An alignment is written from the project that names
the initiative.
"""
input CreateOrganizationStrategicInitiativeInput {
  organizationId: ID!
  title: String!
  intent: String

  """
  The lifecycle state. The vocabulary is the delivered service's: `draft`, `active`, `resolved`, `retired`.
  """
  state: String

  """
  Who may read it. The vocabulary is the delivered service's: `leadership`, `organization`, `team`.
  """
  visibilityScope: String

  """The qualitative signals document, as the caller states it."""
  signals: JSON

  """
  The steward who owns the clarity of the intent. Omit it and the caller's own employee is recorded.
  """
  stewardId: ID
}

"""
What a caller states when it edits a strategic initiative that exists.

Every member is optional, which is the shape the delivered edit body has — it is the create body made
partial — and the delivered handler reads the row before it writes, so an initiative that is not the
caller's is a miss rather than a write under an identifier the caller does not own.
"""
input UpdateOrganizationStrategicInitiativeInput {
  id: ID!
  organizationId: ID
  title: String
  intent: String
  state: String
  visibilityScope: String
  signals: JSON
  stewardId: ID
}

"""
The signals a caller states when it assesses an initiative.

**These merge into the document rather than replacing it.** The delivered write reads the signals the
row already carries, lays the stated members over them, and stamps the assessment with its time and the
assessing employee — so a caller states what it has just learned and the signals a previous assessment
left behind are kept.
"""
input UpdateOrganizationStrategicSignalsInput {
  id: ID!

  """The subjective confidence in the initiative's progress."""
  confidenceLevel: String

  """The directional energy the caller perceives."""
  perceivedMomentum: String

  """Current blockers or concerns, as free text."""
  knownRisks: [String!]

  """Additional context or observations."""
  strategicNotes: String

  """
  The employee who performed this assessment. The delivered write stamps the caller's own employee.
  """
  lastAssessedById: ID
}

"""
The row that decides how the task tracker behaves inside one organization.
"""
type OrganizationTaskSetting {
  id: ID!

  """Whether tasks carry privacy features, such as restricted visibility."""
  isTasksPrivacyEnabled: Boolean!

  """Whether a task may be assigned to more than one person."""
  isTasksMultipleAssigneesEnabled: Boolean!

  """Whether time may be recorded on a task by hand rather than by timer."""
  isTasksManualTimeEnabled: Boolean!

  """Whether a task may be estimated collectively by a group."""
  isTasksGroupEstimationEnabled: Boolean!

  """Whether a task may be estimated in hours."""
  isTasksEstimationInHoursEnabled: Boolean!

  """Whether a task may be estimated in story points."""
  isTasksEstimationInStoryPointsEnabled: Boolean!

  """Whether a task's completion may require proof."""
  isTasksProofOfCompletionEnabled: Boolean!

  """
  What kind of proof a completion requires. Carried as its own value rather than declared as an enum:
  the vocabulary is the shared constants' `TaskProofOfCompletionTypeEnum`, which types the column.
  """
  tasksProofOfCompletionType: String!

  """Whether tasks may be linked to one another."""
  isTasksLinkedEnabled: Boolean!

  """Whether a task accepts comments."""
  isTasksCommentsEnabled: Boolean!

  """Whether changes to a task are recorded for later reference."""
  isTasksHistoryEnabled: Boolean!

  """Whether a task may state acceptance criteria for its completion."""
  isTasksAcceptanceCriteriaEnabled: Boolean!

  """Whether a task may be saved as a draft before it is published."""
  isTasksDraftsEnabled: Boolean!

  """Whether a notification is sent as a task's due date approaches."""
  isTasksNotifyLeftEnabled: Boolean!

  """How many days before its due date a task is notified about."""
  tasksNotifyLeftPeriodDays: Int!

  """Whether a task is closed automatically after a period."""
  isTasksAutoCloseEnabled: Boolean!

  """How many days after it is filed a task is closed automatically."""
  tasksAutoClosePeriodDays: Int!

  """Whether a task is archived automatically after a period."""
  isTasksAutoArchiveEnabled: Boolean!

  """How many days after it is filed a task is archived automatically."""
  tasksAutoArchivePeriodDays: Int!

  """Whether a task's status is updated automatically."""
  isTasksAutoStatusEnabled: Boolean!

  """
  The project this row is scoped to, when it is scoped more narrowly than the organization. The
  project is not joined by the delivered read, so the identifier is carried rather than a member that
  would answer null.
  """
  projectId: ID

  """
  The team this row is scoped to, when it is scoped more narrowly than the organization. The team is
  not joined by the delivered read, so the identifier is carried rather than a member that would
  answer null.
  """
  organizationTeamId: ID

  """The organization the row answers for, by identifier."""
  organizationId: ID

  """The tenant the row belongs to, by identifier."""
  tenantId: ID

  """When the row was withdrawn, or null while it is live."""
  deletedAt: DateTime

  """When the row was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the row was filed."""
  createdAt: DateTime

  """When the row was last changed."""
  updatedAt: DateTime

  """
  Whether the row is active. The delivered read answers active rows only.
  """
  isActive: Boolean

  """
  Whether the row is archived. The delivered read answers unarchived rows only.
  """
  isArchived: Boolean

  """
  The person who filed the row, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the row, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the row, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""
What a caller states when it files the settings for an organization.

Every setting is optional, because the columns behind them all carry a default: a body that states none
of them files a row with the platform's own defaults, which is what an organization that never opened
this screen already runs on. The organization is required — the delivered read and the delivered write
both address a task setting by the organization it answers for.

The object spelling of the organization the delivered body also accepts is not offered: a write names
the row it is scoped to by identifier, and the tenant is stamped from the credential rather than
stated.
"""
input CreateOrganizationTaskSettingInput {
  organizationId: ID!

  """Whether tasks carry privacy features, such as restricted visibility."""
  isTasksPrivacyEnabled: Boolean

  """Whether a task may be assigned to more than one person."""
  isTasksMultipleAssigneesEnabled: Boolean

  """Whether time may be recorded on a task by hand rather than by timer."""
  isTasksManualTimeEnabled: Boolean

  """Whether a task may be estimated collectively by a group."""
  isTasksGroupEstimationEnabled: Boolean

  """Whether a task may be estimated in hours."""
  isTasksEstimationInHoursEnabled: Boolean

  """Whether a task may be estimated in story points."""
  isTasksEstimationInStoryPointsEnabled: Boolean

  """Whether a task's completion may require proof."""
  isTasksProofOfCompletionEnabled: Boolean

  """
  What kind of proof a completion requires. The vocabulary is the shared constants' own.
  """
  tasksProofOfCompletionType: String

  """Whether tasks may be linked to one another."""
  isTasksLinkedEnabled: Boolean

  """Whether a task accepts comments."""
  isTasksCommentsEnabled: Boolean

  """Whether changes to a task are recorded for later reference."""
  isTasksHistoryEnabled: Boolean

  """Whether a task may state acceptance criteria for its completion."""
  isTasksAcceptanceCriteriaEnabled: Boolean

  """Whether a task may be saved as a draft before it is published."""
  isTasksDraftsEnabled: Boolean

  """Whether a notification is sent as a task's due date approaches."""
  isTasksNotifyLeftEnabled: Boolean

  """How many days before its due date a task is notified about."""
  tasksNotifyLeftPeriodDays: Int

  """Whether a task is closed automatically after a period."""
  isTasksAutoCloseEnabled: Boolean

  """How many days after it is filed a task is closed automatically."""
  tasksAutoClosePeriodDays: Int

  """Whether a task is archived automatically after a period."""
  isTasksAutoArchiveEnabled: Boolean

  """How many days after it is filed a task is archived automatically."""
  tasksAutoArchivePeriodDays: Int

  """Whether a task's status is updated automatically."""
  isTasksAutoStatusEnabled: Boolean

  """
  The project to scope the row to, when it is scoped more narrowly than the organization.
  """
  projectId: ID

  """
  The team to scope the row to, when it is scoped more narrowly than the organization.
  """
  organizationTeamId: ID
}

"""
What a caller states when it edits the settings of an organization.

The delivered edit body is the same body the create validates — every setting is optional and the
organization is stated — and the identifier is carried in the path, so it is the one member stated
outside the body here too.
"""
input UpdateOrganizationTaskSettingInput {
  id: ID!
  organizationId: ID!

  """Whether tasks carry privacy features, such as restricted visibility."""
  isTasksPrivacyEnabled: Boolean

  """Whether a task may be assigned to more than one person."""
  isTasksMultipleAssigneesEnabled: Boolean

  """Whether time may be recorded on a task by hand rather than by timer."""
  isTasksManualTimeEnabled: Boolean

  """Whether a task may be estimated collectively by a group."""
  isTasksGroupEstimationEnabled: Boolean

  """Whether a task may be estimated in hours."""
  isTasksEstimationInHoursEnabled: Boolean

  """Whether a task may be estimated in story points."""
  isTasksEstimationInStoryPointsEnabled: Boolean

  """Whether a task's completion may require proof."""
  isTasksProofOfCompletionEnabled: Boolean

  """
  What kind of proof a completion requires. The vocabulary is the shared constants' own.
  """
  tasksProofOfCompletionType: String

  """Whether tasks may be linked to one another."""
  isTasksLinkedEnabled: Boolean

  """Whether a task accepts comments."""
  isTasksCommentsEnabled: Boolean

  """Whether changes to a task are recorded for later reference."""
  isTasksHistoryEnabled: Boolean

  """Whether a task may state acceptance criteria for its completion."""
  isTasksAcceptanceCriteriaEnabled: Boolean

  """Whether a task may be saved as a draft before it is published."""
  isTasksDraftsEnabled: Boolean

  """Whether a notification is sent as a task's due date approaches."""
  isTasksNotifyLeftEnabled: Boolean

  """How many days before its due date a task is notified about."""
  tasksNotifyLeftPeriodDays: Int

  """Whether a task is closed automatically after a period."""
  isTasksAutoCloseEnabled: Boolean

  """How many days after it is filed a task is closed automatically."""
  tasksAutoClosePeriodDays: Int

  """Whether a task is archived automatically after a period."""
  isTasksAutoArchiveEnabled: Boolean

  """How many days after it is filed a task is archived automatically."""
  tasksAutoArchivePeriodDays: Int

  """Whether a task's status is updated automatically."""
  isTasksAutoStatusEnabled: Boolean

  """
  The project to scope the row to, when it is scoped more narrowly than the organization.
  """
  projectId: ID

  """
  The team to scope the row to, when it is scoped more narrowly than the organization.
  """
  organizationTeamId: ID
}

"""
One membership: the row that puts an employee on an organization team, with the role, the roster
position and the task the member works on there.
"""
type OrganizationTeamEmployee {
  id: ID!

  """
  The position of the member in the team's roster. An ordering among siblings, not a quantity.
  """
  order: Int

  """Whether the tracker records this member's time for the team."""
  isTrackingEnabled: Boolean

  """
  Whether the member manages the team. The membership's own flag, beside the role it carries.
  """
  isManager: Boolean

  """When the member was assigned to the team."""
  assignedAt: DateTime

  """
  The task the member is currently working on, by identifier. The task row is not joined by the read
  these writes answer with; it is read from the task surface by this identifier.
  """
  activeTaskId: ID

  """
  The team the membership belongs to, by identifier. The team row is not joined by the read these
  writes answer with; it is read from the organization team surface by this identifier.
  """
  organizationTeamId: ID!

  """
  The employee the membership puts on the team, by identifier. The employee row is not joined by the
  read these writes answer with; it is read from the employee surface by this identifier.
  """
  employeeId: ID!

  """
  The role the membership carries, by identifier, or null for a member who carries none. Nullable
  because the delivered writes store no role for an ordinary member. The role row is not joined by
  the read these writes answer with; it is read from the role surface by this identifier.
  """
  roleId: ID

  """
  The tenant the membership is filed under, by identifier. The tenant is stamped from the credential
  on every write here and is never stated by a caller; the row is read from the tenant surface.
  """
  tenantId: ID

  """
  The organization the membership is filed under, by identifier. The organization row is not joined
  by the read these writes answer with; it is read from the organization surface by this identifier.
  """
  organizationId: ID

  """When the membership was filed."""
  createdAt: DateTime

  """When the membership was last changed."""
  updatedAt: DateTime

  """
  Whether the membership is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the membership is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """When the membership was archived, when an operator archived it."""
  archivedAt: DateTime

  """
  The person who put the member on the team, by identifier. The membership write stamps this from the
  credential, and the user row is not joined by the read.
  """
  createdByUserId: ID

  """
  The person who last changed the membership, by identifier. The user row is not joined by the read.
  """
  updatedByUserId: ID
}

"""
What a caller states when it edits a membership.

The members are the ones the delivered edit body validates. The delivered route carries the identifier
in its path and the facts in its body; the identifier is stated here as the field's own argument rather
than inside the payload, so no payload can name one membership and no path another.

Three members the route's own DTO carries are deliberately absent, and a fourth member of the
platform's update input is absent for a reason of its own:

- the tenant is not declared. The delivered service reads the tenant from the credential first and
  falls back to a stated one only when the credential carries none, which a request on this endpoint
  never does, so a caller-stated tenant could not change the row that is written;
- `organization` is not declared beside `organizationId`. The delivered validator accepts the relation
  object in place of the identifier for a REST caller that has one loaded; a GraphQL caller states the
  identifier, and the row it names is read from the organization surface;
- `sentTo` is not declared. The delivered DTO carries it so that a flow which sends the membership
  onward can omit the organization, and the delivered write never reads it: a caller that sent one
  would be told nothing and no column would change;
- `isManager` is not declared. It is a member of the platform's own update input, but the delivered
  route's body whitelist drops it — the edit DTO does not carry it — so a write stating it would be
  accepted and silently discarded. A flag a caller may set has to be a flag that is written.
"""
input UpdateOrganizationTeamEmployeeInput {
  """
  The organization the membership is filed under. The delivered write scopes its criteria by it.
  """
  organizationId: ID!

  """
  The team the membership belongs to. The delivered write scopes its criteria by it.
  """
  organizationTeamId: ID!

  """
  The task the member is moved onto, by identifier. The task row is read from the task surface.
  """
  activeTaskId: ID

  """Whether the tracker records this member's time for the team."""
  isTrackingEnabled: Boolean

  """The position of the member in the team's roster."""
  order: Int
}

"""
What a caller states when it moves a member onto a different task.

The delivered route validates the same organization and team members as the edit above — they are the
scope the write's criteria are built from — and reads the task from the payload. The identifier is the
field's own argument, as it is on the edit, because the delivered route carries it in the path.
"""
input OrganizationTeamEmployeeActiveTaskInput {
  """
  The organization the membership is filed under. The delivered write scopes its criteria by it.
  """
  organizationId: ID!

  """
  The team the membership belongs to. The delivered write scopes its criteria by it.
  """
  organizationTeamId: ID!

  """
  The task the member is moved onto, by identifier. The task row is read from the task surface.
  Nullable because the delivered body declares it so: a request that names no task passes the route's
  own validation, and a field that demanded one would refuse a request the REST route serves.
  """
  activeTaskId: ID
}

"""
What a caller states when it removes a membership.

The delivered route is a removal whose scope is taken from its query string rather than from a body —
`DELETE /api/organization-team-employee/:id?organizationId=…&organizationTeamId=…` — and the delivered
service builds its criteria from all of it together with the path identifier, so the identifier alone
does not name a row: what the removal reaches is a membership of the caller's own tenant, in the
stated organization and the stated team.

The members are the query string's own names, kept exactly as the delivered DTO declares them, and they
are grouped in one input rather than spread across the field's arguments so that the scope of the
removal reads as the one thing the request states beside the identifier. The tenant the DTO also
accepts is not declared: the delivered service reads the tenant from the credential first and falls
back to a stated one only when the credential carries none, which a request on this endpoint never
does.
"""
input OrganizationTeamEmployeeDeleteInput {
  """
  The organization the membership is filed under. The delivered write scopes its criteria by it.
  """
  organizationId: ID!

  """
  The team the membership belongs to. The delivered write scopes its criteria by it.
  """
  organizationTeamId: ID!
}

"""
One request to join an organization team, as the delivered reads answer it.
"""
type OrganizationTeamJoinRequest {
  """
  The row's identifier. It is present on every answer of the connection and withheld on the
  validation's answer: the delivered reader removes it from the row before it answers, so that a
  caller holding a confirmation code cannot then read the row by identifier. The field is therefore
  nullable, which is the honest statement of the one answer on this surface that has no identifier.
  """
  id: ID

  """
  The tenant the request was filed in. Stamped by the delivered write, never stated by the caller.
  """
  tenantId: ID

  """
  The organization the requested team belongs to. Stamped by the delivered write.
  """
  organizationId: ID

  """
  The address that asked to join, and the address the confirmation is mailed to.
  """
  email: String!

  """
  The name the applicant stated. The delivered write does not persist a stated name — see
  `CreateOrganizationTeamJoinRequestInput` — so this member is read on a row some other write filled
  in, and is null on a request filed over either protocol.
  """
  fullName: String

  """
  A link the applicant stated about itself. Read and written the same way `fullName` is.
  """
  linkAddress: String

  """
  A position the applicant stated about itself. Read and written the same way `fullName` is.
  """
  position: String

  """
  The state of the request. Null while the ask has not been validated, which is the state the
  delivered write of the ask stores; `REQUESTED` once the mailed code has been presented; `ACCEPTED`
  or `REJECTED` once a manager has moved it.
  """
  status: OrganizationTeamJoinRequestStatus

  """
  Whether the confirmation has lapsed. Derived on load by the entity's own subscriber from the
  instant the row expires, which is withheld from the answer — this flag is the liveness fact a
  manager reads, and a lapsed request is one no code can confirm.
  """
  isExpired: Boolean!

  """
  The team the ask is for. The row is read on the organization-team domain, because the delivered
  reads this surface mirrors join no relation.
  """
  organizationTeamId: ID!

  """
  The person the invitation produced once it was accepted: the user of the tenant an acceptance
  recorded or created. Null while the request is pending or rejected. Read on the user domain, which
  is where that row's own type lives.
  """
  userId: ID

  """When the ask was filed."""
  createdAt: DateTime

  """When the row last moved."""
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
The state one join request is in.

The vocabulary is the domain's own — `OrganizationTeamJoinRequestStatusEnum` types this row's column
and nothing else — so it is declared here as a closed set rather than carried as its value. All three
members are ones the delivered reader can produce.
"""
enum OrganizationTeamJoinRequestStatus {
  """
  The ask was confirmed with the mailed code and is waiting for a manager.
  """
  REQUESTED

  """
  A manager accepted the ask, and the person is now a member of the team or of the tenant.
  """
  ACCEPTED

  """A manager rejected the ask."""
  REJECTED
}

"""
The two moves the delivered write acts on, as the route's `:action` segment carries them.

`OrganizationTeamJoinRequestStatusEnum` holds a third member, `REQUESTED`, and this enum does not: it is
the state a request is already in, and no route makes it a move. The delivered writer switches on
exactly the two members below, in two branches, and writes nothing at all for any other value — so a
free string here would accept every other value and answer the caller as though the move had been made,
while the row kept the state it had. A silent no-op read as success is the worst answer a write can
give, and the closed set is what makes such a value unrepresentable rather than ignored.
"""
enum OrganizationTeamJoinRequestAction {
  """
  Accepts the ask: the person joins the team, and a user row is created when the tenant has none.
  """
  ACCEPTED

  """
  Rejects the ask: the row is marked rejected and nothing else is written.
  """
  REJECTED
}

"""The boundary of one page of join requests."""
type OrganizationTeamJoinRequestEdge {
  node: OrganizationTeamJoinRequest!
  cursor: String!
}

"""
One page of join requests, with the total the filters select and the boundary cursors.
"""
type OrganizationTeamJoinRequestConnection {
  nodes: [OrganizationTeamJoinRequest!]!
  edges: [OrganizationTeamJoinRequestEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a join-request list may be sorted by."""
enum OrganizationTeamJoinRequestSortField {
  createdAt
  updatedAt
  email
  status
  isExpired
}

"""
The order the requests are returned in. At most three keys are accepted.
"""
input OrganizationTeamJoinRequestSort {
  field: OrganizationTeamJoinRequestSortField!
  direction: SortDirection!
}

"""Comparison on the state of a request."""
input OrganizationTeamJoinRequestStatusFilter {
  eq: OrganizationTeamJoinRequestStatus
  ne: OrganizationTeamJoinRequestStatus
  in: [OrganizationTeamJoinRequestStatus!]
  nin: [OrganizationTeamJoinRequestStatus!]
  isNull: Boolean
}

"""
The fields a join-request list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows the
rows the service returned: a member the read does not select would be a filter that silently selects
nothing, which is the worst answer a filter can give. `isExpired` is here although it is not a column,
because the entity derives it on every row a read loads and "which of these are still live" is the
first question a manager's queue is read by. The relations the row carries as identifiers — the team
and the person — are here for the same reason they are members of the type above.
"""
input OrganizationTeamJoinRequestFilter {
  id: IDFilter
  email: StringFilter
  fullName: StringFilter
  linkAddress: StringFilter
  position: StringFilter
  status: OrganizationTeamJoinRequestStatusFilter
  organizationTeamId: IDFilter
  userId: IDFilter
  organizationId: IDFilter
  tenantId: IDFilter
  isExpired: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [OrganizationTeamJoinRequestFilter!]
  or: [OrganizationTeamJoinRequestFilter!]
  not: OrganizationTeamJoinRequestFilter
}

"""
What the ask and the resend both answer.

The delivered handlers answer this pair and nothing else: the row they wrote is not in it, and neither
is any fact about whether a confirmation was mailed. That is the design rather than an accident — the
ask is filed by an address that holds no credential, so an answer that distinguished "a request was
filed" from "a request for this address in this team already exists" would let a caller probe which
addresses have asked to join a team. `status` is the member the handlers state in the body, not the
transport's response code: the create route's transport answers `201` while the body it carries states
`200`.
"""
type OrganizationTeamJoinRequestOutcome {
  """
  The status the delivered handlers state in the body: `200` on both routes.
  """
  status: Int!

  """
  The message the delivered handlers state in the body: `OK` on both routes.
  """
  message: String!
}

"""
What a caller states when it asks to join one team.

**Only the members the delivered write actually reads are offered.** `email` and `organizationTeamId`
are the address and the team the row is filed under, and the six branding members are what the
confirmation message is composed with — those are the whole of the payload the delivered create builds
its row from, and it builds it explicitly: the team, the address, the tenant and organization of the
team, the generated code and token, and a null status. A body member for the applicant's own name, link
or position is validated by the delivered body and then dropped, so offering one here would be offering
a write that is accepted and silently discarded; a member for the state would be dropped the same way,
and the state a new ask is in is what the delivered write decides rather than what a caller states.

Nothing names the tenant or the organization. Both are taken from the team the request names — the
delivered write reads the team and stamps its tenant and organization — so a caller cannot file a
request into a scope it does not own, and there is nothing for it to choose.
"""
input CreateOrganizationTeamJoinRequestInput {
  """The address that asks to join. The confirmation code is mailed to it."""
  email: String!

  """
  The team to ask to join. The delivered write reads it, and only a public team can be asked for.
  """
  organizationTeamId: ID!

  """
  The application name the confirmation message greets the applicant with.
  """
  appName: String

  """The logo the confirmation message shows."""
  appLogo: String

  """The signature the confirmation message is signed with."""
  appSignature: String

  """The link to the application the confirmation message points at."""
  appLink: String

  """The link to the company the confirmation message carries."""
  companyLink: String

  """The company name the confirmation message carries."""
  companyName: String
}

"""
What a caller states when it presents the confirmation mailed to its address.

The address, the team, and one of the two credentials the message carried: the code a human retypes, or
the token the link carries. **Exactly one of `code` and `token` is required**, which is the rule the
delivered DTO applies — each of the two is validated only when the other is absent — and it is stated
here because the delivered read drops a credential that was not supplied rather than matching a row
that has none: a body stating neither is refused rather than matched against every pending request of
the team.
"""
input ValidateOrganizationTeamJoinRequestInput {
  """The address the request was filed under."""
  email: String!

  """The team the request was filed for."""
  organizationTeamId: ID!

  """
  The confirmation code from the message. Required when no token is stated.
  """
  code: String

  """
  The confirmation token from the message's link. Required when no code is stated.
  """
  token: String
}

"""
What a caller states when it asks for a fresh confirmation to be mailed.

The same members the ask carries, and for the same reason: the delivered method reads the address, the
team and the branding off the body and nothing else. The row it rewrites is found by the first two —
the pending request of that address in that team — so a caller cannot name a row directly, and the
credentials it replaces are generated rather than stated.
"""
input ResendOrganizationTeamJoinRequestCodeInput {
  """The address the pending request was filed under."""
  email: String!

  """The team the pending request was filed for."""
  organizationTeamId: ID!
  appName: String
  appLogo: String
  appSignature: String
  appLink: String
  companyLink: String
  companyName: String
}

"""
One working group inside an organization: the scope a task, a daily plan and a tracked stretch of time
are filed under, with a membership of its own.
"""
type OrganizationTeam {
  id: ID!

  """
  The tenant the team belongs to, by identifier. The row is read from the tenant surface.
  """
  tenantId: ID

  """
  The organization the team works in, by identifier. The row is read from the organization surface.
  """
  organizationId: ID

  """
  The team's name. A team that names nothing names no group, so the delivered body requires it.
  """
  name: String!

  """The colour the team is presented in, as the row records it."""
  color: String

  """The emoji the team is presented with, as the row records it."""
  emoji: String

  """
  How large the team is, as the row records it. A figure the row carries rather than one this surface counts.
  """
  teamSize: String

  """
  A denormalised logo URL, kept for lists that must not join the asset table.
  """
  logo: String

  """
  The prefix the team's records are numbered under, when the team keeps one.
  """
  prefix: String

  """
  The team's public profile path segment, when the organization publishes one.
  """
  profile_link: String

  """
  Whether the team is visible to the organization beyond its own membership.
  """
  public: Boolean

  """
  Whether the team's members may see each other's profiles. The column defaults it to true, and the
  delivered create read drops the member, so this is a value the edit writes and a filing cannot.
  """
  shareProfileView: Boolean

  """
  Whether tracked time on the team's plans requires a daily plan to exist first. The column defaults
  it to false, and the delivered create read drops the member for the same reason as the flag above.
  """
  requirePlanToTrack: Boolean

  """
  The logo asset, by identifier. The relation is eager, so this and `image` always travel together.
  """
  imageId: ID

  """The logo asset itself."""
  image: ImageAsset

  """When the team was filed."""
  createdAt: DateTime

  """When the team was last changed."""
  updatedAt: DateTime

  """
  When the team was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write — so without it the answer to the write that
  withdrew a team would not say that it did.
  """
  deletedAt: DateTime

  """When the team was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  Whether the team is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the team is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the team, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the team, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the team, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of teams."""
type OrganizationTeamEdge {
  node: OrganizationTeam!
  cursor: String!
}

"""
One page of teams, with the total the filters select and the boundary cursors.
"""
type OrganizationTeamConnection {
  nodes: [OrganizationTeam!]!
  edges: [OrganizationTeamEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a team list may be sorted by."""
enum OrganizationTeamSortField {
  createdAt
  updatedAt
  name
}

"""The order the teams are returned in. At most three keys are accepted."""
input OrganizationTeamSort {
  field: OrganizationTeamSortField!
  direction: SortDirection!
}

"""The fields a team list may be narrowed by."""
input OrganizationTeamFilter {
  id: IDFilter
  name: StringFilter
  color: StringFilter
  emoji: StringFilter
  teamSize: StringFilter
  logo: StringFilter
  prefix: StringFilter
  profile_link: StringFilter
  public: BooleanFilter
  shareProfileView: BooleanFilter
  requirePlanToTrack: BooleanFilter
  imageId: IDFilter
  organizationId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationTeamFilter!]
  or: [OrganizationTeamFilter!]
  not: OrganizationTeamFilter
}

"""
What a caller states when it files a team.

The members are the ones the delivered create body validates and the delivered write reads. Four
groups the body accepts are deliberately absent, and each for its own reason:

- the tenant is never stated, because a write's tenant comes from the credential;
- the five presentation members of the edit below — `color`, `emoji`, `teamSize`, `shareProfileView`
  and `requirePlanToTrack` — are not offered here. The delivered body validates them and the delivered
  create read then builds its row from an explicit set of members that does not include them, so a
  filing that stated one would accept a value and write nothing at all;
- the tags and the projects are stated by identifier rather than as rows, because an identifier is
  what the relation records and what the delivered write attaches;
- `organization` and `sentTo` are omitted: the first is the delivered body's other spelling of the
  same scope, a row rather than an identifier, and the second is the escape hatch the delivered
  validator consults before it requires a scope at all. Neither is a member the write reads.
"""
input CreateOrganizationTeamInput {
  name: String!

  """
  The organization the team works in. The delivered body checks it against the caller's own memberships.
  """
  organizationId: ID!

  """
  The team's public profile path segment, when the organization publishes one.
  """
  profile_link: String

  """
  A denormalised logo URL, kept for lists that must not join the asset table.
  """
  logo: String

  """The prefix the team's records are numbered under."""
  prefix: String

  """The logo asset to file the team with, by identifier."""
  imageId: ID

  """
  Whether the team is visible to the organization beyond its own membership.
  """
  public: Boolean

  """
  The employees the team is made of, by identifier. They are stored as the team's membership.
  """
  memberIds: [ID!]

  """
  The employees who manage the team, by identifier. A caller acting as an employee is added to this
  list by the delivered write itself, which is how the person filing a team keeps hold of it.
  """
  managerIds: [ID!]

  """The tags to attach to the team, by identifier."""
  tagIds: [ID!]

  """The projects the team is attached to, by identifier."""
  projectIds: [ID!]
}

"""
What a caller states when it edits a team that exists.

The members are the create input's with the criterion stated, plus the five presentation members the
delivered create read drops and the delivered edit writes: the edit hands its whole body to the
persist call, so the row's presentation is written here rather than at the filing. `name` is optional
where the filing requires it, because the delivered edit body declares it optional — a member the
caller leaves out is left as it is.
"""
input UpdateOrganizationTeamInput {
  id: ID!
  name: String
  organizationId: ID!
  profile_link: String
  logo: String
  prefix: String
  imageId: ID
  public: Boolean
  color: String
  emoji: String
  teamSize: String
  shareProfileView: Boolean
  requirePlanToTrack: Boolean
  memberIds: [ID!]
  managerIds: [ID!]
  tagIds: [ID!]
  projectIds: [ID!]
}

"""
What a caller states when it removes a team.

The delivered query DTO picks the organization and the tenant for this route; only the organization is
carried. The removal is scoped to the organization it names, and the tenant is not offered because the
tenant a write runs under comes from the credential — the delivered service reads it from the request
context and falls back to what it was handed, so a caller could only ever restate its own.
"""
input OrganizationTeamDeleteInput {
  """The organization the removal is scoped to."""
  organizationId: ID!
}

"""One supplier an expense may be recorded against."""
type OrganizationVendor {
  id: ID!

  """The supplier's name, as the tenant states it."""
  name: String!

  """The address the organization orders through."""
  email: String

  """The telephone number the organization orders through."""
  phone: String

  """The supplier's site."""
  website: String

  """
  The operator-facing supplier code, unique per organization where it is set, and the key an order or an
  import quotes.
  """
  code: String

  """
  The ISO 4217 code a purchase order from this supplier is prefilled with; null when the currency is stated per product.
  """
  currency: String

  """
  The settlement terms in days, as the legacy simple form. A non-null `paymentTermId` wins over it.
  """
  paymentTermsDays: Int

  """
  The instalment schedule agreed with this supplier, as the identifier of the term's own row.
  """
  paymentTermId: ID

  """
  Advisory default lead time in days, used only when no product term carries one.
  """
  leadTimeDays: Int

  """
  The advisory minimum order value, surfaced as a warning at order time and never enforced. It is not the
  per-product minimum, which is a quantity and lives on the supplier's product terms.
  """
  minimumOrderAmount: Decimal

  """
  The party row of the same counterparty, where this supplier also exists as a customer or a seller contact; null otherwise. It is not unique: one counterparty may hold two supplier accounts.
  """
  contactId: ID

  """
  The tax regime that applies to purchases from this supplier, as the identifier of the regime's own row.
  """
  taxRegimeId: ID

  """Tenant-defined, non-indexed extras."""
  metadata: JSON

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered routes —
  the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of suppliers."""
type OrganizationVendorEdge {
  node: OrganizationVendor!
  cursor: String!
}

"""
One page of suppliers, with the total the filters select and the boundary cursors.
"""
type OrganizationVendorConnection {
  nodes: [OrganizationVendor!]!
  edges: [OrganizationVendorEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a supplier list may be sorted by.

The name is the master's own order, which is the order an operator scans a supplier list in; the code is
offered because an operator who files suppliers by code reads them by it; and the two timestamps are how a
change is found again. The amounts and the terms are not sort keys: a minimum order value or a lead time
is a question about one supplier, not an order for a page of them.
"""
enum OrganizationVendorSortField {
  createdAt
  updatedAt
  name
  code
}

"""
The order the suppliers are returned in. At most three keys are accepted.
"""
input OrganizationVendorSort {
  field: OrganizationVendorSortField!
  direction: SortDirection!
}

"""
The fields a supplier list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is: the delivered list read answers
those columns, so each one narrows rows the connection was handed. The minimum order amount is stated
through the decimal family, which is the family money is compared in; **the value is the row's own**, so
an equality states the amount as the read produces it, while the range operators need no such care.

`deletedAt` is deliberately absent although the object carries it: the delivered list read answers live
rows only, so a condition on that column could only ever match the empty set. The tenant and the
organization are absent for a different reason — both are applied to the criterion from the credential
rather than from the caller. The expenses a supplier stands behind are absent for a third: the delivered
list read loads no relations, so a condition on them could only ever select the empty set.
"""
input OrganizationVendorFilter {
  id: IDFilter
  name: StringFilter
  email: StringFilter
  phone: StringFilter
  website: StringFilter
  code: StringFilter
  currency: StringFilter
  paymentTermsDays: NumberFilter
  paymentTermId: IDFilter
  leadTimeDays: NumberFilter
  minimumOrderAmount: DecimalFilter
  contactId: IDFilter
  taxRegimeId: IDFilter
  metadata: JSONFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isArchived: BooleanFilter
  and: [OrganizationVendorFilter!]
  or: [OrganizationVendorFilter!]
  not: OrganizationVendorFilter
}

"""What a caller states when it records a supplier."""
input CreateOrganizationVendorInput {
  """The supplier's name."""
  name: String!

  """The address the organization orders through."""
  email: String

  """The telephone number the organization orders through."""
  phone: String

  """The supplier's site."""
  website: String

  """
  The operator-facing supplier code, unique per organization where it is set.
  """
  code: String

  """
  The ISO 4217 code a purchase order from this supplier is prefilled with.
  """
  currency: String

  """The settlement terms in days, as the legacy simple form."""
  paymentTermsDays: Int

  """The instalment schedule agreed with this supplier."""
  paymentTermId: ID

  """The advisory default lead time in days."""
  leadTimeDays: Int

  """
  The advisory minimum order value, surfaced as a warning at order time and never enforced. It is money,
  and it is stated as the exact decimal the column stores.
  """
  minimumOrderAmount: Decimal

  """The party row of the same counterparty, where one exists."""
  contactId: ID

  """The tax regime that applies to purchases from this supplier."""
  taxRegimeId: ID

  """Tenant-defined, non-indexed extras."""
  metadata: JSON

  """The organization the supplier is filed under."""
  organizationId: ID
}

"""
What a caller states when it changes a supplier.

**The delivered edit is the platform's own upsert, and it is reached through the same write the creation
is.** The route reads the row's identifier from the path, spreads the body beside it and calls the
delivered service, so a member the caller leaves out is left as it is — and an identifier that names no
row at all is **stored as a new row** rather than refused, because an upsert is what the delivered write
is. A client that means "change this supplier" reads it first, or reaches for the mutation above only with
an identifier it obtained from this surface.

This input declares the same members the creation does, because the delivered edit body is the same row:
the route binds the entity itself on both, and nothing is dropped on the edit.
"""
input UpdateOrganizationVendorInput {
  id: ID!

  """The supplier's name."""
  name: String!

  """The address the organization orders through."""
  email: String

  """The telephone number the organization orders through."""
  phone: String

  """The supplier's site."""
  website: String

  """
  The operator-facing supplier code, unique per organization where it is set.
  """
  code: String

  """
  The ISO 4217 code a purchase order from this supplier is prefilled with.
  """
  currency: String

  """The settlement terms in days, as the legacy simple form."""
  paymentTermsDays: Int

  """The instalment schedule agreed with this supplier."""
  paymentTermId: ID

  """The advisory default lead time in days."""
  leadTimeDays: Int

  """
  The advisory minimum order value, as the exact decimal the column stores.
  """
  minimumOrderAmount: Decimal

  """The party row of the same counterparty, where one exists."""
  contactId: ID

  """The tax regime that applies to purchases from this supplier."""
  taxRegimeId: ID

  """Tenant-defined, non-indexed extras."""
  metadata: JSON

  """The organization the supplier is filed under."""
  organizationId: ID
}

"""
One organization: the scope a channel, a warehouse, a price list and an invoice are filed under, and
the settings a write into any of them reads its defaults from.
"""
type Organization {
  id: ID!

  """The organization's name, as it is shown to everyone who works in it."""
  name: String!

  """
  Whether this is the tenant's default organization: the one a client is put into when it names none.
  """
  isDefault: Boolean!

  """
  The public profile's path segment, when the organization publishes one.
  """
  profile_link: String

  """The banner the public profile shows, as a URL."""
  banner: String

  """
  How many people work in the organization, as the row records it. A figure the row carries rather than one this surface counts.
  """
  totalEmployees: Int

  """The one-line description the public profile shows."""
  short_description: String

  """Who the organization works for, as the public profile puts it."""
  client_focus: String

  """
  The long-form description. The delivered writes sanitise this member through the platform's
  server-side allowlist before it is stored, because the public profile renders it as markup — the
  answer here is the sanitised text, not what the caller sent.
  """
  overview: String

  """
  A denormalised logo URL, kept for lists that must not join the asset table.
  """
  imageUrl: String

  """
  The logo asset, by identifier. The relation is eager, so this and `image` always travel together.
  """
  imageId: ID

  """The logo asset itself."""
  image: ImageAsset

  """The ISO 4217 code of the currency the organization's amounts are in."""
  currency: String!

  """
  The date the organization's books were valued at, when an operator set one.
  """
  valueDate: DateTime

  """
  Which date a new document defaults to — `TODAY`, `START_OF_MONTH`, `END_OF_MONTH` and their
  neighbours. Carried as its own value rather than declared as an enum: the vocabulary is the
  platform's `DefaultValueDateTypeEnum`, which types the column.
  """
  defaultValueDateType: String

  """The alignment a rendered document defaults to, as the row records it."""
  defaultAlignmentType: String

  """The organization's timezone, as an IANA name."""
  timeZone: String

  """
  The region code every address in the organization is resolved against when it names none.
  """
  regionCode: String

  """The brand colour a document is rendered with."""
  brandColor: String

  """The date format the organization's documents are rendered in."""
  dateFormat: String

  """
  The organization's registered legal name, when it differs from the one it is known by.
  """
  officialName: String

  """
  Which day the organization's week starts on. Carried as its own value: the vocabulary is the platform's `WeekDaysEnum`.
  """
  startWeekOn: String

  """The organization's tax registration number."""
  taxId: String

  """The number format the organization's documents are rendered in."""
  numberFormat: String

  """
  The smallest project size the organization reports on, as the row records it.
  """
  minimumProjectSize: String

  """
  Which bonus scheme applies — `PROFIT_BASED_BONUS`, `REVENUE_BASED_BONUS` and their neighbours. The vocabulary is the platform's `BonusTypeEnum`.
  """
  bonusType: String

  """
  The bonus rate as a percentage between 0 and 100. A rate rather than an amount, so it is a `Float`:
  nothing here is money, and no amount is ever read from this member.
  """
  bonusPercentage: Float

  """Whether the organization lets its administrators invite people."""
  invitesAllowed: Boolean

  """
  The number of days an invitation stays valid for, when the organization set its own.
  """
  inviteExpiryPeriod: Int

  """When the organization's fiscal year starts."""
  fiscalStartDate: DateTime

  """When the organization's fiscal year ends."""
  fiscalEndDate: DateTime

  """When the organization was registered."""
  registrationDate: DateTime

  """Whether the organization's documents may be dated in the future."""
  futureDateAllowed: Boolean

  """Whether an employee may record time by hand rather than by timer."""
  allowManualTime: Boolean!

  """Whether a recorded time entry may be changed after it was recorded."""
  allowModifyTime: Boolean!

  """Whether a recorded time entry may be removed."""
  allowDeleteTime: Boolean!

  """Whether the organization records inactivity as well as activity."""
  allowTrackInactivity: Boolean!

  """
  How many minutes of inactivity the organization tolerates before it records a gap.
  """
  inactivityTimeLimit: Int!

  """
  How many seconds of activity the organization requires as proof that work happened.
  """
  activityProofDuration: Int!

  """Whether a manually recorded time entry must state why it was recorded."""
  requireReason: Boolean!

  """Whether a manually recorded time entry must carry a description."""
  requireDescription: Boolean!

  """Whether a time entry must name a project."""
  requireProject: Boolean!

  """Whether a time entry must name a task."""
  requireTask: Boolean!

  """Whether a time entry must name a client."""
  requireClient: Boolean!

  """Whether the organization renders times on a 12- or a 24-hour clock."""
  timeFormat: Int!

  """
  Whether a document's tax and its discount are stated as separate lines.
  """
  separateInvoiceItemTaxAndDiscount: Boolean

  """The organization's public website."""
  website: String

  """The legal information a document issued by the organization carries."""
  fiscalInformation: String

  """
  Where the currency symbol sits in a rendered amount. Carried as its own value rather than declared
  as an enum: the vocabulary is the money layer's `MoneySymbolPosition`, which types the column.
  """
  currencyPosition: String!

  """
  Whether a document's discount is applied after its tax rather than before it.
  """
  discountAfterTax: Boolean

  """The time a working day defaults to starting at."""
  defaultStartTime: String

  """The time a working day defaults to ending at."""
  defaultEndTime: String

  """The terms the organization's estimates carry by default."""
  defaultInvoiceEstimateTerms: String

  """
  Whether an accepted estimate is converted into an invoice automatically.
  """
  convertAcceptedEstimates: Boolean

  """How many days an issued document has before it falls due."""
  daysUntilDue: Int

  """
  Whether idle time is removed from a recorded period before it is stored.
  """
  isRemoveIdleTime: Boolean!

  """Whether the tracker captures screenshots."""
  allowScreenshotCapture: Boolean!

  """
  The identifier the organization is known by in an external system of record, when it is linked to one.
  """
  upworkOrganizationId: String

  """The name the organization is known by in that external system."""
  upworkOrganizationName: String

  """
  Whether screenshots are taken at random intervals rather than on a fixed schedule.
  """
  randomScreenshot: Boolean

  """Whether the tracker keeps recording while the machine is asleep."""
  trackOnSleep: Boolean

  """
  How many seconds apart screenshots are taken. The column is `numeric`, so it is carried as a
  `Float` rather than an `Int`: a fractional interval the write accepts is one this surface has to be
  able to answer.
  """
  screenshotFrequency: Float!

  """
  Whether a tracking rule the organization set is enforced rather than merely recorded.
  """
  enforced: Boolean

  """
  The standard number of hours in a working day, which a schedule that states none defaults to.
  """
  standardWorkHoursPerDay: Int

  """Whether an employee may quit the desktop tracker."""
  allowAgentAppExit: Boolean!

  """Whether an employee may sign out of the desktop tracker."""
  allowLogoutFromAgentApp: Boolean!

  """Whether the tracker records keyboard and mouse activity."""
  trackKeyboardMouseActivity: Boolean!

  """
  Whether the tracker follows every display rather than only the primary one.
  """
  trackAllDisplays: Boolean!

  """
  The organization's contact row, by identifier. The contact record itself is a row of the contact
  domain and the delivered reads do not join it, so the identifier is carried rather than a member
  that would answer null; the row is read from the contact surface.
  """
  contactId: ID

  """
  The tenant the organization belongs to, by identifier. The row is read from the tenant surface.
  """
  tenantId: ID

  """
  When the organization was withdrawn, or null while it is live. Withdrawing and restoring are
  delivered routes — the two removals this resource serves — and this column is the whole of what they
  write, so without it a caller could not tell a live row from a withdrawn one on the answer to the
  write that withdrew it.
  """
  deletedAt: DateTime

  """When the organization was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """When the organization was filed."""
  createdAt: DateTime

  """When the organization was last changed."""
  updatedAt: DateTime

  """
  Whether the organization is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the organization is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean

  """
  The person who filed the organization, by identifier. The user row is not joined by this surface's read.
  """
  createdByUserId: ID

  """
  The person who last changed the organization, by identifier. The user row is not joined by this surface's read.
  """
  updatedByUserId: ID

  """
  The person who withdrew the organization, by identifier. The user row is not joined by this surface's read.
  """
  deletedByUserId: ID
}

"""The boundary of one page of organizations."""
type OrganizationEdge {
  node: Organization!
  cursor: String!
}

"""
One page of organizations, with the total the filters select and the boundary cursors.
"""
type OrganizationConnection {
  nodes: [Organization!]!
  edges: [OrganizationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an organization list may be sorted by."""
enum OrganizationSortField {
  createdAt
  updatedAt
  name
  currency
  isDefault
}

"""
The order the organizations are returned in. At most three keys are accepted.
"""
input OrganizationSort {
  field: OrganizationSortField!
  direction: SortDirection!
}

"""
The fields an organization list may be narrowed by.

`name`, `currency`, `regionCode` and the settings beside them are the columns a client narrows a
picker by; the identifier is here so a caller can ask for a known set in one request. The relations
are not: the delivered read joins none of them here, so a filter on one would be evaluated against a
row that carries none and would select nothing at all.
"""
input OrganizationFilter {
  id: IDFilter
  name: StringFilter
  currency: StringFilter
  isDefault: BooleanFilter
  regionCode: StringFilter
  timeZone: StringFilter
  officialName: StringFilter
  taxId: StringFilter
  website: StringFilter
  contactId: IDFilter
  bonusType: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  and: [OrganizationFilter!]
  or: [OrganizationFilter!]
  not: OrganizationFilter
}

"""
The contact row the delivered create files beside the organization, as `IContact` declares it.

The delivered create reads this member, stores it as a contact of the caller's organization and links
it back onto the row; a caller that states none gets no contact row. The members are the contact
domain's own columns, and the row it produces is read from `Contact`.
"""
input OrganizationContactDetailInput {
  """
  The contact's display name, when the person or place is addressed as one.
  """
  name: String
  firstName: String
  lastName: String
  country: String
  city: String
  address: String
  address2: String
  postcode: String
  regionCode: String
  fax: String
  fiscalInformation: String
  website: String
}

"""
What a caller states when it files an organization.

The members are the ones the delivered create body validates and the delivered handler reads. Three
groups the body accepts are deliberately absent:

- the tenant is never stated, because a write's tenant comes from the credential;
- `tagIds` is not declared. The delivered body validates it and the handler then drops it: the row's
  `tags` is a pivot the create does not cascade, so a caller that sent it would be told nothing and
  the tags would not be written. A member that is accepted and silently discarded is worse than one
  that is not offered — tags are attached from the tag surface, which owns the pivot;
- the `show_*` visibility flags belong to the edit below, not here: the delivered create body does not
  validate them.
"""
input CreateOrganizationInput {
  name: String!

  """
  The currency the organization's amounts are in, as an ISO 4217 code. The delivered create requires it.
  """
  currency: String!

  """The logo asset to file the organization with, by identifier."""
  imageId: ID

  """The standard number of hours in a working day, between 1 and 24."""
  standardWorkHoursPerDay: Int

  """
  The organization's identifier in an external system of record, when it is linked to one.
  """
  upworkOrganizationId: String

  """The organization's name in that external system."""
  upworkOrganizationName: String

  """
  Which date a new document defaults to. The vocabulary is the platform's `DefaultValueDateTypeEnum`.
  """
  defaultValueDateType: String

  """
  Which day the organization's week starts on. The vocabulary is the platform's `WeekDaysEnum`.
  """
  startWeekOn: String

  """How many days an invitation stays valid for."""
  inviteExpiryPeriod: Int

  """
  The region code every address in the organization resolves against when it names none.
  """
  regionCode: String

  """The bonus rate as a percentage between 0 and 100."""
  bonusPercentage: Float

  """
  Which bonus scheme applies. The vocabulary is the platform's `BonusTypeEnum`.
  """
  bonusType: String

  """The contact row to file beside the organization."""
  contact: OrganizationContactDetailInput
}

"""
What a caller states when it edits an organization that exists.

**A member of the `show_*` family that is absent is written as `false`, not left as it is.** The
delivered handler reads each of them through a truth test rather than an absence test —
`show_profits: !!input.show_profits` — so an edit that states none of them turns every one of them
off. The rest of the members are written from the body as stated, and the handler reads the row before
it writes, so an organization that is not the caller's is a miss rather than an upsert under an
identifier the caller does not own.

The identifier is the only member stated outside the body, and it is stated because the delivered
route carries it in the path: the body itself is the same body the create validates, plus the
visibility flags.
"""
input UpdateOrganizationInput {
  id: ID!
  name: String!
  currency: String!
  imageId: ID
  standardWorkHoursPerDay: Int
  upworkOrganizationId: String
  upworkOrganizationName: String
  defaultValueDateType: String
  startWeekOn: String
  inviteExpiryPeriod: Int
  regionCode: String
  bonusPercentage: Float
  bonusType: String
  contact: OrganizationContactDetailInput

  """Whether the public profile shows income."""
  show_income: Boolean

  """Whether the public profile shows profits."""
  show_profits: Boolean

  """Whether the public profile shows bonuses paid."""
  show_bonuses_paid: Boolean

  """Whether the public profile shows total hours."""
  show_total_hours: Boolean

  """Whether the public profile shows the minimum project size."""
  show_minimum_project_size: Boolean

  """
  Whether the public profile shows how many projects the organization has.
  """
  show_projects_count: Boolean

  """
  Whether the public profile shows how many clients the organization has.
  """
  show_clients_count: Boolean

  """Whether the public profile shows its clients."""
  show_clients: Boolean

  """
  Whether the public profile shows how many people work in the organization.
  """
  show_employees_count: Boolean
}

type PaymentTerm {
  id: ID!
  organizationId: ID
  name: String!
  code: String!
  description: String
  isDefault: Boolean!
  lines: [PaymentTermLine!]
}

type PaymentTermLine {
  id: ID!
  paymentTermId: ID!
  sequence: Int!
  valueType: PaymentTermLineType!
  valueAmount: String!
  currency: String
  dueBasis: PaymentDueBasis!
  days: Int!
  dayOfMonth: Int
}

enum PaymentTermLineType {
  PERCENT
  FIXED
}

enum PaymentDueBasis {
  INVOICE_DATE
  END_OF_MONTH
  END_OF_NEXT_MONTH
  DAY_OF_NEXT_MONTH
}

type PaymentTermInstalment {
  sequence: Int!
  sequences: [Int!]!
  dueDate: String!
  amount: String!
  currency: String!
  valueType: PaymentTermLineType!
  percentage: String
  dueBasis: PaymentDueBasis!
  days: Int!
  dayOfMonth: Int
}

type PaymentTermSchedule {
  paymentTermId: ID!
  code: String!
  name: String!
  total: String!
  currency: String!
  currencyDecimals: Int!
  basisDate: String!
  instalments: [PaymentTermInstalment!]!
  allocatedTotal: String!
  dueDate: String!
}

input PaymentTermLineInput {
  sequence: Int
  valueType: PaymentTermLineType
  valueAmount: String!
  currency: String
  dueBasis: PaymentDueBasis
  days: Int
  dayOfMonth: Int
}

input CreatePaymentTermInput {
  organizationId: ID!
  name: String!
  code: String!
  description: String
  isDefault: Boolean
  lines: [PaymentTermLineInput!]!
}

input UpdatePaymentTermInput {
  id: ID!
  organizationId: ID!
  name: String
  code: String
  description: String
  isDefault: Boolean
}

input UpdatePaymentTermLinesInput {
  id: ID!
  organizationId: ID!
  lines: [PaymentTermLineInput!]!
}

"""
One money movement: what was paid, in which currency, against which document.
"""
type Payment {
  id: ID!

  """
  The date the money moved. Null on a row written without one, which is a row no ledger order can place.
  """
  paymentDate: DateTime

  """The amount that moved, in the row's own `currency`."""
  amount: Decimal

  """
  The note kept with the movement, for the operator rather than for the payer.
  """
  note: String

  """
  The ISO 4217 code `amount` and the four lifecycle amounts below are expressed in.
  """
  currency: String!

  """
  How the money moved, as the contracts' own value: `BANK_TRANSFER`, `CASH`, `CHEQUE`,
  `CREDIT_CARD`, `DEBIT` or `ONLINE`.
  """
  paymentMethod: String

  """Whether the movement is past its due date."""
  overdue: Boolean

  """
  The order this payment settles; null on a payment recorded against an invoice, an expense or a project instead.
  """
  orderId: ID

  """The collection this payment belongs to, when it came from a checkout."""
  paymentCollectionId: ID

  """The session that produced it."""
  paymentSessionId: ID

  """The provider that processed it; null on a manual or offline payment."""
  paymentProviderId: ID

  """
  Where the movement stands, as the kernel extension's own value — `CAPTURED` on an ordinary
  recorded payment, and the other states the capture, cancellation and refund rows move it through.
  It is derived from the amounts below and is never written by a caller, so it is not a member of any
  write input on this surface.
  """
  status: String!

  """
  The provider's charge id. With `paymentProviderId` it is the key a repeated provider callback
  collides on, which is what stops a duplicate callback from recording a second payment.
  """
  externalId: String

  """The human reference printed on the statement or handed to the payer."""
  reference: String

  """
  The amount the provider authorised; null on a manual payment, which has no authorisation step.
  """
  authorizedAmount: Decimal

  """
  The amount actually captured. On a provider-backed payment the delivered service keeps it equal to
  `amount` once the row reaches `CAPTURED`, so the accounting figure and the provider's cannot
  disagree.
  """
  capturedAmount: Decimal!

  """The amount refunded against this payment."""
  refundedAmount: Decimal!

  """The amount of the authorisation released without capture."""
  canceledAmount: Decimal!

  """
  The currency the provider settles this payment in; null means settlement is in the payment's own
  currency, which is the ordinary case.
  """
  settlementCurrency: String

  """
  The amount settled, in `settlementCurrency`; null while `settlementCurrency` is null.
  """
  settlementAmount: Decimal

  """
  The rate applied at settlement: one unit of the payment's currency equals `fxRate` units of
  `settlementCurrency`. It is a rate rather than an amount, and it is carried in the same exact family
  because the column is `numeric(20,10)`: a binary fraction could not hold the scale it is stored at.
  It is a snapshot, never re-derived from the exchange-rate table, so a later rate change cannot
  rewrite what was settled.
  """
  fxRate: Decimal

  """
  The exchange-rate row the rate was read from, when it came from one; null for an identity
  conversion and for a rate a provider quoted.
  """
  fxRateId: ID

  """The instant the rate was taken, which is the instant the lookups ran."""
  fxCapturedAt: DateTime

  """When the authorisation was taken."""
  authorizedAt: DateTime

  """When the capture settled."""
  capturedAt: DateTime

  """When the authorisation was released."""
  canceledAt: DateTime

  """
  Provider response fragments, card brand and last four, reconciliation notes.
  """
  metadata: JSON

  """
  The employee the movement is recorded against, as the identifier of the employee's own row.
  """
  employeeId: ID

  """
  The invoice the movement is applied against, as the identifier of the document's own row.
  """
  invoiceId: ID

  """
  The project the movement belongs to, as the identifier of the project's own row.
  """
  projectId: ID

  """
  The contact the movement is recorded against, as the identifier of the contact's own row.
  """
  organizationContactId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write, so
  without it a caller could not tell a live row from a withdrawn one on the answer to the write that
  withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of payments."""
type PaymentEdge {
  node: Payment!
  cursor: String!
}

"""
One page of payments, with the total the filters select and the boundary cursors.
"""
type PaymentConnection {
  nodes: [Payment!]!
  edges: [PaymentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a payment list may be sorted by.

The two timestamps are how a change is found again; `paymentDate` is the ledger's own order, which is
the order a statement of movements is read in; `amount` is what a movement list is taken by; and
`status` is offered because a work list is taken in status groups — the values sort as their spelling,
which groups like with like.

The flags and the identifiers are not sort keys: a boolean or an identifier order is not an order a
ledger is read in, and the questions those columns answer are filters. The amounts other than `amount`
are filtered rather than sorted for the same reason — a refund total is a question about one row, not
an order for a page of them.
"""
enum PaymentSortField {
  createdAt
  updatedAt
  paymentDate
  amount
  status
}

"""
The order the payments are returned in. At most three keys are accepted.
"""
input PaymentSort {
  field: PaymentSortField!
  direction: SortDirection!
}

"""
The fields a payment list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is: the delivered list read
answers those columns, so each one narrows rows the connection was handed. The amounts are stated
through the decimal family, which is the family money is compared in; **the values are the row's own**,
so an equality states the amount as the read produces it (`100.5`, not a padded string), while the
range operators — which are what a ledger is actually searched with — need no such care.

`deletedAt` is deliberately absent although the object carries it: the delivered list read answers live
rows only, so a condition on that column could only ever match the empty set, and an argument that
cannot be honoured is worse than an argument that is not offered. The tenant and the organization are
absent for a different reason — both are applied to the criterion from the credential rather than from
the caller, so a member here would suggest a scope decision the caller does not have.
"""
input PaymentFilter {
  id: IDFilter
  paymentDate: DateTimeFilter
  amount: DecimalFilter
  note: StringFilter
  currency: StringFilter
  paymentMethod: StringFilter
  overdue: BooleanFilter
  orderId: IDFilter
  paymentCollectionId: IDFilter
  paymentSessionId: IDFilter
  paymentProviderId: IDFilter
  status: StringFilter
  externalId: StringFilter
  reference: StringFilter
  authorizedAmount: DecimalFilter
  capturedAmount: DecimalFilter
  refundedAmount: DecimalFilter
  canceledAmount: DecimalFilter
  settlementCurrency: StringFilter
  settlementAmount: DecimalFilter
  fxRate: DecimalFilter
  fxRateId: IDFilter
  fxCapturedAt: DateTimeFilter
  authorizedAt: DateTimeFilter
  capturedAt: DateTimeFilter
  canceledAt: DateTimeFilter
  metadata: JSONFilter
  employeeId: IDFilter
  invoiceId: IDFilter
  projectId: IDFilter
  organizationContactId: IDFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [PaymentFilter!]
  or: [PaymentFilter!]
  not: PaymentFilter
}

"""
What a caller states when it records a money movement.

`amount` is stated as the exact decimal and is the one member the delivered body validation requires
beside the organization: the column is `numeric`, and a JSON number — which is what the REST body
carries the same quantity as — is a binary fraction that cannot hold a cent.

The organization is stated as an identifier and is *not* required, because the delivered validation
requires an organization only when the body names neither the organization object nor a `sentTo` — and
the object spelling is a type this schema does not declare, so the identifier is what this surface
offers and the condition stays the delivered validator's rather than being restated as a non-null
member that would refuse a body the route serves.
"""
input CreatePaymentInput {
  """The amount that moved, in the currency below."""
  amount: Decimal!

  """The ISO 4217 code the amount is expressed in."""
  currency: String

  """The date the money moved."""
  paymentDate: DateTime

  """How the money moved."""
  paymentMethod: String

  """Whether the movement is past its due date."""
  overdue: Boolean

  """The note kept with the movement."""
  note: String

  """The organization the movement is filed under."""
  organizationId: ID

  """The employee the movement is recorded against."""
  employeeId: ID

  """The invoice the movement is applied against."""
  invoiceId: ID

  """The project the movement belongs to."""
  projectId: ID

  """The contact the movement is recorded against."""
  organizationContactId: ID

  """
  The facets attached to the movement, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
What a caller states when it changes a movement that exists.

**The delivered edit is the platform's own upsert, and it is reached through the same write the create
is.** The route reads the row's identifier from the path, spreads the body beside it and calls the
delivered create, so a member the caller leaves out is left as it is — the write touches the members
the body states and no others — and two consequences follow from that same fact and are worth stating
plainly:

- An identifier that names a row of another tenant is refused outright, which is the ownership guard
  the delivered write applies before it persists anything.
- An identifier that names no row at all is **stored as a new row** rather than refused, because an
  upsert is what the delivered write is. A client that means "change this movement" reads it first, or
  reaches for the mutation above only with an identifier it obtained from this surface.

`employeeId` is not a member, because the delivered edit body does not carry it — the delivered DTO
drops that member on the edit and keeps it on the create.
"""
input UpdatePaymentInput {
  id: ID!

  """The amount that moved, in the currency below."""
  amount: Decimal!

  """The ISO 4217 code the amount is expressed in."""
  currency: String

  """The date the money moved."""
  paymentDate: DateTime

  """How the money moved."""
  paymentMethod: String

  """Whether the movement is past its due date."""
  overdue: Boolean

  """The note kept with the movement."""
  note: String

  """The organization the movement is filed under."""
  organizationId: ID

  """The invoice the movement is applied against."""
  invoiceId: ID

  """The project the movement belongs to."""
  projectId: ID

  """The contact the movement is recorded against."""
  organizationContactId: ID

  """
  The facets attached to the movement, by identifier. The pivot is written from these.
  """
  tagIds: [ID!]
}

"""
The recipient of a receipt, as the delivered dispatch reads the recipient off the invoice.

Two members, and both are read: the address the message is sent to and the name it is addressed to.
"""
input PaymentReceiptContactInput {
  """The address the receipt is sent to."""
  primaryEmail: String!

  """The name the receipt addresses."""
  name: String!
}

"""
The organization a receipt is issued by, as the delivered dispatch reads it.

The three members are the three the delivered path reads off the posted organization: the two the
message prints, and the tenant the delivery record the dispatch writes behind the receipt is filed
under.
"""
input PaymentReceiptOrganizationInput {
  """
  The organization's own identifier, which the delivery record is filed against.
  """
  id: ID!

  """The organization's name, which the receipt prints."""
  name: String!

  """The tenant the delivery record is filed under."""
  tenantId: ID
}

"""
What a receipt states about the document it settles.

The number is carried in the same exact family the document's own numbering member is carried in, so
the number a client read off the invoice is the number it states here without a conversion in between.
"""
input PaymentReceiptInvoiceInput {
  """The number of the document the receipt is for."""
  invoiceNumber: Decimal!

  """The recipient the receipt is addressed to."""
  toContact: PaymentReceiptContactInput!

  """The organization the receipt is issued by."""
  fromOrganization: PaymentReceiptOrganizationInput!
}

"""
What a receipt states about the money it acknowledges.

The amount is the exact decimal of the movement: it is the figure the receipt prints, and a receipt
that prints a rounded figure acknowledges an amount that was never paid.
"""
input PaymentReceiptPaymentInput {
  """The amount the receipt acknowledges."""
  amount: Decimal!

  """The ISO 4217 code the amount is printed in."""
  currency: String!
}

"""
What a caller states when it sends a receipt for one payment.

The delivered route reads two facts from the request's own headers rather than from its body — the
language the message is written in and the origin the link it carries is built from — and this surface
reads the same two facts off the same request, so a receipt sent over either protocol is the same
message. Nothing here states them, because a caller cannot state one origin over one protocol and a
different one over the other.
"""
input SendPaymentReceiptInput {
  """The document the receipt is for."""
  invoice: PaymentReceiptInvoiceInput!

  """The movement the receipt acknowledges."""
  payment: PaymentReceiptPaymentInput!
}

"""
One payroll run: a single pay period for an organization, with the three totals derived from its lines.
"""
type PayrollRun {
  id: ID!

  """The first day of the period this run pays for."""
  periodStart: DateTime!

  """The last day of the period this run pays for."""
  periodEnd: DateTime!

  """The day the pay is dated for."""
  payDate: DateTime!

  """
  How often a run of this kind recurs: `WEEKLY`, `BI_WEEKLY`, `SEMI_MONTHLY`, `MONTHLY`, `QUARTERLY`
  or `ANNUALLY`.
  """
  frequency: String!

  """
  Where the run stands: `DRAFT`, `PENDING_APPROVAL`, `APPROVED`, `PROCESSING`, `PAID` or `CANCELLED`.
  The moves between them are the delivered workflow routes and nothing else: no input on this surface
  accepts a status.
  """
  status: String!

  """
  The ISO 4217 code of the currency every amount on this run is denominated in.
  """
  currency: String!

  """
  The sum of the earning lines, recomputed in integer cents by the delivered write paths.
  """
  totalGross: Decimal!

  """
  The sum of the deduction lines, recomputed in integer cents by the delivered write paths.
  """
  totalDeductions: Decimal!

  """
  What is left after the deductions — `totalGross` less `totalDeductions`, on the same terms.
  """
  totalNet: Decimal!

  """Whatever the operator who opened the run wrote on it."""
  notes: String

  """When the run was approved."""
  approvedAt: DateTime

  """
  The user who approved the run. A column, because no delivered read joins the relation.
  """
  approvedByUserId: ID

  """When the run was marked paid."""
  paidAt: DateTime

  """
  The earning and deduction lines that make up the run. Answered on a run read by its identifier —
  the delivered node read loads them — and null on a run read out of the list, which does not.
  """
  items: [PayrollItem!]

  """
  The organization the run belongs to. Payroll is per organization, and every route names one.
  """
  organizationId: ID

  """The tenant the run belongs to, stamped from the credential."""
  tenantId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""One earning or deduction line of a payroll run."""
type PayrollItem {
  id: ID!

  """
  What the line represents: `BASIC_SALARY`, `ALLOWANCE`, `BONUS`, `COMMISSION`, `OVERTIME`,
  `TAX_DEDUCTION`, `SOCIAL_SECURITY`, `HEALTH_INSURANCE`, `LOAN_DEDUCTION`, `ADVANCE_DEDUCTION`,
  `LEAVE_DEDUCTION`, `OTHER_ADDITION` or `OTHER_DEDUCTION`.
  """
  type: String!

  """
  Whether the line adds to or subtracts from net pay: `EARNING` or `DEDUCTION`.
  """
  category: String!

  """What the line is for, in the operator's own words."""
  description: String

  """
  Always a positive amount — `category` decides the sign, never the value.
  """
  amount: Decimal!

  """
  How many units the line covers, when it is a rate rather than a flat amount.
  """
  quantity: Decimal

  """
  The rate one unit is paid at, when the line is a rate rather than a flat amount.
  """
  unitPrice: Decimal

  """Whether the amount is subject to tax. Lines default to taxable."""
  taxable: Boolean!

  """
  The run the line belongs to. A column, because the route that answers a line loads no relation.
  """
  payrollRunId: ID!

  """
  The employee the line is paid to. A column, for the same reason — and nullable, because a line outlives the employee record it was paid to.
  """
  employeeId: ID

  """The organization the line belongs to."""
  organizationId: ID

  """The tenant the line belongs to."""
  tenantId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""
Total pay across every paid run of one organization, grouped by currency.

Runs are grouped rather than summed together because adding amounts denominated in different currencies
produces a number that means nothing. The type is the delivered answer's own members and no more: the
service sums in integer cents and strips its accumulators before it answers, so a member for the cents or
for the working set of employees it counted would answer an internal of the calculation rather than a
fact about the payroll — and it would be absent on every row a caller could reach.
"""
type PayrollStatistics {
  """How many paid runs the totals cover."""
  totalRuns: Int!

  """How many distinct employees those runs paid."""
  totalEmployeesPaid: Int!

  """The sum of the gross totals of every paid run in this currency."""
  totalGrossPaid: Decimal!

  """The sum of the deduction totals of every paid run in this currency."""
  totalDeductions: Decimal!

  """The sum of the net totals of every paid run in this currency."""
  totalNetPaid: Decimal!

  """The currency every amount above is denominated in."""
  currency: String!
}

"""
What one employee earned, was deducted and takes home in one payroll run.

The delivered breakdown computes this from the run's lines, in integer cents, and strips its
accumulators before it answers — so a member for the cents would answer an internal of the calculation.
`employee` is not carried either: the run's lines are loaded without the employee relation, so the
member the delivered breakdown copies onto its answer is absent on every row this surface could reach.
`employeeId` is the fact the row holds, and the employee it names is reachable from the employee domain.
"""
type PayrollSummary {
  """The employee this summary is about."""
  employeeId: ID!

  """The first day of the period the run paid for."""
  periodStart: DateTime!

  """The last day of the period the run paid for."""
  periodEnd: DateTime!

  """The sum of this employee's earning lines."""
  grossPay: Decimal!

  """The sum of this employee's deduction lines."""
  totalDeductions: Decimal!

  """What the employee takes home — `grossPay` less `totalDeductions`."""
  netPay: Decimal!

  """The currency every amount above is denominated in, taken from the run."""
  currency: String!
}

"""The boundary of one page of payroll runs."""
type PayrollRunEdge {
  node: PayrollRun!
  cursor: String!
}

"""
One page of payroll runs, with the total the filters select and the boundary cursors.
"""
type PayrollRunConnection {
  nodes: [PayrollRun!]!
  edges: [PayrollRunEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a payroll run list may be sorted by."""
enum PayrollRunSortField {
  periodStart
  periodEnd
  payDate
  createdAt
  updatedAt
  status
  currency
}

"""
The order the payroll run list is returned in. At most three keys are accepted.
"""
input PayrollRunSort {
  field: PayrollRunSortField!
  direction: SortDirection!
}

"""
The fields a payroll run list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

The organization is not filterable here: it is a required argument of the connection, because the
delivered read refuses to answer without one — payroll is per organization, and a read that named none
would answer every organization of the tenant. `status` and `frequency` are stated through the string
family, and the period window through `periodStart: { between: [...] }`, which is the range the delivered
read applies to that column and no other.
"""
input PayrollRunFilter {
  id: IDFilter
  status: StringFilter
  frequency: StringFilter
  currency: StringFilter
  periodStart: DateTimeFilter
  periodEnd: DateTimeFilter
  payDate: DateTimeFilter
  notes: StringFilter
  approvedByUserId: IDFilter
  totalGross: DecimalFilter
  totalDeductions: DecimalFilter
  totalNet: DecimalFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [PayrollRunFilter!]
  or: [PayrollRunFilter!]
  not: PayrollRunFilter
}

"""
What a caller states when it opens a run.

`status` and the three totals are members of no input: the status only moves through the workflow
operations below, and the totals are derived from the lines. The delivered body refuses both for the same
reason, which is what keeps a caller from marking a run paid with a plain write. The organization is
stated because the delivered body requires it and checks it against the caller's own memberships.
"""
input CreatePayrollRunInput {
  """
  The organization the run belongs to. Checked against the caller's own memberships.
  """
  organizationId: ID!

  """The first day of the period the run pays for."""
  periodStart: DateTime!

  """The last day of the period the run pays for."""
  periodEnd: DateTime!

  """The day the pay is dated for. It cannot fall before the period starts."""
  payDate: DateTime!

  """
  How often a run of this kind recurs. The delivered vocabulary is the platform's own.
  """
  frequency: String!

  """
  The ISO 4217 code of the currency the run is denominated in — exactly three characters.
  """
  currency: String!

  """Whatever the operator wants written on the run."""
  notes: String
}

"""
What a caller states when it edits a run that has not been paid.

Every member but the identifier is optional, and a member the caller leaves out is left as it is: the
delivered edit reads the run, assigns the stated members onto it and saves it. The organization is
required although it is not written — it is the scope the run is read under, so a caller must name the
organization whose run it is changing, and the delivered route states it as a query parameter for exactly
that reason. A run that has been paid cannot be edited at all, which the service refuses.
"""
input UpdatePayrollRunInput {
  id: ID!

  """
  The organization the run belongs to. It scopes the read and is not written onto the row.
  """
  organizationId: ID!
  periodStart: DateTime
  periodEnd: DateTime
  payDate: DateTime
  frequency: String
  currency: String
  notes: String
}

"""
What a caller states when it adds one earning or deduction line to a draft run.

The run is not a member: it is the identifier in the path of the delivered route, so a caller cannot post
a line into another run by naming one here. The employee must belong to the same organization as the run,
which the service checks rather than trusting the body. Totals are not members — they are recomputed from
the lines, in integer cents, by the write itself.
"""
input CreatePayrollItemInput {
  """
  The organization the run belongs to. Checked against the caller's own memberships.
  """
  organizationId: ID!

  """
  The employee the line is paid to. Must be an employee of the same organization.
  """
  employeeId: ID!

  """
  What the line represents. The delivered vocabulary is the platform's own.
  """
  type: String!

  """Whether the line adds to or subtracts from net pay."""
  category: String!

  """What the line is for."""
  description: String

  """Always a positive amount; the category decides the sign."""
  amount: Decimal!

  """How many units the line covers, when it is a rate."""
  quantity: Decimal

  """The rate one unit is paid at, when it is a rate."""
  unitPrice: Decimal

  """
  Whether the amount is subject to tax. Omit for the delivered default, which is taxable.
  """
  taxable: Boolean
}

"""An ordered set of stages a deal moves through."""
type Pipeline {
  id: ID!

  """The name of the pipeline, as entered."""
  name: String!

  """What the pipeline is for. Null on a pipeline that states none."""
  description: String
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the pipeline was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of pipelines."""
type PipelineEdge {
  node: Pipeline!
  cursor: String!
}

"""
One page of pipelines, with the total the filters select and the boundary cursors.
"""
type PipelineConnection {
  nodes: [Pipeline!]!
  edges: [PipelineEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a pipeline list may be sorted by."""
enum PipelineSortField {
  createdAt
  updatedAt
  name
}

"""
The order the pipelines are returned in. At most three keys are accepted.
"""
input PipelineSort {
  field: PipelineSortField!
  direction: SortDirection!
}

"""
The fields a pipeline list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers. `deletedAt` is filterable so a
caller can ask the one question the list read cannot — which rows have been withdrawn — while the
read itself answers live rows only.
"""
input PipelineFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  description: StringFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [PipelineFilter!]
  or: [PipelineFilter!]
  not: PipelineFilter
}

"""
One stage of a pipeline, as a write states it.

The stages of a pipeline are not a column of the pipeline row; they are rows of their own that carry
`pipelineId`, and a write replaces the set rather than editing a member of it. The parent of each
stage is the pipeline the write is made on, so a stage states the pipeline neither.

`id` is what tells an update which of the pipeline's existing stages is being kept: a stage that
states one is saved, a stage that states none is created, and an existing stage the body does not
name is removed. That is the delivered writer's own diff and the sharpest edge of this resource's
edit — see `UpdatePipelineInput`.

`index` is deliberately not a member. The delivered writer assigns it from the position of the stage
in the array it is handed, before it saves anything, so an `index` a caller states is accepted and
replaced. A member whose value a write always overwrites is worse stated than not.
"""
input PipelineStageInput {
  """The stage to keep, on an edit. Omit to create a new stage."""
  id: ID

  """The name of the stage, as it appears on the board."""
  name: String!

  """What the stage means, when the board needs to say."""
  description: String
}

"""
What a caller states when it files a pipeline, with the stages it starts with.

The organization is required because the delivered create validates it: `POST /pipelines` binds a DTO
that intersects the organization base. There is no member for the tenant, because the service stamps
the caller's own tenant onto the row and overwrites whatever a body states.

A pipeline with no `stages` is filed with none: the delivered writer cascades the array it is given
and there is no default stage set behind it.
"""
input CreatePipelineInput {
  organizationId: ID!
  name: String!
  description: String

  """The stages to file the pipeline with, in board order."""
  stages: [PipelineStageInput!]
  isActive: Boolean
  isArchived: Boolean
}

"""
What a caller states when it changes a pipeline that exists.

**A member the caller leaves out is left as it is on the pipeline row, and `stages` is the exception
that makes this input worth reading twice.** The delivered writer keeps the stages the body names,
saves them, creates the ones that state no identifier, and **removes every existing stage the body
does not name**. A body that omits `stages` altogether therefore empties the pipeline: the stage rows
are diffed against an absent array and every one of them is removed. Stating this here is what keeps
a client from reading "absent" as "unchanged".

The edit runs in one transaction and hands the stages their `pipelineId` and their `index` itself, in
the order the array states.
"""
input UpdatePipelineInput {
  id: ID!
  organizationId: ID
  name: String
  description: String
  stages: [PipelineStageInput!]
  isActive: Boolean
  isArchived: Boolean
}

"""One node of the product taxonomy."""
type ProductCategory {
  id: ID!

  """
  The category's name in the requested language, merged from its translation.
  """
  name: String

  """
  The category's description in the requested language, merged from its translation.
  """
  description: String

  """
  A denormalised thumbnail URL, kept for lists that must not join the asset table.
  """
  imageUrl: String

  """The asset the navigation shows for the category."""
  imageId: ID
  image: ImageAsset

  """
  The parent category. Null on a root, which is every category that has no parent.
  """
  parentId: ID

  """
  The URL-safe identity of the category, unique within the organization among the live rows.
  """
  slug: String

  """The ordering of this category among its siblings."""
  sortOrder: Int!

  """Whether the category belongs to the featured navigation."""
  isFeatured: Boolean!

  """
  The lifecycle of the category, as the kernel extension's own value: `DRAFT`, `ACTIVE` or
  `ARCHIVED`. The value set is deliberately the same one the product's own lifecycle column uses, so
  one navigation filter covers both.
  """
  status: String!

  """
  When the category was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime

  """
  Tenant extras, for example a navigation icon set or an external taxonomy id.
  """
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of categories."""
type ProductCategoryEdge {
  node: ProductCategory!
  cursor: String!
}

"""
One page of categories, with the total the filters select and the boundary cursors.
"""
type ProductCategoryConnection {
  nodes: [ProductCategory!]!
  edges: [ProductCategoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a category list may be sorted by."""
enum ProductCategorySortField {
  createdAt
  updatedAt
  name
  slug
  sortOrder
  status
  isFeatured
}

"""
The order the categories are returned in. At most three keys are accepted.
"""
input ProductCategorySort {
  field: ProductCategorySortField!
  direction: SortDirection!
}

"""
The fields a category list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`name` and `description` are filterable because the delivered reader merges them onto the row before
the connection sees it: narrowing a storefront's taxonomy by the name in one language is the read the
merged member exists for. A row for which no translation matched carries neither, which is what
`isNull` states and what an `eq` never matches.
"""
input ProductCategoryFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  imageUrl: StringFilter
  imageId: IDFilter
  parentId: IDFilter
  slug: StringFilter
  sortOrder: NumberFilter
  isFeatured: BooleanFilter
  status: StringFilter
  metadata: JSONFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductCategoryFilter!]
  or: [ProductCategoryFilter!]
  not: ProductCategoryFilter
}

"""
One language's copy of a category's descriptive facts.

The rows are the category's own: a translation carries the category it belongs to, so the write
states the language and the text, never which category — the parent of the write is the category the
call is made on.
"""
input ProductCategoryTranslationInput {
  """The language this text is written in."""
  languageCode: String!
  name: String!
  description: String
}

"""
What a caller states when it files a category.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant,
for the same reason there is none on any other write here.

The category's product list is not a member either. The relation is not a column of this row and the
delivered write does not cascade it, so a body stating it would be accepted and then dropped; a
product is filed under a category by writing the product's own `productCategoryId`.
"""
input CreateProductCategoryInput {
  organizationId: ID!

  """
  The text of the category, one member per language. A category with none has no name yet.
  """
  translations: [ProductCategoryTranslationInput!]

  """The asset to show for the category, by identifier."""
  imageId: ID
  imageUrl: String

  """The parent to file the category under. Omit for a root."""
  parentId: ID
  slug: String
  sortOrder: Int
  isFeatured: Boolean
  status: String
  metadata: JSON
}

"""
What a caller states when it edits a category that exists.

**A member that is absent is left as it is.** The service reads the category to prove it is the
caller's and then updates that row where it stands — it is never removed and re-inserted, which is
what keeps the categories below it attached to it. A member stated as `null` clears it: `parentId:
null` makes the category a root. `translations`, when stated, replaces the category's languages as a
set, so an empty list removes them all; when omitted, they are kept. `organizationId` identifies the
caller's organization and does not move the category — a category stays in the organization whose
tree it belongs to.
"""
input UpdateProductCategoryInput {
  id: ID!
  organizationId: ID!
  translations: [ProductCategoryTranslationInput!]
  imageId: ID
  imageUrl: String
  parentId: ID
  slug: String
  sortOrder: Int
  isFeatured: Boolean
  status: String
  metadata: JSON
}

"""One value a product is offered in, such as a colour or a size."""
type ProductOption {
  id: ID!

  """
  The option's text, untranslated. The per-language copies are the translation rows below.
  """
  name: String!

  """
  The operator's short code for the option, when the catalogue names one.
  """
  code: String

  """
  The group this option is filed under, as the identifier every delivered row carries.
  """
  groupId: ID

  """
  The same fact as a row: the group named above, with its own product and translations.
  """
  group: ProductOptionGroup

  """
  The option's per-language text, joined with the row rather than merged onto it.
  
  Null and empty are different facts here and the two are kept apart: an option that has no
  translation answers an empty collection, and a row a write has just answered with — which has not
  been read back with its translations — answers null.
  """
  translations: [ProductOptionTranslation!]
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""One language's copy of an option's text."""
type ProductOptionTranslation {
  id: ID!

  """The option's name in this language."""
  name: String!

  """
  The option's description in this language, when the catalogue carries one.
  """
  description: String

  """
  The language this row is written in, as the platform's own language code.
  """
  languageCode: String!

  """The option this row is a translation of."""
  referenceId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""
A set of options a product offers together, such as all of its colours or all of its sizes.
"""
type ProductOptionGroup {
  id: ID!

  """The group's text, untranslated, as on the option."""
  name: String!

  """The product this group belongs to."""
  productId: ID
  product: Product

  """The options filed under this group."""
  options: [ProductOption!]

  """
  The group's per-language text, joined with the row. Null on a row that was not read with it.
  """
  translations: [ProductOptionGroupTranslation!]
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""One language's copy of an option group's name."""
type ProductOptionGroupTranslation {
  id: ID!

  """The group's name in this language."""
  name: String!

  """
  The language this row is written in, as the platform's own language code.
  """
  languageCode: String!

  """The group this row is a translation of."""
  referenceId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of options."""
type ProductOptionEdge {
  node: ProductOption!
  cursor: String!
}

"""
One page of options, with the total the filters select and the boundary cursors.
"""
type ProductOptionConnection {
  nodes: [ProductOption!]!
  edges: [ProductOptionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields an option list may be sorted by.

`groupId` is not among them. A relation key narrows a list, which is what the filter is for; the
options of one group in a chosen order are that filter plus a sort key stated here.
"""
enum ProductOptionSortField {
  createdAt
  updatedAt
  name
  code
}

"""
The order the options are returned in. At most three keys are accepted.
"""
input ProductOptionSort {
  field: ProductOptionSortField!
  direction: SortDirection!
}

"""
The fields an option list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`groupId` is stated as a filter rather than served as a root field of its own: the options of one
group are these rows narrowed to that group, and a second root field for them would be a second
surface that could disagree with this one.

`name` and `code` narrow the option's own columns, never the translated text. A translation is a
collection of rows, and the query protocol compares columns: the way to read the options whose French
name is `Rouge` is to read the translations, not to state a filter this surface could not evaluate.
"""
input ProductOptionFilter {
  id: IDFilter
  name: StringFilter
  code: StringFilter
  groupId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductOptionFilter!]
  or: [ProductOptionFilter!]
  not: ProductOptionFilter
}

"""
What a caller states when it files an option.

`name` is stated rather than optional because the column is: it carries no default and cannot be
absent, so an option without one is a row the store refuses — requiring it here states that fact where
a caller can read it instead of leaving it to a driver error.

`code` and `groupId` are optional, as their columns are: an option the catalogue has no short name for
is a row, and so is one that is not filed under a group.

The tenant is not among the members. The delivered route stamps the tenant of the credential onto the
row it writes, so a caller cannot state a scope it is not acting in. The organization is optional
because the delivered route takes it from the body and nothing else supplies it.

`translations` is deliberately absent, and its absence is the honest half of this input: the rows live
in their own table with their own service methods, and the delivered create does not cascade to them.
A member here would be accepted and silently dropped, which is worse than a member that does not
exist. The option's per-language text is written by the product's own write, which is the operation
that owns the option set as a whole.
"""
input CreateProductOptionInput {
  name: String!
  code: String
  groupId: ID
  organizationId: ID
}

"""
What a caller may change on an option that exists. A member that is absent is left as it is.

That is the delivered update's own behaviour rather than a promise made here: the route writes the
members the body states and touches no others, so "absent" and "unchanged" coincide.

`groupId` stated as `null` is therefore a different statement from omitting it — it files the option
under no group — and the delivered route honours both, because the column is nullable and the write
distinguishes the two.
"""
input UpdateProductOptionInput {
  id: ID!
  name: String
  code: String
  groupId: ID
  organizationId: ID
}

"""The capability record of one variant."""
type ProductVariantSetting {
  id: ID!
  isSubscription: Boolean!
  isPurchaseAutomatically: Boolean!
  canBeSold: Boolean!
  canBePurchased: Boolean!
  canBeCharged: Boolean!
  canBeRented: Boolean!
  isEquipment: Boolean!
  trackInventory: Boolean!

  """
  The variant this row is the setting of. The foreign key is on the setting, which is the owning
  side of the pairing; the variant reaches back through it rather than carrying one of its own.
  """
  productVariantId: ID
  productVariant: ProductVariant
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of settings."""
type ProductVariantSettingEdge {
  node: ProductVariantSetting!
  cursor: String!
}

"""
One page of settings, with the total the filters select and the boundary cursors.
"""
type ProductVariantSettingConnection {
  nodes: [ProductVariantSetting!]!
  edges: [ProductVariantSettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a setting list may be sorted by.

The two timestamps and the flags. A flag is a column with two values and therefore an order — the
rows that carry it and the rows that do not — and offering a subset of a resource's own members
would be a line no caller could predict. `productVariantId` is not among them: a relation key narrows
a list, which is what the filter is for, and the order of variants is read from the variant
connection rather than from this one.
"""
enum ProductVariantSettingSortField {
  createdAt
  updatedAt
  isSubscription
  isPurchaseAutomatically
  canBeSold
  canBePurchased
  canBeCharged
  canBeRented
  isEquipment
  trackInventory
}

"""
The order the settings are returned in. At most three keys are accepted.
"""
input ProductVariantSettingSort {
  field: ProductVariantSettingSortField!
  direction: SortDirection!
}

"""
The fields a setting list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`productVariantId` is stated as a filter rather than served as a root field of its own: the setting
of one variant is one of these rows narrowed to that variant, and a second root field for it would
be a second surface that could disagree with this one.
"""
input ProductVariantSettingFilter {
  id: IDFilter
  isSubscription: BooleanFilter
  isPurchaseAutomatically: BooleanFilter
  canBeSold: BooleanFilter
  canBePurchased: BooleanFilter
  canBeCharged: BooleanFilter
  canBeRented: BooleanFilter
  isEquipment: BooleanFilter
  trackInventory: BooleanFilter
  productVariantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductVariantSettingFilter!]
  or: [ProductVariantSettingFilter!]
  not: ProductVariantSettingFilter
}

"""
What a caller states when it opens a setting.

The variant, the organization and the flags, and every one of them optional: a setting's columns all
carry a default, so a create that names nothing is the row the platform's own default-row operation
writes, and requiring a member here would refuse a caller the delivered route serves.

The tenant is not among the members. The delivered route stamps the tenant of the credential onto
the row it writes, so a caller cannot state a scope it is not acting in; the organization is among
them because the route takes it from the body and nothing else supplies it.

`id` is not among them: a create that names an existing row is the update route stated on the create
route, and a caller that means to change a row has the update field for it.
"""
input CreateProductVariantSettingInput {
  productVariantId: ID
  organizationId: ID
  isSubscription: Boolean
  isPurchaseAutomatically: Boolean
  canBeSold: Boolean
  canBePurchased: Boolean
  canBeCharged: Boolean
  canBeRented: Boolean
  isEquipment: Boolean
  trackInventory: Boolean
}

"""
What a caller may change on a setting that exists. A member that is absent is left as it is.

Every member but `id` is optional, which is the shape the delivered update route has: it writes the
members the body states and leaves the rest of the row alone.
"""
input UpdateProductVariantSettingInput {
  id: ID!
  productVariantId: ID
  organizationId: ID
  isSubscription: Boolean
  isPurchaseAutomatically: Boolean
  canBeSold: Boolean
  canBePurchased: Boolean
  canBeCharged: Boolean
  canBeRented: Boolean
  isEquipment: Boolean
  trackInventory: Boolean
}

"""One operator-facing classification of a product."""
type ProductType {
  id: ID!

  """
  The type's name in the requested language, merged from its translation.
  """
  name: String

  """
  The type's description in the requested language, merged from its translation.
  """
  description: String

  """
  The icon the back office draws beside the type, as the icon set's own name (`car-outline`). It is
  carried as its value rather than declared as a schema enum: the value set belongs to the interface
  library the console is built from and grows with it, so a closed enum here would make every new
  icon a schema change — and the delivered route validates the member against that same set.
  """
  icon: String

  """
  When the type was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of product types."""
type ProductTypeEdge {
  node: ProductType!
  cursor: String!
}

"""
One page of product types, with the total the filters select and the boundary cursors.
"""
type ProductTypeConnection {
  nodes: [ProductType!]!
  edges: [ProductTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a product type list may be sorted by."""
enum ProductTypeSortField {
  createdAt
  updatedAt
  name
  icon
}

"""
The order the product types are returned in. At most three keys are accepted.
"""
input ProductTypeSort {
  field: ProductTypeSortField!
  direction: SortDirection!
}

"""
The fields a product type list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

`name` and `description` are filterable because the delivered reader merges them onto the row before
the connection sees it: narrowing a back-office list by the name in one language is the read the
merged member exists for. A row for which no translation matched carries neither, which is what
`isNull` states and what an `eq` never matches.
"""
input ProductTypeFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  icon: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductTypeFilter!]
  or: [ProductTypeFilter!]
  not: ProductTypeFilter
}

"""
One language's copy of a type's descriptive facts.

The rows are the type's own: a translation carries the type it belongs to, so the write states the
language and the text, never which type — the parent of the write is the type the call is made on.
"""
input ProductTypeTranslationInput {
  """The language this text is written in."""
  languageCode: String!
  name: String!
  description: String
}

"""
What a caller states when it declares a type.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names and the
delivered route validates against the caller's own memberships. There is no member for the tenant, for
the same reason there is none on any other write here.

The type's product list is not a member either. The relation is not a column of this row and the
delivered write does not cascade it, so a body stating it would be accepted and then dropped; a
product is classified under a type by writing the product's own `productTypeId`.
"""
input CreateProductTypeInput {
  organizationId: ID!

  """
  The text of the type, one member per language. A type with none has no name yet.
  """
  translations: [ProductTypeTranslationInput!]
  icon: String
}

"""
What a caller states when it edits a type that exists.

The update replaces the row: the service reads the type to prove it is the caller's, removes it and
persists the stated body under the verified path identifier, so a member the caller leaves out is
written as the column's default rather than left as it is. `translations` is the one exception, and it
is the reason this paragraph exists: the delivered service carries the existing rows over when the
body omits them, because the rows are removed with their type and a caller editing the icon must not
lose every language by not restating them. An explicit empty list is a caller asking for them to be
cleared, and is honoured as such.
"""
input UpdateProductTypeInput {
  id: ID!
  organizationId: ID!
  translations: [ProductTypeTranslationInput!]
  icon: String
}

"""
What one unit of a variant costs and what it is sold for, each with the currency it is stated in.
"""
type ProductVariantPrice {
  id: ID!

  """
  The unit cost: what one unit of the variant costs the operator.
  
  The column is an integer, and the amount is carried as the platform's exact-decimal scalar because
  the family money is compared in is the exact one. Nothing is rescaled or converted on the way out:
  this is the value the column stores, in the currency named beside it.
  """
  unitCost: Decimal!

  """The ISO 4217 code of the currency `unitCost` is expressed in."""
  unitCostCurrency: String!

  """The retail price: what one unit of the variant is sold for."""
  retailPrice: Decimal!

  """The ISO 4217 code of the currency `retailPrice` is expressed in."""
  retailPriceCurrency: String!

  """
  The variant this row prices, as the identifier.
  
  Nullable, and genuinely so: the foreign key is nullable in the table and the delivered generator
  relies on that — it writes the price row first and attaches it to the variant afterwards.
  """
  productVariantId: ID

  """The variant named above, when the row was read with it."""
  productVariant: ProductVariant
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of prices."""
type ProductVariantPriceEdge {
  node: ProductVariantPrice!
  cursor: String!
}

"""
One page of prices, with the total the filters select and the boundary cursors.
"""
type ProductVariantPriceConnection {
  nodes: [ProductVariantPrice!]!
  edges: [ProductVariantPriceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a price list may be sorted by.

The two timestamps and the two amounts. A price list ordered by what a unit costs is the read the
columns exist for, and one ordered by when the row was written is how a change is found again.

The two currency codes are not sort keys: the order of a currency code is the order of its spelling,
which is not a fact about a price. A list of one currency is that currency stated as a filter.
"""
enum ProductVariantPriceSortField {
  createdAt
  updatedAt
  unitCost
  retailPrice
}

"""The order the prices are returned in. At most three keys are accepted."""
input ProductVariantPriceSort {
  field: ProductVariantPriceSortField!
  direction: SortDirection!
}

"""
The fields a price list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

The two amounts are stated through the decimal family, which is the family money is compared in: a
bound a caller writes is an exact amount and is compared as one rather than through a binary fraction.
**The columns are integers**, so an equality states the amount as the column holds it — a whole number,
which is what every row this platform writes carries; the range operators are the ones a price list is
searched with and need no such care.

`unitCostCurrency` and `retailPriceCurrency` are stated as text rather than as an enum. The vocabulary
is the platform's own currency list, which the entity types the columns with; re-declaring it here
would claim a vocabulary this domain does not own, and the two declarations would diverge the day a
currency is added.

`productVariantId` is stated as a filter rather than served as a root field of its own: the price of
one variant is one of these rows narrowed to that variant, and a second root field for it would be a
second surface that could disagree with this one.
"""
input ProductVariantPriceFilter {
  id: IDFilter
  unitCost: DecimalFilter
  unitCostCurrency: StringFilter
  retailPrice: DecimalFilter
  retailPriceCurrency: StringFilter
  productVariantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductVariantPriceFilter!]
  or: [ProductVariantPriceFilter!]
  not: ProductVariantPriceFilter
}

"""
What a caller states when it prices a variant.

Every member is optional, because every column behind them either carries a default or is nullable:
the amounts default to zero and the currencies to the platform's own, and the variant and the
organization are nullable. A create that names nothing is therefore the row of zeros in the default
currency that the delivered default-price operation writes, and requiring a member here would refuse a
caller the delivered route serves.

`productVariantId` is among the members and optional for the same reason the column is: the delivered
generator writes the price row first and attaches it to the variant afterwards, so a schema that
required the variant here would refuse the order the platform itself writes in.

The tenant is not among the members. The delivered route stamps the tenant of the credential onto the
row it writes, so a caller cannot state a scope it is not acting in.
"""
input CreateProductVariantPriceInput {
  unitCost: Decimal
  unitCostCurrency: String
  retailPrice: Decimal
  retailPriceCurrency: String
  productVariantId: ID
  organizationId: ID
}

"""
What a caller may change on a price that exists. A member that is absent is left as it is.

That is the delivered update's own behaviour rather than a promise made here: the route writes the
members the body states and touches no others, so "absent" and "unchanged" coincide. An amount stated
as the same number in a different currency is a different price and not an edit of this one — both
halves of the pair are members, so a caller that means to move a price to another currency states both.
"""
input UpdateProductVariantPriceInput {
  id: ID!
  unitCost: Decimal
  unitCostCurrency: String
  retailPrice: Decimal
  retailPriceCurrency: String
  productVariantId: ID
  organizationId: ID
}

"""One buyable configuration of a product."""
type ProductVariant {
  id: ID!

  """Free-text notes for the operator."""
  notes: String

  """How many units this variant represents, when it is sold as a bundle."""
  quantity: Int!

  """The tax rate applied to this variant, as a percentage."""
  taxes: Float!

  """
  Whether an invoice is raised when the variant is ordered or when it is fulfilled. Stored as the
  platform's own enumeration value so a client can round-trip it.
  """
  billingInvoicingPolicy: String!

  """
  The operator's own reference, used on purchase orders and in the warehouse.
  """
  internalReference: String

  """
  Whether the variant may be sold. A disabled variant is readable and cannot be ordered.
  """
  enabled: Boolean!

  """The product this variant belongs to."""
  productId: ID
  product: Product

  """The image shown for this variant, which overrides the product's cover."""
  imageId: ID
  image: ImageAsset
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of variants."""
type ProductVariantEdge {
  node: ProductVariant!
  cursor: String!
}

"""
One page of variants, with the total the filters select and the boundary cursors.
"""
type ProductVariantConnection {
  nodes: [ProductVariant!]!
  edges: [ProductVariantEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a variant list may be sorted by."""
enum ProductVariantSortField {
  createdAt
  updatedAt
  internalReference
  quantity
  enabled
}

"""
The order the variants are returned in. At most three keys are accepted.
"""
input ProductVariantSort {
  field: ProductVariantSortField!
  direction: SortDirection!
}

"""
The fields a variant list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`productId` is stated as a filter rather than served as a root field of its own: the variants of one
product are the same rows narrowed to one product, which is what the delivered sub-route answers, and
a second root field for them would be a second surface that could disagree with this one.
"""
input ProductVariantFilter {
  id: IDFilter
  notes: StringFilter
  quantity: NumberFilter
  taxes: DecimalFilter
  billingInvoicingPolicy: StringFilter
  internalReference: StringFilter
  enabled: BooleanFilter
  productId: IDFilter
  imageId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductVariantFilter!]
  or: [ProductVariantFilter!]
  not: ProductVariantFilter
}

"""
One combination of option values a variant is generated for.

The values are the names the option translations carry, which is what the delivered generator
matches against: it resolves each stated value to the option row of the product that names it, so a
value the product does not offer is a combination that produces a variant without that option.
"""
input ProductVariantOptionCombinationInput {
  options: [String!]!
}

"""
What a caller states when it generates the variants of a product.

The product is named rather than carried. Everything else the generator needs — the option groups it
matches the combinations against, and the tenant and the organization the new rows belong to — is
read from the product row, so a caller cannot state a scope the product does not have.
"""
input CreateProductVariantsInput {
  productId: ID!
  optionCombinations: [ProductVariantOptionCombinationInput!]!
}

"""
What a caller may change on a variant that exists. A member that is absent is left as it is.

The product is not among the members: moving a variant to another product would move the stock, the
prices, the publications and the order lines that point at it, which is a different operation than
an edit. `enabled` is here because withdrawing a variant from sale is an edit; the rows it has
already been sold on keep pointing at it.
"""
input UpdateProductVariantInput {
  id: ID!
  notes: String
  quantity: Int
  taxes: Float
  billingInvoicingPolicy: String
  internalReference: String
  enabled: Boolean
  imageId: ID
}

"""A sellable thing, in one language."""
type Product {
  id: ID!

  """
  The operator-facing code. Unique within the organization and stable across renames.
  """
  code: String!

  """
  Whether the product may be sold. A disabled product stays readable and cannot be ordered.
  """
  enabled: Boolean!

  """
  A denormalised thumbnail URL, kept for lists that must not join the asset table.
  """
  imageUrl: String

  """The asset the storefront shows as the product's cover."""
  featuredImageId: ID
  featuredImage: ImageAsset

  """The operator's classification of the product."""
  productTypeId: ID

  """The merchandising grouping the product belongs to."""
  productCategoryId: ID

  """
  The product's name in the requested language, merged from its translation.
  """
  name: String

  """
  The product's description in the requested language, merged from its translation.
  """
  description: String

  """
  The facets attached to the product. The same `tag` rows the rest of the platform uses.
  """
  tags: [Tag!]

  """The extra images of the product, in the order the gallery declares."""
  gallery: [ImageAsset!]

  """
  The variants the product is sold as. A product with no variant cannot be added to a cart.
  """
  variants: [ProductVariant!]
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of products."""
type ProductEdge {
  node: Product!
  cursor: String!
}

"""
One page of products, with the total the filters select and the boundary cursors.
"""
type ProductConnection {
  nodes: [Product!]!
  edges: [ProductEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a product list may be sorted by."""
enum ProductSortField {
  createdAt
  updatedAt
  code
  name
  enabled
}

"""
The order the products are returned in. At most three keys are accepted.
"""
input ProductSort {
  field: ProductSortField!
  direction: SortDirection!
}

"""
The fields a product list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.
"""
input ProductFilter {
  id: IDFilter
  code: StringFilter
  enabled: BooleanFilter
  imageUrl: StringFilter

  """
  The product's name in the requested language, as the delivered list read merges it.
  """
  name: StringFilter

  """
  The product's description in the requested language, as the delivered list read merges it.
  """
  description: StringFilter
  featuredImageId: IDFilter
  productTypeId: IDFilter
  productCategoryId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ProductFilter!]
  or: [ProductFilter!]
  not: ProductFilter
}

"""
One translation of a product's own name and description.

`name` is required rather than accepted and refused later: the translation row's column is not
nullable, so a translation without a name cannot be stored at all.
"""
input ProductTranslationInput {
  languageCode: String!
  name: String!
  description: String
}

"""One translation of an option's name."""
input ProductOptionTranslationInput {
  languageCode: String!
  name: String!
  description: String
}

"""One translation of the group an option belongs to."""
input ProductOptionGroupTranslationInput {
  languageCode: String!
  name: String!
}

"""
One choice a product offers inside a group.

`id` is absent on an option the caller is adding and present on one that exists: the delivered paths
branch on exactly that, storing a row when no identifier is stated and reading the existing one when
it is. `code` is optional because the operator's own shorthand for an option has no column behind it
that requires a value; `name` is required because the row cannot be stored without one.
"""
input ProductOptionInput {
  id: ID
  name: String!
  code: String
  translations: [ProductOptionTranslationInput!]
}

"""
A set of choices a product offers — the size, the colour — with the options it holds.

`id` selects the same branch as it does on an option: an absent identifier is a group being added, a
stated one is a group being changed or withdrawn.
"""
input ProductOptionGroupInput {
  id: ID
  name: String!
  options: [ProductOptionInput!]
  translations: [ProductOptionGroupTranslationInput!]
}

"""
What a caller states when it records a product.

The tenant is not a member: the delivered create stamps the caller's own tenant onto the row and
overwrites anything a body states, so a member here would promise a scope the write refuses. The
organization is a member, because the delivered create takes it from the body and the row's
organization column is what the write stores.
"""
input CreateProductInput {
  """
  The operator-facing code, unique within the organization. The delivered create requires it.
  """
  code: String!

  """The organization the product is filed under."""
  organizationId: ID

  """
  Whether the product may be sold. A disabled product stays readable and cannot be ordered.
  """
  enabled: Boolean

  """
  A denormalised thumbnail URL, for the lists that must not join the asset table.
  """
  imageUrl: String

  """The asset the storefront shows as the product's cover."""
  featuredImageId: ID

  """The operator's classification of the product."""
  productTypeId: ID

  """The merchandising grouping the product belongs to."""
  productCategoryId: ID

  """The facets to attach to the product."""
  tagIds: [ID!]

  """The product's name and description, one row per language."""
  translations: [ProductTranslationInput!]

  """
  The option groups the product offers, stored with their options and translations.
  """
  optionGroupCreateInputs: [ProductOptionGroupInput!]!
}

"""
What a caller may change on a product that exists.

Every member is optional except the group list both delivered paths read unconditionally, and a
member that is absent is left as it is. The group lists are the three directions of one edit — the
groups to add, the groups to change and the groups to withdraw — and the option list is the options
to withdraw from the groups that stay.
"""
input UpdateProductInput {
  id: ID!
  productTypeId: ID
  productCategoryId: ID
  tagIds: [ID!]
  translations: [ProductTranslationInput!]
  optionGroupCreateInputs: [ProductOptionGroupInput!]!
  optionGroupUpdateInputs: [ProductOptionGroupInput!]
  optionGroupDeleteInputs: [ProductOptionGroupInput!]
  optionDeleteInputs: [ProductOptionInput!]
}

"""The operation one item of a product batch performs."""
enum ProductBulkOperation {
  """A row is written from the item's members."""
  create

  """A row the item names is changed."""
  update

  """A row the item names is archived: kept, and no longer listed."""
  delete

  """A row the item names is changed, or written when it names none."""
  upsert
}

"""
One item of a product batch: the operation it performs and the product it performs it on.

The members are the product's own write members, so an item states exactly what the equivalent
single-item request states and nothing beside it. A member that is absent is a member the item says
nothing about, which is a different request from a member stated as empty. Tags are named by the
identifier the pivot row is written from, never by the tag row beside it.
"""
input ProductBulkItem {
  """The operation this item performs."""
  op: ProductBulkOperation!

  """
  The row the item addresses. Stated by the operations that change or archive a product.
  """
  id: ID

  """The operator-facing code, unique within the organization."""
  code: String

  """
  Whether the product may be sold. A disabled product stays readable and cannot be ordered.
  """
  enabled: Boolean

  """
  A denormalised thumbnail URL, for the lists that must not join the asset table.
  """
  imageUrl: String

  """The asset the storefront shows as the product's cover."""
  featuredImageId: ID

  """The operator's classification of the product."""
  productTypeId: ID

  """The merchandising grouping the product belongs to."""
  productCategoryId: ID

  """The facets to attach to the product."""
  tagIds: [ID!]

  """The product's name and description, one row per language."""
  translations: [ProductTranslationInput!]
}

"""
What a caller states when it applies a batch of products.

`atomic` asks for the batch as one write: either every item applies or none of them does. A batch that
does not ask for it applies what it can and reports the items it could not — the flag is the whole
difference between the two answers, and neither is a guess about the other.

`idempotencyKey` is the retry key of this batch, and it is optional: the operation honours a key and
does not demand one. A GraphQL request may carry many mutations in one document and a header cannot
say which of them a key belongs to, so the key travels here, beside the input it qualifies, and is
read by the same kernel that stores and replays the REST surface's keys.
"""
input BulkCreateProductsInput {
  """
  The items of the batch, in caller order. The answer refers to them by position.
  """
  items: [ProductBulkItem!]!

  """Whether the batch is applied as one write."""
  atomic: Boolean

  """The client's retry key for this batch."""
  idempotencyKey: String
}

"""
One item's outcome: the row that changed, or the item's own failure.

The index is part of the answer because a batch is answered in one piece — a caller that sent twenty
items reads which of them applied without matching rows back to a request by hand.
"""
type BulkProductItemResult {
  """The item's position in the request."""
  index: Int!

  """Whether the item applied."""
  ok: Boolean!

  """The row that changed, when the item applied."""
  id: ID

  """The resource that changed, so a mixed batch reads unambiguously."""
  resource: String

  """
  Why the item did not apply, with the code the same item would have produced alone. The path names the item it belongs to.
  """
  error: UserError
}

"""
What a batch of products adds up to.

`succeeded` and `failed` are the counts of the two kinds of entry in `results`, so a client can assert
`succeeded + failed == items.length` against the same answer rather than against a second number that
could disagree with it.
"""
type BulkCreateProductsPayload {
  """One entry per request item, in request order."""
  results: [BulkProductItemResult!]!

  """How many items applied."""
  succeeded: Int!

  """How many items did not."""
  failed: Int!

  """How many items the request carried."""
  total: Int!
}

"""One emoji one actor left on one row of another domain."""
type Reaction {
  id: ID!

  """
  The name of the domain the reacted-to row belongs to, as the contract's own vocabulary states it.
  """
  entity: String!

  """
  The reacted-to row, by identifier. The row itself is not joined by this read.
  """
  entityId: ID!

  """The mark itself, as the actor left it."""
  emoji: String!

  """
  Who left the mark, as the kernel's own value: `User` for a person and `System` for the platform
  itself. Null on a row written before the member existed.
  """
  actorType: String

  """
  The actor's employee record, by identifier. The employee row is not joined by this read.
  """
  employeeId: ID

  """The tenant the reaction belongs to, by identifier."""
  tenantId: ID

  """The organization the reaction belongs to, by identifier."""
  organizationId: ID

  """
  Whether the reaction is active. A flag an operator sets, not a statement about its lifecycle.
  """
  isActive: Boolean

  """
  Whether the reaction is archived. A flag an operator sets, not a statement about its lifecycle.
  """
  isArchived: Boolean

  """When the reaction was archived, as opposed to withdrawn."""
  archivedAt: DateTime

  """
  When the reaction was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime

  """When the mark was left."""
  createdAt: DateTime

  """When the row was last changed."""
  updatedAt: DateTime
}

"""The boundary of one page of reactions."""
type ReactionEdge {
  node: Reaction!
  cursor: String!
}

"""
One page of reactions, with the total the filters select and the boundary cursors.
"""
type ReactionConnection {
  nodes: [Reaction!]!
  edges: [ReactionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a reaction list may be sorted by."""
enum ReactionSortField {
  id
  entity
  entityId
  emoji
  createdAt
  updatedAt
  deletedAt
}

"""
The order the reactions are returned in. At most three keys are accepted.
"""
input ReactionSort {
  field: ReactionSortField!
  direction: SortDirection!
}

"""
The fields a reaction list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the reaction row, because the delivered list read answers the row itself
and joins only the relations a REST caller names in its query string — which this surface never names.
A condition on the reacted-to row or on the employee behind `employeeId` is therefore not a member:
neither is loaded by the read this connection is built over, and a condition on a relation that is
never loaded could only ever match the empty set.

`entity` is a text member rather than an enumerated one, so a caller narrows it by the value the
contract's vocabulary states. The tenant comes from the credential on every read, so `tenantId`
narrows the row's own column rather than restating a scope decision the caller does not have.
`deletedAt` is a member for the same reason the object type carries it — it is the column the
withdrawal routes write — and because the delivered list read answers live rows only, `isNull: true`
selects every row this connection can return.
"""
input ReactionFilter {
  id: IDFilter
  entity: StringFilter
  entityId: IDFilter
  emoji: StringFilter
  actorType: StringFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [ReactionFilter!]
  or: [ReactionFilter!]
  not: ReactionFilter
}

"""
What a caller states when it leaves a reaction.

`entity`, `entityId` and `emoji` are required because they are what the mark names and what the
delivered write is keyed on: the column is non-null and the create is a toggle, so a reaction is
recognised — and removed rather than filed a second time — by the domain, the row, the emoji, the
organization and the actor together. A body omitting one of them names no mark at all.

`actorType` is not a member. The delivered write stamps it: the create handler sets it to the kernel's
`User` value on every reaction it files, so a member here would promise a choice the write overwrites.
The author is not a member either, and for the same reason: the delivered write resolves the acting
employee from the credential and refuses the call when the credential carries none.

There is no member for the tenant: the delivered write reads it from the credential and stamps it,
overwriting whatever a body states, so a member here would promise a scope the write refuses.
"""
input CreateReactionInput {
  """The domain the reacted-to row belongs to."""
  entity: String!

  """The row being reacted to, by identifier."""
  entityId: ID!

  """The mark itself."""
  emoji: String!

  """The organization the reaction belongs to."""
  organizationId: ID
}

"""
What a caller states when it changes a reaction that exists.

**A member the caller leaves out is left as it is.** The delivered edit reads the row the identifier
names — narrowed to the caller's own employee — before it writes, which is how a reaction of another
tenant, or somebody else's, is answered with the refusal rather than with a write, and it then persists
only the members a body states. Stating this here is what keeps a client from reading "absent" as
"written as the column's default".

The identifier is required and is stated because the delivered route carries it in the path, and the
same member is what the write is keyed on.
"""
input UpdateReactionInput {
  id: ID!

  """The mark itself."""
  emoji: String

  """The organization the reaction belongs to."""
  organizationId: ID
}

"""
The commercial geography — a currency, a tax-inclusivity default, a country set and the provider keys
enabled for it.
"""
type Region {
  id: ID!
  tenantId: ID
  organizationId: ID
  name: String!

  """Stable key, unique per organization among the live rows."""
  code: String!

  """
  The region's currency, naming a row of the platform's currency master. A three-letter ISO code.
  """
  currency: String!

  """
  Whether this is the organization's default region. At most one live row carries it.
  """
  isDefault: Boolean!

  """Whether displayed prices include tax in this region."""
  isTaxInclusive: Boolean!

  """
  Registered tax-provider strategy key. Absent means the platform's built-in tax engine.
  """
  taxProviderKey: String

  """Allowed payment-provider codes. Absent means every enabled provider."""
  paymentProviderKeys: [String!]

  """
  Allowed shipping and carrier provider keys. Absent means every enabled provider.
  """
  fulfillmentProviderKeys: [String!]

  """
  Only `ACTIVE` regions are offered by their channels. `ARCHIVED` is terminal.
  """
  status: ChannelStatus!

  """Tenant extras."""
  metadata: JSON

  """
  The countries the region serves, one row per country, when they are expanded.
  """
  countries: [RegionCountry!]
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One country inside one commercial geography.

The row answers two questions in one read: whether a shipping address is inside the region, and
whether sales into that country are exempt there. That is why the membership is a table rather than a
list of identifiers on the region.
"""
type RegionCountry {
  id: ID!
  regionId: ID!
  region: Region
  countryId: ID!

  """
  Whether sales into this country carry no tax in this region — an export sale.
  """
  isTaxExempt: Boolean!

  """
  Optional sub-national scope: when set, only these province or state codes are in the region.
  Absent means the whole country.
  """
  provinceCodes: [String!]
  tenantId: ID
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of regions."""
type RegionEdge {
  node: Region!
  cursor: String!
}

"""
One page of regions, with the total the filters select and the boundary cursors.
"""
type RegionConnection {
  nodes: [Region!]!
  edges: [RegionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a region may be sorted by."""
enum RegionSortField {
  createdAt
  updatedAt
  name
  code
  currency
  status
  isDefault
}

"""
The order the regions are returned in. At most three keys are accepted.
"""
input RegionSort {
  field: RegionSortField!
  direction: SortDirection!
}

"""Comparison on the lifecycle status."""
input RegionStatusFilter {
  eq: ChannelStatus
  ne: ChannelStatus
  in: [ChannelStatus!]
  nin: [ChannelStatus!]
  isNull: Boolean
}

"""The fields a region list may be narrowed by."""
input RegionFilter {
  id: IDFilter
  name: StringFilter
  code: StringFilter
  currency: StringFilter
  status: RegionStatusFilter
  isDefault: BooleanFilter
  taxProviderKey: StringFilter
  isTaxInclusive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [RegionFilter!]
  or: [RegionFilter!]
  not: RegionFilter
}

"""
What a caller states when it opens a commercial geography.

`currency` is required and checked against the platform's currency master before the row is written.
The country set is not part of this input: membership is a pivot the region's own operation writes.
"""
input CreateRegionInput {
  organizationId: ID!
  name: String!
  code: String!
  currency: String!
  isTaxInclusive: Boolean
  taxProviderKey: String
  paymentProviderKeys: [String!]
  fulfillmentProviderKeys: [String!]
  metadata: JSON
}

"""
What a caller may change on a region that exists.

A currency change is re-checked against the currency master like the creation is. `isDefault` and
`status` are absent: each has an operation of its own.
"""
input UpdateRegionInput {
  id: ID!
  organizationId: ID!
  name: String
  code: String
  currency: String
  isTaxInclusive: Boolean
  taxProviderKey: String
  paymentProviderKeys: [String!]
  fulfillmentProviderKeys: [String!]
  metadata: JSON
}

"""One member of a region's country set."""
input RegionCountryInput {
  countryId: ID!
  isTaxExempt: Boolean
  provinceCodes: [String!]
}

"""
The country set a region serves afterwards.

A set rather than a member-per-call pair, so a half-applied replacement is not a state a client can
observe.
"""
input ReplaceRegionCountriesInput {
  id: ID!
  countries: [RegionCountryInput!]!
}

"""
One report of the platform's catalogue: what a client offers, and whether this organization shows it.
"""
type Report {
  id: ID!

  """The report's display name, as the navigation prints it."""
  name: String

  """The URL-safe identity of the report, and how a route addresses it."""
  slug: String

  """One line describing what the report answers."""
  description: String

  """The stored image key. `imageUrl` is the URL a client renders."""
  image: String

  """
  The URL the delivered subscriber resolves from `image` as the row is loaded.
  """
  imageUrl: String

  """The icon class the navigation renders beside the name."""
  iconClass: String

  """
  Whether the calling organization's menu shows this report.
  
  Computed by the delivered read from the organization's own menu row: `true` means an enabled,
  active and unarchived menu row exists for it, and `false` means none does — including the case
  where the caller named no organization at all, which is the answer the route gives that same
  query string.
  """
  showInMenu: Boolean!

  """
  The category the report is filed under, by identifier. The categories are `reportCategories`.
  """
  categoryId: ID

  """
  Whether the catalogue row is active. No route of this resource writes it.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One category of the report catalogue: the heading a group of reports is listed under.
"""
type ReportCategory {
  id: ID!

  """The heading's display name."""
  name: String

  """The icon class the navigation renders beside the heading."""
  iconClass: String

  """
  Whether the catalogue row is active. No route of this resource writes it.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One organization's menu row for one report: the pair that decides whether the report is offered.

A row is not a report and not an organization — it is the membership between them, and it carries the
flag the report list reads to compute `showInMenu`.
"""
type ReportOrganization {
  id: ID!

  """The report this menu row is about."""
  reportId: ID

  """Whether the report is switched on in the organization's menu."""
  isEnabled: Boolean
  tenantId: ID
  organizationId: ID

  """
  Whether the menu row is active. It is one of the conditions the report list reads.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of reports."""
type ReportEdge {
  node: Report!
  cursor: String!
}

"""
One page of reports, with the total the filters select and the boundary cursors.
"""
type ReportConnection {
  nodes: [Report!]!
  edges: [ReportEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The boundary of one page of report categories."""
type ReportCategoryEdge {
  node: ReportCategory!
  cursor: String!
}

"""
One page of report categories, with the total the filters select and the boundary cursors.
"""
type ReportCategoryConnection {
  nodes: [ReportCategory!]!
  edges: [ReportCategoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a report list may be sorted by."""
enum ReportSortField {
  createdAt
  updatedAt
  name
  slug
}

"""
The order the reports are returned in. At most three keys are accepted.
"""
input ReportSort {
  field: ReportSortField!
  direction: SortDirection!
}

"""The fields a category list may be sorted by."""
enum ReportCategorySortField {
  createdAt
  updatedAt
  name
}

"""
The order the categories are returned in. At most three keys are accepted.
"""
input ReportCategorySort {
  field: ReportCategorySortField!
  direction: SortDirection!
}

"""
The fields a report list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`showInMenu` is filterable because that is how the delivered menu sub-route is expressed here: the
reports the menu shows are this one list narrowed to `showInMenu: { eq: true }`, which is the same
selection the menu route performs with its own join rather than a second root field over the same
rows.

There is no member for the organization. The catalogue row carries no organization column — the
organization is a dimension of the *menu* row — so an organization filter here could only be
evaluated against a member the row does not have; the organization is an argument of the root field,
see `report.api.gql`.
"""
input ReportFilter {
  id: IDFilter
  name: StringFilter
  slug: StringFilter
  description: StringFilter
  image: StringFilter
  imageUrl: StringFilter
  iconClass: StringFilter
  showInMenu: BooleanFilter
  categoryId: IDFilter
  isActive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ReportFilter!]
  or: [ReportFilter!]
  not: ReportFilter
}

"""
The fields a category list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.
"""
input ReportCategoryFilter {
  id: IDFilter
  name: StringFilter
  iconClass: StringFilter
  isActive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ReportCategoryFilter!]
  or: [ReportCategoryFilter!]
  not: ReportCategoryFilter
}

"""
What a caller states when it switches a report on or off in an organization's menu.

`reportId` and `organizationId` are required because the delivered write requires them: it looks the
menu row up by that pair and answers `400` for a body that names neither, and stating them here is
what keeps a caller from issuing a request that cannot succeed.

The tenant is deliberately **not** a member. The delivered write resolves it from the credential and
falls back to the body, then builds the row it saves from that same body — so a caller-stated tenant
would be a way to write another tenant's menu row through a surface that never asked for one. The
route accepts such a body only because its body is unvalidated; this surface states the members the
write is for.
"""
input UpdateReportMenuInput {
  """The report whose menu row is being switched."""
  reportId: ID!

  """The organization whose menu is being written."""
  organizationId: ID!

  """
  Whether the report is shown. Omit it to leave the row's own value as it is.
  """
  isEnabled: Boolean
}

"""One request for approval, as the delivered reads answer it."""
type RequestApproval {
  id: ID!

  """
  The name the request is known by, as the document that raised it wrote it.
  """
  name: String!

  """
  The state of the request: `1` while it is waiting for its approvers, `2` once enough of them have
  approved, `3` once one has refused. The vocabulary is the contracts' own
  `RequestApprovalStatusTypesEnum` — see the file comment for why it is carried as its value.
  """
  status: Int

  """Free text kept beside the request, for the approver to read."""
  note: String

  """
  The value the request commits, as an exact decimal string, so a threshold policy can be applied to
  it. Null for a request that commits no money — a time-off request, for instance.
  """
  amount: Decimal

  """
  The ISO 4217 code `amount` is stated in. Null exactly when `amount` is null.
  """
  currency: String

  """
  The document the request is about, by identifier. The pair `requestId` and `requestType` is
  polymorphic rather than a foreign key, so the document itself is read on the domain that owns it.
  """
  requestId: ID

  """
  What kind of document `requestId` names — the vocabulary is the contracts'
  `ApprovalPolicyTypesStringEnum`, carried as its value for the same reason `status` is.
  """
  requestType: String

  """
  How many approvals the request needs before it is approved. Null when the policy states no
  threshold, which the delivered writer treats as no threshold at all rather than as zero.
  """
  min_count: Int

  """
  The approval policy that governs the request, by identifier. The row is joined by the delivered
  list read only as the scope it is evaluated through and is not carried here; it is read on the
  approval-policy domain.
  """
  approvalPolicyId: ID

  """The tenant the request belongs to, by identifier."""
  tenantId: ID

  """The organization the request belongs to, by identifier."""
  organizationId: ID

  """When the request was raised."""
  createdAt: DateTime

  """When the row last changed."""
  updatedAt: DateTime

  """
  When the request was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live row from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime

  """
  When the request was archived, as opposed to withdrawn. A flag an operator sets, not a statement
  about the row's lifecycle.
  """
  archivedAt: DateTime

  """
  Whether the request is active. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isActive: Boolean

  """
  Whether the request is archived. A flag an operator sets, not a statement about the row's lifecycle.
  """
  isArchived: Boolean
}

"""The boundary of one page of requests."""
type RequestApprovalEdge {
  node: RequestApproval!
  cursor: String!
}

"""
One page of requests, with the total the filters select and the boundary cursors.
"""
type RequestApprovalConnection {
  nodes: [RequestApproval!]!
  edges: [RequestApprovalEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a request list may be sorted by."""
enum RequestApprovalSortField {
  id
  name
  status
  min_count
  amount
  currency
  createdAt
  updatedAt
  deletedAt
}

"""
The order the requests are returned in. At most three keys are accepted.
"""
input RequestApprovalSort {
  field: RequestApprovalSortField!
  direction: SortDirection!
}

"""
The fields a request list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the request row, which is why the set is what it is: the connection
narrows the rows the service returned, so a condition on a collection the reader never loaded could
only ever match the empty set. `status` and `min_count` take the whole-number comparison family rather
than an operator set of their own, because the vocabulary of the first is numeric and the second is a
count; `amount` takes the decimal family, whose values are decimal strings for the same reason the
output is.
"""
input RequestApprovalFilter {
  id: IDFilter
  name: StringFilter
  status: NumberFilter
  min_count: NumberFilter
  requestId: IDFilter
  requestType: StringFilter
  amount: DecimalFilter
  currency: StringFilter
  note: StringFilter
  approvalPolicyId: IDFilter
  organizationId: IDFilter
  tenantId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  and: [RequestApprovalFilter!]
  or: [RequestApprovalFilter!]
  not: RequestApprovalFilter
}

"""
What a caller states when it raises a request.

**Every member here is one the delivered create reads**, and the set is short for that reason: the
delivered method builds the row member by member rather than spreading the body into it, so a member
it does not name is not written at all. `status` is the sharpest case and is deliberately absent — the
delivered writer stamps `1` onto every request it raises, so a caller that could state a state would be
stating one the write overwrites; the state a request is in is the approvers' answer, not the raiser's.

The two collections are stated as identifiers because the delivered write resolves them to rows itself:
it reads the employees and the teams the caller names and builds one pivot row per resolved row, so
what it wants from the body is the identifiers and not the rows. The tags are stated the same way — the
tag domain serves them, and this surface names them by identifier rather than restating that domain's
object.

There is no member for the tenant: the delivered write stamps the caller's own tenant onto the row and
overwrites whatever a body states, so a member here would promise a scope the write refuses. The
organization is stated, because the write takes it from the body — the request belongs to the
organization that raised it, and the delivered reads are scoped to the organization the caller is
acting in.
"""
input CreateRequestApprovalInput {
  """The name the request is known by."""
  name: String!

  """
  The policy that governs the answer. Omit for a request no policy applies to.
  """
  approvalPolicyId: ID

  """How many approvals the request needs. Omit for no threshold."""
  min_count: Int

  """
  The document the request is about. Omit only for a request that is about nothing.
  """
  requestId: ID

  """What kind of document that is, in the platform's own vocabulary."""
  requestType: String

  """
  The value the request commits, as a decimal string. Omit for a request that commits no money.
  """
  amount: Decimal

  """
  The ISO 4217 code the value is stated in. State it whenever an amount is stated.
  """
  currency: String

  """Free text kept beside the request, for the approver to read."""
  note: String

  """
  The employees whose approval is asked for, by identifier. The pivots are written from these.
  """
  employeeApprovals: [ID!]

  """
  The teams whose approval is asked for, by identifier. The pivots are written from these.
  """
  teams: [ID!]

  """
  The facets to attach to the request, by identifier. The pivot is written from these.
  """
  tags: [ID!]

  """
  The organization the request belongs to. Omit only when the caller is acting in one organization.
  """
  organizationId: ID
}

"""
What a caller states when it changes a request that exists.

**The delivered update reads the row, assigns the members it acts on, and saves it** — it is neither a
replacement of the row nor a partial column update, and which members it acts on is worth stating
exactly, because the two halves behave differently:

- `name`, `min_count`, `approvalPolicyId`, `organizationId` and `tags` are assigned from the body, so a
  member the caller leaves out is written as the column's default rather than left as it was;
- `employeeApprovals` and `teams` are read only when the body states them: the delivered writer removes
  every existing pivot row first and writes the stated set in its place, so omitting one of the two
  withdraws every approver of that kind;
- `requestId`, `requestType`, `amount`, `currency` and `note` are read from no part of the body at all.
  They are offered here because the delivered route's own body type carries them, and a member this
  surface withheld would be a write a client can make over one protocol and not over the other; a
  caller that states one is answered the row it already had, which is the honest reading of a write
  that does not name it.

The state is not a member for the same reason it is not one of the create: the delivered update stamps
`1` back onto every request it rewrites, so a request that had been answered returns to waiting when it
is edited. That is the delivered behaviour rather than this surface's, and stating it here is what
keeps a client from reading the edit as a way to answer a request.
"""
input UpdateRequestApprovalInput {
  id: ID!
  name: String
  approvalPolicyId: ID
  min_count: Int
  requestId: ID
  requestType: String
  amount: Decimal
  currency: String
  note: String
  employeeApprovals: [ID!]
  teams: [ID!]
  tags: [ID!]
  organizationId: ID
}

"""
One URL with a title, attached to one record of the platform and authored by one employee.
"""
type ResourceLink {
  id: ID!

  """The title the link is shown under, as entered."""
  title: String!

  """The address the link points at."""
  url: String

  """
  What the link is attached to, as the platform's own entity vocabulary names it — for example
  `Task` or `Invoice`. A polymorphic row names its target's table rather than holding a key to one.
  """
  entity: String

  """The record the link is attached to, within the entity named beside it."""
  entityId: ID

  """Metadata read from the address, when a reader was able to fetch some."""
  metaData: JSON

  """
  The employee the link is attributed to. Null on a link written without an author.
  """
  employeeId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the link was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of links."""
type ResourceLinkEdge {
  node: ResourceLink!
  cursor: String!
}

"""
One page of links, with the total the filters select and the boundary cursors.
"""
type ResourceLinkConnection {
  nodes: [ResourceLink!]!
  edges: [ResourceLinkEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a link list may be sorted by."""
enum ResourceLinkSortField {
  createdAt
  updatedAt
  title
  entity
}

"""The order the links are returned in. At most three keys are accepted."""
input ResourceLinkSort {
  field: ResourceLinkSortField!
  direction: SortDirection!
}

"""
The fields a link list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

The two polymorphic identifiers are here, and they are what makes this list usable at all: the
question a client actually asks is "the links of *this* record", and it is asked by narrowing
`entity` and `entityId` together. The delivered list route takes the same narrowing in its query
string, so a connection narrowed this way is the same read the route performs.
"""
input ResourceLinkFilter {
  id: IDFilter
  organizationId: IDFilter
  entity: StringFilter
  entityId: IDFilter
  title: StringFilter
  url: StringFilter
  metaData: JSONFilter
  employeeId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ResourceLinkFilter!]
  or: [ResourceLinkFilter!]
  not: ResourceLinkFilter
}

"""
What a caller states when it files a link.

The title and the address are required because the delivered create validates them — the DTO marks
both non-empty and validates the address as one — and the organization is required for the same
reason. The target is stated as the platform's own entity name beside the record's identifier; the
delivered body states the same pair.

The author is optional because the delivered service decides it rather than the body: it takes the
caller's own employee, falling back to the body only when the credential carries none. A member the
write overwrites is stated as optional rather than as a claim the caller cannot make.
"""
input CreateResourceLinkInput {
  organizationId: ID!
  entity: String!
  entityId: ID!
  title: String!
  url: String!
  metaData: JSON
  employeeId: ID
}

"""
What a caller states when it changes a link that exists.

**Every member but the identifier is optional**, because the delivered edit binds a partial type of
the create body. The delivered write behind it is a create beside the stated identifier — a save of
the columns the body carries — so a member the caller leaves out is left as it is, and the target and
the author are the two the delivered contract expects a caller to leave alone: a link that moved to
another record would be a different link, and the delivered edit types both as excluded even though
its body would accept them.
"""
input UpdateResourceLinkInput {
  id: ID!
  organizationId: ID
  entity: String
  entityId: ID
  title: String
  url: String
  metaData: JSON
  employeeId: ID
}

"""
What a caller states when it adds a permission to a role.

The role is named by its identifier: the delivered body admits either the identifier or a whole role
object, and the one the service resolves is always the identifier — it reads `roleId` first and
refuses the write when neither is stated, which is the fail-closed rule this input states by requiring
the identifier outright.

There is no member for the tenant, for the same reason there is none on any other write in this
schema: the tenant is stamped from the credential and a body that stated one would be overwritten. Nor
is there one for an organization: a role permission belongs to a role, and a role belongs to a tenant.
"""
input CreateRolePermissionInput {
  """The permission, named exactly as the platform's catalogue names it."""
  permission: String!

  """Whether the role carries it."""
  enabled: Boolean!

  """The role the row belongs to."""
  roleId: ID!
}

"""
What a caller states when it edits a role permission that exists.

The identifier names the row, which is the one member the delivered route carries in its path rather
than in its body: the other three are the same shape the create above takes, because the delivered
update DTO is the create's own.
"""
input UpdateRolePermissionInput {
  id: ID!
  permission: String!
  enabled: Boolean!
  roleId: ID!
}

type RolePermission {
  id: ID!

  """
  The permission this row grants or withholds, named exactly as the platform's catalogue names it.
  """
  permission: String!

  """
  Whether the role carries the permission. A row exists for every permission of the catalogue.
  """
  enabled: Boolean!

  """Free text an administrator may keep beside the row."""
  description: String

  """The role the row belongs to."""
  roleId: ID!
  tenantId: ID

  """When the row was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of role permissions."""
type RolePermissionEdge {
  node: RolePermission!
  cursor: String!
}

"""
One page of role permissions, with the total the filters select and the boundary cursors.
"""
type RolePermissionConnection {
  nodes: [RolePermission!]!
  edges: [RolePermissionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a role permission list may be sorted by."""
enum RolePermissionSortField {
  createdAt
  updatedAt
  permission
  enabled
}

"""
The order the role permissions are returned in. At most three keys are accepted.
"""
input RolePermissionSort {
  field: RolePermissionSortField!
  direction: SortDirection!
}

"""
The fields a role permission list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

`roleId` is the member a caller reaches for: the permissions of one role are the rows of that role,
and the delivered list route expresses the same narrowing through the `roleId` its own query string
binds to the store. `permission` and `enabled` are here because a client that renders a role's
catalogue filters it by what the row grants and by what it names.

`tenantId` is deliberately absent, although it is a column: the tenant a read runs under comes from
the credential and never from a filter, and the read this connection is built over is already scoped
by it.
"""
input RolePermissionFilter {
  id: IDFilter
  permission: StringFilter
  enabled: BooleanFilter
  roleId: IDFilter
  description: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [RolePermissionFilter!]
  or: [RolePermissionFilter!]
  not: RolePermissionFilter
}

input CreateRoleInput {
  name: String!
  tenantId: String!
}

input UpdateRoleInput {
  id: ID!
  name: String
  tenantId: String!
}

type Role {
  id: ID!
  name: String!
  tenantId: String!

  """
  Whether the platform provisioned the role. The delivered removal does not select a system role.
  """
  isSystem: Boolean

  """When the role was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The boundary of one page of roles."""
type RoleEdge {
  node: Role!
  cursor: String!
}

"""
One page of roles, with the total the filters select and the boundary cursors.
"""
type RoleConnection {
  nodes: [Role!]!
  edges: [RoleEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a role list may be sorted by."""
enum RoleSortField {
  createdAt
  updatedAt
  name
  isSystem
}

"""The order the roles are returned in. At most three keys are accepted."""
input RoleSort {
  field: RoleSortField!
  direction: SortDirection!
}

"""
The fields a role list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

`name` is the member that matters: a role *is* its name, and the two delivered reads that take a
narrowing — the list route's own clients and the options look-up — narrow by it. `isSystem` is here
because that distinction is what an administrator selects between when it decides which roles its own
removal reaches.

`tenantId` is deliberately absent, although it is a column. The tenant a read runs under comes from
the credential and never from a filter, and the list read this connection is built over is already
scoped by it: a member here would state a narrowing the caller may not perform and could not change
the rows either way.
"""
input RoleFilter {
  id: IDFilter
  name: StringFilter
  isSystem: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [RoleFilter!]
  or: [RoleFilter!]
  not: RoleFilter
}

"""
One numbering series: the counter a key allocates from, and the shape of the numbers it produces.
"""
type Sequence {
  id: ID!

  """
  The tenant the series belongs to, which the caller's credential states and never the caller.
  """
  tenantId: ID

  """
  The organization whose documents the series numbers. A series is never read across organizations.
  """
  organizationId: ID

  """
  Series key, upper snake case, for example `ORDER` or `PURCHASE_ORDER`. It is the word every
  allocation resolves the series by, and it is written once: an edit that renamed it would leave the
  counter reachable under no name at all.
  """
  key: String!

  """
  The channel the counter belongs to, or null for the organization-wide series.
  `null` is not "no channel is configured" — it is a scope of its own, and it is the series an
  allocation falls back to when a channel has none. One organization therefore holds at most one
  organization-wide series and one series per channel under one key, which is what the table's two
  partial unique indexes state.
  """
  channelId: ID

  """Text placed before the number, for example `SO-`."""
  prefix: String

  """
  Minimum number of digits; a shorter value is left-padded with zeroes. A value that outgrows the
  width is never truncated — a number a person quotes is not silently shortened.
  """
  padding: Int!

  """
  **The state, not the configuration.** The value the next allocation hands out. It moves when a
  number is allocated, and backwards only through the restart the series' own policy describes —
  which is what `resetSequence` performs and what an edit refuses to write.
  """
  nextValue: Int!

  """
  The increment each allocation applies. Larger than one when a range is reserved for an external
  system: the allocation hands out one value and advances by this much, so the values in between are
  the reserved block.
  """
  step: Int!

  """When the series restarts, and the reason `lastResetAt` exists."""
  resetPolicy: SequenceResetPolicy!

  """
  **The state, not the configuration.** The moment the series last restarted, or null when it never
  has. The restart check compares the period this falls in with the period an allocation happens in,
  and a series restarts at most once per period — which is why a manual restart is refused inside a
  period that has already been restarted.
  """
  lastResetAt: DateTime

  """Free-text note for operators."""
  description: String

  """
  Whether the series numbers documents at all. Retiring a series is this member and not a removal:
  the counter and the documents numbered from it stay, and an allocation answers with a reason
  instead of a number.
  """
  isActive: Boolean
  createdAt: DateTime
  updatedAt: DateTime
}

"""When a series restarts."""
enum SequenceResetPolicy {
  """
  The series never restarts; it continues for the life of the organization.
  """
  NEVER

  """The series restarts on the first day of each calendar year, in UTC."""
  YEARLY

  """The series restarts on the first day of each calendar month, in UTC."""
  MONTHLY

  """The series restarts at the start of each calendar day, in UTC."""
  DAILY
}

"""The boundary of one page of series."""
type SequenceEdge {
  node: Sequence!
  cursor: String!
}

"""
One page of series, with the total the filters select and the boundary cursors.
"""
type SequenceConnection {
  nodes: [Sequence!]!
  edges: [SequenceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a series list may be sorted by.

`key` is how a numbering configuration is read — the series of one key together, whichever scope each
belongs to — and the two timestamps are how a change is found again. `nextValue` is offered because
"where does this counter stand" is the question an operator brings to this resource, and `resetPolicy`
because a work list of the series that restart is read in policy groups. The identifiers are not sort
keys: an order by identifier is not an order anyone reads a configuration in, and the questions those
columns answer are filters.
"""
enum SequenceSortField {
  key
  nextValue
  resetPolicy
  createdAt
  updatedAt
}

"""The order the series are returned in. At most three keys are accepted."""
input SequenceSort {
  field: SequenceSortField!
  direction: SortDirection!
}

"""Comparison on the restart policy."""
input SequenceResetPolicyFilter {
  eq: SequenceResetPolicy
  ne: SequenceResetPolicy
  in: [SequenceResetPolicy!]
  nin: [SequenceResetPolicy!]
  isNull: Boolean
}

"""
The fields a series list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is — the delivered list read
answers those columns, so each one narrows rows the connection was handed. `key` and `channelId` are
the two members the endpoint table names for this resource's list route, stated here in the
connection's own vocabulary so a caller who narrows over REST narrows the same way here. The tenant
and the organization are absent because both are applied to the read from the credential rather than
from the caller: a member here would suggest a scope decision the caller does not have.
"""
input SequenceFilter {
  id: IDFilter
  key: StringFilter
  channelId: IDFilter
  prefix: StringFilter
  padding: NumberFilter
  nextValue: NumberFilter
  step: NumberFilter
  resetPolicy: SequenceResetPolicyFilter
  lastResetAt: DateTimeFilter
  description: StringFilter
  isActive: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [SequenceFilter!]
  or: [SequenceFilter!]
  not: SequenceFilter
}

"""What a caller states when it opens a series."""
input CreateSequenceInput {
  """Series key, upper snake case, for example `ORDER`."""
  key: String!

  """
  The channel the counter belongs to. Absent means the organization-wide series.
  """
  channelId: ID

  """Text placed before the number, for example `SO-`."""
  prefix: String

  """
  Minimum number of digits. Defaults to the width a document number is quoted at.
  """
  padding: Int

  """
  The value the first allocation hands out. Stated when the series continues numbering an external
  system has already begun, and stated once: from here on the counter moves by allocating a number.
  """
  nextValue: Int

  """
  The increment each allocation applies. Larger than one when a range is reserved for an external
  system.
  """
  step: Int

  """
  When the series restarts. `NEVER` when the caller states none, which is the honest default.
  """
  resetPolicy: SequenceResetPolicy

  """Free-text note for operators."""
  description: String
}

"""
What a caller may change on a series that exists.

**The members this input does not declare are the contract.** `nextValue` and `lastResetAt` are the
series' state — the value the next document will be numbered with and the period it last restarted in
— and an edit that could write either would renumber documents the installation has already issued.
`key` and `channelId` are absent for the neighbouring reason: they are the series' identity and the
scope of its counter.

A `nextValue` a REST body states is refused as an unknown field by the route's own validation rather
than stripped, so a caller is told its edit was not applied instead of being answered `200` for a
write that changed nothing it asked for. Over this protocol the refusal is the schema's: an input
member that is not declared cannot be stated.

`isActive` is a member, and it is the retirement path: an allocation refuses a series that is not
active, so an operator who must stop numbering from a key retires the series instead of removing it —
the counter stays, and stating `true` puts it back to work.
"""
input UpdateSequenceInput {
  id: ID!

  """Text placed before the number, for example `SO-`."""
  prefix: String

  """Minimum number of digits; a shorter value is left-padded with zeroes."""
  padding: Int

  """The increment each allocation applies."""
  step: Int

  """When the series restarts."""
  resetPolicy: SequenceResetPolicy

  """Free-text note for operators."""
  description: String

  """
  Whether the series numbers documents at all. `false` retires it without removing its counter.
  """
  isActive: Boolean
}

"""
The capability that lets one record be read by whoever holds the token, within the stated rules.
"""
type SharedEntity {
  id: ID!

  """
  The bearer capability itself. Anyone holding it may read the record the share points at through
  `sharedEntityByToken`, which is why this member travels only to a caller the tenant guard admitted.
  """
  token: String!

  """
  What the share points at, as the platform's own entity vocabulary names it.
  """
  entity: String

  """The record the share points at, within the entity named beside it."""
  entityId: ID

  """
  Which members of the target are shared, and how far a reader may walk its relations.
  """
  shareRules: JSON

  """Additional options for this share, as the sharing layer states them."""
  sharedOptions: JSON
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the share was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the lifecycle pair this resource serves — and this column is the whole of what they write.
  A withdrawn share's token no longer resolves: the token read answers the same miss it answers for a
  token that never existed.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of shares."""
type SharedEntityEdge {
  node: SharedEntity!
  cursor: String!
}

"""
One page of shares, with the total the filters select and the boundary cursors.
"""
type SharedEntityConnection {
  nodes: [SharedEntity!]!
  edges: [SharedEntityEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a share list may be sorted by."""
enum SharedEntitySortField {
  createdAt
  updatedAt
  entity
}

"""The order the shares are returned in. At most three keys are accepted."""
input SharedEntitySort {
  field: SharedEntitySortField!
  direction: SortDirection!
}

"""
The fields a share list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

A share's own token is filterable, and that is deliberate rather than a leak: the list is
tenant-guarded, its rows are the caller's own, and the token is already a member of every row it
answers. What it is *not* is a way to resolve the shared record — that is the token read, and its
answer is the target rather than a `SharedEntity`.
"""
input SharedEntityFilter {
  id: IDFilter
  organizationId: IDFilter
  token: StringFilter
  entity: StringFilter
  entityId: IDFilter
  shareRules: JSONFilter
  sharedOptions: JSONFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [SharedEntityFilter!]
  or: [SharedEntityFilter!]
  not: SharedEntityFilter
}

"""
What a caller states when it creates a share.

The organization, the target pair and the rules are required because the delivered create validates
all four — the DTO marks the entity, the record identifier and the rules non-empty and intersects the
organization base. There is no member for the token: it is minted by the delivered service, and a
caller that could state one could choose a token somebody else's client already holds.

The delivered service refuses a target that does not belong to the caller's own tenant before it
writes anything, so a share cannot be minted over another tenant's record, and it refuses a target
whose own table carries no tenancy column at all — there would be nothing to scope the read by.
"""
input CreateSharedEntityInput {
  organizationId: ID!
  entity: String!
  entityId: ID!
  shareRules: JSON!
  sharedOptions: JSON
}

"""
What a caller states when it changes a share.

**The target is not a member, and neither is the token.** Both are pinned at creation: a share whose
target could move would be a token that reads a different record than the one it was issued for, and
the delivered edit body excludes both. The rules are the part that changes, and they are re-validated
against the target's own metadata when they do — the same check the creation performs, so a share
cannot be widened into a traversal the tenant boundary would not allow.
"""
input UpdateSharedEntityInput {
  id: ID!
  organizationId: ID!
  shareRules: JSON!
  sharedOptions: JSON
}

"""One thing a person can do, kept per organization."""
type Skill {
  id: ID!

  """
  The name of the skill, as entered. Read by the delivered name route and by the connection's own filter.
  """
  name: String!
  description: String

  """
  The colour the skill is shown in. The column is `NOT NULL` and carries no default, so a skill
  cannot be filed without one — which is why the create input requires it rather than treating it as
  a presentation detail.
  """
  color: String!
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the skill was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of skills."""
type SkillEdge {
  node: Skill!
  cursor: String!
}

"""
One page of skills, with the total the filters select and the boundary cursors.
"""
type SkillConnection {
  nodes: [Skill!]!
  edges: [SkillEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a skill list may be sorted by."""
enum SkillSortField {
  createdAt
  updatedAt
  name
}

"""The order the skills are returned in. At most three keys are accepted."""
input SkillSort {
  field: SkillSortField!
  direction: SortDirection!
}

"""
The fields a skill list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned rather than translating the filter into a query. `deletedAt` is
filterable so a caller can ask which rows have been withdrawn, while the read itself answers live rows
only.
"""
input SkillFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  description: StringFilter
  color: StringFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [SkillFilter!]
  or: [SkillFilter!]
  not: SkillFilter
}

"""
What a caller states when it files a skill.

The organization is optional, which is the delivered route's own contract rather than a relaxation:
the creation is inherited from the CRUD base and binds its body to a partial entity, so nothing
validates a member the row does not itself require. The name and the colour are the two the store
does require — the columns are `NOT NULL` and carry no default — and the tenant is never a member,
because the service stamps the caller's own tenant and overwrites whatever a body states.
"""
input CreateSkillInput {
  organizationId: ID
  name: String!
  description: String
  color: String!
  isActive: Boolean
  isArchived: Boolean
}

"""
What a caller states when it changes a skill that exists.

**A member the caller leaves out is left as it is.** The delivered edit is the CRUD base's partial
column update — it hands the store the members the body carries and writes them — so the identifier
is the only required member and the rest are stated only when they change.
"""
input UpdateSkillInput {
  id: ID!
  organizationId: ID
  name: String
  description: String
  color: String
  isActive: Boolean
  isArchived: Boolean
}

"""
The platform's own aggregates, as the statistics resource computes and answers them.

Every member is computed on the request: the counts are the number of rows the owning service's own
count answers, and the two amounts are the sums its own aggregation answers. Nothing here is stored, so
nothing here is cached or versioned, and two calls a second apart may legitimately disagree.
"""
type GlobalStats {
  """How many tenants the count of the tenant service answers."""
  tenants: Int!

  """How many employees the count of the employee service answers."""
  employees: Int!

  """How many tasks the count of the task service answers."""
  tasks: Int!

  """How many teams the count of the organization-team service answers."""
  teams: Int!

  """How many organizations the count of the organization service answers."""
  organizations: Int!

  """
  The user aggregate: the total the user service counts, and how many of them signed in during the last month.
  """
  users: UserStats!

  """
  The invoice aggregate: how many documents that are not estimates the invoice service counted, and
  what their total value adds up to.
  """
  invoices: InvoiceStats!

  """
  The payment aggregate: how many payment rows the payment service counted, and what their amounts add up to.
  """
  payments: PaymentStats!

  """
  The overall tracked time in hours, as the statistic service computes it: the summed duration of the
  platform's time logs, divided by 3600. A duration rather than an amount, so it carries its fraction.
  """
  hours: Float!
}

"""
The user aggregate: the total, and how many of them were active recently.
"""
type UserStats {
  """How many users the count of the user service answers."""
  count: Int!

  """How many users signed in during the last 30 days."""
  lastMonthActiveUsers: Int!
}

"""The invoice aggregate over the documents that are not estimates."""
type InvoiceStats {
  """
  How many invoices the count answers. Estimates are not among them: the delivered aggregate selects the documents whose `isEstimate` is false.
  """
  count: Int!

  """
  What the total value of those invoices adds up to, as an exact decimal.
  """
  amount: Decimal!
}

"""The payment aggregate over the recorded payments."""
type PaymentStats {
  """How many payment rows the count answers."""
  count: Int!

  """What the recorded amounts add up to, as an exact decimal."""
  amount: Decimal!
}

"""One grouping of labels."""
type TagType {
  id: ID!

  """
  The group's name as the operator writes it (`Category`, `Department`, `Priority`).
  """
  type: String!

  """
  When the group was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live group from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of tag types."""
type TagTypeEdge {
  node: TagType!
  cursor: String!
}

"""
One page of tag types, with the total the filters select and the boundary cursors.
"""
type TagTypeConnection {
  nodes: [TagType!]!
  edges: [TagTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a tag type list may be sorted by."""
enum TagTypeSortField {
  createdAt
  updatedAt
  type
}

"""
The order the tag types are returned in. At most three keys are accepted.
"""
input TagTypeSort {
  field: TagTypeSortField!
  direction: SortDirection!
}

"""
The fields a tag type list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

`type` is the whole of what a caller selects a group by, and the name is a column of the row rather
than a translated member, so an `eq` on it is exact and a `like` on it is the prefix search a picker
performs.

No member narrows by tenant or organization. Both are applied to the criterion by the delivered reader
from the credential rather than from the caller, so offering them here would suggest a scope decision
the caller does not have. `deletedAt` is not filterable for the reason the tag list gives: the reader
answers live rows only, so a condition on that column could only ever match the empty set.
"""
input TagTypeFilter {
  id: IDFilter
  type: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TagTypeFilter!]
  or: [TagTypeFilter!]
  not: TagTypeFilter
}

"""
What a caller states when it declares a group.

The organization is required here, unlike the tag's own create, and the asymmetry is the delivered
controllers': this route binds a body that requires an organization identifier (or the organization
itself, which this input does not offer), while the tag's binds one whose organization members are
partial. The tenant is not a member on either, for the same reason: the write stamps the caller's own
tenant onto the row and overwrites whatever a body states.

The group's tags are not a member. The collection is a reverse side rather than a column of this row,
so the delivered write has nothing to store it in; a tag is grouped by writing the tag's own
`tagTypeId`.
"""
input CreateTagTypeInput {
  """An identifier of one of the caller's own organizations."""
  organizationId: ID!

  """The group's name as the operator writes it."""
  type: String!
}

"""
What a caller states when it changes a group that exists.

**A member the caller leaves out is left as it is.** The delivered write reads the row, proves it is
the caller's, and then updates the columns the body states — a primitive `UPDATE`, never a replacement
of the row — so omitting `type` keeps the name the group already has.
"""
input UpdateTagTypeInput {
  id: ID!
  organizationId: ID
  type: String
}

"""A label the platform can attach to many kinds of record."""
type Tag {
  id: ID!

  """The label text."""
  name: String!

  """The chip's background colour, as the platform's colour string."""
  color: String!

  """The chip's text colour, when the background needs one."""
  textColor: String

  """What the label means, for the operator."""
  description: String

  """An icon name the clients resolve."""
  icon: String

  """
  Whether the tag is maintained by the platform rather than by an operator.
  """
  isSystem: Boolean

  """The icon resolved to a URL, for clients that render it directly."""
  fullIconUrl: String

  """The tag's grouping, when it has one."""
  tagTypeId: ID

  """
  When the tag was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes — the two removals this resource serves — and this column is the whole of what they write,
  so without it a caller could not tell a live tag from a withdrawn one on the answer to the write
  that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of tags."""
type TagEdge {
  node: Tag!
  cursor: String!
}

"""
One page of tags, with the total the filters select and the boundary cursors.
"""
type TagConnection {
  nodes: [Tag!]!
  edges: [TagEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a tag list may be sorted by."""
enum TagSortField {
  createdAt
  updatedAt
  name
  color
  isSystem
}

"""The order the tags are returned in. At most three keys are accepted."""
input TagSort {
  field: TagSortField!
  direction: SortDirection!
}

"""
The fields a tag list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member here is a column of the tag row, which is why the set is what it is: the two readers
behind this connection answer those columns, so each one narrows the rows the connection was handed
rather than a collection the reader would have had to load. `organizationTeamId` and `tagTypeId` are
identifiers of the two groupings a tag may carry, and `isNull` on either is the question "is this tag
grouped at all", which is a question the tag screen asks.

No member narrows by tenant or organization. The readers build that part of their criterion themselves:
the tenant comes from the credential on every read, and the organization is the one the caller is
acting in — which this surface takes from the credential and the routes' own clients send with their
request — so a filter on either would suggest a scope decision the caller does not have. Which rows a
caller may see is decided before the connection is handed them.

`deletedAt` is not filterable either, although it is carried on the object. Both readers answer live
rows only, so a condition on that column could only ever match the empty set — an argument that
cannot be honoured is worse than an argument that is not offered.
"""
input TagFilter {
  id: IDFilter
  name: StringFilter
  color: StringFilter
  textColor: StringFilter
  description: StringFilter
  icon: StringFilter
  isSystem: BooleanFilter
  tagTypeId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TagFilter!]
  or: [TagFilter!]
  not: TagFilter
}

"""
What a caller states when it files a tag.

The organization is optional here, and it is worth stating why: this resource's create route binds a
body whose organization members are partial, so a tag may be filed at the tenant level, where every
organization of the tenant sees it. The tag type's own create is stricter — see
`CreateTagTypeInput` — and that asymmetry is the controllers', not this surface's invention.

There is no member for the tenant: the delivered write stamps the caller's own tenant onto the row and
overwrites whatever a body states, so a member here would promise a scope the write refuses.

`isSystem` is not a member. The column is the platform's own mark on the tags it maintains, the
delivered create route does not accept it, and the readers leave those rows out — so a caller that
could set it would file a tag that disappears from every list the moment it is written.
"""
input CreateTagInput {
  """The label text."""
  name: String!

  """The chip's background colour, as the platform's colour string."""
  color: String!

  """What the label means, for the operator."""
  description: String

  """The chip's text colour, when the background needs one."""
  textColor: String

  """An icon name the clients resolve."""
  icon: String

  """The group to file the tag under. Omit for an ungrouped tag."""
  tagTypeId: ID

  """The team the tag belongs to. Omit for a tag of the whole organization."""
  organizationTeamId: ID

  """The organization the tag belongs to. Omit for a tenant-level tag."""
  organizationId: ID
}

"""
What a caller states when it changes a tag that exists.

**A member the caller leaves out is left as it is.** The delivered write reads the row, proves it is
the caller's, and then updates the columns the body states — a primitive `UPDATE`, never a
replacement of the row — so omitting `color` keeps the colour and omitting `tagTypeId` keeps the
grouping. A caller that means to clear an optional member states it as null.
"""
input UpdateTagInput {
  id: ID!
  name: String
  color: String
  description: String
  textColor: String
  icon: String
  tagTypeId: ID
  organizationTeamId: ID
  organizationId: ID
}

"""One unit of work."""
type Task {
  id: ID!

  """
  The task's title, which is what every list and every notification shows.
  """
  title: String!

  """
  The task's number within its project. Null on a task filed without a project.
  """
  number: Int

  """The project's prefix, stamped onto the task when it was filed."""
  prefix: String

  """What the task is about."""
  description: String

  """
  The task's lifecycle, as the denormalised value of the status row it points at (`taskStatusId`).
  Carried as its string value rather than declared as a schema enum: the value set is the status
  rows' own, and an operator who adds a status adds a row.
  """
  status: String

  """
  The task's urgency, as the value of the priority row it points at (`taskPriorityId`).
  """
  priority: String

  """
  The task's size, as the value of the size row it points at (`taskSizeId`).
  """
  size: String

  """
  The task's kind, as the value of the issue-type row it points at (`taskTypeId`).
  """
  issueType: String

  """
  How long the task is estimated to take, **in hours**. A duration rather than an amount, so a
  `Float` and not a `Decimal`: the delivered column documents its unit as hours and the estimate is
  frequently fractional.
  """
  estimate: Float

  """When work on the task is scheduled to start."""
  startDate: DateTime

  """When the task is due."""
  dueDate: DateTime

  """When the task was marked resolved."""
  resolvedAt: DateTime

  """Whether the task is visible beyond the people it is assigned to."""
  public: Boolean

  """The release or build the task is filed against, as free text."""
  version: String

  """
  Whether the task is still being written and is not yet offered for work.
  """
  isDraft: Boolean

  """
  Whether the task is a screening task — one that is decided on before it becomes work. The
  screening resource's own row is served by `ScreeningTask` beside this type.
  """
  isScreeningTask: Boolean

  """
  The parent task, when this task is a sub-task of one. Null on a root task.
  """
  parentId: ID

  """The project the task belongs to."""
  projectId: ID

  """The sprint the task is worked in."""
  organizationSprintId: ID

  """The status row the task points at. The row itself is a `TaskStatus`."""
  taskStatusId: ID

  """The size row the task points at. The row itself is a `TaskSize`."""
  taskSizeId: ID

  """
  The priority row the task points at. The row itself is a `TaskPriority`.
  """
  taskPriorityId: ID

  """
  The issue-type row the task points at. The row itself is an `IssueType`.
  """
  taskTypeId: ID

  """
  The epic this task descends from. Populated by the one-row query alone, and only when the caller
  asks for it: the delivered reader walks the parent chain with a recursive query that no list read
  performs, so on a connection's rows the member is absent.
  """
  rootEpic: Task

  """
  When the task was withdrawn, or null while it is live. Withdrawing a task is the delivered
  removal route, and this column is the whole of what it writes.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of tasks."""
type TaskEdge {
  node: Task!
  cursor: String!
}

"""
One page of tasks, with the total the filters select and the boundary cursors.
"""
type TaskConnection {
  nodes: [Task!]!
  edges: [TaskEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a task list may be sorted by."""
enum TaskSortField {
  createdAt
  updatedAt
  number
  title
  startDate
  dueDate
  resolvedAt
  status
  priority
  size
  issueType
  estimate
}

"""The order the tasks are returned in. At most three keys are accepted."""
input TaskSort {
  field: TaskSortField!
  direction: SortDirection!
}

"""
The fields a task list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the task row, which is what the connection protocol can evaluate over the
rows a delivered reader answers. The three narrowings that are *not* columns are deliberately absent,
each because it is a root field of its own rather than a filter that could not be honoured:

- who a task is assigned to is a `task_employee` pivot the list reader does not join, so it is
  `myTasks` and `employeeTasks`;
- which team works a task is a `task_team` pivot, so it is `teamTasks`;
- which module a task belongs to is a `project_module_task` pivot, so it is `moduleTasks`.

A filter on one of those would be evaluated against a row that carries none of them and would select
nothing at all, which is the worst answer a filter can give.
"""
input TaskFilter {
  id: IDFilter
  number: NumberFilter
  prefix: StringFilter
  title: StringFilter
  description: StringFilter
  status: StringFilter
  priority: StringFilter
  size: StringFilter
  issueType: StringFilter
  estimate: NumberFilter
  startDate: DateTimeFilter
  dueDate: DateTimeFilter
  resolvedAt: DateTimeFilter
  public: BooleanFilter
  version: StringFilter
  isDraft: BooleanFilter
  isScreeningTask: BooleanFilter
  parentId: IDFilter
  projectId: IDFilter
  organizationSprintId: IDFilter
  taskStatusId: IDFilter
  taskSizeId: IDFilter
  taskPriorityId: IDFilter
  taskTypeId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskFilter!]
  or: [TaskFilter!]
  not: TaskFilter
}

"""
What a caller states when it files a task.

The organization is stated because the delivered body states it and the handler stamps it onto the
row; the tenant is not, because the delivered handler reads it from the credential and overwrites
whatever a body carries.

The relations are stated as identifiers and handed to the write as the rows the delivered handler
reads: `members` are the employees the task is assigned to, `teams` the teams working it, `modules`
the project modules it belongs to, and `tags` the labels it carries. `mentionEmployeeIds` is not a
relation at all — it is the list the handler mentions after the row is written.
"""
input CreateTaskInput {
  organizationId: ID!
  title: String!
  description: String
  status: String
  priority: String
  size: String
  issueType: String

  """An estimate **in hours** — see `Task.estimate`."""
  estimate: Float
  startDate: DateTime
  dueDate: DateTime
  resolvedAt: DateTime
  public: Boolean
  version: String
  isDraft: Boolean
  isScreeningTask: Boolean
  parentId: ID
  projectId: ID
  organizationSprintId: ID
  taskStatusId: ID
  taskSizeId: ID
  taskPriorityId: ID
  taskTypeId: ID

  """
  The labels the task carries. Tags are the platform's one labelling concept.
  """
  tagIds: [ID!]

  """The employees the task is assigned to."""
  memberIds: [ID!]

  """The teams working the task."""
  teamIds: [ID!]

  """The project modules the task belongs to."""
  moduleIds: [ID!]

  """The employees the write mentions once the task exists."""
  mentionEmployeeIds: [ID!]
}

"""
What a caller states when it edits a task that exists.

The delivered edit route binds the same body the create route binds, so `organizationId` and `title`
are required here exactly as they are there. The handler dispatches the delivered update, which reads
the row, proves it is the caller's, and writes the columns the body states.
"""
input UpdateTaskInput {
  id: ID!
  organizationId: ID!
  title: String!
  description: String
  status: String
  priority: String
  size: String
  issueType: String

  """An estimate **in hours** — see `Task.estimate`."""
  estimate: Float
  startDate: DateTime
  dueDate: DateTime
  resolvedAt: DateTime
  public: Boolean
  version: String
  isDraft: Boolean
  isScreeningTask: Boolean
  parentId: ID
  projectId: ID
  organizationSprintId: ID
  taskStatusId: ID
  taskSizeId: ID
  taskPriorityId: ID
  taskTypeId: ID
  tagIds: [ID!]
  memberIds: [ID!]
  teamIds: [ID!]
  moduleIds: [ID!]
  mentionEmployeeIds: [ID!]

  """
  Why the task moved between sprints, when the delivered update moved it.
  """
  taskSprintMoveReason: String
}

"""
One person's estimate of how long a task takes.

The rows are the task's own: an estimation carries the task it belongs to, so a caller narrows the
connection it reads them through with `taskId` rather than reaching for a root field per task.
"""
type TaskEstimation {
  id: ID!

  """
  The estimate, **in hours**, exactly as `Task.estimate` states its unit.
  """
  estimate: Float!

  """The employee who made the estimate."""
  employeeId: ID!

  """The task the estimate is about."""
  taskId: ID!
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of estimations."""
type TaskEstimationEdge {
  node: TaskEstimation!
  cursor: String!
}

"""
One page of estimations, with the total the filters select and the boundary cursors.
"""
type TaskEstimationConnection {
  nodes: [TaskEstimation!]!
  edges: [TaskEstimationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an estimation list may be sorted by."""
enum TaskEstimationSortField {
  createdAt
  updatedAt
  estimate
}

"""
The order the estimations are returned in. At most three keys are accepted.
"""
input TaskEstimationSort {
  field: TaskEstimationSortField!
  direction: SortDirection!
}

"""
The fields an estimation list may be narrowed by. `taskId` is the narrowing that matters: it is how a
task's own estimates are read.
"""
input TaskEstimationFilter {
  id: IDFilter
  estimate: NumberFilter
  employeeId: IDFilter
  taskId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskEstimationFilter!]
  or: [TaskEstimationFilter!]
  not: TaskEstimationFilter
}

"""
What a caller states when it records an estimate. The delivered body requires every member, so every
member is required here.
"""
input CreateTaskEstimationInput {
  organizationId: ID!

  """An estimate **in hours**."""
  estimate: Float!
  employeeId: ID!
  taskId: ID!
}

"""
What a caller states when it changes an estimate. The delivered update binds the same body the create
binds, so the same members are required.
"""
input UpdateTaskEstimationInput {
  id: ID!
  organizationId: ID!

  """An estimate **in hours**."""
  estimate: Float!
  employeeId: ID!
  taskId: ID!
}

"""
One task's relation to another: it is blocked by it, it duplicates it, it relates to it, and so on.
"""
type TaskLinkedIssue {
  id: ID!

  """
  Which way the relation runs, as the integer the delivered column holds: `1` is-blocked-by, `2`
  blocks, `3` is-cloned-by, `4` clones, `5` is-duplicated-by, `6` duplicates, `7` relates-to. It is
  carried as its value rather than declared as a schema enum because it is a fixed contract value set
  rather than a vocabulary an operator maintains.
  """
  action: Int!

  """The task the relation runs from."""
  taskFromId: ID!

  """The task the relation runs to."""
  taskToId: ID!
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of linked issues."""
type TaskLinkedIssueEdge {
  node: TaskLinkedIssue!
  cursor: String!
}

"""
One page of linked issues, with the total the filters select and the boundary cursors.
"""
type TaskLinkedIssueConnection {
  nodes: [TaskLinkedIssue!]!
  edges: [TaskLinkedIssueEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a linked-issue list may be sorted by."""
enum TaskLinkedIssueSortField {
  createdAt
  updatedAt
  action
}

"""
The order the linked issues are returned in. At most three keys are accepted.
"""
input TaskLinkedIssueSort {
  field: TaskLinkedIssueSortField!
  direction: SortDirection!
}

"""
The fields a linked-issue list may be narrowed by. Narrowing on `taskFromId` or `taskToId` is how the
links of one task are read — the connection is the list, and a task's own links are this list filtered.
"""
input TaskLinkedIssueFilter {
  id: IDFilter
  action: NumberFilter
  taskFromId: IDFilter
  taskToId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskLinkedIssueFilter!]
  or: [TaskLinkedIssueFilter!]
  not: TaskLinkedIssueFilter
}

"""What a caller states when it links two tasks."""
input CreateTaskLinkedIssueInput {
  organizationId: ID!

  """The direction of the relation, as `TaskLinkedIssue.action` states it."""
  action: Int!
  taskFromId: ID!
  taskToId: ID!
}

"""
What a caller states when it changes an existing link. Every member but the identifier and the
organization is optional: the delivered update binds a partial body.
"""
input UpdateTaskLinkedIssueInput {
  id: ID!
  organizationId: ID!
  action: Int
  taskFromId: ID
  taskToId: ID
}

"""One lifecycle state a task can be in."""
type TaskStatus {
  id: ID!

  """The status as it reads to a person."""
  name: String!

  """The stable value the task's own `status` column carries."""
  value: String!

  """What the status means, for the operator."""
  description: String

  """Where the status sits in the board's column order."""
  order: Int

  """An icon name the clients resolve."""
  icon: String

  """The status's colour."""
  color: String

  """
  Whether the platform maintains the row rather than an operator. A system row cannot be removed.
  """
  isSystem: Boolean

  """Whether the board shows the column collapsed."""
  isCollapsed: Boolean

  """Whether the status is the scope's default for a newly filed task."""
  isDefault: Boolean

  """Whether the status means the work has not started."""
  isTodo: Boolean

  """Whether the status means the work is under way."""
  isInProgress: Boolean

  """Whether the status means the work is finished."""
  isDone: Boolean

  """The icon resolved to a URL, for clients that render it directly."""
  fullIconUrl: String

  """
  The project the row belongs to. Null on a row that belongs to the whole scope above it.
  """
  projectId: ID

  """The team the row belongs to. Null for the whole organization."""
  organizationTeamId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of statuses."""
type TaskStatusEdge {
  node: TaskStatus!
  cursor: String!
}

"""
One page of statuses, with the total the filters select and the boundary cursors.
"""
type TaskStatusConnection {
  nodes: [TaskStatus!]!
  edges: [TaskStatusEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a status list may be sorted by."""
enum TaskStatusSortField {
  createdAt
  updatedAt
  name
  value
  order
  isSystem
}

"""
The order the statuses are returned in. At most three keys are accepted.
"""
input TaskStatusSort {
  field: TaskStatusSortField!
  direction: SortDirection!
}

"""The fields a status list may be narrowed by."""
input TaskStatusFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  description: StringFilter
  order: NumberFilter
  icon: StringFilter
  color: StringFilter
  isSystem: BooleanFilter
  isCollapsed: BooleanFilter
  isDefault: BooleanFilter
  isTodo: BooleanFilter
  isInProgress: BooleanFilter
  isDone: BooleanFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskStatusFilter!]
  or: [TaskStatusFilter!]
  not: TaskStatusFilter
}

"""
What a caller states when it files a status.

`template` names one of the delivered workflow templates — `TODO`, `IN_PROGRESS` or `DONE` — and the
delivered create merges that workflow's flags onto the row it writes. It is a member of the delivered
body and of no column, which is why it is offered on the write and not on the type.

`isSystem` is not a member: the column is the platform's own mark on the rows it maintains, and a
caller that could set it would file a row the removal route refuses to remove.
"""
input CreateTaskStatusInput {
  organizationId: ID
  name: String!
  value: String!
  description: String
  order: Int
  icon: String
  color: String
  isCollapsed: Boolean
  isDefault: Boolean
  isTodo: Boolean
  isInProgress: Boolean
  isDone: Boolean
  projectId: ID
  organizationTeamId: ID
  template: String
}

"""
What a caller states when it changes a status. Every member but the identifier is optional.
"""
input UpdateTaskStatusInput {
  id: ID!
  organizationId: ID
  name: String
  value: String
  description: String
  order: Int
  icon: String
  color: String
  isCollapsed: Boolean
  isDefault: Boolean
  isTodo: Boolean
  isInProgress: Boolean
  isDone: Boolean
  projectId: ID
  organizationTeamId: ID
  template: String
}

"""
One step the status board is dragged into.

`order` is the whole member set: the delivered route writes that one column per row and never moves
the rows' other facts.
"""
input TaskStatusOrderInput {
  id: ID!
  order: Int!
}

"""
One step of a status reordering, as the delivered route answers it back.
"""
type TaskStatusOrder {
  id: ID!
  order: Int!
}

"""
The outcome of a status reordering: whether it succeeded, and the list it wrote.
"""
type TaskStatusReorderResult {
  success: Boolean!
  list: [TaskStatusOrder!]
}

"""One size a task can be sized as."""
type TaskSize {
  id: ID!
  name: String!
  value: String!
  description: String
  icon: String
  color: String

  """
  Whether the platform maintains the row rather than an operator. A system row cannot be removed.
  """
  isSystem: Boolean
  fullIconUrl: String
  projectId: ID
  organizationTeamId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of sizes."""
type TaskSizeEdge {
  node: TaskSize!
  cursor: String!
}

"""
One page of sizes, with the total the filters select and the boundary cursors.
"""
type TaskSizeConnection {
  nodes: [TaskSize!]!
  edges: [TaskSizeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a size list may be sorted by."""
enum TaskSizeSortField {
  createdAt
  updatedAt
  name
  value
  isSystem
}

"""The order the sizes are returned in. At most three keys are accepted."""
input TaskSizeSort {
  field: TaskSizeSortField!
  direction: SortDirection!
}

"""The fields a size list may be narrowed by."""
input TaskSizeFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  description: StringFilter
  icon: StringFilter
  color: StringFilter
  isSystem: BooleanFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskSizeFilter!]
  or: [TaskSizeFilter!]
  not: TaskSizeFilter
}

"""What a caller states when it files a size."""
input CreateTaskSizeInput {
  organizationId: ID
  name: String!
  value: String!
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""
What a caller states when it changes a size. Every member but the identifier is optional.
"""
input UpdateTaskSizeInput {
  id: ID!
  organizationId: ID
  name: String
  value: String
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""One urgency a task can be worked at."""
type TaskPriority {
  id: ID!
  name: String!
  value: String!
  description: String
  icon: String
  color: String

  """
  Whether the platform maintains the row rather than an operator. A system row cannot be removed.
  """
  isSystem: Boolean
  fullIconUrl: String
  projectId: ID
  organizationTeamId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of priorities."""
type TaskPriorityEdge {
  node: TaskPriority!
  cursor: String!
}

"""
One page of priorities, with the total the filters select and the boundary cursors.
"""
type TaskPriorityConnection {
  nodes: [TaskPriority!]!
  edges: [TaskPriorityEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a priority list may be sorted by."""
enum TaskPrioritySortField {
  createdAt
  updatedAt
  name
  value
  isSystem
}

"""
The order the priorities are returned in. At most three keys are accepted.
"""
input TaskPrioritySort {
  field: TaskPrioritySortField!
  direction: SortDirection!
}

"""The fields a priority list may be narrowed by."""
input TaskPriorityFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  description: StringFilter
  icon: StringFilter
  color: StringFilter
  isSystem: BooleanFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskPriorityFilter!]
  or: [TaskPriorityFilter!]
  not: TaskPriorityFilter
}

"""What a caller states when it files a priority."""
input CreateTaskPriorityInput {
  organizationId: ID
  name: String!
  value: String!
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""
What a caller states when it changes a priority. Every member but the identifier is optional.
"""
input UpdateTaskPriorityInput {
  id: ID!
  organizationId: ID
  name: String
  value: String
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""One release a task can be filed against."""
type TaskVersion {
  id: ID!
  name: String!
  value: String!
  description: String
  icon: String
  color: String

  """
  Whether the platform maintains the row rather than an operator. A system row cannot be removed.
  """
  isSystem: Boolean
  fullIconUrl: String
  projectId: ID
  organizationTeamId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of versions."""
type TaskVersionEdge {
  node: TaskVersion!
  cursor: String!
}

"""
One page of versions, with the total the filters select and the boundary cursors.
"""
type TaskVersionConnection {
  nodes: [TaskVersion!]!
  edges: [TaskVersionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a version list may be sorted by."""
enum TaskVersionSortField {
  createdAt
  updatedAt
  name
  value
  isSystem
}

"""
The order the versions are returned in. At most three keys are accepted.
"""
input TaskVersionSort {
  field: TaskVersionSortField!
  direction: SortDirection!
}

"""The fields a version list may be narrowed by."""
input TaskVersionFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  description: StringFilter
  icon: StringFilter
  color: StringFilter
  isSystem: BooleanFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskVersionFilter!]
  or: [TaskVersionFilter!]
  not: TaskVersionFilter
}

"""What a caller states when it files a version."""
input CreateTaskVersionInput {
  organizationId: ID
  name: String!
  value: String!
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""
What a caller states when it changes a version. Every member but the identifier is optional.
"""
input UpdateTaskVersionInput {
  id: ID!
  organizationId: ID
  name: String
  value: String
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""
One kind of work a task can be: an epic, a story, a task, a bug or a memo.
"""
type IssueType {
  id: ID!
  name: String!
  value: String!
  description: String
  icon: String
  color: String

  """Whether the type is the scope's default for a newly filed task."""
  isDefault: Boolean

  """
  Whether the platform maintains the row rather than an operator. A system row cannot be removed.
  """
  isSystem: Boolean
  fullIconUrl: String

  """
  The asset the clients show for the type, when it carries one rather than an icon name.
  """
  imageId: ID
  projectId: ID
  organizationTeamId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of issue types."""
type IssueTypeEdge {
  node: IssueType!
  cursor: String!
}

"""
One page of issue types, with the total the filters select and the boundary cursors.
"""
type IssueTypeConnection {
  nodes: [IssueType!]!
  edges: [IssueTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an issue-type list may be sorted by."""
enum IssueTypeSortField {
  createdAt
  updatedAt
  name
  value
  isDefault
  isSystem
}

"""
The order the issue types are returned in. At most three keys are accepted.
"""
input IssueTypeSort {
  field: IssueTypeSortField!
  direction: SortDirection!
}

"""The fields an issue-type list may be narrowed by."""
input IssueTypeFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  description: StringFilter
  icon: StringFilter
  color: StringFilter
  isDefault: BooleanFilter
  isSystem: BooleanFilter
  imageId: IDFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [IssueTypeFilter!]
  or: [IssueTypeFilter!]
  not: IssueTypeFilter
}

"""
What a caller states when it files an issue type.

`value` is not a member: the delivered body omits it, and the delivered write derives it from `name`
the way the platform's own types are derived.
"""
input CreateIssueTypeInput {
  organizationId: ID
  name: String!
  description: String
  icon: String
  color: String
  isDefault: Boolean
  imageId: ID
  projectId: ID
  organizationTeamId: ID
}

"""
What a caller states when it changes an issue type. Every member but the identifier is optional.
"""
input UpdateIssueTypeInput {
  id: ID!
  organizationId: ID
  name: String
  description: String
  icon: String
  color: String
  isDefault: Boolean
  imageId: ID
  projectId: ID
  organizationTeamId: ID
}

"""
One kind of relation a task can have to another: a duplicate of it, a blocker of it, and so on.
"""
type TaskRelatedIssueType {
  id: ID!
  name: String!
  value: String!
  description: String
  icon: String
  color: String

  """
  Whether the platform maintains the row rather than an operator. A system row cannot be removed.
  """
  isSystem: Boolean
  fullIconUrl: String
  projectId: ID
  organizationTeamId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of related-issue types."""
type TaskRelatedIssueTypeEdge {
  node: TaskRelatedIssueType!
  cursor: String!
}

"""
One page of related-issue types, with the total the filters select and the boundary cursors.
"""
type TaskRelatedIssueTypeConnection {
  nodes: [TaskRelatedIssueType!]!
  edges: [TaskRelatedIssueTypeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a related-issue-type list may be sorted by."""
enum TaskRelatedIssueTypeSortField {
  createdAt
  updatedAt
  name
  value
  isSystem
}

"""
The order the related-issue types are returned in. At most three keys are accepted.
"""
input TaskRelatedIssueTypeSort {
  field: TaskRelatedIssueTypeSortField!
  direction: SortDirection!
}

"""The fields a related-issue-type list may be narrowed by."""
input TaskRelatedIssueTypeFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  description: StringFilter
  icon: StringFilter
  color: StringFilter
  isSystem: BooleanFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskRelatedIssueTypeFilter!]
  or: [TaskRelatedIssueTypeFilter!]
  not: TaskRelatedIssueTypeFilter
}

"""What a caller states when it files a related-issue type."""
input CreateTaskRelatedIssueTypeInput {
  organizationId: ID
  name: String!
  value: String!
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""
What a caller states when it changes a related-issue type. Every member but the identifier is optional.
"""
input UpdateTaskRelatedIssueTypeInput {
  id: ID!
  organizationId: ID
  name: String
  value: String
  description: String
  icon: String
  color: String
  projectId: ID
  organizationTeamId: ID
}

"""
The scope one vocabulary row is made the default of.

The three members are the three the delivered reader scopes a status or an issue type by, and the
write marks the named row the default of exactly that scope: the whole organization when none of the
narrower two is stated, one project when `projectId` is, one team when `organizationTeamId` is.
"""
input TaskMetadataScopeInput {
  organizationId: ID
  organizationTeamId: ID
  projectId: ID
}

"""
A saved set of task filters, and how its result is presented.

The three document members are the view's own: `queryParams` is the filter set the delivered task
reader replays to answer the view's tasks, and `filterOptions` and `displayOptions` are what the board
renders it with. They are carried as `JSON` because they are documents rather than columns with a
shape this schema could state.
"""
type TaskView {
  id: ID!

  """The view's name, as the board lists it."""
  name: String!

  """What the view is for."""
  description: String

  """
  Who the view is visible to, as the integer the delivered column holds: `0` module-and-sprint, `1`
  team-and-project, `2` organization, `3` workspace. Carried as its value because it is a fixed
  contract value set rather than a vocabulary an operator maintains.
  """
  visibilityLevel: Int

  """
  The filter set the delivered task reader replays to answer this view's tasks.
  """
  queryParams: JSON

  """What the board renders the result with."""
  filterOptions: JSON

  """How the board lays the result out."""
  displayOptions: JSON

  """Which columns the board shows."""
  properties: JSON

  """
  Whether the view is protected from edits. The delivered create never sets it and the column is not updateable.
  """
  isLocked: Boolean
  projectId: ID
  organizationTeamId: ID
  projectModuleId: ID
  organizationSprintId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of views."""
type TaskViewEdge {
  node: TaskView!
  cursor: String!
}

"""
One page of views, with the total the filters select and the boundary cursors.
"""
type TaskViewConnection {
  nodes: [TaskView!]!
  edges: [TaskViewEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a view list may be sorted by."""
enum TaskViewSortField {
  createdAt
  updatedAt
  name
  visibilityLevel
}

"""The order the views are returned in. At most three keys are accepted."""
input TaskViewSort {
  field: TaskViewSortField!
  direction: SortDirection!
}

"""The fields a view list may be narrowed by."""
input TaskViewFilter {
  id: IDFilter
  name: StringFilter
  description: StringFilter
  visibilityLevel: NumberFilter
  projectId: IDFilter
  organizationTeamId: IDFilter
  projectModuleId: IDFilter
  organizationSprintId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TaskViewFilter!]
  or: [TaskViewFilter!]
  not: TaskViewFilter
}

"""What a caller states when it saves a view."""
input CreateTaskViewInput {
  organizationId: ID!
  name: String!
  description: String
  visibilityLevel: Int
  queryParams: JSON
  filterOptions: JSON
  displayOptions: JSON
  properties: JSON
  projectId: ID
  organizationTeamId: ID
  projectModuleId: ID
  organizationSprintId: ID
}

"""
What a caller states when it changes a saved view. Every member but the identifier is optional.
"""
input UpdateTaskViewInput {
  id: ID!
  organizationId: ID
  name: String
  description: String
  visibilityLevel: Int
  queryParams: JSON
  filterOptions: JSON
  displayOptions: JSON
  properties: JSON
  projectId: ID
  organizationTeamId: ID
  projectModuleId: ID
  organizationSprintId: ID
}

"""
One day's plan for one person: how much work the day holds and which tasks are in it.
"""
type DailyPlan {
  id: ID!

  """The day the plan is for."""
  date: DateTime!

  """
  How much work the day reserves, **in minutes**. The delivered column is an exact decimal, carried
  as a `Float` because a duration genuinely has a fractional part and this is not an amount.
  """
  workTimePlanned: Float!

  """
  How far the day has come, as the delivered column's own value: `open`, `in-progress` or
  `completed`.
  """
  status: String!

  """The person the day belongs to."""
  employeeId: ID

  """The team the day is worked in, when it belongs to one."""
  organizationTeamId: ID
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of daily plans."""
type DailyPlanEdge {
  node: DailyPlan!
  cursor: String!
}

"""
One page of daily plans, with the total the filters select and the boundary cursors.
"""
type DailyPlanConnection {
  nodes: [DailyPlan!]!
  edges: [DailyPlanEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a daily-plan list may be sorted by."""
enum DailyPlanSortField {
  createdAt
  updatedAt
  date
  workTimePlanned
  status
}

"""
The order the daily plans are returned in. At most three keys are accepted.
"""
input DailyPlanSort {
  field: DailyPlanSortField!
  direction: SortDirection!
}

"""
The fields a daily-plan list may be narrowed by.

The plan's tasks are **not** filterable. They live in the `daily_plan_task` pivot, which none of the
plan readers this surface mirrors joins, so a filter on them could only ever select the empty set.
Which plans carry a task is `dailyPlansForTask` — a root field of its own, answered by a reader that
does join the pivot.
"""
input DailyPlanFilter {
  id: IDFilter
  date: DateTimeFilter
  workTimePlanned: NumberFilter
  status: StringFilter
  employeeId: IDFilter
  organizationTeamId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [DailyPlanFilter!]
  or: [DailyPlanFilter!]
  not: DailyPlanFilter
}

"""
What a caller states when it opens a day's plan.

`taskId` is the one task the delivered create files into the new plan; the plan's remaining tasks are
added by the two membership writes beside it.
"""
input CreateDailyPlanInput {
  organizationId: ID!
  date: DateTime!

  """How much work the day reserves, **in minutes**."""
  workTimePlanned: Float!
  status: String!
  employeeId: ID
  organizationTeamId: ID

  """The task to file into the new plan."""
  taskId: ID
}

"""
What a caller states when it changes a day's plan. Every member but the identifier and the
organization is optional.
"""
input UpdateDailyPlanInput {
  id: ID!
  organizationId: ID!
  date: DateTime

  """How much work the day reserves, **in minutes**."""
  workTimePlanned: Float
  status: String
  employeeId: ID
  organizationTeamId: ID
}

"""
One task's membership of one day's plan, as the two membership writes state it.
"""
input DailyPlanTaskInput {
  organizationId: ID!
  taskId: ID!
  employeeId: ID!
}

"""The tasks to take out of every plan they are in, for one employee."""
input DailyPlanTaskRemovalInput {
  organizationId: ID!
  employeeId: ID!

  """
  The plans to consider. An empty list means every plan the employee holds for the task.
  """
  planIds: [ID!]
}

"""
A task that is being screened before it becomes work: a decision, and the row it is a decision about.
"""
type ScreeningTask {
  id: ID!

  """
  Where the decision stands, as the delivered column's own value: `accepted`, `declined`,
  `duplicated`, `snoozed` or `pending`.
  """
  status: String!

  """When a snoozed decision comes back, or null while it is not on hold."""
  onHoldUntil: DateTime

  """The task the decision is about."""
  taskId: ID!
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of screening tasks."""
type ScreeningTaskEdge {
  node: ScreeningTask!
  cursor: String!
}

"""
One page of screening tasks, with the total the filters select and the boundary cursors.
"""
type ScreeningTaskConnection {
  nodes: [ScreeningTask!]!
  edges: [ScreeningTaskEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a screening-task list may be sorted by."""
enum ScreeningTaskSortField {
  createdAt
  updatedAt
  status
  onHoldUntil
}

"""
The order the screening tasks are returned in. At most three keys are accepted.
"""
input ScreeningTaskSort {
  field: ScreeningTaskSortField!
  direction: SortDirection!
}

"""The fields a screening-task list may be narrowed by."""
input ScreeningTaskFilter {
  id: IDFilter
  status: StringFilter
  onHoldUntil: DateTimeFilter
  taskId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ScreeningTaskFilter!]
  or: [ScreeningTaskFilter!]
  not: ScreeningTaskFilter
}

"""
What a caller states when it puts a task up for screening.

The delivered create **files the task too**: the `task` member is the body of the task to be created,
and the delivered handler writes it before it writes the decision. `taskId` is required by the
delivered body and then overwritten by the identifier of the task the handler just created — it is
stated here because a body the REST route refuses must not be a body this surface accepts.
"""
input CreateScreeningTaskInput {
  organizationId: ID!

  """The task to file and then screen."""
  task: CreateTaskInput!

  """
  The identifier the delivered body requires; the handler replaces it with the task it writes.
  """
  taskId: ID!

  """The employees the write mentions once the task exists."""
  mentionEmployeeIds: [ID!]
}

"""
What a caller states when it records where a decision stands. The delivered body requires the status
and omits the task: which task is being screened is not a thing a decision changes.
"""
input UpdateScreeningTaskInput {
  id: ID!
  organizationId: ID!
  status: String!
  onHoldUntil: DateTime
}

"""Which sections of the vocabulary a caller asks for by name."""
enum TaskMetadataSection {
  taskStatuses
  taskPriorities
  taskSizes
  taskLabels
  taskVersions
  issueTypes
  relatedIssueTypes
}

"""
The vocabulary one board needs, answered in one request.

Each member is the connection its own root field answers, narrowed the same way and built by the same
codec, so a caller that walks a page here can resume the walk on that root field. `taskLabels` is the
tag domain's own connection: the platform has one labelling concept and this is it, carried here
rather than re-declared.
"""
type TaskMetadata {
  taskStatuses: TaskStatusConnection
  taskPriorities: TaskPriorityConnection
  taskSizes: TaskSizeConnection
  taskLabels: TagConnection
  taskVersions: TaskVersionConnection
  issueTypes: IssueTypeConnection
  relatedIssueTypes: TaskRelatedIssueTypeConnection
}

"""
The pair a caller has just had issued: the identifier it sends on every request, and the secret that
proves the request is its own.

The secret is present on this answer and nowhere else in the schema, which is what the delivered
issuance route does with it: it is shown once, at the moment it is generated, and no read can produce
it again.
"""
type TenantApiKeyPair {
  """
  The tenant the pair authenticates as. Read from the credential, never chosen by the caller.
  """
  tenantId: ID!

  """
  The label the caller gave the pair, for telling two of them apart in an operator's notes.
  """
  name: String

  """
  The public half of the pair. It travels as the application identifier on every request the machine
  caller makes.
  """
  apiKey: String!

  """
  The secret half, in clear text, on this answer only. It is stored hashed, so no later read — over
  either protocol — can produce it again.
  """
  apiSecret: String!
}

"""What a caller states when it has a pair issued."""
input GenerateTenantApiKeyInput {
  """The label to store with the pair."""
  name: String
}

"""
One tenant: the customer record every other row of this installation is scoped by.
"""
type Tenant {
  id: ID!
  name: String!

  """
  A denormalised logo reference, kept for readers that must not join the asset table.
  """
  logo: String

  """The asset the interface shows for the tenant."""
  imageId: ID
  image: ImageAsset

  """
  The billing account this tenant is charged through. Null on an installation that bills nobody,
  which is every self-hosted one.
  """
  stripeCustomerId: String

  """The tenant's standard working day, in whole hours, between 1 and 24."""
  standardWorkHoursPerDay: Int
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""
What a caller states when it creates a tenant.

The members are the delivered creation body's own, and the identifier of the new tenant is not among
them: the row's identifier is generated by the store, and the caller that files a tenant is a user who
has none yet — assigning it afterwards is the delivered onboarding's own work, not a member of the
write.

The three import members are carried although a caller normally states none of them. They are the
delivered body's own — a migration states them so the new tenant's rows can be correlated with the
rows they were copied from — and the platform anonymises them in its documentation for the same
reason it validates them: they are only meaningful to a migration. Offering them costs a client that
never migrates nothing, and refusing them would refuse a body the delivered route serves.
"""
input CreateTenantInput {
  name: String!
  logo: String
  imageId: ID

  """
  Whether this creation is part of a migration, in which case the source identifiers are recorded.
  """
  isImporting: Boolean

  """
  The identifier the tenant had in the installation it is being migrated from.
  """
  sourceId: String

  """
  The identifier the creating user had in the installation it is being migrated from.
  """
  userSourceId: String
}

"""
What a caller states when it edits its own tenant.

The identifier is not a member, and that is the shape of the delivered route rather than a
simplification: `PUT /api/tenant` takes no path parameter and reads the tenant from the credential, so
there is nothing for a caller to name and nothing for this input to carry. Every member is the edit
body's own, and the name is required because the delivered body requires it — an edit states the name
it wants rather than patching around the one on the row.
"""
input UpdateTenantInput {
  name: String!
  logo: String
  imageId: ID
}

"""One configurable knob, addressed by scope."""
type TenantSetting {
  id: ID!

  """
  The key. The column is named `name` and it stays named `name`: renaming a NOT NULL core column is
  not an additive change, so the delivered uniqueness rules name it, and so does this type.
  """
  name: String!

  """
  The structured value: an object, an array, a number or a boolean. A document is carried as `JSON`
  and never as a string of JSON, so a client reads it as the document it is.
  """
  valueJson: JSON

  """The scalar value, on a row written by any delivered route."""
  value: String

  """
  How the stored value must be read. Validated against the value on write.
  """
  valueType: String!

  """
  The value is sensitive: masked in the settings document and excluded from the settings dump.
  """
  isEncrypted: Boolean!

  """Admin-facing explanation of the key."""
  description: String

  """
  The tenant-wide scope: null means this row is the answer for every organization of the tenant.
  """
  organizationId: ID

  """The channel scope: set exactly when `scope` is `CHANNEL`."""
  channelId: ID

  """
  The scope the row is addressed at: `TENANT` for every organization of the tenant, `ORGANIZATION`
  for one of them, `CHANNEL` for one channel of one of them. The resolution layers them most specific
  first, so the most specific row that exists is the answer.
  """
  scope: TenantSettingScope!

  """The tenant the row belongs to."""
  tenantId: ID

  """
  When the row was withdrawn, or null while it is live. Withdrawing and restoring are inherited
  delivered routes and this column is the whole of what they write, so without it a caller could not
  tell a live row from a withdrawn one on the answer to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
}

"""The scope a setting row is addressed at."""
enum TenantSettingScope {
  TENANT
  ORGANIZATION
  CHANNEL
}

"""Comparison on the scope."""
input TenantSettingScopeFilter {
  eq: TenantSettingScope
  ne: TenantSettingScope
  in: [TenantSettingScope!]
  nin: [TenantSettingScope!]
  isNull: Boolean
}

"""The boundary of one page of settings."""
type TenantSettingEdge {
  node: TenantSetting!
  cursor: String!
}

"""
One page of settings, with the total the filters select and the boundary cursors.
"""
type TenantSettingConnection {
  nodes: [TenantSetting!]!
  edges: [TenantSettingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a settings list may be sorted by."""
enum TenantSettingSortField {
  createdAt
  updatedAt
  name
  scope
  isEncrypted
}

"""
The order the settings are returned in. At most three keys are accepted.
"""
input TenantSettingSort {
  field: TenantSettingSortField!
  direction: SortDirection!
}

"""
The fields a settings list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

The three scope columns are filterable, and `organizationId: { isNull: true }` is how a caller reads
the tenant-wide rows alone — the delivered global route's selection, stated as the filter it is.
"""
input TenantSettingFilter {
  id: IDFilter
  name: StringFilter
  value: StringFilter
  valueJson: JSONFilter
  valueType: StringFilter
  isEncrypted: BooleanFilter
  description: StringFilter
  organizationId: IDFilter
  channelId: IDFilter
  scope: TenantSettingScopeFilter
  tenantId: IDFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TenantSettingFilter!]
  or: [TenantSettingFilter!]
  not: TenantSettingFilter
}

"""
One key and the value to store under it.

The delivered dynamic route accepts any key-value pairs, and a GraphQL input is a closed shape — a
map cannot be stated as one. A list of pairs is what a map *is* once it has to be written out, so the
write takes the pairs and the resolver folds them into the document the service reads. The scalar
column is what every delivered write stores, which is why the value member is a string and not a
document: offering a structured member here would promise a column no delivered route writes.
"""
input TenantSettingValueInput {
  """The setting key, as it is stored in the `name` column."""
  name: String!

  """The value to store for the key."""
  value: String!
}

"""
What a caller states when it saves the tenant's settings document.

The members are the delivered body's own: the file storage provider this tenant uses, and the
credentials of each provider the platform can be configured with. They are all nullable here although
the delivered DTO validates the chosen provider's members, because the validation the route performs
is conditional on `fileStorageProvider` — a caller that states one provider's credentials and no
provider name is a body the route accepts, and a schema that refused it would refuse a caller REST
serves.
"""
input UpdateTenantSettingInput {
  """The file storage provider this tenant's files are written to."""
  fileStorageProvider: String

  """Wasabi credentials and bucket, read when the provider is Wasabi."""
  wasabi_aws_access_key_id: String
  wasabi_aws_secret_access_key: String
  wasabi_aws_bucket: String
  wasabi_aws_default_region: String
  wasabi_aws_service_url: String
  wasabi_aws_force_path_style: Boolean

  """
  DigitalOcean Spaces credentials and bucket, read when the provider is DigitalOcean.
  """
  digitalocean_access_key_id: String
  digitalocean_secret_access_key: String
  digitalocean_s3_bucket: String
  digitalocean_service_url: String
  digitalocean_cdn_url: String
  digitalocean_default_region: String
  digitalocean_s3_force_path_style: Boolean

  """AWS S3 credentials and bucket, read when the provider is S3."""
  aws_access_key_id: String
  aws_secret_access_key: String
  aws_default_region: String
  aws_bucket: String
  aws_force_path_style: Boolean

  """Cloudinary credentials, read when the provider is Cloudinary."""
  cloudinary_cloud_name: String
  cloudinary_api_key: String
  cloudinary_api_secret: String
  cloudinary_api_secure: String
}

"""
What a caller states when it saves settings it names itself.

The delivered dynamic route accepts any pairs at all — the monitoring keys and the UI preference
travel through this door — so the input is a list of pairs rather than the closed member list above.
"""
input UpdateDynamicTenantSettingInput {
  """The settings to save, one member per key."""
  settings: [TenantSettingValueInput!]!
}

"""
What a caller states when it edits one settings row.

The delivered edit is the CRUD base's own — a partial body with no validation pipe — so it can write
every writable column of the row, and this input states them. `id` is required because the delivered
route takes the identifier from the path; stating it in the body as well is what keeps the two
readings of one identifier from disagreeing.
"""
input UpdateTenantSettingRowInput {
  id: ID!
  name: String
  value: String
  valueJson: JSON
  valueType: String
  isEncrypted: Boolean
  description: String

  """The organization the row belongs to. Null makes the row tenant-wide."""
  organizationId: ID

  """
  The channel the row is scoped to; state it exactly when `scope` is `CHANNEL`.
  """
  channelId: ID
  scope: TenantSettingScope
}

"""
What a caller states when it asks the platform to verify a Wasabi configuration.

Every member is nullable, and that is the delivered route's own shape rather than a relaxation: the
DTO it binds validates each member only when a `fileStorageProvider` member it does not itself declare
equals Wasabi, so nothing in the body is required there either. The service is what refuses a
configuration missing the two credentials, and its refusal is what a caller reads from this field too.
"""
input WasabiFileStorageConfigInput {
  wasabi_aws_access_key_id: String
  wasabi_aws_secret_access_key: String
  wasabi_aws_bucket: String
  wasabi_aws_default_region: String
  wasabi_aws_service_url: String
  wasabi_aws_force_path_style: Boolean
}

"""
The UI flavour this tenant's users are served.

One member, because that is the whole of what the preference is today: which of the two flavours of a
page that ships in both is rendered. It is a plain tenant setting — no secret, no masking — and it
surfaces in the settings document like any other row.
"""
type TenantUiPreferences {
  """
  The preferred flavour. Carried as its value rather than declared as a schema enum: the vocabulary
  is the platform contract's own `PreferredUiEnum`, whose wire values are lowercase, and a second
  domain that reads the same preference would then collide with this file rather than reuse it.
  """
  preferredUi: String!
}

"""What a caller states when it changes the tenant's UI flavour."""
input UpdateTenantUiPreferencesInput {
  """The flavour to serve. Omit to leave the preference as it is."""
  preferredUi: String
}

"""
One legal document a new account must accept, as the corpus publishes it.
"""
type TermsAcceptanceDocument {
  """The stable document identifier, `<document>:<product>`."""
  documentId: String!

  """The published version of the document."""
  version: String!

  """
  The lowercase hex digest of the exact document source this version publishes.
  
  This is the value a form displays and posts back, and the value the server checks a claim against.
  """
  sha256: String!

  """The locale of the rendering that is being shown."""
  locale: String!

  """
  Where the person can read the document, when the deployment publishes a link for it.
  """
  url: String

  """
  A human title for the checkbox or the modal, when the corpus carries one.
  """
  title: String

  """The date this version takes effect, when the corpus declares one."""
  effectiveDate: String
}

"""
One employee's leave balance under one policy for one year.

Every count is a number of **days**, fractional because half a day is an entitlement the platform
grants, and exact because the ledger has to agree with itself.
"""
type TimeOffBalance {
  id: ID!

  """The year the balance is tracked for."""
  year: Int!

  """Days accrued so far this year."""
  accrued: Decimal!

  """Days taken through approved time off requests."""
  taken: Decimal!

  """Days carried forward into this year from the previous one."""
  carriedForward: Decimal!

  """Days already rolled out of this year into the next one."""
  carriedOut: Decimal!

  """
  The days still available, and a computation rather than a column a caller may write:
  `accrued + carriedForward - taken - carriedOut`, never below zero. It is what the delivered writes
  recompute on every allocation, deduction, reversal and carry-forward.
  """
  remaining: Decimal!

  """
  The employee the balance belongs to. Read the engagement itself from the employee's own surface.
  """
  employeeId: ID!

  """The policy the balance is tracked against."""
  policyId: ID!

  """
  The policy itself, filled by the list read, which loads it beside every row. The writes answer rows
  read without it, exactly as their REST answers carry no policy object either.
  """
  policy: TimeOffPolicy
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """When the row was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of balances."""
type TimeOffBalanceEdge {
  node: TimeOffBalance!
  cursor: String!
}

"""
One page of balances, with the total the filters select and the boundary cursors.
"""
type TimeOffBalanceConnection {
  nodes: [TimeOffBalance!]!
  edges: [TimeOffBalanceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a balance list may be sorted by."""
enum TimeOffBalanceSortField {
  year
  accrued
  taken
  carriedForward
  carriedOut
  remaining
  createdAt
  updatedAt
}

"""
The order the balances are returned in. At most three keys are accepted.
"""
input TimeOffBalanceSort {
  field: TimeOffBalanceSortField!
  direction: SortDirection!
}

"""
The fields a balance list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, which is why the day counts compare as exact decimals and the year
as a whole number. The policy is not a member: the row carries a policy object at `policy`, and the
protocol narrows by comparing a value, so `policyId` is how a caller asks for one policy's balances.
"""
input TimeOffBalanceFilter {
  id: IDFilter
  year: NumberFilter
  accrued: DecimalFilter
  taken: DecimalFilter
  carriedForward: DecimalFilter
  carriedOut: DecimalFilter
  remaining: DecimalFilter
  employeeId: IDFilter
  policyId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimeOffBalanceFilter!]
  or: [TimeOffBalanceFilter!]
  not: TimeOffBalanceFilter
}

"""
What a caller states when it allocates accrued days.

The organization is stated because the delivered write verifies the employee and the policy against it
and will not touch either from another one; the tenant is stamped from the credential, as it is on every
write of this platform, and is therefore not a member.

`accrued` **replaces** the stored figure rather than being added to it, which is what makes re-running
an allocation for a period idempotent.
"""
input AllocateTimeOffBalanceInput {
  """The organization the employee and the policy must belong to."""
  organizationId: ID!

  """The employee the balance belongs to."""
  employeeId: ID!

  """The policy the balance is tracked against."""
  policyId: ID!

  """The year the balance is tracked for."""
  year: Int!

  """The days accrued for the period. Replaces the stored value."""
  accrued: Decimal!
}

"""
What a caller states when it spends days from a balance, or gives them back.

`days` is always positive on both operations — the field decides the direction — so a negative value can
never turn a deduction into a grant. The two operations share one body because they move the same
column by the same amount in the two directions, and the delivered service states the same split: the
input is one, and which of the two writes receives it is the field's own.
"""
input AdjustTimeOffBalanceInput {
  """The organization the employee and the policy must belong to."""
  organizationId: ID!

  """The employee the balance belongs to."""
  employeeId: ID!

  """The policy the balance is tracked against."""
  policyId: ID!

  """The year the balance is tracked for."""
  year: Int!

  """
  How many days to move. Always positive; the field decides the direction.
  """
  days: Decimal!
}

"""
What a caller states when it rolls one policy's unused days into the next year.

The organization is stated for the same reason the allocation states it: the delivered write verifies
the policy against it. The cap is optional and the delivered rule is stated rather than restated: an
explicit cap wins, otherwise the policy's own `maxCarryForwardDays` applies, and `0` or an omitted cap
means no cap at all.
"""
input CarryForwardTimeOffBalanceInput {
  """The organization the policy must belong to."""
  organizationId: ID!

  """The policy whose balances are rolled over."""
  policyId: ID!

  """The year the days are moved out of."""
  fromYear: Int!

  """The year the days are moved into. It must be later than `fromYear`."""
  toYear: Int!

  """
  An explicit cap on the days rolled over per employee. Omitted or 0 falls back to the policy's own cap.
  """
  maxCarryForwardDays: Decimal
}

"""
What a carry-forward answers.

A count rather than a row, and deliberately: the delivered write walks every employee's balance for the
policy and writes two rows per employee, so there is no single balance it would be honest to answer
instead. A caller that wants the resulting rows reads them back through `timeOffBalances`.
"""
type TimeOffCarryForwardResult {
  """How many employee balances were rolled over."""
  carried: Int!
}

"""
One leave arrangement of an organization.

Every entitlement figure on it is a number of **days**, or of days per accrual period, and is carried as
an exact decimal rather than a binary fraction: half a day is an entitlement this platform grants, and a
ledger that rounds is a ledger that disagrees with itself.
"""
type TimeOffPolicy {
  id: ID!

  """
  The policy's name, unique enough within an organization to be recognised in a list.
  """
  name: String!

  """
  Whether taking leave under this policy needs an approval before it counts.
  """
  requiresApproval: Boolean!

  """Whether leave taken under this policy is paid."""
  paid: Boolean!

  """
  The category of leave this policy grants, as the contracts package's own vocabulary states it:
  `ANNUAL`, `SICK`, `MATERNITY`, `PATERNITY`, `UNPAID`, `COMPENSATORY`, `BEREAVEMENT`, `STUDY` or
  `OTHER`. The value is deliberately not redeclared as a schema enum here.
  """
  leaveType: String

  """
  The upper bound, in days, on what an employee may take in a year under this policy.
  """
  maxDaysPerYear: Decimal

  """Whether unused days roll over into the next year."""
  allowCarryForward: Boolean

  """
  The upper bound, in days, on what may roll over. `0` or an absent value means no cap, which is the
  rule the delivered carry-forward reads.
  """
  maxCarryForwardDays: Decimal

  """The days accrued per accrual period, under this policy."""
  accrualRate: Decimal

  """
  How often the accrual rate is granted, as the contracts package's own vocabulary states it:
  `DAILY`, `WEEKLY`, `BIWEEKLY`, `MONTHLY` or `ANNUALLY`.
  """
  accrualFrequency: String

  """Whether this is the organization's default policy."""
  isDefault: Boolean
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """When the policy was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of policies."""
type TimeOffPolicyEdge {
  node: TimeOffPolicy!
  cursor: String!
}

"""
One page of policies, with the total the filters select and the boundary cursors.
"""
type TimeOffPolicyConnection {
  nodes: [TimeOffPolicy!]!
  edges: [TimeOffPolicyEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a policy list may be sorted by."""
enum TimeOffPolicySortField {
  createdAt
  updatedAt
  name
  leaveType
  maxDaysPerYear
  allowCarryForward
  isDefault
  requiresApproval
  paid
}

"""
The order the policies are returned in. At most three keys are accepted.
"""
input TimeOffPolicySort {
  field: TimeOffPolicySortField!
  direction: SortDirection!
}

"""
The fields a policy list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered list read returns. The two vocabularies compare as
text, because the row carries the vocabulary's own value; the entitlement figures compare as exact
decimals. Neither relation is a member: the rows carry none.
"""
input TimeOffPolicyFilter {
  id: IDFilter
  name: StringFilter
  requiresApproval: BooleanFilter
  paid: BooleanFilter
  leaveType: StringFilter
  maxDaysPerYear: DecimalFilter
  allowCarryForward: BooleanFilter
  maxCarryForwardDays: DecimalFilter
  accrualRate: DecimalFilter
  accrualFrequency: StringFilter
  isDefault: BooleanFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimeOffPolicyFilter!]
  or: [TimeOffPolicyFilter!]
  not: TimeOffPolicyFilter
}

"""
What a caller states when it files a policy.

`organizationId` is required because the delivered write names the organization on the row it builds and
resolves the membership against it; the tenant is stamped from the credential, as it is on every write of
this platform, and is therefore not a member.

`name`, `requiresApproval` and `paid` are required because they are the three columns the row cannot be
without: the entity declares them non-nullable and the delivered write assigns each of them from the
body. Everything else is the entitlement the platform's own input type states, and an omitted member is
the column's own default.
"""
input CreateTimeOffPolicyInput {
  """
  The organization the policy belongs to, and the one its membership is resolved against.
  """
  organizationId: ID!
  name: String!
  requiresApproval: Boolean!
  paid: Boolean!

  """
  The employees the policy applies to, by identifier. The delivered write resolves them to employees of
  the caller's own organization and replaces the policy's membership with the result, which is the only
  way this surface states a membership — the list read loads no relation and the type therefore carries
  none.
  """
  employeeIds: [ID!]

  """
  The category of leave this policy grants, as the contracts package's vocabulary states it.
  """
  leaveType: String
  maxDaysPerYear: Decimal
  allowCarryForward: Boolean
  maxCarryForwardDays: Decimal
  accrualRate: Decimal

  """The accrual period, as the contracts package's vocabulary states it."""
  accrualFrequency: String
  isDefault: Boolean
}

"""
What a caller states when it edits a policy that exists.

The same body as the filing above plus the identifier, because the two writes reach the same service
shape: the delivered edit takes the identifier from its own argument and assigns every member the body
states. There is no partial body here — a member the caller leaves out is not written — which is why the
three columns the row cannot be without are required on this body too.
"""
input UpdateTimeOffPolicyInput {
  id: ID!
  organizationId: ID!
  name: String!
  requiresApproval: Boolean!
  paid: Boolean!
  employeeIds: [ID!]
  leaveType: String
  maxDaysPerYear: Decimal
  allowCarryForward: Boolean
  maxCarryForwardDays: Decimal
  accrualRate: Decimal
  accrualFrequency: String
  isDefault: Boolean
}

"""One stretch of leave asked for under a policy."""
type TimeOffRequest {
  id: ID!

  """The first day of the leave."""
  start: DateTime!

  """The last day of the leave."""
  end: DateTime!

  """
  The day the leave was asked for, which is not the day the row was filed.
  """
  requestDate: DateTime!

  """
  Where the decision stands, as the contracts package's vocabulary states it: `REQUESTED`,
  `APPROVED` or `DENIED`. It is deliberately not redeclared as a schema enum here.
  """
  status: String!

  """Why the leave is being asked for."""
  description: String

  """
  A link to whatever document the request carries, kept beside the asset identifier.
  """
  documentUrl: String

  """
  The document the request carries, by identifier. Read the asset itself from its own surface.
  """
  documentId: ID

  """
  Whether the row is a public holiday rather than a request somebody filed.
  """
  isHoliday: Boolean

  """The policy the leave is taken under."""
  policyId: ID

  """
  The policy itself, filled by the list read, which joins it beside every row. The write fields answer
  rows read without it.
  """
  policy: TimeOffPolicy

  """
  The employees the leave is for, filled by the list read, which joins them and each of their accounts
  beside every row. The accounts themselves are not members of this surface: an engagement carries its
  account as an identifier, on the employee's own surface.
  """
  employees: [Employee!]
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """When the request was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of requests."""
type TimeOffRequestEdge {
  node: TimeOffRequest!
  cursor: String!
}

"""
One page of requests, with the total the filters select and the boundary cursors.
"""
type TimeOffRequestConnection {
  nodes: [TimeOffRequest!]!
  edges: [TimeOffRequestEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a request list may be sorted by."""
enum TimeOffRequestSortField {
  createdAt
  updatedAt
  start
  end
  requestDate
  status
  isHoliday
}

"""
The order the requests are returned in. At most three keys are accepted.
"""
input TimeOffRequestSort {
  field: TimeOffRequestSortField!
  direction: SortDirection!
}

"""
The fields a request list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row. The employee is deliberately not here: the row carries its
employees as a collection, so there is no value for the protocol to compare, and the field's own
`employeeId` argument is what narrows the read by one — through the join the delivered read performs.

Two narrowings the delivered paginated route can state are deliberately absent here, and for one
reason: that route's `where` can search the joined account's name and the joined policy's name as text,
and this input narrows by comparing a value at a declared field — while the row carries an object at
each of those two. `policyId` is how a caller narrows to one policy, and the employees of a request are
read from the employee's own surface. Stating a member over a relation here would be a filter the
schema offers and the evaluator refuses on every row.
"""
input TimeOffRequestFilter {
  id: IDFilter
  documentUrl: StringFilter
  description: StringFilter
  start: DateTimeFilter
  end: DateTimeFilter
  requestDate: DateTimeFilter
  status: StringFilter
  isHoliday: BooleanFilter
  policyId: IDFilter
  documentId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimeOffRequestFilter!]
  or: [TimeOffRequestFilter!]
  not: TimeOffRequestFilter
}

"""
What a caller states when it files a request.

The organization, the two ends of the leave and the day it was asked for are required because they are
the four columns the row cannot be without: the entity declares all four non-nullable and the delivered
write assigns them straight onto the row it stores. The tenant is stamped from the credential, as it is
on every write of this platform, and is therefore not a member.

`status` seeds the approval record the delivered write files beside the request; an absent status is
that write's own default, which is "requested".
"""
input CreateTimeOffRequestInput {
  """The organization the request is filed in."""
  organizationId: ID!

  """The first day of the leave."""
  start: DateTime!

  """The last day of the leave."""
  end: DateTime!

  """The day the leave was asked for."""
  requestDate: DateTime!

  """The employees the leave is for, by identifier."""
  employeeIds: [ID!]

  """Why the leave is being asked for."""
  description: String

  """The policy the leave is taken under, by identifier."""
  policyId: ID

  """
  Where the decision starts, if the caller states it rather than leaving the delivered default.
  """
  status: String

  """
  Whether the row is a public holiday rather than a request somebody filed.
  """
  isHoliday: Boolean

  """A link to whatever document the request carries."""
  documentUrl: String
}

"""
What a caller states when it edits a filed request.

The delivered edit takes the identifier from its own argument and passes the body through to the write,
and the body's own interface declares one member — where the decision stands. So this body declares one
member beside the identifier, and stating it is the whole of what an edit can change: a request's dates
and its employees are what the request *is*, and rewriting them would leave the approval record filed
beside it describing a leave nobody asked for.
"""
input UpdateTimeOffRequestInput {
  id: ID!

  """
  Where the decision stands. The delivered write stores the value it is given.
  """
  status: String
}

"""One slice of tracked activity, as the list read answers it."""
type Activity {
  id: ID!

  """
  The activity's title: the window title, or the name of the application or URL the person was in.
  """
  title: String

  """What the activity was, when the tracker recorded more than its title."""
  description: String

  """
  The tracker's own extras for the activity, as the document column holds them — the URL metadata the
  clients render. Carried as `JSON` because it is a document rather than a column with a shape this
  schema could state.
  """
  metaData: JSON

  """
  The calendar day the activity was recorded on, in the store's own date form (`YYYY-MM-DD`).
  """
  date: String

  """
  The time of day the activity was recorded at, in the store's own time form.
  """
  time: String

  """
  How long the activity lasted, **in seconds** — the unit the slot it was filed into is measured in,
  and the unit every duration in this domain is measured in.
  """
  duration: Int

  """
  What kind of activity it is, as the delivered value: `APP` for an application and `URL` for a page.
  Carried as its value because the value set is a fixed contract set the tracker writes.
  """
  type: String

  """
  Where the tracking came from, as the delivered value — `BROWSER`, `DESKTOP`, `MOBILE`,
  `BROWSER_EXTENSION` and the integrations the platform ships.
  """
  source: String

  """
  The instant the activity was recorded at. This is the column a date range narrows on, which is why
  it is carried beside `date` and `time`: those two are what the aggregation groups by, and this one is
  what a range selects.
  """
  recordedAt: DateTime

  """
  The engagement the activity belongs to. The row behind the identifier is the employee domain's.
  """
  employeeId: ID

  """
  The project the activity is attributed to, when it is attributed to one.
  """
  projectId: ID

  """
  The ten-minute slot the activity was filed into. The row behind the identifier is the slot domain's,
  and it is the row both aggregations group through.
  """
  timeSlotId: ID

  """The task the activity is attributed to, when it is attributed to one."""
  taskId: ID

  """When the activity was withdrawn, or null while it is live."""
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  tenantId: ID
  organizationId: ID
}

"""The boundary of one page of activities."""
type ActivityEdge {
  node: Activity!
  cursor: String!
}

"""
One page of activities, with the total the narrowing selects and the boundary cursors.
"""
type ActivityConnection {
  nodes: [Activity!]!
  edges: [ActivityEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields an activity list may be sorted by."""
enum ActivitySortField {
  createdAt
  updatedAt
  date
  time
  duration
  title
  type
  source
  recordedAt
}

"""
The order the activities are returned in. At most three keys are accepted.
"""
input ActivitySort {
  field: ActivitySortField!
  direction: SortDirection!
}

"""
The fields an activity list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the activity row, which is what the connection protocol can evaluate over the
rows the delivered read returned. The narrowings that are **not** columns are deliberately absent, and
each is an argument of the list field instead, stated the way the delivered query string states it:
which slot's activity band a row falls in is a condition on `time_slot.overall`, and which log a row was
tracked under is a condition on the log rows the read joins — `logType` in particular has no column of
its own on the activity row, so a filter for it here could only ever select the empty set.

`source` is the one name that appears in both places, and the two are different questions rather than a
duplication: as a filter it narrows on the **activity's own** source column — where this activity was
tracked — and as an argument it narrows on the **log's** source, which is the timer the slot belongs to.
"""
input ActivityFilter {
  id: IDFilter
  title: StringFilter
  description: StringFilter
  metaData: JSONFilter
  date: StringFilter
  time: StringFilter
  duration: NumberFilter
  type: StringFilter
  source: StringFilter
  recordedAt: DateTimeFilter
  employeeId: IDFilter
  projectId: IDFilter
  timeSlotId: IDFilter
  taskId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [ActivityFilter!]
  or: [ActivityFilter!]
  not: ActivityFilter
}

"""
The activity band a list narrows to, stated as the percentage of the slot it is measured against.

The delivered read converts the two bounds into the unit the slot's `overall` column is measured in
before it compares them, which is why the band is stated as a percentage here and as a percentage in the
delivered query string: the conversion belongs to the read, not to the caller.
"""
input ActivityLevelInput {
  """
  Where the band starts, as a percentage of the slot's overall activity, from 0 to 100.
  """
  start: Int!

  """
  Where the band ends, as a percentage of the slot's overall activity, from 0 to 100.
  """
  end: Int!
}

"""
One day's activity for one engagement and one title, as the daily aggregation folds it.

The aggregation groups by the day, the title and the engagement, and answers one row per group, so
`duration` is a sum and `sessions` is a count rather than a single activity's facts. Both are nullable
because they are the store's own aggregate values rather than columns of a row, and an aggregate the
store answered as absent is not a zero this schema may invent.
"""
type DailyActivity {
  """
  How many activity rows the group folds together. The delivered read names this count `sessions`,
  which is the read's own name for the fold — it counts activity rows, not tracking sessions, and the
  two are different quantities.
  """
  sessions: Int

  """How long the group's activities lasted in total, **in seconds**."""
  duration: Int

  """The engagement whose activities the group folds together."""
  employeeId: ID

  """The calendar day the group is for."""
  date: String

  """The activity title the group is for."""
  title: String
}

"""
How a daily-activities report groups the rows it computed.

The values are the delivered filter's own, and the three declared here are the three the delivered route
actually groups by. The filter declares a fourth value, and the route does nothing with it: a caller that
states it is answered the ungrouped rows. Offering it here would be offering an argument whose effect is
that no grouping happened, which the omission of `groupBy` already states.
"""
enum DailyActivityReportGrouping {
  date
  employee
  project
}

"""
The grouped daily-activities report.

**One field answers all three groupings and the ungrouped rows, because they are one computation.** The
delivered route reads the report rows once and then nests them one of three ways; three root fields would
recompute the same rows three times and could disagree with each other. Which nesting an answer carries is
therefore stated by the `groupBy` argument, and exactly one of the four members below is filled.

The nesting is the delivered mapper's own, and it is not symmetric — each grouping puts a different thing
at the outside:

- by date, a day holds its engagements, and an engagement holds its projects;
- by employee, an engagement holds its days, and a day holds its projects;
- by project, a project holds its days, and a day holds its engagements.
"""
type DailyActivityReport {
  """The report rows themselves, when the caller stated no grouping."""
  activities: [DailyActivityReportRow!]

  """The days, when the caller grouped by date."""
  dates: [DailyActivityReportDate!]

  """The engagements, when the caller grouped by employee."""
  employees: [DailyActivityReportEmployee!]

  """The projects, when the caller grouped by project."""
  projects: [DailyActivityReportProject!]
}

"""
One row of the grouped report: a title on a day for an engagement and a project, with the time it holds.

The members are the columns the delivered report read selects. The engagement row and the project row the
mapper nests beside them are not declared here — they belong to the domains that own those resources, and
the identifier the group is keyed by is what always travels.
"""
type DailyActivityReportRow {
  """How many activity rows the group folds together."""
  sessions: Int

  """How long the group's activities lasted in total, **in seconds**."""
  duration: Int

  """
  The row's share of the tracked time of the group it is nested under, **as a percentage**: the
  delivered mapper divides this row's duration by the sum of the durations the grouping ran over — the
  day for a date grouping, the engagement for an employee grouping, the project for a project grouping
  — and answers the share to two decimal places. So the same row carries a different percentage under
  two different groupings, which is what makes reading it without its `groupBy` meaningless.
  
  The delivered grouping writes this number onto the row under the contract's own snake-case member
  name; this surface carries it under the member name the contract declares for it.
  """
  durationPercentage: Float

  """
  The engagement the row belongs to; the engagement `groupBy: employee` groups by.
  """
  employeeId: ID

  """
  The project the row is attributed to; the project `groupBy: project` groups by.
  """
  projectId: ID

  """The calendar day the row is for."""
  date: String

  """The activity title the row is for."""
  title: String
}

"""One project's activity inside a day, or inside an engagement's day."""
type DailyActivityReportProjectGroup {
  """The project the group's rows are attributed to."""
  projectId: ID

  """The rows attributed to that project."""
  activity: [DailyActivityReportRow!]!
}

"""One engagement's activity inside a day."""
type DailyActivityReportEmployeeGroup {
  """The engagement the group's rows belong to."""
  employeeId: ID

  """The rows belonging to that engagement."""
  activity: [DailyActivityReportRow!]!
}

"""One engagement inside a day, with the projects it worked on."""
type DailyActivityReportEmployeeProjects {
  """The engagement."""
  employeeId: ID

  """The projects the engagement worked on that day."""
  projects: [DailyActivityReportProjectGroup!]!
}

"""One day inside an engagement, with the projects worked on."""
type DailyActivityReportDateProjects {
  """The day, as the mapper's own `YYYY-MM-DD` key."""
  date: String!

  """The projects worked on that day."""
  projects: [DailyActivityReportProjectGroup!]!
}

"""One day inside a project, with the engagements that worked on it."""
type DailyActivityReportDateEmployees {
  """The day, as the mapper's own `YYYY-MM-DD` key."""
  date: String!

  """The engagements that worked on the project that day."""
  employees: [DailyActivityReportEmployeeGroup!]!
}

"""One day of the report, with the engagements that were active in it."""
type DailyActivityReportDate {
  """The day, as the mapper's own `YYYY-MM-DD` key."""
  date: String!

  """The engagements active that day."""
  employees: [DailyActivityReportEmployeeProjects!]!
}

"""One engagement of the report, with the days it was active on."""
type DailyActivityReportEmployee {
  """The engagement."""
  employeeId: ID

  """The days the engagement was active on."""
  dates: [DailyActivityReportDateProjects!]!
}

"""One project of the report, with the days it was worked on."""
type DailyActivityReportProject {
  """The project."""
  projectId: ID

  """The days the project was worked on."""
  dates: [DailyActivityReportDateEmployees!]!
}

"""
What a caller states when it files several activities at once.

The organization and the engagement are stated because the delivered body states them and its validation
requires them; the tenant is not, because the delivered write reads it from the credential and overwrites
whatever a body carries. The engagement and the project are also stamped onto every row the write persists,
which is what makes the two of them the envelope's members rather than each row's.
"""
input ActivityBulkInput {
  """The organization every filed activity belongs to."""
  organizationId: ID!

  """The engagement every filed activity belongs to."""
  employeeId: ID!

  """
  The project to attribute every filed activity to, when the caller attributes them to one.
  """
  projectId: ID

  """The activities to file."""
  activities: [ActivityInput!]!
}

"""
One activity a bulk write files.

The members are the columns the delivered write persists from the body. The organization, the tenant, the
engagement and the project are deliberately **not** members: the delivered handler stamps all four onto
every row, so a row that stated them would have them overwritten — an input member whose value is
discarded is worse than one that is absent.
"""
input ActivityInput {
  """The activity's title."""
  title: String

  """What the activity was."""
  description: String

  """The tracker's own extras for the activity, as a document."""
  metaData: JSON

  """
  The calendar day the activity belongs to, in the store's own date form.
  """
  date: String

  """The time of day the activity belongs to, in the store's own time form."""
  time: String

  """How long the activity lasted, **in seconds**."""
  duration: Int

  """What kind of activity it is."""
  type: String

  """Where the tracking came from."""
  source: String

  """
  The instant the activity was recorded at. The delivered write stamps this itself when a row states
  none, so a row that states it is stating the instant the row is filed under.
  """
  recordedAt: DateTime

  """
  The slot to file the activity into, when the caller files it into a known one.
  """
  timeSlotId: ID

  """The task to attribute the activity to."""
  taskId: ID
}

"""
One observation a client reported, as it was stored and as it can be decoded.

`encodedData` is the payload exactly as the client sent it, which is what makes a stored session
re-decodable by the client that produced it. `decodedData` is the same observation as the platform decoded
it, which is what makes it readable without the client.
"""
type TrackingSessionPayload {
  """When the observation was made, as the text the payload carries."""
  timestamp: String!

  """
  The payload as the client sent it, verbatim. It is the only form the platform can hand back unchanged.
  """
  encodedData: String!

  """
  The observation as the platform decoded it, carried as the opaque document it is.
  """
  decodedData: JSON
}

"""
One tracked session, decoded out of the slot documents that carry it.

A session spans slots: a client that keeps tracking past the end of one ten-minute slot continues the same
session in the next, and the delivered reads either carry one row per slot or fold the rows of one session
together — which is what the `groupBySession` argument of the sessions field states.
"""
type TrackingSession {
  """
  The identifier the tracker minted for this session. It is not a row of this platform; it is a key.
  """
  sessionId: String!

  """When the session began, as the text the tracker wrote."""
  startTime: String!

  """When the session was last active, as the text the tracker wrote."""
  lastActivity: String!

  """
  When the platform first stored a payload of this session, as the text it wrote.
  """
  createdAt: String!

  """
  When the platform last stored a payload of this session, as the text it wrote.
  """
  updatedAt: String!

  """The observations the session holds, oldest first."""
  payloads: [TrackingSessionPayload!]!
}

"""
As much of the slot a decoded session was filed into as the session answers carry.

The two members are the two the delivered answers expose: when the slot started and how long it lasted. The
slot's own resource is the slot domain's, and the rows behind `timeSlotId` are read through it.
"""
type CustomTrackingTimeSlotSummary {
  """When the slot started."""
  startedAt: DateTime!

  """How long the slot lasted, **in seconds**."""
  duration: Int
}

"""
One slot a session was filed into, as the answers that list them state it.
"""
type CustomTrackingTimeSlotEntry {
  """The slot."""
  timeSlotId: ID!

  """The slot's own summary."""
  timeSlot: CustomTrackingTimeSlotSummary!
}

"""
One tracked log row, projected to the members a tracking answer is read for.

The rows are the delivered reads' own: the slot's pivot joins complete log rows, and a tracking answer
carries them whole. This type is the projection those rows are read through on **this** surface, and it is
not a second declaration of the log resource — the log's own domain owns that resource and its whole row,
including the label pivot and the nested relations this projection does not join.
"""
type CustomTrackingTimeLog {
  id: ID!

  """When the log started."""
  startedAt: DateTime

  """When the log stopped, or null while it is running."""
  stoppedAt: DateTime

  """When the log was last edited by hand."""
  editedAt: DateTime

  """How long the log lasted, **in seconds**."""
  duration: Int!

  """The kind of log this is, as the delivered value."""
  logType: String

  """Where the log was tracked from, as the delivered value."""
  source: String

  """Whether the log is billable."""
  isBillable: Boolean

  """Whether the log is still running."""
  isRunning: Boolean

  """Whether the log has been edited since it was recorded."""
  isEdited: Boolean

  """What the log is about."""
  description: String

  """Why the log was entered or edited by hand, when it was."""
  reason: String

  """The engagement the log belongs to."""
  employeeId: ID

  """The project the log is attributed to."""
  projectId: ID

  """The task the log is attributed to."""
  taskId: ID

  """The timesheet the log was filed into."""
  timesheetId: ID

  """The client the log is attributed to."""
  organizationContactId: ID

  """The team the log was worked in."""
  organizationTeamId: ID
  tenantId: ID
  organizationId: ID
}

"""
One tracked session as a read answers it: the session, the slots it touched, and the logs of those slots.

Two of the members the delivered contract declares are carried although the reads this surface mirrors do
not fill them, and saying so is the point of naming them here: `timeSlotId` and `timeSlot` belong to the
*ungrouped* shape the delivered service builds internally, and the shape it answers with carries the
`timeSlots` list instead. A client that reads the pair is reading a member that is null on every row these
fields return, and it should know that before it builds on it.
"""
type CustomTrackingSessionResponse {
  """The session identifier every row of this answer shares."""
  sessionId: String!

  """
  The slot the ungrouped shape pointed at; null on the answers these fields return.
  """
  timeSlotId: ID

  """
  The slot summary of the ungrouped shape; null on the answers these fields return.
  """
  timeSlot: CustomTrackingTimeSlotSummary

  """The logs of the slots this session touched."""
  timeLogs: [CustomTrackingTimeLog!]!

  """The session itself, with its payloads."""
  session: TrackingSession!

  """The slots this session touched, in the order the read answered them."""
  timeSlots: [CustomTrackingTimeSlotEntry!]
}

"""
What a set of answered sessions adds up to.

The three members are the delivered summary's own. The range is the span of the slots the sessions touched
— not of the payloads — which is why it is null for an answer whose sessions carry no slot summary at all.
"""
type TrackingSessionSummary {
  """How many distinct sessions the answer holds."""
  totalSessions: Int!

  """How many distinct slots those sessions touched."""
  totalTimeSlots: Int!

  """
  The span of the slots that were touched, or null when none carried a start.
  """
  dateRange: TrackingSessionDateRange
}

"""The span of slots a set of sessions touched."""
type TrackingSessionDateRange {
  """The earliest slot start."""
  start: DateTime!

  """The latest slot start."""
  end: DateTime!
}

"""The sessions a read answered, with what they add up to."""
type CustomTrackingSessions {
  """
  The sessions, as the caller asked for them: grouped by session or one row per slot.
  """
  sessions: [CustomTrackingSessionResponse!]!

  """What the answered sessions add up to."""
  summary: TrackingSessionSummary!
}

"""
One slot's tracking data: the slot, and the sessions decoded out of its document column.

The slot carries three members rather than the two a session's own summary carries, because this answer is
*about* the slot: it is the row the write filed the tracking into, and the logs are what the slot is
attributed to.
"""
type CustomTrackingTimeSlot {
  """When the slot started."""
  startedAt: DateTime!

  """How long the slot lasted, **in seconds**."""
  duration: Int!

  """The logs the slot is attributed to."""
  timeLogs: [CustomTrackingTimeLog!]!
}

"""
One slot's tracking data, as the delivered read answers it.

A slot that holds no tracking data is a different answer from a slot that is not there: the first carries
`hasTrackingData: false` and a message, the second is a miss — which is why the root field that answers
this is nullable and this type's own members are not.
"""
type CustomTrackingTimeSlotData {
  """The slot the answer is about."""
  timeSlotId: ID!

  """Whether the slot's document column holds any tracking session at all."""
  hasTrackingData: Boolean!

  """Why there is no tracking data, when there is none."""
  message: String

  """
  The slot and the logs it is attributed to, when it holds tracking data.
  """
  timeSlot: CustomTrackingTimeSlot

  """The sessions decoded out of the slot, when it holds any."""
  trackingSessions: [TrackingSession!]
}

"""
The outcome of submitting one tracking payload.

A submission that could not be processed is a failure of the *request* and is reported as an error rather
than as this payload with `success: false`: the delivered write either files the payload or refuses it.
"""
type CustomTrackingSubmission {
  """Whether the payload was filed."""
  success: Boolean!

  """The session identifier the payload decoded to."""
  sessionId: String!

  """The slot the payload was filed into."""
  timeSlotId: ID!

  """What the write reports about itself."""
  message: String!

  """
  The session as it stands after the write, or null when the write could not read it back.
  """
  session: TrackingSession
}

"""
One submission's outcome inside a bulk call, with the position it held in the list.

The index is part of the answer because a bulk call is answered in one piece: a caller that sent twenty
payloads and was told some failed needs to know which, and the position in the list it sent is the only
name a caller already has for a payload that failed before a session existed.
"""
type CustomTrackingBulkResult {
  """Whether this payload was filed."""
  success: Boolean!

  """The session identifier this payload decoded to, when it decoded."""
  sessionId: String!

  """The slot this payload was filed into, when it was filed."""
  timeSlotId: ID!

  """What the write reports about this payload."""
  message: String!

  """
  The session as it stands, or null when this payload could not be read back.
  """
  session: TrackingSession

  """The position this payload held in the list that was sent."""
  index: Int!

  """Why this payload failed, when it did."""
  error: String
}

"""What a bulk submission adds up to."""
type CustomTrackingBulkSummary {
  """How many payloads were sent."""
  total: Int!

  """How many of them were filed."""
  successful: Int!

  """How many of them failed."""
  failed: Int!
}

"""The outcome of submitting several tracking payloads in one call."""
type CustomTrackingBulkSubmission {
  """One outcome per payload, in the order the payloads were sent."""
  results: [CustomTrackingBulkResult!]!

  """What the outcomes add up to."""
  summary: CustomTrackingBulkSummary!
}

"""
What a caller states when it submits one tracking payload.

The organization is required, exactly as the delivered body requires it: a tracking write is attributed to
an organization, and the delivered validation refuses a body that names none even though its handler would
otherwise fall back to the caller's own. The engagement is optional, and a payload that names none is
attributed to the caller's own.
"""
input CustomTrackingSubmissionInput {
  """The organization the tracking is attributed to."""
  organizationId: ID!

  """The encoded payload, exactly as the tracker produced it."""
  payload: String!

  """
  When the tracking began: the instant the payload belongs to, and the instant the slot it is filed into
  is found by. Required, because the delivered write refuses an unreadable instant and an absent one is
  exactly that — stating the requirement in the schema is what keeps a client from discovering it as a
  runtime refusal.
  """
  startTime: DateTime!

  """
  The engagement the tracking is attributed to. Omit it for the caller's own.
  """
  employeeId: ID
}

"""
What a caller states when it submits several tracking payloads in one call.

The list is the body's own member, and it must hold at least one payload: the delivered validation refuses
an empty list rather than answering an empty result, because a bulk call that files nothing is a mistake
rather than an outcome.
"""
input CustomTrackingBulkInput {
  """The payloads to submit."""
  list: [CustomTrackingSubmissionInput!]!
}

"""The days a period was worked on, and how long the work lasted."""
type ProfileActivityDay {
  """
  The local calendar day, as `YYYY-MM-DD` in the zone the period was resolved against.
  """
  date: String!

  """
  The time tracked on that day. Seconds, and the sum of the days is the period's total.
  """
  duration: Float!
}

"""
The period a profile-activity answer was computed over, as the caller stated it.
"""
type ProfileActivityPeriod {
  """
  The first local calendar day of the period, inclusive, as the caller stated it.
  """
  startDate: String!

  """
  The local calendar day the period ends on, exclusive, as the caller stated it.
  """
  endDate: String!

  """The zone the two days were resolved against."""
  timeZone: String!
}

"""One employee's activity over a period of local calendar days."""
type ProfileActivity {
  """
  The engagement the summary belongs to. The engagement's own row is read from the employee surface by this identifier.
  """
  employeeId: ID!

  """
  How many of the period's local days carried activity. A count of days, and zero for a period in which nothing was tracked.
  """
  activeDays: Int!

  """
  The time tracked over the period. Seconds, summed over the period's days.
  """
  totalDuration: Float!

  """
  The first local calendar day of the period that carried activity, as `YYYY-MM-DD`. Absent when no day did.
  """
  firstActiveOn: String

  """
  The last local calendar day of the period that carried activity, as `YYYY-MM-DD`. Absent when no day did.
  """
  lastActiveOn: String

  """
  The period the answer was computed over, restated as the caller gave it.
  """
  period: ProfileActivityPeriod!

  """
  The per-day breakdown, ordered by day. Present only when the caller asked for it; a caller that did not is answered the summary alone, which is what the delivered read does rather than an empty list it never computed.
  """
  daily: [ProfileActivityDay!]
}

"""
What one organization's tracked time comes to over a period: how many people and projects it covers,
how actively it was tracked, and how long it lasted.

The two `week` members are the range's and the two `today` members are today's, which the delivered
read computes over its own period rather than over the range — the two pairs answer different questions
and are not two pages of one.
"""
type TimeTrackingCounts {
  """
  How many distinct engagements tracked time over the range. A count of people, not of logs.
  """
  employeesCount: Int!

  """
  How many distinct projects tracked time over the range. A count of projects, not of logs.
  """
  projectsCount: Int!

  """
  The average activity over the range: a percentage of the recorded slot activity of the slots that were counted, rounded to two places by the delivered calculation.
  """
  weekActivities: Float!

  """The time tracked over the range. Seconds."""
  weekDuration: Float!

  """
  The average activity over the today period, on the same basis and with the same rounding as `weekActivities`.
  """
  todayActivities: Float!

  """The time tracked over the today period. Seconds."""
  todayDuration: Float!
}

"""The name and image of the account behind an engagement."""
type TimeTrackingUserSummary {
  """
  The account's name, as the batch account read answers it. Null when that read found no account for the engagement, which the delivered answer reports as a null name rather than as a missing member.
  """
  name: String

  """
  The account's image, resolved by that same read into a URL a browser can load.
  """
  imageUrl: String
}

"""One member's tracked total over a period, with the activity behind it."""
type TimeTrackingMemberTime {
  """The engagement the total belongs to."""
  employeeId: ID

  """The time tracked over the period. Seconds."""
  duration: Float

  """
  The average activity behind that total: a percentage of the recorded slot activity, rounded to one place by the delivered calculation.
  """
  overall: Float
}

"""
One day of a member's week, as the delivered read splits their tracked time.
"""
type TimeTrackingMemberWeekHour {
  """
  The day of the week, as the store's own number: `0` is Sunday. A calendar position rather than a quantity, which is why it is carried as the number the store computed instead of a locale's day name.
  """
  day: Int

  """The time tracked on that day of the range. Seconds."""
  duration: Float
}

"""One member of an organization who tracked time over a range."""
type TimeTrackingMemberStatistic {
  """
  The engagement's identifier. It is also the identifier the delivered read groups the answer by.
  """
  id: ID!

  """
  Whether the engagement is currently online, as the employee row states it.
  """
  isOnline: Boolean

  """
  Whether the engagement is currently away, as the employee row states it.
  """
  isAway: Boolean

  """
  The account behind the engagement, as the batch account read answers it.
  """
  user: TimeTrackingUserSummary

  """
  The member's total over the range, with the activity behind it. Absent when the range's slots carried nothing for this member.
  """
  weekTime: TimeTrackingMemberTime

  """
  The member's total over the today period, with the activity behind it. Absent on the same basis.
  """
  todayTime: TimeTrackingMemberTime

  """
  The member's tracked time over the range, split by day of the week. Empty when the range carried nothing for this member.
  """
  weekHours: [TimeTrackingMemberWeekHour!]!
}

"""One project of an organization that was worked on over a range."""
type TimeTrackingProjectStatistic {
  """
  The project's identifier, which is also the identifier the delivered read groups the answer by. The project's own row is read from the project surface by this identifier.
  """
  id: ID!

  """
  The project's name, as the delivered read reads it beside the identifier.
  """
  name: String!

  """The time tracked against the project over the range. Seconds."""
  duration: Float!

  """
  The share of the range's tracked time this project accounts for: a percentage of the time tracked against every project of the organization over the same range — not of the five answers returned beside it — rounded to two places by the delivered calculation.
  """
  durationPercentage: Float!
}

"""One task of an organization that was worked on over a range."""
type TimeTrackingTaskStatistic {
  """
  The task's identifier. The task's own row is read from the task surface by this identifier.
  """
  id: ID!

  """
  The task's title, as the delivered read reads it beside the identifier.
  """
  title: String!

  """
  The time tracked against the task over the range. Seconds. Absent when the delivered aggregation produced no number for it, which the answer reports as an absent member rather than as a zero.
  """
  duration: Float

  """
  The time tracked against the task over the today period. Seconds, and absent on the same basis.
  """
  todayDuration: Float

  """
  The share of the range's tracked time this task accounts for: a percentage of the time tracked against every task the range touched, rounded to two places by the delivered calculation. Zero when the range's total is zero.
  """
  durationPercentage: Float!

  """When the task's most recent log in the range was last updated."""
  updatedAt: DateTime
}

"""The name and image of a project, as a manual time log carries them."""
type TimeTrackingProjectSummary {
  """
  The project's name, as the delivered read reads it from the project row beside the log.
  """
  name: String

  """The project's image, as that row carries it."""
  imageUrl: String
}

"""One manual time log of an organization."""
type TimeTrackingManualTimeStatistic {
  """The time log's identifier."""
  id: ID!

  """When the log was started."""
  startedAt: DateTime

  """The time the log accounts for. Seconds."""
  duration: Float

  """
  The engagement the log belongs to, carried as its identifier: the two reads behind this answer fill the engagement itself on one and its identifier on the other, so the identifier is what both fill.
  """
  employeeId: ID!

  """
  The account behind the engagement, as the batch account read answers it.
  """
  user: TimeTrackingUserSummary

  """
  The project the log was filed against, as the delivered read reads it from the project row. Absent when the log names no project, which the delivered read leaves as an empty projection rather than as a failure.
  """
  project: TimeTrackingProjectSummary
}

"""One screenshot filed against a time slot."""
type TimeTrackingScreenshot {
  """The screenshot's identifier."""
  id: ID!

  """The stored path of the image."""
  file: String

  """The stored path of the thumbnail."""
  thumb: String

  """The thumbnail's URL."""
  thumbUrl: String
}

"""
One time slot of one engagement, as the delivered read answers it beside the member it was read for.
"""
type TimeTrackingTimeSlot {
  """The slot's identifier."""
  id: ID!

  """When the slot began."""
  startedAt: DateTime

  """When the slot ended. Absent while the slot is still open."""
  stoppedAt: DateTime

  """The time the slot covers. Seconds."""
  duration: Float

  """How many keyboard interactions were recorded in the slot."""
  keyboard: Int

  """How many mouse interactions were recorded in the slot."""
  mouse: Int

  """
  The slot's recorded activity, as the store counts it: interaction units accumulated over the slot's minutes rather than a percentage. The activity band a statistics read narrows by is stated against this same scale.
  """
  overall: Float

  """
  The engagement the slot belongs to, carried as its identifier: the delivered reads fill the engagement itself on one and its identifier on the other.
  """
  employeeId: ID

  """
  The screenshots filed against the slot, in the order the read returns them. Empty when none was filed.
  """
  screenshots: [TimeTrackingScreenshot!]!
}

"""
One engagement that tracked time over a range, as the time-slot read answers it: when it was last
active, whether it is online, and the most recent slots of the range.
"""
type TimeTrackingTimeSlotStatistic {
  """The engagement's identifier."""
  id: ID!

  """
  Whether the engagement is currently online, as the employee row states it.
  """
  isOnline: Boolean

  """
  Whether the engagement is currently away, as the employee row states it.
  """
  isAway: Boolean

  """
  When the engagement's most recent log of the range was started, which is the instant the delivered read ranks these answers by.
  """
  startedAt: DateTime

  """
  The account behind the engagement, as the batch account read answers it.
  """
  user: TimeTrackingUserSummary

  """
  The engagement's most recent slots of the range, newest first. The delivered read answers up to nine of them.
  """
  timeSlots: [TimeTrackingTimeSlot!]!
}

"""One activity title an organization spent time on over a range."""
type TimeTrackingActivityStatistic {
  """
  The activity's title, which is what the delivered read groups these answers by. An activity has a row of its own and is read from the activity surface by its identifier; this answer is the title's total, not one row.
  """
  title: String!

  """
  How many activity rows were recorded under this title over the range. A count of sessions, not of seconds.
  """
  sessions: Int

  """The time recorded under the title over the range. Seconds."""
  duration: Float

  """
  The share of the range's recorded activity this title accounts for: a percentage of the time recorded against every activity of the organization over the same range — not of the five answers returned beside it.
  """
  durationPercentage: Float
}

"""The band of activity a statistics read narrows its time slots by."""
input TimeTrackingActivityLevelInput {
  """
  The lower bound of the band, as a percentage of a slot's own activity scale.
  """
  start: Int!

  """The upper bound of the band, inclusive."""
  end: Int!
}

"""One interval of recorded work."""
type TimeLog {
  id: ID!

  """The instant the interval starts."""
  startedAt: DateTime

  """
  The instant the interval stops, or null while no end has been recorded.
  """
  stoppedAt: DateTime

  """When the log was last edited by hand. Null on a log nobody has edited."""
  editedAt: DateTime

  """
  How long the interval ran, **in seconds**: the entity fills this member on load from the two
  instants, and a running log is measured against the present moment. It is a duration and never an
  amount.
  """
  duration: Int

  """
  How the time was recorded: `TRACKED`, `MANUAL`, `IDLE` or `RESUMED`. Carried as its value rather
  than declared as a schema enum, because the vocabulary belongs to the contracts package.
  """
  logType: String

  """
  Where the time came from: `BROWSER`, `DESKTOP`, `MOBILE`, `BROWSER_EXTENSION`, `HUBSTAFF`,
  `UPWORK`, `TEAMS` or `CLOC`, carried as its value for the same reason as `logType`.
  """
  source: String

  """What was worked on, as the tracker or the person wrote it."""
  description: String

  """Why a hand-recorded interval was recorded."""
  reason: String

  """Whether the interval is billed for."""
  isBillable: Boolean

  """Whether a timer is still running against this row."""
  isRunning: Boolean

  """The version of the client that recorded the interval, where one did."""
  version: String

  """
  Whether the log has been edited since it was recorded. The delivered list read narrows by this
  member, which is why it is carried: without it a caller could not read back which of the rows it
  asked about were the edited ones.
  """
  isEdited: Boolean

  """
  The employee the interval belongs to. The engagement is read from the employee surface by this identifier.
  """
  employeeId: ID

  """
  The payroll period the interval was filed into. The period is read from the timesheet surface by this identifier.
  """
  timesheetId: ID

  """
  The project the work was attributed to. The project is read from the project surface by this identifier.
  """
  projectId: ID

  """
  The task the work was attributed to. The task is read from the task surface by this identifier.
  """
  taskId: ID

  """
  The client the work was attributed to. The client is read from the contact surface by this identifier.
  """
  organizationContactId: ID

  """
  The team the work was attributed to. The team is read from the team surface by this identifier.
  """
  organizationTeamId: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived."""
  archivedAt: DateTime

  """
  When the row was withdrawn. The delete field below writes this member, which is why it is carried.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of time logs."""
type TimeLogEdge {
  node: TimeLog!
  cursor: String!
}

"""
One page of time logs, with the total the filters select and the boundary cursors.
"""
type TimeLogConnection {
  nodes: [TimeLog!]!
  edges: [TimeLogEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a time-log list may be sorted by."""
enum TimeLogSortField {
  startedAt
  stoppedAt
  duration
  createdAt
  updatedAt
  logType
  source
  isBillable
}

"""
The order the time logs are returned in. At most three keys are accepted.
"""
input TimeLogSort {
  field: TimeLogSortField!
  direction: SortDirection!
}

"""
The fields a time-log list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read returns, and `duration` is the row's own
computed member. The relation identifiers are here and the relations themselves are not: the rows
carry the identifiers, so a filter on one narrows by a value every row has, while a filter on a
relation would narrow by a value the connection never sees.
"""
input TimeLogFilter {
  id: IDFilter
  startedAt: DateTimeFilter
  stoppedAt: DateTimeFilter
  editedAt: DateTimeFilter

  """
  The interval's length in seconds, which is the row's own computed member.
  """
  duration: NumberFilter
  logType: StringFilter
  source: StringFilter
  description: StringFilter
  reason: StringFilter
  isBillable: BooleanFilter
  isRunning: BooleanFilter
  version: StringFilter
  isEdited: BooleanFilter
  employeeId: IDFilter
  timesheetId: IDFilter
  projectId: IDFilter
  taskId: IDFilter
  organizationContactId: IDFilter
  organizationTeamId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimeLogFilter!]
  or: [TimeLogFilter!]
  not: TimeLogFilter
}

"""
The activity window a read may be narrowed to.

Both bounds are a percentage of activity between 0 and 100, which is the scale the delivered read
takes them on: it converts each to the ten-minute scale the tracked slot records its overall activity
on before it compares them. A log whose slots fall inside the window is answered; a log with no
recorded activity is not.
"""
input TimeLogActivityLevelInput {
  """The lower bound, as a percentage between 0 and 100."""
  start: Int!

  """The upper bound, as a percentage between 0 and 100."""
  end: Int!
}

"""
One day of the daily report's chart: how much of the day was tracked, recorded by hand, idle and
resumed.

All four figures are **in hours**, which is what the delivered calculation produces — the summed
seconds of each kind divided by three thousand six hundred and rounded to one decimal. A day on which
nothing was recorded is answered as four zeroes rather than dropped, so a chart drawn from this answer
has a point for every date in the range.
"""
type TimeLogDurationBreakdown {
  """Hours recorded by a tracker."""
  tracked: Float!

  """Hours recorded by hand."""
  manual: Float!

  """Hours the tracker counted as idle."""
  idle: Float!

  """Hours recorded after an idle period was resumed."""
  resumed: Float!
}

"""One day of the daily chart."""
type TimeLogDailyReportChartPoint {
  """The day, as `YYYY-MM-DD` in the time zone the caller named."""
  date: String!

  """The day's four figures."""
  value: TimeLogDurationBreakdown!
}

"""
One line of a daily report's leaf: the work one employee did on one task.

`duration` is **in seconds**. `clientId` is the client the line was attributed to — the log's own
client where it has one, and otherwise the client behind the project the work was booked to.
"""
type TimeLogDailyReportTask {
  """
  The task the line describes. The task is read from the task surface by this identifier.
  """
  taskId: ID

  """What was worked on, as the log recorded it."""
  description: String

  """The line's duration, **in seconds**."""
  duration: Int

  """
  The client the line was attributed to. The client is read from the contact surface by this identifier.
  """
  clientId: ID
}

"""
One leaf of a daily report: one employee's task lines within the level above them.

The delivered answer names this leaf `employeeLogs` in three of its four groupings and `projectLogs` in
the fourth, and it carries the same figures under either name — which is why one type is declared for
both fields rather than two a client could not tell apart, and why the type is named for what it is
rather than for one of the two names.

`sum` is **in seconds**, and it is the delivered calculation's own sum of the durations below it.
`activity` is a percentage of the tracked activity over that time. `employeeId` and `projectId` are
present where the grouping that produced the leaf was grouped by the other one.
"""
type TimeLogDailyReportGroup {
  """
  The employee the lines belong to, where the grouping did not already name them.
  """
  employeeId: ID

  """
  The project the lines were booked to, where the grouping did not already name it.
  """
  projectId: ID

  """The summed duration of the lines, **in seconds**."""
  sum: Int

  """The tracked activity over that duration, as a percentage."""
  activity: Float

  """The lines themselves, one per log."""
  tasks: [TimeLogDailyReportTask!]!
}

"""
One level of a daily report.

**The answer's shape is chosen by `groupBy`, and this type states all four shapes.** Grouped by date a
level carries the date and the projects of that date; grouped by employee it carries the employee and
the dates they worked; grouped by project it carries the project and the dates; grouped by client it
carries the client, then the projects, then the dates. The four trees are the same node with a
different dimension filled in, so one recursive type states them rather than four a client would have
to tell apart by asking which grouping it requested — and every member a level does not carry is null
rather than absent, which is what makes the recursion total.

`date` is the day as `YYYY-MM-DD` in the time zone the caller named. `sum` is **in seconds**.
"""
type TimeLogDailyReportEntry {
  """The day this level covers, where the grouping is by date."""
  date: String

  """The employee this level covers, where the grouping is by employee."""
  employeeId: ID

  """The project this level covers, where the grouping is by project."""
  projectId: ID

  """The client this level covers, where the grouping is by client."""
  clientId: ID

  """The summed duration of everything below this level, **in seconds**."""
  sum: Int

  """The tracked activity over that duration, as a percentage."""
  activity: Float

  """The levels below this one. Empty at a leaf."""
  logs: [TimeLogDailyReportEntry!]!

  """
  The employee lines of this level, under the name the grouping that produced them uses. Empty where this level carries `projectLogs` instead.
  """
  employeeLogs: [TimeLogDailyReportGroup!]!

  """
  The employee lines of this level, under the name one of the four groupings uses. Empty where this level carries `employeeLogs` instead.
  """
  projectLogs: [TimeLogDailyReportGroup!]!
}

"""
One employee's line of an owed-amount day.

`duration` is **in seconds** and `amount` is **money**: the delivered calculation prices the summed
duration at the rate the read selected for that employee, and the value travels as the exact decimal
the `numeric` column it was read from is carried as.
"""
type TimeLogOwedAmountReportEmployee {
  """
  The employee the line belongs to. The engagement is read from the employee surface by this identifier.
  """
  employeeId: ID

  """The employee's summed duration on that day, **in seconds**."""
  duration: Int!

  """What the organization owes for it. Money."""
  amount: Decimal!
}

"""One day of the owed-amount report, with the lines it is made of."""
type TimeLogOwedAmountReportEntry {
  """The day, as `YYYY-MM-DD` in the time zone the caller named."""
  date: String!

  """One line per employee who recorded time that day."""
  employees: [TimeLogOwedAmountReportEmployee!]!
}

"""One day of the owed-amount chart: the whole day's total in one figure."""
type TimeLogOwedAmountReportChartPoint {
  """The day, as `YYYY-MM-DD` in the time zone the caller named."""
  date: String!

  """What the organization owes for that day, over every employee. Money."""
  value: Decimal!
}

"""
One log of a weekly report's day.

The weekly read projects four columns of each log and no more — its identifier, the employee behind
it and the two instants — so this type states exactly those: a member for a column the read did not
select would answer null on every row it carries.
"""
type TimeLogWeeklyReportLog {
  id: ID!

  """
  The employee who recorded the interval. The engagement is read from the employee surface by this identifier.
  """
  employeeId: ID

  """The instant the interval starts."""
  startedAt: DateTime

  """The instant the interval stops."""
  stoppedAt: DateTime
}

"""
One day of a weekly report.

`sum` is **in seconds**. The delivered calculation answers the range's days as a map keyed by their own
date strings — a day nothing was recorded on being the bare number zero rather than a group — and this
surface answers the same days as a list with the key moved into the entry, so a day nothing was
recorded on is an entry whose sum is zero and whose log list is empty.
"""
type TimeLogWeeklyReportDay {
  """The day, as `YYYY-MM-DD` in the time zone the caller named."""
  date: String!

  """The summed duration recorded on that day, **in seconds**."""
  sum: Int!

  """
  The logs of that day, projected to the four columns the weekly read selected.
  """
  logs: [TimeLogWeeklyReportLog!]!
}

"""
One employee's week.

`sum` is **in seconds** and is null where the range holds no log of that employee at all, which is the
delivered answer rather than a fabricated zero. `activity` is a percentage of the tracked activity of
the week over its tracked time.
"""
type TimeLogWeeklyReport {
  """
  The employee the week belongs to. The engagement is read from the employee surface by this identifier.
  """
  employeeId: ID

  """One entry per day of the range, in the range's own order."""
  dates: [TimeLogWeeklyReportDay!]!

  """The employee's summed duration over the week, **in seconds**."""
  sum: Int

  """The week's tracked activity, as a percentage."""
  activity: Float
}

"""
One employee's line of a time-limit period.

`duration` and `limit` are both **in seconds**: the limit is the employee's weekly limit converted to
whichever of the three durations the caller asked about — a day, a week or a month — and it is zero
where the employee states no limit at all.

`durationPercentage` is the percentage of that limit the duration represents, rendered to two decimals
by the delivered calculation, and the bare number zero where there is no limit to divide by. It is a
percentage and never an amount.
"""
type TimeLogTimeLimitReportEmployee {
  """
  The employee the line belongs to. The engagement is read from the employee surface by this identifier.
  """
  employeeId: ID

  """The duration recorded in the period, **in seconds**."""
  duration: Int!

  """The percentage of `limit` the duration represents, to two decimals."""
  durationPercentage: Float!

  """The employee's limit for the period, **in seconds**."""
  limit: Int!
}

"""One period of the time-limit report, with the lines it is made of."""
type TimeLogTimeLimitReportEntry {
  """
  The first day of the period, as `YYYY-MM-DD` in the time zone the caller named.
  """
  date: String!

  """One line per employee who recorded time in the period."""
  employees: [TimeLogTimeLimitReportEmployee!]!
}

"""
How much of one project's budget a range consumed.

`budget`, `spent` and `remainingBudget` are **money**, and `spent` counts hours instead where the
project's budget is stated in hours. `spentPercentage` is a percentage of the budget and is never an
amount. `remainingBudget` is floored at zero by the delivered calculation: a project over its budget
reports nothing remaining rather than a negative remainder, and the overrun is what `spentPercentage`
above a hundred says.
"""
type ProjectBudgetLimit {
  """
  The project the budget belongs to. The project is read from the project surface by this identifier.
  """
  projectId: ID!

  """
  What the budget is measured in: the delivered vocabulary is `hours` or `cost`, carried as its value
  because it is shared with the client's own budget column rather than owned here.
  """
  budgetType: String

  """
  The budget itself. Money, or a number of hours where `budgetType` says so.
  """
  budget: Decimal!

  """
  What the range consumed against it. Money, or hours, on the same terms.
  """
  spent: Decimal!

  """What is left of the budget. Never negative."""
  remainingBudget: Decimal!

  """What share of the budget was consumed, as a percentage."""
  spentPercentage: Float!
}

"""
How much of one client's budget a range consumed.

`spent` counts hours instead where the client's budget is stated in hours, and the three amounts are
money on the same terms as the project report's.
"""
type ClientBudgetLimit {
  """
  The client the budget belongs to. The client is read from the contact surface by this identifier.
  """
  organizationContactId: ID!

  """
  What the budget is measured in: the delivered vocabulary is `hours` or `cost`.
  """
  budgetType: String

  """
  The budget itself. Money, or a number of hours where `budgetType` says so.
  """
  budget: Decimal!

  """
  What the range consumed against it. Money, or hours, on the same terms.
  """
  spent: Decimal!

  """What is left of the budget. Never negative."""
  remainingBudget: Decimal!

  """What share of the budget was consumed, as a percentage."""
  spentPercentage: Float!
}

"""
What a caller states when it records time by hand.

The organization is stated because the delivered create does not derive one: the tenant is stamped
from the credential, never from an argument, but the organization is a member the caller names. The
employee is stated and is the caller's own unless the caller may change the selected employee — the
delivered pipe rewrites it, and this surface restates that rule rather than trusting the body.

Neither the log type nor the source is a member. The delivered body's own transform states both for
itself — `MANUAL` and `BROWSER` — whatever a caller sends, so an input carrying them would be offering
two values the write ignores.
"""
input CreateManualTimeLogInput {
  organizationId: ID!

  """
  The employee the interval belongs to. A caller that may not choose one records against itself.
  """
  employeeId: ID!

  """The instant the interval starts."""
  startedAt: DateTime!

  """The instant the interval stops."""
  stoppedAt: DateTime!

  """The project to attribute the work to."""
  projectId: ID

  """The task to attribute the work to."""
  taskId: ID

  """The client to attribute the work to."""
  organizationContactId: ID

  """What was worked on."""
  description: String

  """Why the interval is being recorded by hand."""
  reason: String

  """Whether the interval is billed for."""
  isBillable: Boolean
}

"""
What a caller states when it edits a hand-recorded interval.

The same body as the create with the row's own identifier beside it, and without the two members the
create stamps for itself: the delivered update carries neither a log type nor a source, so the ones
the row already has are left as they are.
"""
input UpdateManualTimeLogInput {
  id: ID!
  organizationId: ID!
  employeeId: ID!
  startedAt: DateTime!
  stoppedAt: DateTime!
  projectId: ID
  taskId: ID
  organizationContactId: ID
  description: String
  reason: String
  isBillable: Boolean
}

"""
What a caller states when it removes time logs.

`logIds` is required and must not be empty, which is the delivered body's own validation: a removal
that names no row is refused by the read before anything is written. `forceDelete` decides whether the
rows are deleted outright or withdrawn; an absent flag is read as `false`, which is both the delivered
body's transform and the delivered handler's default.
"""
input DeleteTimeLogsInput {
  """The rows to remove."""
  logIds: [ID!]!

  """The organization the rows must belong to."""
  organizationId: ID!

  """Whether to delete the rows outright rather than withdraw them."""
  forceDelete: Boolean
}

"""One ten-minute window of one employee's tracked time."""
type TimeSlot {
  id: ID!

  """
  The seconds this window counted. Seconds, never money: a value beyond the six hundred a ten-minute
  window spans is a slot the delivered merge has not yet split.
  """
  duration: Int!

  """The number of keyboard interactions recorded inside the window."""
  keyboard: Int!

  """The number of mouse interactions recorded inside the window."""
  mouse: Int!

  """The number of movements the client reported inside the window."""
  location: Int!

  """
  The seconds of activity recorded inside the window — the counter the activity band is stated
  against and the numerator of `percentage`.
  """
  overall: Int!

  """The instant the window starts at."""
  startedAt: DateTime!

  """
  The instant the window ends at: its start plus the ten minutes it spans, derived when the row is
  loaded rather than stored. It is the end the delivered removal spans when it removes the logs a slot
  covers.
  """
  stoppedAt: DateTime

  """
  The percentage of the window that was active — `overall` against `duration`, in the range 0 to 100.
  A ratio and never an amount of money.
  """
  percentage: Float

  """
  The percentage of the window that was spent at the keyboard, in the range 0 to 100.
  """
  keyboardPercentage: Float

  """
  The percentage of the window that was spent on the mouse, in the range 0 to 100.
  """
  mousePercentage: Float

  """
  The raw keyboard and mouse activity the client posted for the window, as the document it sent.
  """
  kbMouseActivity: JSON

  """
  The raw location activity the client posted for the window, as the document it sent.
  """
  locationActivity: JSON

  """The document the tracking extensions attached to the window."""
  customActivity: JSON

  """
  The employee the window belongs to. The delivered list read does join the employee and selects a
  projection of it — the identifier, and the name and image of the account behind it — and this
  surface carries the identifier rather than that projection: a partly selected row is not a row a
  client can read anything else from, while the identifier is the one member that always travels and
  is what the employee is read by.
  """
  employeeId: ID!

  """The organization the window belongs to."""
  organizationId: ID

  """The tenant the window belongs to."""
  tenantId: ID
  createdAt: DateTime
  updatedAt: DateTime
  isActive: Boolean
  isArchived: Boolean

  """
  When the row was withdrawn, or null while it is live. The delivered writes remove slots outright
  rather than withdrawing them, so this column is stated for what it narrows — a REST caller can filter
  by it — and no field on this surface writes it.
  """
  deletedAt: DateTime
}

"""The boundary of one page of slots."""
type TimeSlotEdge {
  node: TimeSlot!
  cursor: String!
}

"""
One page of slots, with the total the filters select and the boundary cursors.
"""
type TimeSlotConnection {
  nodes: [TimeSlot!]!
  edges: [TimeSlotEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a slot list may be sorted by. Every member is a column of the row; the four derived members
are not sortable, because the store has no such column to order by.
"""
enum TimeSlotSortField {
  createdAt
  updatedAt
  startedAt
  duration
  keyboard
  mouse
  overall
  location
}

"""The order the slots are returned in. At most three keys are accepted."""
input TimeSlotSort {
  field: TimeSlotSortField!
  direction: SortDirection!
}

"""
The fields a slot list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a column of the row the delivered list read returns. `employeeId` and `organizationId`
are the identifiers the row carries for the two relations that read joins, which is how a client asks
for one employee's slots or for one organization's within the set the credential may read — the
service applies the tenant and the organization from the credential as well, so a filter naming
another scope narrows to nothing rather than reading across it.
"""
input TimeSlotFilter {
  id: IDFilter
  duration: NumberFilter
  keyboard: NumberFilter
  mouse: NumberFilter
  overall: NumberFilter
  location: NumberFilter
  startedAt: DateTimeFilter
  kbMouseActivity: JSONFilter
  locationActivity: JSONFilter
  customActivity: JSONFilter
  employeeId: IDFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimeSlotFilter!]
  or: [TimeSlotFilter!]
  not: TimeSlotFilter
}

"""
The band of activity a slot list is narrowed to, as percentages of the window.

The delivered read converts each end into the seconds its activity counter is stored in — a ten-minute
window is six hundred seconds, so one percent of it is six — so the two members are percentages and
the conversion stays where the delivered read states it.
"""
input TimeSlotActivityLevelInput {
  """The lower end of the band, as a percentage of the window."""
  start: Int!

  """The upper end of the band, as a percentage of the window."""
  end: Int!
}

"""
One activity of the window, as a tracker reports it.

The members are the activity document's own: what was in front of the employee, for how long, and when.
The employee, the organization and the tenant are deliberately absent — the delivered write stamps all
three from the credential of the call — and so is the window the activity ends up in, which is the slot
the write is being made for rather than a member a caller chooses.
"""
input TimeSlotActivityInput {
  """
  The title of what the employee was looking at — a page title, an application name.
  """
  title: String

  """A longer description of it, where the client sent one."""
  description: String

  """The document the client attached to the activity."""
  metaData: JSON

  """The calendar date the activity happened on, as the client stated it."""
  date: String

  """The time of day the activity happened at, as the client stated it."""
  time: String

  """The seconds the employee spent on it. Seconds, never money."""
  duration: Int

  """
  The kind of activity — `URL` or `APP`, as the activity domain states it. Carried as its value: the vocabulary belongs to that domain.
  """
  type: String

  """
  The source the activity is attributed to, by value (`DESKTOP`, `BROWSER`, …).
  """
  source: String

  """When the activity was recorded."""
  recordedAt: DateTime

  """
  The task the activity is attributed to. Omit it and the activity belongs to no task.
  """
  taskId: ID

  """
  The project the activity is attributed to. The slot's own `projectId` overwrites it when one is
  stated on the slot, which is the delivered write's own rule.
  """
  projectId: ID
}

"""
What a caller states when it records a window of tracked time.

The employee, the organization and the tenant are optional here rather than absent, and that is the
delivered write's own shape: the handler stamps each of them from the credential when the body does not
state one, so a tracker that reports on behalf of the account it is signed in as states neither.

The instant is the member the whole write turns on. The delivered handler rounds it to the second,
looks for the slot that instant already belongs to — answering that one rather than writing a second —
and merges the result into the ten-minute window it falls in. An absent instant is the instant the
write is handled at, which is what the handler does with it.
"""
input CreateTimeSlotInput {
  """
  The organization to record the window in. Omit it and the delivered write takes the employee's own.
  """
  organizationId: ID

  """
  The employee the window belongs to. Omit it and the delivered write stamps the caller's own employee.
  """
  employeeId: ID

  """The project the activities of this window are attributed to."""
  projectId: ID

  """
  The instant the window starts at. Omit it and the delivered write takes the instant it is handling the call.
  """
  startedAt: DateTime

  """The seconds the window counted. Seconds, never money."""
  duration: Int

  """The number of keyboard interactions recorded inside the window."""
  keyboard: Int

  """The number of mouse interactions recorded inside the window."""
  mouse: Int

  """The seconds of activity recorded inside the window."""
  overall: Int

  """The number of movements the client reported inside the window."""
  location: Int

  """
  The raw keyboard and mouse activity of the window, as the document the client sent.
  """
  kbMouseActivity: JSON

  """
  The raw location activity of the window, as the document the client sent.
  """
  locationActivity: JSON

  """The document the tracking extensions attached to the window."""
  customActivity: JSON

  """The source the window's log is attributed to, by value."""
  source: String

  """The kind of log the window is attached to, by value."""
  logType: String

  """
  The log to attach the window to when the delivered write's own lookup finds none. The delivered body
  admits one identifier or a list of them; this surface states the single identifier, which is what a
  tracker reports a window against.
  """
  timeLogId: ID

  """
  The activities recorded inside the window, as the client reported them.
  """
  activities: [TimeSlotActivityInput!]
}

"""
What a caller states when it edits a window of tracked time that exists.

The identifier is the path of the delivered route and is the first member here for the same reason: the
route states it beside the body, and the resolver hands the two to the command separately.

There is no `timeLogId` member, although the create body has one: the delivered edit never reads it, so
a caller stating it would be stating something the write drops. `source` and `logType` are absent for a
sharper reason — they are not columns of the row, and the delivered edit persists the body it is handed
column for column, so a caller stating them would be handing the store a column the row does not have.
"""
input UpdateTimeSlotInput {
  """
  The window to edit. It is stated here because a GraphQL mutation has no path.
  """
  id: ID!

  """The organization the row is written to."""
  organizationId: ID

  """
  The employee the row is written to. The delivered handler refuses to move a row onto another
  employee for a caller that may not select employees, and writes the row it found for everyone else.
  """
  employeeId: ID

  """The project the activities of this window are attributed to."""
  projectId: ID

  """
  The instant the window starts at. The delivered write rounds it to the second.
  """
  startedAt: DateTime

  """The seconds the window counted. Seconds, never money."""
  duration: Int

  """The number of keyboard interactions recorded inside the window."""
  keyboard: Int

  """The number of mouse interactions recorded inside the window."""
  mouse: Int

  """The seconds of activity recorded inside the window."""
  overall: Int

  """The number of movements the client reported inside the window."""
  location: Int

  """
  The raw keyboard and mouse activity of the window, as the document the client sent.
  """
  kbMouseActivity: JSON

  """
  The raw location activity of the window, as the document the client sent.
  """
  locationActivity: JSON

  """The document the tracking extensions attached to the window."""
  customActivity: JSON

  """
  The activities to file against the window, beside the ones it already carries.
  """
  activities: [TimeSlotActivityInput!]
}

"""
The caller's timer as it stands over the period the delivered read was anchored to.
"""
type TimerStatus {
  """
  The seconds the timer accounts for over the period: the sum of the completed logs' durations, plus,
  while a log is open, the seconds elapsed since it started. Seconds, never money.
  """
  duration: Int!

  """
  Whether a log is open right now, which is the running flag of the log the state was derived from.
  """
  running: Boolean!

  """
  The log the state was derived from — the latest log of the period, open or completed — or null when
  the period holds none. The row behind it is a time log and is read through the surface that owns it.
  """
  lastLogId: ID
}

"""One employee's worked state, as the worked read computed it."""
type TimerWorkedStatus {
  """
  The employee the state belongs to. The delivered read answers one state per employee it considered
  and the log behind each state is the member that names which employee that is, so this is read from
  the log's own column rather than from the answer — without it the list could not be attributed.
  """
  employeeId: ID!

  """The seconds the employee's last log ran. Seconds, never money."""
  duration: Int!

  """Whether that log is still open."""
  running: Boolean!

  """
  The state the delivered read computed from the log's running flag and how long ago it stopped.
  """
  timerStatus: TimerState!

  """
  The employee's last log, which the delivered read always has: it selects one log per employee and
  requires that log to have both ends. The row behind it is read through the surface that owns it.
  """
  lastLogId: ID!
}

"""
The three states the delivered worked read computes.

The values are the strings that read writes — `running` while a log is open, `pause` when the last log
stopped during the current day, `idle` when it stopped on an earlier one — and they are stated in that
case rather than upper-cased, because the platform's own answers carry them as they are: an enum whose
values differed from the computed strings would be a vocabulary nothing else in the platform speaks,
and the resolver would have to translate between the two.
"""
enum TimerState {
  running
  pause
  idle
}

"""
What a caller states when it starts, stops or toggles the timer.

One input for the three writes, because the two bodies their routes bind declare the same members —
see the note in `timer.api.gql`.

**The tenant is deliberately not a member.** The delivered writes stamp it from the credential and
never read it from a body, so a member here would promise a scope the write refuses; the same holds
for the organization *row* the route's DTO admits beside the identifier, which this surface states as
the identifier alone.

**`sentTo` is not a member either.** The DTO the timer's routes bind inherits it from the base query
DTO, and no timer write reads it: it selects a recipient for the notification mail some other
resource's writes send, so a caller stating it here would be stating something no part of these three
writes consults.
"""
input TimerToggleInput {
  """The organization the log is written in."""
  organizationId: ID

  """
  The source the log is attributed to, by value (`DESKTOP`, `BROWSER`, `MOBILE`, …).
  """
  source: String

  """
  The kind of log to open, by value (`TRACKED`, `MANUAL`, `IDLE`, `RESUMED`).
  """
  logType: String

  """Whether the recorded time is billable."""
  isBillable: Boolean

  """What the log is about, in the caller's own words."""
  description: String

  """
  The client version that opened the log, recorded as the log's version column.
  """
  version: String

  """The team the log is attributed to."""
  organizationTeamId: ID

  """The client the log is attributed to."""
  organizationContactId: ID

  """The project the log is attributed to."""
  projectId: ID

  """The task the log is attributed to."""
  taskId: ID

  """
  When the timer started. Omit it and the delivered write takes the instant it is handling the call.
  """
  startedAt: DateTime

  """
  When the timer stopped. Omit it and the delivered write takes the instant it is handling the call —
  except for a timer tracked from a desktop client, whose stop instant is derived from the last time
  slot that client recorded rather than from the request.
  """
  stoppedAt: DateTime

  """
  Whether the stop should also close the manual time slot the log is covered by. Read by the stop
  write and by nothing else.
  """
  manualTimeSlot: Boolean

  """The tags to file the log under, by the tag identifiers' text form."""
  tags: [String!]
}

"""
One employee's request to move the time booked to one project over to another, with the approver's
decision once it has one.
"""
type TimesheetProjectChangeRequest {
  id: ID!

  """
  Why the employee is asking for the move. Required, and bounded by the delivered body's own length.
  """
  reason: String!

  """
  Where the request stands, as the contracts package's own `TimesheetProjectChangeStatus` value: `PENDING`, `APPROVED` or `REJECTED`.
  """
  status: String!

  """
  When the request was approved or rejected, or null while it is pending.
  """
  reviewedAt: DateTime

  """The note the approver left, where one was left."""
  reviewNote: String

  """
  The period whose time the request concerns. The period is read from the timesheet surface by this identifier.
  """
  timesheetId: ID!

  """
  The project the time should move to. The project is read from the project surface by this identifier.
  """
  requestedProjectId: ID!

  """
  The project the time is booked to while the request is pending. Only the logs on this project are ever moved by an approval.
  """
  previousProjectId: ID!

  """
  The account that approved or rejected the request, where one has. The account is read from the user surface by this identifier.
  """
  reviewedById: ID
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of project change requests."""
type TimesheetProjectChangeRequestEdge {
  node: TimesheetProjectChangeRequest!
  cursor: String!
}

"""
One page of project change requests, with the total the filters select and the boundary cursors.
"""
type TimesheetProjectChangeRequestConnection {
  nodes: [TimesheetProjectChangeRequest!]!
  edges: [TimesheetProjectChangeRequestEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields the project change request list may be sorted by.

Every member is a column of the row: the two instants a request is dated by and the status a queue is
worked in. The project identifiers are deliberately absent — a queue is read in the order requests
arrived in, not in the order of the projects they name.
"""
enum TimesheetProjectChangeRequestSortField {
  createdAt
  updatedAt
  reviewedAt
  status
}

"""
The order the project change requests are returned in. At most three keys are accepted.
"""
input TimesheetProjectChangeRequestSort {
  field: TimesheetProjectChangeRequestSortField!
  direction: SortDirection!
}

"""
The fields the project change request list may be narrowed by.

Every member is a column of the row, which is what makes it answerable: no relation is loaded by the
delivered read, so a member for one would narrow by a value those rows do not carry. The three
identifiers the move is stated in are here for the reason they exist — the requests of one period, the
moves one project received, the moves one project is waiting for — and the period and the organization
are here as well as among the field's own arguments, because the read narrows by them in the store
while the connection narrows the rows the read answered.
"""
input TimesheetProjectChangeRequestFilter {
  id: IDFilter
  timesheetId: IDFilter
  requestedProjectId: IDFilter
  previousProjectId: IDFilter
  reviewedById: IDFilter
  status: StringFilter
  reason: StringFilter
  reviewNote: StringFilter
  reviewedAt: DateTimeFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimesheetProjectChangeRequestFilter!]
  or: [TimesheetProjectChangeRequestFilter!]
  not: TimesheetProjectChangeRequestFilter
}

"""
What a caller states when it raises a request.

Every member is required, because every one of them is what the delivered write needs: the period
whose time is in question, both endpoints of the move, the reason a reviewer reads, and the
organization both projects are resolved inside. The tenant is not a member — the service stamps it
from the credential, which is the one value a caller cannot misstate.
"""
input RequestTimesheetProjectChangeInput {
  """
  The period whose time the request concerns. The service refuses a period that is not the caller's own.
  """
  timesheetId: ID!

  """
  The project the time should move to. It must differ from the project the time is booked to.
  """
  requestedProjectId: ID!

  """
  The project the time is booked to now. Only the logs on this project are ever moved.
  """
  previousProjectId: ID!

  """Why the move is being asked for, which is what a reviewer decides on."""
  reason: String!

  """
  The organization both projects must belong to. The delivered body requires it.
  """
  organizationId: ID!
}

"""
What a caller states when it decides on a pending request.

`PENDING` is deliberately not admitted: a review always moves the request out of the pending state,
which is what the delivered body validates. Only an approval moves the time — a rejection leaves every
log exactly where it is.
"""
input ReviewTimesheetProjectChangeInput {
  """
  The decision, as the contracts package's own `TimesheetProjectChangeStatus` value. Only `APPROVED` and `REJECTED` are admitted.
  """
  status: String!

  """An optional note for the employee who raised the request."""
  reviewNote: String

  """
  The organization the request belongs to. The service refuses a request outside it.
  """
  organizationId: ID!
}

"""
One employee's tracked time over one period, with the lifecycle an approver moves it through and the
totals the platform recomputed over its slots.
"""
type Timesheet {
  id: ID!

  """
  The tracked time this period collects, **in seconds**, summed from its slots by the platform's own recalculation. A duration rather than an amount, so an `Int` and never a `Decimal`.
  """
  duration: Int!

  """
  The number of keyboard interactions, averaged over the slots of this period by the same recalculation. A count, not a duration.
  """
  keyboard: Int!

  """
  The number of mouse interactions, averaged over the slots of this period by the same recalculation. A count, not a duration.
  """
  mouse: Int!

  """
  The slots' own activity measure, averaged over this period. It is the raw figure the platform divides by the tracked time to state activity as a percentage, so it is not itself a percentage.
  """
  overall: Int!

  """When the period begins."""
  startedAt: DateTime

  """
  When the period was closed for tracking, or null while it is still open.
  """
  stoppedAt: DateTime

  """
  When an approver approved the period, or null while it has not been approved.
  """
  approvedAt: DateTime

  """
  When the period was submitted to its approver, or null while it has not been submitted.
  """
  submittedAt: DateTime

  """
  When the period was locked against further edits, or null while it is open.
  """
  lockedAt: DateTime

  """
  When the period was last edited, or null while it has never been. Whether a period has been edited is this column being set, which is the fact the entity's own derived `isEdited` member spells as a boolean.
  """
  editedAt: DateTime

  """
  Whether the period has been billed. A billed period is one the platform refuses to move tracked time out of.
  """
  isBilled: Boolean

  """
  The period's lifecycle status, as the contracts package's own `TimesheetStatus` value: `DRAFT`, `PENDING`, `IN REVIEW`, `DENIED` or `APPROVED`. Carried as its value rather than declared as a schema enum because the vocabulary belongs to the contracts package — and because one of its five members carries a space.
  """
  status: String!

  """
  The employee the period belongs to. The engagement behind it is read from the employee surface by this identifier.
  """
  employeeId: ID

  """
  The account that approved the period, where one did. The account is read from the user surface by this identifier.
  """
  approvedById: ID
  tenantId: ID

  """The organization the period is settled in."""
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime

  """
  When the period was withdrawn, or null while it is live. Withdrawing is a delivered route, so without this column a caller could not tell a withdrawn period from a live one on the answer to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of timesheets."""
type TimesheetEdge {
  node: Timesheet!
  cursor: String!
}

"""
One page of timesheets, with the total the filters select and the boundary cursors.
"""
type TimesheetConnection {
  nodes: [Timesheet!]!
  edges: [TimesheetEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields the timesheet list may be sorted by.

Every member is a column of the row. The set is the one a payroll screen reads a period in an order
for — the dates a period is filed and settled on, the tracked total and the two flags that say where
it stands — and it is deliberately narrower than the columns the type carries: sorting a tenant's
periods by who approved them is a report, and a report belongs to the statistics surface.
"""
enum TimesheetSortField {
  createdAt
  updatedAt
  startedAt
  stoppedAt
  approvedAt
  submittedAt
  lockedAt
  duration
  status
  isBilled
}

"""
The order the timesheets are returned in. At most three keys are accepted.
"""
input TimesheetSort {
  field: TimesheetSortField!
  direction: SortDirection!
}

"""
The fields the timesheet list may be narrowed by.

Every member is a column of the row, which is what makes it answerable: the delivered read loads no
relation, so a member for one would narrow by a value those rows do not carry. The two employee
identifiers are here for the reason they exist — selecting the periods of one engagement, or the
periods one account approved, is the read they are for — and the organization and the dates are here
as well as among the field's own arguments, because the read narrows by them in the store while the
connection narrows the rows the read answered.
"""
input TimesheetFilter {
  id: IDFilter
  employeeId: IDFilter
  approvedById: IDFilter
  duration: NumberFilter
  keyboard: NumberFilter
  mouse: NumberFilter
  overall: NumberFilter
  startedAt: DateTimeFilter
  stoppedAt: DateTimeFilter
  approvedAt: DateTimeFilter
  submittedAt: DateTimeFilter
  lockedAt: DateTimeFilter
  editedAt: DateTimeFilter
  isBilled: BooleanFilter
  status: StringFilter
  tenantId: IDFilter
  organizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  archivedAt: DateTimeFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [TimesheetFilter!]
  or: [TimesheetFilter!]
  not: TimesheetFilter
}

"""
What a caller states when it moves periods to a new status.

The delivered body also carries a tenant; this input does not, because the handler reads the tenant
from the credential — a member that could never change the write is not a member. The organization is
carried because the handler does read it: it narrows which of the caller's own periods the update may
touch.
"""
input UpdateTimesheetStatusInput {
  """
  The periods to move. The delivered handler refuses a body that states none.
  """
  ids: [ID!]!

  """
  The status to move them to, as the contracts package's own `TimesheetStatus` value. Approving one records the approver and the instant.
  """
  status: String

  """The organization the periods belong to, which narrows the update."""
  organizationId: ID
}

"""
What a caller states when it submits periods to their approver, or withdraws them.
"""
input SubmitTimesheetInput {
  """
  The periods to submit or withdraw. The delivered handler refuses a body that states none.
  """
  ids: [ID!]!

  """
  `submit` stamps the submission instant and mails the employees; `unsubmit` clears it. The two directions are one route on the delivered surface, so they are one field here.
  """
  status: String!

  """The organization the periods belong to, which narrows the write."""
  organizationId: ID
}

"""
One person's membership of one organization: the row that scopes a caller to an organization.
"""
type UserOrganization {
  id: ID!
  tenantId: ID

  """
  The organization this membership grants access to. The organization row is read from the organization domain by this identifier.
  """
  organizationId: ID

  """
  The account this membership belongs to. The account row is read from this surface's own `user(id)` by this identifier.
  """
  userId: ID

  """
  Whether this is the membership the person's session opens in.
  
  A person belongs to several organizations and acts in one at a time, and this flag is which one. It
  is the only fact the row carries that is not an identifier.
  """
  isDefault: Boolean!

  """
  Whether the membership currently grants what it says.
  
  The removal handler and the per-person organization count both read this flag, which is why the two
  writes below can state it: suspending a membership is a write to this column and there is no route
  of its own for it.
  """
  isActive: Boolean
  isArchived: Boolean

  """
  When the membership was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of memberships."""
type UserOrganizationEdge {
  node: UserOrganization!
  cursor: String!
}

"""
One page of memberships, with the total the filters select and the boundary cursors.
"""
type UserOrganizationConnection {
  nodes: [UserOrganization!]!
  edges: [UserOrganizationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a membership list may be sorted by.

The two timestamps and the flag. The identifiers are not here: a list of one person's memberships or
of one organization's members is that identifier stated as a filter, which is both narrower and cheaper
than an order.
"""
enum UserOrganizationSortField {
  createdAt
  updatedAt
  isDefault
}

"""
The order the memberships are returned in. At most three keys are accepted.
"""
input UserOrganizationSort {
  field: UserOrganizationSortField!
  direction: SortDirection!
}

"""
The fields a membership list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a value the delivered list read answers on the row, because the connection narrows the
rows the service returned. The two relation members the type does not carry have no filter either: a
filter on `user.email` would be a narrowing the rows this read returns cannot be evaluated against.
"""
input UserOrganizationFilter {
  id: IDFilter
  organizationId: IDFilter
  userId: IDFilter
  isDefault: BooleanFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [UserOrganizationFilter!]
  or: [UserOrganizationFilter!]
  not: UserOrganizationFilter
}

"""
What a caller states when it puts a person inside an organization.

Both identifiers are required, because a membership is the pivot between exactly two rows and one
without either is not a membership. There is no member for the tenant: the service stamps the caller's
own tenant onto the row and overwrites whatever a body states.

`isActive` and `isDefault` are members because both are facts about the membership that the delivered
writes store and the delivered reads act on. The base's `isArchived`, its soft-delete marker and its
three audit-user columns are not: the two lifecycle routes are what move the marker, and an input that
could set an audit column would be a way to write a fact the platform's own bookkeeping owns.
"""
input CreateUserOrganizationInput {
  organizationId: ID!
  userId: ID!

  """Whether the membership is the one the person's session opens in."""
  isDefault: Boolean

  """Whether the membership grants what it says. Defaults to true."""
  isActive: Boolean
}

"""
What a caller states when it changes a membership that exists.

Every member but the criterion is optional, and a member the caller leaves out is left as it is: the
delivered edit is a partial column update against the identifier the path names, not a replacement of
the row. The identifier is not among the mutable members — a membership that could be re-pointed at
another person or another organization would be a way to move access without a trace.
"""
input UpdateUserOrganizationInput {
  id: ID!
  isDefault: Boolean
  isActive: Boolean
}

"""
One person's account: who they are, how they are reached, and what they sign in as.
"""
type User {
  id: ID!
  tenantId: ID

  """
  The identity this account signs in as at a third-party provider, when it signs in through one.
  Null on an account that only signs in with a password.
  """
  thirdPartyId: String
  firstName: String
  lastName: String

  """
  The full name, merged from the two parts by the delivered reader before it answers. Null on a row
  that states neither part.
  """
  name: String

  """
  The email address, which is also one of the two ways this account signs in.
  """
  email: String
  phoneNumber: String

  """
  The handle, where the account states one. The other way this account signs in.
  """
  username: String

  """The zone this person's times are shown in."""
  timeZone: String

  """
  How a time is rendered for this person, as the kernel's own `TimeFormatEnum` value. Carried as its
  value rather than as a schema enum, because the vocabulary belongs to the platform.
  """
  timeFormat: String

  """
  The avatar URL. A column of this row, refreshed on every load from the asset `imageId` names.
  """
  imageUrl: String

  """
  The avatar asset. The asset row itself is read from the asset domain by this identifier; the URL a
  client shows is `imageUrl`, which the delivered reader keeps in step with it.
  """
  imageId: ID

  """The language this person reads and is answered in."""
  preferredLanguage: String

  """The layout this person's lists open in."""
  preferredComponentLayout: String

  """
  Per-user, per-feature interface state, keyed by feature. Written only through
  `updateUserUiPreferences`, which merges one key at a time.
  """
  uiPreferences: JSON

  """When this person last signed in. Null before the first sign-in."""
  lastLoginAt: DateTime

  """
  Whether the account's email address has been verified.
  
  Carried as the derived fact rather than as the stamp it is derived from: the delivered reader
  computes it from `emailVerifiedAt` on every load, and that column is one of the six the delivered
  answer excludes.
  """
  isEmailVerified: Boolean

  """
  The role this account acts under. The role row is read from the role domain by this identifier; the
  delivered read of this resource joins no relation.
  """
  roleId: ID

  """
  The team this person's session opens in. The team row is read from the team domain by this identifier.
  """
  defaultTeamId: ID

  """
  The team this person was last in. The team row is read from the team domain by this identifier.
  """
  lastTeamId: ID

  """
  The organization this person's session opens in. The organization row is read from the organization domain by this identifier.
  """
  defaultOrganizationId: ID

  """
  The organization this person was last in. The organization row is read from the organization domain by this identifier.
  """
  lastOrganizationId: ID

  """
  Whether the account is active. A removal sets `deletedAt`; it does not clear this flag.
  """
  isActive: Boolean
  isArchived: Boolean

  """
  When the account was withdrawn, or null while it is live. Withdrawing and restoring are delivered
  routes, and this column is the whole of what they write — so without it a caller could not tell a
  live account from a withdrawn one on the answer to the write that withdrew it.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of users."""
type UserEdge {
  node: User!
  cursor: String!
}

"""
One page of users, with the total the filters select and the boundary cursors.
"""
type UserConnection {
  nodes: [User!]!
  edges: [UserEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a user list may be sorted by.

The two timestamps, the three parts of a name, the address a person is looked up by, the handle they
sign in with, and the last sign-in — which is the order an access review reads the list in. The
identifiers are not here: a list of the accounts holding one role is that role stated as a filter,
which is both narrower and cheaper than an order.
"""
enum UserSortField {
  createdAt
  updatedAt
  name
  firstName
  lastName
  email
  username
  lastLoginAt
}

"""The order the users are returned in. At most three keys are accepted."""
input UserSort {
  field: UserSortField!
  direction: SortDirection!
}

"""
The fields a user list may be narrowed by. `and` is the default; `or` requires any of its members and
`not` excludes the rows that match it.

Every member is a value the delivered list read answers on the row, because the connection narrows the
rows the service returned: a member the read does not select would be a filter that silently selects
nothing. The six excluded columns have no member, and their absence is the point — a filter on `hash`
would answer which digest a password produced, which is the same disclosure the read refuses, asked
sideways.

`name` and `isEmailVerified` are filterable although neither is a column, because the delivered reader
merges both onto the row before the connection sees it — the name from the two name columns, the flag
from the verification stamp — so narrowing by either is a question the read answers.
"""
input UserFilter {
  id: IDFilter
  thirdPartyId: StringFilter
  name: StringFilter
  firstName: StringFilter
  lastName: StringFilter
  email: StringFilter
  phoneNumber: StringFilter
  username: StringFilter
  timeZone: StringFilter
  timeFormat: StringFilter
  imageUrl: StringFilter
  imageId: IDFilter
  preferredLanguage: StringFilter
  preferredComponentLayout: StringFilter
  uiPreferences: JSONFilter
  lastLoginAt: DateTimeFilter
  isEmailVerified: BooleanFilter
  roleId: IDFilter
  defaultTeamId: IDFilter
  lastTeamId: IDFilter
  defaultOrganizationId: IDFilter
  lastOrganizationId: IDFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [UserFilter!]
  or: [UserFilter!]
  not: UserFilter
}

"""
What a caller states when it files an account.

The members are the ones the delivered body validates: the address is required, because an account
without one cannot be reached or signed into, and the role, the two name parts, the avatar URL and the
language are optional. There is no member for the tenant, which the service stamps from the credential
and overwrites whatever a body states, and none for the organization either — a user is a row of a
tenant and not of an organization; which organizations the account belongs to is the membership
resource's fact.
"""
input CreateUserInput {
  email: String!

  """
  The role the account acts under. Resolved from the database, and refused when the caller may not grant it.
  """
  roleId: ID
  firstName: String
  lastName: String
  imageUrl: String
  preferredLanguage: String
}

"""
What a caller states when it edits an account that exists.

Every other member is optional, and a member the caller leaves out is left as it is: the delivered edit
is a partial column update against the criterion the path names, not a replacement of the row.

**`hash` carries the new password, in clear text**, under the name the delivered body states it by —
the service hashes it before the write. It is an input member and never an output one: no field of
`User` answers a digest back, and this is the only place the schema spells the word at all. The
credential columns the delivered read excludes stay excluded either way; this member writes a
replacement, it does not read the old one.

The two preferred-interface columns are deliberately not members. Each has a route of its own that
writes it for the caller's own account and nothing else, and a general edit that could set them would
be a second, wider path to the same two facts.
"""
input UpdateUserInput {
  id: ID!
  email: String
  roleId: ID
  firstName: String
  lastName: String
  imageUrl: String
  preferredLanguage: String

  """
  The new password, in clear text. Hashed by the service before it is stored.
  """
  hash: String
  defaultOrganizationId: ID
  defaultTeamId: ID
  lastOrganizationId: ID
  lastTeamId: ID
  imageId: ID
  isActive: Boolean
}

"""One stock location and its role in the network."""
type Warehouse {
  id: ID!

  """The name of the location, as entered."""
  name: String!

  """
  The location's own code, unique within the organization among the live rows.
  """
  code: String!

  """The address the location is written to."""
  email: String!
  description: String

  """
  Whether the location is in service. A removal sets `deletedAt`; it does not clear this flag.
  """
  active: Boolean!

  """
  What kind of place this is, as the kernel extension's own value: `WAREHOUSE`, `STORE`,
  `THIRD_PARTY`, `DROPSHIP` or `VIRTUAL`. It decides which locations an allocation may pick and
  whether the stock held there is own stock or a supplier's. The vocabulary is the extension's, so
  the member is carried as its value rather than declared as an enum here.
  """
  type: String

  """
  Allocation preference when several locations can serve a line. An integer rank, and **lower
  wins**; the default is `0`.
  """
  priority: Int

  """Whether the location can be selected as an in-store pickup point."""
  isPickupLocation: Boolean

  """
  Whether the location may ship. False on a virtual or dropship placeholder that only aggregates.
  """
  isFulfillmentLocation: Boolean

  """Latitude of the location, from -90 to 90. A coordinate, so a `Float`."""
  latitude: Float

  """
  Longitude of the location, from -180 to 180. A coordinate, so a `Float`.
  """
  longitude: Float

  """
  The IANA zone the location's daily cutoff and its local reporting are read in.
  """
  timezone: String

  """`HH:mm` local time after which an order ships the next working day."""
  cutoffTime: String

  """
  The seller whose own location this is, or null for a platform location. The seller table belongs
  to another package, so this is an identifier and nothing more.
  """
  sellerId: ID

  """
  The asset the location is shown with. Null on a location filed without one.
  """
  logoId: ID

  """The location's own contact row. Null on a location filed without one."""
  contactId: ID

  """Tenant extras: dock count, carrier accounts."""
  metadata: JSON
  tenantId: ID
  organizationId: ID
  isActive: Boolean
  isArchived: Boolean

  """When the row was archived, or null while it is not."""
  archivedAt: DateTime

  """
  When the location was withdrawn, or null while it is live. Withdrawing and restoring are
  delivered routes — the two removals this resource serves — and this column is the whole of what
  they write.
  """
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of locations."""
type WarehouseEdge {
  node: Warehouse!
  cursor: String!
}

"""
One page of locations, with the total the filters select and the boundary cursors.
"""
type WarehouseConnection {
  nodes: [Warehouse!]!
  edges: [WarehouseEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a location list may be sorted by."""
enum WarehouseSortField {
  createdAt
  updatedAt
  name
  code
  email
  priority
}

"""
The order the locations are returned in. At most three keys are accepted.
"""
input WarehouseSort {
  field: WarehouseSortField!
  direction: SortDirection!
}

"""
The fields a location list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row the delivered list read answers, because the connection narrows
the rows the service returned rather than translating the filter into a query. `type` is carried as
text for the same reason the object type carries it that way.
"""
input WarehouseFilter {
  id: IDFilter
  organizationId: IDFilter
  name: StringFilter
  code: StringFilter
  email: StringFilter
  description: StringFilter
  active: BooleanFilter
  type: StringFilter
  priority: NumberFilter
  isPickupLocation: BooleanFilter
  isFulfillmentLocation: BooleanFilter
  timezone: StringFilter
  cutoffTime: StringFilter
  sellerId: IDFilter
  logoId: IDFilter
  contactId: IDFilter
  metadata: JSONFilter
  isActive: BooleanFilter
  isArchived: BooleanFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [WarehouseFilter!]
  or: [WarehouseFilter!]
  not: WarehouseFilter
}

"""
What a caller states when it files a location.

The organization is required because the delivered create validates it: `POST /warehouses` binds a
DTO that intersects the organization base. The name, the code and the email are required for the same
reason — the DTO marks all three non-empty. There is no member for the tenant, because the service
stamps the caller's own tenant onto the row.

The members the entity carries but the delivered body does not — the location's kind, its rank, its
two role flags, its coordinates, its zone, its cutoff, its seller and its metadata — are deliberately
absent: that body is validated with `whitelist: true`, so a member the DTO does not declare is
stripped before the write sees it. Filing a location that ships, or one that can be picked up from,
is therefore not something either protocol currently offers, and this input says so by not offering
it either.

The tag pivot is stated as an identifier list, which is this protocol's spelling of the same write:
the delivered body carries a whole `tags` array and the store persists the pivot rows for it.
"""
input CreateWarehouseInput {
  organizationId: ID!
  name: String!
  code: String!
  email: String!
  description: String
  active: Boolean

  """The asset the location is shown with, by identifier."""
  logoId: ID

  """The location's own contact row, by identifier."""
  contactId: ID

  """The tags to classify the location with."""
  tagIds: [ID!]
}

"""
What a caller states when it changes a location that exists.

**The organization, the name, the code and the email are required, and the write is nevertheless a
save.** The delivered route validates those four on the body and then hands the parsed body to a
service whose edit persists the stated columns beside the path identifier: the columns the body
carries are written and the ones it does not are left as they are. The route is simply stricter about
what a caller must restate than the write is about what it overwrites.
"""
input UpdateWarehouseInput {
  id: ID!
  organizationId: ID!
  name: String!
  code: String!
  email: String!
  description: String
  active: Boolean
  logoId: ID
  contactId: ID
  tagIds: [ID!]
}

"""
The cached product-level stock of one location: the level row an allocation reads.
"""
type WarehouseProduct {
  id: ID!

  """The location the level belongs to."""
  warehouseId: ID!

  """The product the level counts."""
  productId: ID!

  """
  The quantity held, in the reference unit of the level's measurement family, as an exact decimal
  with six fractional digits.
  """
  quantity: Decimal

  """
  Quantity held by open reservations, aggregated over the variants of this product at this location.
  """
  reservedQuantity: Decimal

  """
  Quantity on approved purchase orders and inbound transfers, not yet received.
  """
  incomingQuantity: Decimal

  """The buffer that has to remain unsold; availability subtracts it."""
  safetyStock: Decimal

  """Whether orders may be taken beyond availability at this location."""
  allowBackorder: Boolean

  """
  The most negative availability permitted when `allowBackorder` is true. Null means no ceiling was
  set, which is not the same as a ceiling of zero.
  """
  backorderLimit: Decimal

  """
  The level at or below which the low-stock alert and the purchasing suggestion fire.
  """
  restockThreshold: Decimal

  """
  When false the ledger is not consulted and the product is always sellable at this location.
  """
  trackInventory: Boolean

  """Whether availability at this location is reported as unlimited."""
  isUnlimited: Boolean

  """
  The physical shelf or bin label, where the location is not organised into zones and bins.
  """
  binLocation: String

  """
  The optimistic-lock counter: every write takes the value it read and bumps it.
  """
  version: Int

  """Tenant extras."""
  metadata: JSON

  """
  The variant levels under this product at this location. Nullable because the delivered quantity
  write answers the level row it saved without loading them, while the inventory read loads them —
  so a client that selects this member on the answer to a write may be told there are none loaded
  rather than that there are none.
  """
  variants: [WarehouseProductVariant!]
  tenantId: ID
  organizationId: ID
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The authoritative quantity of one variant at one location."""
type WarehouseProductVariant {
  id: ID!

  """The product-level row this variant's quantity rolls up into."""
  warehouseProductId: ID!

  """The variant being counted."""
  variantId: ID!

  """
  The quantity held at this level, as an exact decimal with six fractional digits.
  """
  quantity: Decimal

  """Quantity held by open reservations at this level."""
  reservedQuantity: Decimal

  """The buffer that has to stay unsold, subtracted from availability."""
  safetyStock: Decimal

  """Quantity already on its way to this level."""
  incomingQuantity: Decimal

  """
  How far a hold may exceed the quantity on hand. Null means no ceiling was set.
  """
  backorderLimit: Decimal

  """
  Whether the level is untracked. A write never drives an untracked level negative.
  """
  isUnlimited: Boolean

  """Whether a hold may exceed the quantity on hand at this level."""
  allowBackorder: Boolean

  """The optimistic-lock counter for this level."""
  version: Int

  """
  The threshold for low-stock alerting and replenishment suggestions at this level.
  """
  restockThreshold: Decimal

  """When false this variant is not counted at this location."""
  trackInventory: Boolean

  """
  The pick location for this variant, where the location is not organised into zones and bins.
  """
  binLocation: String

  """
  The level's home bin on a location in zone-and-bin mode. The bin table belongs to another package.
  """
  binId: ID

  """The measurement family every quantity on this row is expressed in."""
  unitCategoryId: ID

  """Tenant extras."""
  metadata: JSON
  tenantId: ID
  organizationId: ID
  deletedAt: DateTime
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of stock levels."""
type WarehouseProductEdge {
  node: WarehouseProduct!
  cursor: String!
}

"""
One page of stock levels, with the total the filters select and the boundary cursors.
"""
type WarehouseProductConnection {
  nodes: [WarehouseProduct!]!
  edges: [WarehouseProductEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""The fields a stock-level list may be sorted by."""
enum WarehouseProductSortField {
  createdAt
  updatedAt
  quantity
  reservedQuantity
  productId
  binLocation
}

"""
The order the stock levels are returned in. At most three keys are accepted.
"""
input WarehouseProductSort {
  field: WarehouseProductSortField!
  direction: SortDirection!
}

"""
The fields a stock-level list may be narrowed by.

`quantity` and `reservedQuantity` are `Decimal` rather than `NUMBER`: they are `numeric` columns, and
a bound compared through a binary fraction would select the wrong rows.
"""
input WarehouseProductFilter {
  id: IDFilter
  warehouseId: IDFilter
  productId: IDFilter
  quantity: DecimalFilter
  reservedQuantity: DecimalFilter
  incomingQuantity: DecimalFilter
  safetyStock: DecimalFilter
  allowBackorder: BooleanFilter
  backorderLimit: DecimalFilter
  restockThreshold: DecimalFilter
  trackInventory: BooleanFilter
  isUnlimited: BooleanFilter
  binLocation: StringFilter
  version: NumberFilter
  metadata: JSONFilter
  deletedAt: DateTimeFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [WarehouseProductFilter!]
  or: [WarehouseProductFilter!]
  not: WarehouseProductFilter
}

"""
What a caller states when it opens stock levels at one location.

The delivered body is an array of rows, each carrying a product identifier; this protocol states the
same list as one member of one input, because an argument that is itself a list of documents has no
spelling this schema's other resolvers establish, and the delivered service reads nothing from each
row but that identifier — it loads each product with its own variants and opens a level for every one
of them.

The `variantIds` member of the contracts type the delivered body accepts is deliberately not here:
the delivered service never reads it. A member a write accepts and drops is worse stated than one it
does not have.
"""
input AddWarehouseProductsInput {
  """The products to open a level for, at the location the field names."""
  productIds: [ID!]!
}

"""
An outbound delivery endpoint: where a consumer outside the platform receives events.

A subscription matches an event name against its `events` patterns at dispatch time, so a domain that
writes to the outbox is immediately subscribable without this table being told about it. One
subscription per `(organization, url)`, because a duplicate endpoint would double-deliver every
matching event; and an endpoint that keeps refusing is switched off rather than left to be hammered.
"""
type WebhookSubscription {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The operator-facing name of the endpoint."""
  name: String!

  """
  Absolute HTTPS endpoint. The platform refuses to call anything else, in production.
  """
  url: String!

  """
  The event names or patterns this endpoint receives: `order.placed` matches exactly that event,
  `order.*` matches one more segment, `*` matches any event at any depth, and `*.payment.*` matches a
  payment event under any aggregate. The list is never empty — a subscription that could never
  deliver anything is refused rather than stored.
  """
  events: [String!]!

  """The channel this subscription listens to; null means every channel."""
  channelId: ID

  """Free-text note for operators."""
  description: String

  """
  Static extra headers sent with every delivery, for a routing key a receiver needs. Non-secret by
  contract, and never allowed to override a reserved delivery header.
  """
  headers: JSON

  """
  The payload version this endpoint receives; the platform's own version when it pins none.
  """
  apiVersion: String

  """Consecutive refusals; reset by a success and by the enable operation."""
  failureCount: Int!

  """When the endpoint last accepted a delivery."""
  lastSuccessAt: DateTime

  """When the endpoint last refused one."""
  lastFailureAt: DateTime

  """
  When the switch was thrown, by an operator or by the auto-disable rule.
  """
  disabledAt: DateTime

  """
  The operator switch. A disabled subscription is never selected for a delivery.
  """
  isActive: Boolean

  """
  Operator metadata. It carries the previous secret's *ciphertext* and its expiry during a rotation
  grace window, along with the reason the last disable recorded — never a readable secret.
  """
  metadata: JSON

  """
  A short digest of the signing secret: enough to tell two subscriptions apart, and useless for
  signing. It is what every read of this resource answers in the secret's place.
  """
  secretFingerprint: String!
  createdAt: DateTime
  updatedAt: DateTime
}

"""The boundary of one page of subscriptions."""
type WebhookSubscriptionEdge {
  node: WebhookSubscription!
  cursor: String!
}

"""
One page of subscriptions, with the total the filters select and the boundary cursors.
"""
type WebhookSubscriptionConnection {
  nodes: [WebhookSubscription!]!
  edges: [WebhookSubscriptionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a subscription list may be sorted by.

A webhook list is read as a fleet: what was added most recently, what is failing most, and what has
not been heard from in the longest. The identifiers and the event patterns are not sort keys — those
are filters — and neither is the secret, in any form.
"""
enum WebhookSubscriptionSortField {
  createdAt
  updatedAt
  name
  failureCount
  lastSuccessAt
  lastFailureAt
  isActive
}

"""
The order the subscriptions are returned in. At most three keys are accepted.
"""
input WebhookSubscriptionSort {
  field: WebhookSubscriptionSortField!
  direction: SortDirection!
}

"""
The fields a subscription list may be narrowed by. `and` is the default; `or` requires any of its
members and `not` excludes the rows that match it.

Every member is a column of the row, which is why the set is what it is. The secret is deliberately
absent although the row carries it: a condition on a value no read answers could only ever be a
condition a client cannot verify, and the fingerprint is a digest rather than something a caller
filters on. The tenant and the organization are absent because both are applied to the criterion from
the credential rather than from the caller.
"""
input WebhookSubscriptionFilter {
  id: IDFilter
  name: StringFilter
  url: StringFilter
  events: JSONFilter
  channelId: IDFilter
  description: StringFilter
  headers: JSONFilter
  apiVersion: StringFilter
  failureCount: NumberFilter
  lastSuccessAt: DateTimeFilter
  lastFailureAt: DateTimeFilter
  disabledAt: DateTimeFilter
  isActive: BooleanFilter
  metadata: JSONFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [WebhookSubscriptionFilter!]
  or: [WebhookSubscriptionFilter!]
  not: WebhookSubscriptionFilter
}

"""
One delivery of one event to one subscription.

`(subscriptionId, eventId)` is unique, which is what makes the dispatcher idempotent: a re-run of the
dispatch pass finds this row instead of creating a second one, so an event is never delivered twice
because a pass was retried. The row holds the body that was decided on and never regenerates it, so a
redelivery sends exactly what was originally intended.

The body itself is deliberately not a member; see the note at the head of this document.
"""
type WebhookDelivery {
  id: ID!
  tenantId: ID
  organizationId: ID

  """The subscription the delivery belongs to."""
  subscriptionId: ID!

  """The event this delivery carries, as the outbox recorded it."""
  eventId: ID!

  """
  Denormalised event name, so a delivery list is readable without the outbox row.
  """
  eventName: String!

  """
  Where the delivery stands. `DEAD` is terminal and is reached only when nothing is left to try.
  """
  status: WebhookDeliveryStatus!

  """Times the endpoint was called."""
  attemptCount: Int!

  """
  HTTP status the endpoint answered with, absent when the request never completed.
  """
  responseStatus: Int

  """The receiver's response body, truncated, kept for triage."""
  responseBody: String

  """How long the last attempt took, in milliseconds."""
  durationMs: Int

  """When the next attempt is due; null once the delivery is settled."""
  nextAttemptAt: DateTime

  """When the endpoint acknowledged the delivery."""
  deliveredAt: DateTime

  """The transport, TLS or timeout error of the last attempt."""
  lastError: String

  """
  The attempt the row records, or null when it has not been attempted yet. One attempt rather than a
  history: the row keeps the counter and the outcome of the attempt it last made, and what a receiver
  deduplicated on is the two of them joined.
  """
  lastAttempt: WebhookAttempt
  createdAt: DateTime
  updatedAt: DateTime
}

"""Where a delivery stands."""
enum WebhookDeliveryStatus {
  """
  Written and not yet attempted, or awaiting the instant the next attempt is due.
  """
  PENDING

  """
  The endpoint answered `2xx`; the delivery is settled and the subscription's failure count resets.
  """
  DELIVERED

  """The last attempt was refused and a further one is scheduled."""
  FAILED

  """
  Nothing left to try: the schedule ran out, or the endpoint answered in a way that will not change.
  """
  DEAD
}

"""
One attempt of one delivery.

The platform's webhook contract names an attempt `<deliveryId>.<attempt>` — that is the `X-Delivery`
header value a receiver deduplicates on — so an attempt is a concept rather than a row, and this is
it: the identity the receiver saw and the outcome the delivery row kept. Nothing else is stored per
attempt, and nothing here claims to be a history of them.
"""
type WebhookAttempt {
  """
  The attempt identity the receiver saw in `X-Delivery`: `<deliveryId>.<attempt>`.
  """
  id: ID!

  """The delivery the attempt belongs to."""
  deliveryId: ID!

  """The 1-based attempt number, which is the `X-Attempt` header value."""
  attempt: Int!

  """Whether the endpoint answered `2xx`."""
  delivered: Boolean!

  """HTTP status of the attempt, absent when the request never completed."""
  responseStatus: Int

  """The receiver's response body, truncated for triage."""
  responseBody: String

  """How long the attempt took, in milliseconds."""
  durationMs: Int

  """The transport, TLS or timeout error, when there was no answer."""
  lastError: String
}

"""The boundary of one page of deliveries."""
type WebhookDeliveryEdge {
  node: WebhookDelivery!
  cursor: String!
}

"""
One page of deliveries, with the total the filters select and the boundary cursors.
"""
type WebhookDeliveryConnection {
  nodes: [WebhookDelivery!]!
  edges: [WebhookDeliveryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
The fields a delivery list may be sorted by.

The two timestamps are how a delivery is found again, `nextAttemptAt` is the work list's own order —
what is due first stands first, and the rows already settled carry no instant and trail it — and the
rest answer the questions a queue is inspected with.
"""
enum WebhookDeliverySortField {
  createdAt
  updatedAt
  nextAttemptAt
  deliveredAt
  attemptCount
  responseStatus
  eventName
  status
}

"""
The order the deliveries are returned in. At most three keys are accepted.
"""
input WebhookDeliverySort {
  field: WebhookDeliverySortField!
  direction: SortDirection!
}

"""Comparison on the delivery status."""
input WebhookDeliveryStatusFilter {
  eq: WebhookDeliveryStatus
  ne: WebhookDeliveryStatus
  in: [WebhookDeliveryStatus!]
  nin: [WebhookDeliveryStatus!]
  isNull: Boolean
}

"""
The fields a delivery list may be narrowed by. `and` is the default; `or` requires any of its members
and `not` excludes the rows that match it.

Every member is a column of the row, and the one column the row carries that is absent here is the
stored body — for the reason stated at the head of this document, and because a condition on a value
this surface does not answer could only ever be a condition a client cannot verify.
"""
input WebhookDeliveryFilter {
  id: IDFilter
  subscriptionId: IDFilter
  eventId: IDFilter
  eventName: StringFilter
  status: WebhookDeliveryStatusFilter
  attemptCount: NumberFilter
  responseStatus: NumberFilter
  responseBody: StringFilter
  durationMs: NumberFilter
  nextAttemptAt: DateTimeFilter
  deliveredAt: DateTimeFilter
  lastError: StringFilter
  createdAt: DateTimeFilter
  updatedAt: DateTimeFilter
  and: [WebhookDeliveryFilter!]
  or: [WebhookDeliveryFilter!]
  not: WebhookDeliveryFilter
}

"""What a caller states when it subscribes an endpoint."""
input CreateWebhookSubscriptionInput {
  """The operator-facing name of the endpoint."""
  name: String!

  """Absolute HTTPS endpoint."""
  url: String!

  """
  The event names or patterns the endpoint receives. At least one is required.
  """
  events: [String!]!

  """The channel to listen to; omit for every channel."""
  channelId: ID

  """Free-text note for operators."""
  description: String

  """
  Static extra headers sent with every delivery; a reserved header name is ignored, never honoured.
  """
  headers: JSON

  """The payload version this endpoint expects."""
  apiVersion: String

  """
  Operator metadata, including the development-only `allowInsecure` the endpoint check reads.
  """
  metadata: JSON
}

"""
What a caller may change on a subscription that exists.

Every member is optional: the delivered write touches the members the body states and leaves the rest
as they are. `channelId: null` is a statement rather than an absence — it widens the subscription back
to every channel — and an omitted member leaves the current channel alone.
"""
input UpdateWebhookSubscriptionInput {
  id: ID!

  """The operator-facing name of the endpoint."""
  name: String

  """Absolute HTTPS endpoint."""
  url: String

  """
  The event names or patterns the endpoint receives, replacing the set whole.
  """
  events: [String!]

  """The channel to listen to; an explicit null listens to every channel."""
  channelId: ID

  """Free-text note for operators."""
  description: String

  """Static extra headers, replacing the set whole."""
  headers: JSON

  """The payload version this endpoint expects."""
  apiVersion: String

  """Operator metadata, replaced whole."""
  metadata: JSON
}

"""
The subscription and the secret that was generated for it.

The secret is readable at exactly two moments — creation and rotation — and this is how those two
operations hand it over. It is an answer rather than a type member on purpose: a member would be
selectable by any read of the resource, and a secret that any read can produce is not a secret.
`previousSecretValidUntil` is stated on a rotation and absent on a creation, where there is no
previous secret to keep working.
"""
type WebhookSubscriptionCredential {
  """The subscription, with its secret replaced by a fingerprint."""
  subscription: WebhookSubscription!

  """The plaintext secret, which no later read can produce again."""
  secret: String!

  """When the previous secret stops being accepted, on a rotation."""
  previousSecretValidUntil: DateTime
}

type ProductReview {
  id: ID!
  body: String
  rating: Float!
}

enum PublicationStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum CollectionType {
  MANUAL
  RULE_BASED
  HYBRID
}

enum ProductRelationType {
  RELATED
  UPSELL
  CROSS_SELL
  ACCESSORY
  SPARE_PART
}

type Collection {
  id: ID!
  name: String!
  slug: String!
  description: String
  type: CollectionType!
  status: PublicationStatus!
  sortOrder: Int!
  isFeatured: Boolean!
  startsAt: DateTime
  endsAt: DateTime
  publishedAt: DateTime
  imageAssetId: ID
  parentId: ID
  customerId: ID
  parent: Collection
  children: [Collection!]
  products: [CollectionProduct!]
  variants: [CollectionVariant!]
  channelPublications: [CollectionChannel!]
  metadata: JSON
  createdAt: DateTime!
  updatedAt: DateTime!
}

type CollectionProduct {
  id: ID!
  collectionId: ID!
  productId: ID!
  position: Int!
  addedAt: DateTime!
  collection: Collection
  product: Product
  createdAt: DateTime!
  updatedAt: DateTime!
}

type CollectionVariant {
  id: ID!
  collectionId: ID!
  variantId: ID!
  position: Int!
  addedAt: DateTime!
  collection: Collection
  variant: ProductVariant
  createdAt: DateTime!
  updatedAt: DateTime!
}

type CollectionChannel {
  id: ID!
  collectionId: ID!
  channelId: ID!
  status: PublicationStatus!
  publishedAt: DateTime
  collection: Collection
  createdAt: DateTime!
  updatedAt: DateTime!
}

type ProductPublication {
  id: ID!
  productId: ID!
  channelId: ID!
  status: PublicationStatus!
  publishedAt: DateTime
  unpublishedAt: DateTime
  sortOrder: Int!
  isFeatured: Boolean!
  product: Product
  createdAt: DateTime!
  updatedAt: DateTime!
}

type ProductVariantPublication {
  id: ID!
  variantId: ID!
  channelId: ID!
  status: PublicationStatus!
  publishedAt: DateTime
  unpublishedAt: DateTime
  sortOrder: Int!
  variant: ProductVariant
  createdAt: DateTime!
  updatedAt: DateTime!
}

type ProductRelation {
  id: ID!
  productId: ID!
  relatedProductId: ID!
  type: ProductRelationType!
  position: Int!
  product: Product
  relatedProduct: Product
  createdAt: DateTime!
  updatedAt: DateTime!
}

type ProductVariantMedia {
  id: ID!
  variantId: ID!
  imageAssetId: ID!
  position: Int!
  isPrimary: Boolean!
  variant: ProductVariant
  imageAsset: ImageAsset
  createdAt: DateTime!
  updatedAt: DateTime!
}

type TagProductVariant {
  id: ID!
  productVariantId: ID!
  tagId: ID!
  productVariant: ProductVariant
  tag: Tag
  createdAt: DateTime!
  updatedAt: DateTime!
}

type CollectionVariantConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [CollectionVariant!]!
  edges: [CollectionVariantEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One collection variant inside a page, with the cursor that addresses it.
"""
type CollectionVariantEdge {
  cursor: String!
  node: CollectionVariant!
}

type CollectionChannelConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [CollectionChannel!]!
  edges: [CollectionChannelEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One collection channel inside a page, with the cursor that addresses it.
"""
type CollectionChannelEdge {
  cursor: String!
  node: CollectionChannel!
}

type TagProductVariantConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [TagProductVariant!]!
  edges: [TagProductVariantEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One tag product variant inside a page, with the cursor that addresses it.
"""
type TagProductVariantEdge {
  cursor: String!
  node: TagProductVariant!
}

type CollectionConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [Collection!]!
  edges: [CollectionEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One collection inside a page, with the cursor that addresses it."""
type CollectionEdge {
  cursor: String!
  node: Collection!
}

type CollectionProductConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [CollectionProduct!]!
  edges: [CollectionProductEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One collection product inside a page, with the cursor that addresses it.
"""
type CollectionProductEdge {
  cursor: String!
  node: CollectionProduct!
}

type ProductPublicationConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [ProductPublication!]!
  edges: [ProductPublicationEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One product publication inside a page, with the cursor that addresses it.
"""
type ProductPublicationEdge {
  cursor: String!
  node: ProductPublication!
}

type ProductVariantPublicationConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [ProductVariantPublication!]!
  edges: [ProductVariantPublicationEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One product variant publication inside a page, with the cursor that addresses it.
"""
type ProductVariantPublicationEdge {
  cursor: String!
  node: ProductVariantPublication!
}

type ProductRelationConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [ProductRelation!]!
  edges: [ProductRelationEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One product relation inside a page, with the cursor that addresses it."""
type ProductRelationEdge {
  cursor: String!
  node: ProductRelation!
}

type ProductVariantMediaConnection {
  """The rows of this page, in the order the service returned them."""
  nodes: [ProductVariantMedia!]!
  edges: [ProductVariantMediaEdge!]!

  """How many rows the filters match, in total rather than in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
One product variant media inside a page, with the cursor that addresses it.
"""
type ProductVariantMediaEdge {
  cursor: String!
  node: ProductVariantMedia!
}

input CollectionFilter {
  id: ID
  slug: String
  type: CollectionType
  status: PublicationStatus
  parentId: ID
  customerId: ID
  isFeatured: Boolean
}

input CollectionSort {
  field: String!
  direction: String
}

input CollectionProductFilter {
  collectionId: ID
  productId: ID
}

input CollectionVariantFilter {
  collectionId: ID
  variantId: ID
}

input CollectionChannelFilter {
  collectionId: ID
  channelId: ID
  status: PublicationStatus
}

input TagProductVariantFilter {
  productVariantId: ID
  tagId: ID
}

input ProductPublicationFilter {
  productId: ID
  channelId: ID
  status: PublicationStatus
}

input ProductVariantPublicationFilter {
  variantId: ID
  channelId: ID
  status: PublicationStatus
}

input ProductRelationFilter {
  productId: ID
  relatedProductId: ID
  type: ProductRelationType
}

input ProductVariantMediaFilter {
  variantId: ID
  imageAssetId: ID
}

input CreateCollectionInput {
  name: String!
  slug: String!
  description: String
  type: CollectionType
  status: PublicationStatus
  sortOrder: Int
  isFeatured: Boolean
  startsAt: DateTime
  endsAt: DateTime
  imageAssetId: ID
  parentId: ID
  customerId: ID
  metadata: JSON
}

input UpdateCollectionInput {
  name: String
  slug: String
  description: String
  type: CollectionType
  status: PublicationStatus
  sortOrder: Int
  isFeatured: Boolean
  startsAt: DateTime
  endsAt: DateTime
  imageAssetId: ID
  parentId: ID
  metadata: JSON
}

input CreateProductRelationInput {
  productId: ID!
  relatedProductId: ID!
  type: ProductRelationType
  position: Int
}

input UpdateProductRelationInput {
  type: ProductRelationType
  position: Int
}

input CollectionChannelInput {
  channelId: ID!
  status: PublicationStatus!
  publishedAt: DateTime
}

input ProductVariantPublicationInput {
  channelId: ID!
  status: PublicationStatus!
  publishedAt: DateTime
}

input ProductVariantMediaInput {
  imageAssetId: ID!
  position: Int
  isPrimary: Boolean
}

input UpdateProductPublicationInput {
  status: PublicationStatus
  publishedAt: DateTime
  unpublishedAt: DateTime
  sortOrder: Int
  isFeatured: Boolean
}

input UpdateProductVariantPublicationInput {
  status: PublicationStatus
  publishedAt: DateTime
  unpublishedAt: DateTime
  sortOrder: Int
}

"""A named, scoped, time-boxed set of prices."""
type PriceList {
  id: ID!
  tenantId: ID
  organizationId: ID
  name: String!
  code: String!
  description: String

  """Whether the list competes on price or wins outright for its context."""
  type: PriceListType!
  status: PriceListStatus!

  """Higher wins between two eligible lists."""
  priority: Int!

  """
  Currency the list's prices are expressed in; null applies to any currency.
  """
  currency: String
  channelId: ID
  customerGroupId: ID
  regionId: ID
  startsAt: DateTime
  endsAt: DateTime
  isTaxInclusive: Boolean!
  metadata: JSON
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""The price of one product variant."""
type ProductPrice {
  id: ID!
  tenantId: ID
  organizationId: ID

  """
  Null is the default price of the variant, used when no price list wins.
  """
  priceListId: ID

  """
  Null is an open-scoped row: its applicability is exactly its own PRICE rules.
  """
  variantId: ID
  currency: String!

  """
  The price, for a row that states one; null exactly when the row derives its price.
  """
  amount: Decimal

  """How the row computes its price."""
  computeMode: PriceComputeMode!

  """
  Signed fraction of the base: positive reduces it, negative is a cost-plus markup.
  """
  percent: Decimal

  """Which price a derivation starts from."""
  baseSource: PriceBaseSource

  """The list a PRICE_LIST derivation reads."""
  basePriceListId: ID

  """
  The multiple the derived amount is rounded to, before the currency rounding boundary.
  """
  roundTo: Decimal

  """
  The unit the quantity bounds are expressed in; null means the variant's sales unit.
  """
  unitId: ID

  """Display-only "was" price. It never enters a total."""
  compareAtAmount: Decimal

  """Cost snapshot the margin guard reads. It never enters a total."""
  costAmount: Decimal

  """Lower bound of the quantity band; null is open."""
  minQuantity: Decimal

  """Upper bound of the quantity band; null is open."""
  maxQuantity: Decimal

  """Null inherits from the price list, then from a price preference."""
  taxInclusive: Boolean

  """Margin floor as a fraction, e.g. 0.250000 for 25 %."""
  minMarginPercent: Decimal

  """Discount ceiling as a fraction, e.g. 0.300000 for 30 %."""
  maxDiscountPercent: Decimal
  status: PriceStatus!
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""How one scope presents prices."""
type PricePreference {
  id: ID!
  tenantId: ID
  organizationId: ID
  attribute: PricePreferenceAttribute!

  """An ISO currency code, a region id or code, or a channel code."""
  value: String!
  isTaxInclusive: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""The conversion between two currencies, valid from an instant."""
type ExchangeRate {
  id: ID!
  tenantId: ID
  organizationId: ID
  fromCurrency: String!
  toCurrency: String!
  rate: Decimal!

  """Rate source; null means it was entered by hand."""
  providerKey: String
  validFrom: DateTime!
  validUntil: DateTime
  isManual: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""The effective price of one variant for one context."""
type ResolvedPrice {
  variantId: ID!

  """
  The winning `product_price` row, absent when the legacy retail price was used.
  """
  priceId: ID

  """
  The winning price list, absent for a default price or the legacy fallback.
  """
  priceListId: ID
  currency: String!

  """What one unit costs."""
  amount: Decimal!

  """
  The base a derived winner computed from; null for a row that states its own amount.
  """
  baseAmount: Decimal

  """How the winning row arrived at its amount."""
  computation: PriceComputation

  """
  What it would cost without the winning list, when that is a real reduction.
  """
  originalAmount: Decimal
  compareAtAmount: Decimal
  taxInclusive: Boolean!
  source: PriceSource!

  """
  The conditions the resolution applied, for an operator reading the decision.
  """
  matchedRules: [String!]!

  """
  Notices the resolution raised: a margin floor passed, a candidate whose base did not resolve.
  """
  notices: [String!]!

  """A human-readable account of the decision."""
  explain: String!
}

"""How a winning price row arrived at its amount."""
type PriceComputation {
  computeMode: PriceComputeMode!

  """Signed fraction of the base."""
  percent: Decimal

  """Which price the derivation started from."""
  baseSource: PriceBaseSource

  """The list a PRICE_LIST derivation read."""
  basePriceListId: ID

  """The multiple the derived amount was quantised to."""
  roundTo: Decimal
}

"""How a price row computes its price."""
enum PriceComputeMode {
  """The row's amount is the price."""
  AMOUNT

  """The price is derived from a base and a signed share."""
  PERCENT_OFF
}

"""Which price a derivation starts from."""
enum PriceBaseSource {
  """The legacy variant retail price, converted when the currencies differ."""
  LIST

  """The row's cost snapshot, else the legacy variant unit cost."""
  COST

  """Another price list, named by basePriceListId."""
  PRICE_LIST
}

"""How a price list competes for a context."""
enum PriceListType {
  """
  Competes on price: wins only when its resolved price is lower than the default price.
  """
  SALE

  """Wins outright for its context, whatever the default price is."""
  OVERRIDE
}

"""The lifecycle of a price list."""
enum PriceListStatus {
  """Being built. Not eligible for resolution."""
  DRAFT

  """Eligible for resolution inside its window."""
  ACTIVE

  """
  Deliberately withdrawn; its prices are retained and resolve to nothing.
  """
  INACTIVE
}

"""The lifecycle of a single price row."""
enum PriceStatus {
  """Prepared, not yet offered. Excluded from resolution."""
  DRAFT

  """Eligible for resolution."""
  ACTIVE

  """Retained for history, excluded from resolution."""
  INACTIVE
}

"""The scope a tax-inclusivity preference is keyed by."""
enum PricePreferenceAttribute {
  """Keyed by an ISO 4217 currency code."""
  CURRENCY

  """Keyed by a region id or region code."""
  REGION

  """Keyed by a channel code."""
  CHANNEL
}

"""Where a resolved amount came from."""
enum PriceSource {
  """A price row belonging to an eligible price list."""
  PRICE_LIST

  """A price row with no price list: the default price of the variant."""
  DEFAULT_PRICE

  """
  No price row was eligible, so the legacy variant retail price was used.
  """
  VARIANT_RETAIL_PRICE
}

"""How a bulk price upsert treats the rows it was not given."""
enum PriceBulkMode {
  """Write exactly the rows supplied and leave every other row alone."""
  UPSERT

  """Also retire the rows of the mentioned pairs that were not supplied."""
  REPLACE
}

"""A page of price lists."""
type PriceListConnection {
  nodes: [PriceList!]!
  edges: [PriceListEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One price list in a page, with the cursor that addresses it."""
type PriceListEdge {
  node: PriceList!
  cursor: String!
}

"""A page of price rows."""
type ProductPriceConnection {
  nodes: [ProductPrice!]!
  edges: [ProductPriceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One price row in a page, with the cursor that addresses it."""
type ProductPriceEdge {
  node: ProductPrice!
  cursor: String!
}

"""A page of tax-inclusivity preferences."""
type PricePreferenceConnection {
  nodes: [PricePreference!]!
  edges: [PricePreferenceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One preference in a page, with the cursor that addresses it."""
type PricePreferenceEdge {
  node: PricePreference!
  cursor: String!
}

"""A page of exchange rates."""
type ExchangeRateConnection {
  nodes: [ExchangeRate!]!
  edges: [ExchangeRateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One exchange rate in a page, with the cursor that addresses it."""
type ExchangeRateEdge {
  node: ExchangeRate!
  cursor: String!
}

"""What deleting a price list did."""
type DeletePriceListPayload {
  id: ID!
  deleted: Boolean!

  """
  True when the row was removed outright, false when it was soft-deleted.
  """
  hard: Boolean!
}

"""What deleting a price row did."""
type DeleteProductPricePayload {
  id: ID!
  deleted: Boolean!

  """
  True when the row was removed outright, false when it was soft-deleted.
  """
  hard: Boolean!
}

"""What deleting an exchange rate did."""
type DeleteExchangeRatePayload {
  id: ID!
  deleted: Boolean!

  """
  True when the row was removed outright, false when it was soft-deleted.
  """
  hard: Boolean!
}

"""What deleting a preference did."""
type DeletePricePreferencePayload {
  id: ID!
  deleted: Boolean!

  """
  True when the row was removed outright, false when it was soft-deleted.
  """
  hard: Boolean!
}

"""What a bulk price upsert did, row by row."""
type BulkUpsertProductPricesPayload {
  succeeded: [ProductPrice!]!
  failed: [BulkPriceFailure!]!
  succeededCount: Int!
  failedCount: Int!
}

"""One row of a bulk price batch that was refused."""
type BulkPriceFailure {
  """The row's position in the batch, from zero."""
  index: Int!
  variantId: ID

  """Why the row was refused."""
  message: String!
}

"""How to narrow a list of price lists."""
input PriceListFilter {
  ids: [ID!]
  code: String
  name: String
  type: PriceListType
  status: PriceListStatus
  currency: String
  channelId: ID
  customerGroupId: ID
  regionId: ID
  isTaxInclusive: Boolean
}

"""How to order a list of price lists."""
input PriceListSort {
  field: PriceListSortField!
  direction: SortDirection
}

"""The sortable fields of a price list."""
enum PriceListSortField {
  NAME
  CODE
  PRIORITY
  STATUS
  STARTS_AT
  ENDS_AT
  CREATED_AT
  UPDATED_AT
}

"""How to narrow a list of price rows."""
input ProductPriceFilter {
  ids: [ID!]
  variantId: ID
  variantIds: [ID!]
  priceListId: ID
  currency: String
  status: PriceStatus
  computeMode: PriceComputeMode

  """Only the rows that derive from this list."""
  basePriceListId: ID
}

"""How to order a list of price rows."""
input ProductPriceSort {
  field: ProductPriceSortField!
  direction: SortDirection
}

"""The sortable fields of a price row."""
enum ProductPriceSortField {
  AMOUNT
  MIN_QUANTITY
  STATUS
  CREATED_AT
  UPDATED_AT
}

"""How to narrow a list of tax-inclusivity preferences."""
input PricePreferenceFilter {
  ids: [ID!]
  attribute: PricePreferenceAttribute
  value: String
}

"""How to order a list of tax-inclusivity preferences."""
input PricePreferenceSort {
  field: PricePreferenceSortField!
  direction: SortDirection
}

"""The sortable fields of a tax-inclusivity preference."""
enum PricePreferenceSortField {
  ATTRIBUTE
  VALUE
  CREATED_AT
  UPDATED_AT
}

"""How to narrow a list of exchange rates."""
input ExchangeRateFilter {
  ids: [ID!]
  fromCurrency: String
  toCurrency: String
  isManual: Boolean
}

"""How to order a list of exchange rates."""
input ExchangeRateSort {
  field: ExchangeRateSortField!
  direction: SortDirection
}

"""The sortable fields of an exchange rate."""
enum ExchangeRateSortField {
  FROM_CURRENCY
  TO_CURRENCY
  VALID_FROM
  RATE
  CREATED_AT
}

"""The fields a price list is created with."""
input CreatePriceListInput {
  organizationId: ID
  name: String!
  code: String!
  description: String
  type: PriceListType
  status: PriceListStatus
  priority: Int
  currency: String
  channelId: ID
  customerGroupId: ID
  regionId: ID
  startsAt: DateTime
  endsAt: DateTime
  isTaxInclusive: Boolean
  metadata: JSON
}

"""The fields a price list is updated with. Only what is sent changes."""
input UpdatePriceListInput {
  id: ID!
  name: String
  code: String
  description: String
  type: PriceListType
  status: PriceListStatus
  priority: Int
  currency: String
  channelId: ID
  customerGroupId: ID
  regionId: ID
  startsAt: DateTime
  endsAt: DateTime
  isTaxInclusive: Boolean
  metadata: JSON
}

"""
The fields a price row is created with.

A row either states its amount or says how to derive one, and either names the variant it prices or
is scoped by its own rules. Both pairs are deliberate: the first is what makes a price book one row
instead of a matrix, and the second is what makes one row able to price a category.
"""
input CreateProductPriceInput {
  organizationId: ID
  variantId: ID
  priceListId: ID
  currency: String!
  amount: Decimal
  computeMode: PriceComputeMode

  """Signed fraction of the base: 0.25 is 25 % off, -0.15 is a 15 % markup."""
  percent: Decimal
  baseSource: PriceBaseSource
  basePriceListId: ID
  roundTo: Decimal
  unitId: ID
  compareAtAmount: Decimal
  costAmount: Decimal
  minQuantity: Decimal
  maxQuantity: Decimal
  taxInclusive: Boolean
  minMarginPercent: Decimal
  maxDiscountPercent: Decimal
  status: PriceStatus
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""
The fields a price row is updated with.

`variantId` is deliberately absent: a price's variant is part of its identity, and re-pointing a
tier at another variant would leave that variant's other bands knowing nothing about it. Delete
and re-create instead.
"""
input UpdateProductPriceInput {
  id: ID!
  priceListId: ID
  currency: String
  amount: Decimal
  computeMode: PriceComputeMode
  percent: Decimal
  baseSource: PriceBaseSource
  basePriceListId: ID
  roundTo: Decimal
  unitId: ID
  compareAtAmount: Decimal
  costAmount: Decimal
  minQuantity: Decimal
  maxQuantity: Decimal
  taxInclusive: Boolean
  minMarginPercent: Decimal
  maxDiscountPercent: Decimal
  status: PriceStatus
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""One row of a bulk price batch."""
input ProductPriceBulkItemInput {
  id: ID
  variantId: ID
  priceListId: ID
  currency: String!
  amount: Decimal
  computeMode: PriceComputeMode
  percent: Decimal
  baseSource: PriceBaseSource
  basePriceListId: ID
  roundTo: Decimal
  unitId: ID
  minQuantity: Decimal
  maxQuantity: Decimal
  status: PriceStatus
}

"""A price matrix to write."""
input BulkUpsertProductPricesInput {
  organizationId: ID
  items: [ProductPriceBulkItemInput!]!
  mode: PriceBulkMode

  """
  When set, the whole batch is validated before the first row is written, so a refused row
  writes nothing at all.
  """
  atomic: Boolean
}

"""The answer a scope gives about tax-inclusive presentation."""
input UpdatePricePreferenceInput {
  id: ID!
  isTaxInclusive: Boolean!
}

"""
The scope a preference is created for, and the answer it gives.

The scope is the row's identity rather than a field it can be repointed at: the table carries one
live row per `(organizationId, attribute, value)`, which is why the members here are exactly the
ones the create route accepts and why the update input beside this one can change only the answer.
"""
input CreatePricePreferenceInput {
  """
  Which kind of scope the preference answers for: a currency, a region or a channel.
  """
  attribute: PricePreferenceAttribute!

  """An ISO currency code, a region id or code, or a channel code."""
  value: String!
  isTaxInclusive: Boolean
}

"""The fields an exchange rate is created with."""
input CreateExchangeRateInput {
  organizationId: ID
  fromCurrency: String!
  toCurrency: String!
  rate: Decimal!
  providerKey: String
  validFrom: DateTime!
  validUntil: DateTime
  isManual: Boolean
}

"""
The fields an exchange rate is updated with.

The pair and the instant the rate became valid are the row's business key and are therefore
absent: correcting a rate entered wrongly for a moment is a delete and a re-create, which is also
what keeps the "greatest `validFrom` wins" lookup unambiguous.
"""
input UpdateExchangeRateInput {
  id: ID!
  rate: Decimal
  providerKey: String
  validUntil: DateTime
  isManual: Boolean
}

"""The context a price is resolved against."""
input ResolvePriceInput {
  variantIds: [ID!]!
  currency: String!

  """
  Units being priced. Quantity bands are selected by it; defaults to one.
  """
  quantity: Decimal
  channelId: ID
  regionId: ID
  customerId: ID
  customerGroupIds: [ID!]

  """The instant the price must be valid at; defaults to now."""
  date: DateTime
}

"""
The taxable class of the things an organization sells and of the parties it sells to.
"""
type TaxCategory {
  id: ID!
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  name: String!
  code: String!
  description: String

  """
  Whether this is the category an organization applies to anything that names none.
  """
  isDefault: Boolean!
  metadata: JSON

  """The rates of the category, in the order the resolver reads them."""
  rates: [TaxRate!]
}

"""One rate of one category, scoped geographically and in time."""
type TaxRate {
  id: ID!
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  taxCategoryId: ID!
  taxCategory: TaxCategory
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String

  """
  The rate as a fraction, serialised at six decimal places. Never a Float.
  """
  rate: Decimal!
  name: String!
  code: String
  isCompound: Boolean!
  isInclusive: Boolean
  isDefault: Boolean!
  priority: Int!

  """The arithmetic of the rate, which its parts may override."""
  amountType: TaxAmountType!

  """The side of a document the rate applies to."""
  direction: TaxDirection!

  """
  The ordered parts the rate is made of; one implied part when it declares none.
  """
  parts: [TaxRatePart!]
  providerKey: String
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""
One part of a rate: a base, a signed share of the rate and an optional posting code.
"""
type TaxRatePart {
  id: ID!
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  taxRateId: ID!

  """
  The order the part is applied in; a compound rate's base is the rounded amounts of the parts before it.
  """
  sequence: Int!

  """Whether the part declares a base or produces an amount."""
  partType: TaxPartType!

  """
  Signed share of the rate's computed amount this part carries, at six decimal places.
  """
  factorPercent: Decimal!

  """Share of the owner's net-after-discount this part is computed on."""
  baseFactor: Decimal!

  """How the part arrives at its amount."""
  amountType: TaxAmountType!

  """
  The amount contributed per unit of the owner's quantity, for a fixed part.
  """
  fixedAmount: Decimal

  """Currency of the fixed amount."""
  fixedCurrency: String

  """
  The code the receiving accounting system posts this part under. Not an account.
  """
  postingKey: String

  """Printed name of the part; falls back to the rate's name."""
  label: String
  metadata: JSON
}

"""The named set of rates a party or a destination switches to."""
type TaxRegime {
  id: ID!
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime
  isActive: Boolean
  isArchived: Boolean
  name: String!
  code: String!

  """Tie-break among equally specific regimes; the higher priority wins."""
  priority: Int!
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String

  """
  Whether the regime applies only when the party states a usable registration number.
  """
  requiresPartyTaxRegistration: Boolean!
  startsAt: DateTime
  endsAt: DateTime
  description: String
  metadata: JSON

  """The rates the regime selects."""
  rates: [TaxRegimeRate!]
}

"""
The membership of one rate in one regime. Its presence is what makes a rate regime-specific.
"""
type TaxRegimeRate {
  id: ID!
  organizationId: ID
  createdAt: DateTime
  updatedAt: DateTime
  taxRegimeId: ID!
  taxRateId: ID!
}

"""One part of a resolved rate, as it will be applied to a document."""
type ResolvedTaxPart {
  """
  The part row this came from; null for the implied part of a rate that declares none.
  """
  taxRatePartId: ID
  sequence: Int!
  partType: TaxPartType!
  factorPercent: Decimal!
  baseFactor: Decimal!
  amountType: TaxAmountType!
  fixedAmount: Decimal
  fixedCurrency: String
  postingKey: String
  label: String
}

"""The regime a document is taxed under."""
type ResolvedTaxRegime {
  taxRegimeId: ID!
  code: String
  name: String
  priority: Int!

  """
  How the regime was chosen: the party's assignment, or the most specific matching level.
  """
  matchLevel: TaxRegimeMatchLevel!
}

"""One rate of a resolved chain, as it will be applied to a document."""
type ResolvedTaxRate {
  """
  The rate row this came from; null only for the legacy per-variant fallback.
  """
  taxRateId: ID
  taxCategoryId: ID!
  code: String
  name: String!
  rate: Decimal!
  isCompound: Boolean!
  isInclusive: Boolean!
  priority: Int!
  amountType: TaxAmountType!
  direction: TaxDirection!

  """The ordered parts the rate is applied through."""
  parts: [ResolvedTaxPart!]!
  providerKey: String

  """The level of the ladder the rate won at."""
  matchLevel: TaxRateMatchLevel!

  """
  True for the rate the ladder stopped at; the compound rates of the chain follow it.
  """
  isWinner: Boolean!
}

"""One rate's contribution to one line, in the shape of a tax-line row."""
type TaxLineDraft {
  """
  The rate row that applied; null when the amount came from the legacy per-variant fallback.
  """
  taxRateId: ID

  """
  The part of the rate that produced this row; null for a rate that declares none.
  """
  taxRatePartId: ID

  """
  The regime the document was taxed under; evidence rather than a foreign key.
  """
  taxRegimeId: ID

  """Snapshot of the part's posting code; null when the part declares none."""
  postingKey: String

  """Jurisdiction or rate code."""
  code: String

  """
  The rate's name at calculation time, or the part's label when it has one.
  """
  name: String!

  """The rate as a fraction."""
  rate: Decimal!

  """Whether the amount compounds on the earlier amounts of the same line."""
  isCompound: Boolean!

  """Whether the amount is already inside the price."""
  isInclusive: Boolean!

  """The amount the rate was applied to — the part's own base."""
  baseAmount: Decimal!

  """The resulting tax amount, rounded once at the currency's scale."""
  amount: Decimal!

  """
  The owner's quantity at computation time, which a fixed part is applied per unit of.
  """
  quantity: Decimal

  """Currency of the amounts."""
  currency: String!

  """External engine that produced the amount, when one did."""
  providerKey: String

  """Jurisdiction name, engine request id, exemption reason."""
  metadata: JSON
}

"""The tax of one line."""
type TaxCalculationLine {
  """
  The caller's identifier for the line, echoed back so a result can be matched to its line.
  """
  referenceId: ID

  """The category the line was taxed in."""
  taxCategoryId: ID!

  """
  The regime the line was taxed under, when a regime was selected for the document.
  """
  taxRegimeId: ID

  """Currency of the amounts."""
  currency: String!

  """The line's amount without tax."""
  netAmount: Decimal!

  """The line's tax."""
  taxAmount: Decimal!

  """
  What the customer pays for the line: the net plus the tax, or the gross that was given.
  """
  grossAmount: Decimal!

  """One draft per rate that applied, in the order the rates were applied."""
  taxLines: [TaxLineDraft!]!
}

"""The tax of a set of lines."""
type TaxCalculation {
  """Currency of the amounts."""
  currency: String!

  """Exact sum of the lines' amounts without tax."""
  netTotal: Decimal!

  """Exact sum of the lines' tax."""
  taxTotal: Decimal!

  """Exact sum of what the customer pays."""
  grossTotal: Decimal!

  """One entry per line, in the order the lines were given."""
  lines: [TaxCalculationLine!]!
}

"""What a part of a rate does with its share of the rate."""
enum TaxPartType {
  BASE
  TAX
}

"""How a rate or a part of one arrives at its amount."""
enum TaxAmountType {
  PERCENT
  FIXED
}

"""The side of a document a rate applies to."""
enum TaxDirection {
  SALE
  PURCHASE
  BOTH
}

"""How a regime was chosen for a document."""
enum TaxRegimeMatchLevel {
  PARTY_OVERRIDE
  COUNTRY_PROVINCE_POSTAL
  COUNTRY_PROVINCE
  COUNTRY
  REGION
}

"""How specific the zone of a resolved rate is."""
enum TaxRateMatchLevel {
  COUNTRY_PROVINCE_POSTAL
  COUNTRY_PROVINCE
  COUNTRY
  REGION
  DEFAULT
}

enum TaxCategorySortField {
  NAME
  CODE
  IS_DEFAULT
  CREATED_AT
  UPDATED_AT
}

input TaxCategorySort {
  field: TaxCategorySortField!
  direction: SortDirection!
}

enum TaxRateSortField {
  PRIORITY
  RATE
  NAME
  CODE
  COUNTRY_CODE
  DIRECTION
  STARTS_AT
  CREATED_AT
  UPDATED_AT
}

input TaxRateSort {
  field: TaxRateSortField!
  direction: SortDirection!
}

enum TaxRegimeSortField {
  PRIORITY
  NAME
  CODE
  STARTS_AT
  CREATED_AT
  UPDATED_AT
}

input TaxRegimeSort {
  field: TaxRegimeSortField!
  direction: SortDirection!
}

input TaxRegimeFilter {
  ids: [ID!]
  code: String
  name: String
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String
  requiresPartyTaxRegistration: Boolean
  isActive: Boolean

  """Only the regimes whose validity window contains this moment."""
  liveAt: DateTime
}

input TaxCategoryFilter {
  ids: [ID!]
  code: String
  name: String
  isDefault: Boolean
  isActive: Boolean

  """Matched against the name and the code."""
  search: String
}

input TaxRateFilter {
  ids: [ID!]
  taxCategoryId: ID
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String
  code: String
  name: String
  providerKey: String
  isCompound: Boolean
  isInclusive: Boolean
  isDefault: Boolean
  isActive: Boolean
  amountType: TaxAmountType
  direction: TaxDirection

  """Only the rates whose validity window contains this moment."""
  liveAt: DateTime
}

type TaxCategoryEdge {
  node: TaxCategory!
  cursor: String!
}

type TaxCategoryConnection {
  nodes: [TaxCategory!]!
  edges: [TaxCategoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type TaxRateEdge {
  node: TaxRate!
  cursor: String!
}

type TaxRateConnection {
  nodes: [TaxRate!]!
  edges: [TaxRateEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type TaxRegimeEdge {
  node: TaxRegime!
  cursor: String!
}

type TaxRegimeConnection {
  nodes: [TaxRegime!]!
  edges: [TaxRegimeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

input CreateTaxCategoryInput {
  name: String!
  code: String!
  description: String
  isDefault: Boolean
  organizationId: ID
  metadata: JSON
}

input UpdateTaxCategoryInput {
  id: ID!
  name: String
  code: String
  description: String
  isDefault: Boolean
  metadata: JSON
}

input CreateTaxRateInput {
  taxCategoryId: ID!
  name: String!

  """The rate as a fraction — 0.2 is twenty percent."""
  rate: Decimal!
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String
  code: String
  isCompound: Boolean
  isInclusive: Boolean
  isDefault: Boolean
  priority: Int

  """The arithmetic of the rate; a percentage when it is omitted."""
  amountType: TaxAmountType

  """The side of a document the rate applies to; a sale when it is omitted."""
  direction: TaxDirection
  providerKey: String
  startsAt: DateTime
  endsAt: DateTime
  organizationId: ID
  metadata: JSON
}

input UpdateTaxRateInput {
  id: ID!
  taxCategoryId: ID
  name: String
  rate: Decimal
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String
  code: String
  isCompound: Boolean
  isInclusive: Boolean
  isDefault: Boolean
  priority: Int
  amountType: TaxAmountType
  direction: TaxDirection
  providerKey: String
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

input ResolveTaxRateInput {
  taxCategoryId: ID

  """
  The regime assigned to the party; the destination is matched when it is omitted.
  """
  taxRegimeId: ID

  """
  Whether the party states a usable registration number, which a regime may require.
  """
  partyTaxRegistrationPresent: Boolean

  """The side of the document being taxed; a sale when it is omitted."""
  documentDirection: TaxDirection
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCode: String
  regionTaxInclusive: Boolean

  """
  The moment the rates' validity windows are evaluated at; the current time when omitted.
  """
  at: DateTime
}

"""One line to compute tax for."""
input TaxCalculationLineInput {
  """
  The caller's identifier for the line, echoed back so a result can be matched to its line.
  """
  referenceId: ID

  """
  The category to tax the line in; the organization default applies when omitted.
  """
  taxCategoryId: ID!

  """Net or gross amount of the line, as an exact decimal string."""
  amount: Decimal!

  """The quantity a fixed part is applied per unit of; one when omitted."""
  quantity: Decimal
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCode: String
}

"""
The amounts to compute tax for, and the destination they are taxed at.

Every member beside `lines` and `currency` is a default for the lines that state none, which is
why each one is nullable: a caller that computed its own context states it once here rather than
repeating it on every line.
"""
input TaxCalculationInput {
  """Currency the amounts are expressed in."""
  currency: String!

  """The lines to compute."""
  lines: [TaxCalculationLineInput!]!

  """
  The regime manually assigned to the party; the destination is matched when it is omitted.
  """
  taxRegimeId: ID

  """
  Whether the party carries a usable tax registration number, as a regime may require.
  """
  partyTaxRegistrationPresent: Boolean

  """The direction of the document being taxed; a sale when it is omitted."""
  documentDirection: TaxDirection
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCode: String

  """Whether prices at this destination already include tax."""
  regionTaxInclusive: Boolean

  """
  Tax a destination no rate matches at zero instead of refusing the calculation.
  """
  allowUntaxedCatalog: Boolean

  """
  The moment the rates' validity windows are evaluated at; the current time when omitted.
  """
  at: DateTime
}

"""
One part of a rate, as a caller supplies it when a breakdown is written.
"""
input TaxRatePartInput {
  sequence: Int
  partType: TaxPartType

  """Signed share of the rate's computed amount this part carries."""
  factorPercent: Decimal!

  """Share of the owner's net-after-discount this part is computed on."""
  baseFactor: Decimal
  amountType: TaxAmountType
  fixedAmount: Decimal
  fixedCurrency: String
  postingKey: String
  label: String
  metadata: JSON
}

input SetTaxRatePartsInput {
  id: ID!

  """
  The complete ordered list; an empty list returns the rate to its one implied part.
  """
  parts: [TaxRatePartInput!]!
}

input CreateTaxRegimeInput {
  name: String!
  code: String!
  priority: Int
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String
  requiresPartyTaxRegistration: Boolean
  startsAt: DateTime
  endsAt: DateTime
  description: String
  organizationId: ID
  metadata: JSON
}

input UpdateTaxRegimeInput {
  id: ID!
  name: String
  code: String
  priority: Int
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCodePattern: String
  requiresPartyTaxRegistration: Boolean
  startsAt: DateTime
  endsAt: DateTime
  description: String
  metadata: JSON
}

input SetTaxRegimeRatesInput {
  id: ID!

  """The complete set of rates the regime selects; an empty set is refused."""
  taxRateIds: [ID!]!
}

input ResolveTaxRegimeInput {
  """
  The regime assigned to the party; the destination is matched when it is omitted.
  """
  taxRegimeId: ID
  partyTaxRegistrationPresent: Boolean
  regionId: ID
  countryCode: String
  provinceCode: String
  postalCode: String

  """
  The moment the regimes' validity windows are evaluated at; the current time when omitted.
  """
  at: DateTime
}

type StockMovement {
  id: ID!
  warehouseId: ID!
  variantId: ID!
  binId: ID
  type: String!
  quantity: Float!
  quantityBefore: Float!
  quantityAfter: Float!
  reservedBefore: Float!
  reservedAfter: Float!
  referenceType: String!
  referenceId: ID!
  reason: String
  note: String
  occurredAt: DateTime!
}

type StockReservation {
  id: ID!
  variantId: ID!
  warehouseId: ID!
  quantity: Float!
  status: String!
  referenceType: String!
  referenceId: ID!
  lineId: ID
  expiresAt: DateTime
  releasedAt: DateTime
  consumedAt: DateTime
  isBackorder: Boolean!
  expectedAt: DateTime
}

type StockTransferLine {
  id: ID!
  transferId: ID!
  variantId: ID!
  requestedQuantity: Float!
  shippedQuantity: Float!
  receivedQuantity: Float!
  damagedQuantity: Float!

  """Cost carried across the transfer for valuation, as an exact decimal."""
  unitCost: Decimal
  note: String
}

type StockTransfer {
  id: ID!
  number: String!
  fromWarehouseId: ID!
  toWarehouseId: ID!
  status: String!
  shippedAt: DateTime
  receivedAt: DateTime
  note: String
  version: Int!
  lines: [StockTransferLine!]
}

type StockAlert {
  id: ID!
  variantId: ID!
  warehouseId: ID
  threshold: Float!
  notifyEmails: [String!]
  notifyRoles: [String!]
  isActive: Boolean!
  lastTriggeredAt: DateTime
  cooldownMinutes: Int!
}

type StockAdjustment {
  id: ID!
  number: String!
  warehouseId: ID!
  variantId: ID!
  type: String!
  quantity: Float!
  reasonCode: String
  reason: String
  note: String
  status: String!
  appliedAt: DateTime
  appliedByUserId: ID
  movementId: ID
}

type StockCountLine {
  id: ID!
  stockCountId: ID!
  variantId: ID!
  binId: ID
  binPathSnapshot: String
  expectedQuantity: Float!
  countedQuantity: Float
  recountedQuantity: Float
  variance: Float
  status: String!
  countedAt: DateTime
  movementId: ID
  note: String
}

type StockCount {
  id: ID!
  number: String!
  warehouseId: ID!
  status: String!
  mode: String!
  blindCount: Boolean!
  freezeMovements: Boolean!
  countedLineCount: Int!
  varianceUnits: Float!

  """
  Variance of the session valued at the recorded unit cost, as an exact decimal.
  """
  varianceValue: Decimal!
  startedAt: DateTime
  closedAt: DateTime
  note: String
  lines: [StockCountLine!]
}

type ChannelWarehouse {
  id: ID!
  channelId: ID!
  warehouseId: ID!
  isDefault: Boolean!
  priority: Int!
}

"""Availability of one variant at one location. Derived, never stored."""
type StockLevel {
  id: ID!
  warehouseId: ID!
  variantId: ID!

  """
  The counter a conditional write is stated against. It travels with the availability because a
  client can only condition a write on a version it has read.
  """
  version: Int!
  quantity: Float!
  reservedQuantity: Float!
  safetyStock: Float!
  availableQuantity: Float!
  incomingQuantity: Float!
  isUnlimited: Boolean!
  allowBackorder: Boolean!
  backorderLimit: Float
}

"""
One level a reconciliation put back in agreement with its movement ledger.
"""
type StockLevelCorrection {
  levelId: ID!
  warehouseId: ID!
  variantId: ID!
  quantityBefore: Float!
  ledgerQuantity: Float!
  quantityAfter: Float!
}

"""What one reconciliation run scanned, and what it corrected."""
type StockLevelReconciliation {
  scanned: Int!
  corrected: Int!
  corrections: [StockLevelCorrection!]!
}

"""A page of stock movements."""
type StockMovementConnection {
  nodes: [StockMovement!]!
  edges: [StockMovementEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock movement in a page, with the cursor that addresses it."""
type StockMovementEdge {
  node: StockMovement!
  cursor: String!
}

"""A page of stock reservations."""
type StockReservationConnection {
  nodes: [StockReservation!]!
  edges: [StockReservationEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock reservation in a page, with the cursor that addresses it."""
type StockReservationEdge {
  node: StockReservation!
  cursor: String!
}

"""A page of stock transfers."""
type StockTransferConnection {
  nodes: [StockTransfer!]!
  edges: [StockTransferEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock transfer in a page, with the cursor that addresses it."""
type StockTransferEdge {
  node: StockTransfer!
  cursor: String!
}

"""A page of stock transfer lines."""
type StockTransferLineConnection {
  nodes: [StockTransferLine!]!
  edges: [StockTransferLineEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock transfer line in a page, with the cursor that addresses it."""
type StockTransferLineEdge {
  node: StockTransferLine!
  cursor: String!
}

"""A page of stock alerts."""
type StockAlertConnection {
  nodes: [StockAlert!]!
  edges: [StockAlertEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock alert in a page, with the cursor that addresses it."""
type StockAlertEdge {
  node: StockAlert!
  cursor: String!
}

"""A page of stock adjustments."""
type StockAdjustmentConnection {
  nodes: [StockAdjustment!]!
  edges: [StockAdjustmentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock adjustment in a page, with the cursor that addresses it."""
type StockAdjustmentEdge {
  node: StockAdjustment!
  cursor: String!
}

"""A page of stock count sessions."""
type StockCountConnection {
  nodes: [StockCount!]!
  edges: [StockCountEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock count session in a page, with the cursor that addresses it."""
type StockCountEdge {
  node: StockCount!
  cursor: String!
}

"""A page of stock count lines."""
type StockCountLineConnection {
  nodes: [StockCountLine!]!
  edges: [StockCountLineEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One stock count line in a page, with the cursor that addresses it."""
type StockCountLineEdge {
  node: StockCountLine!
  cursor: String!
}

"""A page of channel assignments."""
type ChannelWarehouseConnection {
  nodes: [ChannelWarehouse!]!
  edges: [ChannelWarehouseEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One channel assignment in a page, with the cursor that addresses it."""
type ChannelWarehouseEdge {
  node: ChannelWarehouse!
  cursor: String!
}

"""One level in a page, with the cursor that addresses it."""
type StockLevelEdge {
  node: StockLevel!
  cursor: String!
}

"""A page of stock levels, with their derived availability."""
type StockLevelConnection {
  nodes: [StockLevel!]!
  edges: [StockLevelEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""
What the caller wants a reconciliation run to walk. Everything may be left out.

`version` is the level counter the run is conditioned on and `idempotencyKey` the key a retry
presents. Both are nullable, and both answer exactly as the REST route answers: a version that has
moved is refused with the platform's conflict code, and a key already used for this request is
replayed instead of running the run a second time.
"""
input StockLevelReconciliationInput {
  warehouseId: ID
  variantId: ID
  take: Int
  version: Int
  idempotencyKey: String
}

input StockAdjustmentInput {
  warehouseId: ID!
  variantId: ID!
  type: String!
  quantity: Float!
  reasonCode: String
  reason: String
  note: String
}

"""
The hold a caller wants placed. `version` is the level counter the availability decision was
made from and `idempotencyKey` the key a retry presents; both mirror the REST route, and both are
nullable so a caller that states neither is answered rather than refused.
"""
input StockReservationInput {
  variantId: ID!
  productId: ID!
  warehouseId: ID!
  quantity: Float!
  referenceType: String!
  referenceId: ID!
  lineId: ID
  expiresAt: DateTime
  allowBackorder: Boolean
  version: Int
  idempotencyKey: String
}

"""
The transfer a caller wants drafted. `idempotencyKey` makes a retry of the draft safe.
"""
input StockTransferInput {
  fromWarehouseId: ID!
  toWarehouseId: ID!
  note: String
  lines: [StockTransferLineInput!]
  idempotencyKey: String
}

input StockTransferLineInput {
  transferId: ID
  variantId: ID!
  requestedQuantity: Float!

  """Cost carried across the transfer for valuation, as an exact decimal."""
  unitCost: Decimal
  note: String
}

"""
The variance of one count session, in units and valued at the recorded unit cost.
"""
type StockCountVariance {
  units: Float!

  """Variance valued at the recorded unit cost, as an exact decimal."""
  value: Decimal!
  unpricedLines: Int!
}

input StockTransferShipLineInput {
  lineId: ID!
  shippedQuantity: Float!
}

input StockTransferReceiveLineInput {
  lineId: ID!
  receivedQuantity: Float!
  damagedQuantity: Float
}

"""
The alert rule a caller wants created. `idempotencyKey` makes a retry of the rule safe.
"""
input StockAlertInput {
  variantId: ID!
  warehouseId: ID
  threshold: Float!
  notifyEmails: [String!]
  notifyRoles: [String!]
  cooldownMinutes: Int
  isActive: Boolean
  idempotencyKey: String
}

input StockCountInput {
  warehouseId: ID!
  mode: String
  blindCount: Boolean
  freezeMovements: Boolean
  scope: JSON
  note: String
}

input StockCountLineInput {
  lineId: ID!
  countedQuantity: Float!
  note: String
}

input ChannelWarehouseInput {
  channelId: ID!
  warehouseId: ID!
  isDefault: Boolean
  priority: Int
}

"""Where an area of a stock location sits in the work."""
enum WarehouseZoneType {
  RECEIVING
  STORAGE
  PICKING
  PACKING
  STAGING
  SHIPPING
  RETURNS
  QUARANTINE
  DAMAGE
}

"""What kind of addressable position a bin is."""
enum WarehouseBinType {
  SHELF
  RACK
  PALLET
  BIN
  FLOOR
  DOCK
  STAGING
}

"""How a wave grouped the work it was generated from."""
enum PickWaveStrategy {
  SINGLE_ORDER
  BATCH
  CLUSTER
  ZONE
  WAVE
}

"""Where a wave is in its lifecycle."""
enum PickWaveStatus {
  DRAFT
  RELEASED
  IN_PROGRESS
  PICKED
  PARTIALLY_PICKED
  CLOSED
  CANCELED
}

"""Where a pick list is in its lifecycle."""
enum PickListStatus {
  PENDING
  ASSIGNED
  IN_PROGRESS
  PICKED
  CANCELED
}

"""What happened to one line on the floor."""
enum PickListLineStatus {
  PENDING
  PICKED
  SHORT
  SKIPPED
  CANCELED
}

"""Where a pack slip is in its lifecycle."""
enum PackSlipStatus {
  OPEN
  PACKED
  CANCELED
}

"""Where a carrier manifest is in its lifecycle."""
enum CarrierManifestStatus {
  DRAFT
  CLOSED
  HANDED_OVER
  CANCELED
}

"""A named area of one stock location."""
type WarehouseZone {
  id: ID!

  """The stock location the area belongs to."""
  warehouseId: ID!
  name: String!

  """Unique inside the location."""
  code: String!
  type: WarehouseZoneType!

  """Visiting order in the pick path; lower is visited first."""
  priority: Int!
  isPickable: Boolean!
  isReceivable: Boolean!
  isShippable: Boolean!

  """Out of service; the stock inside it does not move."""
  isBlocked: Boolean!
  minTemperature: Decimal
  maxTemperature: Decimal
  version: Int!
  metadata: JSON

  """The positions inside the area."""
  bins: [WarehouseBin!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One addressable storage position inside a zone."""
type WarehouseBin {
  id: ID!
  warehouseId: ID!
  zoneId: ID

  """The parent position; null for a root."""
  parentId: ID
  code: String!
  barcode: String
  type: WarehouseBinType!
  isPickable: Boolean!
  isBlocked: Boolean!
  capacityUnits: Decimal

  """
  The unit `capacityUnits` is counted in; null is an uninterpreted count.
  """
  capacityUnitId: ID
  maxWeight: Decimal

  """
  The mass unit `maxWeight` is expressed in; null means the organization's default.
  """
  maxWeightUnitId: ID
  maxVolume: Decimal

  """
  The volume unit `maxVolume` is expressed in; null means the organization's default.
  """
  maxVolumeUnitId: ID
  aisle: String
  rack: String
  level: String
  position: String
  sortOrder: Int!
  version: Int!
  metadata: JSON
  zone: WarehouseZone
  parent: WarehouseBin
  children: [WarehouseBin!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One variant's derived balance in one bin."""
type WarehouseBinBalance {
  binId: ID
  variantId: ID!
  quantity: Decimal!
}

"""A batch of picking work released to the floor together."""
type PickWave {
  id: ID!
  warehouseId: ID!
  channelId: ID
  number: String!
  strategy: PickWaveStrategy!
  status: PickWaveStatus!
  priority: Int!
  pickerUserId: ID
  plannedAt: DateTime
  releasedAt: DateTime
  startedAt: DateTime
  completedAt: DateTime

  """Cache of the distinct orders the wave covers."""
  orderCount: Int!

  """Cache of the lines the wave covers."""
  lineCount: Int!
  version: Int!
  metadata: JSON
  pickLists: [PickList!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One list of work for one picker."""
type PickList {
  id: ID!
  waveId: ID
  warehouseId: ID!
  zoneId: ID

  """The shipment this list serves, when it serves one."""
  fulfillmentId: ID
  orderId: ID
  number: String!
  status: PickListStatus!
  assignedToUserId: ID
  priority: Int!
  lineCount: Int!
  pickedCount: Int!
  shortCount: Int!
  startedAt: DateTime
  completedAt: DateTime
  note: String
  version: Int!
  metadata: JSON
  wave: PickWave
  lines: [PickListLine!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One line to pick: what, how much, from which bin, and what happened."""
type PickListLine {
  id: ID!
  pickListId: ID!
  orderLineId: ID
  fulfillmentLineId: ID
  variantId: ID!
  binId: ID
  zoneId: ID
  quantityRequested: Decimal!
  quantityPicked: Decimal!
  quantityShort: Decimal!
  status: PickListLineStatus!
  substituteVariantId: ID
  substituteQuantity: Decimal
  substitutionReason: String
  packSlipId: ID

  """Position in the sorted pick path."""
  position: Int!
  pickedAt: DateTime
  pickedByUserId: ID
  lotNumber: String
  expiryDate: DateTime
  serialNumbers: [String!]
  note: String
  metadata: JSON
  bin: WarehouseBin
  createdAt: DateTime
  updatedAt: DateTime
}

"""The packing record: which picked lines went into which package."""
type PackSlip {
  id: ID!
  warehouseId: ID!
  pickListId: ID
  orderId: ID
  fulfillmentId: ID
  number: String!
  status: PackSlipStatus!
  carrierKey: String
  packageCount: Int!

  """The weight of record; never recomputed from the catalogue."""
  totalWeight: Decimal
  totalVolume: Decimal
  trackingNumber: String
  labelUrl: String
  packedAt: DateTime
  packedByUserId: ID
  note: String
  version: Int!
  metadata: JSON
  lines: [PickListLine!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""The document a carrier accepts: the parcels handed over at one dock."""
type CarrierManifest {
  id: ID!
  warehouseId: ID!
  carrier: String!
  service: String
  number: String!
  status: CarrierManifestStatus!
  manifestDate: DateTime!
  windowFrom: DateTime
  windowTo: DateTime
  shipmentCount: Int!
  packageCount: Int!
  totalWeight: Decimal!
  closedAt: DateTime
  handedOverAt: DateTime
  canceledAt: DateTime
  documentUrl: String
  documentData: JSON
  note: String
  version: Int!
  metadata: JSON

  """
  The shipments the manifest currently covers, read from the fulfilment capability.
  """
  members: [ManifestMember!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One shipment on a manifest."""
type ManifestMember {
  fulfillmentId: ID!
  warehouseId: ID
  orderId: ID
  carrier: String
  service: String
  shippedAt: DateTime
  trackingNumber: String
  packageCount: Int
  packedWeight: Decimal
}

"""One page of zones."""
type WarehouseZoneConnection {
  edges: [WarehouseZoneEdge!]!
  nodes: [WarehouseZone!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One zone inside a page."""
type WarehouseZoneEdge {
  cursor: String!
  node: WarehouseZone!
}

"""One page of bins."""
type WarehouseBinConnection {
  edges: [WarehouseBinEdge!]!
  nodes: [WarehouseBin!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One bin inside a page."""
type WarehouseBinEdge {
  cursor: String!
  node: WarehouseBin!
}

"""One page of the bins under a bin."""
type WarehouseBinSubtreeConnection {
  edges: [WarehouseBinSubtreeEdge!]!
  nodes: [WarehouseBin!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One bin inside a page of a subtree."""
type WarehouseBinSubtreeEdge {
  cursor: String!
  node: WarehouseBin!
}

"""One page of a bin's derived contents."""
type WarehouseBinContentsConnection {
  edges: [WarehouseBinContentsEdge!]!
  nodes: [WarehouseBinBalance!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One variant's balance inside a page of a bin's contents."""
type WarehouseBinContentsEdge {
  cursor: String!
  node: WarehouseBinBalance!
}

"""One page of waves."""
type PickWaveConnection {
  edges: [PickWaveEdge!]!
  nodes: [PickWave!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One wave inside a page."""
type PickWaveEdge {
  cursor: String!
  node: PickWave!
}

"""One page of pick lists."""
type PickListConnection {
  edges: [PickListEdge!]!
  nodes: [PickList!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One pick list inside a page."""
type PickListEdge {
  cursor: String!
  node: PickList!
}

"""One page of a pick list's lines."""
type PickListLinesConnection {
  edges: [PickListLinesEdge!]!
  nodes: [PickListLine!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One line inside a page of a pick list's lines."""
type PickListLinesEdge {
  cursor: String!
  node: PickListLine!
}

"""One page of pack slips."""
type PackSlipConnection {
  edges: [PackSlipEdge!]!
  nodes: [PackSlip!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One pack slip inside a page."""
type PackSlipEdge {
  cursor: String!
  node: PackSlip!
}

"""One page of manifests."""
type CarrierManifestConnection {
  edges: [CarrierManifestEdge!]!
  nodes: [CarrierManifest!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One manifest inside a page."""
type CarrierManifestEdge {
  cursor: String!
  node: CarrierManifest!
}

"""Filters a page of zones."""
input WarehouseZoneFilter {
  warehouseId: ID
  type: WarehouseZoneType
  code: String
  isPickable: Boolean
  isBlocked: Boolean
}

"""Filters a page of bins."""
input WarehouseBinFilter {
  warehouseId: ID
  zoneId: ID
  parentId: ID
  code: String
  type: WarehouseBinType
  isPickable: Boolean
  isBlocked: Boolean
}

"""Filters a page of waves."""
input PickWaveFilter {
  warehouseId: ID
  status: PickWaveStatus
  strategy: PickWaveStrategy
  pickerUserId: ID
  number: String
}

"""Filters a page of pick lists."""
input PickListFilter {
  warehouseId: ID
  waveId: ID
  zoneId: ID
  fulfillmentId: ID
  status: PickListStatus
  assignedToUserId: ID
  number: String
}

"""Filters a page of pack slips."""
input PackSlipFilter {
  warehouseId: ID
  pickListId: ID
  fulfillmentId: ID
  status: PackSlipStatus
  number: String
  trackingNumber: String
}

"""Filters a page of manifests."""
input CarrierManifestFilter {
  warehouseId: ID
  carrier: String
  status: CarrierManifestStatus
  number: String
  manifestDate: DateTime
}

"""One zone's place in the walking order."""
input WarehouseZonePriorityInput {
  id: ID!
  priority: Int!
}

"""The definition of a zone."""
input WarehouseZoneInput {
  warehouseId: ID
  name: String
  code: String
  type: WarehouseZoneType
  priority: Int
  isPickable: Boolean
  isReceivable: Boolean
  isShippable: Boolean
  isBlocked: Boolean
  minTemperature: Decimal
  maxTemperature: Decimal
  metadata: JSON
}

"""The definition of a bin."""
input WarehouseBinInput {
  warehouseId: ID
  zoneId: ID
  parentId: ID
  code: String
  barcode: String
  type: WarehouseBinType
  isPickable: Boolean
  isBlocked: Boolean
  capacityUnits: Decimal

  """
  The unit `capacityUnits` is counted in. A capacity without one cannot be compared with a request.
  """
  capacityUnitId: ID
  maxWeight: Decimal

  """The mass unit `maxWeight` is expressed in."""
  maxWeightUnitId: ID
  maxVolume: Decimal

  """The volume unit `maxVolume` is expressed in."""
  maxVolumeUnitId: ID
  aisle: String
  rack: String
  level: String
  position: String
  sortOrder: Int
  metadata: JSON
}

"""What one capacity measurement found."""
type WarehouseBinCapacityCheck {
  binId: ID!

  """The unit the capacity is declared in; null when the bin declares none."""
  capacityUnitId: ID

  """The declared capacity, in `capacityUnitId`."""
  capacityUnits: Decimal

  """The quantity asked for, as it was supplied."""
  requestedQuantity: Decimal!

  """The unit the request was supplied in, when the caller named one."""
  requestedUnitId: ID

  """
  The request converted into the capacity's unit; null when there is nothing to convert into.
  """
  requestedInCapacityUnit: Decimal

  """The exact difference `capacityUnits - requestedInCapacityUnit`."""
  remainingQuantity: Decimal

  """
  Whether the request is larger than the declared capacity. A warning, never a refusal.
  """
  exceeded: Boolean!

  """The codes a caller acts on."""
  notices: [String!]!
}

"""One bin whose capacity is declared without the unit it is counted in."""
type WarehouseBinCapacityWarning {
  binId: ID!
  code: String!
  warehouseId: ID!
  type: WarehouseBinType!
  capacityUnits: Decimal!

  """The warning code: WAREHOUSE_BIN_CAPACITY_UNIT_UNDECLARED."""
  notice: String!
}

"""The request one capacity measurement is made of."""
input WarehouseBinCapacityInput {
  binId: ID!
  quantity: Decimal!
  unitId: ID

  """
  How many of the capacity's units one of the request's units is; one when omitted.
  """
  conversionFactor: Decimal
}

"""A consecutive range of bins."""
input WarehouseBinRangeInput {
  warehouseId: ID!
  zoneId: ID
  parentId: ID
  from: String!
  count: Int!
  type: WarehouseBinType
  isPickable: Boolean
  sortOrder: Int
}

"""
The scope of a bin reconciliation.

`version` is the level counter the run is conditioned on and `idempotencyKey` the key a retry
presents; both are nullable and both answer exactly as the REST route answers, because a run walks
a batch of levels and a caller cannot name one version for all of them.
"""
input ReconcileBinsInput {
  warehouseId: ID!
  zoneId: ID
  binIds: [ID!]
  repair: Boolean
  version: Int
  idempotencyKey: String
}

"""What one reconciliation found."""
type BinReconciliationLine {
  binId: ID!
  variantId: ID!
  expectedQuantity: Decimal!
  countedQuantity: Decimal!
  difference: Decimal!
  repaired: Boolean!

  """Where the level row says the variant is kept, when it says."""
  homeBinId: ID

  """
  The units the ledger holds at the location with no bin, which the step reports separately.
  """
  unplacedQuantity: Decimal

  """The sum of binQuantity over the bins of the run."""
  placedQuantity: Decimal

  """What the bin this line is about holds, as the ledger derives it."""
  declaredQuantity: Decimal

  """The quantity the run relocated for this line, when it wrote a pair."""
  relocatedQuantity: Decimal
}

"""What one reconciliation run did."""
type BinReconciliationReport {
  warehouseId: ID!
  binIds: [ID!]!
  lines: [BinReconciliationLine!]!
  driftCount: Int!
  movementIds: [ID!]!

  """The sum of placedQuantity over the run."""
  placedQuantity: Decimal

  """The sum of unplacedQuantity over the run."""
  unplacedQuantity: Decimal
}

"""The definition of a wave."""
input PickWaveInput {
  warehouseId: ID!
  channelId: ID
  priority: Int
  plannedAt: DateTime
  strategy: PickWaveStrategy
  pickerUserId: ID

  """The shipments the wave is planned from."""
  fulfillmentIds: [ID!]
}

"""
An update to a wave that has not been released.

Every member is optional because the route's body is: `PUT /pick-waves/:id` is typed
`UpdatePickWaveDTO & PickWaveDTO`, so the patch a REST caller may state is the wave's own columns.
The counters are re-derived by the service rather than supplied, and the route declares no version
precondition — so `version` here is the column the body names and not a compare-and-set. Both are
stated because the route states them: a GraphQL caller that could set fewer members than a REST
caller is the difference this wave exists to remove.
"""
input UpdatePickWaveInput {
  warehouseId: ID
  channelId: ID
  number: String
  strategy: PickWaveStrategy
  status: PickWaveStatus
  priority: Int
  pickerUserId: ID
  plannedAt: DateTime
  releasedAt: DateTime
  startedAt: DateTime
  completedAt: DateTime
  orderCount: Int
  lineCount: Int
  version: Int
  metadata: JSON
}

"""The definition of a pick list."""
input PickListInput {
  warehouseId: ID!
  waveId: ID
  zoneId: ID
  priority: Int

  """The shipments the lines are derived from."""
  fulfillmentIds: [ID!]
}

"""
An update to a pick list.

Every member is optional because the route's body is: `PUT /pick-lists/:id` is typed
`UpdatePickListDTO & PickListDTO`, so the patch a REST caller may state is the list's own columns.
The dispatcher's own edits — the assignee, the priority and the picker-facing note — are its
reason for existing, and the wider column set is mirrored rather than narrowed because the route
accepts it: a member a REST caller can write and a GraphQL caller cannot is a capability
asymmetry, not a tidy schema.
"""
input UpdatePickListInput {
  waveId: ID
  warehouseId: ID
  zoneId: ID
  fulfillmentId: ID
  orderId: ID
  number: String
  status: PickListStatus
  assignedToUserId: ID
  priority: Int
  lineCount: Int
  pickedCount: Int
  shortCount: Int
  startedAt: DateTime
  completedAt: DateTime
  note: String
  version: Int
  metadata: JSON
}

"""
What sealing a slip recorded. `idempotencyKey` is the key a retry of the seal presents.
"""
input PackSlipContentInput {
  packageCount: Int!
  totalWeight: Decimal
  totalVolume: Decimal
  carrierKey: String
  trackingNumber: String
  labelUrl: String
  note: String
  idempotencyKey: String
}

"""
An update to an open pack slip.

Every member is optional because the route's body is: `PUT /pack-slips/:id` is typed
`UpdatePackSlipDTO & PackSlipDTO`, so the patch a REST caller may state is the slip's own columns.
The members are the ones an open slip still owns — the parcel count, the weight and volume of
record, the carrier and the label — and a `PACKED` slip is immutable, which is the service's
refusal rather than this document's: the same refusal answers the route.
"""
input UpdatePackSlipInput {
  warehouseId: ID
  pickListId: ID
  orderId: ID
  fulfillmentId: ID
  number: String
  status: PackSlipStatus
  carrierKey: String
  packageCount: Int
  totalWeight: Decimal
  totalVolume: Decimal
  trackingNumber: String
  labelUrl: String
  packedAt: DateTime
  packedByUserId: ID
  note: String
  version: Int
  metadata: JSON
}

"""The definition of a manifest."""
input CarrierManifestInput {
  warehouseId: ID!
  carrier: String!
  service: String
  manifestDate: DateTime
  windowFrom: DateTime
  windowTo: DateTime
  note: String
}

"""
An update to a draft manifest.

Every member is optional because the route's body is: `PUT /carrier-manifests/:id` is typed
`UpdateCarrierManifestDTO & CarrierManifestDTO`, so the patch a REST caller may state is the
manifest's own columns. Membership is not among them at any status — a draft resolves it from the
shipments and a close freezes it — which is why the correction the route exists for is the day,
the window and the note, and why the counters here are the columns the body names rather than a
way to state membership.
"""
input UpdateCarrierManifestInput {
  warehouseId: ID
  carrier: String
  service: String
  number: String
  status: CarrierManifestStatus
  manifestDate: DateTime
  windowFrom: DateTime
  windowTo: DateTime
  shipmentCount: Int
  packageCount: Int
  totalWeight: Decimal
  closedAt: DateTime
  handedOverAt: DateTime
  canceledAt: DateTime
  documentUrl: String
  documentData: JSON
  note: String
  version: Int
  metadata: JSON
}

"""
What the dock recorded at hand-over. `idempotencyKey` is the key a retry of the hand-over presents.
"""
input HandOverManifestInput {
  scanCount: Int

  """Tracking numbers the carrier scanned that the manifest does not carry."""
  scannedTrackingNumbers: [String!]
  note: String
  idempotencyKey: String
}

"""The outcome of a mutation on a zone."""
type WarehouseZonePayload {
  warehouseZone: WarehouseZone
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a bin."""
type WarehouseBinPayload {
  warehouseBin: WarehouseBin
  userErrors: [UserError!]!
}

"""The outcome of creating a range of bins."""
type WarehouseBinRangePayload {
  warehouseBins: [WarehouseBin!]
  userErrors: [UserError!]!
}

"""The outcome of a bin reconciliation."""
type BinReconciliationPayload {
  report: BinReconciliationReport
  userErrors: [UserError!]!
}

"""
One home-bin declaration: the variant and the location it is kept at.

`version` is the level counter the declaration is conditioned on — the address is part of the
state a caller reads, so a declaration overwriting a concurrent one is the same lost update as a
quantity — and `idempotencyKey` is the key a retry presents.
"""
input AssignWarehouseBinInput {
  variantId: ID!
  warehouseId: ID!

  """The level row, when the caller has it."""
  levelId: ID
  reason: String
  version: Int
  idempotencyKey: String
}

"""
The outcome of declaring a home bin. Nothing moved, so there is no movement to report.
"""
type AssignWarehouseBinPayload {
  """Whether a level row was found and named."""
  assigned: Boolean!
  userErrors: [UserError!]!
}

"""
One put-away: the units being placed, and where they walk from.

`version` is the level counter the walk is conditioned on, so units are never walked into an
address whose state the caller has not seen, and `idempotencyKey` is the key a retry presents —
a second walk of the same units is a second arrival.
"""
input PutAwayWarehouseBinInput {
  variantId: ID!
  warehouseId: ID!

  """The positive quantity being placed, as an exact decimal string."""
  quantity: String!

  """The bin the units walk from, when they were recorded in one."""
  fromBinId: ID

  """The movement the units were received by."""
  stockMovementId: ID

  """The row that asked for the walk."""
  referenceId: ID
  reason: String
  version: Int
  idempotencyKey: String
}

"""What a put-away wrote."""
type PutAwayResult {
  """
  The leg out of the receiving area, when the units were recorded in a bin.
  """
  transferOutMovementId: ID

  """The leg into the target bin."""
  transferInMovementId: ID!

  """The bin the level row now names as the variant's home."""
  binId: ID!

  """The level after the walk."""
  quantityAfter: Decimal!
}

"""The outcome of a put-away."""
type PutAwayWarehouseBinPayload {
  putAway: PutAwayResult
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a wave."""
type PickWavePayload {
  pickWave: PickWave
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a pick list."""
type PickListPayload {
  pickList: PickList
  userErrors: [UserError!]!
}

"""The outcome of recording an outcome against a line."""
type PickListLinePayload {
  pickListLine: PickListLine
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a pack slip."""
type PackSlipPayload {
  packSlip: PackSlip
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a carrier manifest."""
type CarrierManifestPayload {
  carrierManifest: CarrierManifest
  userErrors: [UserError!]!
}

"""A cart: what a buyer assembles before it becomes an order."""
type Cart {
  id: ID!
  tenantId: ID
  organizationId: ID
  channelId: ID!
  regionId: ID
  customerId: ID
  userId: ID
  email: String
  currency: String!
  currencyDecimals: Int!
  locale: String
  status: String!
  orderId: ID
  note: String
  isTaxExempt: Boolean!
  version: Int!
  itemSubtotal: Decimal!
  itemDiscountTotal: Decimal!
  itemTaxTotal: Decimal!
  shippingSubtotal: Decimal!
  shippingDiscountTotal: Decimal!
  shippingTaxTotal: Decimal!
  discountTotal: Decimal!
  taxTotal: Decimal!
  grandTotal: Decimal!
  paidTotal: Decimal!
  refundedTotal: Decimal!
  expiresAt: DateTime
  lastActivityAt: DateTime
  completedAt: DateTime
  abandonedAt: DateTime
  externalId: String
  metadata: JSON
  lines: [CartLine!]
  shippingMethods: [CartShippingMethod!]
  promotions: [CartPromotion!]
  createdAt: DateTime
  updatedAt: DateTime
}

"""One line of a cart, with its price snapshot."""
type CartLine {
  id: ID!
  cartId: ID!
  productId: ID
  variantId: ID
  sellerId: ID
  title: String!
  sku: String
  thumbnail: String
  quantity: Decimal!
  unitPrice: Decimal!
  originalUnitPrice: Decimal!
  isTaxInclusive: Boolean!
  taxCategoryId: ID
  isDiscountable: Boolean!
  requiresShipping: Boolean!
  weight: Decimal
  position: Int!
  note: String
  warehouseId: ID
  subscriptionPlanId: ID
  metadata: JSON
}

"""A delivery choice held against a cart."""
type CartShippingMethod {
  id: ID!
  cartId: ID!
  shippingOptionId: ID
  name: String!
  amount: Decimal!
  isTaxInclusive: Boolean!
  isManual: Boolean!
  taxCategoryId: ID
  position: Int!
  data: JSON
  metadata: JSON
}

"""A promotion as it was applied to a cart."""
type CartPromotion {
  id: ID!
  cartId: ID!
  promotionId: ID
  couponId: ID
  code: String
  amount: Decimal!
  isAutomatic: Boolean!
  appliedAt: DateTime
}

"""The state of an in-progress checkout."""
type CheckoutSession {
  id: ID!
  cartId: ID!
  status: String!
  step: String
  completedSteps: [String!]
  data: JSON
  expiresAt: DateTime
  operationId: ID
  createdAt: DateTime
  updatedAt: DateTime
}

"""The totals an order placed from a cart was computed with."""
type CartTotals {
  itemSubtotal: Decimal!
  itemDiscountTotal: Decimal!
  itemTaxTotal: Decimal!
  shippingSubtotal: Decimal!
  shippingDiscountTotal: Decimal!
  shippingTaxTotal: Decimal!
  discountTotal: Decimal!
  taxTotal: Decimal!
  grandTotal: Decimal!
  currency: String!
  currencyDecimals: Int!
}

"""A page of carts."""
type CartConnection {
  nodes: [Cart!]!
  edges: [CartEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One cart in a page, with the cursor that addresses it."""
type CartEdge {
  node: Cart!
  cursor: String!
}

"""A page of checkout sessions."""
type CheckoutSessionConnection {
  nodes: [CheckoutSession!]!
  edges: [CheckoutSessionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One checkout session in a page, with the cursor that addresses it."""
type CheckoutSessionEdge {
  node: CheckoutSession!
  cursor: String!
}

"""The outcome of a checkout: the order the cart became."""
type CheckoutResult {
  orderId: ID!
  orderNumber: String!
  cart: Cart!
}

input CreateCartInput {
  channelId: ID!
  regionId: ID
  customerId: ID
  email: String
  currency: String!
  currencyDecimals: Int
  locale: String
  note: String
  externalId: String
  idempotencyKey: String
}

input UpdateCartInput {
  email: String
  regionId: ID
  customerId: ID
  shippingAddressId: ID
  billingAddressId: ID
  note: String
  isTaxExempt: Boolean
  externalId: String
  version: Int
}

input AddCartLineInput {
  cartId: ID!
  productId: ID
  variantId: ID!
  title: String
  sku: String
  thumbnail: String
  quantity: Decimal!
  unitPrice: Decimal!
  originalUnitPrice: Decimal
  isTaxInclusive: Boolean
  taxCategoryId: ID
  isDiscountable: Boolean
  requiresShipping: Boolean
  weight: Decimal
  note: String
  warehouseId: ID
  subscriptionPlanId: ID
  version: Int
  idempotencyKey: String
}

input UpdateCartLineInput {
  cartId: ID!
  lineId: ID!
  quantity: Decimal
  unitPrice: Decimal
  note: String
  warehouseId: ID
  version: Int
}

input SetCartShippingMethodInput {
  cartId: ID!
  shippingOptionId: ID
  name: String!
  amount: Decimal!
  isTaxInclusive: Boolean
  isManual: Boolean
  taxCategoryId: ID
  data: JSON
  version: Int
  idempotencyKey: String
}

input ApplyCartPromotionInput {
  cartId: ID!
  promotionId: ID
  couponId: ID
  code: String
  amount: Decimal!
  isAutomatic: Boolean
  version: Int
  idempotencyKey: String
}

input StartCheckoutInput {
  cartId: ID!
  step: String
  idempotencyKey: String
}

input CompleteCheckoutInput {
  cartId: ID!
  version: Int
  idempotencyKey: String
  paymentSessionId: ID
}

"""
The writable surface of a checkout session, as the session's own update route states it.
"""
input UpdateCheckoutSessionInput {
  cartId: ID
  status: String
  step: String

  """
  The steps already completed, in the order they were completed; `completeStepCommerceCheckoutSession` appends to it.
  """
  completedSteps: [String!]

  """The input accumulated across the steps."""
  data: JSON
}

"""An order: the immutable commercial record."""
type Order {
  id: ID!
  tenantId: ID
  organizationId: ID
  number: String!
  displayId: String
  channelId: ID!
  regionId: ID
  customerId: ID
  userId: ID
  email: String
  phone: String
  currency: String!
  currencyDecimals: Int!
  locale: String
  status: String!
  paymentStatus: String!
  fulfillmentStatus: String!
  isDraft: Boolean!
  isTest: Boolean!
  cartId: ID
  parentOrderId: ID
  invoiceId: ID
  quoteInvoiceId: ID
  source: String
  sellerCount: Int!
  itemSubtotal: Decimal!
  itemDiscountTotal: Decimal!
  itemTaxTotal: Decimal!
  shippingSubtotal: Decimal!
  shippingDiscountTotal: Decimal!
  shippingTaxTotal: Decimal!
  discountTotal: Decimal!
  taxTotal: Decimal!
  grandTotal: Decimal!
  paidTotal: Decimal!
  refundedTotal: Decimal!
  creditTotal: Decimal!
  outstandingTotal: Decimal!
  version: Int!
  placedAt: DateTime
  completedAt: DateTime
  canceledAt: DateTime
  cancelReason: String
  purchaseOrderNumber: String

  """
  The settlement schedule the order was placed against; null when none was agreed.
  """
  paymentTermId: ID

  """
  The date the goods were promised: a cache of the lines, never authored.
  """
  promisedAt: DateTime
  externalId: String
  metadata: JSON
  lines: [OrderLine!]
  addresses: [OrderAddress!]
  shippingMethods: [OrderShippingMethod!]
  creditLines: [OrderCreditLine!]
  changes: [OrderChange!]
  history: [OrderHistory!]
  createdAt: DateTime
  updatedAt: DateTime
}

"""One line of an order, with the price snapshot it was bought at."""
type OrderLine {
  id: ID!
  orderId: ID!
  productId: ID
  variantId: ID
  sellerId: ID
  invoiceItemId: ID
  title: String!
  sku: String
  barcode: String
  thumbnail: String
  quantity: Decimal!
  unitPrice: Decimal!
  originalUnitPrice: Decimal!
  isTaxInclusive: Boolean!
  isDiscountable: Boolean!
  requiresShipping: Boolean!
  taxCategoryId: ID
  weight: Decimal
  position: Int!
  note: String
  warehouseId: ID
  subscriptionId: ID
  fulfilledQuantity: Decimal!
  shippedQuantity: Decimal!
  deliveredQuantity: Decimal!
  returnRequestedQuantity: Decimal!
  returnReceivedQuantity: Decimal!
  returnDismissedQuantity: Decimal!
  writtenOffQuantity: Decimal!

  """
  Quantity delivered beyond what was outstanding, recorded rather than clamped.
  """
  overFulfilledQuantity: Decimal!

  """What kind of line this is: a real line, a heading or a note."""
  kind: OrderLineKind!

  """Cache: the quantity actually billed, over the line's invoice links."""
  invoicedQuantity: Decimal!

  """Cache: the same links with direction CREDIT."""
  creditedQuantity: Decimal!

  """Derived from the billing basis and the two counters; never authored."""
  invoiceStatus: OrderLineInvoiceStatus!

  """
  Cache: the quantity paid back, over the refund lines of succeeded refunds.
  """
  refundedQuantity: Decimal!

  """
  Cache: the money paid back, in the order's currency, as a positive magnitude.
  """
  refundedAmount: Decimal!

  """The date this line was promised to the customer."""
  promisedAt: DateTime

  """The lead time the promise was computed from."""
  leadTimeDays: Int
  metadata: JSON

  """Every invoice item and credit-note item this line was billed through."""
  invoiceLinks: [OrderLineInvoice!]
}

"""
One line-to-invoice link: the pivot that makes a partial invoice expressible.
"""
type OrderLineInvoice {
  id: ID!
  orderLineId: ID!

  """The invoice item, or the credit-note item, this link records."""
  invoiceItemId: ID!

  """Which way this link moves the line's counters."""
  direction: OrderLineInvoiceDirection!

  """The quantity the item actually billed, in the line's unit."""
  quantity: Decimal!

  """The signed amount the item carried; negative for a credit."""
  amount: Decimal!
  currency: String!
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""What one line's invoicing counters say, and what is left to bill."""
type OrderLineInvoicingPosition {
  """The quantity the line is invoiced against."""
  basisQuantity: Decimal!
  invoicedQuantity: Decimal!
  creditedQuantity: Decimal!

  """The basis less what has been invoiced, never negative."""
  toInvoiceQuantity: Decimal!
  invoiceStatus: OrderLineInvoiceStatus!
}

"""What kind of line this is."""
enum OrderLineKind {
  """A real line: priced, fulfilled, invoiced."""
  ITEM

  """A presentation heading, which carries no quantity and no price."""
  SECTION

  """A free-text line between items."""
  NOTE
}

"""How much of a line has been billed."""
enum OrderLineInvoiceStatus {
  NOT_INVOICED
  PARTIALLY_INVOICED
  INVOICED
  OVER_INVOICED
}

"""Which way a line-to-invoice link moves the counters."""
enum OrderLineInvoiceDirection {
  """A positive invoice item that bills part of the line."""
  INVOICE

  """A negative credit-note item that credits part of the line."""
  CREDIT
}

"""A frozen address of an order."""
type OrderAddress {
  id: ID!
  orderId: ID!
  type: String!
  sourceAddressId: ID
  contactName: String
  company: String
  firstName: String
  lastName: String
  phone: String
  email: String
  line1: String!
  line2: String
  city: String!
  province: String
  provinceCode: String
  postalCode: String
  countryCode: String!
  countryId: ID
  latitude: Decimal
  longitude: Decimal
}

"""A delivery choice frozen on an order."""
type OrderShippingMethod {
  id: ID!
  orderId: ID!
  shippingOptionId: ID
  name: String!
  amount: Decimal!
  isTaxInclusive: Boolean!
  taxCategoryId: ID
  position: Int!
  data: JSON
  metadata: JSON
}

"""The totals of one committed order version."""
type OrderSummary {
  id: ID!
  orderId: ID!
  version: Int!
  totals: JSON!
  currency: String!
  reason: String
  createdAt: DateTime
}

"""One movement of money against an order. Append-only."""
type OrderTransaction {
  id: ID!
  orderId: ID!
  amount: Decimal!
  currency: String!
  type: String!
  referenceType: String
  referenceId: ID
  description: String
  occurredAt: DateTime
  metadata: JSON
}

"""A post-placement modification of an order."""
type OrderChange {
  id: ID!
  orderId: ID!

  """
  The order version this change produces when it is applied; not a version of the change itself.
  """
  version: Int!
  changeType: String!
  status: String!
  returnId: ID
  claimId: ID
  exchangeId: ID
  subscriptionId: ID
  requestedAt: DateTime
  confirmedAt: DateTime
  declinedAt: DateTime
  canceledAt: DateTime
  note: String
  priceChange: Decimal
  isSettled: Boolean!
  metadata: JSON
  actions: [OrderChangeAction!]
  createdAt: DateTime
}

"""One action inside a change."""
type OrderChangeAction {
  id: ID!
  changeId: ID!
  action: String!
  details: JSON
  amount: Decimal
  referenceType: String
  referenceId: ID
  ordering: Int!
  applied: Boolean!
  appliedAt: DateTime
}

"""Money owed back to the buyer."""
type OrderCreditLine {
  id: ID!
  orderId: ID!
  version: Int!
  referenceType: String
  referenceId: ID
  amount: Decimal!
  currency: String!
  description: String
}

"""One entry of an order's own timeline. Append-only."""
type OrderHistory {
  id: ID!
  orderId: ID!
  action: String!
  title: String
  description: String
  userId: ID
  metadata: JSON
  createdAt: DateTime
}

"""The computed totals of an order."""
type OrderTotals {
  itemSubtotal: Decimal!
  itemDiscountTotal: Decimal!
  itemTaxTotal: Decimal!
  shippingSubtotal: Decimal!
  shippingDiscountTotal: Decimal!
  shippingTaxTotal: Decimal!
  discountTotal: Decimal!
  taxTotal: Decimal!
  grandTotal: Decimal!
  creditTotal: Decimal!
  paidTotal: Decimal!
  refundedTotal: Decimal!
  outstandingTotal: Decimal!
  currency: String!
  currencyDecimals: Int!
}

"""A page of orders."""
type OrderConnection {
  nodes: [Order!]!
  edges: [OrderEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One order in a page, with the cursor that addresses it."""
type OrderEdge {
  node: Order!
  cursor: String!
}

"""A page of order changes."""
type OrderChangeConnection {
  nodes: [OrderChange!]!
  edges: [OrderChangeEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One change in a page, with the cursor that addresses it."""
type OrderChangeEdge {
  node: OrderChange!
  cursor: String!
}

"""A page of totals summaries."""
type OrderSummaryConnection {
  nodes: [OrderSummary!]!
  edges: [OrderSummaryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One summary in a page, with the cursor that addresses it."""
type OrderSummaryEdge {
  node: OrderSummary!
  cursor: String!
}

"""A page of ledger transactions."""
type OrderTransactionConnection {
  nodes: [OrderTransaction!]!
  edges: [OrderTransactionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One transaction in a page, with the cursor that addresses it."""
type OrderTransactionEdge {
  node: OrderTransaction!
  cursor: String!
}

"""A page of order timeline entries."""
type OrderHistoryConnection {
  nodes: [OrderHistory!]!
  edges: [OrderHistoryEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One history entry in a page, with the cursor that addresses it."""
type OrderHistoryEdge {
  node: OrderHistory!
  cursor: String!
}

"""A page of line-to-invoice links."""
type OrderLineInvoiceConnection {
  nodes: [OrderLineInvoice!]!
  edges: [OrderLineInvoiceEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One line-to-invoice link in a page, with the cursor that addresses it."""
type OrderLineInvoiceEdge {
  node: OrderLineInvoice!
  cursor: String!
}

input CreateOrderInput {
  channelId: ID!
  regionId: ID
  customerId: ID
  email: String
  phone: String
  currency: String!
  currencyDecimals: Int
  locale: String
  isTest: Boolean
  source: String

  """The settlement schedule the order is placed against."""
  paymentTermId: ID
  externalId: String

  """
  Retry key: a repeat of this input under the same key is answered from the first attempt.
  """
  idempotencyKey: String
}

input UpdateOrderInput {
  email: String
  phone: String
  locale: String
  note: String
  externalId: String
  cancelReason: String
  paymentTermId: ID

  """
  The version of the order this change was reasoned about; omitted, the write is refused.
  """
  version: Int
}

"""The request that records one line-to-invoice link."""
input OrderLineInvoiceInput {
  orderLineId: ID!
  invoiceItemId: ID!

  """Which way the link moves the counters; a bill when omitted."""
  direction: OrderLineInvoiceDirection

  """The quantity the item actually billed, in the line's unit."""
  quantity: Decimal!

  """The signed amount the item carried; negative for a credit."""
  amount: Decimal!
  currency: String!

  """
  The quantity the line is invoiced against; the ordered quantity when omitted.
  """
  basisQuantity: Decimal
  metadata: JSON
}

"""The answer to recording a link."""
type OrderLineInvoicePayload {
  link: OrderLineInvoice

  """The line as it stands after the counters moved."""
  line: OrderLine
}

input OrderChangeActionInput {
  action: String!
  details: JSON
  amount: Decimal
  referenceType: String
  referenceId: ID
}

input RequestOrderEditInput {
  orderId: ID!
  changeType: String
  note: String
  actions: [OrderChangeActionInput!]!

  """
  Retry key: a repeat of this input under the same key is answered from the first attempt.
  """
  idempotencyKey: String
}

"""
The writable surface of an order change, as the change's own update route states it.
"""
input UpdateOrderChangeInput {
  orderId: ID
  changeType: String
  note: String
  metadata: JSON
}

"""
The writable surface of one action inside a change, as the action's own update route states it.
"""
input UpdateOrderChangeActionInput {
  """
  The change the action belongs to, when the action is to be re-parented.
  """
  changeId: ID

  """
  What the action does; the set of them is enumerated by `order.change.action`.
  """
  action: String

  """The action's own payload."""
  details: JSON

  """The signed money effect of the action on the order's grand total."""
  amount: Decimal
  referenceType: String
  referenceId: ID

  """Where the action sits in the sequence the change is applied in."""
  ordering: Int

  """Whether the apply step has run for this action."""
  applied: Boolean
}

"""
The writable surface of a credit line, as the credit line's own update route states it.
"""
input UpdateOrderCreditLineInput {
  orderId: ID

  """The order version the credit was applied against."""
  version: Int
  referenceType: String
  referenceId: ID

  """
  The credit, as a positive magnitude: a credit reduces what the customer owes.
  """
  amount: Decimal
  currency: String
  description: String
}

"""The request that records one refund against an order line."""
input OrderLineRefundInput {
  orderLineId: ID!

  """The quantity paid back, as a positive magnitude."""
  quantity: Decimal!

  """The money paid back, in the order currency, as a positive magnitude."""
  amount: Decimal!

  """Currency of the amount; checked against the order the line belongs to."""
  currency: String!
}

"""What removing a link did."""
type DeleteOrderLineInvoicePayload {
  id: ID!
  deleted: Boolean!
}

"""
What a destructive delete of one of this domain's rows did.
One type for the domain rather than one per resource: the answer is the same three facts whatever was
removed — which row the delete named, and whether a row was there to remove. The recoverable
withdrawal beside each of these fields answers that resource's own type instead, because it answers
the row it retired.
"""
type OrderDeleteResult {
  """The identifier the delete named."""
  id: ID!

  """
  Whether a row was there to remove; a delete of one that was not answers false.
  """
  deleted: Boolean!
}

enum PaymentCollectionStatus {
  NOT_PAID
  AWAITING
  AUTHORIZED
  PARTIALLY_AUTHORIZED
  PARTIALLY_CAPTURED
  COMPLETED
  CANCELED
  FAILED
}

enum PaymentSessionStatus {
  PENDING
  PENDING_AUTHORIZATION
  REQUIRES_MORE
  AUTHORIZED
  CAPTURED
  CANCELED
  ERROR
  EXPIRED
}

enum RefundStatus {
  PENDING
  SUCCEEDED
  FAILED
  CANCELED
}

enum PaymentWebhookEventStatus {
  RECEIVED
  PROCESSED
  FAILED
  IGNORED
}

enum PaymentCaptureMode {
  MANUAL
  AUTOMATIC
}

enum PaymentAccountHolderType {
  CUSTOMER
  SELLER
  ORGANIZATION
}

enum PaymentAccountHolderStatus {
  PENDING
  ACTIVE
  RESTRICTED
  REJECTED
  DISABLED
}

enum PaymentAccountVerificationStatus {
  UNVERIFIED
  PENDING
  VERIFIED
  REJECTED
  EXPIRED
}

enum PaymentMethodTokenType {
  CARD
  BANK_ACCOUNT
  WALLET
  DIRECT_DEBIT
}

enum PaymentMethodTokenStatus {
  ACTIVE
  EXPIRED
  REVOKED
  FAILED
}

enum PaymentProviderSortField {
  CODE
  NAME
  SORT_ORDER
  CREATED_AT
  UPDATED_AT
}

enum PaymentCollectionSortField {
  AMOUNT
  STATUS
  CAPTURED_AT
  CREATED_AT
  UPDATED_AT
}

enum PaymentSessionSortField {
  AMOUNT
  STATUS
  EXPIRES_AT
  AUTHORIZED_AT
  CREATED_AT
  UPDATED_AT
}

enum PaymentCaptureSortField {
  AMOUNT
  CAPTURED_AT
  CREATED_AT
  UPDATED_AT
}

enum RefundSortField {
  AMOUNT
  STATUS
  REFUNDED_AT
  CREATED_AT
  UPDATED_AT
}

enum RefundReasonSortField {
  CODE
  LABEL
  CREATED_AT
  UPDATED_AT
}

enum RefundLineSortField {
  ORDER_LINE_ID
  QUANTITY
  AMOUNT
  CREATED_AT
  UPDATED_AT
}

enum PaymentWebhookEventSortField {
  TYPE
  STATUS
  RECEIVED_AT
  PROCESSED_AT
  CREATED_AT
  UPDATED_AT
}

enum PaymentAccountHolderSortField {
  TYPE
  STATUS
  VERIFICATION_STATUS
  CREATED_AT
  UPDATED_AT
}

enum PaymentMethodTokenSortField {
  TYPE
  BRAND
  STATUS
  IS_DEFAULT
  EXPIRY_YEAR
  LAST_USED_AT
  CREATED_AT
  UPDATED_AT
}

type PaymentProvider {
  id: ID!
  code: String!
  name: String!
  isEnabled: Boolean!
  isTestMode: Boolean!
  integrationId: ID
  supportedCurrencies: [String!]
  supportedCountries: [String!]
  supportedPaymentMethods: [String!]
  sortOrder: Int!
  configuration: JSON
  metadata: JSON
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentCollection {
  id: ID!
  orderId: ID
  cartId: ID
  amount: Decimal!
  currency: String!
  status: PaymentCollectionStatus!
  authorizedAmount: Decimal!
  capturedAmount: Decimal!
  refundedAmount: Decimal!
  canceledAmount: Decimal!
  settlementCurrency: String
  settlementAmount: Decimal
  fxRate: Decimal
  fxRateId: ID
  fxCapturedAt: DateTime
  completedAt: DateTime
  metadata: JSON
  sessions: [PaymentSession!]
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentSession {
  id: ID!
  collectionId: ID!
  providerId: ID!
  status: PaymentSessionStatus!
  amount: Decimal!
  currency: String!
  externalId: String
  paymentMethodTokenId: ID
  data: JSON
  idempotencyKey: String
  expiresAt: DateTime
  authorizedAt: DateTime
  metadata: JSON
  collection: PaymentCollection
  provider: PaymentProvider
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentCapture {
  id: ID!
  paymentId: ID!
  amount: Decimal!
  currency: String!
  externalId: String
  capturedAt: DateTime!
  metadata: JSON
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Refund {
  id: ID!
  orderId: ID!
  paymentId: ID
  returnId: ID
  claimId: ID
  amount: Decimal!
  currency: String!
  reasonId: ID
  reason: String
  status: RefundStatus!
  externalId: String
  refundedAt: DateTime
  note: String
  metadata: JSON
  refundReason: RefundReason
  lines: [RefundLine!]
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type RefundLine {
  id: ID!
  refundId: ID!
  orderLineId: ID!
  quantity: Decimal!
  amount: Decimal!
  currency: String!
  metadata: JSON
  legacy: Boolean
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type RefundReason {
  id: ID!
  code: String!
  label: String!
  description: String
  parentId: ID
  parent: RefundReason
  children: [RefundReason!]
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentWebhookEvent {
  id: ID!
  providerId: ID!
  eventId: String!
  type: String!
  payload: JSON!
  signature: String
  receivedAt: DateTime!
  processedAt: DateTime
  status: PaymentWebhookEventStatus!
  lastError: String
  attemptCount: Int!
  provider: PaymentProvider
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentAccountHolder {
  id: ID!
  contactId: ID
  paymentProviderId: ID
  providerKey: String!
  externalAccountId: String
  type: PaymentAccountHolderType!
  status: PaymentAccountHolderStatus!
  verificationStatus: PaymentAccountVerificationStatus!
  country: String
  defaultCurrency: String
  mandateReference: String
  mandateAcceptedAt: DateTime
  metadata: JSON
  methodTokens: [PaymentMethodToken!]
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentMethodToken {
  id: ID!
  accountHolderId: ID!
  paymentProviderId: ID
  providerKey: String!
  token: String
  type: PaymentMethodTokenType!
  brand: String
  last4: String
  expiryMonth: Int
  expiryYear: Int
  holderName: String
  billingAddressId: ID
  isDefault: Boolean!
  status: PaymentMethodTokenStatus!
  lastUsedAt: DateTime
  revokedAt: DateTime
  metadata: JSON
  isActive: Boolean
  isArchived: Boolean
  archivedAt: DateTime
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

type PaymentProviderEdge {
  cursor: String!
  node: PaymentProvider!
}

type PaymentCollectionEdge {
  cursor: String!
  node: PaymentCollection!
}

type PaymentSessionEdge {
  cursor: String!
  node: PaymentSession!
}

type PaymentCaptureEdge {
  cursor: String!
  node: PaymentCapture!
}

type RefundEdge {
  cursor: String!
  node: Refund!
}

type RefundReasonEdge {
  cursor: String!
  node: RefundReason!
}

type RefundLineEdge {
  cursor: String!
  node: RefundLine!
}

type PaymentWebhookEventEdge {
  cursor: String!
  node: PaymentWebhookEvent!
}

type PaymentAccountHolderEdge {
  cursor: String!
  node: PaymentAccountHolder!
}

type PaymentMethodTokenEdge {
  cursor: String!
  node: PaymentMethodToken!
}

type PaymentProviderConnection {
  nodes: [PaymentProvider!]!
  edges: [PaymentProviderEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type PaymentCollectionConnection {
  nodes: [PaymentCollection!]!
  edges: [PaymentCollectionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type PaymentSessionConnection {
  nodes: [PaymentSession!]!
  edges: [PaymentSessionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type PaymentCaptureConnection {
  nodes: [PaymentCapture!]!
  edges: [PaymentCaptureEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type RefundConnection {
  nodes: [Refund!]!
  edges: [RefundEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type RefundReasonConnection {
  nodes: [RefundReason!]!
  edges: [RefundReasonEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type RefundLineConnection {
  nodes: [RefundLine!]!
  edges: [RefundLineEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type PaymentWebhookEventConnection {
  nodes: [PaymentWebhookEvent!]!
  edges: [PaymentWebhookEventEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type PaymentAccountHolderConnection {
  nodes: [PaymentAccountHolder!]!
  edges: [PaymentAccountHolderEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type PaymentMethodTokenConnection {
  nodes: [PaymentMethodToken!]!
  edges: [PaymentMethodTokenEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

input PaymentProviderFilter {
  id: ID
  code: String
  isEnabled: Boolean
  isTestMode: Boolean
  integrationId: ID
}

input PaymentCollectionFilter {
  id: ID
  orderId: ID
  cartId: ID
  status: PaymentCollectionStatus
  currency: String
}

input PaymentSessionFilter {
  id: ID
  collectionId: ID
  providerId: ID
  status: PaymentSessionStatus
  externalId: String
  paymentMethodTokenId: ID
}

input PaymentCaptureFilter {
  id: ID
  paymentId: ID
  externalId: String
  capturedAtFrom: DateTime
  capturedAtTo: DateTime
}

input RefundFilter {
  id: ID
  orderId: ID
  paymentId: ID
  returnId: ID
  claimId: ID
  reasonId: ID
  status: RefundStatus
}

input RefundReasonFilter {
  id: ID
  code: String
  parentId: ID
  isActive: Boolean
}

input RefundLineFilter {
  id: ID
  refundId: ID
  orderLineId: ID
  currency: String
}

input PaymentWebhookEventFilter {
  id: ID
  providerId: ID
  eventId: String
  type: String
  status: PaymentWebhookEventStatus
  receivedAtFrom: DateTime
  receivedAtTo: DateTime
}

input PaymentAccountHolderFilter {
  id: ID
  contactId: ID
  paymentProviderId: ID
  providerKey: String
  type: PaymentAccountHolderType
  status: PaymentAccountHolderStatus
  verificationStatus: PaymentAccountVerificationStatus
  defaultCurrency: String
}

input PaymentMethodTokenFilter {
  id: ID
  contactId: ID
  accountHolderId: ID
  paymentProviderId: ID
  providerKey: String
  type: PaymentMethodTokenType
  status: PaymentMethodTokenStatus
  isDefault: Boolean
}

input PaymentProviderSort {
  field: PaymentProviderSortField!
  direction: SortDirection
}

input PaymentCollectionSort {
  field: PaymentCollectionSortField!
  direction: SortDirection
}

input PaymentSessionSort {
  field: PaymentSessionSortField!
  direction: SortDirection
}

input PaymentCaptureSort {
  field: PaymentCaptureSortField!
  direction: SortDirection
}

input RefundSort {
  field: RefundSortField!
  direction: SortDirection
}

input RefundReasonSort {
  field: RefundReasonSortField!
  direction: SortDirection
}

input RefundLineSort {
  field: RefundLineSortField!
  direction: SortDirection
}

input PaymentWebhookEventSort {
  field: PaymentWebhookEventSortField!
  direction: SortDirection
}

input PaymentAccountHolderSort {
  field: PaymentAccountHolderSortField!
  direction: SortDirection
}

input PaymentMethodTokenSort {
  field: PaymentMethodTokenSortField!
  direction: SortDirection
}

input CreatePaymentProviderInput {
  code: String!
  name: String!
  isEnabled: Boolean
  isTestMode: Boolean
  integrationId: ID
  supportedCurrencies: [String!]
  supportedCountries: [String!]
  supportedPaymentMethods: [String!]
  sortOrder: Int
  configuration: JSON
  metadata: JSON
  idempotencyKey: String
}

input UpdatePaymentProviderInput {
  id: ID!
  name: String
  isEnabled: Boolean
  isTestMode: Boolean
  integrationId: ID
  supportedCurrencies: [String!]
  supportedCountries: [String!]
  supportedPaymentMethods: [String!]
  sortOrder: Int
  configuration: JSON
  metadata: JSON
}

input CreatePaymentCollectionInput {
  orderId: ID
  cartId: ID
  amount: Decimal!
  currency: String!
  settlementCurrency: String
  settlementAmount: Decimal
  fxRate: Decimal
  fxRateId: ID
  fxCapturedAt: DateTime
  metadata: JSON
  idempotencyKey: String
}

input UpdatePaymentCollectionInput {
  id: ID!
  settlementCurrency: String
  settlementAmount: Decimal
  fxRate: Decimal
  fxRateId: ID
  fxCapturedAt: DateTime
  metadata: JSON
}

input OpenPaymentSessionInput {
  collectionId: ID!
  providerId: ID
  providerCode: String
  amount: Decimal!
  currency: String
  paymentMethod: String
  data: JSON
  paymentMethodTokenId: ID
  idempotencyKey: String
  expiresAt: DateTime
}

input AuthorizePaymentSessionInput {
  id: ID!
  amount: Decimal
  data: JSON
  idempotencyKey: String
}

input VoidPaymentSessionInput {
  id: ID!
  reason: String
  idempotencyKey: String
}

input UpdatePaymentSessionInput {
  id: ID!
  externalId: String
  paymentMethodTokenId: ID
  data: JSON
  expiresAt: DateTime
  authorizedAt: DateTime
  metadata: JSON
}

input CapturePaymentInput {
  paymentId: ID!
  amount: Decimal!
  currency: String
  externalId: String
  capturedAt: DateTime
  finalize: Boolean
  metadata: JSON
  idempotencyKey: String
}

input CreateRefundInput {
  orderId: ID!
  paymentId: ID
  returnId: ID
  claimId: ID
  amount: Decimal!
  currency: String!
  reasonId: ID
  reason: String
  note: String
  storeCredit: Boolean
  metadata: JSON
  lines: [RefundLineInput!]
  idempotencyKey: String
}

input RefundLineInput {
  orderLineId: ID!
  quantity: Decimal!
  amount: Decimal!
  currency: String
  metadata: JSON
}

input CreateRefundLineInput {
  refundId: ID!
  orderLineId: ID!
  quantity: Decimal!
  amount: Decimal!
  currency: String
  metadata: JSON
  idempotencyKey: String
}

input UpdateRefundLineInput {
  id: ID!
  quantity: Decimal
  amount: Decimal
  currency: String
  metadata: JSON
}

input UpdateRefundInput {
  id: ID!
  reasonId: ID
  reason: String
  note: String
  metadata: JSON
}

input ApproveRefundInput {
  id: ID!
  note: String
  idempotencyKey: String
}

input CancelRefundInput {
  id: ID!
  reason: String
}

input CreateRefundReasonInput {
  code: String!
  label: String!
  description: String
  parentId: ID
  idempotencyKey: String
}

input UpdateRefundReasonInput {
  id: ID!
  label: String
  description: String
  parentId: ID
  isActive: Boolean
}

input ReprocessPaymentWebhookEventInput {
  id: ID!
  force: Boolean
  idempotencyKey: String
}

input CreatePaymentAccountHolderInput {
  contactId: ID
  paymentProviderId: ID
  providerKey: String!
  type: PaymentAccountHolderType
  country: String
  defaultCurrency: String
  metadata: JSON
  idempotencyKey: String
}

input UpdatePaymentAccountHolderInput {
  id: ID!
  contactId: ID
  paymentProviderId: ID
  providerKey: String
  verificationStatus: PaymentAccountVerificationStatus
  country: String
  defaultCurrency: String
  mandateReference: String
  mandateAcceptedAt: DateTime
  metadata: JSON
}

input VerifyPaymentAccountHolderInput {
  id: ID!
  verificationStatus: PaymentAccountVerificationStatus!
  status: PaymentAccountHolderStatus
  reference: String
  expiresAt: DateTime
  note: String
  idempotencyKey: String
}

input PaymentMethodTokenConfirmationInput {
  token: String!
  confirmedAt: DateTime!
}

input CreatePaymentMethodTokenInput {
  accountHolderId: ID!
  providerKey: String!
  token: String!
  providerConfirmation: PaymentMethodTokenConfirmationInput!
  type: PaymentMethodTokenType
  brand: String
  last4: String
  expiryMonth: Int
  expiryYear: Int
  holderName: String
  billingAddressId: ID
  isDefault: Boolean
  metadata: JSON
  idempotencyKey: String
}

input UpdatePaymentMethodTokenInput {
  id: ID!
  brand: String
  last4: String
  expiryMonth: Int
  expiryYear: Int
  holderName: String
  billingAddressId: ID
  metadata: JSON
}

type CreatePaymentProviderPayload {
  paymentProvider: PaymentProvider
  operation: Operation
  userErrors: [UserError!]!
}

type UpdatePaymentProviderPayload {
  paymentProvider: PaymentProvider
  operation: Operation
  userErrors: [UserError!]!
}

type DeletePaymentProviderPayload {
  paymentProvider: PaymentProvider
  deleted: Boolean!
  operation: Operation
  userErrors: [UserError!]!
}

type CreatePaymentCollectionPayload {
  paymentCollection: PaymentCollection
  operation: Operation
  userErrors: [UserError!]!
}

type UpdatePaymentCollectionPayload {
  paymentCollection: PaymentCollection
  operation: Operation
  userErrors: [UserError!]!
}

type OpenPaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

type AuthorizePaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

type VoidPaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

type UpdatePaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

"""
The outcome of re-reading an attempt, closing one that has outlived its lifetime.
"""
type RefreshPaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

type CapturePaymentPayload {
  paymentCapture: PaymentCapture
  operation: Operation
  userErrors: [UserError!]!
}

type CreateRefundPayload {
  refund: Refund
  operation: Operation
  userErrors: [UserError!]!
}

type UpdateRefundPayload {
  refund: Refund
  operation: Operation
  userErrors: [UserError!]!
}

type ApproveRefundPayload {
  refund: Refund
  operation: Operation
  userErrors: [UserError!]!
}

type CancelRefundPayload {
  refund: Refund
  operation: Operation
  userErrors: [UserError!]!
}

type CreateRefundReasonPayload {
  refundReason: RefundReason
  operation: Operation
  userErrors: [UserError!]!
}

type UpdateRefundReasonPayload {
  refundReason: RefundReason
  operation: Operation
  userErrors: [UserError!]!
}

type DeleteRefundReasonPayload {
  refundReason: RefundReason
  deleted: Boolean!
  operation: Operation
  userErrors: [UserError!]!
}

type CreateRefundLinePayload {
  refundLine: RefundLine
  operation: Operation
  userErrors: [UserError!]!
}

type UpdateRefundLinePayload {
  refundLine: RefundLine
  operation: Operation
  userErrors: [UserError!]!
}

type DeleteRefundLinePayload {
  refundLine: RefundLine
  deleted: Boolean!
  operation: Operation
  userErrors: [UserError!]!
}

type ReprocessPaymentWebhookEventPayload {
  paymentWebhookEvent: PaymentWebhookEvent
  operation: Operation
  userErrors: [UserError!]!
}

type CreatePaymentAccountHolderPayload {
  paymentAccountHolder: PaymentAccountHolder
  operation: Operation
  userErrors: [UserError!]!
}

type UpdatePaymentAccountHolderPayload {
  paymentAccountHolder: PaymentAccountHolder
  operation: Operation
  userErrors: [UserError!]!
}

type VerifyPaymentAccountHolderPayload {
  paymentAccountHolder: PaymentAccountHolder
  operation: Operation
  userErrors: [UserError!]!
}

type DeletePaymentAccountHolderPayload {
  paymentAccountHolder: PaymentAccountHolder
  deleted: Boolean!
  revokedTokenCount: Int!
  operation: Operation
  userErrors: [UserError!]!
}

type CreatePaymentMethodTokenPayload {
  paymentMethodToken: PaymentMethodToken
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of repairing the display facts of a saved instrument."""
type UpdatePaymentMethodTokenPayload {
  paymentMethodToken: PaymentMethodToken
  operation: Operation
  userErrors: [UserError!]!
}

type SetDefaultPaymentMethodTokenPayload {
  paymentMethodToken: PaymentMethodToken
  previousDefaultId: ID
  operation: Operation
  userErrors: [UserError!]!
}

type RevokePaymentMethodTokenPayload {
  paymentMethodToken: PaymentMethodToken
  deleted: Boolean!
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a provider registration recoverably."""
type SoftDeletePaymentProviderPayload {
  paymentProvider: PaymentProvider
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted provider registration."""
type RecoverPaymentProviderPayload {
  paymentProvider: PaymentProvider
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a collection recoverably."""
type SoftDeletePaymentCollectionPayload {
  paymentCollection: PaymentCollection
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted collection."""
type RecoverPaymentCollectionPayload {
  paymentCollection: PaymentCollection
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a session recoverably."""
type SoftDeletePaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted session."""
type RecoverPaymentSessionPayload {
  paymentSession: PaymentSession
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a capture recoverably."""
type SoftDeletePaymentCapturePayload {
  paymentCapture: PaymentCapture
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted capture."""
type RecoverPaymentCapturePayload {
  paymentCapture: PaymentCapture
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a refund recoverably."""
type SoftDeleteRefundPayload {
  refund: Refund
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted refund."""
type RecoverRefundPayload {
  refund: Refund
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a governed refund reason recoverably."""
type SoftDeleteRefundReasonPayload {
  refundReason: RefundReason
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted refund reason."""
type RecoverRefundReasonPayload {
  refundReason: RefundReason
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a line of a refund's breakdown recoverably."""
type SoftDeleteRefundLinePayload {
  refundLine: RefundLine
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted line of a refund's breakdown."""
type RecoverRefundLinePayload {
  refundLine: RefundLine
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring an inbound callback recoverably."""
type SoftDeletePaymentWebhookEventPayload {
  paymentWebhookEvent: PaymentWebhookEvent
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted inbound callback."""
type RecoverPaymentWebhookEventPayload {
  paymentWebhookEvent: PaymentWebhookEvent
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a party's account at a provider recoverably."""
type SoftDeletePaymentAccountHolderPayload {
  paymentAccountHolder: PaymentAccountHolder
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted account at a provider."""
type RecoverPaymentAccountHolderPayload {
  paymentAccountHolder: PaymentAccountHolder
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a saved instrument recoverably."""
type SoftDeletePaymentMethodTokenPayload {
  paymentMethodToken: PaymentMethodToken
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted saved instrument."""
type RecoverPaymentMethodTokenPayload {
  paymentMethodToken: PaymentMethodToken
  operation: Operation
  userErrors: [UserError!]!
}

"""Lifecycle of a campaign."""
enum CampaignStatus {
  DRAFT
  ACTIVE
  INACTIVE
}

"""What a campaign budget counts."""
enum CampaignBudgetType {
  """A money ceiling across the campaign."""
  SPEND

  """A redemption count."""
  USAGE

  """A money ceiling per value of the budget's attribute."""
  SPEND_BY_ATTRIBUTE

  """A redemption count per value of the budget's attribute."""
  USAGE_BY_ATTRIBUTE
}

"""Lifecycle of a promotion."""
enum PromotionStatus {
  DRAFT
  ACTIVE
  INACTIVE

  """Set once the window has closed."""
  EXPIRED
}

"""
The shape of an offer, which decides the action types that are legal for it.
"""
enum PromotionType {
  """A plain discount: fixed, percentage or tiered percentage."""
  STANDARD

  """Buy some, get some: needs a buy scope and a buy quantity."""
  BUY_GET

  """Shipping is discounted to zero."""
  FREE_SHIPPING

  """A set of items is discounted down to a bundle price."""
  BUNDLE

  """Target items are given away."""
  FREE_ITEM
}

"""What a promotion action does."""
enum PromotionActionType {
  """Subtract a fixed amount."""
  FIXED

  """Subtract a fraction of the discountable amount."""
  PERCENTAGE

  """Discount shipping to zero."""
  FREE_SHIPPING

  """Add the target units as free lines."""
  FREE_ITEM

  """Discount a target set down to a bundle price."""
  BUNDLE_PRICE

  """A percentage selected by a threshold."""
  TIERED_PERCENTAGE
}

"""What an action's benefit lands on."""
enum PromotionActionTargetType {
  ORDER
  ITEMS
  SHIPPING
}

"""How an action's benefit is spread over its targets."""
enum PromotionActionAllocation {
  """Spread once over the whole targeted set, by largest remainder."""
  ACROSS

  """Once per discounted unit, so a partial return can reverse whole units."""
  EACH

  """Once, on a single selected unit or on the set as a whole."""
  ONCE
}

"""Lifecycle of one application of a promotion."""
enum PromotionUsageStatus {
  """Held while the basket is being checked out."""
  RESERVED

  """The order was placed; the discount stands."""
  REGISTERED

  """Cancelled, expired or returned under the reversibility policy."""
  REVERTED
}

"""
Why a promotion was excluded, or was applied only in part.

Every exclusion is reported, because a promotion that silently does nothing is the defect an
operator cannot diagnose. The set is closed: a notice the enum does not carry is an answer the
protocol cannot deliver, so a new reason is added here and to the domain enumeration together.
"""
enum PromotionNoticeCode {
  """The promotion is not active."""
  PROMOTION_INACTIVE

  """The promotion window is closed."""
  PROMOTION_EXPIRED

  """The campaign the promotion belongs to has closed its window."""
  CAMPAIGN_WINDOW_CLOSED

  """The promotion's rules did not match the context."""
  RULES_NOT_MATCHED

  """The promotion is scoped to another seller."""
  SELLER_SCOPE_MISMATCH

  """The promotion is priced in another currency."""
  CURRENCY_MISMATCH

  """The promotion's global usage limit is reached."""
  USAGE_LIMIT_EXCEEDED

  """The customer has already used the promotion as often as it allows."""
  PER_CUSTOMER_LIMIT_EXCEEDED

  """No promotion carries the code presented."""
  COUPON_INACTIVE

  """The coupon's own window is closed."""
  COUPON_EXPIRED

  """The coupon's own usage limit is reached."""
  COUPON_LIMIT_EXCEEDED

  """The coupon's own per-customer limit is reached."""
  COUPON_CUSTOMER_LIMIT_EXCEEDED

  """The budget behind the promotion is spent."""
  BUDGET_EXCEEDED

  """The budget admitted only part of the computed discount."""
  PARTIALLY_APPLIED_BUDGET

  """
  Another promotion of the same stacking group is exclusive and already applied.
  """
  STACKING_CONFLICT

  """The action matched nothing to discount."""
  NO_TARGETS

  """Nothing was left to discount by the time this promotion ran."""
  NO_DISCOUNTABLE_AMOUNT

  """The promotion's own discount ceiling capped the benefit."""
  PROMOTION_CAPPED

  """The evaluation stopped after its candidate limit."""
  PROMOTION_CANDIDATE_LIMIT
}

"""Which side of a basket an allocation lands on."""
enum PromotionAllocationOwner {
  """A line of the basket."""
  LINE

  """A shipping method of the basket."""
  SHIPPING
}

"""Lifecycle of a stored-value instrument."""
enum GiftCardStatus {
  ACTIVE

  """The balance reached zero."""
  REDEEMED

  """The expiry instant passed."""
  EXPIRED

  """Withdrawn by an operator; the ledger is kept."""
  CANCELED
}

"""What a gift-card ledger row records."""
enum GiftCardTransactionType {
  """The card was created and credited with its face value."""
  ISSUE

  """The balance was spent; the amount is negative."""
  REDEEM

  """Value was returned to the card."""
  REFUND

  """A manual correction, in either direction."""
  ADJUST

  """A forfeited balance at expiry."""
  EXPIRE
}

"""
A promotion: the offer itself.

Its conditions are not a column — they are `rule` rows the kernel's rule engine owns and this
domain reads — and the money it moves is recorded in the kernel's adjustment ledger. What is
declared here is what the promotion *is*: what it gives (its action set), when it runs, who may
use it, and how much of it may be given away.
"""
type Promotion {
  id: ID!

  """
  Optional code. A promotion with no code and `isAutomatic` applies itself.
  """
  code: String

  """Customer-facing name of the offer."""
  title: String!
  description: String
  type: PromotionType!
  status: PromotionStatus!

  """Applies without a code when its rules match."""
  isAutomatic: Boolean!

  """When false the promotion closes its stacking group."""
  isCombinable: Boolean!

  """Group key for the combination policy."""
  stackingGroup: String

  """Ordering weight; lower runs earlier."""
  priority: Int!

  """The campaign whose window and budget bound this promotion."""
  campaignId: ID

  """
  The sales channel the promotion is restricted to; null means all channels.
  """
  channelId: ID

  """
  The currency the fixed-amount actions are restricted to; null means currency-agnostic.
  """
  currency: String

  """
  The contact group the promotion is restricted to; null means all customers.
  """
  customerGroupId: ID
  startsAt: DateTime

  """End of the window, exclusive."""
  endsAt: DateTime

  """Global redemption cap; null means unlimited."""
  usageLimit: Int

  """Redemptions so far, including reservations."""
  usageCount: Int!

  """Redemption cap per customer; null means unlimited."""
  perCustomerUsageLimit: Int

  """Inline budget for a promotion that has no campaign. Exact decimal."""
  budgetAmount: Decimal

  """Consumption of the inline budget. Exact decimal."""
  budgetSpent: Decimal!

  """Whether the discount is computed on the tax-inclusive amount."""
  isTaxInclusive: Boolean!

  """
  Funding mode, revert-on-return policy and the other open-ended settings.
  """
  metadata: JSON
  campaign: Campaign

  """The effect of the offer, in application order."""
  actions: [PromotionAction!]

  """The codes that grant it."""
  coupons: [Coupon!]

  """Its applications, as reservations and registrations."""
  usages: [PromotionUsage!]
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
One thing a promotion does when it matches.

An action's position is part of its meaning: the actions of one promotion compose in order, and
replacing the set is the only way to remove one without renumbering the rest.
"""
type PromotionAction {
  id: ID!
  promotionId: ID!
  type: PromotionActionType!
  targetType: PromotionActionTargetType!
  allocation: PromotionActionAllocation!

  """The amount, the fraction or the bundle price. Exact decimal."""
  value: Decimal!

  """The currency of a fixed-amount action; null for a proportional one."""
  currency: String

  """Upper bound on the benefit. Exact decimal."""
  maxQuantity: Decimal

  """Units the benefit applies to. Exact decimal."""
  applyToQuantity: Decimal

  """
  Units that must be bought before a buy-get action fires. Exact decimal.
  """
  buyRulesMinQuantity: Decimal
  isTaxInclusive: Boolean!

  """Position in the application order."""
  position: Int!

  """Tier list, bundle size and the other per-action settings."""
  metadata: JSON
  promotion: Promotion
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""One promotion that was applied, with the amount it gave away."""
type PromotionApplication {
  promotionId: ID!

  """The coupon the application came through, when it came through one."""
  couponId: ID

  """The code presented, when the application came from one."""
  code: String

  """
  Whether the promotion applied itself rather than being asked for by a code.
  """
  isAutomatic: Boolean!

  """The discount granted. Exact decimal."""
  amount: Decimal!
  currency: String!
}

"""Why a promotion was excluded, or was applied only in part."""
type PromotionNotice {
  """
  The promotion the notice is about; empty when the notice is about a code that names none.
  """
  promotionId: ID!

  """The code presented, when the notice is about a code."""
  code: String
  notice: PromotionNoticeCode!

  """What a person should read."""
  message: String!

  """The figures that make the notice actionable, when it carries any."""
  details: JSON
}

"""
What one evaluation decided.

It is deterministic: the same context and the same promotion set always produce the same
applications and the same allocations, which is what makes a simulation a promise rather than an
estimate.
"""
type PromotionEvaluationResult {
  """The promotions that applied, in the order they ran."""
  applications: [PromotionApplication!]!

  """Every exclusion and every partial application, with its reason."""
  notices: [PromotionNotice!]!

  """
  The discount the evaluation grants, as a negative decimal. Exact decimal.
  """
  discountTotal: Decimal!
  currency: String!
}

"""
One owner an action decided to take money off.

An allocation is money that was or would be granted to a line or a shipping method, never what an
action computed: a discount the budget truncated is re-allocated, so Σ allocations is the amount
actually given away, to the last minor unit.
"""
type PromotionAllocation {
  ownerType: PromotionAllocationOwner!

  """The line or shipping method the amount lands on."""
  ownerId: ID!
  promotionId: ID!
  actionId: ID!
  code: String

  """The amount taken off this owner, negative. Exact decimal."""
  amount: Decimal!
}

"""
A campaign: a window and a budget, nothing more.

It holds no rules of its own. Every promotion that names it inherits its window — outside which
that promotion is not a candidate at all — and is bounded by its budget.
"""
type Campaign {
  id: ID!

  """
  Stable handle used by imports and by every external caller. Unique per organization.
  """
  identifier: String!
  name: String!
  description: String
  status: CampaignStatus!

  """Start of the window; null means already open."""
  startsAt: DateTime

  """End of the window, exclusive; null means it never closes."""
  endsAt: DateTime

  """Owner, cost centre and free-form notes."""
  metadata: JSON

  """The single ceiling of this campaign."""
  budget: CampaignBudget

  """The promotions it bounds."""
  promotions: [Promotion!]
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
The spend or usage ceiling of one campaign.

`used` is a cache of the usage ledger rather than an authority: the checkout consumes it through a
single conditional statement and the reconciliation job re-derives it, so reading it here and
reading the ledger can disagree only between those two events.
"""
type CampaignBudget {
  id: ID!
  campaignId: ID!
  type: CampaignBudgetType!

  """
  The ceiling: money for the spend types, a count for the usage types. Exact decimal.
  """
  limit: Decimal!

  """Consumption so far, including reservations. Exact decimal."""
  used: Decimal!

  """
  Context attribute path the budget is split by, for the *_BY_ATTRIBUTE types.
  """
  attribute: String

  """Currency of the ceiling, for the spend types."""
  currency: String
  campaign: Campaign

  """The per-value consumption of a split budget."""
  usages: [CampaignBudgetUsage!]
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
What a budget split by attribute has spent for one value of that attribute.

The row is the gate: a consumption against a value is admitted here and the parent budget is
advanced by the same amount in the same transaction, so an exhausted value blocks its own value
and nothing else.
"""
type CampaignBudgetUsage {
  id: ID!
  budgetId: ID!

  """The value of the budget's attribute this row counts."""
  attributeValue: String!

  """Consumption against that value. Exact decimal."""
  used: Decimal!
  budget: CampaignBudget
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
A redeemable code belonging to a promotion.

The code is stored upper-cased, so `save10` and `SAVE10` cannot both exist in one organization,
and its own window and limits narrow the promotion's rather than widening them.
"""
type Coupon {
  id: ID!

  """The code the customer types, stored upper-cased."""
  code: String!

  """
  The promotion a redemption grants; null means the code is not attached yet.
  """
  promotionId: ID

  """Groups the coupons produced by one mailing."""
  batchId: String

  """Per-code cap; null inherits the promotion limit."""
  usageLimit: Int

  """Redemptions so far, including reservations."""
  usageCount: Int!

  """Per-code, per-customer cap."""
  perCustomerLimit: Int
  startsAt: DateTime

  """End of the code window, exclusive."""
  endsAt: DateTime

  """Issued-to, mailing and single-use markers."""
  metadata: JSON
  promotion: Promotion

  """The redemptions this code granted."""
  usages: [PromotionUsage!]
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
One application of a promotion: the row the limits and the budget are checked against.

A row is written by the checkout — reserved while the basket is being paid for, registered when
the order is placed — and moved to `REVERTED` by a cancellation or a return. It is a fact, not a
record an operator maintains.
"""
type PromotionUsage {
  id: ID!
  promotionId: ID!
  couponId: ID
  orderId: ID
  cartId: ID
  customerId: ID

  """The code presented, when the application came from one."""
  code: String

  """The discount granted. Exact decimal."""
  amount: Decimal!
  currency: String!
  usedAt: DateTime!
  status: PromotionUsageStatus!
  promotion: Promotion
  coupon: Coupon
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
A stored-value instrument.

`balance` is a materialised cache of the card's ledger, never the authority; the ledger is what a
redemption is actually checked against.
"""
type GiftCard {
  id: ID!

  """The redeemable string."""
  code: String!

  """Face value at issue. Exact decimal."""
  initialAmount: Decimal!

  """Current balance. Exact decimal."""
  balance: Decimal!

  """The card currency. A card is only redeemable against an order in it."""
  currency: String!
  status: GiftCardStatus!

  """The registered holder."""
  customerId: ID

  """The order that issued the card, on a refund-to-card flow."""
  orderId: ID
  expiresAt: DateTime

  """Reloadable marker, issuer, recipient and message."""
  metadata: JSON

  """Every movement on the card, most recent first."""
  transactions: [GiftCardTransaction!]
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""
One movement on a gift card. Append-only.

Each row records the amount, the balance it left behind and what caused it, which is what makes a
card's history explainable without recomputing it.
"""
type GiftCardTransaction {
  id: ID!
  giftCardId: ID!
  orderId: ID

  """The movement's amount. Exact decimal; negative when value was spent."""
  amount: Decimal!

  """The balance after this movement. Exact decimal."""
  balanceAfter: Decimal!
  type: GiftCardTransactionType!

  """Why a manual correction was made."""
  note: String
  occurredAt: DateTime!
  giftCard: GiftCard
  tenantId: ID
  organizationId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""One promotion inside a page."""
type PromotionEdge {
  node: Promotion!

  """The page's opaque cursor at this row."""
  cursor: String!
}

"""One page of promotions."""
type PromotionConnection {
  nodes: [Promotion!]!
  edges: [PromotionEdge!]!

  """The number of rows the filter selects, not the number in this page."""
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One action inside a page."""
type PromotionActionEdge {
  node: PromotionAction!
  cursor: String!
}

"""One page of promotion actions."""
type PromotionActionConnection {
  nodes: [PromotionAction!]!
  edges: [PromotionActionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One campaign inside a page."""
type CampaignEdge {
  node: Campaign!
  cursor: String!
}

"""One page of campaigns."""
type CampaignConnection {
  nodes: [Campaign!]!
  edges: [CampaignEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One budget inside a page."""
type CampaignBudgetEdge {
  node: CampaignBudget!
  cursor: String!
}

"""One page of campaign budgets."""
type CampaignBudgetConnection {
  nodes: [CampaignBudget!]!
  edges: [CampaignBudgetEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One per-value consumption row inside a page."""
type CampaignBudgetUsageEdge {
  node: CampaignBudgetUsage!
  cursor: String!
}

"""One page of per-value consumption rows."""
type CampaignBudgetUsageConnection {
  nodes: [CampaignBudgetUsage!]!
  edges: [CampaignBudgetUsageEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One coupon inside a page."""
type CouponEdge {
  node: Coupon!
  cursor: String!
}

"""One page of coupons."""
type CouponConnection {
  nodes: [Coupon!]!
  edges: [CouponEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One application inside a page."""
type PromotionUsageEdge {
  node: PromotionUsage!
  cursor: String!
}

"""One page of promotion applications."""
type PromotionUsageConnection {
  nodes: [PromotionUsage!]!
  edges: [PromotionUsageEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One gift card inside a page."""
type GiftCardEdge {
  node: GiftCard!
  cursor: String!
}

"""One page of gift cards."""
type GiftCardConnection {
  nodes: [GiftCard!]!
  edges: [GiftCardEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One movement inside a page."""
type GiftCardTransactionEdge {
  node: GiftCardTransaction!
  cursor: String!
}

"""One page of gift-card movements."""
type GiftCardTransactionConnection {
  nodes: [GiftCardTransaction!]!
  edges: [GiftCardTransactionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""Which promotions to read."""
input PromotionFilter {
  id: ID
  code: String
  title: String
  type: PromotionType
  status: PromotionStatus
  isAutomatic: Boolean
  isCombinable: Boolean
  stackingGroup: String
  campaignId: ID
  channelId: ID
  customerGroupId: ID
  currency: String
  startsAt: DateTime
  endsAt: DateTime
}

"""The fields a promotion page may be ordered by."""
enum PromotionSortField {
  CODE
  TITLE
  TYPE
  STATUS
  PRIORITY
  STARTS_AT
  ENDS_AT
  USAGE_COUNT
  CREATED_AT
  UPDATED_AT
}

"""How to order a promotion page."""
input PromotionSort {
  field: PromotionSortField!
  direction: SortDirection
}

"""Which actions to read."""
input PromotionActionFilter {
  id: ID
  promotionId: ID
  type: PromotionActionType
  targetType: PromotionActionTargetType
  allocation: PromotionActionAllocation
  position: Int
}

"""The fields a promotion-action page may be ordered by."""
enum PromotionActionSortField {
  POSITION
  TYPE
  VALUE
  CREATED_AT
  UPDATED_AT
}

"""How to order a promotion-action page."""
input PromotionActionSort {
  field: PromotionActionSortField!
  direction: SortDirection
}

"""Which campaigns to read."""
input CampaignFilter {
  id: ID
  identifier: String
  name: String
  status: CampaignStatus
  startsAt: DateTime
  endsAt: DateTime
}

"""The fields a campaign page may be ordered by."""
enum CampaignSortField {
  IDENTIFIER
  NAME
  STATUS
  STARTS_AT
  ENDS_AT
  CREATED_AT
  UPDATED_AT
}

"""How to order a campaign page."""
input CampaignSort {
  field: CampaignSortField!
  direction: SortDirection
}

"""Which budgets to read."""
input CampaignBudgetFilter {
  id: ID
  campaignId: ID
  type: CampaignBudgetType
  attribute: String
  currency: String
  limit: Decimal
  used: Decimal
}

"""The fields a budget page may be ordered by."""
enum CampaignBudgetSortField {
  TYPE
  LIMIT
  USED
  CREATED_AT
  UPDATED_AT
}

"""How to order a budget page."""
input CampaignBudgetSort {
  field: CampaignBudgetSortField!
  direction: SortDirection
}

"""Which per-value consumption rows to read."""
input CampaignBudgetUsageFilter {
  id: ID
  budgetId: ID
  attributeValue: String
  used: Decimal
}

"""The fields a per-value consumption page may be ordered by."""
enum CampaignBudgetUsageSortField {
  ATTRIBUTE_VALUE
  USED
  CREATED_AT
  UPDATED_AT
}

"""How to order a per-value consumption page."""
input CampaignBudgetUsageSort {
  field: CampaignBudgetUsageSortField!
  direction: SortDirection
}

"""Which coupons to read."""
input CouponFilter {
  id: ID
  code: String
  promotionId: ID
  batchId: String
  startsAt: DateTime
  endsAt: DateTime
}

"""The fields a coupon page may be ordered by."""
enum CouponSortField {
  CODE
  BATCH_ID
  USAGE_COUNT
  STARTS_AT
  ENDS_AT
  CREATED_AT
  UPDATED_AT
}

"""How to order a coupon page."""
input CouponSort {
  field: CouponSortField!
  direction: SortDirection
}

"""Which applications to read."""
input PromotionUsageFilter {
  id: ID
  promotionId: ID
  couponId: ID
  orderId: ID
  cartId: ID
  customerId: ID
  code: String
  status: PromotionUsageStatus
  currency: String
  usedAt: DateTime
}

"""The fields an application page may be ordered by."""
enum PromotionUsageSortField {
  USED_AT
  AMOUNT
  STATUS
  CREATED_AT
}

"""How to order an application page."""
input PromotionUsageSort {
  field: PromotionUsageSortField!
  direction: SortDirection
}

"""Which gift cards to read."""
input GiftCardFilter {
  id: ID
  code: String
  status: GiftCardStatus
  currency: String
  customerId: ID
  orderId: ID
  balance: Decimal
  expiresAt: DateTime
}

"""The fields a gift-card page may be ordered by."""
enum GiftCardSortField {
  CODE
  BALANCE
  STATUS
  EXPIRES_AT
  CREATED_AT
  UPDATED_AT
}

"""How to order a gift-card page."""
input GiftCardSort {
  field: GiftCardSortField!
  direction: SortDirection
}

"""Which movements to read."""
input GiftCardTransactionFilter {
  id: ID
  giftCardId: ID
  orderId: ID
  type: GiftCardTransactionType
  occurredAt: DateTime
}

"""The fields a movement page may be ordered by."""
enum GiftCardTransactionSortField {
  OCCURRED_AT
  AMOUNT
  TYPE
  CREATED_AT
}

"""How to order a movement page."""
input GiftCardTransactionSort {
  field: GiftCardTransactionSortField!
  direction: SortDirection
}

"""One action of a promotion, as it is written."""
input PromotionActionInput {
  """
  The action this entry names; omit it to add a new one.
  
  The identifier is accepted and **not** honoured as an identity: a replacement rewrites the
  promotion's whole action set — every stored row is deleted and the set this input carries is written
  in its place — so an action named here does not keep its row across the write, and the identifiers
  the mutation answers with are the new ones. It is carried for symmetry with the action resource's own
  identifier-addressed routes, and a caller that needs a particular action to keep its identity has to
  read the stored set back and address the row it finds.
  """
  id: ID
  type: PromotionActionType!
  targetType: PromotionActionTargetType!
  allocation: PromotionActionAllocation
  value: Decimal!
  currency: String
  maxQuantity: Decimal
  applyToQuantity: Decimal
  buyRulesMinQuantity: Decimal
  isTaxInclusive: Boolean

  """
  Where the action sits in the application order; defaults to its position in the list.
  """
  position: Int
  metadata: JSON
}

"""The fields a promotion is created with."""
input CreatePromotionInput {
  code: String
  title: String!
  description: String
  type: PromotionType
  status: PromotionStatus
  isAutomatic: Boolean
  isCombinable: Boolean
  stackingGroup: String
  priority: Int
  campaignId: ID
  channelId: ID
  currency: String
  customerGroupId: ID
  startsAt: DateTime
  endsAt: DateTime
  usageLimit: Int
  perCustomerUsageLimit: Int
  budgetAmount: Decimal
  isTaxInclusive: Boolean
  metadata: JSON

  """
  The effect of the offer, applied as a whole set after the promotion is stored.
  A promotion without one has none, which is a legitimate draft.
  """
  actions: [PromotionActionInput!]
}

"""
The fields of a promotion that may be changed. Every one of them is optional.
"""
input UpdatePromotionInput {
  code: String
  title: String
  description: String
  type: PromotionType
  status: PromotionStatus
  isAutomatic: Boolean
  isCombinable: Boolean
  stackingGroup: String
  priority: Int
  campaignId: ID
  channelId: ID
  currency: String
  customerGroupId: ID
  startsAt: DateTime
  endsAt: DateTime
  usageLimit: Int
  perCustomerUsageLimit: Int
  budgetAmount: Decimal
  isTaxInclusive: Boolean
  metadata: JSON

  """
  When present, replaces the whole action set rather than merging into it.
  """
  actions: [PromotionActionInput!]
}

"""The fields a campaign is created with."""
input CreateCampaignInput {
  identifier: String!
  name: String!
  description: String
  status: CampaignStatus
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""The fields of a campaign that may be changed."""
input UpdateCampaignInput {
  identifier: String
  name: String
  description: String
  status: CampaignStatus
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""
The ceiling to store on a campaign. Omitted fields keep their stored values.

`limit` is what a campaign that has no budget yet cannot do without: setting a ceiling for the
first time stores one, so a request that leaves it out can only succeed against a budget that
already exists.
"""
input UpdateCampaignBudgetInput {
  type: CampaignBudgetType

  """Exact decimal."""
  limit: Decimal
  attribute: String
  currency: String
}

"""
Why a campaign's ceiling is being re-opened.

The reason is optional and is not passed to the service, which keeps none: the figure is reset and
the explanation belongs with the activity log entry that recorded the act, which is where the route
leaves it too.
"""
input ResetCampaignBudgetInput {
  reason: String
}

"""The fields a coupon is created with."""
input CreateCouponInput {
  code: String!
  promotionId: ID
  batchId: String
  usageLimit: Int
  perCustomerLimit: Int
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""The fields of a coupon that may be changed."""
input UpdateCouponInput {
  promotionId: ID
  batchId: String
  usageLimit: Int
  perCustomerLimit: Int
  startsAt: DateTime
  endsAt: DateTime
  metadata: JSON
}

"""The fields a gift card is issued with."""
input IssueGiftCardInput {
  """
  The redeemable string; generated from the platform alphabet when absent.
  """
  code: String

  """Face value at issue. Exact decimal, and positive."""
  initialAmount: Decimal!
  currency: String!
  customerId: ID
  orderId: ID
  expiresAt: DateTime
  metadata: JSON
}

"""
The code format a batch is minted in.

Every member is optional, and the platform's own alphabet, length and grouping stand in for the ones
a caller leaves out — so a batch that states no format is generated the way the platform generates
one, which is what makes the format a narrowing of the default rather than a thing to restate.
"""
input CouponCodeFormatInput {
  """Regular expression every generated code matches."""
  pattern: String

  """Alphabet the significant characters are drawn from."""
  alphabet: String

  """Number of significant characters."""
  significantLength: Int

  """Characters per group."""
  groupSize: Int

  """Separator between groups."""
  separator: String

  """Optional fixed prefix."""
  prefix: String

  """Optional fixed suffix."""
  suffix: String
}

"""
The batch of codes to mint.

`code` is carried because the route's body is the create shape plus the two members a batch adds, and
it names nothing here: every code of a batch is minted from the format, so the member is accepted and
never read, exactly as it is on the route. `count` is the one member a batch cannot do without.

The remaining members are the shared fields of every code in the batch — they are what makes a
mailing one request rather than a thousand, and they are applied to each code as it is written.
"""
input CreateCouponBatchInput {
  """Accepted and never read: a batch mints its own codes."""
  code: String

  """The promotion every code of the batch grants."""
  promotionId: ID

  """Groups the codes of this batch; generated when absent."""
  batchId: String

  """Per-code cap; null inherits the promotion limit."""
  usageLimit: Int

  """Per-code, per-customer cap."""
  perCustomerLimit: Int

  """Start of the window every code shares."""
  startsAt: DateTime

  """End of the window every code shares, exclusive."""
  endsAt: DateTime

  """Issued-to, mailing and single-use markers carried onto every code."""
  metadata: JSON

  """How many codes to mint. Required: a batch of nothing is not a batch."""
  count: Int!

  """The format the codes are minted in; the platform's own when absent."""
  couponCodeFormat: CouponCodeFormatInput
}

"""
The fields of a gift card that may be changed.

The shape is the route's own body minus the three members whose write the card's rules refuse.
`initialAmount` is the base the balance is derived from, so changing it after the card was issued
rewrites the derivation rather than the card; `balance` is the materialised cache of that derivation,
whose correction is `adjustGiftCard`; and `pin` is the second factor, which is stored as a digest and
which no field of this schema reads or writes. Carrying them would hand a GraphQL caller a write over
the ledger's own arithmetic — a write no act of the domain offers on either surface.

Every member is optional, because an update states what changed rather than restating the card.
"""
input UpdateGiftCardInput {
  """The redeemable string."""
  code: String

  """The card currency. A card is only redeemable against an order in it."""
  currency: String

  """Active, redeemed, expired or canceled."""
  status: GiftCardStatus

  """The registered holder."""
  customerId: ID

  """The order that issued the card, on a refund-to-card flow."""
  orderId: ID

  """Expiry instant; null means the card never expires."""
  expiresAt: DateTime

  """Reloadable marker, issuer, recipient and message."""
  metadata: JSON
}

"""How much of a card is returned, and against what."""
input RefundGiftCardInput {
  """The amount to return. Exact decimal."""
  amount: Decimal!

  """The order the value came from."""
  orderId: ID

  """Why the value is being returned; the movement carries it."""
  note: String
}

"""
The correction to make to a card's balance.

The note is required here as it is on the route, because the movement this writes is the only place
the correction is ever explained: an adjustment without a reason is indistinguishable from a defect,
and a caller that has no reason to state has no correction to make.
"""
input AdjustGiftCardInput {
  """
  The signed amount. Exact decimal: negative debits the card, positive credits it.
  """
  amount: Decimal!

  """Why the correction was made."""
  note: String!
}

"""How much of a card is spent, and against what."""
input RedeemGiftCardInput {
  """The amount requested. Exact decimal."""
  amount: Decimal!
  orderId: ID

  """The order's currency, which must be the card's."""
  orderCurrency: String

  """
  What the order still owes, so the card never overpays it. Exact decimal.
  """
  outstanding: Decimal
}

"""Why a card is being withdrawn."""
input VoidGiftCardInput {
  reason: String
}

"""Why a promotion is being closed before its window ends."""
input ExpirePromotionInput {
  reason: String
}

"""
Why a promotion is being stopped.

The reason is optional and is not passed to the service, which keeps none: an operator's
explanation belongs with the activity that recorded the act, which is where the REST route leaves
it too.
"""
input DeactivatePromotionInput {
  reason: String
}

"""The action set that replaces a promotion's own, in application order."""
input ReplacePromotionActionsInput {
  """
  The new set, applied as a whole: an action's position is part of its meaning, so a replacement
  is the only way to remove one without renumbering the rest. An empty set is refused.
  """
  actions: [PromotionActionInput!]!
}

"""One line an evaluation may discount."""
input PromotionEvaluationLineInput {
  id: ID!

  """The line's amount. Exact decimal."""
  amount: Decimal!
  quantity: Int!
  variantId: ID
  sku: String
}

"""One shipping method an evaluation may discount."""
input PromotionEvaluationShippingInput {
  id: ID!

  """The method's amount. Exact decimal."""
  amount: Decimal!
}

"""
The basket and the customer a promotion is simulated against.

The context is stated by the caller rather than taken from its session, which is what lets an
analyst answer "what would this cost us" for a basket nobody has assembled yet.
"""
input PromotionSimulationInput {
  """
  The currency the basket is priced in. A promotion in another currency is excluded with a notice.
  """
  currency: String!
  channelId: ID
  customerId: ID
  customerGroupIds: [ID!]

  """
  The codes the basket presents. A code that names nothing is a notice, not a failure.
  """
  codes: [String!]

  """The lines that may be discounted."""
  lines: [PromotionEvaluationLineInput!]!

  """The shipping methods that may be discounted."""
  shipping: [PromotionEvaluationShippingInput!]

  """The instant the evaluation is made at; defaults to now."""
  at: DateTime
}

"""The outcome of creating a promotion."""
type CreatePromotionPayload {
  promotion: Promotion

  """
  The durable operation the write belongs to, when it spans more than one step.
  """
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of changing a promotion."""
type UpdatePromotionPayload {
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of deleting a promotion."""
type DeletePromotionPayload {
  """The promotion as it was, so a caller can report what it removed."""
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of starting a promotion."""
type ActivatePromotionPayload {
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of closing a promotion before its window ends."""
type ExpirePromotionPayload {
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of stopping a promotion."""
type DeactivatePromotionPayload {
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of replacing a promotion's action set."""
type ReplacePromotionActionsPayload {
  """The stored actions, in application order."""
  actions: [PromotionAction!]!
  operation: Operation
  userErrors: [UserError!]!
}

"""
The outcome of simulating a promotion against a basket.

Nothing was written: no reservation, no budget consumption, no usage row. The payload states what
the evaluation decided, which is the same answer the checkout path computes.
"""
type SimulatePromotionPayload {
  """
  The applications, the notices and the total the promotion would produce.
  """
  result: PromotionEvaluationResult

  """Every owner the discount would land on, to the last minor unit."""
  allocations: [PromotionAllocation!]!
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a promotion recoverably."""
type SoftDeletePromotionPayload {
  """
  The promotion as the soft delete left it: still queryable, its ledger intact.
  """
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted promotion."""
type RecoverPromotionPayload {
  promotion: Promotion
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a campaign recoverably."""
type SoftDeleteCampaignPayload {
  campaign: Campaign
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted campaign."""
type RecoverCampaignPayload {
  campaign: Campaign
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a campaign's ceiling recoverably."""
type SoftDeleteCampaignBudgetPayload {
  budget: CampaignBudget
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted campaign ceiling."""
type RecoverCampaignBudgetPayload {
  budget: CampaignBudget
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring one per-value consumption row recoverably."""
type SoftDeleteCampaignBudgetUsagePayload {
  budgetUsage: CampaignBudgetUsage
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted per-value consumption row."""
type RecoverCampaignBudgetUsagePayload {
  budgetUsage: CampaignBudgetUsage
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a coupon recoverably."""
type SoftDeleteCouponPayload {
  coupon: Coupon
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted coupon."""
type RecoverCouponPayload {
  coupon: Coupon
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring a gift card recoverably."""
type SoftDeleteGiftCardPayload {
  giftCard: GiftCard
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted gift card."""
type RecoverGiftCardPayload {
  giftCard: GiftCard
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring one gift-card movement recoverably."""
type SoftDeleteGiftCardTransactionPayload {
  transaction: GiftCardTransaction
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted gift-card movement."""
type RecoverGiftCardTransactionPayload {
  transaction: GiftCardTransaction
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring one promotion action recoverably."""
type SoftDeletePromotionActionPayload {
  action: PromotionAction
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted promotion action."""
type RecoverPromotionActionPayload {
  action: PromotionAction
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of retiring one application of a promotion recoverably."""
type SoftDeletePromotionUsagePayload {
  usage: PromotionUsage
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of restoring a soft-deleted application of a promotion."""
type RecoverPromotionUsagePayload {
  usage: PromotionUsage
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of creating a campaign."""
type CreateCampaignPayload {
  campaign: Campaign
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of changing a campaign."""
type UpdateCampaignPayload {
  campaign: Campaign
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of deleting a campaign."""
type DeleteCampaignPayload {
  campaign: Campaign
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of setting a campaign's ceiling."""
type UpdateCampaignBudgetPayload {
  budget: CampaignBudget
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of re-opening a campaign's ceiling."""
type ResetCampaignBudgetPayload {
  """The ceiling after the reset, with its consumption back at zero."""
  budget: CampaignBudget
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of creating a coupon."""
type CreateCouponPayload {
  coupon: Coupon
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of changing a coupon."""
type UpdateCouponPayload {
  coupon: Coupon
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of deleting a coupon."""
type DeleteCouponPayload {
  coupon: Coupon
  operation: Operation
  userErrors: [UserError!]!
}

"""
What a batch of codes adds up to.

The identifier and the two counts travel together because the batch is all-or-nothing: a caller that
holds the identifier its mailing is keyed on and the counts it was answered with can prove the
mailing it asked for is the mailing it got, without counting rows. `failed` is not a partial success
to be retried one code at a time — it is the figure that says the request did not deliver, which is
why the service refuses a shortfall rather than reporting one.
"""
type CouponBatchResult {
  """The identifier every code of the batch carries."""
  batchId: String!

  """How many codes were asked for."""
  requested: Int!

  """How many codes were written."""
  created: Int!

  """How many codes were not."""
  failed: Int!
}

"""The outcome of minting a batch of codes."""
type CreateCouponBatchPayload {
  """The batch identifier and the counts."""
  batch: CouponBatchResult
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of issuing a gift card."""
type IssueGiftCardPayload {
  giftCard: GiftCard
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of spending part of a card's balance."""
type RedeemGiftCardPayload {
  giftCard: GiftCard

  """
  The amount actually applied, which is never more than the card carried. Exact decimal.
  """
  applied: Decimal!
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of withdrawing a card from circulation."""
type VoidGiftCardPayload {
  giftCard: GiftCard
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of changing what a card says about itself."""
type UpdateGiftCardPayload {
  giftCard: GiftCard
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of returning value to a card."""
type RefundGiftCardPayload {
  giftCard: GiftCard

  """
  The amount actually returned, which is never more than the card was spent from. Exact decimal.
  """
  applied: Decimal!
  operation: Operation
  userErrors: [UserError!]!
}

"""The outcome of correcting a card's balance by hand."""
type AdjustGiftCardPayload {
  giftCard: GiftCard

  """The signed amount actually applied. Exact decimal."""
  applied: Decimal!
  operation: Operation
  userErrors: [UserError!]!
}

"""The answer to a code presented without applying it."""
type CouponValidationPayload {
  """Whether the code may be used."""
  valid: Boolean!

  """The coupon, when the code exists."""
  coupon: Coupon

  """Why it may not be used, as a stable platform code."""
  reason: String

  """The discount the code's promotion would give. Exact decimal."""
  discount: Decimal
}

"""The balance of a card, answered from its ledger."""
type GiftCardBalancePayload {
  balance: Decimal!
  currency: String!
  status: GiftCardStatus!
}

"""
A promotion was created, activated or expired.

The state the promotion moved to travels with the event, so a subscriber that caches a promotion
set — the checkout path does — can purge on it rather than on a timer: a promotion that has expired
must stop applying at the instant it expired, not at the next refresh.
"""
type PromotionChangedPayload {
  """
  The event's own identity, which is the idempotency key a consumer applies its effects under.
  """
  id: ID!

  """When the fact was recorded, as opposed to when it was dispatched."""
  createdAt: DateTime!

  """The promotion that changed."""
  promotionId: ID!

  """The state it moved to."""
  status: PromotionStatus!

  """The organization the promotion belongs to."""
  organizationId: ID!
}

"""
A campaign budget reached its ceiling.

Emitted by the evaluation path when a reservation is refused, which is the moment an operator can
still act: a budget reported as spent at the end of the month cannot be topped up in time.
"""
type PromotionBudgetExhaustedPayload {
  id: ID!
  createdAt: DateTime!

  """The promotion whose budget refused the reservation."""
  promotionId: ID!

  """The budget that refused it."""
  budgetId: ID!

  """The ceiling that was reached. Exact decimal."""
  limit: Decimal!

  """What has been consumed, including reservations. Exact decimal."""
  used: Decimal!

  """The organization the budget belongs to."""
  organizationId: ID!
}

"""
A code was redeemed.

The code travels because the usage counters and any mailing report are keyed on it, and the amount
because that is the figure a campaign's spend is reconciled against.
"""
type CouponRedeemedPayload {
  id: ID!
  createdAt: DateTime!

  """The coupon that was redeemed."""
  couponId: ID!

  """The code as presented."""
  code: String!

  """The discount the redemption granted. Exact decimal."""
  amount: Decimal!

  """The currency of the discount."""
  currency: String!

  """The order the redemption was registered against, when it has one yet."""
  orderId: ID

  """The organization the coupon belongs to."""
  organizationId: ID!
}

"""
Stored value was spent.

The balance after the movement travels with the event, because the consumer that reacts to a
redemption — a notification, a balance display — needs the figure the customer will see next, and
reading it separately would race with the next movement on the same card.
"""
type GiftCardRedeemedPayload {
  id: ID!
  createdAt: DateTime!

  """The card that was debited."""
  giftCardId: ID!

  """The amount taken off the card. Exact decimal."""
  amount: Decimal!

  """The balance the card holds afterwards. Exact decimal."""
  balanceAfter: Decimal!

  """The order the value settled."""
  orderId: ID

  """The organization the card belongs to."""
  organizationId: ID!
}

"""A set of variants that ship the same way."""
type ShippingProfile {
  id: ID!
  tenantId: ID
  organizationId: ID
  name: String!
  code: String!
  isDefault: Boolean!
  description: String
  metadata: JSON
  variants: [ShippingProfileVariant!]
  createdAt: DateTime
  updatedAt: DateTime
}

"""The pivot that attaches a variant to a shipping profile."""
type ShippingProfileVariant {
  id: ID!
  profileId: ID!
  variantId: ID!
  metadata: JSON
}

"""A configured, sellable delivery choice."""
type ShippingOption {
  id: ID!
  tenantId: ID
  organizationId: ID
  name: String!
  code: String!
  priceType: String!
  amount: Decimal
  currency: String
  isTaxInclusive: Boolean!
  taxCategoryId: ID
  providerKey: String
  profileId: ID
  channelId: ID
  regionId: ID
  isActive: Boolean
  priority: Int!
  estimatedMinDays: Int
  estimatedMaxDays: Int
  requiresShippingAddress: Boolean!
  allowPickup: Boolean!
  maxWeight: Decimal
  maxItemCount: Int
  version: Int!
  metadata: JSON
  createdAt: DateTime
}

"""One shipment against an order."""
type Fulfillment {
  id: ID!
  tenantId: ID
  organizationId: ID
  orderId: ID!
  direction: String!
  warehouseId: ID
  providerId: String
  status: String!
  trackingNumber: String
  trackingUrl: String
  carrier: String
  service: String
  labelUrl: String
  labelData: JSON
  shippedAt: DateTime
  deliveredAt: DateTime
  canceledAt: DateTime
  requiresShipping: Boolean!
  noNotification: Boolean!
  note: String
  version: Int!
  metadata: JSON
  lines: [FulfillmentLine!]
  createdAt: DateTime
  updatedAt: DateTime
}

"""What is in one shipment."""
type FulfillmentLine {
  id: ID!
  fulfillmentId: ID!
  orderLineId: ID!
  quantity: Decimal!
  warehouseId: ID
  metadata: JSON
}

"""One option together with why it is or is not available to a cart."""
type ShippingOptionEligibility {
  option: ShippingOption!
  eligible: Boolean!
  reason: String
}

"""
The price of one option for one cart, or the strategy that must be asked for it.
"""
type ShippingRate {
  amount: Decimal!
  currency: String
  providerKey: String
  eligible: Boolean!
  reason: String
}

"""A page of shipping profiles."""
type ShippingProfileConnection {
  nodes: [ShippingProfile!]!
  edges: [ShippingProfileEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One shipping profile in a page, with the cursor that addresses it."""
type ShippingProfileEdge {
  node: ShippingProfile!
  cursor: String!
}

"""A page of shipping options."""
type ShippingOptionConnection {
  nodes: [ShippingOption!]!
  edges: [ShippingOptionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One shipping option in a page, with the cursor that addresses it."""
type ShippingOptionEdge {
  node: ShippingOption!
  cursor: String!
}

"""A page of fulfilments."""
type FulfillmentConnection {
  nodes: [Fulfillment!]!
  edges: [FulfillmentEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One fulfilment in a page, with the cursor that addresses it."""
type FulfillmentEdge {
  node: Fulfillment!
  cursor: String!
}

input CreateShippingProfileInput {
  name: String!
  code: String!
  isDefault: Boolean
  description: String

  """
  The client's own key for this request, honoured when one is presented. A request retried under
  the same key is answered with what the first attempt wrote instead of raising a second profile.
  """
  idempotencyKey: String
}

input CreateShippingOptionInput {
  name: String!
  code: String!
  priceType: String!
  amount: Decimal
  currency: String
  isTaxInclusive: Boolean
  taxCategoryId: ID
  providerKey: String
  profileId: ID
  channelId: ID
  regionId: ID
  priority: Int
  estimatedMinDays: Int
  estimatedMaxDays: Int
  requiresShippingAddress: Boolean
  allowPickup: Boolean
  maxWeight: Decimal
  maxItemCount: Int

  """
  The client's own key for this request, honoured when one is presented. A request retried under
  the same key is answered with what the first attempt wrote instead of raising a second option.
  """
  idempotencyKey: String
}

input ShippingOptionAssignmentInput {
  profileId: ID!
  add: [ID!]
  remove: [ID!]
}

input FulfillmentLineInput {
  orderLineId: ID!
  quantity: Decimal!
  warehouseId: ID
}

input CreateFulfillmentInput {
  orderId: ID!
  direction: String
  warehouseId: ID
  providerId: String
  trackingNumber: String
  carrier: String
  service: String
  requiresShipping: Boolean
  noNotification: Boolean
  note: String
  lines: [FulfillmentLineInput!]!

  """
  The client's own key for this request, which this operation requires. A fulfilment that is
  created twice ships the same goods twice, so the mutation is refused without one.
  """
  idempotencyKey: String
}

input UpdateFulfillmentInput {
  trackingNumber: String
  trackingUrl: String
  carrier: String
  service: String
  noNotification: Boolean
  note: String
}

"""
The writable surface of a shipment line, as the line's own edit route validates it.

Every member is nullable, because an edit states what changed rather than restating the row. The set
is that route's own DTO member for member: the shipment the line belongs to, the order line it covers,
the quantity, the location the quantity came from, and the free payload the picking flow reads. The
tenancy members that DTO inherits are deliberately absent, as they are on every input of this document
— tenancy is resolved from the caller rather than stated by it, and a member that let a caller name
another organization's row is a member no service of this platform accepts.
"""
input UpdateFulfillmentLineInput {
  fulfillmentId: ID
  orderLineId: ID
  quantity: Decimal
  warehouseId: ID
  metadata: JSON
}

input ShipFulfillmentInput {
  trackingNumber: String
  carrier: String
  service: String
  noNotification: Boolean

  """
  The client's own key for this request, honoured when one is presented. A request retried under
  the same key is answered with the shipment the first attempt handed over.
  """
  idempotencyKey: String
}

input RequestFulfillmentLabelInput {
  """The registered carrier strategy the label is requested from."""
  providerId: String!

  """
  The service level to label the parcel for, when it differs from the one the shipment records.
  """
  service: String

  """
  The client's own key for this request, honoured when one is presented. A request retried under
  the same key is answered with the label the first attempt recorded, instead of asking the
  carrier a second time.
  """
  idempotencyKey: String

  """
  The version the caller read the fulfilment at. A label is written through the version-predicated
  update, so a shipment that moved on since it was read is refused rather than overwritten.
  """
  version: Int
}

input ShippingEligibilityInput {
  channelId: ID
  regionId: ID
  profileIds: [ID!]
  totalWeight: Decimal
  itemCount: Int
  orderTotal: Decimal
}

"""Where a return is in its lifecycle."""
enum OrderReturnStatus {
  OPEN
  REQUESTED
  APPROVED
  RECEIVED
  PARTIALLY_RECEIVED
  REJECTED
  CANCELED
  CLOSED
}

"""The resolution a claim asks for."""
enum OrderClaimType {
  REFUND
  REPLACE
}

"""Where a claim is in its lifecycle."""
enum OrderClaimStatus {
  OPEN
  REQUESTED
  APPROVED
  REJECTED
  CANCELED
  CLOSED
}

"""Why a claim line is being claimed."""
enum OrderClaimReason {
  MISSING_ITEM
  WRONG_ITEM
  PRODUCTION_FAILURE
  DAMAGED
  OTHER
}

"""Where an exchange is in its lifecycle."""
enum OrderExchangeStatus {
  OPEN
  REQUESTED
  APPROVED
  REJECTED
  CANCELED
  CLOSED
}

"""Goods coming back against a delivered order."""
type OrderReturn {
  id: ID!

  """The order the goods came from."""
  orderId: ID

  """The allocated return number."""
  number: String!
  status: OrderReturnStatus!

  """Receiving location."""
  warehouseId: ID

  """The governed reason the return was filed under."""
  reasonId: ID
  reasonCode: OrderReturnReason

  """Free-text explanation kept beside the governed reason."""
  reason: String

  """What the tenant expects to refund."""
  refundAmount: Decimal
  currency: String!
  requestedAt: DateTime
  approvedAt: DateTime
  receivedAt: DateTime
  canceledAt: DateTime
  closedAt: DateTime

  """The claim this return was created by, when it was."""
  claimId: ID

  """The exchange this return is the inbound half of, when it is."""
  exchangeId: ID
  shippingOptionId: ID
  noNotification: Boolean!
  note: String
  metadata: JSON
  lines: [OrderReturnLine!]!

  """The quantity still expected back across the return's lines."""
  outstandingQuantity: Decimal!

  """
  The version of the return, which every write of it moves on. A client states the version it
  read so its write can be refused when someone else moved the return on in between, and reads
  the new one back from the payload of the write.
  """
  version: Int!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One line of a return."""
type OrderReturnLine {
  id: ID!
  returnId: ID
  orderLineId: ID
  quantity: Decimal!
  receivedQuantity: Decimal!
  damagedQuantity: Decimal!
  reasonId: ID
  restock: Boolean!
  warehouseId: ID
  note: String
  metadata: JSON

  """The quantity still expected back on this line."""
  outstandingQuantity: Decimal!
}

"""A governed reason code for returns."""
type OrderReturnReason {
  id: ID!
  code: String!
  label: String!
  description: String
  parentId: ID
  isActive: Boolean
  children: [OrderReturnReason!]!
}

"""A complaint about a delivered order and the resolution chosen for it."""
type OrderClaim {
  id: ID!
  orderId: ID
  number: String!
  type: OrderClaimType!
  status: OrderClaimStatus!
  refundAmount: Decimal
  currency: String!

  """The return created for this claim, when the goods have to come back."""
  returnId: ID
  returnOfClaim: OrderReturn
  reason: String
  note: String
  canceledAt: DateTime
  metadata: JSON
  lines: [OrderClaimLine!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One line of a claim."""
type OrderClaimLine {
  id: ID!
  claimId: ID
  orderLineId: ID
  variantId: ID
  quantity: Decimal!
  reason: OrderClaimReason!
  isAdditionalItem: Boolean!
  note: String
  metadata: JSON
}

"""A return that immediately becomes a new shipment."""
type OrderExchange {
  id: ID!
  orderId: ID
  number: String!
  status: OrderExchangeStatus!

  """
  Outbound value minus inbound value: positive when the customer owes money.
  """
  differenceDue: Decimal
  currency: String!

  """The inbound half."""
  returnId: ID
  returnOfExchange: OrderReturn
  allowBackorder: Boolean!
  note: String
  canceledAt: DateTime
  metadata: JSON
  lines: [OrderExchangeLine!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One outbound line of an exchange."""
type OrderExchangeLine {
  id: ID!
  exchangeId: ID
  orderLineId: ID
  variantId: ID
  quantity: Decimal!

  """
  The replacement price, snapshotted so the difference stays explainable.
  """
  unitPrice: Decimal!
  note: String
  metadata: JSON

  """
  The value of this line: quantity multiplied by the snapshotted unit price, expressed in the
  currency the caller states. The currency is an argument rather than a stored field because a
  line belongs to an exchange, and the exchange is where the currency lives.
  """
  lineTotal(currency: String!): Decimal!
}

"""One page of returns."""
type OrderReturnConnection {
  edges: [OrderReturnEdge!]!
  nodes: [OrderReturn!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One return inside a page."""
type OrderReturnEdge {
  cursor: String!
  node: OrderReturn!
}

"""One page of return reasons."""
type OrderReturnReasonConnection {
  edges: [OrderReturnReasonEdge!]!
  nodes: [OrderReturnReason!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One return reason inside a page."""
type OrderReturnReasonEdge {
  cursor: String!
  node: OrderReturnReason!
}

"""One page of claims."""
type OrderClaimConnection {
  edges: [OrderClaimEdge!]!
  nodes: [OrderClaim!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One claim inside a page."""
type OrderClaimEdge {
  cursor: String!
  node: OrderClaim!
}

"""One page of exchanges."""
type OrderExchangeConnection {
  edges: [OrderExchangeEdge!]!
  nodes: [OrderExchange!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One exchange inside a page."""
type OrderExchangeEdge {
  cursor: String!
  node: OrderExchange!
}

"""One page of return lines."""
type OrderReturnLineConnection {
  edges: [OrderReturnLineEdge!]!
  nodes: [OrderReturnLine!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One return line inside a page."""
type OrderReturnLineEdge {
  cursor: String!
  node: OrderReturnLine!
}

"""One page of claim lines."""
type OrderClaimLineConnection {
  edges: [OrderClaimLineEdge!]!
  nodes: [OrderClaimLine!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One claim line inside a page."""
type OrderClaimLineEdge {
  cursor: String!
  node: OrderClaimLine!
}

"""One page of exchange lines."""
type OrderExchangeLineConnection {
  edges: [OrderExchangeLineEdge!]!
  nodes: [OrderExchangeLine!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One exchange line inside a page."""
type OrderExchangeLineEdge {
  cursor: String!
  node: OrderExchangeLine!
}

"""Filters a page of returns."""
input OrderReturnFilter {
  status: OrderReturnStatus
  orderId: ID
  number: String
  warehouseId: ID
}

"""Filters a page of return reasons."""
input OrderReturnReasonFilter {
  isActive: Boolean
  parentId: ID
  code: String
}

"""Filters a page of claims."""
input OrderClaimFilter {
  status: OrderClaimStatus
  type: OrderClaimType
  orderId: ID
  number: String
}

"""Filters a page of exchanges."""
input OrderExchangeFilter {
  status: OrderExchangeStatus
  orderId: ID
  number: String
}

"""One requested line of a return."""
input OrderReturnLineInput {
  orderLineId: ID!
  quantity: Decimal!
  reasonId: ID
  restock: Boolean
  warehouseId: ID
  note: String
}

"""The request that opens a return."""
input RequestOrderReturnInput {
  orderId: ID!
  lines: [OrderReturnLineInput!]!
  warehouseId: ID
  reasonId: ID
  reason: String
  currency: String!
  shippingOptionId: ID
  noNotification: Boolean
  note: String

  """
  The client's own key for this request, honoured when one is presented. A request that is
  retried under the same key is answered with the first attempt's response instead of raising a
  second return.
  """
  idempotencyKey: String
}

"""One line as it is received back."""
input ReceiveOrderReturnLineInput {
  lineId: ID!
  receivedQuantity: Decimal!
  damagedQuantity: Decimal
  restock: Boolean
}

"""The receipt of a return's goods."""
input ReceiveOrderReturnInput {
  lines: [ReceiveOrderReturnLineInput!]!
  warehouseId: ID
  refund: Decimal
  note: String

  """
  The version of the return the caller read, which this receipt is predicated on.
  """
  version: Int

  """
  The client's own key for this request, which this operation requires. A receipt is refused
  without one, because receiving the same goods twice restocks and refunds them twice.
  """
  idempotencyKey: String
}

"""The definition of a governed return reason."""
input OrderReturnReasonInput {
  code: String!
  label: String!
  description: String
  parentId: ID
  isActive: Boolean
}

"""One claimed line."""
input OrderClaimLineInput {
  orderLineId: ID
  variantId: ID
  quantity: Decimal!
  reason: OrderClaimReason
  isAdditionalItem: Boolean
  note: String
}

"""The request that opens a claim."""
input RequestOrderClaimInput {
  orderId: ID!
  type: OrderClaimType!
  currency: String!
  lines: [OrderClaimLineInput!]!
  reason: String
  note: String
}

"""One requested replacement line."""
input OrderExchangeLineInput {
  orderLineId: ID
  variantId: ID!
  quantity: Decimal!
  unitPrice: Decimal
  note: String
}

"""The request that opens an exchange."""
input RequestOrderExchangeInput {
  orderId: ID!
  currency: String!
  lines: [OrderExchangeLineInput!]!
  returnId: ID
  allowBackorder: Boolean
  note: String
}

"""
One line as an operator replaces a return's line set.

It is the request's own line shape rather than a second one, because the edit writes the same
columns the request wrote — a set replaced with a shape of its own would be two ways to state one
line, and the ceiling check runs against whichever arrived.
"""
input UpdateOrderReturnLineInput {
  orderLineId: ID!
  quantity: Decimal!
  reasonId: ID
  restock: Boolean
  warehouseId: ID
  note: String
}

"""
The edit of a requested return: its header fields, and the line set that replaces the old one.

The version is a member here rather than an argument of the mutation, because this act takes an
input and the kernel reads the accepted version from the operation's arguments: a client states it
beside what it is changing. It is nullable because the refusal is the kernel's to state — a write
that sends none is answered `VERSION_REQUIRED`, which is the answer the REST route's missing
`If-Match` gets — and a document that made the member non-null would refuse the request before the
kernel could answer it.
"""
input UpdateOrderReturnInput {
  lines: [UpdateOrderReturnLineInput!]
  warehouseId: ID
  reason: String
  note: String
  version: Int
}

"""The amount a received return is refunded, and what explains it."""
input RefundOrderReturnInput {
  amount: Decimal!
  reasonId: ID
  note: String
  version: Int
}

"""The return leg: the shipment that brings the goods back."""
input ShipOrderReturnInput {
  shippingOptionId: ID
  warehouseId: ID
  trackingNumber: String
  version: Int
}

"""
One line as an operator replaces a claim's line set.

`orderLineId` and `variantId` are both optional because a claim line is one of two things — the
defective or missing original, or a replacement unit to be shipped — and the service refuses a line
that states both or neither. Stating both here would let the document imply a shape the service
rejects.
"""
input UpdateOrderClaimLineInput {
  orderLineId: ID
  variantId: ID
  quantity: Decimal
  reason: OrderClaimReason
  isAdditionalItem: Boolean
  note: String
}

"""
The edit of an open claim: what explains it, and the line set that replaces the old one.
"""
input UpdateOrderClaimInput {
  lines: [UpdateOrderClaimLineInput!]
  reason: String
  note: String
}

"""
One line as an operator replaces an exchange's line set.

`variantId` is required and `unitPrice` is not, which is the route's own body: a replacement is
always for a named variant, and a line that states no price is priced by the service against the
resolved price of the day.
"""
input UpdateOrderExchangeLineInput {
  orderLineId: ID
  variantId: ID!
  quantity: Decimal
  unitPrice: Decimal
  note: String
}

"""
The edit of an open exchange: the backorder allowance, a note, and the line set that replaces the old one.
"""
input UpdateOrderExchangeInput {
  lines: [UpdateOrderExchangeLineInput!]
  allowBackorder: Boolean
  note: String
}

"""The outcome of a mutation on a return."""
type RequestOrderReturnPayload {
  orderReturn: OrderReturn
  userErrors: [UserError!]!
}

"""The outcome of receiving a return's goods."""
type ReceiveOrderReturnPayload {
  orderReturn: OrderReturn

  """The stock movements the receipt wrote."""
  movementIds: [ID!]!

  """The refund that was issued, when one was."""
  refundId: ID
  refundAmount: Decimal

  """Completed quantity across the received lines."""
  receivedQuantity: Decimal!

  """Quantity still expected back."""
  outstandingQuantity: Decimal!
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a claim."""
type RequestOrderClaimPayload {
  orderClaim: OrderClaim

  """The refund that was issued, when the resolution settled in money."""
  refundId: ID
  userErrors: [UserError!]!
}

"""The outcome of a mutation on an exchange."""
type RequestOrderExchangePayload {
  orderExchange: OrderExchange
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a return reason."""
type OrderReturnReasonPayload {
  orderReturnReason: OrderReturnReason
  userErrors: [UserError!]!
}

"""The outcome of removing a return reason."""
type DeleteOrderReturnReasonPayload {
  id: ID
  userErrors: [UserError!]!
}

"""
The outcome of refunding a received return.

It answers the return beside the refund because the write moves two rows: the refund is what the
provider settled, and the return is where the running refund total and the version moved on. A
payload carrying only the refund would leave a client to re-read the return to learn the version
its next write has to state.
"""
type RefundOrderReturnPayload {
  orderReturn: OrderReturn

  """The refund that was written."""
  refundId: ID

  """
  The amount actually paid back, which is what was recorded rather than what was asked for.
  """
  refundAmount: Decimal
  currency: String
  userErrors: [UserError!]!
}

"""
The outcome of creating a return's inbound shipment.

The leg is a fulfillment of another domain, so it is named by its identifier rather than carried as
an object graph this document does not own; the label, when the carrier issued one, travels with it
because re-fetching it is another call to the carrier.
"""
type ShipOrderReturnPayload {
  orderReturn: OrderReturn

  """The fulfillment that carries the goods back."""
  fulfillmentId: ID
  trackingNumber: String
  labelUrl: String
  userErrors: [UserError!]!
}

"""
The outcome of removing a return destructively.

It carries the identifier and nothing else, because there is no row left to carry: the deletion is
physical, and a payload shaped like the resource's others would promise a row that no longer exists.
"""
type DeleteOrderReturnPayload {
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of removing a claim destructively."""
type DeleteOrderClaimPayload {
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of removing an exchange destructively."""
type DeleteOrderExchangePayload {
  id: ID
  userErrors: [UserError!]!
}

"""How often a plan bills; the interval multiplies the period."""
enum SubscriptionBillingPeriod {
  DAILY
  WEEKLY
  MONTHLY
  QUARTERLY
  YEARLY
}

"""Where a subscription stands."""
enum SubscriptionStatus {
  PENDING
  ACTIVE
  PAUSED
  CANCELED
  EXPIRED
  FAILED
}

"""Where one billing cycle stands."""
enum SubscriptionBillingStatus {
  PENDING
  INVOICED
  PAID
  FAILED
  REFUNDED
  WAIVED
}

"""How a mid-cycle change settled the difference it produced."""
enum SubscriptionSettlement {
  """Charged now, through the ordinary order path."""
  CHARGED

  """Carried as a credit against the next cycle."""
  DEFERRED

  """Smaller than the cost of collecting it, so forgiven."""
  WAIVED

  """Scheduled to apply from the next period; no money moved."""
  SCHEDULED
}

"""What can be subscribed to, and on what terms."""
type SubscriptionPlan {
  id: ID!
  name: String!

  """The tenant's own key for the plan, unique inside the organization."""
  code: String!
  description: String

  """Plan attached to a whole product, when it is."""
  productId: ID

  """Plan attached to one variant, when it is."""
  variantId: ID
  billingPeriod: SubscriptionBillingPeriod!

  """How many periods pass between two billings."""
  billingInterval: Int!

  """
  Cycles after which a subscription expires; null means it runs until cancelled.
  """
  maxBillingCycles: Int

  """Days the first period is free for."""
  trialDays: Int

  """Charged once, with the first paid cycle."""
  setupFee: Decimal

  """Recurring discount as a fraction: 0.1 is ten per cent."""
  discountPercentage: Decimal
  currency: String!

  """Whether a new subscription may be created from this plan."""
  isActive: Boolean
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""A running agreement with a customer."""
type CustomerSubscription {
  id: ID!
  planId: ID!
  plan: SubscriptionPlan
  customerId: ID!

  """The order that started the subscription, when there was one."""
  originOrderId: ID

  """
  The account at the provider renewals are charged against, when one is remembered.
  """
  paymentAccountHolderId: ID

  """The instrument renewals are charged against, when one is remembered."""
  paymentMethodTokenId: ID
  status: SubscriptionStatus!
  quantity: Decimal!
  currentPeriodStart: DateTime
  currentPeriodEnd: DateTime

  """The due-billing scan key: set while the subscription bills."""
  nextBillingAt: DateTime

  """Cycles billed so far."""
  billingCycleCount: Int!

  """
  Resume instant for a paused subscription; null means the pause is indefinite.
  """
  pausedUntil: DateTime
  canceledAt: DateTime
  cancelReason: String
  currency: String!
  metadata: JSON

  """
  Optimistic lock: the value a caller states back on the mutation that writes this subscription.
  """
  version: Int!

  """The recurring lines each cycle bills."""
  items: [SubscriptionItem!]!

  """Every cycle that was billed, or attempted."""
  billings: [SubscriptionBilling!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One recurring line of a subscription."""
type SubscriptionItem {
  id: ID!
  subscriptionId: ID!
  variantId: ID!
  quantity: Decimal!

  """
  Recurring unit price, snapshotted when the line was written or last repriced.
  """
  unitPrice: Decimal!
  position: Int!
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""One billing cycle's attempt and result."""
type SubscriptionBilling {
  id: ID!
  subscriptionId: ID!

  """The order this cycle produced, once it has one."""
  orderId: ID
  periodStart: DateTime!
  periodEnd: DateTime!

  """The recurring amount for the period, less the plan discount."""
  amount: Decimal!
  currency: String!
  status: SubscriptionBillingStatus!

  """When the cycle became payable."""
  dueAt: DateTime

  """When the payment settled; set exactly when the cycle is paid."""
  paidAt: DateTime

  """How many attempts the cycle has used."""
  attemptCount: Int!

  """Why the last attempt failed."""
  lastError: String

  """When the next dunning attempt is owed."""
  nextRetryAt: DateTime
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""One page of subscription plans."""
type SubscriptionPlanConnection {
  edges: [SubscriptionPlanEdge!]!
  nodes: [SubscriptionPlan!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One plan inside a page."""
type SubscriptionPlanEdge {
  cursor: String!
  node: SubscriptionPlan!
}

"""One page of subscriptions."""
type CustomerSubscriptionConnection {
  edges: [CustomerSubscriptionEdge!]!
  nodes: [CustomerSubscription!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One subscription inside a page."""
type CustomerSubscriptionEdge {
  cursor: String!
  node: CustomerSubscription!
}

"""One page of recurring lines."""
type SubscriptionItemConnection {
  edges: [SubscriptionItemEdge!]!
  nodes: [SubscriptionItem!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One recurring line inside a page."""
type SubscriptionItemEdge {
  cursor: String!
  node: SubscriptionItem!
}

"""One page of billing cycles."""
type SubscriptionBillingConnection {
  edges: [SubscriptionBillingEdge!]!
  nodes: [SubscriptionBilling!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One billing cycle inside a page."""
type SubscriptionBillingEdge {
  cursor: String!
  node: SubscriptionBilling!
}

"""Filters a page of plans."""
input SubscriptionPlanFilter {
  code: String
  isActive: Boolean
  billingPeriod: SubscriptionBillingPeriod
  productId: ID
  variantId: ID
}

"""Filters a page of subscriptions."""
input SubscriptionFilter {
  status: SubscriptionStatus
  planId: ID
  customerId: ID
  originOrderId: ID
  currency: String
}

"""Filters a page of recurring lines."""
input SubscriptionItemFilter {
  subscriptionId: ID
  variantId: ID
}

"""Filters a page of billing cycles."""
input SubscriptionBillingFilter {
  status: SubscriptionBillingStatus
  subscriptionId: ID
  orderId: ID
}

"""The plan as it is created."""
input CreateSubscriptionPlanInput {
  name: String!
  code: String!
  currency: String!
  description: String
  productId: ID
  variantId: ID
  billingPeriod: SubscriptionBillingPeriod
  billingInterval: Int
  maxBillingCycles: Int
  trialDays: Int
  setupFee: Decimal
  discountPercentage: Decimal
  metadata: JSON
}

"""The plan as it is updated."""
input UpdateSubscriptionPlanInput {
  name: String
  code: String
  description: String
  productId: ID
  variantId: ID
  billingPeriod: SubscriptionBillingPeriod
  billingInterval: Int
  maxBillingCycles: Int
  trialDays: Int
  setupFee: Decimal
  discountPercentage: Decimal
  isActive: Boolean
  metadata: JSON
}

"""One recurring line as it is stated."""
input SubscriptionItemInput {
  variantId: ID!
  quantity: Decimal

  """
  Omitted means the price is resolved through the ordinary pricing pipeline.
  """
  unitPrice: Decimal
  position: Int
}

"""The request that puts a customer on a plan."""
input CreateSubscriptionInput {
  planId: ID!
  customerId: ID!
  originOrderId: ID
  quantity: Decimal
  currency: String
  items: [SubscriptionItemInput!]
  paymentAccountHolderId: ID
  paymentMethodTokenId: ID

  """Bill the first period as part of creation."""
  activate: Boolean

  """Leave the subscription inside the plan's trial."""
  startTrial: Boolean
  discountPercentage: Decimal
  metadata: JSON
  idempotencyKey: String
}

"""The fields a subscription's caller may move."""
input UpdateSubscriptionInput {
  paymentAccountHolderId: ID
  paymentMethodTokenId: ID
  quantity: Decimal
  metadata: JSON

  """The version the caller read the subscription at."""
  version: Int
  idempotencyKey: String
}

"""The request that moves a subscription to another plan."""
input ChangeSubscriptionPlanInput {
  planId: ID!
  quantity: Decimal
  effective: String
  note: String

  """The version the caller read the subscription at."""
  version: Int
  idempotencyKey: String
}

"""The request that suspends billing."""
input PauseSubscriptionInput {
  until: DateTime
  reason: String

  """The version the caller read the subscription at."""
  version: Int
  idempotencyKey: String
}

"""The request that ends a subscription."""
input CancelSubscriptionInput {
  reason: String

  """True stops billing now; false lets the paid period run out."""
  immediate: Boolean

  """The version the caller read the subscription at."""
  version: Int
  idempotencyKey: String
}

"""The request that ends a subscription because it ran out."""
input ExpireSubscriptionInput {
  reason: String

  """The version the caller read the subscription at."""
  version: Int
  idempotencyKey: String
}

"""The request that bills one cycle of one subscription."""
input BillSubscriptionInput {
  asOf: DateTime

  """The version the caller read the subscription at."""
  version: Int
  idempotencyKey: String
}

"""The request that runs a billing pass."""
input RunSubscriptionBillingInput {
  subscriptionId: ID
  limit: Int
  asOf: DateTime
}

"""One recurring line as it is added."""
input CreateSubscriptionItemInput {
  subscriptionId: ID!
  variantId: ID!
  quantity: Decimal
  unitPrice: Decimal
  position: Int
}

"""One recurring line as it is corrected."""
input UpdateSubscriptionItemInput {
  quantity: Decimal
  unitPrice: Decimal
  position: Int
  metadata: JSON
}

"""A billing cycle as it is opened by hand."""
input CreateSubscriptionBillingInput {
  subscriptionId: ID!
  periodStart: DateTime!
  periodEnd: DateTime!
  amount: Decimal!
  currency: String!
  dueAt: DateTime
}

"""A billing cycle as it is corrected."""
input UpdateSubscriptionBillingInput {
  amount: Decimal
  dueAt: DateTime
  metadata: JSON
}

"""The request that records a cycle's payment."""
input PaySubscriptionBillingInput {
  paidAt: DateTime
  note: String
}

"""The request that deliberately does not charge a cycle."""
input WaiveSubscriptionBillingInput {
  reason: String!
}

"""The outcome of a mutation on a plan."""
type SubscriptionPlanPayload {
  subscriptionPlan: SubscriptionPlan
  userErrors: [UserError!]!
}

"""The outcome of deactivating a plan."""
type DeleteSubscriptionPlanPayload {
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a subscription."""
type CustomerSubscriptionPayload {
  subscription: CustomerSubscription
  userErrors: [UserError!]!
}

"""The outcome of deleting a subscription outright."""
type DeleteSubscriptionPayload {
  """The subscription that was removed; null when the removal was refused."""
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a recurring line."""
type SubscriptionItemPayload {
  subscriptionItem: SubscriptionItem
  userErrors: [UserError!]!
}

"""The outcome of removing a recurring line."""
type DeleteSubscriptionItemPayload {
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a billing cycle."""
type SubscriptionBillingPayload {
  subscriptionBilling: SubscriptionBilling
  userErrors: [UserError!]!
}

"""What a mid-cycle change decided and what it settled."""
type SubscriptionPlanChangePayload {
  subscription: CustomerSubscription

  """What the old plan's unused time was worth."""
  credit: Decimal!

  """What the new plan costs for the same remaining time."""
  charge: Decimal!

  """charge minus credit: positive is owed by the customer."""
  net: Decimal!
  settlement: SubscriptionSettlement!
  currency: String!
  userErrors: [UserError!]!
}

"""What one billing cycle did."""
type SubscriptionBillingOutcomePayload {
  subscriptionId: ID!
  billingId: ID
  status: SubscriptionBillingStatus!

  """
  True when the cycle was already settled and nothing was charged a second time.
  """
  replayed: Boolean!
  orderId: ID
  amount: Decimal
  currency: String
  periodStart: DateTime
  periodEnd: DateTime
  nextRetryAt: DateTime

  """A platform code explaining a failure."""
  errorCode: String
  message: String
  userErrors: [UserError!]!
}

"""What one billing pass did."""
type SubscriptionBillingRunPayload {
  examined: Int!
  billed: Int!
  failed: Int!
  skipped: Int!
  results: [SubscriptionBillingOutcomePayload!]!
  userErrors: [UserError!]!
}

"""Where a purchase order is in its lifecycle."""
enum PurchaseOrderStatus {
  DRAFT
  SENT
  ACKNOWLEDGED
  PARTIALLY_RECEIVED
  RECEIVED
  CANCELED
  CLOSED
}

"""Where a goods receipt is in its lifecycle."""
enum GoodsReceiptStatus {
  POSTED
  CANCELED
}

"""Where a negotiated vendor term is in its life."""
enum VendorTermStatus {
  DRAFT
  ACTIVE
  INACTIVE
}

"""What a supplier's bill is matched against."""
enum PurchaseBillingPolicy {
  ON_ORDERED
  ON_RECEIVED
}

"""A document ordering goods from a supplier."""
type PurchaseOrder {
  id: ID!

  """The allocated order number, unique inside the organization."""
  number: String!

  """The supplier the goods are ordered from."""
  vendorId: ID!

  """The location the goods are expected at."""
  warehouseId: ID!

  """
  The supplier's own order number, which their acknowledgement and their bill quote.
  """
  vendorReference: String

  """
  Who owns this order - the routing key for every approval and follow-up.
  """
  buyerUserId: ID
  status: PurchaseOrderStatus!

  """Currency every amount on this order is expressed in."""
  currency: String!

  """Sum of the line amounts before the header-level adjustments."""
  subtotal: Decimal!

  """Sum of the line discounts."""
  discountTotal: Decimal!

  """Sum of the line taxes."""
  taxTotal: Decimal!

  """Freight and handling charged for the order as a whole."""
  shippingTotal: Decimal!

  """
  subtotal - discountTotal + taxTotal + shippingTotal, derived on every write.
  """
  grandTotal: Decimal!

  """The settlement schedule the order runs on."""
  paymentTermId: ID

  """The simple settlement form as it stood at order time, snapshotted."""
  paymentTermsDaysSnapshot: Int

  """
  Computed once, at order time, from the resolved term. A dunning report reads this.
  """
  dueDate: DateTime
  expectedAt: DateTime

  """
  When the order was sent, which is when its quantities start counting as incoming.
  """
  orderedAt: DateTime
  sentAt: DateTime
  acknowledgedAt: DateTime

  """When the order was approved internally."""
  approvedAt: DateTime
  approvedByUserId: ID

  """
  The platform approval request the order is waiting on, when the tenant requires one.
  """
  approvalId: ID

  """Non-null exactly when the status is RECEIVED or CLOSED."""
  receivedAt: DateTime
  canceledAt: DateTime
  closedAt: DateTime

  """The optimistic-lock counter, which every transition bumps."""
  version: Int!
  note: String
  metadata: JSON

  """
  The ordered lines. A draft's set may be rewritten; once goods arrived it is frozen.
  """
  lines: [PurchaseOrderLine!]!

  """What actually arrived against this order, once per delivery."""
  receipts: [GoodsReceipt!]!

  """
  The quantity still expected: the ordered quantity minus everything received and damaged.
  """
  outstandingQuantity: Decimal!

  """Whether the order carries an internal approval."""
  isApproved: Boolean!
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One ordered line: a sellable unit, how many are expected, and what it costs.
"""
type PurchaseOrderLine {
  id: ID!

  """The order this line belongs to."""
  purchaseOrderId: ID!

  """The order this line belongs to, as an object."""
  purchaseOrder: PurchaseOrder

  """The variant being bought."""
  variantId: ID!

  """How many units were ordered."""
  quantity: Decimal!

  """The unit the buyer ordered in, when the line states one."""
  unitId: ID

  """Snapshot of the unit factor at entry, never re-read."""
  conversionFactor: Decimal!

  """How many good units have arrived."""
  receivedQuantity: Decimal!

  """How many units arrived unsellable."""
  damagedQuantity: Decimal!

  """Cache re-derived from the bill lines, never incremented."""
  billedQuantity: Decimal!

  """Purchase cost of one unit, in the order's currency."""
  unitCost: Decimal!

  """The supplier container the term priced, snapshotted at order time."""
  orderedPackSize: Decimal

  """
  Which term row priced this line. Provenance only: the line never re-reads it.
  """
  vendorTermId: ID

  """Tax rate applied to the line total, as a fraction."""
  taxRate: Decimal

  """Discount negotiated for this line."""
  discountTotal: Decimal!

  """quantity x unitCost - discountTotal + tax, derived on every write."""
  total: Decimal!
  expectedAt: DateTime
  note: String
  metadata: JSON

  """The quantity still expected on this line."""
  outstandingQuantity: Decimal!

  """What is still unbilled, derived at read and never stored."""
  toBillQuantity(policy: PurchaseBillingPolicy!): Decimal!
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One physical delivery. Its order is optional: a consolidated delivery covers several, and goods may arrive with no order at all.
"""
type GoodsReceipt {
  id: ID!

  """The order this delivery is anchored to, when it is anchored to one."""
  purchaseOrderId: ID

  """The location the goods arrived at."""
  warehouseId: ID!

  """The allocated receipt number, unique inside the organization."""
  number: String!
  status: GoodsReceiptStatus!

  """When the goods physically arrived."""
  receivedAt: DateTime!
  receivedByUserId: ID

  """When the receipt was reversed."""
  canceledAt: DateTime

  """The optimistic-lock counter, which a reversal bumps."""
  version: Int!
  note: String
  metadata: JSON

  """What arrived, line by line."""
  lines: [GoodsReceiptLine!]!

  """The stock movements this receipt wrote, one per line and disposition."""
  stockMovementIds: [ID!]!
  createdAt: DateTime
  updatedAt: DateTime
}

"""
One line of a goods receipt: what arrived and how much of it is sellable.
"""
type GoodsReceiptLine {
  id: ID!

  """The receipt this line belongs to."""
  receiptId: ID!

  """The receipt this line belongs to, as an object."""
  receipt: GoodsReceipt

  """The order line this delivery is against."""
  purchaseOrderLineId: ID!

  """The variant that arrived."""
  variantId: ID!

  """Good units that go into sellable stock."""
  quantity: Decimal!

  """Units that arrived unsellable and are recorded but never sellable."""
  damagedQuantity: Decimal!

  """Actual landed cost per unit, which may differ from the ordered cost."""
  unitCost: Decimal!

  """Lot or batch the units belong to."""
  batchNumber: String
  expiresAt: DateTime

  """
  The bin the units are to be placed into, when the line asked for put-away.
  """
  warehouseBinId: ID

  """
  The RECEIPT movement this line produced, written once and never rewritten.
  """
  stockMovementId: ID
  note: String
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""One page of purchase orders."""
type PurchaseOrderConnection {
  edges: [PurchaseOrderEdge!]!
  nodes: [PurchaseOrder!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One purchase order inside a page."""
type PurchaseOrderEdge {
  cursor: String!
  node: PurchaseOrder!
}

"""One page of goods receipts."""
type GoodsReceiptConnection {
  edges: [GoodsReceiptEdge!]!
  nodes: [GoodsReceipt!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One goods receipt inside a page."""
type GoodsReceiptEdge {
  cursor: String!
  node: GoodsReceipt!
}

"""The commercial agreement with one supplier, for one sellable unit."""
type VendorProductTerm {
  id: ID!

  """The supplier the agreement is with."""
  vendorId: ID!

  """The sellable unit the agreement is about."""
  variantId: ID!

  """ISO 4217 code the price is stated in."""
  currency: String!

  """Price for one base unit excluding tax, at or above minQuantity."""
  unitCost: Decimal!

  """Negotiated fraction off the price; null means none."""
  discountPercent: Decimal

  """The quantity from which this price applies."""
  minQuantity: Decimal!

  """The supplier's selling container, e.g. a case of twelve."""
  packSize: Decimal

  """What the supplier calls that container."""
  packLabel: String

  """
  Days from order confirmation to receipt for this product; null inherits the supplier's.
  """
  leadTimeDays: Int

  """The supplier's own code for our variant."""
  vendorProductCode: String

  """The supplier's own name for it."""
  vendorProductName: String

  """
  Negotiated over-shipment allowance; null falls through to the organization's setting.
  """
  overReceiptTolerancePercent: Decimal

  """Lower wins between two rows that both match."""
  priority: Int!

  """Validity window; null is open-ended on that side."""
  startsAt: DateTime
  endsAt: DateTime
  status: VendorTermStatus!
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""One page of vendor terms."""
type VendorProductTermConnection {
  edges: [VendorProductTermEdge!]!
  nodes: [VendorProductTerm!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One vendor term inside a page."""
type VendorProductTermEdge {
  cursor: String!
  node: VendorProductTerm!
}

"""What a caller asks a vendor term to price."""
input ResolveVendorProductTermInput {
  vendorId: ID!
  variantId: ID!

  """The quantity in the reference unit."""
  quantity: Decimal!

  """The currency the answer is wanted in."""
  currency: String!

  """The instant the term's window must contain; now when omitted."""
  date: DateTime
}

"""What the resolution answered with."""
type VendorProductTermResolution {
  """The row that priced the line, when one matched."""
  term: VendorProductTerm

  """The price to apply, in the requested currency."""
  unitCost: Decimal!

  """
  The negotiated fraction off the price, when the winning row carried one.
  """
  discountPercent: Decimal

  """The currency the price is stated in."""
  currency: String!

  """
  Days from order confirmation to receipt, after the term-then-vendor precedence.
  """
  leadTimeDays: Int!

  """The supplier's container, when the winning row carried one."""
  packSize: Decimal
  packLabel: String
  vendorProductCode: String
  vendorProductName: String

  """The allowance the winning row negotiated, when it carried one."""
  overReceiptTolerancePercent: Decimal

  """The vendor-level floor on one order, as it stands."""
  minimumOrderAmount: Decimal

  """Where the price came from: TERM, VARIANT_COST_PRICE or NONE."""
  source: String!

  """What the caller should be told about the outcome."""
  warnings: [String!]!
}

"""Filters a page of vendor terms."""
input VendorProductTermFilter {
  vendorId: ID
  variantId: ID
  currency: String
  status: VendorTermStatus
  vendorProductCode: String
}

"""One term as a caller writes it."""
input VendorProductTermInput {
  vendorId: ID!
  variantId: ID!
  currency: String

  """The price of one base unit. State this or a pack price."""
  unitCost: Decimal

  """
  The price of the supplier's container, from which the unit price is derived.
  """
  packPrice: Decimal
  packSize: Decimal
  packLabel: String
  discountPercent: Decimal
  minQuantity: Decimal
  leadTimeDays: Int
  vendorProductCode: String
  vendorProductName: String
  overReceiptTolerancePercent: Decimal
  priority: Int
  startsAt: DateTime
  endsAt: DateTime
  status: VendorTermStatus
}

"""The request that writes several terms at once."""
input BulkVendorProductTermInput {
  terms: [VendorProductTermInput!]!
}

"""The outcome of a mutation on a vendor term."""
type VendorProductTermPayload {
  vendorProductTerm: VendorProductTerm

  """Every term a bulk write touched."""
  vendorProductTerms: [VendorProductTerm!]!
  userErrors: [UserError!]!
}

"""The outcome of retiring a vendor term."""
type DeleteVendorProductTermPayload {
  id: ID
  userErrors: [UserError!]!
}

"""Filters a page of purchase orders."""
input PurchaseOrderFilter {
  status: PurchaseOrderStatus
  vendorId: ID
  warehouseId: ID
  number: String
  expectedAt: DateTime
}

"""Filters a page of goods receipts."""
input GoodsReceiptFilter {
  purchaseOrderId: ID
  warehouseId: ID
  status: GoodsReceiptStatus
  number: String
}

"""One ordered line as a caller supplies it."""
input PurchaseOrderLineInput {
  variantId: ID!
  quantity: Decimal!

  """
  The unit the quantity is stated in; the variant's purchase unit when omitted.
  """
  unitId: ID

  """Snapshot of the unit factor, when the caller states one."""
  conversionFactor: Decimal

  """
  Omit to price the line from the standing agreement, then from the variant's own cost price.
  """
  unitCost: Decimal
  taxRate: Decimal
  discountTotal: Decimal
  expectedAt: DateTime
  note: String
}

"""The request that raises a purchase order."""
input CreatePurchaseOrderInput {
  vendorId: ID!
  warehouseId: ID!
  currency: String!

  """
  The supplier's own order number, which their acknowledgement and their bill quote.
  """
  vendorReference: String

  """Who owns the order. Defaults to the caller."""
  buyerUserId: ID

  """
  The settlement schedule to snapshot from the supplier's own when it states none.
  """
  paymentTermId: ID

  """The simple settlement form to snapshot, in days."""
  paymentTermsDaysSnapshot: Int
  expectedAt: DateTime
  shippingTotal: Decimal
  note: String
  lines: [PurchaseOrderLineInput!]!

  """
  The client's own key for this request, honoured when one is presented. A request retried under
  the same key is answered with the order the first attempt raised instead of raising a second
  document for the same purchase.
  """
  idempotencyKey: String
}

"""An amendment to a draft purchase order."""
input UpdatePurchaseOrderInput {
  vendorReference: String
  buyerUserId: ID
  paymentTermId: ID
  paymentTermsDaysSnapshot: Int
  expectedAt: DateTime
  shippingTotal: Decimal
  note: String

  """Replaces the line set when supplied."""
  lines: [PurchaseOrderLineInput!]
}

"""One received line as a caller supplies it."""
input GoodsReceiptLineInput {
  purchaseOrderLineId: ID!
  quantity: Decimal!
  damagedQuantity: Decimal
  unitCost: Decimal
  batchNumber: String
  expiresAt: DateTime
  warehouseBinId: ID
  note: String
}

"""The request that records a delivery."""
input CreateGoodsReceiptInput {
  """
  The order the delivery is anchored to. Omit for a consolidated delivery covering several orders.
  """
  purchaseOrderId: ID
  warehouseId: ID
  receivedAt: DateTime

  """
  Fraction of the ordered quantity a line may be exceeded by. The line's own standing allowance applies when omitted.
  """
  overReceiptTolerance: Decimal
  note: String
  lines: [GoodsReceiptLineInput!]!

  """
  The client's own key for this request, which this operation requires. A delivery that is booked
  twice books the stock twice, so the mutation is refused without one.
  """
  idempotencyKey: String
}

"""One further line recorded against a receipt that was already posted."""
input RecordGoodsReceiptLineInput {
  purchaseOrderLineId: ID!
  quantity: Decimal!
  damagedQuantity: Decimal
  unitCost: Decimal
  batchNumber: String
  expiresAt: DateTime
  warehouseBinId: ID
  note: String
}

"""
The delivery a caller records against the order the field already names.

It is the body of `POST /purchase-orders/:id/receipts` plus the two things that route reads from its
headers. The order is the field's own argument on this path, as it is the route's path parameter. The
location is absent for the same reason it is absent from the route: a delivery anchored to an order
inherits that order's receiving location, and an input that accepted a location the service never
receives would tell a caller it had moved the goods somewhere it had not.
"""
input ReceivePurchaseOrderInput {
  receivedAt: DateTime

  """
  Fraction of the ordered quantity a line may be exceeded by, e.g. 0.050000 for five percent. The line's own standing allowance applies when omitted.
  """
  overReceiptTolerance: Decimal
  note: String
  lines: [GoodsReceiptLineInput!]!

  """
  The version of the purchase order the caller read, which the route states as `If-Match`. When it is
  stated and the order has moved on since, the delivery is refused rather than booked against an order
  another buyer has amended.
  """
  version: Int

  """
  The client's own key for this request, which this operation requires, as `createGoodsReceipt` does
  and under the same scope. A delivery that is booked twice books the stock twice, so the mutation is
  refused without one.
  """
  idempotencyKey: String
}

"""The outcome of a mutation on a purchase order."""
type PurchaseOrderPayload {
  purchaseOrder: PurchaseOrder
  userErrors: [UserError!]!
}

"""The outcome of deleting a purchase order."""
type DeletePurchaseOrderPayload {
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a goods receipt."""
type GoodsReceiptPayload {
  goodsReceipt: GoodsReceipt

  """The stock movements the receipt wrote or reversed."""
  movementIds: [ID!]!

  """The quantity still expected on the order after this receipt."""
  outstandingQuantity: Decimal
  userErrors: [UserError!]!
}

"""The outcome of deleting a goods receipt."""
type DeleteGoodsReceiptPayload {
  """The receipt that was removed; null when the removal was refused."""
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of deleting a goods receipt line."""
type DeleteGoodsReceiptLinePayload {
  """The line that was removed; null when the removal was refused."""
  id: ID
  userErrors: [UserError!]!
}

"""Where a right is in its life."""
enum EntitlementStatus {
  PENDING
  ACTIVE
  SUSPENDED
  EXPIRED
  REVOKED
}

"""
What a purchase granted, which decides what the quantity column counts.
"""
enum EntitlementKind {
  LICENCE
  SEAT
  TERM
  USAGE
}

"""What a device, instance or named user is doing with a slot."""
enum EntitlementActivationStatus {
  ACTIVE
  RELEASED
  REVOKED
  EXPIRED
}

"""What an issued credential may still be used for."""
enum EntitlementKeyStatus {
  ISSUED
  ACTIVATED
  REVOKED
  EXPIRED
}

"""Which generator rendered a licence key."""
enum LicenceKeyFormat {
  UUID

  """Four groups of four upper-case alphanumerics."""
  GROUPED_16

  """Twenty characters of an unambiguous alphabet, grouped in fours."""
  BASE32_20
}

"""The stable code the check answers with."""
enum EntitlementCheckReason {
  ALLOWED
  ENTITLEMENT_NOT_FOUND
  ENTITLEMENT_PENDING
  ENTITLEMENT_SUSPENDED
  ENTITLEMENT_EXPIRED
  ENTITLEMENT_REVOKED
  ENTITLEMENT_TERM_NOT_STARTED
  ENTITLEMENT_QUANTITY_EXHAUSTED
  ENTITLEMENT_ACTIVATION_LIMIT_REACHED
  ENTITLEMENT_REFERENCE_REQUIRED
  ENTITLEMENT_KEY_NOT_FOUND
  ENTITLEMENT_KEY_REVOKED
  ENTITLEMENT_KEY_EXPIRED
  ENTITLEMENT_KEY_USED
  ENTITLEMENT_CONDITIONS_NOT_MET
}

"""The right a purchase granted."""
type Entitlement {
  id: ID!

  """The party the right was granted to."""
  customerId: ID

  """The order that granted it."""
  orderId: ID

  """The line that granted it."""
  orderLineId: ID

  """The subscription that renews it, when one does."""
  subscriptionId: ID

  """The catalogue item it is over, by identifier."""
  productId: ID

  """The variant it is over, when it is over one."""
  variantId: ID

  """The product it is over, resolved through the catalogue capability."""
  product: Product

  """The variant it is over, resolved through the catalogue capability."""
  variant: ProductVariant

  """The allocated number, unique inside the organization."""
  number: String!
  kind: EntitlementKind!

  """Seats, uses or 1; 0 means unlimited."""
  quantity: Int!
  startsAt: DateTime!

  """
  The instant the right stops being exercisable; null is the perpetual case.
  """
  endsAt: DateTime

  """
  Days after endsAt during which the right stays in force while a renewal is chased.
  """
  gracePeriodDays: Int!

  """Maximum simultaneous activations, when that is tighter than quantity."""
  activationLimit: Int

  """Live activations, as the cache the usage audit re-derives."""
  activationCount: Int!
  status: EntitlementStatus!
  revokedAt: DateTime
  revokedByUserId: ID
  revokedReason: String
  suspendedReason: String
  metadata: JSON

  """
  The point past which the right is expired, grace included; null when it is perpetual.
  """
  validUntil: DateTime

  """Seats or uses still available; null when the right is unlimited."""
  remainingQuantity: Int

  """The devices, instances and named seats occupying its slots."""
  activations: [EntitlementActivation!]!

  """The credentials issued against it."""
  keys: [EntitlementKey!]!

  """
  The revision of the row. A mutation states the revision it read as the input member 'version'
  and is refused when the right has moved on since, so two callers editing one right cannot
  silently overwrite each other.
  """
  version: Int!
  createdAt: DateTime
  updatedAt: DateTime
}

"""One device, instance or named user occupying a slot of a right."""
type EntitlementActivation {
  id: ID!
  entitlementId: ID!

  """The key that was used, when activation went through a licence key."""
  entitlementKeyId: ID

  """The stable device or instance identifier the limit is counted over."""
  deviceId: String!
  deviceName: String
  fingerprint: String

  """The named seat this activation occupies."""
  seatReference: String

  """
  The buyer who performed the activation, when it came from a logged-in customer.
  """
  activatedByCustomerId: ID
  status: EntitlementActivationStatus!
  activatedAt: DateTime!

  """Refreshed by validation calls, at most once per configured interval."""
  lastSeenAt: DateTime
  deactivatedAt: DateTime
  revokedAt: DateTime
  revokedByUserId: ID
  revocationReason: String
  ipAddress: String
  userAgent: String
  metadata: JSON
}

"""
An issued licence key: the credential a customer types into the product.
"""
type EntitlementKey {
  id: ID!
  entitlementId: ID!

  """The leading characters, in clear, so support can identify a key."""
  keyPrefix: String
  format: String!
  status: EntitlementKeyStatus!
  assignedAt: DateTime

  """The recipient of the key, when it was delivered to someone."""
  assignedToEmail: String
  assignedToCustomerId: ID

  """Per-key override of the entitlement's activation limit; null inherits."""
  activationLimit: Int

  """Live activations of this key, as the cache the usage audit re-derives."""
  activationCount: Int!

  """The key's own expiry, which may be earlier than the entitlement's."""
  expiresAt: DateTime
  revokedAt: DateTime
  revokedByUserId: ID
  metadata: JSON
  createdAt: DateTime
}

"""One page of rights."""
type EntitlementConnection {
  edges: [EntitlementEdge!]!
  nodes: [Entitlement!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One right inside a page."""
type EntitlementEdge {
  cursor: String!
  node: Entitlement!
}

"""One page of activations."""
type EntitlementActivationConnection {
  edges: [EntitlementActivationEdge!]!
  nodes: [EntitlementActivation!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One activation inside a page."""
type EntitlementActivationEdge {
  cursor: String!
  node: EntitlementActivation!
}

"""One page of licence keys."""
type EntitlementKeyConnection {
  edges: [EntitlementKeyEdge!]!
  nodes: [EntitlementKey!]!
  pageInfo: PageInfo!
  totalCount: Int!
}

"""One licence key inside a page."""
type EntitlementKeyEdge {
  cursor: String!
  node: EntitlementKey!
}

"""Filters a page of rights."""
input EntitlementFilter {
  status: EntitlementStatus
  kind: EntitlementKind
  customerId: ID
  orderId: ID
  orderLineId: ID
  subscriptionId: ID
  productId: ID
  variantId: ID
  number: String
}

"""Filters a page of activations."""
input EntitlementActivationFilter {
  entitlementId: ID
  status: EntitlementActivationStatus
  deviceId: String
  activatedByCustomerId: ID
}

"""Filters a page of licence keys."""
input EntitlementKeyFilter {
  entitlementId: ID
  status: EntitlementKeyStatus
  assignedToEmail: String
  assignedToCustomerId: ID
}

"""One condition attached to a right, stored as a rule row."""
input EntitlementConditionInput {
  """Dotted path into the evaluation context, for example context.region."""
  attribute: String!

  """The comparison, for example EQ, IN, GTE or IS_NULL."""
  operator: String!

  """The operand; an array for IN and BETWEEN."""
  value: JSON

  """How the operand is coerced: STRING, NUMBER, DATE, ..."""
  valueType: String

  """Wraps the rule in NOT after the operator is applied."""
  isNegated: Boolean

  """Rules sharing a group are AND-ed; groups are OR-ed."""
  groupIndex: Int

  """Evaluation order inside a group."""
  priority: Int
  description: String
}

"""The request that grants a right."""
input GrantEntitlementInput {
  customerId: ID
  orderId: ID
  orderLineId: ID
  subscriptionId: ID
  productId: ID
  variantId: ID
  kind: EntitlementKind
  quantity: Int
  startsAt: DateTime
  endsAt: DateTime
  gracePeriodDays: Int
  activationLimit: Int
  metadata: JSON
  conditions: [EntitlementConditionInput!]

  """
  Issue a licence key in the same transaction and return its plaintext once.
  """
  issueKey: Boolean
  keyFormat: LicenceKeyFormat

  """The holder an issued key is assigned to."""
  assignedToEmail: String

  """Grant the right already in force instead of PENDING."""
  activateImmediately: Boolean

  """
  The key this grant is retried under, so a client whose response was lost re-states the same
  key rather than granting a second right. Honoured by the surfaces that declare a retry scope;
  a grant that names no key behaves exactly as it always did.
  """
  idempotencyKey: String
}

"""The request that occupies a slot."""
input ActivateEntitlementInput {
  entitlementId: ID!

  """The stable device or instance identifier the limit is counted over."""
  deviceId: String!
  deviceName: String
  fingerprint: String
  seatReference: String

  """The licence key presented, when activation goes through one."""
  key: String
  activatedByCustomerId: ID
  metadata: JSON

  """
  The key this activation is retried under, so a device that lost the response re-states the same
  key rather than taking a second slot. Honoured by the surfaces that declare a retry scope; an
  activation that states no key behaves exactly as it always did.
  """
  idempotencyKey: String
}

"""The request that asks whether a right may be exercised."""
input CheckEntitlementInput {
  entitlementId: ID

  """The licence key the caller holds, in clear."""
  key: String
  deviceId: String
  seatReference: String

  """Attributes the attached rule rows are evaluated against."""
  context: JSON
}

"""The request that issues a licence key."""
input IssueEntitlementKeyInput {
  entitlementId: ID!
  format: LicenceKeyFormat
  assignedToEmail: String
  assignedToCustomerId: ID
  activationLimit: Int
  expiresAt: DateTime

  """Store a recoverable ciphertext so an operator can re-display the key."""
  storeKey: Boolean
  metadata: JSON

  """
  The key this issuance is retried under, so a client that lost the response re-states the same
  key rather than issuing a second credential. Honoured by the surfaces that declare a retry
  scope; an issuance that states no key behaves exactly as it always did.
  """
  idempotencyKey: String
}

"""
The fields an edit to a right may change.

The route this mirrors validates `UpdateEntitlementDTO`, whose live metadata also carries the
provenance, the allocated number, the kind and the lifecycle state. Those are not members of this
input: a right's state moves by being suspended, extended, reduced or revoked, each of which is an
action with its own permission and its own event, and a body that could set `status` would move the
row past every one of them. The suite derives the omitted set from that DTO's class-validator
metadata rather than restating it here.
"""
input UpdateEntitlementInput {
  """Seats, uses or 1; 0 means unlimited."""
  quantity: Int
  startsAt: DateTime

  """
  The instant the right stops being exercisable; null is the perpetual case.
  """
  endsAt: DateTime

  """
  Days after endsAt during which the right stays in force while a renewal is chased.
  """
  gracePeriodDays: Int

  """Maximum simultaneous activations, when that is tighter than quantity."""
  activationLimit: Int
  metadata: JSON

  """
  The conditions the right should end up with, replacing the rule rows attached to it.
  """
  conditions: [EntitlementConditionInput!]
}

"""The request that records who holds an issued licence key."""
input AssignEntitlementKeyInput {
  """
  The recipient. A key that already names a different holder is refused, not reassigned.
  """
  assignedToEmail: String
}

"""
The request that replaces an issued licence key with a freshly generated one.
"""
input ReissueEntitlementKeyInput {
  """
  Which generator renders the replacement; absent keeps the replaced key's format.
  """
  format: LicenceKeyFormat

  """
  Why the key is being replaced, kept on the old row and on its replacement.
  """
  reason: String

  """
  Store a recoverable ciphertext for the replacement, so an operator can re-display it.
  """
  storeKey: Boolean
}

"""
The recorded fields of an activation, as an edit may state them.

This is the route's own body type (`UpdateEntitlementActivationDTO`) member for member: the descriptive
fields of a slot, and nothing its lifecycle owns. `status`, `entitlementId`, `entitlementKeyId`,
`deviceId` and `revocationReason` are absent from both, because each is written by the operation that
takes, releases or revokes a slot, and each is what a rule of that operation is decided over — the
activation limit, the right's ceiling, the key's release on revocation, the device the seat is counted
over and the bar on a device withdrawn for abuse. A slot is taken through `activateEntitlement` and
given back through `deactivateEntitlement`.
"""
input UpdateEntitlementActivationInput {
  deviceName: String
  fingerprint: String

  """The named seat this activation occupies."""
  seatReference: String

  """
  The buyer who performed the activation, when it came from a logged-in customer.
  """
  activatedByCustomerId: ID

  """Refreshed by validation calls, at most once per configured interval."""
  lastSeenAt: DateTime
  ipAddress: String
  userAgent: String
  metadata: JSON
}

"""What a check answered."""
type EntitlementCheckResult {
  allowed: Boolean!
  reason: EntitlementCheckReason!
  entitlementId: ID
  kind: EntitlementKind
  status: EntitlementStatus

  """Seats or uses still available; null when the right is unlimited."""
  remainingQuantity: Int

  """
  The instant the right stops being exercisable; null is the perpetual case.
  """
  validUntil: DateTime

  """Whether the rule rows attached to the right matched."""
  conditionsMatched: Boolean!

  """The rules that did not match, so a denial is explainable."""
  failedRules: [String!]

  """Attributes the evaluation context did not carry."""
  unresolvedAttributes: [String!]
}

"""The outcome of a grant."""
type GrantEntitlementPayload {
  entitlement: Entitlement

  """The credential that was issued, when one was."""
  key: EntitlementKey

  """
  The plaintext of that credential. Returned once, in this response, and never again: only its
  digest is stored.
  """
  plaintextKey: String

  """
  True when this call granted the right, false when it returned one an earlier call granted.
  """
  created: Boolean!
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a right."""
type EntitlementPayload {
  entitlement: Entitlement
  userErrors: [UserError!]!
}

"""The outcome of occupying a slot."""
type ActivateEntitlementPayload {
  activation: EntitlementActivation
  entitlement: Entitlement

  """
  True when this call took the slot, false when it returned a slot already held.
  """
  created: Boolean!
  userErrors: [UserError!]!
}

"""The outcome of giving a slot back."""
type DeactivateEntitlementPayload {
  activation: EntitlementActivation
  entitlement: Entitlement
  userErrors: [UserError!]!
}

"""
The outcome of a mutation on an activation, for the two acts that are not a release or a
revocation. Declared here because the activation resource had no payload of its own: its other
mutations answer `ActivateEntitlementPayload` or `DeactivateEntitlementPayload`, and neither
states the act a caller performs when it retires a slot recoverably.
"""
type EntitlementActivationPayload {
  activation: EntitlementActivation
  userErrors: [UserError!]!
}

"""The outcome of a mutation on a credential."""
type EntitlementKeyPayload {
  key: EntitlementKey
  userErrors: [UserError!]!
}

"""The outcome of issuing a credential."""
type IssueEntitlementKeyPayload {
  key: EntitlementKey

  """The plaintext. Returned once, in this response, and never again."""
  plaintextKey: String
  userErrors: [UserError!]!
}

"""The outcome of replacing a credential with a freshly generated one."""
type ReissueEntitlementKeyPayload {
  """The replacement."""
  key: EntitlementKey

  """
  The plaintext of the replacement. Returned once, in this response, and never again.
  """
  plaintextKey: String

  """
  The credential it replaced, now revoked, with the activations it was used for released.
  """
  replacedKey: EntitlementKey
  userErrors: [UserError!]!
}

"""
Where a seller account stands. `APPROVED` means every required verification passed; `ACTIVE` means an operator then let it trade.
"""
enum SellerStatus {
  """Created, not yet submitted for review."""
  DRAFT

  """Submitted and waiting to be claimed by a reviewer."""
  SUBMITTED

  """Being reviewed."""
  IN_REVIEW

  """A fixable verification failure; the applicant may resubmit."""
  ACTION_REQUIRED

  """Refused; terminal."""
  REJECTED

  """Every required verification passed; not yet trading."""
  APPROVED

  """Trading and payable."""
  ACTIVE

  """Stopped from doing anything new; balances are held."""
  SUSPENDED

  """Being closed down; only the final payout may move."""
  OFFBOARDING

  """Closed; terminal."""
  OFFBOARDED
}

"""Which verification a recorded result belongs to."""
enum SellerVerificationKind {
  """The seller's legal identity as a business."""
  BUSINESS_IDENTITY

  """The seller's tax registration."""
  TAX_IDENTIFIER

  """The account payouts are sent to."""
  PAYOUT_ACCOUNT
}

"""
The state of one verification kind. A seller carries three independent verdicts, performed by different parties at different times.
"""
enum SellerVerificationStatus {
  """Never checked."""
  UNVERIFIED

  """Sent to a provider or a reviewer; no verdict yet."""
  PENDING

  """A fixable failure: the applicant can correct it and resubmit."""
  ACTION_REQUIRED

  """Passed."""
  VERIFIED

  """A failure that is not fixable by resubmission."""
  FAILED

  """Was verified and the validity window has passed."""
  EXPIRED
}

"""
What a commission rate multiplies. The basis decides the amount, and the amount decides the commission.
"""
enum CommissionBasis {
  """Quantity multiplied by unit price, before any discount."""
  ITEM_SUBTOTAL

  """The same, after the seller's own discounts."""
  DISCOUNTED_SUBTOTAL

  """The tax-inclusive amount."""
  INCLUDING_TAX

  """A flat fee per item; no rate is involved."""
  FIXED_PER_ITEM

  """The tier containing the amount sets the rate for the whole amount."""
  TIERED_AMOUNT

  """The tier containing the quantity sets the rate for the whole line."""
  TIERED_QUANTITY
}

"""Whether an offering is sellable, and why not."""
enum OfferingStatus {
  """Being authored."""
  DRAFT

  """Waiting for moderation."""
  PENDING_REVIEW

  """Sellable, subject to the publication rules."""
  ACTIVE

  """Not sellable; resumable by the seller."""
  PAUSED

  """Refused by a moderator."""
  REJECTED

  """Withdrawn; terminal."""
  WITHDRAWN
}

"""
The condition of the goods offered: the same variant may be offered new by one seller and refurbished by another.
"""
enum OfferingCondition {
  NEW
  REFURBISHED
  USED_LIKE_NEW
  USED_GOOD
  USED_ACCEPTABLE
}

"""
Who physically fulfils a seller's line, which is also what decides whose stock may be allocated.
"""
enum OfferingFulfilmentMode {
  """The platform ships from its own locations."""
  PLATFORM

  """The seller ships from its own location."""
  SELLER

  """The seller's supplier ships; no allocation against a stock level."""
  DROPSHIP
}

"""
What one item of an offering batch does. The values are the resource's own vocabulary, because none of the four is one of the platform's write kinds: no operation creates a row and none removes one.
"""
enum SellerOfferingBulkOperation {
  """
  Moves the offering to `ACTIVE`, checking every publication clause first.
  """
  PUBLISH

  """
  Moves the offering to `PAUSED`, so it stops being sellable and stays resumable.
  """
  PAUSE

  """Moves the offering to `WITHDRAWN`; the row is kept."""
  WITHDRAW

  """
  Rewrites the offering's price, and the commission it is sold under when the item states one.
  """
  REPRICE
}

"""
How a batch writes the offering an item names. The values are the platform bulk contract's own wire values, copied un-re-cased, so one batch statement reads identically over either protocol.
"""
enum SellerOfferingBulkMode {
  """Merge into the offering the item names; the platform's default."""
  upsert

  """Overwrite the addressed offering rather than merging into it."""
  replace
}

"""Who collects and remits the tax on a seller's lines."""
enum TaxCollectionMode {
  """The seller remits on its own registration."""
  SELLER_REMITS

  """
  The sale is the seller's; the marketplace collects and remits on its behalf.
  """
  MARKETPLACE_COLLECTS_AND_REMITS

  """The platform is the supplier of record for the jurisdiction."""
  DEEMED_SUPPLIER
}

"""The tax registration vocabulary."""
enum TaxRegistrationScheme {
  NONE
  VAT
  GST
  SALES_TAX
  ABN
  TAX_ID
}

"""
How a seller's money reaches the seller. In both modes the seller's share is credited to the seller's own provider account at capture.
"""
enum SellerPayoutMode {
  """
  The provider split the charge at capture; the platform instructs no transfer.
  """
  PROVIDER_SPLIT

  """
  The provider holds the seller's balance; the platform instructs a transfer to the seller's bank.
  """
  PROVIDER_TRANSFER
}

"""When a payout run may create a payout for a seller."""
enum SellerPayoutSchedule {
  """Only an explicit request creates a payout."""
  MANUAL
  DAILY
  WEEKLY
  BI_WEEKLY
  SEMI_MONTHLY
  MONTHLY

  """
  A payout is created once the settleable balance reaches the seller's threshold.
  """
  THRESHOLD
}

"""Where a payout instruction stands."""
enum SellerPayoutStatus {
  """Built and not yet submitted for approval."""
  DRAFT

  """Waiting for approval."""
  PENDING

  """Approved; the transfer has not been attempted."""
  APPROVED

  """The provider call is in flight."""
  PROCESSING

  """The provider reports the transfer executed; terminal."""
  PAID

  """The provider refused the transfer; retryable."""
  FAILED

  """
  Cancelled before payment; its transactions return to settleable. Terminal.
  """
  CANCELED
}

"""Where a settlement report stands."""
enum SellerSettlementStatus {
  """Accepting the platform's lines for the period."""
  OPEN

  """Compared against the provider's report."""
  RECONCILED

  """Final; no further lines are accepted."""
  CLOSED

  """
  The provider's figures and the platform's ledger disagree and the difference is unresolved.
  """
  DISPUTED
}

"""What a seller ledger row records."""
enum SellerTransactionKind {
  """A seller-owned order line was sold."""
  SALE

  """Shipping revenue attributed to the seller."""
  SHIPPING

  """
  A carrier cost the platform paid on the seller's behalf; a negative row.
  """
  SHIPPING_FEE

  """A reversal of money returned to the buyer."""
  REFUND

  """A reversal of a charged-back sale."""
  CHARGEBACK

  """A fee deducted from the seller's balance."""
  FEE

  """A manual adjustment, always with a reason."""
  ADJUSTMENT
}

"""
The lifecycle of a seller ledger row. The status is the only mutable thing about the row; its amounts are append only.
"""
enum SellerTransactionStatus {
  """Written at placement, before the money is captured."""
  PENDING

  """Captured and past the hold window; eligible for a payout."""
  SETTLEABLE

  """Deliberately kept out of payouts, with a reason."""
  HELD

  """Included in a payout."""
  SETTLED

  """The payout covering it was paid."""
  PAID

  """Reversed in full by a later row."""
  REVERSED
}

"""Why a ledger row is held out of payouts."""
enum SellerHoldReason {
  SELLER_SUSPENDED
  CHARGEBACK
  VERIFICATION_EXPIRED
  DISPUTE
  MANUAL
}

"""
A seller: a party plus the commercial relationship that makes it a marketplace participant.
"""
type Seller {
  id: ID!

  """
  Stable, human-usable key, unique per organization and immutable after creation.
  """
  code: String!

  """Trading name."""
  name: String!

  """
  Registered name, retained through offboarding, because a ledger row resolves to a named legal entity.
  """
  legalName: String

  """
  Snapshot of the party's reachability, so a notification path survives an edit to the contact row.
  """
  email: String
  phone: String

  """
  The seller's party row: the platform organization contact the seller trades as.
  """
  contactId: ID
  status: SellerStatus!

  """Business-identity verification."""
  businessVerificationStatus: SellerVerificationStatus!

  """Tax-identifier verification."""
  taxVerificationStatus: SellerVerificationStatus!

  """Payout-account verification; a payout requires this to be `VERIFIED`."""
  payoutAccountStatus: SellerVerificationStatus!

  """
  Verification expires: an expired payout account holds payouts, an expired identity suspends the seller.
  """
  verificationExpiresAt: DateTime

  """Jurisdiction of registration, ISO 3166-1 alpha-2."""
  taxCountryCode: String

  """Who collects and remits the tax on this seller's lines."""
  taxCollectionMode: TaxCollectionMode!

  """
  The seller's own commission rate, as a fraction: `0.15` is fifteen per cent. Null inherits the platform default.
  """
  defaultCommissionRate: Decimal
  commissionBasis: CommissionBasis
  payoutMode: SellerPayoutMode!
  payoutSchedule: SellerPayoutSchedule!

  """Null means the seller is paid per order currency."""
  payoutCurrency: String

  """
  The minimum balance a payout run will pay out; a balance below it carries forward.
  """
  payoutThreshold: Decimal!

  """
  Fraction of the settleable balance withheld at each run. A policy applied at run time, never a stored balance.
  """
  reservePercent: Decimal!

  """
  Delays a ledger row's inclusion in any payout, so a chargeback window passes before money moves.
  """
  payoutHoldDays: Int!

  """
  Set on every entry to `ACTIVE`, including a reinstatement after suspension.
  """
  activatedAt: DateTime
  suspendedAt: DateTime

  """
  Present when the status is `SUSPENDED`: a suspension a seller cannot read the reason for is one it cannot remedy.
  """
  suspensionReason: String
}

"""
What a hard deletion reports.

The REST route answers with the ORM's own `DeleteResult`; this is that result's one actionable member
rather than the whole of it. `raw` is deliberately not projected: it is the driver's payload rather
than the platform's answer, and parity here is capability parity rather than shape parity.
"""
type SellerDeleteResult {
  """
  How many rows the deletion removed: one, or none when the id matched nothing.
  """
  affected: Int!
}

"""
What a caller supplies to open a seller account.

The members are the writable half of the REST body and nothing else. `organizationId`, `tenantId` and
the organization object are absent because they are the request's rather than the body's: the service
takes them from the caller's context and overwrites whatever a body states, so a member declared here
would be one the document advertised and the platform discarded.
"""
input CreateSellerInput {
  """
  Required: a seller without a code cannot be referred to by a ledger row or a statement.
  """
  code: String!

  """
  Required: a seller without a party cannot be verified, contracted with or taxed.
  """
  contactId: ID!
  name: String
  legalName: String
  email: String
  phone: String
  merchantId: ID
  userId: ID
  channelIds: [String!]
  regionIds: [String!]
  payoutAccountReference: String
  payoutAccountHolderId: ID
  taxId: String
  vatNumber: String
  taxCountryCode: String
  taxRegistrationScheme: TaxRegistrationScheme
  taxCollectionMode: TaxCollectionMode

  """A fraction, not a percentage: 0.15 is fifteen percent."""
  defaultCommissionRate: Decimal
  commissionBasis: CommissionBasis
  commissionTiers: [CommissionTierInput!]
  fixedFeePerItem: Decimal
  fixedFeeCurrency: String
  commissionOnShipping: Boolean
  chargeShippingCost: Boolean
  allowNegativeNet: Boolean
  payoutMode: SellerPayoutMode
  payoutSchedule: SellerPayoutSchedule
  payoutCurrency: String
  payoutThreshold: Decimal
  reservePercent: Decimal
  reserveHoldDays: Int
  payoutHoldDays: Int
  externalId: String
  metadata: JSON
}

"""
What a caller supplies to amend a seller account.

The party binding and the code are absent because they are immutable on the REST body too: a seller
belongs to one organization and one contact forever, and its code is the stable key a ledger row and a
statement are read by.
"""
input UpdateSellerInput {
  name: String
  legalName: String
  email: String
  phone: String
  merchantId: ID
  userId: ID
  channelIds: [String!]
  regionIds: [String!]
  payoutAccountReference: String
  payoutAccountHolderId: ID
  taxId: String
  vatNumber: String
  taxCountryCode: String
  taxRegistrationScheme: TaxRegistrationScheme
  taxCollectionMode: TaxCollectionMode
  defaultCommissionRate: Decimal
  commissionBasis: CommissionBasis
  commissionTiers: [CommissionTierInput!]
  fixedFeePerItem: Decimal
  fixedFeeCurrency: String
  commissionOnShipping: Boolean
  chargeShippingCost: Boolean
  allowNegativeNet: Boolean
  payoutMode: SellerPayoutMode
  payoutSchedule: SellerPayoutSchedule
  payoutCurrency: String
  payoutThreshold: Decimal
  reservePercent: Decimal
  reserveHoldDays: Int
  payoutHoldDays: Int
  externalId: String
  metadata: JSON
}

"""
What a caller supplies to record one verification kind's result, as the verifier reported it.
"""
input VerifySellerInput {
  kind: SellerVerificationKind!
  status: SellerVerificationStatus!

  """The evidence the verdict rests on: a provider's reference for it."""
  reference: String

  """Who performed the verification."""
  provider: String

  """
  When the verdict stops being good; a verdict with no window is one a statement cannot age.
  """
  expiresAt: DateTime
  note: String
}

"""
What a caller supplies to offer a variant.

`sellerId` and `variantId` are required on both surfaces: `CreateSellerOfferingDTO` intersects the offering's own shape with a pick of those two, and the service refuses a body that names neither — an offering that does not say what is offered by whom is not an offering. `productId` is deliberately absent, as it is from the REST body: it is derived from the variant, so a caller cannot make an unpublished variant visible by asserting one.
"""
input CreateSellerOfferingInput {
  """Required: the seller whose right to sell this records."""
  sellerId: ID!

  """Required: the catalogue variant offered."""
  variantId: ID!

  """The seller's own SKU for this listing, unique per seller when set."""
  sellerSku: String

  """The seller's own listing title; null uses the catalogue title."""
  title: String
  condition: OfferingCondition

  """
  The seller's authored price, materialised into a price row when the offering is published.
  """
  priceAmount: Decimal
  priceCurrency: String
  productPriceId: ID

  """
  Commission override for this offering; null inherits the seller's default, which inherits the platform's.
  """
  commissionRate: Decimal
  commissionBasis: CommissionBasis
  commissionTiers: [CommissionTierInput!]
  status: OfferingStatus

  """
  Channels this offering is published to; null inherits the seller's set.
  """
  channelIds: [String!]

  """Regions this offering is published to; null inherits the seller's set."""
  regionIds: [String!]

  """Availability window start; null is open."""
  availableFrom: DateTime

  """Availability window end; null is open."""
  availableTo: DateTime
  maxQuantityPerOrder: Int
  fulfilmentMode: OfferingFulfilmentMode
  fulfilmentWarehouseId: ID
  handlingDays: Int
  isFeatured: Boolean
  allowNegativeNet: Boolean
  externalId: String
  metadata: JSON
}

"""
What a caller supplies to amend an offering.

The subject is absent because it is immutable on the REST body too: `UpdateSellerOfferingDTO` omits `sellerId` and `variantId`, and the service deletes both from any body that carries them — an offering that changed variant would silently rewrite what past orders were priced against.
"""
input UpdateSellerOfferingInput {
  sellerSku: String
  title: String
  condition: OfferingCondition
  priceAmount: Decimal
  priceCurrency: String
  productPriceId: ID
  commissionRate: Decimal
  commissionBasis: CommissionBasis
  commissionTiers: [CommissionTierInput!]
  status: OfferingStatus
  channelIds: [String!]
  regionIds: [String!]
  availableFrom: DateTime
  availableTo: DateTime
  maxQuantityPerOrder: Int
  fulfilmentMode: OfferingFulfilmentMode
  fulfilmentWarehouseId: ID
  handlingDays: Int
  isFeatured: Boolean
  allowNegativeNet: Boolean
  externalId: String
  metadata: JSON
}

"""
What a caller supplies to amend a payout: what the payout states about itself, and nothing its lifecycle owns.

The amounts are absent, and that is the point: a payout's amount is the sum of the transactions it covers, so a caller chooses which rows are paid and never how much. `status` is absent because the lifecycle moves through the approve, pay, cancel and retry fields — each under its own grant — rather than through this input; `feeAmount` because it is what the provider reported when the payout was paid; `transactionIds` because the lines are what the payout was built from; and `payoutMode`, `sellerId` and `currency` because each is fixed once the payout exists. `UpdateSellerPayoutDTO` omits the same members, and the service refuses them whichever way they arrive.
"""
input UpdateSellerPayoutInput {
  periodStart: DateTime
  periodEnd: DateTime
  scheduledAt: DateTime
  providerKey: String
  providerReference: String
  note: String
  metadata: JSON
}

"""
What a caller supplies to run the payout pass.

Every member is optional, as it is on the route's own body: a run that states no period reads the schedule and the ledger as they stand. `dryRun` decides whether the pass reports or pays, and it reaches the service coerced to a boolean rather than as it arrived, exactly as the route coerces it.
"""
input RunSellerPayoutInput {
  """
  The first day of the period; absent reads from the beginning of the ledger.
  """
  periodStart: DateTime

  """The last day of the period; absent reads to now."""
  periodEnd: DateTime

  """
  The sellers to consider; absent considers every seller whose schedule is due.
  """
  sellerIds: [String!]
  currency: String

  """Report what the run would do without creating a payout."""
  dryRun: Boolean
}

"""
What a caller supplies to amend a settlement.

The provider, the seller and the currency are absent because `UpdateSellerSettlementDTO` omits them: a settlement transcribes what one provider reported about one seller in one currency, and none of the three can change after the fact. `status` is absent because a settlement is reconciled, closed and disputed through its own fields, each with its own checks. The figures are absent because they are the provider's report as recorded, with the net derived from them rather than stated beside them, and `discrepancyAmount` is what the reconciliation computes — a caller that could set it could silence the one number the report exists to surface. The service refuses the same members whichever way they arrive.
"""
input UpdateSellerSettlementInput {
  payoutAccountHolderId: ID
  periodStart: DateTime
  periodEnd: DateTime
  providerReportId: String
  externalReference: String
  note: String
  metadata: JSON
}

"""
What a caller supplies to reconcile a settlement.

Two members and nothing else, because they are what the route's body carries: the provider's own report identifier, recorded beside the comparison so a later reader can find the document the figures came from, and a note, which the comparison records when it finds a discrepancy.
"""
input ReconcileSellerSettlementInput {
  providerReportId: String
  note: String
}

"""
A seller's right to sell one product variant, at the seller's price and under the seller's own SKU.
"""
type SellerOffering {
  id: ID!
  sellerId: ID!

  """
  The catalogue variant: the saleable unit, defined once for the whole platform.
  """
  variantId: ID!

  """
  Derived from the variant and carried so an offering report needs no join.
  """
  productId: ID

  """The seller's own SKU for this listing, unique per seller when set."""
  sellerSku: String

  """The seller's own listing title; null uses the catalogue title."""
  title: String
  condition: OfferingCondition!

  """
  The seller's authored price, materialised into a price row when the offering is published.
  """
  priceAmount: Decimal
  priceCurrency: String

  """
  Commission override for this offering; null inherits the seller's default, which inherits the platform's.
  """
  commissionRate: Decimal
  status: OfferingStatus!

  """
  Channels this offering is published to; null inherits the seller's set.
  """
  channelIds: [String!]

  """Availability window start; null is open. The window is half open."""
  availableFrom: DateTime
  availableTo: DateTime
  fulfilmentMode: OfferingFulfilmentMode!

  """
  Orders competing offers of the same variant in a listing. Merchandising only.
  """
  isFeatured: Boolean!
}

"""
One band of a graduated commission schedule: `from <= x < to`, with `to` null meaning open ended. The bounds are in major units for an amount basis and in units for a quantity basis.
"""
input CommissionTierInput {
  """Inclusive lower bound."""
  from: Float!

  """Exclusive upper bound; null is open ended."""
  to: Float

  """
  Rate applied to the whole amount or quantity when the value falls in this band. An exact decimal, never a float.
  """
  rate: Decimal!
}

"""
One item of an offering batch: the operation it performs and the offering it performs it on.

The members are the members the delivered single-item routes accept, so an item states exactly what the equivalent call states and nothing beside it. A member that is absent is a member the item says nothing about, which is a different request from a member stated as empty — a re-price of the amount alone leaves the currency, the window and the channel set as the offering holds them.

Each member belongs to the operation that reads it: `channelIds` narrows what a `PUBLISH` publishes to, the two price members and the three commission members are what a `REPRICE` writes, and a `PAUSE` and a `WITHDRAW` write the status alone.
"""
input SellerOfferingBulkItem {
  """
  The offering the item acts on. Required: no operation of this batch creates or guesses a row.
  """
  id: ID!

  """
  The operation this item performs. Required: an item that states none is refused.
  """
  operation: SellerOfferingBulkOperation!

  """The price the offering is to carry; read by a re-price."""
  priceAmount: Decimal

  """The currency of that price; read by a re-price."""
  priceCurrency: String

  """
  The commission rate the offering overrides the seller's default with; read by a re-price.
  """
  commissionRate: Decimal

  """What that rate multiplies; read by a re-price."""
  commissionBasis: CommissionBasis

  """
  The graduated schedule that replaces the flat rate; read by a re-price.
  """
  commissionTiers: [CommissionTierInput!]

  """
  The channel subset to publish to; null inherits the seller's set. Read by a publish.
  """
  channelIds: [String!]
}

"""
A page of listings to move in one request.

`mode` is the platform bulk contract's write mode and `atomic` is its all-or-nothing flag: an atomic batch applies every item or none of them, and a batch that is not atomic applies what it can and reports the rest. This route creates no row, so the mode names how the platform's contract merges rather than a choice the resource offers.

`idempotencyKey` is the retry key of this batch, and it is optional: the route honours a key and does not demand one. A GraphQL request may carry many mutations in one document and a header cannot say which of them a key belongs to, so the key travels here, beside the input it qualifies, and is read by the same kernel that stores and replays the REST surface's keys.
"""
input BulkSellerOfferingsInput {
  """
  The items of the batch, in caller order. The answer refers to them by position.
  """
  items: [SellerOfferingBulkItem!]!

  """How the addressed offering is written. Null is the platform's default."""
  mode: SellerOfferingBulkMode

  """Whether the batch is applied as one write."""
  atomic: Boolean

  """The client's retry key for this batch."""
  idempotencyKey: String
}

"""
One item's outcome: the offering that moved, or the item's own failure.

The index is part of the answer because a batch is answered in one piece — a caller that sent a page of listings reads which of them applied without matching rows back to a request by hand.
"""
type SellerOfferingBulkItemResult {
  """The item's position in the request."""
  index: Int!

  """Whether the item applied."""
  ok: Boolean!

  """The offering that moved, when the item applied."""
  id: ID

  """The resource that moved, so a mixed batch reads unambiguously."""
  resource: String

  """
  Why the item did not apply, with the code the same item would have produced alone. The path names the item it belongs to.
  """
  error: UserError
}

"""
What a batch of offerings adds up to.

`succeeded` and `failed` are the counts of the two kinds of entry in `results`, so a client can assert `succeeded + failed == items.length` against the same answer rather than against a second number that could disagree with it.
"""
type BulkSellerOfferingsPayload {
  """One entry per request item, in request order."""
  results: [SellerOfferingBulkItemResult!]!

  """How many items applied."""
  succeeded: Int!

  """How many items did not."""
  failed: Int!

  """How many items the request carried."""
  total: Int!
}

"""
The split of one order line's money for one seller: the ledger, and the truth about what a seller earned or owes.
"""
type SellerTransaction {
  id: ID!
  sellerId: ID!

  """The order the row splits."""
  orderId: ID!

  """
  The order line the row splits; null for shipping, fee and order-level adjustment rows.
  """
  orderLineId: ID
  kind: SellerTransactionKind!
  status: SellerTransactionStatus!
  currency: String!

  """
  Signed: positive for a sale or a shipping charge, negative on a reversal row.
  """
  grossAmount: Decimal!

  """The line's tax, from the line's own tax rows."""
  taxAmount: Decimal!

  """
  The seller-funded discount only, non-positive on a sale. It reduces the commission basis.
  """
  sellerDiscountAmount: Decimal!

  """
  The platform-funded discount attributed to this row. It reduces neither the seller's net nor the commission basis.
  """
  platformDiscountAmount: Decimal!

  """The basis convention snapshotted at placement."""
  commissionBasis: CommissionBasis!

  """The amount the rate applied to, signed like the row."""
  commissionBasisAmount: Decimal!

  """
  The resolved rate, snapshotted: the rate this seller was actually charged.
  """
  commissionRate: Decimal!
  commissionAmount: Decimal!

  """
  The seller's entitlement for this row: gross plus tax plus the seller discount, less the commission.
  """
  netAmount: Decimal!

  """
  The business moment the row describes: placement for a sale, the refund's own date for a reversal.
  """
  occurredAt: DateTime!

  """
  When the row became eligible for a payout; null until the money was captured and the hold elapsed.
  """
  settleableAt: DateTime

  """
  Present when the status is `HELD`: a held row a seller cannot read the reason for is one it cannot act on.
  """
  holdReason: SellerHoldReason

  """
  The row this one reverses. A reversal is always a new row, never an edit of the row it reverses.
  """
  reversesTransactionId: ID
}

"""
One instruction to move one seller's settleable balance, in one currency, to the seller's bank account.
"""
type SellerPayout {
  id: ID!
  sellerId: ID!

  """
  Human-facing number, drawn from the platform's sequence for payouts and unique per organization.
  """
  number: String!
  status: SellerPayoutStatus!

  """
  Snapshotted from the seller at creation: a seller that changes how it is paid does not change how a built payout is executed.
  """
  payoutMode: SellerPayoutMode!
  currency: String!

  """
  The sum of the payout's lines, and nothing else: the lines are what make the amount derivable rather than asserted.
  """
  netAmount: Decimal!

  """
  The provider's transfer fee, which the seller bears and the platform does not earn.
  """
  feeAmount: Decimal!

  """
  Withheld at this run under the reserve policy; a per-run computation rather than a balance.
  """
  reserveAmount: Decimal!

  """The amount actually instructed to the provider."""
  paidAmount: Decimal!

  """
  The offboarding payout: exempt from the minimum threshold and from the reserve.
  """
  isFinal: Boolean!
  paidAt: DateTime

  """The provider that was instructed."""
  providerKey: String

  """
  The provider's transfer id, unique per provider: what makes a retried execution replay the stored response instead of moving money twice.
  """
  providerTransferId: String

  """
  The ledger rows this payout covers. Loaded by the single-payout read; absent from a list row.
  """
  lines: [SellerPayoutLine!]
}

"""
What one payout run decided for one seller.

A run answers a decision per seller rather than a row: a seller the schedule did not find due, one whose balance was under its threshold and one that was paid are all answers, and `payoutId` and `skippedReason` are the two members that tell them apart. The amounts are the run's own arithmetic — the settleable balance it saw, what the reserve withheld and what was payable after it — so a client reads why a seller was or was not paid without reconstructing the run's policy.
"""
type SellerPayoutRunResult {
  sellerId: ID!
  currency: String!

  """The settleable balance the run saw."""
  balance: Decimal!

  """The amount the reserve policy withheld at this run."""
  reserveAmount: Decimal!

  """What was payable after the reserve and the hold window."""
  payable: Decimal!

  """The payout the run created, absent when it created none."""
  payoutId: ID

  """Why no payout was created, when none was."""
  skippedReason: String
}

"""
The join between a payout and one ledger row it pays. Deliberately thin: the money is already on the transaction.
"""
type SellerPayoutLine {
  id: ID!
  sellerPayoutId: ID!

  """The ledger row being paid."""
  sellerTransactionId: ID!

  """The portion of the transaction's net paid by this payout."""
  amount: Decimal!

  """
  Always the payout's own currency: a payout is built for one seller in one currency.
  """
  currency: String!
}

"""What the payment provider reported it did, recorded as reported."""
type SellerSettlement {
  id: ID!
  sellerId: ID!

  """The provider that reported the settlement."""
  providerKey: String!
  status: SellerSettlementStatus!
  currency: String!

  """The provider's own gross for the period."""
  grossAmount: Decimal!

  """
  The commission withheld at source, snapshotted from the provider's report.
  """
  commissionAmount: Decimal!

  """
  The provider's fee, which is not the platform's commission and is never netted into it.
  """
  feeAmount: Decimal!

  """Gross less the commission and the provider's fee."""
  netAmount: Decimal!

  """
  The platform's lines for the period less the reported net: zero when the two agree. The ledger is never edited to agree with an external report.
  """
  discrepancyAmount: Decimal!
  closedAt: DateTime
}

"""
One platform line of a settlement's period, with the net the ledger carries for it.

The reconciliation answers the lines the comparison was made over rather than only its verdict: a discrepancy a client cannot attribute to a line is one it cannot take to the provider, and the settlement's own `discrepancyAmount` is the difference of the sums rather than any one row's.
"""
type SellerSettlementDifference {
  """The ledger row the platform holds for the period."""
  transactionId: ID!

  """The net that row carries: the platform's side of the comparison."""
  platformNet: Decimal!
}

"""
What a reconciliation found: the settlement it moved and the platform's lines it compared.

Both halves are answered because both are what the route answers. The settlement is the service's own return, so the status a client reads is the one the comparison decided — `RECONCILED` when the figures agree and `DISPUTED` when they do not.
"""
type SellerSettlementReconciliation {
  """The settlement after the comparison."""
  settlement: SellerSettlement!

  """
  The platform's lines for the settlement's period, each with the net the ledger carries.
  """
  differences: [SellerSettlementDifference!]!
}

"""
A seller's balance in one currency. The balance is the ledger: summed from the rows every time it is asked for, never cached.
"""
type SellerBalance {
  currency: String!

  """Rows that are captured and past the hold window."""
  available: Decimal!

  """Rows written but not yet captured."""
  pending: Decimal!

  """Rows deliberately held out of payouts."""
  held: Decimal!

  """
  The negative part of the balance, reported explicitly: a refund after a payout makes the balance negative on purpose.
  """
  negativeCarryForward: Decimal!

  """What the next run would withhold under the reserve policy."""
  reserveNextRun: Decimal!

  """
  The next date the seller's schedule would create a payout; null for a manual or threshold schedule.
  """
  nextPayoutAt: DateTime
}

"""One line of a seller statement."""
type SellerStatementLine {
  """The ledger row the line reports."""
  transactionId: ID!
  kind: SellerTransactionKind!
  status: SellerTransactionStatus!
  occurredAt: DateTime!
  description: String

  """The ledger's own exact decimals, never a rounded rendering of them."""
  grossAmount: Decimal!
  commissionAmount: Decimal!
  netAmount: Decimal!
  currency: String!
}

"""
A seller's statement for a period: what it earned, what it was charged and what it was paid.
"""
type SellerStatement {
  sellerId: ID!
  currency: String!

  """The first day of the period; null is open at the start."""
  from: DateTime

  """The last day of the period; null is open at the end."""
  to: DateTime

  """What the seller was owed before the period began."""
  openingBalance: Decimal!

  """The ledger rows of the period, oldest first."""
  lines: [SellerStatementLine!]!

  """The payouts of the period, oldest first."""
  payouts: [SellerPayout!]!

  """The provider settlements of the period, oldest first."""
  settlements: [SellerSettlement!]!

  """What the seller is owed at the end of the period."""
  closingBalance: Decimal!
  negativeCarryForward: Decimal!
  reserveNextRun: Decimal!
  nextPayoutAt: DateTime
}

"""
The reconciliation of one order's split against the money actually captured.
"""
type SellerSplitReconciliation {
  orderId: ID!
  currency: String!

  """The captured amount of the order."""
  capturedAmount: Decimal!

  """
  The part attributable to content no seller owns, which this ledger does not carry.
  """
  platformOwnCaptured: Decimal!

  """The sum of the sellers' nets."""
  sumNet: Decimal!

  """The sum of the platform's commissions."""
  sumCommission: Decimal!

  """
  The platform's own contribution to seller-owned lines, as a positive figure.
  """
  platformDiscount: Decimal!

  """
  Zero when the seller rows and the platform's commission account for exactly what was captured. A non-zero delta is a defect, not a rounding curiosity.
  """
  splitDelta: Decimal!

  """
  What the platform kept on the seller-owned part; legitimately negative.
  """
  platformRetained: Decimal!
}

"""A page of sellers."""
type SellerConnection {
  nodes: [Seller!]!
  edges: [SellerEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One seller in a page, with the cursor that addresses it."""
type SellerEdge {
  node: Seller!
  cursor: String!
}

"""A page of what sellers offer."""
type SellerOfferingConnection {
  nodes: [SellerOffering!]!
  edges: [SellerOfferingEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One offering in a page, with the cursor that addresses it."""
type SellerOfferingEdge {
  node: SellerOffering!
  cursor: String!
}

"""A page of ledger rows."""
type SellerTransactionConnection {
  nodes: [SellerTransaction!]!
  edges: [SellerTransactionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One ledger row in a page, with the cursor that addresses it."""
type SellerTransactionEdge {
  node: SellerTransaction!
  cursor: String!
}

"""A page of payout instructions."""
type SellerPayoutConnection {
  nodes: [SellerPayout!]!
  edges: [SellerPayoutEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One payout in a page, with the cursor that addresses it."""
type SellerPayoutEdge {
  node: SellerPayout!
  cursor: String!
}

"""A page of the ledger rows one payout covers."""
type SellerPayoutLineConnection {
  nodes: [SellerPayoutLine!]!
  edges: [SellerPayoutLineEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One payout line in a page, with the cursor that addresses it."""
type SellerPayoutLineEdge {
  node: SellerPayoutLine!
  cursor: String!
}

"""A page of settlements."""
type SellerSettlementConnection {
  nodes: [SellerSettlement!]!
  edges: [SellerSettlementEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One settlement in a page, with the cursor that addresses it."""
type SellerSettlementEdge {
  node: SellerSettlement!
  cursor: String!
}

"""How the terms of a query are combined."""
enum SearchMatchMode {
  """Any term may match."""
  ANY

  """Every term must match."""
  ALL

  """The terms are matched as one phrase."""
  PHRASE
}

"""The predicate a filter applies to a declared field."""
enum SearchFilterOperator {
  EQ
  NEQ
  IN
  NIN
  GT
  GTE
  LT
  LTE
  CONTAINS
  STARTS_WITH
  BETWEEN
  EXISTS
}

"""The direction of a sort key."""
enum SearchSortDirection {
  ASC
  DESC
}

"""
The kind of value a declared field holds, which decides how it is matched.
"""
enum SearchFieldKind {
  TEXT
  KEYWORD
  NUMBER
  DATE
  BOOLEAN
  ENTITY
}

"""What a rebuild covers."""
enum SearchReindexScope {
  """Every active definition."""
  ALL

  """One entity type."""
  ENTITY

  """Every entity published to one channel."""
  CHANNEL
}

"""One predicate of a search."""
input SearchFilterInput {
  """The declared field name, or 'entity' to narrow the entity set."""
  attribute: String!
  operator: SearchFilterOperator!

  """
  The value to compare. A scalar for the comparison operators, a list for the set operators, and a
  two-element range for BETWEEN. It is a JSON scalar because the value's own type is the declared
  field's business: a price is compared as a decimal string, a flag as a boolean.
  """
  value: JSON

  """
  The entity the attribute belongs to; required when one request filters across entity types.
  """
  entity: String
}

"""How a result set is ordered."""
input SearchSortInput {
  """'score' orders by relevance; anything else names a declared field."""
  attribute: String!
  direction: SearchSortDirection
}

"""A search request, in the terms every provider implements."""
input SearchInput {
  """The free-text query; empty means the request is filter-only."""
  q: String

  """Entity keys to search; empty means every entity the caller may see."""
  entities: [String!]
  matchMode: SearchMatchMode
  filters: [SearchFilterInput!]

  """
  Declared field names whose values are counted in the response's facets.
  """
  facets: [String!]
  sort: SearchSortInput

  """How many hits to skip."""
  skip: Int

  """How many hits to return."""
  take: Int

  """Restricts the result to one channel's documents."""
  channelId: ID

  """
  Explicit organization scope; the caller's own organization is used when it is absent.
  """
  organizationId: ID
}

"""One hit of a result set."""
type SearchHit {
  """The entity type the hit is an instance of."""
  entity: String!

  """
  The id of the matched row. It is the reader's job to re-read the entity it names.
  """
  entityId: ID!

  """The indexed title, produced by the declaration's title template."""
  title: String!

  """
  Relevance, normalised at index time so it is comparable across entity types.
  """
  score: Float!

  """The matched fragment, when the provider can produce one."""
  highlight: String

  """
  The declared attributes of the matched row, read from the entity itself rather than from the
  index. They are display values a listing renders; anything authoritative is read from the entity.
  """
  attributes: JSON
}

"""One value of a facet and how many hits carry it."""
type SearchFacetValue {
  value: String!
  count: Int!
}

"""
The value counts of one faceted attribute, on the page's own predicate.
"""
type SearchFacet {
  attribute: String!
  values: [SearchFacetValue!]!
}

"""One type-ahead suggestion."""
type SearchSuggestion {
  """The completed text."""
  text: String!

  """The entity type the suggestion belongs to."""
  entity: String!

  """The row the suggestion names."""
  entityId: ID!
}

"""One ranked, faceted page of hits."""
type SearchResult {
  items: [SearchHit!]!

  """
  How many hits matched. It counts what the caller may see: an entity the caller may not read is
  removed from the query rather than filtered out of its answer, so a total never reveals how many
  rows were withheld.
  """
  total: Int!
  facets: [SearchFacet!]!

  """
  Entity types the caller may see, so a client can explain what was searched.
  """
  searchedEntities: [String!]!
  pageInfo: PageInfo!
}

"""How fresh one entity's index is."""
type SearchIndexStatus {
  entity: String!

  """The documents the index holds for the entity."""
  indexedCount: Int!

  """Source rows that moved after the index last did."""
  pendingCount: Int!
  lastIndexedAt: DateTime

  """How long ago the index last moved, in seconds."""
  lagSeconds: Int
}

"""One field of an entity that the index holds."""
type SearchIndexField {
  """
  Field name, unique inside its definition; the name a filter and a facet refer to.
  """
  name: String!
  kind: SearchFieldKind!

  """Ranking multiplier, normalised into the document at index time."""
  weight: Decimal!

  """Whether the field contributes to the free-text match."""
  searchable: Boolean!

  """Whether the field may be used in a filter predicate."""
  filterable: Boolean!

  """Whether the field's values are counted in a facet."""
  facetable: Boolean!

  """The dotted path read on the entity; defaults to 'name'."""
  source: String
}

"""
Which fields of which entity the index holds, and how much each of them counts.
"""
type SearchIndexDefinition {
  id: ID!

  """The entity key being indexed, in the platform's own vocabulary."""
  entity: String!

  """Human readable name, used wherever searchable entities are listed."""
  label: String!

  """
  The registered engine provider key; null means the built-in database provider.
  """
  engineKey: String
  fields: [SearchIndexField!]!

  """The weight applied to a field that declares none."""
  defaultWeight: Decimal!

  """Template producing the document title."""
  titleTemplate: String

  """Template producing the document body."""
  bodyTemplate: String

  """
  Which declared fields are promoted into the document's filterable token list.
  """
  keywordFields: [String!]!

  """The column whose value is copied into the document's source timestamp."""
  sourceUpdatedAtField: String!

  """
  An inactive definition is neither indexed nor queried; its documents are retained.
  """
  isActive: Boolean!

  """
  A shipped definition: its weights may be edited, but it may not be deleted.
  """
  isSystem: Boolean!

  """
  Bumped whenever the fields or a template change, so stale documents are rebuilt.
  """
  version: Int!
  metadata: JSON
  createdAt: DateTime
  updatedAt: DateTime
}

"""One field as an operator may re-weight it."""
input SearchIndexFieldInput {
  name: String!
  kind: SearchFieldKind!
  weight: Decimal
  searchable: Boolean
  filterable: Boolean
  facetable: Boolean
  source: String
}

"""How an operator re-weights a definition."""
input SearchIndexDefinitionUpdateInput {
  label: String
  fields: [SearchIndexFieldInput!]
  titleTemplate: String
  bodyTemplate: String
  keywordFields: [String!]
  defaultWeight: Decimal
  sourceUpdatedAtField: String
  isActive: Boolean
}

"""What a rebuild covers."""
input SearchReindexInput {
  scope: SearchReindexScope!

  """Required for the entity scope."""
  entity: String

  """Required for the channel scope."""
  channelId: ID

  """Rebuild only these source ids."""
  ids: [ID!]

  """Rebuild only rows whose source moved after this moment."""
  since: DateTime
}

"""What one entity's rebuild did."""
type SearchIndexRun {
  entity: String!

  """How many documents were written."""
  indexed: Int!

  """How many source rows could not be read and were skipped."""
  skipped: Int!

  """How many documents were removed because their source row is gone."""
  removed: Int!

  """How many batches the sweep took."""
  batches: Int!
}

"""The outcome of a rebuild."""
type SearchReindexPayload {
  """The entity that was rebuilt, when one was named."""
  entity: String

  """Whether the platform accepted the work."""
  queued: Boolean!

  """How many source rows the scope covers, counted before the sweep."""
  estimatedCount: Int!

  """What each entity's run did."""
  runs: [SearchIndexRun!]!
  userErrors: [UserError!]!
}

"""The outcome of a change to an index definition."""
type SearchIndexDefinitionPayload {
  searchIndexDefinition: SearchIndexDefinition
  userErrors: [UserError!]!
}

"""The outcome of removing an index definition."""
type DeleteSearchIndexDefinitionPayload {
  id: ID
  userErrors: [UserError!]!
}

"""The outcome of dropping an index."""
type DropSearchIndexPayload {
  """How many documents were dropped."""
  deletedCount: Int!
  userErrors: [UserError!]!
}

"""A page of index definitions."""
type SearchIndexDefinitionConnection {
  nodes: [SearchIndexDefinition!]!
  edges: [SearchIndexDefinitionEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One index definition in a page, with the cursor that addresses it."""
type SearchIndexDefinitionEdge {
  node: SearchIndexDefinition!
  cursor: String!
}

"""A page of index statuses."""
type SearchIndexStatusConnection {
  nodes: [SearchIndexStatus!]!
  edges: [SearchIndexStatusEdge!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

"""One index status in a page, with the cursor that addresses it."""
type SearchIndexStatusEdge {
  node: SearchIndexStatus!
  cursor: String!
}