/**
 * Interface for ActivePieces Integration Configuration
 */
export interface IActivepiecesConfig {
	/**
	 * API key for authenticating Activepieces API calls (Platform/Enterprise)
	 */
	readonly apiKey?: string;
}
