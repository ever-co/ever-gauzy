export enum QueueStatus {
    WAITING = 'waiting',
    PENDING = 'pending',
    RUNNING = 'running',
    SUCCEEDED = 'succeeded',
    FAILED = 'failed',
}

export enum StatusBadgeType {
    SUCCESS = 'success',
    WARNING = 'warning',
    DANGER = 'danger',
    INFO = 'info',
    BASIC = 'basic',
}
