# Changelog for @gauzy/plugin-docs-ui

## [Unreleased]

## 0.1.0

- Initial M1 release: Documents hub shell (tree + browse table + detail panel),
  plugin definition with `MENU.DOCUMENTS` nav entry, `DOCS` i18n namespace,
  filter bar with preset chips and live facet counts, multi-file upload with
  classification dialog and 5 s processing poll, review queue, bulk bar, and a
  read-only PAGE viewer placeholder (full editor lands with the TipTap wave).
