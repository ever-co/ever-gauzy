# @gauzy/plugin-integration-plane-ui

This library was generated with [Nx](https://nx.dev).

## Building

Run `yarn nx build plugin-integration-plane-ui` to build the library.

## Publishing

After building your library with `yarn nx build plugin-integration-plane-ui`, go to the dist folder `dist/packages/plugins/integration-plane-ui` and run `npm publish`.

## Installation

Install the Integration Plane UI Plugin using your preferred package manager:

```bash
npm install @gauzy/plugin-integration-plane-ui
# or
yarn add @gauzy/plugin-integration-plane-ui
```
