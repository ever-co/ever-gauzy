# @gauzy/plugin-integration-sim-ui

This library was generated with [Nx](https://nx.dev).

## Building

Run `yarn nx build plugin-integration-sim-ui` to build the library.

## Publishing

After building your library with `yarn nx build plugin-integration-sim-ui`, go to the dist folder `dist/packages/plugins/integration-sim-ui` and run `npm publish`.

## Installation

Install the Integration SIM UI Plugin using your preferred package manager:

```bash
npm install @gauzy/plugin-integration-sim-ui
# or
yarn add @gauzy/plugin-integration-sim-ui
```
