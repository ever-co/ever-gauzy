/**
 * Create StockTransferLine request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { StockTransferLineDTO } from './stock-transfer-line.dto';

/**
 * Create StockTransferLine request DTO validation.
 */
export class CreateStockTransferLineDTO extends StockTransferLineDTO {}
