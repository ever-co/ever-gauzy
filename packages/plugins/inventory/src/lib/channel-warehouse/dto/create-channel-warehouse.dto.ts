/**
 * Create ChannelWarehouse request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { ChannelWarehouseDTO } from './channel-warehouse.dto';

/**
 * Create ChannelWarehouse request DTO validation.
 */
export class CreateChannelWarehouseDTO extends ChannelWarehouseDTO {}
