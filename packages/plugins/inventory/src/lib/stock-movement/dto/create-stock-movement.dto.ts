/**
 * Create StockMovement request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { StockMovementDTO } from './stock-movement.dto';

/**
 * Create StockMovement request DTO validation.
 */
export class CreateStockMovementDTO extends StockMovementDTO {}
