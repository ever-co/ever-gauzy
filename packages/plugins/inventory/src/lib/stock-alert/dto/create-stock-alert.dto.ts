/**
 * Create StockAlert request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { StockAlertDTO } from './stock-alert.dto';

/**
 * Create StockAlert request DTO validation.
 */
export class CreateStockAlertDTO extends StockAlertDTO {}
