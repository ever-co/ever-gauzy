/**
 * Create StockCountLine request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { StockCountLineDTO } from './stock-count-line.dto';

/**
 * Create StockCountLine request DTO validation.
 */
export class CreateStockCountLineDTO extends StockCountLineDTO {}
