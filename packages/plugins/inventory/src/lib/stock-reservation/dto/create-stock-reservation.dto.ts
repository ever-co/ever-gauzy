/**
 * Create StockReservation request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { StockReservationDTO } from './stock-reservation.dto';

/**
 * Create StockReservation request DTO validation.
 */
export class CreateStockReservationDTO extends StockReservationDTO {}
