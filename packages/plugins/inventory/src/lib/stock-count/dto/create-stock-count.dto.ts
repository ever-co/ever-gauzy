/**
 * Create StockCount request DTO validation.
 *
 * The columns a caller must supply when the row is created.
 */
import { StockCountDTO } from './stock-count.dto';

/**
 * Create StockCount request DTO validation.
 */
export class CreateStockCountDTO extends StockCountDTO {}
