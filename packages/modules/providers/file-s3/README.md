# file-s3

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build file-s3` to build the library.

## Running unit tests

Run `nx test file-s3` to execute the unit tests via [Jest](https://jestjs.io).
