# Project-level npm configuration
# This overrides any user-level ~/.npmrc registry settings
# to ensure consistent dependency resolution across all developers.
registry=https://registry.yarnpkg.com/
