{
	"name": "@gauzy/mcp-auth",
	"version": "0.1.0",
	"description": "MCP Authorization Server for Ever Gauzy Platform used as Identity Server",
	"author": {
		"name": "Ever Co. LTD",
		"email": "ever@ever.co",
		"url": "https://ever.co"
	},
	"homepage": "https://gauzy.co",
	"repository": {
		"type": "git",
		"url": "https://github.com/ever-co/ever-gauzy.git"
	},
	"bugs": {
		"url": "https://github.com/ever-co/ever-gauzy/issues"
	},
	"license": "AGPL-3.0",
	"private": true,
	"type": "commonjs",
	"scripts": {
		"build": "npx rimraf build && npx tsc --project tsconfig.json",
		"build:prod": "yarn nx build mcp-auth --configuration=production",
		"build:dev": "yarn nx build mcp-auth --configuration=development",
		"start": "yarn nx serve mcp-auth",
		"test": "node build/main.js --test"
	},
	"dependencies": {
		"@gauzy/auth": "^0.1.0",
		"@gauzy/core": "^0.1.0",
		"@gauzy/contracts": "^0.1.0"
	},
	"devDependencies": {
		"@types/node": "^24.10.0",
		"rimraf": "^3.0.2",
		"typescript": "^5.9.3"
	}
}
