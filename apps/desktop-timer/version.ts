/**
 * @description
 * Current version of the Gauzy Desktop Timer.
 *
 * @example
 * ```
 * import { version } from './version';
 *
 * console.log('Gauzy Desktop Timer version:', version);
 * ```
 *
 * @since 0.1.0
 */
export const version: string = require('./package.json').version;
