/**
 * @description
 * Current version of the i4net Server.
 *
 * @example
 * ```
 * import { version } from './version';
 *
 * console.log('i4net Server Version:', version);
 * ```
 *
 * @since 0.1.0
 */
export const version: string = require('./package.json').version;
