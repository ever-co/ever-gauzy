/**
 * @description
 * Current version of the i4net API.
 *
 * @example
 * ```
 * import { version } from './version';
 *
 * console.log('i4net API version:', version);
 * ```
 *
 * @since 0.1.0
 */
export const version: string = require('./package.json').version;
